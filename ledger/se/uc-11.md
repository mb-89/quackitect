---
id: se.uc-11
kind: use_case
statement: The vendored vehicle runs standalone, with the same functions v2 has.
provenance:
  migrated_by: se.set.migrate v1-import
  iteration: bootstrap-b3
  ai_involvement: engine-migrated
  adjudicated_by: owner
v1_statement: Vendor the engine into a vehicle's .quack/ and run every quack command through the vehicle, resolving guidance, templates, and the report shell along a vehicle-to-engine chain.
passes_when: A vehicle with the engine vendored into it runs every SE function with no reference back to the source repo.
---

## Passes when

A vehicle with the engine vendored into it runs every SE function with no reference back to the source repo.

## v1 use case (migrated)

## note (not load-bearing)
i0003 use-case. The overlay resolver. Most-specific (vehicle) wins. Un-overridden resources inherit from the engine.
