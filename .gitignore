# machine-local — never committed
.se/
*.log

# node
node_modules/

# editors / OS
.vscode/
.obsidian/workspace.json
Thumbs.db
.DS_Store

# worktrees (later milestone)
.worktrees/

# placed by the extension into the OPENED folder from product/_cage/ — never committed
product/.mcp.json
product/.claude/settings.json
product/.claude/output-styles/
product/.copilot/
product/.github/
product/.claude/output-styles/voice.md
# the temporary product -> project junction, alive only until the engine reloads
/product
