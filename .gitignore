# machine-local — never committed
.se/
*.log

# node
node_modules/

# editors / OS
.vscode/
.obsidian/workspace.json
Thumbs.db
.DS_Store

# worktrees (later milestone)
.worktrees/

# placed by the extension into the OPENED folder from project/_cage/ — never committed
project/.mcp.json
project/.claude/settings.json
project/.claude/output-styles/
project/.copilot/
project/.github/copilot-instructions.md
