---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: reviewer count untested
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-messiaen
claimed_at: "2026-09-05T16:17:50Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - f14c34d0809cb8fec874d2af2683342d48a81d22
---

## detail

Named in the detail of wk-9cf8a57b51, and older than the change that token was about.

StaffingOf in src/engine/staffing.go raises ReviewersHere in two places: once in the loop over AgentsPresent, on roles[a.Actor] being RoleReviewer, and once in the loop over ActorsThatPulled, on the same reading. Every staffing test asserts ReviewersHere is 0, so neither increment has a test that reaches it.

A count nothing reaches is a count nothing holds to its word. The guard spawns reviewers off this number, so a reviewer that is here and not counted is a spawn nobody needed, and one counted that is not here is a verdict queue that never gets a hand.

The measurement is the same one wk-9cf8a57b51 used: a coverage profile over the staffing tests reports count 0 for both increments.

## proposed action

Give the staffing tests a reviewer: one helper registered with NoteAgent and ArrivedAs RoleReviewer, and one that pulled as a reviewer without being registered. Assert ReviewersHere counts each, and stops counting it when it claims a stop.

## done when

- a test asserts ReviewersHere is not zero for a registered reviewer that is here: se test --propose TestAReviewerIsAHandWhetherOrNotItIsRegistered
- a test asserts ReviewersHere is not zero for a reviewer that pulled and was never registered: the same test, at its second assertion
- a coverage profile over the staffing tests counts both ReviewersHere increments in staffing.go: .se/tests/src_engine.cover with -test.coverprofile over those tests

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one new test file of 69 lines, and staffing.go untouched | git status --porcelain |
| [x] | every done-when line is decidable, and names the command where one decides it | none of the three named a command. Each names one now | the done when section |
| [x] | the basics it stands on exist, or are minted first | aTreeWithTheProcesses, NoteAgent, NoteTheNameItPullsWith and ArrivedAs are already in the suite | src/engine/staffinghands_test.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | rule 12: the check was driven red twice, once for each increment | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | the profile said count 0 at staffing.go 85 and 134. With 85 taken out the test failed at line 42, with 134 out at line 48 | .se/tests/src_engine.cover |
| [x] | the same test was seen green after the change, and named | TestAReviewerIsAHandWhetherOrNotItIsRegistered, ok true, and the profile counts 1 at both lines | se test --on this token |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | one file added, src/engine/reviewersarehands_test.go | the frontmatter |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | none. staffing.go is right and was only untested, and it is back byte for byte | git diff src/engine/staffing.go |

