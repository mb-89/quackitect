---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: clear the linter wall
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
# true when this waits for a person rather than an agent
needs_human: true
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - c108b3927e42c7d62bb74506a9c38f8101527656
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - bf36ee9840b2e52de19850986976fdd0c6e2b562
---

## detail

The problems panel shows a wall of red across the tree. Every entry is a linter complaint rather than a compile error, so a real error cannot be told from the noise while the wall stands.

The change is form only. No behaviour moves and no content is rewritten. Where a rule needs a name, a doc comment or a directive, that addition is made and nothing else.

## proposed action

Run each linter the panel runs, from the root. Group the findings by rule. Fix each group with the smallest edit that clears it. Re-run and compare the counts.

## done when

- golangci-lint run over the tree from the root reports 0 issues
- the markdown linter over doc reports 0 issues
- the engine still builds and its tests pass, showing no behaviour moved

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

## note

Measured on a cloud box, and it cannot be decided here.

golangci-lint is installed and refuses to run. It was built against Go 1.25 and this module targets 1.27, so it stops at the config: the Go language version used to build golangci-lint is lower than the targeted Go version.

There is no markdown linter on this box at all, under either name the panel uses.

And the wall itself is a desk thing. The problems panel is the editor's, and a cloud box has no editor, so what a person sees there cannot be read from here.

One more reason to hold it. The change is form across the whole tree, and three agents are writing in this one working tree right now. A tree-wide reformat here would collide with every one of them and bury their work in a diff nobody can read.

What a desk needs: the panel, a golangci-lint built for this toolchain, and a quiet tree.

