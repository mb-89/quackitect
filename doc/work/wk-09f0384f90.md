---
id: wk-09f0384f90
seq: 71
type: work
title: a command is statements
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "Both halves are done: the newline is in the separator list at src/engine/search.go:116, withoutHereDocs strips here-doc bodies before the split, and TestAHereDocBodyIsNotCommands drives both."
aborted_from: backlogged
minted_by: reviewer4
---

## detail

search.go splits a shell command on the pipe, on && and on the semicolon, but not on the newline. A recursive search on the second line of a multi-line command passes the rule. The same split reads a here-doc body as commands, so writing a script that contains a search is refused. Add the newline to the separator list. Then decide the here-doc: skip the body between << and its terminator, or accept the false positive and say why in the comment. Check: a two-line command whose second line is a recursive search is refused, and a here-doc that writes one is not. Found on wk-908639bd2b.
