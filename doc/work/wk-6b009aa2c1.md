---
id: wk-6b009aa2c1
seq: 1000182
type: work
title: scope against answer
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-17
---

## detail

Open point from the acceptance of wk-42adef4818 at round 4, one clause on its criterion 6. The criterion says the check reads every criterion command for util paths git does not track, then asserts it is red naming util/checks/count-observations.py. That file is named only in criterion 3's sentence and in the detail, never in a command, so the two halves describe different checks. Fix: say the check reads a criterion's command and the commands its sentence names. Eight other untracked files under util/checks are named in commands on other notes, so criterion 6 cannot go green until they are tracked too.
