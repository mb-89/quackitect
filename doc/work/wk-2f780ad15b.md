---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: retro reports survive drains
# where the token stands. The process owns these values.
status: closed
author: worker-bell
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 73a48ed4d783d3ff352bc829410743fa3f974558
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - c6dbe4516d017270930be33f7edcde4f26244607
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

A retro writes its report into .se/retro/<stamp>/, and the next retro drains what it collected. So the reports it needs are the reports it can lose. Two of the retro's own rules read them: score the last retro's improvements, and compare this session's shape against earlier ones. A second note asks the retro to publish its period's counts into a file that survives the drain, which is the same file. The owner's ruling: it is too early for a proper retro system. The reports stay out of git and per machine, and the drain never touches them. Consumes wk-2ac36d8fa2 and wk-88f4fcc517.

## done when

- a retro's report and its counts land where a drain does not reach, and the folder is out of git. Decide it with se retro, then git status --short answers nothing about them
- a second retro reads the first one's report and counts, and drains everything else: go test -C src/engine -run TestARetroKeepsWhatTheNextOneReads

## evidence: step 1. ask

<!-- restored after submitting: the engine's submit wrote the payload's map over these tables. Same text, put back by hand. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | — |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Line two names its command: go test -C src/engine -run TestARetroKeepsWhatTheNextOneReads, run through se_test, green. Line one is decided by git check-ignore -v .se/reports/20260902-184643.md, which answers .gitignore:2:.se/, and by git status --short, which now says nothing about a report or its counts. I did not run se retro on this tree: the live engine is the pre-change binary, so it would exercise the old code, and a real drain mid-session would move other agents' kept output and undo journals out from under them. The built binary is driven instead by TestARetroCollectsAndDrains and TestASecondRetroTakesNothingTwice, both green. |  |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [x] | one test was written first and seen red for the reason expected | TestARetroKeepsWhatTheNextOneReads was written first and went red twice. On the build: undefined: ReportsDir, undefined: Counts, undefined: Period, first.Counts undefined (type Collected has no field or method Counts). Then, with the declarations added and no behaviour behind them, the run printed Counts and Report empty and Earlier empty. |  |
| [x] | the same test was seen green after the change, and named | src/engine/TestARetroKeepsWhatTheNextOneReads, ok, 0.18s, through se_test. Fourteen other retro tests were named outright in the same run and are green, among them TestARetroDrainsTheKeptOutputAndTheUndoJournal, TestASecondRetroTakesNothingTwice, TestARetroStaysInsideTheWorkFolder and TestTheBatteryIsWholeAfterARetro. The whole battery stays owed. The not done chapter says why. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

## evidence: a retro's report and its counts land where a drain does not reach, and the folder is out of git

The engine writes .se/reports/<stamp>.counts.json itself and names .se/reports/<stamp>.md as where the report goes. No drain in retro.go names .se/reports: the drains are .se/log, .se/scratchpad, .se/out and .se/undo. git check-ignore -v .se/reports/20260902-184643.md answers .gitignore:2:.se/, and git status --short says nothing about a report or its counts. doc/retro, which the old rule 17 created and which showed as untracked, is gone, its one report moved to .se/reports so this machine keeps it.

## evidence: a second retro reads the first one's report and counts, and drains everything else

go test -C src/engine -run TestARetroKeepsWhatTheNextOneReads, through se_test: ok, 0.18s. It runs two retros. The second is handed the first one's period under earlier, with the counts read back off disk and the report path. It drains a log, a scratchpad file, a kept output and an undo journal. Both of the first one's files are still on disk afterwards.

## evidence: not done

The whole battery could not run: no sh on this machine. It was already owed, triggered by another actor's change under util/checks, and is not this token's.

## evidence: test

src/engine/TestARetroKeepsWhatTheNextOneReads green, plus fourteen other retro tests named outright and green in the same run. Red first: the build (undefined: ReportsDir, undefined: Counts, undefined: Period), then behavioural (the retro says nothing about its counts or its report).

