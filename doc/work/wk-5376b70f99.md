---
id: wk-5376b70f99
seq: 1000125
type: work
title: the write goes unnamed
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-7
---

## detail

For every criterion over a dispatcher, write two assertions. One says which token came back, and one says what the record now holds for every token that did not. Case in point: wk-386169824b criterion 5 requires the draft put down and the implementation handed out. The only put-down in src/engine/pull.go, the branch after aheadOf, writes put.Status, put.Holder = ImpOpen, "" whatever half the held token was in. The state-aware answer, putBack in src/engine/gate.go, is called from nowhere in pull.go. Before agreeing a criterion that widens which states a loop may see, put every constant the loop body writes into the criterion sentence.
