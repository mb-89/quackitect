---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: handover refusal has test
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
# what has to be true before this is worth reading again
ready_when: the context change of wk-30821724fc is committed to v4, so a test naming handOver with a context builds
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - f3cd241108247df199b7fd3202a1b9d50bab5b28
---

## detail

From the verdict on wk-30821724fc, spawns take a context.

handOver in src/engine/swap.go gained a refusal at its top: `if err := ctx.Err(); err != nil { return fmt.Errorf("this engine is ending, so it starts no successor: %w", err) }`. Nothing can reach it. The only caller is main.go's swap arm, and the context it hands over is the one main makes at the top of main(); the only thing that cancels it is `defer cancel()`, which runs after main's loop has already returned. release() stops the indexer and closes the hook listener, it does not cancel. So the branch cannot fire in production, and no test drives it: the two tests the token added cover realGit and ProbeTools, not handOver.

The dead weight is an error path a reader has to reason about and a behaviour the suite cannot see. The token's approach says handOver "starts nothing once it is cancelled, and says so" — this is not an argument with that approach, it is that the sentence is currently unverifiable.

Either give it a test that calls handOver with a cancelled context and sees it refuse without starting a process, or take the guard out and say in the comment that main never cancels before the swap.

## done when

- a test names handOver on a cancelled context and asserts it refuses and starts no process, decided by: go test -C src/engine -run 'HandOver' ./... answers ok — or the guard is gone and swap.go's comment says why
- sh util/checks/battery.sh reports no new failure against the run before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

## note

The test is written and green in this tree, and it cannot be committed yet.

The guard it drives lives in the context change of wk-30821724fc, which passed its verdict and closed. That change is not in git: it is nineteen files in this box's working tree, and the verdict commit carried only the checklist.

So a commit of this test alone does not build. The index still has handOver taking two arguments, and the test calls it with three.

What was measured. Twelve of that token's files are byte-identical to what its author wrote, by the hash the undo journal records. The other seven have been written since by other hands, and one of them now imports a package that is itself untracked.

The test file is src/engine/handover_test.go, and it stands in the tree ready for whoever commits that change.

