---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: old checklists block verdicts
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-borromini
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 7311b5d0cc6ca7a9a134d00aa8fdcffc83693014
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 7a38af064954b739d1e1a78d11552b02ffb0914a
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Seen twice while ruling on wk-6a0b7f2d38 and wk-36b8ea59dc. The standard process gained two ask criteria, what is gained and what breaks. A token minted before that carries a step 1 table without those rows. The gate refuses the verdict submission and names the missing row, so the reviewer has to write rows into the author's own step. The word cap makes that worse. Both notes were already at or over the two hundred word bound for step 1, so the rows only went in after the author's evidence was cut down to fit. A reviewer should not be rewriting an answered step to close a verdict. Let the gate accept a step whose rows match the process as it stood when the step was answered, or let the engine add a missing row itself and leave it unticked without blocking the later step. A test that answers step 1 under an old process, adds a criterion, and then submits the verdict catches the class.

## done when

- a verdict submits on a token whose step 1 table predates a new ask criterion, decided by a test in src/engine that adds a criterion between the steps
- no reviewer has to write or shorten a row in an earlier step to submit, decided by: read what the gate refuses

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A reviewer stops writing rows into the author's step | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | Every criterion added blocks the verdict of older tokens | the detail |
| [x] | the ask is small enough to review whole, or it is split first | — |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Both are, and one test decides them | the test |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [x] | one test was written first and seen red for the reason expected | It said step one has no row for the new criterion | the run below |
| [x] | the same test was seen green after the change, and named | TestARowAddedToAnEarlierStepDoesNotBlock | commit f107421e |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

THE GUARD MOVED RATHER THAN CAME OFF. The second half of the test adds a criterion to the step being submitted and reads the refusal, which still names it.

THE CLONE HERE CANNOT BUILD PACKAGE MAIN, on another hand's write, so the red and the green were both seen at the branch tip.

