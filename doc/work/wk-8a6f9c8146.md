---
id: wk-8a6f9c8146
seq: 1000019
type: work
title: here holds the queue
status: spec_open
assignee: work-a
scope: single-step
traced: true
depends_on:
  - wk-66a28ca311
rounds: 4
minted_by: person
submitted_by: work-a
spec_seen: 75a6f0c669ee855ef78dd4970f0f91c9159aecd8d005d3df37561a4f1f610380
---

## detail

util/views/work.base declares here as imp_open or imp_in_work, so here misses the spec_open and spec_in_work tokens a pull also hands out. imp_open has no group of its own because here claims it first. The owner picked: a filter group is a query with a q slash, and a row appears under every matching query and its bucket. The set of states is derived from the engine, not typed into the view. The build was handed to wk-66a28ca311, which is now imp_done and asks for none of it. Give the three pieces a live owner, point depends_on at it, and see wk-e5ef245cc5.

## done when

- The here query takes its states from the engine's one declaration, HandedOut and HeldBy in src/engine/arrival.go, and the picking loops in src/engine/pull.go and countQueue in src/engine/nudge.go read it with no state name written by hand.
  `rg -q func.TestThePickingLoopsAskWhatAPullHandsOut src/engine && go test -C src/engine -count=1 -run TestThePickingLoopsAskWhatAPullHandsOut$ .`
- The here query covers exactly the states that declaration answers for a worker, and the check fails by naming the state the query misses.
  `rg -q func.TestHereIsWhatAPullWouldHand src/engine && go test -C src/engine -count=1 -run TestHereIsWhatAPullWouldHand$ .`
- The check holds the here count against the whole queue, three imp open and six spec open, over a fixture of its own.
  `rg -q func.TestHereCountsTheWholeQueue src/engine && go test -C src/engine -count=1 -run TestHereCountsTheWholeQueue$ .`
- For every token whose detail hands a piece of work to another token, that token is not ended and its done when carries the piece, and the check names both ids, the piece, and which condition failed.
  `node util/checks/handed-over.mjs .`
- The whole battery is green afterwards.
  `sh util/checks/battery.sh`
- Every test above was watched failing on its own assertion, with the change absent, before passing, and the evidence names the test and what it said.
