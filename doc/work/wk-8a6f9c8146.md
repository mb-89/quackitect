---
id: wk-8a6f9c8146
seq: 1000019
type: work
title: here holds the queue
status: spec_submitted
assignee: main
scope: single-step
traced: true
minted_by: person
---

## detail

TWO BUGS IN THE WORK EDITOR'S GROUPS, AND THEY SHARE ONE CAUSE. THE OWNER'S WORDS: the here filter in the editor does not show the spec open stuff, but it needs to. Spec open is also here, and it is also open, and this is part of your queue. When you pull for a new token, you also get the spec open stuff. So right now not three items are here, but nine, the three implementation open, which I actually do not see as a group, and the six spec open. WHAT THE VIEW SAYS TODAY. util/views/work.base declares here as status is imp_open or imp_in_work. spec_open and spec_in_work each have a group of their own further down. THE FIRST BUG IS PLAIN. A pull hands a spec_open token exactly as it hands an imp_open one, so it is the same queue and here excludes half of it. THE SECOND IS NOT A MISSING GROUP. imp_open has no group of its own because here already claims it, and the file's own comment says the declared groups take their rows first, in order, so the page is a partition and no row appears twice. Whatever here takes, an imp_open group below it never sees. So the owner sees three rows under here and no imp_open group anywhere, and both symptoms come from here doing two jobs at once, naming a state and summarising a queue. THE SHAPE THAT MAY WORK, and it wants agreeing because it changes the view engine rather than the view. Every state gets its own group, imp_open included, and here becomes the sum of what a pull would hand this actor: imp_open, imp_in_work, spec_open, spec_in_work. That needs one of two things. Either the partition rule gives way so a row can sit in a summary group and in its state group both, which is a change to how a view is drawn, or here stops being a group and becomes a count on the bar, which is a change to what here means. THE OWNER PICKS BETWEEN THOSE TWO. AND THE SET IS DERIVED RATHER THAN TYPED. Which states a pull hands out is the engine's answer, not a list in a view file, so whatever is built asks the engine rather than repeating four names in work.base, because a fifth state added later would leave the view quietly wrong.

THE OWNER PICKED, AND IT IS THE FIRST OF THE TWO. A group declared by a filter
is a query, drawn with a q slash before its name, and a row appears under every
query that matches it and under its bucket as well. The partition rule gives
way. That work is written into wk-66a28ca311, the v3 editor port, at the owner's
direction, so this token is the measurement and the two bugs and that one is the
build. Whoever takes this one reads that one first.

## done when

- The here query answers what a pull would hand this actor, and the set is the engine's rather than a list of state names in the view file: the check asks the engine which states it hands out and requires the query to cover exactly those, failing by name on one it misses
  `rg -q func.TestHereIsWhatAPullWouldHand src/engine && go test -C src/engine -count=1 -run TestHereIsWhatAPullWouldHand$ .`
- A row appears under every query that matches it and under its bucket as well, so imp_open has a group of its own and here still counts it. The check draws a view over a fixture holding one row in a bucket and requires it in both
  `rg -q func.TestARowIsInEveryQueryThatMatchesIt src/engine && go test -C src/engine -count=1 -run TestARowIsInEveryQueryThatMatchesIt$ .`
- Every state has a group of its own, derived from the engine's list of states rather than typed into the view, so a twelfth state added later is drawn rather than silently absent
  `rg -q func.TestEveryStateHasAGroup src/engine && go test -C src/engine -count=1 -run TestEveryStateHasAGroup$ .`
- A query draws with q slash before its name and a bucket draws with its own name, so a reader can tell a question from a place without a legend, and the check drives the page rather than reading the source
  `node util/checks/queries.mjs .`
- Counted, and this is the number the owner read off the panel: here answered three and the queue held nine, three implementation open and six spec open. The check asserts the two counts agree over a fixture rather than over today's tree
  `rg -q func.TestHereCountsTheWholeQueue src/engine && go test -C src/engine -count=1 -run TestHereCountsTheWholeQueue$ .`
- The whole battery is green afterwards
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. THE EVIDENCE NAMES THE TEST AND WHAT IT SAID rather than a line number

