---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: engine splits at seams
# where the token stands. The process owns these values.
status: done
# who did the work step, so the verdict is never theirs
author: worker-messiaen
claimed_by: aeaf7bd9/worker-messiaen
claimed_at: "2026-09-05T15:17:23Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 7221f18406198d8f70f4699ff19685bc098f02f0
  - dadd31ff0beefff3304f562bea69409f289ae5ad
  - c2682c671c7ab75306367de55d36104c0ec51b96
  - 28abf9f4ea054a7fe379cc144f576039b64b7cc0
  - 0a27af44a6240e841184fd54cb0f2b812950df37
  - cd0d59cef7d1ee9a4cf6435b3b092f2bf97dff03
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - c3f682830ead2d135d8337866dad46db48ef55e8
  - 81515387bff855206ad43172f49f1df2b2dcbb33
  - 591ac6a6e99383291bf5989a5e630d1a5251136f
  - 9bb947e4efefb96292633d887065c2c1b26d7f73
  - b07c52d561f7303820b29d61ffeae11ca7aed4c5
---

## detail

src/engine is one flat package: 80 non-test files and 23193 lines, and 184 files and 38199 lines counted with tests. Every name in it shares one namespace, so writing a helper needs a tree-wide search first.

Three build failures came from collisions: firstLine, mustJSON, and fs against the io/fs import.

The guidance already gives the method. A folder whose files only call each other is a package under internal, named for what it holds. The call graph shows the seams. The test of a bad seam is an import cycle, so a cycle found is a seam drawn wrong.

The seams are measured rather than chosen.

## proposed action

Compute the call graph over src/engine and name the groups it shows, with a file count and a line count each. Move nothing until that is written down.

Then each named group moves to its own package under src/engine/internal, named for what it holds. Build and vet after every move.

## done when

- the call graph names the groups reaching nothing outside themselves, with a file and line count each: the approach on this token
- each named group is a package under src/engine/internal named for what it holds: ls src/engine/internal
- no import cycle is introduced, after every move: go build -C src/engine ./... and go vet -C src/engine ./...
- firstLine, mustJSON and fs are no longer one namespace problem: se find --regex "func (firstLine|mustJSON)" answers one package each
- the engine tests pass after the split: se test --on this token

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | written before the first move, and it still holds | the approach section |
| [x] | every done-when line is decidable, and names the command where one decides it | four of the five named no command, so each names one now | the done when section |
| [x] | the change is small enough to review whole, or it is split first | split by group, one move at a time. This pass is expr | ten moved, log left |
| [x] | the basics it stands on exist, or are minted first | src/engine/internal exists and already carries four of the groups | ls src/engine/internal |

Still flat: log. Under internal on 62c62a65: alive, console, frontmatter, quiet, replaced, runme, treelock, version, voice, yaml, and expr with this change.

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | red first: expr.go out, and the build answered 56 undefined names, 17 Row, 17 Expr, 9 vs, 6 Parse, 3 vl, 3 contains, 1 vb | go build -C src/engine -gcflags=-e ./... |
| [x] | the change follows the approach on the token, or the token says why it departed | one group moved: expr, 1 file, 483 lines. log stays, its callers include claim.go and claimsync.go, held by another hand | wk-3fb658ea61 |
| [x] | se test --on this token answered ok, and what it ran is named | ok true, 3.46 seconds: the go tests reaching view, tally and nest, then 35 util/checks | ./RUNME.sh test --on wk-40abb881a7 |
| [x] | the note says what changed and why, for a reader who was not here | for a reader with neither file open | the note chapter |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | contains left with expr, so two callers read slices.Contains | wk-3fb658ea61 |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | read, and the candidate list cut to three | this table |
| [x] | every hunk of git diff began..ended was read, and any not read is named | 50 commits, mostly other hands. This token's is 69dc3aed: twelve files, all read. The other 49 were not | git show 69dc3aed |
| [x] | every criterion's command was run again, and what it said is named | 1 one group stale, wk-f27d605348. 2 eleven directories. 3 both answer nothing. 4 unchanged since began, wk-41bb3edaa3. 5 ten go tests, ok true | se test |
| [x] | every hunk improves the product, or a finding names the one that does not | 486 lines left package main, no cycle, build and vet clean. Only the contains left behind does not earn its place | wk-3954b260e5 |
| [x] | every finding is a trivial token naming this one, and their ids are here | wk-41bb3edaa3, wk-f27d605348, wk-3954b260e5, worst first. Also seen: the package has no test | se work |

## note

expr.go moved out of package main to src/engine/internal/expr, 483 lines, the eleventh group the call graph named.

vs, vn, vb and vl became expr.Str, Num, Bool and List, and absent became expr.Absent. view.go built nodes out of the tree's unexported fields, so the package answers expr.Op and keeps its fields to itself. contains was a list helper that happened to live in expr.go, and its two callers outside now read slices.Contains.

The lexer's token type went with it, so package main no longer carries a lowercase token beside Token.

The compiler found the callers, not a search. The build runs with -gcflags=-e, and each undefined name is qualified where it was reported.

log is the group still flat, and it is wk-3fb658ea61.

## approach

The call graph is computed with Go's own parser. Each identifier resolves against package scope, and shadowing is tracked. So a local named like a package symbol is not a use, and neither is a field name. Platform twins are read once.

Eleven groups reach nothing outside themselves. Files, then lines:

- version 1, 5
- alive 1, 26
- console 1, 28
- quiet 1, 53
- wasbin 1, 81
- voice 1, 156
- yaml 1, 233
- runme 1, 248
- log 1, 248
- frontmatter 1, 300
- expr 1, 483

The other 74 files are one mutual cycle of 24389 lines. Each of the eleven becomes a package under src/engine/internal, named for what it holds. Moves go cheapest first, counted by callers, with go build and go vet after each.

The knot is named rather than cut. roots.go reaches the rest through one field, Roots.snap, which holds a slice of Token. Cutting that is its own token.

