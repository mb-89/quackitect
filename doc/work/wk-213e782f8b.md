---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: refusal names absent flag
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-varese
claimed_at: "2026-09-05T14:12:58Z"
---

## detail

A finding on wk-b75b6ebff3, notes become tracked tokens.

src/engine/notesgohome.go:63 prints the one runnable instruction in the refusal:

    - it is useless, so drop it: se work --close <id> --as dropped --why "..."

Neither flag exists. src/engine/work.go:19-53 declares the whole flag set for `se work`, and it has no --close and no --as. Run at this tree:

    .bin/se work --close wk-0000000000 --as dropped --why "..."
    flag provided but not defined: -close

The command that actually drops a note is `se work --abort <id> --why "..."`, which src/engine/abort.go:65 closes as Dropped, so Ended() goes true and NotesInHand stops counting it.

THE DAMAGE. The whole guard is a handing-over: it holds the work and tells the agent how to get out. Its one copyable line is a parse error, so the agent held by it spends a turn on a command that cannot run and has to go and read work.go to find the real one.

THE CHECK. The refusal's commands are never read by anything. A test that pulls each `se ...` line out of the refusal and parses it against runWork's flag set catches this class, here and in the next refusal that names a flag somebody renamed.

## done when

- src/engine/notesgohome.go names se work --abort <id> --why "..." for dropping a note, and names a runnable command for the other two answers or says plainly that they go through the lane
- a Go test in src/engine parses every 'se work' line the refusal prints against the flag set runWork declares, and fails on an undefined flag
- the test is seen red against the wording as it stands now

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one string in TooManyNotes, and one test file |  |
| [x] | every done-when line is decidable, and names the command where one decides it | line 1 by `se find --regex "se work --abort" --path src/engine/notesgohome.go`, one hit. Lines 2 and 3 by `go test -run TestEverySeWorkCommandARefusalPrintsIsParsed`, red before the change and green after |  |
| [x] | the basics it stands on exist, or are minted first | TooManyNotes, NotesGoWithTheBox, aTreeWithTheProcesses, aHostTable and mintNote all existed. Nothing was minted first |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md read. The command comes from the refusal and the flag set from work.go, so neither half is a copy of the other |  |
| [x] | one test was written first and seen red for the reason expected | TestEverySeWorkCommandARefusalPrintsIsParsed, twice: "the refusal hands the agent se work --close, and se work declares no --close", and the same for --as |  |
| [x] | the same test was seen green after the change, and named | green with the four notes tests beside it. refusals-name-a-door ok, 8 refusals judged. gofmt and go vet clean. It ran on a copy of HEAD, because the tree here will not build: notesgohome_test.go:160 calls a runVerbInside nothing declares, which is another hand's |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | it is |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-42cc1c2f6e: a pull payload has no shell door |  |

