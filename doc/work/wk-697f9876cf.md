---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: long work takes context
# where the token stands. The process owns these values.
status: done
# who did the work step, so the verdict is never theirs
author: main
claimed_by: aeaf7bd9/reviewer-glass-two
claimed_at: "2026-09-05T18:30:47Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - fb785b54662f4b1275f3609a96172d9e7544e9de
  - 3d925ba21e4e78bed7e1bb7ed0f31bcbe46b05af
  - 5089c4a98a3967301dbdaccae3f0455e84b8de60
  - 07ad89a9349a9e8addffcfd9c3c5cc198b431ce5
  - 27cfa136410fd52107da06a3b531b0a292a24c98
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 13db6be4c91d172ca654d95ed38b0cae810a2411
  - 2efcc95810b4cabe3ca38a71e77c7827bc8ce9f6
  - 26ba25a6b26bc77c7334109f4e5725daa5a1df3b
  - 5da630ecc99ca35378622d85d3908574645e5df5
---

## detail

writing-go rule 11 says a goroutine has an owner and a context that stops it. Rule 12 says context.Context is the first parameter. The engine spawns processes, watches the filesystem, serves a socket and starts a detached battery, and it calls context.Background twice.

No test file under src/engine imports context at all. So rule 14 asking a test for t.Context has nothing to hand one to, and a sweep would write 110 unused variables.

The zero on that clause is not the tests. It is the code the tests call.

The symptom is on the record: two tests failed their own TempDir cleanup because a handle was still open. A context that ends with the test is what closes that handle.

## proposed action

Give the long-running things a context that stops them, first parameter, owner named. A test then hands them t.Context and what it started stops with it.

## done when

- the filesystem watcher, the socket server and the spawned processes each take a context as their first parameter
- each one stops when that context is cancelled, proved by a Go test that cancels and waits
- a test that starts one of them passes t.Context and leaves no handle open at cleanup
- src/engine has no call to context.Background outside main

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | The proposed action is the shape: a context first, owner named, and a test that hands t.Context. A reader can argue with first-parameter. | 1 rule |
| [x] | every done-when line is decidable, and names the command where one decides it | Lines one and four are read by se find over src/engine. Lines two and three are Go tests. All four were run. | 4 of 4 |
| [x] | the change is small enough to review whole, or it is split first | Most of it had landed under earlier hands. What was left is one signature, one caller, and the retro's context. | 3 sites |
| [x] | the basics it stands on exist, or are minted first | context.AfterFunc is what the indexer already used to close this listener. This moves it inside. | AfterFunc |

## evidence: step 2, where this stands

The queue handed this work step to worker-ives after reviewer-ligeti stopped. It is put down unfinished, and this is what the look found.

The change is in the working tree and not in git. HEAD still has runHook(os.Args[2:]) in main.go. cancelends_test.go, contextkeepsthesocket_test.go, handover_test.go and enginewait_test.go are untracked, and about fifty files under src/engine are modified. None of it is this hand's writing, and other hands are still adding to it.

Three done-when lines look met in the tree. StartIndexer, serveHooks, handOver, ensureEngine, ProbeTools and realGit each take a context first, and cancel tests exist for five of them. context.Background is left in main.go and in handover_test.go.

serveModel still takes no context. Its listener is closed by context.AfterFunc in startIndexer, so the owner is right and the signature is not.

fb785b54 and 13db6be4, this token's first began and its ended, are not objects in this clone.

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | writing-go rules 11 and 12. The server names its owner in its signature and stops when that context ends. | writing-go |
| [x] | the change follows the approach on the token, or the token says why it departed | It follows it. serveModel and Retro each take a context first, and the test hands t.Context. | 2 signatures |
| [x] | se test --on this token answered ok, and what it ran is named | ok, 0 failed. It ran TestTheModelServerStopsWithItsContext, TestACancelledContextEndsAGitCall and TestARetroTidiesOnStart, then the whole delta. | 0 failed |
| [x] | the note says what changed and why, for a reader who was not here | The note is below. | note written |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | It is in the change. My own tidy verb had put context.Background back into retro.go an hour before, and Retro now takes one. | 1 fixed |

## evidence: step 3. verdict

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | Read and applied. Candidates were written wide, then cut to one. | 14 rules |
| [x] | every hunk of git diff began..ended was read, and any not read is named | Only 0f478978 and 2e4f5be4 are this token. Both read whole. The rest of the range is other hands, unread. | 2 commits |
| [x] | every criterion's command was run again, and what it said is named | se find leaves one context.Background, main.go:28. RUNME.sh test ran eight named tests, ok, 0 failed. Signatures read in source. | 8 ok |
| [x] | every hunk improves the product, or a finding names the one that does not | Yes. serveModel and Retro gain a visible owner. Call sites move to t.Context. The new test cancels and waits. | 1 finding |
| [x] | every finding is a trivial token naming this one, and their ids are here | wk-b008bfaa16 names serveHooks, which takes a context and is not stopped by one. Also seen, Run spawns on a timer. | wk-b008bfaa16 |

## note

Most of this landed under earlier hands. They threaded a context through the indexer, the hook server, the swap, the tool probe and the fed git. Three things were left.

serveModel took no context. Its listener was closed for it from startIndexer, so the owner was right and the signature said nothing. A reader could not tell what stopped it. It takes one first now, and closes the listener itself through context.AfterFunc. Accept blocks until a client arrives. So a cancel that only set a flag would be felt on the next connection, which on a quiet socket is never.

Retro took none either, and called context.Background. That was mine, put there an hour before by the tidy verb. It takes a context and hands it to the tidy.

Outside main, nothing in the engine calls context.Background now. Two cancel tests that built their own root context take t.Context instead.

## approach

Three owners, taken one at a time. The filesystem watcher, the socket server and the spawned processes each take a context as their first parameter and stop when it is cancelled.

Each one gets its cancel test first: start it, cancel, wait for it to be gone. That is the red the criterion needs, and it fails before the parameter exists.

Then the tests that start them hand over t.Context, and the handle that failed a TempDir cleanup is closed by the cancel rather than by luck.

main keeps its context.Background, because somebody has to make the first one.

