---
id: wk-4ea4afbcbd
seq: 1000186
type: work
title: A class ends narrowly
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-18
---

## detail

A token minted to end a class, scoped to the file the class was found in. The draft writes nowhere else into a criterion and lets the file the reviewer had open be the meaning of else. On wk-6868ca3074 criterion 1 names three helpers in src/engine/shapes_test.go, but rg -ln theShapes src/engine also returns src/engine/red_test.go, whose oneLineRows is a fourth. When a draft says a thing appears nowhere else, ask the tree for the set that else ranges over with one search for the name. Never let a criterion say this file or nowhere else without the set written beside it and the command that derives it. Related: wk-ce5d6865e4, whose abort reason names a home in doc/guidance that does not exist.
