---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: loop refusal overreaches
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-webern-two
claimed_at: "2026-09-05T21:17:15Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 27a9b07b6466dc819ec9c08d5aac866049818291
---

## detail

Found reviewing wk-2ef79848eb. src/engine/removal.go ALoopThatRemoves sets loop and deletes over the whole pipeline and never asks whether the removal is inside the loop. pipeline splits on ; && || | & and newlines, so any loop anywhere in the command plus any removal anywhere else is refused. Verified live in this tree: `for f in a; do echo $f; done; rm -f "C:/Users/mb/AppData/Local/Temp/claude/nope-xyz-not-here.txt"` was refused with A LOOP THAT DELETES IS REFUSED, though the rm is not in the loop and names a path outside the tree, which the rule says it does not touch. The damage is that the refusal says something untrue about the command it stops, and the remedy it offers, list what you mean and delete those files by name, is what the command already did, so there is no door. A removal should count only when it is inside the loop's body: between the do or brace that opens it and the done or brace that closes it.

## done when

- a loop followed by a separate named removal is not refused: go test -C src/engine -run TestALoopThatDeletesIsRefused
- a removal inside the loop body is still refused, for each of for, while, until and the case that ate live code: the same test
- a loop that removes nothing is still allowed: the same test

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A cleanup beside a loop runs. The refusal stops saying something untrue about the command it stops. | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | An agent tidying up before a loop is refused with no door, because the remedy offered is what the command already did. | the detail |
| [x] | the ask is small enough to review whole, or it is split first | One function and a helper beside it, plus two table cases. | git diff HEAD --stat |
| [x] | every done-when line is decidable, and names the command where one decides it | All three lines are cases in TestALoopThatDeletesIsRefused. | go test -run TestALoopThatDeletesIsRefused |
| [x] | the basics it stands on exist, or are minted first | pipeline, removerAt, filesAmong and anyInside all stand, so nothing was minted. | src/engine/removal.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove red first. Rule 13 drove both halves, beside the loop and inside it. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | Two cases were added, a named removal after a loop and one before it. Both were refused, and the refusal said rm runs inside a loop. | /tmp/engloop |
| [x] | the same test was seen green after the change, and named | All eleven cases of TestALoopThatDeletesIsRefused pass, the four loop-body ones among them. | /tmp/engloop |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Two files. ALoopThatRemoves tracks the body, and the test gains two cases. | git diff HEAD |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | It ran on a copy outside the tree, because another lane's file stops src/engine building. | /tmp/engloop |

