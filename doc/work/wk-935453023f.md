---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: loop refusal overreaches
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-relay-trial
claimed_by: 547b9365/worker-relay-trial
claimed_at: "2026-09-05T19:27:46Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - d2688f3e3f64b61b0ac3c3824ae2eb2e51212e45
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 7d1efdb89ca44568d0e6f2213c647e0de3868dad
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Found reviewing wk-2ef79848eb. src/engine/removal.go ALoopThatRemoves sets loop and deletes over the whole pipeline and never asks whether the removal is inside the loop. pipeline splits on ; && || | & and newlines, so any loop anywhere in the command plus any removal anywhere else is refused. Verified live in this tree: `for f in a; do echo $f; done; rm -f "C:/Users/mb/AppData/Local/Temp/claude/nope-xyz-not-here.txt"` was refused with A LOOP THAT DELETES IS REFUSED, though the rm is not in the loop and names a path outside the tree, which the rule says it does not touch. The damage is that the refusal says something untrue about the command it stops, and the remedy it offers, list what you mean and delete those files by name, is what the command already did, so there is no door. A removal should count only when it is inside the loop's body: between the do or brace that opens it and the done or brace that closes it.

## done when

- a loop followed by a separate named removal is not refused: go test -C src/engine -run TestALoopThatDeletesIsRefused
- a removal inside the loop body is still refused, for each of for, while, until and the case that ate live code: the same test
- a loop that removes nothing is still allowed: the same test

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One loop in removal.go and two cases in removal_test.go. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Three lines, all decided by go test over TestALoopThatDeletesIsRefused. |  |
| [x] | the basics it stands on exist, or are minted first | loopWord, removerAt, filesAmong and anyInside all exist, and none of them changed. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. The two cases were written first and watched red. |  |
| [x] | one test was written first and seen red for the reason expected | A loop followed by a named removal, and a named removal before a loop. Both were refused, and neither is in a loop. |  |
| [x] | the same test was seen green after the change, and named | TestALoopThatDeletesIsRefused, all eleven cases green, the loop that ate live code among them. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began is d2688f3e and is no object here. The change is two files, on origin as 25b0bcd1. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change. The header says the body is what is walked, so the rule and the refusal say the same thing. |  |

