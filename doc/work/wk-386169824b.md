---
id: wk-386169824b
seq: "-32"
type: work
title: finish before starting
status: imp_done
assignee: cowork
scope: single-step
traced: true
disposition: done
evidence:
  - outcome
rounds: 3
minted_by: cowork
---

## detail

Both queues in src/engine/pull.go hand out drafting before implementation. Swap the order: a reviewer takes an implementation review before a spec review, and a worker implements what is open before drafting anything new. A spec is still handed out when no implementation is waiting. A draft the agent already holds comes back before an open implementation. When the aheadOf exception fires, the draft is put down through whereItCameFrom.

## done when

- A reviewer with one token in imp_submitted and one in spec_submitted in one lane is handed the implementation, asked through the queue.
  `rg -q func.TestAReviewerTakesAnImplementationBeforeASpec src/engine && go test -C src/engine -count=1 -run TestAReviewerTakesAnImplementationBeforeASpec$ .`
- A worker with one token in imp_open and one in spec_open, both assigned to it, is handed the implementation.
  `rg -q func.TestAWorkerImplementsBeforeItDrafts src/engine && go test -C src/engine -count=1 -run TestAWorkerImplementsBeforeItDrafts$ .`
- A spec still comes out of each queue when only a spec is waiting, and the test names which queue stranded it.
  `rg -q func.TestASpecStillComesOutWhenNothingIsWaiting src/engine && go test -C src/engine -count=1 -run TestASpecStillComesOutWhenNothingIsWaiting$ .`
- The old sentence about a waiting submission is gone from pull.go and the new one says not yet counted.
  `rg -q "and a submission waiting" src/engine/pull.go && exit 1 || rg -q "not yet counted" src/engine/pull.go`
- A draft in hand comes back before an open implementation, and when the implementation is minted first the draft is put down to spec_open, holder cleared, with a put down line in the record.
  `rg -q func.TestADraftInHandComesBackBeforeAnOpenImplementation src/engine && go test -C src/engine -count=1 -run TestADraftInHandComesBackBeforeAnOpenImplementation$ .`
- Every test named above was watched failing on its own assertion with the swap absent before it was watched passing.

## evidence: outcome

The loops in src/engine/pull.go were swapped in both queues and the comment now says a waiting submission is not yet counted. The four tests named above were each watched red then green. The put-down draft reads spec_open with holder cleared, from whereItCameFrom.
