---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: backfill the why rows
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/main
claimed_at: "2026-09-05T19:59:27Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 10b539f64044df9de9a990655c19ed0d768a37a5
---

## detail

The tracked ask gained two criteria on wk-2b8f45c1e7. What is gained by doing it, and what breaks if it is never done. A token minted before that change carries no row for either, and the gate refuses a submission whose checklist has no row for a criterion. So every open token is refused at its next submission until a worker adds the two rows by hand and answers them.

What is gained by doing this is that the open queue moves without each worker rediscovering the same refusal one token at a time.

What breaks if it is never done is that every open token pays the detour, and a worker who reads the refusal as a bug undoes the criteria instead of answering them.

The rows go on unticked and unanswered. A backfill that ticked them would claim work nobody did.

## proposed action

Walk doc/work, and for each open token insert the two rows into its step 1 ask table, in the order the process now words them, unticked.

## done when

- every open token file under doc/work carries a row for each of the two new ask criteria, decided by: se find --regex 'what breaks if it is never done' --path doc/work/*.md returns one hit per open token
- the backfilled rows are unticked with an empty evidence cell, decided by: read three of the changed files
- a token that was open before the backfill submits without the gate naming a missing row, decided by: submit one and read the answer

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | the open queue moves without each worker meeting the same refusal | |
| [x] | what breaks if it is never done, and not only that it stays undone | every open token pays the detour, and a worker may undo the criteria instead | |
| [x] | the ask is small enough to review whole, or it is split first | one insertion per file, the same two lines every time | |
| [x] | every done-when line is decidable, and names the command where one decides it | two lines read files, the third submits a token and reads the answer | |
| [x] | the basics it stands on exist, or are minted first | both criteria are in the two process files already | |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | The rows went on unticked and empty. Ticking them would claim work nobody did. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | The red is the refusal, met twice on wk-4fbce27e2e. The gate named a missing row, one at a time. | refused twice |
| [x] | the same test was seen green after the change, and named | This token was open before the backfill and carries the rows now. Its own submission is the green. | this submit |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | 125 files, two added lines each, nothing removed. Each was checked to carry only those lines. | 250 lines |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | One could not take the rows inside its 200 word cap, so it was left for its author: wk-3b2bb11243, at 218. | 1 left |

