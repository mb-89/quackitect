---
id: wk-9a92ca488c
seq: 1000066
type: work
title: a second section wins
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer12
---

## detail

readBody in src/engine/store.go assigns rather than appends for a heading it reads once. A second section added by hand under an engine-owned heading replaces the first. The next SaveToken rewrites the note with the first section gone, and nothing is refused or reported. Refuse a note carrying two sections under a heading readBody keeps one of, naming the heading and the line the second one opens on. Add a test that loads a note with two done-when sections and requires the refusal. Paragraphs after the last lesson belong to no kept section and are lost the same way. Related: wk-1b7c1a2da1.
