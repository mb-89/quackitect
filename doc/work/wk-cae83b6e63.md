---
id: wk-cae83b6e63
seq: 1000017
type: work
title: a reviewer repairs trivia
status: spec_open
assignee: main
scope: single-step
traced: true
depends_on:
  - wk-24be1c06ae
rounds: 4
minted_by: person
---

## detail

THE OWNER'S WORDS: small stuff like this should not force a new round. We need a no nitpick rule. If it is not breaking anything, if it does not touch the functionality, then just fix it and tell the agent what you fixed. I do not see why this warrants a review round. MEASURED, AND IT IS WHY THIS EXISTS. wk-2b78b911b1 was rejected for a round because a recorded observation cited drain_test.go:201 where the assertion is at 241. The line number changes no behaviour, breaks nothing, and would have taken one edit. THE SHAPE THAT MAY WORK, and it wants agreeing before it is built. A reviewer that finds something which changes no behaviour and can be repaired in one edit repairs it, records the repair on the token, and does not open a round for it. A round is for something that would ship wrong. WHERE THE LINE SITS IS THE HARD PART, because one reviewer's trivial is another's silent defect, and a reviewer that starts editing the work it is judging stops being a second pair of eyes. THE NARROW VERSION, WHICH IS WHAT I WOULD BUILD. A reviewer may repair only what it can prove changes nothing that runs: a stale line number in a recorded observation, a citation that has moved, a count in prose that recounts differently, a wording slip. Never a criterion, never a command, never code, never a detail's claim. Every repair is written on the token in its own section so the owner can see what the reviewer changed rather than only what it refused. AND THE ENGINE CAN HOLD THE LINE. A repair section is a field, so the engine can refuse a repair that touches a criterion, and a rule the engine refuses is a rule that holds where a rule a reviewer remembers is not. WHAT IS UNCERTAIN. Whether a repaired token still counts as a pass for the failure rate, and I think it should, because the round it saved is the point. And whether a reviewer that repairs three things on one token should be made to say so louder than one that repairs none.

THE RATE IS NOT WRITTEN HERE AND NOT REQUIRED INTO THE METHOD. It is alive: three drafts carried three different figures for it today and a count an hour later answered differently again, because reviews land while anybody is counting. A number nobody re-derives has no owner. It belongs on the burndown bar, wk-1b7c1a2da1, where the engine computes it on every draw and it is right at the moment somebody looks. WHAT DOES NOT MOVE IS THE CASE: wk-2b78b911b1 lost a whole round to a line number that had shifted, and it changed no behaviour and would have taken one edit.

THE OWNER AGREED THE SHAPE AND ASKED FOR IT NOW RATHER THAN IN THE BACKLOG.
Their words: I feel like nitpicks should just be fixed, do not put it in the
backlog, put that in a thing you are doing right now, I think the shape you
described is correct. And on the cost: reviewers that fix stuff is bad
separation of concerns, but it is better than him telling you what to do, and
then you coming around, and then he finding new stuff.

WHAT A REPAIR IS. A change the reviewer makes to the note it is judging, which
it can show changes nothing that runs. It is recorded on the token in its own
section, with what it changed and why that changes nothing, and it does not open
a round.

WHAT MAY BE REPAIRED. A stale line number in a recorded observation. A citation
that has moved. A count in prose that recounts differently. A wording slip. All
four are claims about the record rather than instructions to a worker.

WHAT MAY NOT, AND THE ENGINE HOLDS THIS RATHER THAN THE REVIEWER REMEMBERING IT.
A criterion, a command, a detail's claim, and any file under src or util. Those
are the work, and a reviewer that edits the work stops being a second pair of
eyes. A repair naming a path outside doc/work is refused, which is a rule a
program can apply and a rule a tired reviewer cannot forget.

THE FIRST THING THE RULE WILL BE USED ON is already standing.
TestEveryRedSaidResolves, written this session on wk-2b78b911b1, requires every
recorded observation to name a line where an assertion stands, so an edit above
that line turns it red with nothing wrong. Taking the line out of the check and
making it find the assertion by its words is the repair, and wk-42adef4818
carries it.

## done when

- A verdict carries repairs, each one saying what it changed, where, and why that changes nothing that runs, and the engine writes each to the note in its own section so a reader sees what the reviewer changed and not only what it refused
  `rg -q func.TestARepairIsRecordedOnTheToken src/engine && go test -C src/engine -count=1 -run TestARepairIsRecordedOnTheToken$ .`
- A repair naming anything outside doc/work is refused, and the refusal names the path and says a reviewer repairs the record and not the work. THE ENGINE HOLDS THE LINE, because a rule a reviewer has to remember is a rule that lasts until the round somebody is tired
  `rg -q func.TestARepairOutsideTheRecordIsRefused src/engine && go test -C src/engine -count=1 -run TestARepairOutsideTheRecordIsRefused$ .`
- That refusal is asserted on what only it can say, and it has a case of its own because it stands beside the refusals for a missing finding, a missing lesson and a lesson naming no token: the case deletes that one refusal with the other three left standing and requires the verdict to come back ACCEPTED rather than merely refused by something else
  `rg -q func.TestOnlyTheRepairRefusalCanSayIt src/engine && go test -C src/engine -count=1 -run TestOnlyTheRepairRefusalCanSayIt$ .`
- A verdict of accept may carry repairs, so a reviewer that found nothing worth a round can still say what it tidied, and an acceptance with repairs is an acceptance rather than a rejection
  `rg -q func.TestAnAcceptanceMayCarryRepairs src/engine && go test -C src/engine -count=1 -run TestAnAcceptanceMayCarryRepairs$ .`
- The method says what may be repaired and what must be a round, in doc/guidance/reviewing.md under a section of its own, and the check reads that section rather than the whole file
  `rg -q func.TestTheReviewMethodSaysWhatMayBeRepaired src/engine && go test -C src/engine -count=1 -run TestTheReviewMethodSaysWhatMayBeRepaired$ .`
- It carries the measurement it came from rather than the claim alone, in doc/guidance/reviewing.md under the section criterion 5 names, and the check reads the section that heading opens rather than the whole file. THE MEASUREMENT IS THE CASE AND NOT THE RATE: the round wk-2b78b911b1 lost to a line number that had shifted, which changed no behaviour and would have taken one edit. NO RATE GOES INTO THE METHOD, because a rate is alive and a number nobody re-derives has no owner
  `rg -q func.TestTheRepairRuleCarriesItsMeasurement src/engine && go test -C src/engine -count=1 -run TestTheRepairRuleCarriesItsMeasurement$ .`
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. For the refusal the change taken away is the refusal itself, deleted on its own with the others left standing. The evidence names the test and what it said rather than a line number, because a line moves and the message does not

## finding 1 · round 1 · done when, criterion 1 / runs: rg -q func.ARepairIsRecordedOnTheToken src/engine && go test -C src/engine -count=1 -run TestARepairIsRecordedOnTheToken$ . · by reviewer10

**wrong:** The two halves of criterion 1's command name different symbols, so the guard cannot pass for the test the runner names and the criterion cannot go green by doing the work. It reads rg -q func.ARepairIsRecordedOnTheToken src/engine and then go test with -run TestARepairIsRecordedOnTheToken. The search pattern has no Test in it. I reproduced it rather than reading it: I wrote a file holding nothing but func TestARepairIsRecordedOnTheToken and ran both patterns against it. rg -q func.ARepairIsRecordedOnTheToken exits 1, and rg -q func.TestARepairIsRecordedOnTheToken exits 0. So the worker writes the test the criterion asks for, the guard stays red, and the only ways to close it are to add a second function called ARepairIsRecordedOnTheToken that nothing runs, or to change the command, which a worker may not do. The guard is there for a reason and the reason is why this matters: go test -run on a name that does not exist answers ok and exits zero, which is the red-by-absence case CriteriaThatAlreadyPass in src/engine/spec.go exists to catch, so the search in front of it is the thing making the criterion able to report at all. A guard whose pattern has drifted off the runner's does not fail open here, it fails shut, and either way the two halves are no longer about one thing. Extent, one pass over all seven commands on this list: criterion 1 is the only one missing Test from its search, and criteria 2, 3, 4, 6 and 7 all carry func.Test with the same name in both halves. Criterion 5 is a search with no runner and is a separate finding.

**satisfies:** Write the name once and put the same string in both halves: rg -q func.TestARepairIsRecordedOnTheToken src/engine and then -run TestARepairIsRecordedOnTheToken$. Then check the pair rather than reading it, which is one file: put a file holding only func TestARepairIsRecordedOnTheToken into a scratch directory, run the search half against it alone, and require it to exit zero. A guard that is red with the artefact present is a guard that is not about that artefact. Do that for all seven before this goes out again, because the same pair shape is on every line of this list.

## finding 2 · round 1 · done when, criterion 3: "That refusal is asserted on what only it can say, because it stands beside the refusals for a missing finding, a missing lesson and a lesson naming no token, and a case asking only whether the call was refused passes with any of them deleted" · by reviewer10

**wrong:** Criteria 2 and 3 carry the same command, byte for byte, and their sentences are about different things. I pulled the seven commands off the note and compared them rather than reading down the page: both are rg -q func.TestARepairOutsideTheRecordIsRefused src/engine and then go test on that one name. Criterion 2 says the refusal exists and names the path. Criterion 3 says something else, that the refusal is asserted on what only it can say because it stands beside three other refusals. Whatever makes criterion 2 green makes criterion 3 green, so criterion 3 puts a sentence on the list and no decision in the gate, and the token can close with the property nobody checked. It matters most here because criterion 3's sentence is word for word the trap doc/guidance/behaviour.md describes under The check comes first: a check that asks only whether the call was refused, with two refusals in a row, where deleting the first leaves the suite green. The criterion that exists to forbid that shape is decided by a command that cannot see it. This is not one draft's slip. wk-2fb5bf5bb1, which I rejected an hour ago, carries the identical pair in the identical words, criteria 2 and 3 with one command and the second sentence being the anti-vacuity one, so the extent is two of the drafts minted today and the phrasing travelled between them.

**satisfies:** Give criterion 3 a command of its own naming a second test, so the two sentences are decided separately. The test that decides it is the one behaviour.md describes: delete the outside-the-record refusal on its own, with the missing finding, missing lesson and lesson-names-no-token refusals left standing, and require the verdict to be ACCEPTED rather than merely refused by something else. Assert on what only that refusal can say, which is the path and the sentence about repairing the record and not the work. If one test really covers both, then they are one criterion and the second sentence belongs inside the first. Then take the red the way criterion 8 already asks: cut that one refusal, run the new test, and record the test name and what it said.

## finding 3 · round 1 · done when, criterion 5: "The repair field is in the shape table on wk-24be1c06ae" / runs: rg -q Repair doc/work/wk-24be1c06ae.md · by reviewer10

**wrong:** The sentence names a place inside a file and the command searches the whole file for one word, and the file is another token's unagreed draft. Criterion 5 says the repair field is in the shape table on wk-24be1c06ae, and its command is rg -q Repair doc/work/wk-24be1c06ae.md. The word is absent from that note today, which I confirmed, so the command is red and looks sound. It goes green the moment the word appears anywhere in a note that runs to tens of thousands of characters of detail, findings and lessons, and a reviewer writing a finding about repairs on that token would turn it green without touching the table. Criterion 6 on this same list says the right thing about itself, that the check reads that section rather than the whole file, so the token knows the rule and did not apply it here. There is a second problem in the same line and it is not the same problem. wk-24be1c06ae is a draft in spec review, whose shape table has been rewritten in each of the last three rounds and which I rejected again this session, so this criterion asserts a property of a table that is not agreed, and this token declares no dependency on it. And this is the second draft minted today with the shape: wk-2fb5bf5bb1's criterion 4 is rg -q prevents doc/work/wk-24be1c06ae.md, which I demonstrated goes green with the word in a stray paragraph.

**satisfies:** Anchor the search to the table rather than to the file, and say so in the sentence the way criterion 6 does: read the block that table's heading opens and require the row inside it, so the check fails when the word is present and the row is not. Then decide the cross-token question and write it down: either declare the dependency with depends_on so the ordering is the engine's rather than a reader's, or drop the criterion and say in the detail that the row is wk-24be1c06ae's work, naming it, so the exclusion can be told from an oversight. Then take the red on the case that decides it: put the word somewhere in a copy of the note that is not the table, run the check, and require it to stay red.

## finding 4 · round 2 · detail: "WHAT MAY NOT, AND THE ENGINE HOLDS THIS RATHER THAN THE REVIEWER REMEMBERING IT. A criterion, a command, a detail's claim, and any file under src or util" / done when, criterion 2: "A repair naming anything outside doc/work is refused" · by reviewer11

**wrong:** THE DETAIL FORBIDS FOUR THINGS AND ONE CRITERION DECIDES ONE OF THEM, AND THE THREE LEFT OUT ARE THE THREE THE RULE EXISTS FOR. WHAT MAY NOT reads: a criterion, a command, a detail's claim, and any file under src or util, and the same paragraph says AND THE ENGINE HOLDS THIS RATHER THAN THE REVIEWER REMEMBERING IT. Criterion 2 decides one member, a repair naming a path outside doc/work, and criterion 3 is the anti-vacuity case for that same refusal. Nothing on the list asks after a repair that rewrites a criterion, a repair that rewrites a command, or a repair that rewrites a detail's claim. All three of those live inside doc/work, inside the very note being repaired, so the path refusal cannot see any of them. I CONSTRUCTED THE CASE THAT PASSES EVERY CRITERION AND BREAKS THE SENTENCE, and it is one line: a repair whose path is doc/work/wk-24be1c06ae.md and whose change rewrites a criterion's runs. It is inside doc/work, so criterion 2 is green. It is recorded with what it changed and why, so criterion 1 is green. Criteria 4, 5 and 6 are about acceptance, the method and the measurement. So the reviewer edits the work it is judging, which is the risk the detail names in its own words, a reviewer that starts editing the work it is judging stops being a second pair of eyes, and every criterion on the token agrees. THE INSTRUMENT CHOSE THE MEMBER. A path is a string in the payload and a path check is one comparison. A repair that touched a criterion can only be seen by holding the note the engine loaded against the note it loaded before, which is a diff and is work nobody has specified. So the member the existing instrument reaches is the one that got a criterion, and the detail goes on claiming all four. AND THE DETAIL ASSERTS A MECHANISM NO CRITERION ASKS FOR: A repair section is a field, so the engine can refuse a repair that touches a criterion. Nothing on the list requires that, and the engine cannot do it by reading the repair's own words, because the words are the reviewer's account of the edit rather than the edit. EXTENT, ONE PASS OVER ALL SEVEN CRITERIA: criterion 1 records a repair, 2 refuses a path, 3 is 2's anti-vacuity case, 4 lets an acceptance carry repairs, 5 puts the rule in the method, 6 puts the measurement in the method, 7 is the observation clause. Four forbidden things, one decided.

**satisfies:** GIVE EACH MEMBER OF THE FORBIDDEN SET A CRITERION, OR TAKE IT OUT OF THE DETAIL WITH THE REASON. The set is four and the criteria decide one, so either three criteria are missing or the detail is claiming three things the token does not build. The one that matters most is a criterion: require that a verdict carrying repairs is refused when the note it repaired comes back with a different criterion from the one the engine handed out, and say in the sentence that the engine decides it by holding the loaded token against what it loaded rather than by reading the repair's own account of itself. Do the same for a command, which is a criterion's runs, and say what a detail's claim means as a rule a program can apply or move it to the reviewer's judgment with that said in the detail. Where a member is deliberately left to the reviewer, write it into WHAT MAY NOT with that answer beside it, so a reader can tell an exclusion from an oversight, which is what this project asks of every table it agrees. THE CHECK, RED TODAY: write the criterion-diff refusal and drive the case above through it before agreeing the draft. A verdict carrying a repair whose path is doc/work and whose edit changed a criterion's runs must be refused with a message naming the criterion, and against the engine as it stands nothing refuses it, which is the red. Take one red per member rather than one for the set, the way criterion 3 already asks for the path refusal.

## finding 5 · round 2 · done when, criterion 6: "It carries the measurement it came from rather than the claim alone: the round wk-2b78b911b1 lost to a line number," / detail: "a review failure rate of 249 per cent over 61 tokens" · by reviewer11

**wrong:** THE THREE NUMBERS ARE THE ONES I REJECTED wk-2fb5bf5bb1 FOR AN HOUR AGO, AND CRITERION 6 WRITES TWO OF THEM INTO THE METHOD. The detail says That round is one of the 122 rejections behind a review failure rate of 249 per cent over 61 tokens, and says it a second time in the appended paragraph, and criterion 6 requires that 49 distinct tokens carry a review event with 122 rejections across them into doc/guidance/reviewing.md. I TOOK THE READINGS RATHER THAN CARRYING THEM OVER. Counting verdict events across .se/log/*.jsonl the record holds 76 rejected and 45 spec rejected, which is 121 rejections, and those events name 49 distinct token ids. Counting notes across doc/work and .se/work, 48 carry at least one finding heading. So 106 was right when it was taken and answers 121 now, 61 answers 49 from the log and 48 from the notes and reproduces from neither, and 249 per cent is 106 over 61 and reproduces from nothing. reviewing.md is handed to every reviewer on every review pull, it is projected nowhere, and nothing in this tree re-derives a number written into it, so the criterion makes a figure permanent that was already moving while the round ran. THIS IS THE THIRD DRAFT CARRYING IT. reviewer10 rejected wk-1b7c1a2da1 and wk-2fb5bf5bb1 for the same three numbers, and round 1 here swept the seven commands and did not sweep the numbers, so the sentence travelled between the drafts untouched. The class is wk-aea7597e98. AND CRITERION 6 NAMES NO FILE. It opens with the word It, where criterion 5 beside it names doc/guidance/reviewing.md, names its section, and says the check reads that section rather than the whole file. So the one criterion that puts a number into permanent guidance is the one that does not say where the number goes.

**satisfies:** TAKE THE NUMBERS OUT OF CRITERION 6 AND LEAVE THE ARGUMENT IN, or write each with the command that produced it and the commit it was taken at. This token does not need the rate: the round wk-2b78b911b1 lost to a moved line number is the whole argument, it is one concrete case a reader can follow, and it is already the first half of criterion 6. If a figure has to reach the method, then reviewing.md carries the command and its answer and the sha beside it, because a number nobody re-derives has no owner, which doc/guidance/voice.md says under Authority. Say what reached a review means as a rule a command can apply, and take both halves of any rate from one instrument, since the two candidates here answer 49 and 48 and neither is 61. AND GIVE CRITERION 6 ITS SUBJECT: doc/guidance/reviewing.md, the section by name, and the check reads that section rather than the whole file, the way criterion 5 already does. THE CHECK, RED TODAY: run each number's command before agreeing the draft and paste what it answered. Against the record as it stands the rejection count is 121 and the two candidate denominators are 49 and 48, so a check requiring 61 in reviewing.md would be requiring a sentence no instrument in this tree supports. WHAT IS CLOSED AND SHOULD NOT BE REDONE, because I checked it: criterion 1's two halves now name the same symbol and I ran both against a file holding only that function, criterion 3 now names TestOnlyTheRepairRefusalCanSayIt and describes the delete-one-refusal case, and the shape-table criterion is gone with depends_on naming wk-24be1c06ae instead, which is the better of the two answers round 1 offered.

## finding 6 · round 3 · detail: "WHAT MAY NOT, AND THE ENGINE HOLDS THIS RATHER THAN THE REVIEWER REMEMBERING IT. A criterion, a command, a detail's claim, and any file under src or util" / done when, criterion 2: "A repair naming anything outside doc/work is refused" · by reviewer11

**wrong:** THE FINDING WAS NOT ANSWERED AND NOTHING ON THE LIST CHANGED. Last round I said the detail forbids four things and one criterion decides one of them, and that the three left out are the three the rule exists for. WHAT MAY NOT still reads: a criterion, a command, a detail's claim, and any file under src or util, and still says AND THE ENGINE HOLDS THIS RATHER THAN THE REVIEWER REMEMBERING IT. I compared the seven criteria against the seven I read last round: criterion 2 is unchanged and still refuses only a path outside doc/work, criterion 3 is unchanged and is that same refusal's anti-vacuity case, and no criterion was added. The only line that moved is criterion 6, which is the other finding and is answered well. So the case I wrote out last round is still one line and still passes everything: a repair whose path is doc/work/wk-24be1c06ae.md and whose change rewrites a criterion's runs is inside doc/work, so criterion 2 is green, and it is recorded with what it changed and why, so criterion 1 is green. The reviewer edits the work it is judging and the token agrees. AND THE PARAGRAPH THAT ARGUED THE MECHANISM IS GONE RATHER THAN THE GAP BEING CLOSED. The old detail said AND THE ENGINE CAN HOLD THE LINE. A repair section is a field, so the engine can refuse a repair that touches a criterion. That sentence is not in the note any more, and no criterion took its place, so the claim was withdrawn from the prose and never turned into a check. THE COUNT IS STILL FOUR AGAINST ONE. Four forbidden things in the detail. One decided.

**satisfies:** GIVE EACH MEMBER OF THE FORBIDDEN SET A CRITERION, OR TAKE IT OUT OF THE DETAIL WITH THE REASON BESIDE IT. The one that matters is a criterion: require that a verdict carrying repairs is refused when the note it repaired comes back with a different criterion from the one the engine handed out, and say in the sentence that the engine decides it by holding the loaded token against what it loaded rather than by reading the repair's own account of itself, because the account is the reviewer's words and not the edit. Do the same for a command, which is a criterion's runs. For a detail's claim, either say what it means as a rule a program can apply or move it to the reviewer's judgment and write that into WHAT MAY NOT, so a reader can tell an exclusion from an oversight. Put the two numbers in the draft, how many the forbidden set names and how many the criteria decide, so the next reviewer holds one against the other instead of reading the word holds. THE CHECK, RED TODAY: write the criterion-diff refusal and drive the case through it before agreeing the draft. A verdict carrying a repair whose path is doc/work and whose edit changed a criterion's runs must be refused with a message naming the criterion, and against the engine as it stands nothing refuses it. Take one red per member rather than one for the set, the way criterion 3 already asks for the path refusal.

## finding 7 · round 3 · detail, the first paragraph / doc/guidance/specifying.md, The two parts: "THE PROBLEM, in the words it was asked in. What is wrong, or what is wanted, and enough of the circumstances that somebody arriving later can tell whether it still applies" · by reviewer11

**wrong:** THE PROBLEM STATEMENT WENT OUT WITH THE NUMBER, AND THE DETAIL NOW OPENS BY SAYING WHAT IT DOES NOT CARRY. The rate had to go and it has gone, which is right and is the other finding answered. What went with it is the whole first paragraph, and I compared the note against the one I read last round rather than reasoning about it. Gone: THE OWNER'S WORDS: small stuff like this should not force a new round. We need a no nitpick rule. If it is not breaking anything, if it does not touch the functionality, then just fix it and tell the agent what you fixed. Gone with it: THE SHAPE THAT MAY WORK, WHERE THE LINE SITS IS THE HARD PART, THE NARROW VERSION WHICH IS WHAT I WOULD BUILD, AND THE ENGINE CAN HOLD THE LINE, and WHAT IS UNCERTAIN, which flagged two open questions, whether a repaired token still counts as a pass and whether a reviewer that repairs three things should say so louder. Nothing says those two were settled or dropped. doc/guidance/specifying.md says a spec carries THE PROBLEM, in the words it was asked in, and the words this token was asked in are now absent from the paragraph that used to state the problem. A second quotation of the owner survives lower down, agreeing the shape, and agreeing a shape is not the same sentence as asking for the thing. AND WHAT REPLACED IT IS A NEGATIVE. The detail's first paragraph is now THE RATE IS NOT WRITTEN HERE AND NOT REQUIRED INTO THE METHOD, which is a sentence about what the token leaves out, and doc/guidance/voice.md's Absences section says a sentence saying what a document leaves out is itself the thing it says it leaves out. A worker arriving at this token reads a paragraph about a number before it reads what is wrong.

**satisfies:** PUT THE PROBLEM BACK AT THE TOP, IN THE OWNER'S WORDS, AND MOVE THE PARAGRAPH ABOUT THE RATE DOWN OR OUT. The opening paragraph says what is wrong and what was asked for: the owner's sentence about nitpicks not forcing a round, and the case, which is the round wk-2b78b911b1 lost to a line number that had shifted. Both are already in the token, one in criterion 6 and one in the second owner quotation, so this is a move rather than an invention. Then either restore the two open questions under WHAT IS UNCERTAIN or say what was decided about each, because a question a draft raised and a later draft deleted reads as settled and nothing settled it. Keep the reasoning about the rate if it is worth keeping, and keep it where it belongs, beside criterion 6, rather than as the first thing a reader meets. THE CHECK, AND IT IS A READER'S RATHER THAN A COMMAND'S: read the detail's first paragraph and ask whether somebody arriving with no history can tell what is wrong and what was asked for. Today the answer is a paragraph about a figure the token does not carry.

## finding 8 · round 4 · detail: "WHAT MAY NOT, AND THE ENGINE HOLDS THIS RATHER THAN THE REVIEWER REMEMBERING IT. A criterion, a command, a detail's claim, and any file under src or util" and, in the restored first paragraph, "AND THE ENGINE CAN HOLD THE LINE. A repair section is a field, so the engine can refuse a repair that touches a criterion" / done when, criterion 2: "A repair naming anything outside doc/work is refused" · by reviewer12

**wrong:** THE COUNT IS STILL FOUR AGAINST ONE, AND THIS REDRAFT PUT THE UNBUILT CLAIM BACK INTO THE PROSE RATHER THAN INTO A CRITERION. I counted the two sides of the note myself rather than taking the last round's arithmetic. The forbidden set is named twice now. The restored first paragraph says never a criterion, never a command, never code, never a detail's claim. WHAT MAY NOT says a criterion, a command, a detail's claim, and any file under src or util. Four members either way. Then I read all seven criteria and asked of each which member it decides. Criterion 1 records a repair. Criterion 2 refuses a path outside doc/work. Criterion 3 is criterion 2's anti-vacuity case. Criterion 4 lets an acceptance carry repairs. Criteria 5 and 6 put the rule and the measurement into the method. Criterion 7 is the observation clause. One member decided, four named. I DROVE THE PASSING CASE THROUGH EVERY CRITERION RATHER THAN ASSERTING IT. A repair whose path is doc/work/wk-24be1c06ae.md and whose change rewrites a criterion's runs sits inside doc/work, so criterion 2 is green, and it can say what it changed, where, and why, so criterion 1 is green. Criteria 3 to 7 are about the anti-vacuity case, acceptance, the method text and the reds, so none of them sees it. The reviewer rewrites a criterion in the note it is judging and every line of the token agrees. WHAT IS NEW THIS ROUND, AND IT IS THE OPPOSITE SHAPE TO THE LAST ONE. Round 3 recorded that the sentence arguing the mechanism had been deleted, so the claim left the prose and never became a check. Commit 8c62dfba answers the other finding by restoring that whole paragraph, and the restored text says the engine can refuse a repair that touches a criterion. So the note now asserts the mechanism in two places and builds it in none, and the same round that fixed the problem statement re-armed the claim without answering the finding about it. I compared the criteria before and after that commit: not one of the seven changed and none was added. AND NOTHING ELSE IN THE TREE COVERS IT, WHICH I CHECKED RATHER THAN ASSUMED. src/engine/pull.go carries no repair of any kind, so no existing refusal reaches this. Criterion 5 does not cover it either: it asks doc/guidance/reviewing.md to say what may be repaired, which is a rule a reviewer reads, while the detail's claim is that the engine holds it.

**satisfies:** GIVE EACH MEMBER OF THE FORBIDDEN SET A CRITERION, OR TAKE IT OUT OF THE DETAIL WITH THE REASON BESIDE IT. This is round 3's remedy and I reproduced the case it rests on, so the shortest route is to take it. The one that matters is a criterion: a verdict carrying repairs is refused when the note it repaired comes back with a different criterion from the one the engine handed out. Say in that sentence that the engine decides it by holding the loaded token against what it loaded, and not by reading the repair's own account of itself, because the account is the reviewer's words and not the edit. Do the same for a command, which is a criterion's runs. For a detail's claim, either say what it means as a rule a program can apply, or move it to the reviewer's judgment and write that answer into WHAT MAY NOT beside the member. AND SAY WHICH OF THE TWO ENGINE CLAIMS STANDS, because the note now makes it twice. If the engine is to refuse a repair that touches a criterion, that sentence needs a criterion under it. If it is not, the sentence comes out of both paragraphs rather than out of one. PUT THE TWO NUMBERS IN THE DRAFT, how many the forbidden set names and how many the criteria decide, and re-take both after this round, so the next reviewer holds one against the other instead of reading the word holds. THE CHECK, RED TODAY: write the criterion-diff refusal and drive the case above through it before agreeing the draft. A verdict carrying a repair whose path is doc/work and whose edit changed a criterion's runs must be refused with a message naming the criterion, and against the engine as it stands nothing refuses it, which is the red. Take one red per member rather than one for the set, the way criterion 3 already asks for the path refusal, and record each by the test name and what it said.

## lesson 1 · round 1 · by reviewer10

**the class:** A TWO-PART COMMAND WHOSE GUARD AND WHOSE RUNNER NAME DIFFERENT SYMBOLS. The pattern in this project is sound and it is there for a reason: go test -run on a name nothing defines answers ok and exits zero, so a criterion puts a search for the definition in front of the run and the pair together can report on the work. The two halves are then written out twice, by hand, from the same idea of the name, and one of them drifts. The failure is not the one a reader expects. A drifted guard does not fail open, it fails SHUT: the worker writes exactly the test the criterion names, the search finds nothing, and the criterion is red with the work done. The only ways out are a decoy definition carrying the guard's spelling, which is worse than no guard, or editing the criterion, which a worker may not do, so the drift is discovered at the keyboard by somebody with no authority to fix it. It hides because the command reads as one thing: the two long identifiers differ by four characters in the middle of a line nobody re-reads, and the whole line is red today for the honest reason that nothing is built yet, so no gate can tell the two reds apart. Measured on wk-cae83b6e63: criterion 1 searches for func.ARepairIsRecordedOnTheToken and runs TestARepairIsRecordedOnTheToken, and against a file holding func TestARepairIsRecordedOnTheToken the search exits 1 while the same search with Test in it exits 0.

**instead:** Two halves, and the first is what stops it being made. Write the identifier once and paste it into both halves of the command, so the guard's pattern literally contains the runner's, and then read the finished line asking whether the same string appears twice. A command that names one thing twice is a command you can check by eye in a second; one that names it in two spellings is one nobody can. The second half is the check that catches it, and it costs one file: before submitting, for every command of the shape prove it exists and then run it, put the artefact the runner names into a scratch directory on its own and run the guard against that. A guard that is red with the artefact present is a guard that is not about the artefact, and that is the one thing a draft cannot learn from running the command against the tree, because the tree is red anyway.

**minted as:** wk-775a8ed90f

## lesson 2 · round 2 · by reviewer11

**the class:** A FORBIDDEN SET NAMED IN THE DETAIL, AND CRITERIA THAT ASSERT THE ONE MEMBER THE EASIEST INSTRUMENT CAN ALREADY SEE. The detail says what a rule refuses and names four things. Then the criteria are written, and one of the four is a string sitting in the payload while the other three need something the engine has not got yet, a diff of the note before and after, or a judgment. So the criteria assert the string, the list reads as covered because the detail still names four, and the three that were dropped are the three the rule was written for. THE INSTRUMENT CHOOSES THE MEMBER AND THE CHOICE IS NEVER WRITTEN DOWN: nothing says the other three were considered and left out, so an omission looks exactly like an oversight because that is what it is. THE TELL is to count the detail's forbidden list, count the criteria that decide a member of it, and ask of each missing member what the engine would have to look at to see it. MEASURED ON wk-cae83b6e63: WHAT MAY NOT names four, a criterion, a command, a detail's claim and any file under src or util, one criterion decides a path outside doc/work, and a repair whose path is doc/work and whose change rewrites a criterion satisfies every criterion on the list.

**instead:** WHAT WOULD HAVE STOPPED IT BEING MADE: write one criterion per member of the forbidden set BEFORE choosing how any of them is checked, so the instrument is chosen after the set rather than the set after the instrument. Where a member needs an instrument that does not exist, the instrument is the work: say what it has to look at, a diff of the loaded note against what was loaded, and make that the criterion. A member left out on purpose goes into the detail with the reason beside it, so a reader can tell it from an oversight. WHAT WOULD HAVE CAUGHT IT: count the members of the forbidden list and the criteria that decide one, and put both numbers in the draft. Then write the case that satisfies every criterion and breaks the detail's sentence. If that case takes one line to write, the set is covered by one member and the draft is not finished.

**minted as:** wk-54c96c4002

## lesson 3 · round 3 · by reviewer11

**the class:** A FORBIDDEN SET NAMED IN THE DETAIL, AND CRITERIA THAT ASSERT THE ONE MEMBER THE EASIEST INSTRUMENT CAN ALREADY SEE, NAMED AGAIN BECAUSE A ROUND PASSED AND THE SET IS STILL FOUR AGAINST ONE. The class is wk-54c96c4002. What this round adds to it is the shape the answer took: the round answered the finding that could be answered by deleting words, the number in the guidance, and left the finding that needed a criterion written. And the sentence that argued the missing mechanism, the engine can refuse a repair that touches a criterion, was deleted along with the paragraph it sat in, so the claim left the prose and never became a check. A GAP CLOSED BY DELETING THE CLAIM READS THE SAME AS A GAP CLOSED BY BUILDING THE CHECK, on a token where nothing counts the two sides. MEASURED ON wk-cae83b6e63, round 2: four forbidden things in the detail, one criterion deciding one of them, no criterion added, and a one-line case that satisfies every criterion while the reviewer rewrites a criterion in the note it is judging.

**instead:** WHAT WOULD HAVE STOPPED IT BEING MADE: answer the findings in the order of what they cost rather than what they cost you, and write the criterion the hard one asks for before touching the prose the easy one names. Where a round removes a sentence a finding rested on, say in the redraft that the claim was withdrawn and why, because deleting a claim and satisfying it look identical in a note. And write one criterion per member of a forbidden set before choosing how any of them is checked, so the instrument is chosen after the set rather than the set after the instrument. WHAT WOULD HAVE CAUGHT IT: put two numbers in the draft, how many things the detail forbids and how many the criteria decide, and re-take both after every round, because a redraft that changes one criterion leaves the other count untouched and nothing else in the note will say so. Then write the case that satisfies every criterion and breaks the detail's sentence: if it takes one line, the draft is not finished.

**minted as:** wk-54c96c4002

## lesson 4 · round 4 · by reviewer12

**the class:** A FORBIDDEN SET NAMED IN THE DETAIL, AND CRITERIA THAT ASSERT THE ONE MEMBER THE EASIEST INSTRUMENT CAN ALREADY SEE, NAMED A THIRD TIME BECAUSE TWO ROUNDS HAVE PASSED AND THE SET IS STILL FOUR AGAINST ONE. The class is wk-54c96c4002 and what this round adds is that the prose moved in the opposite direction and the count did not move at all. Round 2 left the claim standing and the criteria short. Round 3 found the sentence that argued the missing mechanism deleted, so the claim had left the prose without becoming a check. Round 4 restored that whole paragraph, because the other finding asked for the problem statement back, so the claim is now asserted in two places and built in none. A GAP READS THE SAME WHETHER THE CLAIM IS DELETED, RESTORED OR LEFT ALONE, because all three change the prose and none of them changes how many members the criteria decide. MEASURED ON wk-cae83b6e63 round 4: the redraft added one paragraph, changed no criterion and added none, and the case that satisfies every criterion while the reviewer rewrites a criterion is still one line long.

**instead:** After every redraft, count the members of the detail's forbidden list and the criteria that decide one, and put both numbers in the draft. Take the second number from the criteria as they stand rather than from what the round intended to change. Then write the case that satisfies every criterion and breaks the detail's sentence. If it takes one line, the set is covered by one member and the draft is not finished.

**before it:** Write the criterion the expensive finding asks for first, while the draft is still untouched. Prose answers the cheap finding and moves the claim, and a claim moved is not a claim decided. Ask what would have to be compared to see the member, and make that comparison the criterion.

**minted as:** wk-54c96c4002

