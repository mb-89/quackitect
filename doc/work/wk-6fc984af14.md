---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: held by nothing reads
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-berio-three
claimed_at: "2026-09-05T19:51:55Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 3cf4241a7012b0d0bb79887d6519415da89363fc
---

## detail

A finding on wk-4fbce27e2e, lint reads prose held. That token narrowed holdersIn and its four named lines are gone from the findings. One line written since trips it again. wk-cbc9d0854e carries: So the ordering half is held and this half is held by nothing. It is a sentence about two halves of a rule, not a claim that a token is held. It reaches the branch that flags is held with no by after it. The other half of the same sentence says held by nothing, where the word after by is not a person. That is the shape to narrow on. A by whose object is nothing, nobody or no one claims no holder, and neither does a subject that is a half, a rule or a check rather than a token. The lint answers two hold findings on this tree. The other, on wk-db1416769e, names worker-ives and is the rule working.

## done when

- the sentence on wk-cbc9d0854e is in the clean map of holderinprose_test.go and the test passes
- se lint answers one hold finding on this tree, the one that names an actor

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The rule answers one finding instead of three. A lint that is mostly noise gets run past. | se lint |
| [x] | what breaks if it is never done, and not only that it stays undone | Two prose lines stay flagged for good, and the one real hold hides among them. | se lint |
| [x] | the ask is small enough to review whole, or it is split first | One word in a list in lint.go, and one line in the test's clean map. | git show --stat |
| [x] | every done-when line is decidable, and names the command where one decides it | TestATokensProseNamesNoHolder names the sentence. se lint counts the hold findings on this tree. | se test |
| [x] | the basics it stands on exist, or are minted first | holdersIn, namesSomebody and the clean map are all here. wk-4fbce27e2e narrowed the same rule. | lint.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove the red first, rule 14 the re-run after the engine swap. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | The sentence went into the clean map. The test failed, saying it was read as a holder claim. | TestATokensProseNamesNoHolder |
| [x] | the same test was seen green after the change, and named | The same test passed once nothing joined the word list. se lint answers one hold finding, on wk-db1416769e. | se lint |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began 3cf4241a. One word and its comment in lint.go, one line in holderinprose_test.go. | git show --stat |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None. This token's own title tripped the rule and reads clean now. | se lint |

