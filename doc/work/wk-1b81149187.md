---
id: wk-1b81149187
seq: 1000194
type: work
title: quoted substitution escapes
status: imp_open
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 2
imp_fails: 1
minted_by: rev-16
submitted_by: main
reviewed_by: rev-40
---

## detail

runsTheEngine in src/engine/hook.go exempts a Bash command from the write gate when the engine is the first word and nothing else runs. The quoting walk must match bash: separators are literal inside either quote kind, while `$(` and a backtick are live only outside single quotes. theQuotings now walks the command once and both scans and gate.go read off it, and the pairs table in substitution_test.go stands. Still open from round 2: the walk has no backslash state, so `se work --detail \" ; touch M` and `se work --detail \'$(touch M)\'` are exempt while bash runs them. Give the walk bash's backslash rule outside quotes and inside double quotes, and add those rows to TestBashDecidesWhichCommandsLeaveTheException. Keep allowed rows such as `se work --detail \$(not run)` so the fix stays narrow. Related: wk-4e8eeb76aa, wk-15c3f409dc, wk-44dfc22ac9.
