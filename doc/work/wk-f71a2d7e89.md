---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: verify jory answered-line tests
# where the token stands. The process owns these values.
status: open
# the token this is a part of. It cannot close while this is open
parent: [[wk-e870f1909f]]
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 355be9f1a92e0bb1264d66ae37c141b07196f8e6
---

## detail

Parent wk-e870f1909f is in the hands of the actor who wrote the gate fix in src/engine/pull.go and two tests in src/engine/answeredline_test.go but could not see either red or green: src/engine would not compile at that moment (staffing.go, timeline_test.go). The tree now builds. This child token carries the verification that actor could not do, because the parent cannot be taken from whoever has it and one token has one holder.

The two tests are TestAnUntickedLineThatIsAnsweredCloses and TestAnUnansweredLineIsRefusedAndSaysSo. The fix is already in pull.go, so green alone proves nothing about red. The red must be produced against the pre-fix code. Either revert the fix in a scratch copy outside the tree, or name the exact assertion that fails without it.

## done when

- src/engine builds and vets clean at exit 0, named by the command that says so
- Both named tests are seen GREEN against current source via se_test propose, and the pass line is quoted
- Both named tests are seen RED against the pre-fix pull.go, in a scratch copy outside the tree, and the failure text of each is quoted with which assertion failed
- The verdict says for each test which state was red and which was green, and claims no red that was not seen

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One question: are jory's two tests red before the fix and green after. | 2 tests |
| [x] | every done-when line is decidable, and names the command where one decides it | Build/vet by go build and go vet -C src/engine. Green by se_test propose. Red by go test -C on a scratch copy. | se_test |
| [x] | the basics it stands on exist, or are minted first | Helpers aTreeWithAChecklist, mintWithChecklist and noteText all exist in notekeeps_test.go, so the tests compile. | notekeeps_test.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | rule 15 read. Rule 12 applied: red seen on pre-fix code, not assumed. | rule 12, 15 |
| [x] | one test was written first and seen red for the reason expected | jory wrote both. Both seen RED pre-fix: answeredline_test.go:49 and :74. | 2 FAIL |
| [x] | the same test was seen green after the change, and named | Both named tests ok via se_test: 0.18s and 0.14s. | se_test |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | NOT MET. No tree file changed. This token only verified. | 355be9f1 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Parent wk-e870f1909f is still in another actor's hands, so I cannot close it. | wk-e870f1909f |

