---
id: wk-5bec911840
seq: "-5"
type: work
title: sort and filter work
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-66a28ca311
subs:
  - wk-080da4232c
rounds: "2"
minted_by: person
evidence:
  - outcome
---

## detail

Sort and filter do nothing when a person uses them, while properties works. The select renders 29 options, so the defect is in the wiring or in what the message does when it arrives. Render the page and follow one control from the press to the file it writes, starting from the v3 bases client. All three popovers close the moment they open, and stopPropagation survives the bundle. Owner's rule: the button toggles a popover, a press inside does nothing, and it closes on a press outside or the same button. Sort and group by take more than one level, so SetSort and SetGroup keep a level list and a heading press sets the first level.

## evidence: outcome

SetFilterGroups writes one list item per group and an ored group as `- or:`, and TestAFilterReadsBackAsWhatWasBuilt drives all four shapes comparing rows. reCompare in src/engine/filterbuild.go now matches only one literal, so a compound value falls to Raw rather than being written back mangled. TestAValueThatIsNotOneLiteralIsNotReadAsAComparison in src/engine/filterbuild_test.go was watched red on the real corruption and asserts both directions. The battery afterwards answers all ok.
