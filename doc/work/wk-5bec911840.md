---
id: wk-5bec911840
seq: "-5"
type: work
title: sort and filter work
status: imp_in_review
assignee: main
scope: single-step
traced: true
disposition: done
holder: reviewer5
parent: wk-66a28ca311
subs:
  - wk-080da4232c
rounds: "1"
minted_by: person
---

## detail

Sort and filter do nothing when a person uses them.

WHAT THE OWNER SEES: the popovers open and the controls inside them have no
effect. Properties is the one that works.

WHAT IS RULED OUT: the select is not empty. It renders 29 options, one per
property, measured on the rendered page.

SO THE DEFECT IS IN THE WIRING OR IN WHAT THE MESSAGE DOES WHEN IT ARRIVES.
Render the page, read what is drawn, and follow one control from the press to
the file it writes. Do not guess at it again.

V3'S WORKED. bases had one client and the controls were part of it, so start
there rather than from this one.

IT OPENS AND DISAPPEARS. The owner sees all three popovers close the moment
they open. The rule they asked for: the button toggles it, a press inside does
nothing to it, and it closes on a press outside or on the same button again.

WHAT IS RULED OUT: the tool's own handler calls stopPropagation and that
survives the bundle, so the opening press does not reach the document handler
that hides every popover. Something else hides it.

SORT AND GROUP BY TAKE MORE THAN ONE LEVEL, like v3. That is the engine as well
as the editor: SetSort and SetGroup replace every level with one today, on
purpose, so a heading and the list could not disagree. With a real level list a
person adds and removes levels, and a heading press sets the first level rather
than replacing the list.

## evidence: and the Go test stays

TestAFilterReadsBackAsWhatWasBuilt covers four shapes over the function, including one that mixes an ored group with a plain one. It is the cheaper of the two and it says what the shape is.

## evidence: checks

sh .se/scratchpad/battery.sh, run immediately before this submission. All ok.

## evidence: driven through the binary, both shapes

Two groups: the file reads and: with status == open and assignee == main, the pane answers 11 rows, and the query answers two groups of one row each. One group of two: the file reads - or: with the two statements under it, the pane answers 13 rows, and the query answers one group of two rows. Before this, the second was read back as one condition whose value ran to the end of the line.

## evidence: finding 1, the filter went one way

The finding is right. The builder wrote one flat statement and the reader reads a structure, so anything past a single condition was written correctly and read back wrong. A person who added a second group had the misread filter handed back to the engine on the next touch of the popover, and their table emptied.

## evidence: it is written in the shape the reader reads

Groups are anded, so several become an and list. Rows inside a group are ored, so several become an or list under one item. One row in one group is one statement, which is what a person writing the file by hand would put there. SetFilterGroups writes it and the verb calls that rather than flattening first.

## evidence: the check is on the round trip and it goes through the verb

A Go test would have stayed green when the verb stopped calling the function, and I proved that: reverting the verb alone left TestAFilterReadsBackAsWhatWasBuilt green. engine-args.mjs sends both shapes through the binary and reads the pane back, and it reports both as failing when the verb writes the flat statement again.

## evidence: two things in the battery that had to be fixed first

A missing check was skipped and the run still said all ok, so any sweep could shrink the battery in silence while every submission cited a green run. A missing check is a failure now, proved by hiding one. And a failed build left .bin/se.exe missing, because the old one was moved aside before the build ran, so every agent's next pull answered no such file. The build goes to a name of its own and the swap happens only when it succeeded, proved by breaking the build and finding the engine still there.

## evidence: what round 1 settled

The popovers open and stay open, the controls send their messages, and the messages reach the engine. That half is unchanged and driven.

## finding 1 · round 1 · Sort and filter do nothing when a person uses them. / SO THE DEFECT IS IN THE WIRING OR IN WHAT THE MESSAGE DOES WHEN IT ARRIVES. Render the page, read what is drawn, and follow one control from the press to the file it writes. · by reviewer4

**wrong:** THE FILTER GOES ONE WAY. A person who builds anything more than a single condition gets it written correctly and read back wrong, and on the next visit to the popover the page hands the wrong thing back to the engine. Both of the two controls the popover offers beyond one condition -- add a condition, add a group -- break on the return leg, and I drove both end to end against a lab tree with an engine built from source minutes ago. ADD A GROUP, AND THE TABLE EMPTIES. The page builds the message correctly: I pressed + Add group in a real DOM and it sent {"type":"filter","groups":[{"rows":[status is open]},{"rows":[assignee is main]}]}. The engine writes it correctly too: the view file reads - status == "open" && assignee == "main" and the pane answers 12 rows. Then ask the engine for the pane again, which is what the page redraws its builder from, and it answers ONE condition whose value is open" && assignee == "main. One touch on any box in the popover sends that back, the file becomes - status == "open\" && assignee == \"main" and the pane answers 0 rows. The person added a second filter group and their table is empty. ADD A CONDITION, AND THE BUILDER GOES DEAD. One group with two rows is written (status == "open" || status == "in_work") and answers 13 rows, correctly. Read back, the engine answers {"rows": null, "raw": "(status == \"open\" || status == \"in_work\")"}, so the popover draws the raw block that says 'Not a condition this builder can draw ... The query is where it is edited.' The two rows the person built are gone from the form the next time they open it. WHY. filterbuild.go:96-115 joins rows inside a group with || in parentheses and groups with &&, and SetFilter writes the whole thing as one list item. FromExpression reads a list item back with reCompare at filterbuild.go:123, whose value group (.+?) is anchored to $ and so must run to the end of the line: on status == "open" && assignee == "main" it captures "open" && assignee == "main" and unquoteValue strips the outer quotes. The parenthesised OR fails the same regex at the opening bracket and falls to Raw. AND THE SAFETY NET IS AIMED AT THE WRONG WRITER. The file's own comment says anything a person wrote that this cannot draw is kept exactly as written rather than rewritten into something it can. That protects hand-written filters. It does not protect the filters this builder itself writes, because the compound slips through reCompare rather than falling to Raw, and the one shape that does reach Raw is one the builder produced and could have drawn. THE REMEDY IS PROVED, not proposed. I rewrote the two groups by hand as two list items -- - status == "open" and - assignee == "main" -- and the same filter answers the same 12 rows AND round-trips exactly: the engine answers two groups with the right properties, operators and values. The list is already ANDed, so nothing else has to change. EXTENT, one pass on what the builder can emit. Three shapes: one row, which round-trips and is the only one the evidence drove; several rows in one group, which becomes raw; and several groups, which is misread and then corrupted. Two of the three are broken and they are the two the popover's own buttons produce. WHAT IS DONE, AND I RAN IT RATHER THAN READING IT. The popover rule holds: all three open on their button, stay open when something inside is pressed, and close on the button again and on a press outside. I pressed every control in the sort and filter popovers in a real DOM: picking a sort property, turning the direction round, dropping a level, picking a grouping, turning its direction, picking a filter property, an operator, typing a value, and removing a condition all send a message. The only ones that send nothing are add-a-level and add-a-condition, which draw a blank row and are right to send nothing until it is filled. And the outbound half of the round trip is real: se view --sort assignee --direction DESC writes the level and se query answers it, and one filter group takes the left pane from 35 rows to 12 while the right pane stays at 35. The evidence says 38 to 9; the tree has gained tokens since, and the shape is the same.

**satisfies:** WRITE EACH GROUP AS ITS OWN LIST ITEM. The filters list is already ANDed, so the && join at filterbuild.go:115 is doing structurally what the file can say for itself, and it is the join that makes the line unreadable. Have SetFilter take the groups and write one item per group. I have run this: the same filter answers the same 12 rows and comes back as two groups with every property, operator and value intact. THEN GIVE A GROUP'S ROWS A SHAPE THAT READS BACK. The reader already understands an or: map (filterbuild.go:222-235) and the writer does not produce one; produce it, or teach the reader the parenthesised form the writer does produce. Either way a person's two conditions must come back as two rows rather than as a raw block, because the raw block tells them to go and edit the query by hand, which is what this token exists to stop. AND MEND THE NET, because the next shape nobody thought of should be kept rather than mangled. reCompare's value must be a single literal -- a quoted string with no unescaped quote inside, or a number, or a bare word -- so anything compound fails to match and falls to Raw. Raw is lossless; a misread is not. THE CHECK, RED TODAY, AND IT HAS TO BE THE ROUND TRIP RATHER THAN EITHER LEG. For every shape the builder can emit -- one row; two rows in one group; two groups; two groups where one has two rows -- compile it, write it to a view, read it back, and require the same groups and rows out as went in. Drive it through the binary so it covers SetFilter's writing as well as filterbuild's compiling. Today shape one passes and the other three do not. Assert the rows and not just the row count, because the two-group case comes back as one row with a value that is almost right, which a count will not catch.

## lesson 1 · round 1 · by reviewer4

**the class:** A writer and a reader of one format, tested in one direction. Every check on this token drives the outbound leg: the control sends a message, the engine writes a file, the pane answers fewer rows. Not one of them reads the file back into the form the person will see next time, and that is where two of the three shapes the builder can produce are lost. The escape hatch meant for this -- keep anything unreadable exactly as written -- was aimed at filters a person typed by hand, so nobody asked whether the builder's own output could slip past it. It can, because the regex that reads a comparison happily swallows a whole compound expression.

**instead:** When code writes a format that the same code reads back, test the round trip, not the two legs. Enumerate every shape the writer can emit -- not the ones you had in mind, the ones the code can produce -- and for each one assert that reading it returns what was written, structure and values both. Do it through the real entry points, so the serialising and the file layer are in the loop. And when there is a fall-back for input the reader cannot understand, check that the writer's own output reaches it: a lenient pattern that matches too much will take a compound expression as a simple one and hand back something almost right, which is worse than the fall-back, because almost right gets written back.

**minted as:** wk-d2f6e959ef

