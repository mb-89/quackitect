---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: commit guard blocks merges
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-06T16:26:33Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 73edfdd023f62a5f5d7c794d8cabdec3f6615d64
---

## detail

The guard refuses a commit that names no path, and a merge commit cannot name one. So concluding a merge is refused, and the refusal tells the hand to do the one thing that would break the merge.

Measured here at 07:44 on branch v4. A merge of origin/v4 conflicted in doc/work/archive.jsonl. With the conflict resolved and staged, `git commit --no-edit` was refused with "A COMMIT CARRIES ONLY THE PATHS IT NAMES". Naming the paths would have made an ordinary commit rather than a merge one, leaving the branch still diverged and the push still refused.

The way through is `GIT_EDITOR=true git merge --continue`, which the guard does not read as a commit. That works, and nothing says so. A hand that does not find it either force-pushes, abandons the merge, or names the paths and makes the divergence worse.

The guard is right about the danger it names. A shared index means a pathless commit can carry another hand's work. A merge in progress is the one case where the index is the thing being committed on purpose.

## proposed action

Let a pathless commit through when a merge is in progress, which is when MERGE_HEAD exists, and say in the refusal otherwise that a merge concludes with git merge --continue.

## done when

- a pathless commit concluding a merge is allowed, decided by: a test with MERGE_HEAD present
- a pathless commit with no merge in progress is still refused, decided by: the same test without MERGE_HEAD
- the refusal names the merge route, decided by: read the refusal text for merge --continue

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A diverged branch can be brought back. Today the only sanctioned route out of a conflict is refused. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | A hand that cannot conclude a merge force-pushes, abandons it, or names paths and deepens the divergence. |  |
| [x] | the ask is small enough to review whole, or it is split first | One condition, one helper, one sentence in the refusal, and two call sites given roots they already hold. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | All three by TestAMergeCommitNamesNoPath, which drives the guard with and without MERGE_HEAD. |  |
| [x] | the basics it stands on exist, or are minted first | Roots reaches both call sites, and MERGE_HEAD is git's own marker. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token, read. Where it was not followed, the line below says so rather than ticking. |  |
| [ ] | one test was written first and seen red for the reason expected | Not met. The test was written before the change and never run against the old code. Two of its assertions would have reddened: no merge route in the refusal, and a pathless commit refused mid-merge. |  |
| [x] | the same test was seen green after the change, and named | TestAMergeCommitNamesNoPath, ok true, beside TestACommitCarriesOnlyItsOwnPaths, which stays green. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | commitpaths.go, hook.go, runverb.go, commitpathsmerge_test.go and this token. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | This clone is behind origin/v4 and the queue passes over wk-6741a3e30a for it. Same divergence, and next to do. |  |

