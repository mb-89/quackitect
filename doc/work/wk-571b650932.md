---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: check cuts quoted pipes
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/main
claimed_at: "2026-09-07T15:09:50Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 72495480588cd465867af982fa2fd5d1b0df5e90
---

## detail

Found working wk-c0930c300e, on a criterion written earlier the same day.

util/checks/criteria-name-a-runnable-command.mjs carries its own pipeline, and it splits on the shell's separators before anything reads quotes. So a pipe inside a quoted regex cuts the command in two, and each half is judged as its own program.

Measured on 2026-09-07. doc/work/wk-0bed3ea63b.md:40 reads: Decided by: se find --regex 'archived-notes-are-gone|commands-mirror-the-keywords|criteria-name-a-runnable-command|the-bundle-is-not-stale' --path util/checks/battery.sh. The check answers FAIL, saying a check under util/checks is one the engine runs. se find is the door the guards name, so nothing about that command is refused.

The blind split leaves a fragment whose quote never closes. The check's own word splitter then swallows the rest of the fragment into one word, and that word carries util/checks/, which is the arm that fires.

THE ENGINE FIXED THIS EXACT DEFECT IN ITS OWN pipeline. src/engine/search.go walks the command once tracking the open quote and cuts only outside one, and wk-8c76f768c1 closed on it. The check reimplemented the older shape by hand, which is the same class of defect the check's own header warns about for the program names.

## proposed action

The check splits a command the way the engine splits one, so a separator inside quotes cuts nothing.

## approach

Mirror src/engine/search.go's pipeline in the check: walk the command once, track the open quote, and cut only at a separator seen outside one. The check already reads its program names off the guard rather than keeping a list, and this is the same argument applied to the rule.

## done when

- a done-when line whose quoted regex holds a pipe is not refused. Decided by: se test --propose criteria-name-a-runnable-command answers ok over the tree, where it answers 1 failed today
- a real separator still cuts the command. Decided by: the same check still refusing a done-when line reading 'Decided by: true && go test ./...'

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

