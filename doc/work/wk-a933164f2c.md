---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: one parallel control
# where the token stands. The process owns these values.
status: open
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e631f7fb6b4c3bff82641794560b85dde71ba599
---

## detail

Staffing holds the main agent until the hands the queue wants have pulled. Today it wants them by a ratio, one worker per four tokens and one reviewer per five verdicts, under two ceilings. The older nudge and wall still speak beside it about the same queue. The owner's ruling: one control, a maximum, and it goes for every role. The engine wants that many of each role as long as there is work for them, never more than the number. It holds the main agent until they are here. The nudge, the wall and the four ratio parameters go. Consumes wk-87de90d82b.

## done when

- limits.parallel_agents is the one number, drawn on the sidebar under agent and engine control. The four ratio parameters, the nudge steps and unreviewed_before_blocked are gone: node util/checks/panel-draws-the-register.mjs . and .bin/se.exe --config
- with the number at three and one open token the engine wants one worker, with twelve open it wants three. With the number at zero it wants none: go test -C src/engine -run TestOneNumberStaffsEveryRole
- a verdict owed wants a reviewer under the same number: the same test

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

