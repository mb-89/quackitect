---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: cloud answers reach chat
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-06T16:10:16Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 941dcaabd44045384b02477b4b39433cbe221400
---

## detail

On a cloud box the record is not what the person reads, and se_answer says nothing about that.

THE OWNER'S WORDS, 2026-09-06: when you answer me with se_answer, you have to do it in the chat, and the engine should remind you of that, because it is hard to see your answers otherwise. It is possible, but it is difficult.

WHAT HAPPENS TODAY. runAnswer in src/engine/recordverbs.go:49 writes the line and prints "recorded". That is the same word on a desk and in the cloud, and it reads as done. On a desk it is: the person is beside the box and the panel shows the record. In the cloud the person reads the chat, so an answer that went only to the record went nowhere they look.

IT IS ALREADY WRITTEN DOWN AND STILL HAPPENS. util/cage/cloud-runner.md carries it as actionable 9, and the owner dated the ruling September 2026. A card handed out at session start is read once, at session start, and an agent forty calls deep is not rereading it. The reminder belongs at the door being used.

THE BOX KNOWS. TheHost in src/engine/host.go:44 answers Cloud off util/cage/hosts.json, which names CLAUDE_CODE_REMOTE among others. One table, read by the cage and the engine both, so this needs no second reading.

## proposed action

runAnswer, where TheHost(c.roots.Method).Cloud is true, prints the reminder instead of the bare "recorded": the answer is in the record, the person on a cloud box does not read the record, so say it in the chat as well.

On a desk it prints what it prints today, because a reminder that fires everywhere is one an agent learns to read past, and on a desk it is not true.

The word for it comes from the host table's own says field where that reads well, rather than a second sentence about clouds kept in the engine.

## done when

- with CLAUDE_CODE_REMOTE=true, runAnswer's output tells the agent to say the answer in the chat: go test -C src/engine -run TestACloudAnswerIsSentToTheChat answers ok
- with none of the cloud variables set, runAnswer's output is what it is today and carries no reminder, decided by the same test's desk half
- the cloud reading comes from TheHost, so the change carries no second list of environment variable names
- the existing record verb tests stay green: go test -C src/engine -run TestAKeywordAnAgentWritesMovesNothing answers ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

