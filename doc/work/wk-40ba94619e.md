---
id: wk-40ba94619e
seq: 1000210
type: work
title: nesting unbalances the rule
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
evidence:
  - outcome
minted_by: main
submitted_by: main
reviewed_by: rev-31
---

## detail

render-check is red on the assertion that the buckets add up to the number on the rule. A child nested under a parent in the same pane is in the total and in no bucket. Group.Count is len(Lines) in src/engine/view.go, taken after nest dropped every child whose parent is on the page. Decision: the total is right and a group counts every line in its subtree. Reproduction is .se/scratchpad/bucket-sum.mjs.

## done when

- A group counts a nested child: a pane holding a parent and its sub-token in one group answers a count of two, asked through the table.
  `rg -q func.TestAGroupCountsANestedChild src/engine && go test -C src/engine -count=1 -run TestAGroupCountsANestedChild$ .`
- The buckets add up to the total whatever is nested: a parent, its sub-token and one unrelated token sum the group counts to exactly the total.
  `rg -q func.TestTheBucketsAddUpToTheTotal src/engine && go test -C src/engine -count=1 -run TestTheBucketsAddUpToTheTotal$ .`
- The standing check is green on this tree.
  `node util/checks/render-check.mjs .`
- Every test named above was watched failing on its own assertion with the fix absent before it was watched passing.

## evidence: outcome

rowsDrawn in src/engine/view.go walks the subtree and nest sets Group.Count and Pinned.Count from it. TestAGroupCountsANestedChild and TestTheBucketsAddUpToTheTotal assert somethingIsNested before counting and were watched red without the walk. render-check answers 0 failed.
