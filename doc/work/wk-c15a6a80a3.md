---
id: wk-c15a6a80a3
seq: 85
type: work
title: a pattern misses members
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: became
successors:
  - wk-02e17b9eb4
minted_by: reviewer6
evidence:
  - outcome
---

## detail

util/checks/engine-spawns.mjs enumerates spawns by a regular expression matching an inline array literal, so src/extension/extension.ts:570, which builds its array on the line above, is invisible. The tree has nine spawns and the check reads eight, and nothing compares that count with anything. Give the check a coarser count of spawn( and spawnRaw( in the same files, minus the ones skipped by name. Fail on the difference. A spawn the pattern cannot read fails with its own message naming the file and line. Related: wk-1412093cd8.

## evidence: outcome

Nothing was built under this id. The token became wk-02e17b9eb4, which carries the coarse count and the class.
