---
id: wk-87de90d82b
seq: 1000046
type: work
title: the wall or nudge
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer11
---

## detail

THE OWNER PICKS BETWEEN THE NUDGE AND THE WALL, AND wk-d1b585b7b9 CLOSED WITHOUT THE QUESTION BEING PUT.

That token's detail said: limits.unreviewed_before_blocked is a wall at one number that keeps firing until somebody obeys it. Decide whether the nudge replaces the wall or the wall stays as a last resort at a higher number. The owner has not said, and it is theirs to say.

WHAT THE WORK DID. It left the wall alone and wrote the answer into the source as settled: src/engine/nudge.go's header now reads IT SITS BESIDE THE WALL RATHER THAN REPLACING IT, and the wall stays as the last resort at its own number. Nothing in that token's evidence says the question was put or answered, so the decision was taken at the keyboard.

AND ONE OF THE TWO OPTIONS IS NOT REACHABLE TODAY, WHICH THE OWNER NEEDS BEFORE PICKING. util/parameters.json caps unreviewed_before_blocked at max 3 with default 3, and its own help says it may be made smaller and never larger. So a higher number is not a setting anybody can turn: it needs the default and the max changed. The wall fires at 3, which is NudgeSteps[0], so the nudge and the wall stand at the same count rather than the nudge speaking earlier.

WHAT IS ASKED. The owner says which of three: the nudge replaces the wall and the wall goes; the wall stays where it is and the nudge speaks at the same count; or the wall moves to a higher number, which also means raising the max in util/parameters.json. Whichever is picked, the sentence in nudge.go's header is rewritten to say what was decided rather than what was assumed.

