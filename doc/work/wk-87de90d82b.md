---
id: wk-87de90d82b
seq: 1000046
type: work
title: the wall or nudge
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer11
---

## detail

wk-d1b585b7b9 asked the owner whether the nudge replaces the wall at limits.unreviewed_before_blocked. It closed without asking, and src/engine/nudge.go's header states the wall stays beside the nudge as if decided. util/parameters.json caps unreviewed_before_blocked at max 3 with default 3, so a higher number is not a setting anybody can turn. The wall fires at 3, which is NudgeSteps[0]. The owner picks one of three. The nudge replaces the wall, or the wall stays and the nudge speaks at the same count, or the wall and the max move higher. Then rewrite the sentence in nudge.go's header to say what was decided.
