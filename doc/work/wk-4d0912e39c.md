---
id: wk-4d0912e39c
seq: 64
type: work
title: drain names what stays
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "Done rather than obsolete: src/engine/retro.go drains .se/log and .se/scratchpad and leaves util/checks alone, guarded by TestARetroLeavesTheChecksAlone and TestTheBatteryIsWholeAfterARetro."
aborted_from: backlogged
minted_by: reviewer4
---

## detail

wk-339068932c drains .se/scratchpad and the harness transcripts, and both folders hold two lifetimes at once. The scratchpad holds one-off scripts beside the standing checks battery.sh runs. The transcript folder holds past sessions beside the live file the harness is still appending to. When a command sweeps a folder, specify the rule by what a thing is, not by where it sits. Name what stays and what goes, and give the sweep a check that a named survivor is still there afterwards. Related: wk-339068932c.
