---
id: wk-4d0912e39c
seq: 64
type: work
title: drain names what stays
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "Done rather than obsolete: se retro names what stays. src/engine/retro.go drains .se/log and .se/scratchpad and touches util/checks nowhere, and two checks say so, TestARetroLeavesTheChecksAlone and TestTheBatteryIsWholeAfterARetro, the second stated as a number because the failure it guards is a battery that shrinks rather than one that disappears. The lesson was that a sweep specified by place cannot tell two lifetimes apart, and the answer was to move one lifetime out."
aborted_from: backlogged
minted_by: reviewer4
---

## detail

A sweep specified by where things live rather than by what they are. wk-339068932c drains .se/scratchpad and the harness transcripts, and both folders hold two kinds of thing at once: one-off scripts a retro wants to read, and the standing checks the project runs before every submission; a session's history, and the live file the harness is still appending to.

MEASURED today: 15 scripts in .se/scratchpad, including battery.sh and every check it runs, and 12 token notes cite sh .se/scratchpad/battery.sh as their evidence. AGENTS.md:232 and doc/guidance/behaviour.md:48 tell every agent to write helper scripts there. The transcript .se/heard.json points at is 22,615,701 bytes and still being written, outside the work folder, and CopyWhatWasHeard seeks into it by byte offset.

WHAT TO DO: when a command sweeps a folder, specify the rule by what a thing IS, not by where it sits. Name what stays and why, name what goes and why, and give the sweep a check that a named survivor is still there afterwards. A folder is a place two unrelated lifetimes share.

Found on wk-339068932c.

