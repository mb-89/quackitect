---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: panel classes have rules
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T17:27:31Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - db30ae5f2c62e9ab54a39e08c53efdb6f317503d
---

## detail

Four of the classes the panel emits carry no rule at all. They are written at src/extension/panel.ts:608 to 611. Nothing draws them deliberately, so how they look is whatever the surrounding cascade happens to do.

The standing check that should have caught this asks only about the toggled classes, not about every class the page emits.

The rules already built for the head row and the doing strip are correct and stay as they are.

## proposed action

Give every class the panel emits a rule.

Widen the standing check, or add a sibling, so it asks about every emitted class rather than the toggled ones. Prove the widening by deleting one existing rule and watching it go red before trusting it.

## done when

- every class panel.ts emits has a rule, the four unruled ones included
- the check asks whether every emitted class has a rule, not only the toggled ones
- the widened check is seen red by deleting one existing rule, then green when it is put back

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

