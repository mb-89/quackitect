---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: panel classes have rules
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-dutilleux
claimed_at: "2026-09-05T15:41:06Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 82d75716108839d9c41cadeeebb7f3c50adf1bac
---

## detail

Four of the classes the panel emits carry no rule at all. They are written at src/extension/panel.ts:608 to 611. Nothing draws them deliberately, so how they look is whatever the surrounding cascade happens to do.

The standing check that should have caught this asks only about the toggled classes, not about every class the page emits.

The rules already built for the head row and the doing strip are correct and stay as they are.

## proposed action

Give every class the panel emits a rule.

Widen the standing check, or add a sibling, so it asks about every emitted class rather than the toggled ones. Prove the widening by deleting one existing rule and watching it go red before trusting it.

## done when

- every class panel.ts emits has a rule, the four unruled ones included
- the check asks whether every emitted class has a rule, not only the toggled ones
- the widened check is seen red by deleting one existing rule, then green when it is put back

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | two rules in panel.ts and one half added to one check | read whole |
| [x] | every done-when line is decidable, and names the command where one decides it | the widened check decides the first two. The third is decided by deleting a rule on a copy and running it again | ./RUNME.sh test --on wk-894fb72f52 --by worker-dutilleux --propose drawn-classes-have-rules |
| [x] | the basics it stands on exist, or are minted first | the check already builds the extension and reads the sheet off the emitted page. panelHtml and everyGroup are exported, and util/parameters.json holds the declaration a cell takes its class from | se find --regex everyGroup |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token, in the prompt | — |
| [x] | one test was written first and seen red for the reason expected | the new half named actor and title, worn by a th the sheet never names. Both are on td too. Deleting the doings rule on a copy reddened it again, and putting it back made it green | 1 of 3 failed |
| [x] | the same test was seen green after the change, and named | drawn-classes-have-rules, ok true, three ok lines. The battery ran after and passed | RUNME test |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | panel.ts gains width rules for actor and title on th and td. The check gains a half over every emitted class | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | the sheet's claim that no rule names a column is corrected here | — |

