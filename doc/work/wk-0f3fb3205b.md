---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: se_status text omits counts
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T16:28:47Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b714fae71affa109fde466fd9e192a35687f041e
---

## detail

A finding on wk-6d74c93848, count wrong results.

The change makes se_status answer with the state of play, which carries "results N, M wrong". What tells an agent that se_status is the place to look was not changed. src/mcp/lane.go:97 still says of the tool: "What the engine knows: the two roots, the log it is writing, and the rules in force." util/cage/tools.json:296 carries the same sentence.

That criterion asked for the numbers where "a reader gets them without knowing a new command". An agent picks a tool off the descriptions, and this one names three things, none of them the count or the state of play. So the numbers arrive, and the only reader who thinks to ask for them is one who already read the diff.

The fix is one sentence on lane.go:97 naming the state of play and the count, and the same sentence regenerated into util/cage/tools.json.

## done when

- src/mcp/lane.go:97 says of se_status that it also answers the state of play and what this session returned, and how much of it was wrong
- util/cage/tools.json carries the same sentence, and node util/checks/mcp-tools.mjs says ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One sentence in src/mcp/lane.go, the same sentence regenerated into util/cage/tools.json, and the test that holds the two together. | |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one: se_test --propose TestSeStatusSaysItAnswersTheStateOfPlay, which reads the description off tools(), the list an agent is actually shown. Line two: the same test compares that with util/cage/tools.json, and se_test --propose util/checks/mcp-tools answers 0 failed. | |
| [x] | the basics it stands on exist, or are minted first | Both stand: the state of play already carries results N, M wrong, and se_status already prints it, because src/mcp/roots.go asks the engine for state. Only what the tool said of itself was missing. | |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rule 12, red first. Rule 13, both halves: the lane's sentence and the cold door's copy, each driven. | |
| [x] | one test was written first and seen red for the reason expected | TestSeStatusSaysItAnswersTheStateOfPlay, red on the lane: se_status does not say "state of play", "returned", "wrong". Then red on the cold door, once the lane said it and tools.json did not. | |
| [x] | the same test was seen green after the change, and named | It green, with util/checks/mcp-tools 0 failed, TestToolSchemasComeFromTheirStructs and TestTheToolSaysWhatTheGuidanceSays. | |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began b714fae71affa109fde466fd9e192a35687f041e, ended on submission. | |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Nothing left over. One trap: the check drives .bin/se-mcp, and a stale one answers the old sentence, which reads as the snapshot being wrong. The engine already reports two names, two files, and RUNME relinks them. | |

