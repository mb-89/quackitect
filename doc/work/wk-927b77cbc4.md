---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: broken filter says nothing
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
---

## detail

FOUND REVIEWING wk-8863048da6, which is not this token's parent because nothing here blocks it.

theQueueFilter in src/engine/queuefilter.go swallows two errors and answers the empty filter for both: LoadValues failing at :38-40, and filter.ParseFilter failing at :45-48. Nothing is recorded and nothing is said.

The file argues for it at :25-29: a half-typed filter would otherwise starve an agent while somebody is still typing, "and the box beside it is where a person sees what they typed".

THAT BOX IS NOT THERE IN THE CASE IT WAS BUILT FOR. The same file's motivation, twelve lines above, says a person on a cloud box "cannot press anything, so the filter is a parameter". So on the one box the feature exists for, nobody sees the box, and a mistyped expression is indistinguishable from no filter at all.

WHAT IT COSTS. A person sets a filter to hand one bucket to an unattended box, gets a word wrong, and the box is handed the whole backlog instead. Both readings look identical from outside: work comes out either way.

The fix is not to refuse the filter. It is to say so: record the unreadable expression where it is read, and let the pull's notice carry it, so the widening is visible where the narrowing was asked for.

## done when

- an unreadable filter is recorded, decided by: a Go test setting work.queue_filter to an expression ParseFilter refuses, then reading the session log for a line naming it
- the queue still hands out everything under an unreadable filter, decided by the same test asserting a token comes back, so the widening is reported and not enforced
- the test reddens first against theQueueFilter as it stands, which records nothing

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

