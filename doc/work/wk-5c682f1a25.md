---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: A close gates nothing
# where the token stands. The process owns these values.
status: done
# who did the work step, so the verdict is never theirs
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 246b128b34aaeac910dcb4161a0b8d8fd9de35a5
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 7308cd4698fe1dae67e2fd79f22258ddbb0e02a7
---

## detail

Nothing checks that a token's tests passed before it closes. The standard process carries a row saying se test on this token answered ok, and an agent writes that row itself. checkEvidence asks only that each row is ticked or carries a sentence. So done means an agent said so. Measured 2026-09-06 while working wk-857e0a4f99, which made the battery answer honestly and then showed that nothing reads the answer at a close.

## proposed action

Gate the close on what the engine ran rather than on what the agent wrote. A token whose last run over its delta did not pass does not reach done.

## done when

- A submission on a token whose last run failed is refused, and a test drives it and reads the refusal.
- A submission on a token whose last run passed is accepted, and a test drives it.
- A submission on a token whose battery has not finished is refused as not yet green, and a test drives it.

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | Done stops meaning an agent said so. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | A red run closes a token, because the row saying it passed is the agent's own. |  |
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | Written before the cut, including the limit it accepts. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | All three by se test naming the two tests in testedgate_test.go. |  |
| [x] | the change is small enough to review whole, or it is split first | One new file, one call in submit, one in runTest. |  |
| [x] | the basics it stands on exist, or are minted first | Tested and ran.Pending are in tests.go, the second from wk-857e0a4f99. |  |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token. Rule 6: the gate was written unwired first, so the red was an assertion and not a build error. |  |
| [x] | the change follows the approach on the token, or the token says why it departed | Yes, limit included. |  |
| [x] | se test --on this token answered ok, and what it ran is named | Five green: the two here plus the standard process, an evidence close, and a close whose archive fails. |  |
| [x] | the note says what changed and why, for a reader who was not here | The commit message carries it, at 47d71a3. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-be226f6ab8. An absent run still passes, and that hole is named rather than left. |  |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | [[reviewing]] was read and applied | — |  |
| [ ] | every hunk of git diff began..ended was read, and any not read is named |  |  |
| [ ] | every criterion's command was run again, and what it said is named |  |  |
| [ ] | every hunk improves the product, or a finding names the one that does not |  |  |
| [ ] | every finding is a trivial token naming this one, and their ids are here |  |  |

## approach

se test writes what it ran against the token. The submission reads it, before the checklist, because a red run is a bigger thing to hear than a row nobody answered.

Three answers, from wk-857e0a4f99. Passed goes through. Failed is refused, naming the first test to fail. Not yet finished is refused as neither.

A token nothing has run is not refused. Every token open today predates the store, so refusing an absent record would refuse the whole queue at once. wk-be226f6ab8 carries closing that.

