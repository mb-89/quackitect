---
id: wk-80935a1f2e
seq: 1000133
type: work
title: states must partition
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-8
---

## detail

A detail asserts a set of states partitions and the criterion drives one fixture per member, so no overlap or gap can go red. Found on wk-42888299f3 at round 2. WORKING and STOPPED are both true together, because AskToStop in src/engine/pull.go refuses a stop once and ClaimStop in src/engine/stop.go never reads what the actor holds. WAITING is defined both as the complement and as the last pull answered wait, which differ after arrival.Reclaim takes a token away. For any set of exclusive states, write the precedence as an ordered list in the detail and name where each state is read from. Give the criterion one fixture where two sources are true at once and one where none is, requiring one named word from each. Define a complement state once, never also as a positive fact.
