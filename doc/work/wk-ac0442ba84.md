---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: onetree test reads log
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e28a96b85f6aeb4552f2304c920afa2f1f741ff7
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 4a4ce81baedb1e5757b2487657b5942cd2582bac
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Found reviewing wk-5e2c601cce. Its second criterion says engine.json's pid and the log's session name the same process after two starts, decided by TestASecondStartAttachesToTheFirst. The test in src/engine/onetree_test.go asserts the pid alone: it waits for engine.json to name a socket and compares its pid with the engine that stayed. Nothing in it opens .se/log, so the second half of the criterion is decided by construction, because the lock is taken before OpenLog, and not by the test.

The measured defect was two halves out of step: engine.json named one pid while session.jsonl carried the other's session. A change that moved HoldTheTree below OpenLog, or let the leaver write a start record before it read the lock, would put the two out of step again and this test would stay green.

## proposed action

In TestASecondStartAttachesToTheFirst, after engine.json names the stayed engine, read .se/log: one session file, named by running.Session, whose start record carries the stayed engine's pid. Two files, or a start record from the leaver, is the failure.

## done when

- the test asserts one session log named by engine.json's session with the stayed pid in its start record, decided by: go test -C src/engine -run ASecondStartAttachesToTheFirst . answers ok
- moving HoldTheTree below OpenLog in main.go makes it fail on the log, seen once and put back

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The second half of the criterion is decided by the test rather than by the order of two lines. | onetree_test.go |
| [x] | what breaks if it is never done, and not only that it stays undone | A change putting the record and the marker out of step again leaves this test green. | the measured defect |
| [x] | the ask is small enough to review whole, or it is split first | — | one test, one assertion |
| [x] | every done-when line is decidable, and names the command where one decides it | One names a go test run, and one names a mutation of main.go. | the done when section |
| [x] | the basics it stands on exist, or are minted first | — | sessionlog.Current and SessionOf were there |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | With sessionlog.Open moved above HoldTheTree, the test failed at onetree_test.go line 92, naming two session files. | go test FAIL 3.975s |
| [x] | the same test was seen green after the change, and named | TestASecondStartAttachesToTheFirst answers ok with the order put back. | go test ok 2.929s |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | the change had landed as b8e91e32, so nothing was written |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The mutation was put back at once, and the worktree reads clean. | git status |

