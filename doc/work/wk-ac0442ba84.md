---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: onetree test reads log
# where the token stands. The process owns these values.
status: open
---

## detail

Found reviewing wk-5e2c601cce. Its second criterion says engine.json's pid and the log's session name the same process after two starts, decided by TestASecondStartAttachesToTheFirst. The test in src/engine/onetree_test.go asserts the pid alone: it waits for engine.json to name a socket and compares its pid with the engine that stayed. Nothing in it opens .se/log, so the second half of the criterion is decided by construction, because the lock is taken before OpenLog, and not by the test.

The measured defect was two halves out of step: engine.json named one pid while session.jsonl carried the other's session. A change that moved HoldTheTree below OpenLog, or let the leaver write a start record before it read the lock, would put the two out of step again and this test would stay green.

## proposed action

In TestASecondStartAttachesToTheFirst, after engine.json names the stayed engine, read .se/log: one session file, named by running.Session, whose start record carries the stayed engine's pid. Two files, or a start record from the leaver, is the failure.

## done when

- the test asserts one session log named by engine.json's session with the stayed pid in its start record, decided by: go test -C src/engine -run ASecondStartAttachesToTheFirst . answers ok
- moving HoldTheTree below OpenLog in main.go makes it fail on the log, seen once and put back

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [x] | the ask is small enough to review whole, or it is split first | one test gains one block reading the log, in one file | b8e91e32 |
| [x] | every done-when line is decidable, and names the command where one decides it | the first by go test, the second by one run with the lock moved, which this token's evidence names | b8e91e32 |
| [x] | the basics it stands on exist, or are minted first | sessionOf, Record and Current are the engine's own, and the start record carries pid | a9a450c7 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read, and each row below names its check and what it said | b8e91e32 |
| [x] | one test was written first and seen red for the reason expected | with HoldTheTree below the start record: start records name pids [2943 2944], and the engine that stayed is 2943 | b8e91e32 |
| [x] | the same test was seen green after the change, and named | TestASecondStartAttachesToTheFirst ok on a9a450c7 with the lock where it is, before the move and after it | b8e91e32 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began is 43f06fef here, and the change is b8e91e32 on a9a450c7, onetree_test.go alone | b8e91e32 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | criterion two departs: below OpenLog alone reddens nothing, since open() writes no record, so the lock went below the start record | b8e91e32 |