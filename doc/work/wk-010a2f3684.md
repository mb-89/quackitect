---
id: wk-010a2f3684
seq: "-8"
type: work
title: the headings stay
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-66a28ca311
rounds: "1"
minted_by: person
evidence:
  - outcome
---

## detail

The heading row with title, status and assignee is missing until a property is added. Draw it unconditionally. Scrolling the rows must not scroll the headings away. Check by driving the page: the headings are present with no property added, name every column the view declares, and stay put on scroll.

## evidence: outcome

The extension never sent the column headings with a data update, so every data change wrote the string undefined into the heading block. extension.ts now sends heads with every body message, and editor.ts refuses to write nothing over headings that are already right. The headings sit outside the scrolling area, .heads is flex 0 0 auto, and thead th is position sticky. node .se/scratchpad/drive-editor.mjs gained eight assertions and prints 41 assertions, 0 failed, and npx tsc --noEmit passes.
