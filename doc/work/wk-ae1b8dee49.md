---
id: wk-ae1b8dee49
seq: "-36"
type: work
title: the separator loses prose
status: spec_open
assignee: main
scope: single-step
traced: true
minted_by: main
---

## detail

The owner said: keep the separator, but the text is not actionable, throw it out. The work editor draws a separator line between the queries and the buckets. It reads the token count followed by prose about the groups and the queries. The prose after the token count goes out. The separator stays.
