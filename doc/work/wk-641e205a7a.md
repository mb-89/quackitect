---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: counts carry their whole
# where the token stands. The process owns these values.
status: closed
author: worker-erin
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 077c790144437cb060ae2683881e3164129d62d4
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - ea12ce0b195947a35f1db01a94ae11a5de4a06d6
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

The person asked the bar badges to show the part over the whole. In work reads 2/21 over in_work plus open, and in review reads 0/1 over in_review plus submitted. Authorized as a direct fix outside the cage and built by cowork. view.go Count gains an outOf filter and Tally an out_of denominator over the same kept rows. work.base declares outOf on both views' counts, and editor.ts draws n/out_of on the pill and the tally span. TestACountSaysWhatItIsOutOf pins part, whole, members staying the part, and bare counts staying bare. This token records that shipped change.

## done when

- TestACountSaysWhatItIsOutOf green
- pills read part over whole in work and review
- bare counts stay bare

## evidence: criterion 1 test green

se test ran src/engine/TestACountSaysWhatItIsOutOf, chosen because it was named outright, and answered ok in 0.09 seconds. It builds a view declaring a count with outOf over four rows and asserts the count reads 1 out of 3.

## evidence: criterion 2 part over whole

util/views/work.base declares outOf on every count in both views. The left view declares it on in hand and on waiting for a person, the right view on in hand. Each reads disposition is empty as the whole. view.go parses outOf into the count and carries the denominator as OutOf, tagged out_of. editor.ts draws the pill and the tally span as n slash out_of. The detail names the views work and review and quotes status pairs as the whole, while work.base now calls them left and right over disposition. The property the criterion asks for holds on both, and those names in the detail are stale.

## evidence: criterion 3 bare counts stay bare

The same test declares a second count with no outOf, and asserts its OutOf is zero. A view that never adopts the field is untouched. editor.ts draws the fraction only where out_of is set and a bare number otherwise, in both the pill and the tally span.

## evidence: the red was taken, not inherited

The test shipped with the change, so its green proved nothing on its own. It was made to go red by stopping view.go from reading outOf. It failed saying one of three is in work and the count says 1 slash 0. That is the whole going missing and nothing else. The break was reverted and git status now answers nothing for src/engine/view.go, so the tree is as it was.

