---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: journal read lives once
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-birch
claimed_at: "2026-09-05T18:40:39Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 239d626e946d4dc3d588246c8e0f5a1b3718cc12
---

## detail

Found reviewing wk-70dde20ba7 (delta is the token's).

src/engine/tests.go, in thePathsWrittenOn, opens every entry under undoDir and unmarshals it into an anonymous struct of its own:

    var named struct {
        On    string `json:"on"`
        Files []struct {
            File string `json:"file"`
        } `json:"files"`
    }
    if err := json.Unmarshal(b, &named); err != nil || named.On != on {

src/engine/apply.go:380 already has readJournal, which does exactly this read, is the reader newestOn uses, and carries the note about the old bare-list shape. The journal's json shape is now declared in two files: journal and wasFile in apply.go, and this partial copy in tests.go.

The dead weight is about fifteen lines and one encoding/json import that tests.go took on for it. The cost of the copy is that a rename of a json tag in apply.go leaves this reader matching nothing, every token falls back to the whole diff, and the bug wk-70dde20ba7 was written to fix is back with nothing said.

The fix is one line: j, err := readJournal(filepath.Join(undoDir(r), e.Name())), then walk j.Files. The blank-name guard the first draft had (if f.File != "") comes back with it.

## proposed action

Call readJournal from thePathsWrittenOn, and delete the second copy of the journal shape.

## done when

- thePathsWrittenOn reads entries through readJournal, and src/engine/tests.go declares no journal shape of its own: se find --regex 'json:"files"' answers only src/engine/apply.go
- the encoding/json import goes from src/engine/tests.go if nothing else there uses it
- se test --on <id> --propose TestTheDeltaIsTheTokensOwn is green

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

