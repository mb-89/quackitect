---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the events table travels
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-berg-two
claimed_at: "2026-09-05T17:08:01Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 1ea68d992233ba59d33a81c5d6d4ed34aa24570b
---

## detail

The cage rests on a table nobody on this box can read, and two open tokens ask for that table to be corrected.

.claude/settings.json and util/cage/claude-settings.json both end their comment with: what each event fires was measured off the record, and the table is on wk-218e541ec2. That token is nowhere. Measured on this box: no note under doc/work or .se/work names it, no row in doc/work/archive.jsonl carries it, no commit message on any branch mentions it, and no tag under archive names it. The spike was a private note on another box, so it never travelled, and the measurement it carried went with it.

So the shipped cage cites a source that cannot be reached, which is the same fault wk-8ab502ee9d and wk-977319bccb were minted about. wk-8ab502ee9d asks for a third legend mark and a desktop Code tab column in that table, and wk-977319bccb asks for the query behind its counts. Neither can be worked while the table is gone, and neither can be closed by inventing numbers.

The counts cannot be recovered by reading. wk-977319bccb already measured that the record carries the engine's own kinds and not the harness event names, so the mapping from a kind to an event was the measurement and it is written down nowhere.

## proposed action

Decide first whether the measurement can be had at all, because everything else waits on that.

If the spike's box can still be reached, recover the note and commit it under doc, then do the three corrections wk-8ab502ee9d asks for. If it cannot, re-drive the events under each harness and write the table fresh, with the query beside it.

Either way the cage comment must stop citing an id that resolves to nothing. Until the table is back, the honest sentence names what is unmeasured rather than pointing at a note.

## done when

- the cage comment names a source that exists: se find on the id it cites answers a file in the tree, or the sentence citing it is gone
- the three table lines moved to [[wk-34e061d80a]] with their criteria: se find on that id answers a note

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | It was not. Four done-when lines, and three needed a box that drives three harnesses. Those three moved to wk-34e061d80a. | doc/work/wk-34e061d80a.md |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one is decided by the check the-cage-cites-what-is-here. The moved line is decided by se find on wk-34e061d80a. | ok true, one file |
| [x] | the basics it stands on exist, or are minted first | The table stands on knowing which harness posted a line, which wk-899d2dbc6c says nothing does. wk-34e061d80a depends on it. | wk-899d2dbc6c |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 5 moved the criteria before the sentence saying where they went. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | util/checks/the-cage-cites-what-is-here.mjs, written before the edit. It failed on both cage files, naming wk-218e541ec2 as in no note and no archive row. | ok false |
| [x] | the same test was seen green after the change, and named | util/checks/the-cage-cites-what-is-here, ok true. no-private-links and the-travelling-cage-cannot-block stayed green beside it. | ok true |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Three paths: the new check, util/cage/claude-settings.json, and .claude/settings.json, which is its projection. | git show --stat |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The lost table is a token of its own, wk-34e061d80a. Nothing else in the cage cites an id the tree lacks. | ok true |

## evidence: step 2, the decision the token asked for first

The proposed action said to decide first whether the measurement can be had. It cannot be had here.

The spike was a private note on another box, and this box cannot reach that one. Nothing under doc/work, .se/work or the archive carries it. The new check reports 259 tokens to resolve against and wk-218e541ec2 among none of them.

Re-driving is no better from here. This box is driven by one surface. It cannot open the editor or the desktop Code tab, and wk-899d2dbc6c already measured that no record line says which surface sent it.

So the cage comment now names what is unmeasured rather than pointing at a note. The three table lines went to wk-34e061d80a, which says what a box needs to work them.

