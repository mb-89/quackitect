---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the engine holds holders
# where the token stands. The process owns these values.
status: open
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 883cda2fbc5b893353599236c5f32aa398a77ecb
  - efce691e33505b33ec2c4161b84c7f9e34e7aa90
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - e6187724a2c989d4903d64cca0c616103d2dd97a
---

## detail

Owner ruling: the holder is engine state, not token content. Today TakeUp writes holder into the token frontmatter. A put-down that never happens leaves a stale name in the file, which the owner just found. The engine already snapshots the tree at take-up and put-down, so it can keep the hold the same way: in its own store under .se, keyed by token and actor, with the token file untouched.

## done when

- TakeUp and PutDown write no holder field into the token file. A test shows the file unchanged apart from began and ended
- se --doing, the queue and the reclaim still know who holds what. go test -C src/engine ./... is green
- a token file carrying a stale holder from before the change is read without the engine believing it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One store in holdstore.go and two call sites in store.go, at 383 and 720. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Three lines. Two are decided by tests in holdstore_test.go. The third names the whole engine suite, which is the project rather than this change. |  |
| [x] | the basics it stands on exist, or are minted first | locked, writeAtomic and currentSession all exist. The store sits beside arrivals.json and looked.json. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. The change was already on origin, so this hand decided it rather than wrote it. |  |
| [x] | one test was written first and seen red for the reason expected | Written on another box with the change. holdstore_test.go carries four, one per clause. |  |
| [x] | the same test was seen green after the change, and named | All four pass on a worktree of origin at 094933fc: TakeUpAndPutDownWriteNoHolderIntoTheFile, TheEngineStillKnowsWhoHoldsWhat, AStaleHolderInTheFileIsNotBelieved, AHoldFromAnEndedSessionIsNotBelieved. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Neither hash is an object here, which is wk-89847112fe. The change is holdstore.go and two call sites. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Clause three is unmet. Eight tests fail, seven already red before this tip, none reading a hold. |  |