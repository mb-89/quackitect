---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the engine holds holders
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-poplar-two
claimed_at: "2026-09-05T20:25:51Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 883cda2fbc5b893353599236c5f32aa398a77ecb
  - efce691e33505b33ec2c4161b84c7f9e34e7aa90
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - e6187724a2c989d4903d64cca0c616103d2dd97a
---

## detail

Owner ruling: the holder is engine state, not token content. Today TakeUp writes holder into the token frontmatter. A put-down that never happens leaves a stale name in the file, which the owner just found. The engine already snapshots the tree at take-up and put-down, so it can keep the hold the same way: in its own store under .se, keyed by token and actor, with the token file untouched.

## done when

- TakeUp and PutDown write no holder field into the token file. A test shows the file unchanged apart from began and ended
- se --doing, the queue and the reclaim still know who holds what. go test -C src/engine ./... is green
- a token file carrying a stale holder from before the change is read without the engine believing it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The queue hands out work instead of answering investigate on a token whose holder ended with its session. | — |
| [x] | what breaks if it is never done, and not only that it stays undone | A hold never put down outlives the agent. Nine tokens here carried a name from a session that had ended. | — |
| [x] | the ask is small enough to review whole, or it is split first | One store, holdstore.go, and store.go writing and reading no holder. Three tests. | — |
| [x] | every done-when line is decidable, and names the command where one decides it | Three lines, decided by go test -C src/engine over the three test names, and by the package run. | — |
| [x] | the basics it stands on exist, or are minted first | The private folder under .se already holds arrivals.json and looked.json. holds.json sits with them. | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove it: the change was already in, so each red was made by inverting the mechanism. | — |
| [x] | one test was written first and seen red for the reason expected | Inverted in a worktree. TestTakeUpAndPutDownWriteNoHolderIntoTheFile failed saying take-up wrote holder worker-1 into the file. TestAStaleHolderInTheFileIsNotBelieved failed on worker-gone. TestTheEngineStillKnowsWhoHoldsWhat failed when the store answered nobody. | — |
| [x] | the same test was seen green after the change, and named | All three pass unchanged, with TestAHoldFromAnEndedSessionIsNotBelieved and TestATokensProseNamesNoHolder beside them. | — |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | The mechanism was already committed. This note is what changed here, and it names the tests. | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The package's seven other failures are retro, publish and verb. None names a hold, and all seven fail at HEAD unchanged. | — |

