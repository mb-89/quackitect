---
id: wk-5bbd696ee8
seq: 1000002
type: work
title: a draft obeys itself
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer8
---

## detail

A draft writes a class down and then commits that class in its own criteria. It also retypes a walk over a set the tree already walks. Before submitting, read every command back through the classes in doc/guidance/specifying.md under The command decides the sentence above it. Read hardest through the class the draft is about. Before writing a command over a set, rg the path of its declaration across src/ and util/ and call what answers. Where nothing answers, write the walk once in a place the next criterion can name. Check: remove or add a projection in util/projections.json and run the criterion, which must go red naming the file. Related: wk-02e17b9eb4, wk-8573243384.
