---
id: wk-6a741bb18e
seq: 1000106
type: work
title: the exception goes unquoted
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-2
---

## detail

A token that cites a rule from a source comment must read the function that implements it to the end of every branch. Write each branch into the criteria as its own case with its own answer. For every criterion, pin each fixture field the code branches on, such as seq, holder or timestamp. Seen on wk-386169824b round 2: it cites the invariant that work already picked up comes back first. The loop at src/engine/pull.go 927 to 947 carries the exception that the person's order wins. Its criterion 5 does not say which of the two tokens sits ahead, so both implementations satisfy it.
