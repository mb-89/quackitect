---
id: wk-ce5d6865e4
seq: 82
type: work
title: an extent is swept
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "The class is written in doc/guidance/specifying.md under The command decides the sentence above it, so the token is obsolete."
aborted_from: backlogged
minted_by: reviewer6
---

## detail

A draft inherits its extent from the finding that reported it rather than measuring it against the file. On wk-24be1c06ae the draft names the reviewer's sample, Says, Runs, Without and Red. src/engine/store.go also truncates a finding's Clause, Wrong and Satisfies at the first newline. Refusing a newline is right for one-line fields and wrong for Wrong and Satisfies, which are paragraphs by design. When drafting from a finding, re-sweep the file before writing the word every, say what was swept, and group fields by shape before choosing. Check, red today: a test walks every field store.go reads back and requires a value to come back byte-identical. Values: a newline, a blank line, the lead, the delimiter, an empty string.
