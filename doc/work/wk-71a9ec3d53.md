---
id: wk-71a9ec3d53
seq: 1000199
type: work
title: any agent takes any
status: imp_in_work
assignee: main
scope: single-step
traced: true
disposition: done
holder: main
rounds: 1
rung: 1
minted_by: main
submitted_by: main
evidence:
  - finding 1
  - finding 2
---

## detail

A worker pull hands out only what is assigned to the puller, so the queue answers wait while open work sits there. Owner's words: every agent can take every open token, and the engine decides what it gives you. Reclaim in src/engine/arrival.go skips a token whose assignee and minted_by are not the actor. The worker pick stops asking about the assignee, and a token in imp_open or spec_open goes to whoever pulls next. yours in the work view still reads assignee == human, the reviewer pick is untouched, and four eyes stays as it is. Related: wk-92664c9150.

## evidence: finding 1

In src/engine/pull.go the implementation pick writes Status, Holder and Assignee to the actor in one line. The spec pick does the same for a SpecOpen token. submit, submitSpec, the two reclaim loops and AskToStop then match on the one name, and aheadOf lost its assignee parameter. TestAWidenedHandoutCanBeSubmitted in src/engine/anyagent_test.go walks HandedOut(RoleWorker), pulls and submits as imp-1, and was red on the assignee clause before the fix.

## evidence: finding 2

TestASecondPullHandsBackTheWidenedToken and TestATokenWhoseHolderWentAwayIsReachableAgain in src/engine/anyagent_test.go were red first and now answer ok. waitNotice ends with Nothing is open. go test on the engine package says ok and bash util/checks/battery.sh ends all ok.
