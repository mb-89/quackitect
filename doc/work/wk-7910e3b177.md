---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: RUNME.sh carries CR
# where the token stands. The process owns these values.
status: closed
author: worker-hollis
# the token this is a part of. It cannot close while this is open
parent: [[wk-faeaddd8a7]]
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b71076935a5a4c86e4c9b48604156d4ebbb16f25
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 699f63e9646b4f6a4e3ff158d2f845e39e1182db
# how it ended. Only an ended token carries one.
disposition: dropped
# why it was dropped
reason: "Minted with the wrong shape, by me, and replaced rather than worked. I minted it as a sub-token of wk-faeaddd8a7 when it is a finding from that token, not a piece of it. An open sub-token blocks its parent from closing: se pull refused wk-faeaddd8a7 with \"it holds 1 open sub-token(s)\". A finding names the token it came from and does not hold it up. The same words are now wk-437137c7a1 with no parent, and the RUNME.sh carriage return is still there to be decided by whoever planted it. Nothing was changed under this id."
# what it became. They have to exist.
successors:
  - "[[wk-437137c7a1]]"
---

## detail

util/checks/scripts-are-lf fails: "RUNME.sh is LF ... it carries a carriage return at byte 86, and sh reads that as part of the line, so the script dies on Linux". Byte 86 ends line 2, which reads "# planted for the battery proof: this line ends in a carriage return". It is uncommitted against HEAD and was planted on purpose by earlier battery work. The check has been red since it was planted, and no session since has answered that check green.

Found while working wk-faeaddd8a7, which edits RUNME.sh further down the same file. Counting every byte equal to 13 in RUNME.sh answers 1, at byte 86, so nothing else in the file carries one and the whole of the failure is that one planted line.

Either the proof it was planted for has been taken and the line goes. Or the proof still wants it, and scripts-are-lf is told this one line is planted. Which of the two is not decidable from the line itself, so whoever planted it decides.

## done when

- util/checks/scripts-are-lf answers ok for RUNME.sh, through se test
- counting bytes equal to 13 in RUNME.sh answers 0, or scripts-are-lf.mjs names the planted line as allowed and says why

