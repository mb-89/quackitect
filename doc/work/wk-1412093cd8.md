---
id: wk-1412093cd8
seq: "-30"
type: work
title: controls reach the engine
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-66a28ca311
rounds: 11
evidence:
  - outcome
minted_by: person
submitted_by: main
---

## detail

Every argument list the extension sends the engine is built in one place and driven against the real engine. The owner typed a token called test into the work controls and the panel sent `se work --form test`. The engine has --title and no --form, so it printed its usage and minted nothing. Nothing reads the array of strings joining the two programs against the flags the engine has. The argument lists live in src/extension/engineargs.ts, one builder per control, with nothing in it that reads a file or knows the editor. A check builds every one and hands it to the real binary in a folder made for the run. It refuses an undefined flag, the engine's usage, or an argument that is not a string, and a refusal about the content is a pass.

## evidence: outcome

engineargs.ts holds one builder per control and util/checks/engine-spawns.mjs finds every spawn of the engine in src/extension, walking subdirectories and keying by file offset. boundToChildProcess reads each file's import from node:child_process and searches for the bound names and their forwarders. It fails by name on a binding it does not search. It catches a two-line object holding --form, a start the pattern cannot parse, and a start under a renamed binding, each answering 1 failed. The clean tree answers 9 read, 0 failed. bash util/checks/battery.sh exits 0 with nineteen runs all ok.
