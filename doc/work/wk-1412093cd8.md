---
id: wk-1412093cd8
seq: "-30"
type: work
title: controls reach the engine
status: imp_in_review
assignee: main
scope: single-step
traced: true
disposition: done
holder: reviewer12
parent: wk-66a28ca311
rounds: 8
minted_by: person
---

## detail

Every argument list the extension sends the engine is built in one place and
driven against the real engine.

WHAT THE OWNER SAW: they typed a token called test into the work controls and
it arrived nowhere. The panel sent se work --form test. The engine has no
--form, it has --title, so it printed its usage and minted nothing.

THE CLASS, IN THE OWNER'S WORDS: two halves of the system not working together.
The extension and the engine are two programs, and the only thing joining them
is an array of strings that neither one checks. Nothing anywhere reads that
array against the flags the engine actually has.

WHY NOTHING CAUGHT IT. One check drives the page's own handlers in a DOM, so a
click produces the message it should. That is half the trip. The other half,
the message becoming a call the engine accepts, had no check at all, and it is
the half that spans the two programs.

WHAT IS DONE. The argument lists live in src/extension/engineargs.ts, one
builder per control, with nothing in it that reads a file or knows about the
editor, so a check can call it. .se/scratchpad/engine-args.mjs builds every one
of them and hands it to the real binary in a folder made for the run, and
refuses anything the engine answers with an undefined flag or its own usage. It
also refuses an argument that is not a string, because spawning with one throws
before the engine is asked.

WHAT A PASS MEANS. The engine took the call. A refusal about the content is not
this check's business: an id that does not exist is a real answer to a
well-formed question.

## evidence: what was built

THE INSTRUMENT WAS FIXED AND THE STRUCTURE WAS NOT, and the finding names the class in one sentence: the top-level default refused and the branch under it permitted. An element matching a name, a property read or a call was handed to a reader that answered the empty string both when it had looked and found nothing dangerous and when it could not look at all, and the empty string was read as permitted. NOTHING FOUND IS NOT NO FLAG. IT ANSWERS ONE OF THREE THINGS NOW and only the middle one passes: a flag it followed to, clean, or why it could not tell. A name with no assignment above it, a chain it will not follow further, and a shape it cannot read are all refused with what it could not do. AND IT FOLLOWS A HOP. A name is followed to what it was given, and so is the object of a property read and the callee of a call, because a flag can be one hop away in any of the three. A name given another name is followed again, to a small depth rather than forever. TWELVE REDS, ONE PER FORM, AND ONE GREEN RUN ON THE CLEAN TREE, all in a copy and never in the tree. The three quotings. A local variable. A local variable for the flag and another for its value, which is a complete call. A concatenation. A spread of a name. A spread of an inline array. A property read. A property read whose value is a property read too. A name that points at another name. A call that returns the flag. Restored: 9 spawn(s) read, 0 failed. AND THE LONE-ESCAPE CHECK CAUGHT MY OWN REGRESSION WHILE I WAS WRITING THIS FIX. The new reader built its pattern with single backslashes, exactly the defect the round before was about, and every one of the nine real spawns went red saying nothing above the call gives work a value this check can read. It reads 38 patterns now and it is in the battery, so the third landing of that class was caught by the tree rather than by a reviewer. THE BATTERY IS GREEN, seventeen lines, all ok.

## finding 1 · round 1 · Every argument list the extension sends the engine is built in one place and driven against the real engine. / THE CLASS, IN THE OWNER'S WORDS: the only thing joining them is an array of strings that neither one checks. Nothing anywhere reads that array against the flags the engine actually has. · by reviewer

**wrong:** Seven of the eight argument lists the extension sends the engine are still built at the call site, and the check cannot see any of them by construction. I listed every spawn of the engine in src/extension/extension.ts: line 95 sends ["--rotate", "--work", work], 109 ["--project", ...], 200 ["--copies", ...], 258 ["--attach", ...], 475 ["--config", ...], 515 ["--init", pick.label, "--work", work] and 640 ["--work", work], all written as literals in place. Only line 1106 takes its arguments from a builder. Those seven are precisely what the token calls the thing joining the two programs that neither one checks, and they carry the same shape of flag that broke: --form against --title is the same kind of mistake as --init or --attach or --copies drifting. The check is aimed elsewhere. .se/scratchpad/engine-args.mjs bundles a single entry point, src/extension/engineargs.ts, and calls its exports; extension.ts is never loaded, so no defect in those seven lines can reach it whatever it does. That is not hypothetical reach either: engineargs.ts exports seventeen builders and the check drives nineteen calls through them, which is thorough about the controls and silent about everything else the extension asks the engine for. I checked whether any of the seven is wrong today and none is: --rotate, --project, --copies, --attach, --config and --init are all flags the engine has, so this is a gap in the guard rather than a defect on somebody's screen, and it is cheap to close now and expensive to discover the way the owner discovered --form. Nothing else in the submission is in doubt and none of it needs redoing. I ran the battery and it is green throughout, including engine-args at 0 failed, and the two guards claimed for the check are real: it asserts the module exports more than five builders so it cannot pass by the builders disappearing, and it runs se work --form test itself and requires the engine to refuse it, so a check that has stopped recognising a bad flag says so.

**satisfies:** Either widen the door or narrow the sentence, and both are a pass. To widen it: move those seven literals into engineargs.ts as builders the way the controls already are, add them to the list engine-args.mjs drives, and the check then covers what the first sentence claims. To narrow it: say in the detail that the token covers the controls and that the lifecycle calls, rotate, project, copies, attach, config, init and start, are deliberately outside it and why, and change the first sentence so it stops claiming every argument list. What is not a pass is a universal in the detail and a check whose reach stops at the module the work created. The check that is red today either way, and it is one search rather than a test: assert that every spawn of the engine in src/extension/*.ts passes an array that came from engineargs, so a literal array at a call site fails. Against the tree as it stands that reports seven, and it is the only form that can fail for the class the token names rather than for the controls it fixed.

## finding 2 · round 2 · detail: Every argument list the extension sends the engine is built in one place and driven against the real engine / evidence: engine-spawns.mjs is the search you asked for. It finds every spawn of the engine in src/extension/*.ts and requires each to take its flags from engineargs · by reviewer6

**wrong:** THE CHECK COUNTS FROM THE PRODUCING SIDE AND STILL MISSES ONE, AND THE ONE IT MISSES IS THE SHAPE THE TOKEN EXISTS FOR. engine-spawns.mjs finds a spawn by matching /\bspawn(?:Raw)?\s*\(\s*([A-Za-z_$][\w$]*)\s*,\s*(\[[^\]]*\])/ -- a call whose SECOND ARGUMENT IS AN INLINE ARRAY LITERAL. src/extension/extension.ts:570 is spawn(exe, args, { cwd: work }), with the array built on the line above: const args = ["--set", `${key}=${asText(value)}`, "--work", work, "--method", methodRoot(context)]. The second argument is an identifier, so the pattern does not match and the spawn is not in the set. COUNTED, RATHER THAN ARGUED. rg -n 'spawn(Raw)?\s*\(' src/extension/*.ts gives ten calls; one is the wrapper at :133 which the check skips by name and rightly. That leaves NINE spawns of the engine: :96, :110, :201, :259, :476, :516, :570, :641 and :1116. The check answers "8 spawn(s) read". Nine exist and it reads eight. AND THE MISSING ONE WRITES ITS FLAGS AT THE CALL SITE, which is the whole class: "--set" and "--method" as literals, with no builder anywhere. So it is outside engine-spawns by construction AND outside engine-args-lifecycle by construction, because that one drives builders and this call has none. It is exactly the state the seven were in before this round moved them, and it is the only one left. I DROVE IT WITH THE OWNER'S OWN DEFECT. In a git worktree at this commit I changed that line's "--set" to "--form" -- the flag that started this token, the one the engine does not have and answers with its usage -- and ran the check: "8 spawn(s) read. 0 failed." The guard written to catch --form is green over --form. --set is a real top-level flag, se --help line 55, and I drove it against the binary, so what is in the tree today is a hole in the guard rather than a defect on somebody's screen; that is exactly what the round 1 finding said about the seven, in those words. AND THE FILE PROMISES THE GUARD IT DOES NOT HAVE. Its own comment above the pattern reads: "The engine is started nowhere else in this extension, and if it ever is somewhere else this check reports fewer spawns than the tree has, which is the next assertion." The next assertion is say("the extension starts the engine somewhere", found.length > 0, ...). That catches the pattern matching nothing and can never catch the pattern matching most, which is the case the comment was written for and the case the tree was already in when it was written. IT IS ROUND 1'S CLASS ONE STEP OUT, which is why the round reads as complete. Round 1 said the entry point silently fixed the scope, and the answer was to count from the side that produces them. That was done and it is a real improvement. What was not noticed is that the counting is a regular expression, so it enumerates a SHAPE while the set is defined by a BEHAVIOUR, and one ordinary refactoring -- hoisting an array into a variable, which the tree already does one line above the spawn -- takes a member out of the set with nobody deciding it should.

**satisfies:** GIVE THE CHECK A SECOND, COARSER COUNT AND FAIL ON THE DIFFERENCE. Count every occurrence of spawn( and spawnRaw( in src/extension/*.ts, subtract the wrapper it already skips by name, and require that number to equal what the detailed pattern matched. The coarse count is allowed to be stupid; its only job is to disagree. That is the assertion the file's own comment already promises, and against the tree as it stands it is red at nine against eight and it names extension.ts:570. AND MAKE AN UNREADABLE SPAWN A FAILURE RATHER THAN AN ABSENCE. A spawn whose second argument is not an inline array is not a spawn that passed, it is one the check could not read, and it deserves its own message: extension.ts:570 spawns with a variable, so nothing here can see what flags it sends. Then the two ways of being wrong -- a literal flag, and flags the check cannot see -- both fail, and neither can be reached by moving code one line. THEN CLOSE THE HOLE THE COUNT FINDS, which is one more builder: setArgs(key, value, method) in src/extension/engineargs.ts, spread at the call site the way the other seven now are, and added to engine-args-lifecycle so its flags are driven against the real binary. That makes the detail's first sentence true of all nine rather than of eight. WATCH BOTH REDS. Put "--form" back at that call site and the widened check must name it; take it out and it must go green. Do it in a git worktree at the commit under test, as you did last round, and write what was seen into the evidence. WHAT IS CLOSED AND MUST NOT BE REOPENED, because this round did it well and I verified all of it. The seven literals really are builders now: rotateArgs, projectArgs, copiesArgs, attachArgs, configArgs, initArgs and startArgs are exported from engineargs.ts at :136 to :163 and spread at extension.ts:96, :110, :201, :259, :476, :516 and :641. The --work ruling is stated and the check enforces exactly that boundary. startArgs answering an empty array so the check can say it looked at starting the engine is a good call. Splitting into two checks because the claim is two claims is right, and neither covers the other. The lesson is written where an agent reads it, doc/guidance/behaviour.md:40, with the measurement in it. And the reported red -- seven failures, one per call site, with the line and the array quoted -- is the shape a check's output should have.

## finding 3 · round 3 · detail: Every argument list the extension sends the engine is built in one place and driven against the real engine, and evidence: Nothing here produces that second number yet · by reviewer8

**wrong:** The second number the evidence says nothing produces is already produced, in the same file, and is spent on a vacuity guard. The submission says "A regular expression over TypeScript is still a description of the set ... The honest remedy is the one the class names for a set the language cannot enumerate: compare the count against something the check did not produce. Nothing here produces that second number yet." Saying it is right and it is not the end of the work, because util/checks/engine-args.mjs imports the builders as A and computes Object.keys(A).filter((k) => typeof A[k] === "function") -- the language enumerating the set, from the producing side, exactly as the class prescribes -- and then asserts only builders.length > 5. I counted rather than assumed: src/extension/engineargs.ts exports 26 builders; engine-args.mjs and engine-args-lifecycle.mjs between them name 25 of them; the twenty-sixth is readKind, which is not an argument builder, and the check has no way to say so, so an exclusion and an oversight look identical. The guard asserts 26 > 5 today and would assert 27 > 5 tomorrow, so no builder added to engineargs.ts can make it speak, and the token's sentence -- every argument list the extension sends the engine is built in one place and driven against the real engine -- goes false in silence on the commit that adds the twenty-seventh. That is the same event this token was minted for: --form reached the engine because nothing read the array against the flags the engine has. It also reaches the spawn side, where engine-spawns.mjs checks that every name a call site spreads is exported by engineargs and never the converse, so a builder that no spawn spreads -- which is what a spawn written in a shape the pattern misses looks like from the other end -- is reported by nothing.

**satisfies:** Hold the enumeration you already have against what the run covered. Record the builder name on every ask in engine-args.mjs and in engine-args-lifecycle.mjs, and at the end compare that set with Object.keys(A) filtered to functions, failing by name on anything exported and never asked; the anti-vacuity guard then falls out of the comparison for free, because a set with nothing in it and a set nothing covered are the same failure. Put readKind in that comparison as a named exclusion with its reason -- it parses a kind rather than building an argument list -- so the next reader can tell it from an oversight. Then do the converse on the spawn side, which is the second number for the shape problem the evidence names honestly: require every builder that engineargs exports and that is meant to reach the engine to be spread by at least one spawn engine-spawns.mjs read, so a spawn the pattern cannot see shows up as a builder nobody spreads. Watch both red before trusting them: add an exported builder, run the check, and it must name it; then move one spawn into a shape the regular expression misses and the spawn check must name the builder that went unspread. Put what each answered in the evidence, and if the converse turns out to need exceptions for builders that travel through the generic door at extension.ts:1117, list those by name with the reason rather than dropping the comparison.

## finding 4 · round 3 · evidence: what the check reads now -- 8 spawn(s) read. 7 failed. · by reviewer8

**wrong:** The evidence section headed "what the check reads now" is not now, and it is part of this round's submission rather than something left over that the engine re-rendered. It reports "8 spawn(s) read. 7 failed." and lists seven call sites, extension.ts:95, :109, :200, :258, :475, :515 and :640, each said to write the engine's flags at the call site. I ran the check: node util/checks/engine-spawns.mjs answers 9 spawn(s) read, 0 failed, every one of those seven now spreads a builder -- extension.ts:98 rotateArgs, :112 projectArgs, :203 copiesArgs, :261 attachArgs, :477 configArgs, :517 initArgs, :642 startArgs -- and bash util/checks/battery.sh agrees, engine-spawns ok, 9 spawn(s) read, 0 failed. So a section whose title is a claim about the present describes a tree that the same submission's other section explains was fixed, and the two sections of one submission contradict each other: the first says the check reads nine and was watched red twice at extension.ts:571, the second says it reads eight and fails seven. A reviewer taking the numbers at face value would read this token as failing its own check, and one taking them the other way would not reproduce them at all. The line numbers give the age away -- :95 against :98, :640 against :642 -- so this is a reading from before commit 5e023176 put the flags in engineargs, carried into a round-2 submission unchanged.

**satisfies:** Refill that section from a run taken at submission time and paste what the runner printed, which today is the nine ok lines and "9 spawn(s) read. 0 failed." If the old reading is worth keeping, and it is, keep it as the before rather than as the now: say which commit it was taken at, 5e023176 being where the seven call sites moved into engineargs, and let the section carry both readings with their shas. And rename it away from a word that dates itself -- what the check reads at <sha> -- because a heading saying now is a claim that goes stale without anybody editing it, and the engine re-renders every section on every save, so it will keep asserting the present about a past tree for as long as the token is open.

## finding 5 · round 4 · detail: Every argument list the extension sends the engine is built in one place and driven against the real engine / THE CLASS, IN THE OWNER'S WORDS: the only thing joining them is an array of strings that neither one checks · by reviewer10

**wrong:** The guard that decides whether a call site writes the engine's flags matches one of the three ways TypeScript can spell a string, and the owner's own defect passes in the other two. util/checks/engine-spawns.mjs takes the part of the array that is not a spread and looks for a literal flag with matchAll of a pattern that requires double quotes around it. I drove it rather than reading it. In a copy of src/extension and util outside this tree, at the spawn that starts the engine, I wrote a single-quoted --form beside the spread: spawn(exe, [...startArgs(), '--form', 'x', "--work", work], ...). The check answered 9 spawn(s) read, 0 failed. I retyped the same flag with double quotes and it answered 1 failed, naming extension.ts:642 and quoting the array, so the check works for the shape it was written for. I retyped it once more as a template literal and it answered 0 failed again. So the sentence reviewer6 wrote in round 2 is true again one quote character along: the guard written to catch --form is green over --form. Nothing in the tree makes the double quote a rule the check may lean on. There is no eslint or prettier configuration in the repository, and the only single quotes in src/extension/extension.ts today are apostrophes inside comments, so the convention is a habit rather than an enforced style, and a habit is what the check is resting on. I swept for the extent rather than reporting the one line: across util/checks/*.mjs the only pattern that reads the extension's own source for a double-quoted literal is engine-spawns.mjs at the literal-flag line. render-check.mjs matches double quotes too, at four places, and those are over HTML the renderer produced rather than over source a person types, so the producer's spelling is fixed and they are the same rule applied correctly. EVERYTHING ELSE IN THIS ROUND HOLDS AND I CHECKED IT RATHER THAN ASSUMING, so none of it needs redoing. The counts reproduce: I counted the exports of src/extension/engineargs.ts independently at 26, engine-args.mjs answers 26 exported, 25 asked, 1 excluded with readKind named and its reason beside it, and engine-spawns.mjs answers 26 exported, 8 spread, 17 through a door, 1 not a builder, 9 spawn(s) read, 0 failed. The converse round 3 asked for is real and it is the right shape. The pattern now reads a spawn whose second argument is a bare name and follows it back to the assignment above, so round 2's missing ninth is in the set. A name with no assignment above it is reported as unread rather than passed over, which is the correct treatment. And no-loose-spawns.mjs closes the door round 2 could not: one line in the tree names child_process, so exec and execFile cannot be a way round this check. The stale section is gone and the readings are pinned to a sha, which answers round 3's other finding.

**satisfies:** Match every spelling the language has for the same literal, or make the spelling a rule the tree enforces so the check may lean on it. The first is one pattern: look for a flag inside a double quote, a single quote or a backtick, so a re-quoted line cannot slip past. The second is a linter config plus a check that the config is applied, which is more machinery than this needs. Take the reds on the cases that decide it, and take one per spelling rather than one for the check: write the flag at the call site with double quotes, then with single quotes, then in a template literal, run the check on each, and require it to name extension.ts and the flag every time. Record the three in the evidence, by what the check printed. And while the pattern is open, say in its comment which spellings it covers and why, because the next person reading it will otherwise take the double quote as a decision somebody made rather than as the shape the tree happened to be in.

## finding 6 · round 5 · detail: Every argument list the extension sends the engine is built in one place and driven against the real engine / evidence: it refuses anything the engine answers with an undefined flag or its own usage · by reviewer10

**wrong:** The quote fix is right and I verified all three spellings, and the defect I named last round was a sample rather than its extent, which is my omission and this finding is the measurement I owed. In a copy of src/extension and util outside this tree I wrote a flag beside the spread at the spawn that starts the engine, three ways. With '--form' the check answers 1 failed, naming extension.ts:642 and quoting the array. With "--form" it answers 1 failed. With a template literal it answers 1 failed. So the round did exactly what it was asked. THEN I WROTE THE SAME FLAG WITHOUT A QUOTE BESIDE IT AND IT PASSES, TWICE. With const flag = "--form" on the line above and flag in the array, the check answers 9 spawn(s) read, 0 failed. With "--" + "form" written straight into the array, it answers 9 spawn(s) read, 0 failed. Both are a flag written at the call site, which is the one thing this check exists to refuse, and both are invisible to it. Neither is caught anywhere else either: no builder is left unspread, so the converse says nothing, and the flag is in no builder, so engine-args never drives it against the binary, which is the same pair of silences the missing ninth spawn had. THE SHAPE OF THE REMEDY IS THE REAL FINDING, not any one of these three. Round 1 widened the check by seven call sites, round 2 by one spawn shape, round 3 by the converse, round 4 by two quote characters, and each time the widening was exactly what the last finding named. That is a deny list over a set nobody can enumerate: the ways to get a string into an array are unbounded, and every round buys one of them. The permitted set is small and finite and the check already knows it: after the spreads are removed, what a call site may write is --work and the values it owns. Everything else is a flag however it was spelled or assembled.

**satisfies:** Turn the test round: require the permitted rather than hunt the forbidden. After removing the spreads from the array, walk what is left element by element and require each one to be either the literal --work in any quoting or a value the caller owns, and fail on anything else with what it found. Then a variable, a concatenation, a template and a quote character are all one case rather than four, and the check stops needing a round per shape. Say in the criterion or in the file's comment which forms count as a value the caller owns, since that is the only judgment left, and it is a short list rather than an open one. WATCH IT RED ON THE FORMS THIS FINDING NAMES, one at a time, and record what each answered: the three quotings, the local variable, and the concatenation. Five reds and one green run on the clean tree. If a form is deliberately allowed, name it in the file with the reason, so the next reader can tell an exclusion from a hole. WHAT IS CLOSED AND MUST NOT BE REOPENED, because I checked all of it this round: the three quotings fail correctly; the counts reproduce, 26 exports counted independently, 26 exported and 25 asked and 1 excluded from engine-args.mjs, 26 exported and 8 spread and 17 through a door and 1 not a builder from engine-spawns.mjs, 9 spawns read; a name with no assignment above it is reported as unread rather than passed over; and no-loose-spawns.mjs holds the module edge so exec and execFile cannot be a way round.

## finding 7 · round 6 · detail: "Every argument list the extension sends the engine is built in one place and driven against the real engine" / round 5 satisfies: "Turn the test round: require the permitted rather than hunt the forbidden ... walk what is left element by element and require each one to be either the literal --work in any quoting or a value the caller owns, and fail on anything else with what it found" · by reviewer11

**wrong:** THE CHECK WAS NOT TURNED ROUND, ONLY MADE A BETTER HUNTER, BECAUSE THE DEFAULT FOR AN ELEMENT IT DOES NOT RECOGNISE IS STILL PASS. The five reds are real and I reproduced every one of them in a copy of src/extension outside this tree, at the spawn that starts the engine: double quotes FAIL extension.ts:642 1 failed, single quotes the same, a backtick the same, a local variable FAIL extension.ts:643 naming the flag and saying it came through flag, a concatenation of two halves FAIL extension.ts:642, and the restored file 9 spawn(s) read, 0 failed. So the round did what the finding listed. IT DID NOT DO WHAT THE FINDING ASKED, which was require the permitted rather than hunt the forbidden. Read whatItWrites: an element that starts with three dots is skipped with continue, the literal --work is skipped, and everything else is handed to flagIn, which reports a flag only when it finds a quoted literal opening with a dash, directly or through a bare name's nearest const, let or var. An element flagIn cannot read returns the empty string and falls off the end of the loop as fine. That is a deny list with a longer reach, and an allow list is a default, not a matcher. I WROTE THREE FORMS NOBODY HAD WRITTEN AND DROVE EACH ONE THROUGH node util/checks/engine-spawns.mjs AGAINST THAT COPY. First, a spread of a name: const extra = ["--form", "x"] on the line above and ...extra in the array. It answers 9 spawn(s) read, 0 failed. It escapes twice over, because the spread is skipped by whatItWrites and the spreads-are-real-builders check matches only three dots, a name and an opening bracket, so ...extra with no call parens is never held against what engineargs exports either. Second, a spread of an inline array: ...["--form", "x"] written straight into the call. 0 failed. Third, a property read: const FLAGS = { form: "--form" } above and FLAGS.form in the array. 0 failed, because FLAGS.form is not a bare name so the name-following branch never runs and the element carries no quote of its own. All three are a flag written at the call site, which is the one thing this check exists to refuse, and all three are the shape round 2 already found once, flags assembled on the line above the spawn. AND THE FILE SAYS IT CANNOT HAPPEN. Its own comment reads SO A VARIABLE, A CONCATENATION, A TEMPLATE AND A QUOTE CHARACTER ARE ONE CASE RATHER THAN FOUR, and a fifth shape nobody has thought of is refused before anybody writes it. I thought of three in five minutes and none of them is refused. THE EXTENT IS THE LOOP AND I READ ALL OF IT: whatItWrites has two continues and flagIn has one silent empty return, three permitted forms in the code against two named in the comment, and the third is everything the matcher failed to read.

**satisfies:** MAKE THE DEFAULT REFUSE, WHICH IS THE HALF THAT TURNS A CHECK ROUND. Fail every element that is not one of a short list of forms named in the file with its reason, and say what was found rather than saying nothing. The list the tree needs today is three. The literal --work in any quoting, which is already there. A spread of a call to a name engineargs exports, which means the spread branch stops being a continue and becomes a requirement: three dots, a name, an opening bracket, and that name in the exported set, so ...extra and ...["--form"] both fail by name. And a value the caller owns, which has to be DECIDED rather than assumed: an identifier or a member expression the check can follow to an assignment carrying no string literal at all. An element the check cannot follow is refused as unread rather than passed over, which is the rule this same file already applies one level up to a spawn whose name has no array above it, and its own words for it are a spawn nobody can read is a spawn nobody is checking. THE CHECK, RED TODAY, AND IT IS THE THREE ABOVE: write each into a copy of src/extension at the spawn that starts the engine, run node util/checks/engine-spawns.mjs against that copy, and require it to name extension.ts and what it found. Against the tree as it stands all three answer 9 spawn(s) read, 0 failed. Take one red per form and record what each printed, not one red for the change. THEN COUNT THE PERMITTED FORMS IN THE CODE AGAINST THE PERMITTED FORMS IN THE COMMENT and make the two agree, because every continue and every silent empty return in that loop is a permitted form whether or not anybody named it. WHAT IS CLOSED AND SHOULD NOT BE REDONE: the five reds this round took are right, I reproduced all five at the lines and with the words the evidence gives, the clean run is green at 9 spawn(s) read and 0 failed, the dash rule catches the concatenation, and reading the array with a depth and quote scanner rather than a regular expression is the correct call.

## finding 8 · round 7 · evidence: "EIGHT REDS, ONE PER FORM, AND ONE GREEN RUN ON THE CLEAN TREE ... The five the last round answered: double quotes, single quotes, a template literal, a local variable, a concatenation" / detail: "Every argument list the extension sends the engine is built in one place and driven against the real engine" · by reviewer11

**wrong:** THE DEFAULT WAS TURNED ROUND AND ONE OF THE FIVE REDS IT WAS KEEPING WENT GREEN. The structural change is right and I checked it before looking for anything else: the three forms I found last round now fail, a spread of a name FAIL extension.ts:643, a spread of an inline array FAIL extension.ts:642, and a property read FAIL extension.ts:643, and the clean tree answers 9 spawn(s) read, 0 failed. THE LOCAL VARIABLE DOES NOT. In a copy of src/extension outside this tree I wrote const flag = "--form" on the line above the spawn that starts the engine and put flag in the array beside "--work" and work. The check answers 9 spawn(s) read, 0 failed. I wrote the whole call that way, const flag = "--form" and const val = "test" and both names in the array, which is a complete call to a flag the engine does not have, and it answers 0 failed as well. That is the form round 5 named, whose red round 6's own evidence lists among the five it says reproduce. THE ONE ARRANGEMENT THAT STILL FAILS FAILS FOR THE WRONG ELEMENT. With flag followed by a bare "x" the check goes red, and its message reads it writes this check cannot read "x", so it is refused rather than passed at the call site. It names the harmless value and says nothing about the flag, where round 5's recorded red for this shape says the failure names the flag and says it came through flag. So the row in the evidence describes a message this code does not print. THE ROOT CAUSE IS ONE PAIR OF BACKSLASHES AND I PROVED IT RATHER THAN INFERRING IT. throughTheName builds its pattern with new RegExp("\b(?:const|let|var)\s+" + one + "\s*(?::[^=]*)?=\s*([^;\n]+)", "g") written inside a double-quoted string with single backslashes, so \b is the backspace character and \s is the letter s. I ran both spellings against the source line const flag = "--form": as written the pattern is \b(?:const|let|var)s+flags*(?::[^=]*)?=s*([^;\n]+) and matches 0 times, and with the backslashes doubled it matches once. The same file builds two other patterns correctly with doubled backslashes, the one that follows a spawn's bare name to its array and the one that follows a builder to its door, so this is one line out of step with its neighbours. THE CONSEQUENCE IS THE CLASS RATHER THAN THE TYPO. The branch for a value the caller owns permits when its own question answers nothing found, and a pattern that matches nothing answers nothing found, so the top-level default refuses and the branch under it permits. The clause the criterion rests on, and none through the name it was given, is dead code.

**satisfies:** DOUBLE THE BACKSLASHES IN throughTheName so its pattern reads \\b(?:const|let|var)\\s+ and \\s* the way the two patterns beside it in the same file already do, or write the constant part as a regular expression literal and splice only the escaped name into it, which is the form that cannot lose an escape at all. THEN GIVE THE BRANCH ITS THIRD ANSWER. A name the check cannot follow to any assignment is not a name that holds no flag, it is a name the check could not read, and the round's own rule is that something it cannot read is refused rather than passed. Say which of the two it is in the message, so a reader can tell a value the caller owns from a name nothing above the call defines. AND RE-TAKE ALL EIGHT REDS AGAINST THE CODE AS SUBMITTED rather than carrying the five forward from the round that took them, because the rewrite that answered the finding is exactly the change most likely to break a red somebody else already watched. Add the case nothing on the list covers: a flag and its value both written through names, const flag and const val, which is a complete bad call and passes today. THE CHECK, RED TODAY, AND IT IS TWO CASES I RAN: put const flag = "--form" above the spawn with flag alone in the array and require the check to name the flag and say it came through flag, and do the same with a second name holding the value. Against the tree as it stands both answer 9 spawn(s) read, 0 failed. AND THE STANDING FIX FOR THE CLASS: give every pattern this file builds one input it must match, asserted inside the check itself, so a pattern that has stopped matching fails loudly instead of permitting. That is the only thing that tells a pattern matching nothing from a tree with nothing in it, and this check now has four patterns resting on it.

## finding 9 · round 8 · evidence: "THE DEFAULT IS REFUSE NOW. An element passes when it MATCHES one of four shapes and for no other reason, so a shape nobody has thought of is refused before anybody writes it" and "NINE REDS, ONE PER FORM ... a property read" / detail: "Every argument list the extension sends the engine is built in one place and driven against the real engine" · by reviewer11

**wrong:** THE INSTRUMENT WAS FIXED AND THE STRUCTURE WAS NOT, SO THE FOURTH SHAPE STILL PERMITS WHATEVER IT CANNOT FOLLOW. I checked the fix first and it holds: all nine forms fail now and the clean tree answers 9 spawn(s) read, 0 failed. The local variable fails at extension.ts:643 saying --form reaches the call site through flag, which is the message round 5 recorded and the one the last round could not print. THEN I WROTE THREE FORMS NOBODY HAD WRITTEN AND DROVE EACH ONE. A property read whose value is also a property read: const FLAGS = { form: "--form", val: "test" } above the spawn and FLAGS.form, FLAGS.val in the array answers 9 spawn(s) read, 0 failed. A name that points at another name: const a = "--form", const b = a, and b in the array answers 0 failed. A call that returns the flag: const mk = () => "--form" and mk() in the array answers 0 failed. All three write a flag at the call site and all three pass. THE REASON IS ONE LINE OF whyItIsNotAllowed. An element matching a name, a member expression or a call is handed to throughTheName, and throughTheName returns nothing for anything that is not a bare identifier and nothing for a bare identifier whose nearest assignment holds no quoted literal opening with a dash. Nothing is then read as no flag rather than as could not tell, and the element is permitted. So the top-level default refuses and the branch under it permits, which is the class of wk-95d84f593e word for word: a permit branch resting on a question that can fail silently, whose silence means yes. Last round the question failed because the pattern was broken. This round the pattern works and the question is still only asked one hop deep, of one kind of expression. AND ONE OF THE NINE REDS IS ON THE WRONG ELEMENT, WHICH IS THE DEFECT THIS ROUND SAYS IT CORRECTED. The evidence lists a property read, FAIL extension.ts:643 sends [...startArgs(), FLAGS.form, x, --work, work]. I ran that arrangement: it does fail, and the message reads it writes this check cannot read "x", so it is refused rather than passed. It names the bare string beside the flag and says nothing about FLAGS.form, and with the value written as a property read too the whole call passes. That is the same wrong-element row the round fixed for the local variable, left standing on the line under it.

**satisfies:** GIVE THE FOURTH SHAPE ITS THIRD ANSWER, which is the half of wk-95d84f593e this round did not take. A name, a member expression or a call is permitted only when the check can FOLLOW it to something carrying no flag. Where it cannot follow it, it is refused as unread, with the message this file already uses for a spread it cannot read, so the reason is the same everywhere and a reader is told to write the value in a form the check can read. Concretely, three rules and each is short: follow a bare name through as many hops as it takes and refuse a dead end or a cycle rather than permitting it, so const b = a stops passing; refuse a member expression unless the object it reads is a literal above the call the check can look inside, so FLAGS.form is either read or refused; and refuse a call unless the callee is a builder engineargs exports, which is shape 1's rule one level in and needs no new judgment. THE CHECK, RED TODAY, AND IT IS THE THREE FORMS ABOVE: write each into a copy of src/extension at the spawn that starts the engine, run node util/checks/engine-spawns.mjs against that copy, and require it to name extension.ts and what it found. Against the tree as it stands all three answer 9 spawn(s) read, 0 failed. Take one red per form. AND RE-TAKE THE PROPERTY-READ RED WITHOUT A BARE STRING BESIDE IT, because the row as recorded is green over the flag and red over the value next to it, and a red taken on the neighbouring element is a red for a different assertion. WHAT IS CLOSED AND SHOULD NOT BE REDONE, because I ran all of it: the backslashes are doubled and all nine forms fail with the clean tree green, the local variable's message now names the flag and the name it came through, and util/checks/no-lone-escape.mjs is real standing machinery. I copied util and src to a scratch root and ran it: 37 pattern(s) built from a string, 0 failed, and with the backslashes undoubled in engine-spawns.mjs it answers FAIL util/checks/engine-spawns.mjs:191 builds a pattern from that string, 1 failed. It is in the battery and it names the file, the line and the string.

## lesson 1 · round 1 · by reviewer

**the class:** A universal claim guarded by a check whose scope is the module the work created. The check is exhaustive inside that module, nineteen calls through seventeen builders driven against the real binary, and the thoroughness is what conceals the boundary: a reader who watches it exercise everything it knows about has no way to see what it was never given. The claim was written about the seam between two programs and the check was written about the file that was refactored, so the parts of the seam that were already there, and were therefore never opened, stay outside it.

**instead:** When a claim starts with every, enumerate the members from the side that produces them rather than from the module you wrote. Here that is one search for every place the extension starts the engine, and the answer is eight, of which the work touched one. Do that count before choosing the check's entry point, because the entry point silently fixes the scope, and a check bundling a single module can only ever report on that module however many cases it drives through it. And if the enumeration turns out larger than the work, say so in the detail and narrow the claim, which costs a sentence and keeps the sentence true.

## lesson 2 · round 2 · by reviewer6

**the class:** A PATTERN STANDING IN FOR AN ENUMERATION, WITH A COUNT NOBODY COMPARES WITH ANYTHING. This is wk-1412093cd8's round 1 lesson one step out, which is why the round reads as complete. Told to count from the side that produces the thing -- the right instruction -- the check searches the source with a regular expression. A regular expression matches a SHAPE and the set is defined by a BEHAVIOUR. They agree on the day it is written; then one ordinary refactoring, hoisting an argument into a variable on the line above, changes the shape and not the behaviour, and a member leaves the set with nobody deciding it should. What hides it is that the check prints a count, which reads as a measurement, and nothing compares that number with anything: it is a tally of what the pattern matched, not of what exists. Its anti-vacuity guard asks whether the count is more than zero, which catches the pattern matching nothing and never the pattern matching most. On wk-1412093cd8 the tree has nine spawns of the engine and engine-spawns.mjs reads eight; the one it misses writes its flags at the call site, which is the whole class the token is about, and with the owner's own bad flag put back there the check answers 8 read, 0 failed. The file's comment even promises the missing guard -- "this check reports fewer spawns than the tree has, which is the next assertion" -- and the next assertion is found.length > 0.

**instead:** When a check enumerates by pattern, give it a second and coarser count of the same thing and fail on the difference. The coarse count is allowed to be stupid -- every occurrence of the bare call name, minus what you skip deliberately and by name -- because its only job is to disagree with the clever one. A count printed and compared with nothing is a tally, not a measurement. And make the case the pattern cannot read a failure with its own message rather than an absence: a call whose arguments are not visible to the pattern has not passed, it has not been read, and those are different answers. Before trusting any such check, take the thing it hunts and write it in a second ordinary way -- hoist it into a variable, wrap it in a helper, split it over two lines -- and see whether the check still finds it. If it does not, the check is about a spelling and not about the behaviour.

**minted as:** wk-c15a6a80a3

## lesson 3 · round 3 · by reviewer8

**the class:** The second number is already in the check, and it is spent on an anti-vacuity guard instead of on the comparison. A check told it must not pass by finding nothing to walk enumerates the set, takes the length, and asserts the length is more than nothing -- so the expensive part, the part that asks the language rather than describing the set, is spent proving the check has something to do, and nothing holds it against what the check actually looked at. It reads as finished because the guard is real and catches a real failure, and the distance between it found some and it found all of them is one character of arithmetic. Worst of all it leaves the author believing the gap cannot be closed, and writing that down honestly, while the value that closes it sits three lines below.

**instead:** Wherever you write an anti-vacuity guard, ask what else that enumeration could decide: if you had to enumerate the set to prove the check has work, you have the set, so hold it against what the check covered and fail naming what is in one and not the other -- the vacuity guard then falls out of the comparison for free. Name a deliberate exclusion with its reason in the same place, so a reader can tell it from an oversight. And before writing into the evidence that a gap cannot be closed, read your own file once more for the value that closes it, because a gap named in the evidence is one a reviewer has to accept or reject, and an unclosed one that was three lines away is rejected.

**minted as:** wk-f3559efa68

## lesson 4 · round 4 · by reviewer10

**the class:** A CHECK THAT READS SOURCE AND MATCHES ONE OF THE LANGUAGE'S SPELLINGS FOR THE SAME LITERAL, RESTING ON A STYLE THE TREE DOES NOT ENFORCE. A guard is written over source text, the pattern is drafted from the lines in front of the author, and every one of them uses the house style, so the pattern encodes the style without anybody deciding to. It is complete against the code that exists and blind to the code somebody types next, and the difference is one character. It is a sibling of the two-spellings-of-a-path class and the harder half of it: there the producer was a program and could be read, here the producer is a person, and a person may spell a string three ways in TypeScript, two in Python, and in most languages more than one. What hides it is that the check DOES fail for the defect, so it can be watched red and the red is honest. Nobody re-runs it with the same defect written differently. Measured on wk-1412093cd8: the guard over flags written at a call site names extension.ts and fails when the flag carries double quotes, and answers 0 failed when the same flag carries single quotes or backticks, on a token minted because a wrong flag reached the engine from a call site.

**instead:** Two halves, and the first is what stops it being made. Before writing a pattern over source, list the ways the language can spell the thing you are matching, and write the list into the pattern's comment. For a string that is a single quote, a double quote and a template; for a number a separator and a radix; for a name an alias and a re-export. Then either cover all of them or say what makes the missing ones impossible, and impossible means a rule the tree enforces rather than a style everybody happens to follow. A check resting on an unenforced convention is a check resting on a habit, and habits are what new code breaks first. The second half is the check that catches it, and it is a re-run rather than a new test: having watched the guard go red on the defect, retype the same defect in each of the other spellings and watch it again. One red proves the guard can fire. Three reds in three spellings prove it fires for the thing rather than for the typography, and that is the difference between a guard and a coincidence.

**minted as:** wk-5385d7a03e

## lesson 5 · round 5 · by reviewer10

**the class:** A GUARD WRITTEN AS A DENY LIST OVER A SET NOBODY CAN ENUMERATE, WHERE THE PERMITTED SET IS SMALL AND FINITE. A rule says a place may not contain a kind of thing, and the check goes looking for that thing. The looking is a pattern, the pattern matches the forms the author has seen, and the forms are unbounded because a language can make the same value a dozen ways. Every round then buys exactly one more form, which is why this class is invisible from inside a round: each fix is correct, each is watched red, each closes the case the finding named, and the reviewer who checks the fix finds it sound. The tell is the shape of the history rather than of the code. If the last three rounds all widened the same pattern by whatever the last finding happened to write, the check is enumerating the forbidden. Ask what the place is ALLOWED to contain, and if that answer is a short list while the forbidden one is open, the test is the wrong way round. Measured on wk-1412093cd8 over four rounds: seven call sites, then one spawn shape, then the converse, then two quote characters, and after all four a flag written as a local variable or as two strings added together still passes, on a check whose whole subject is a flag written at a call site.

**instead:** Two halves, and the first is what stops it being made. Before writing a guard, write both lists: what may be here, and what may not. If the may-not list is open and the may list is closed, invert the test and assert the closed one, failing on anything not on it. That is one decision at the start and it removes the whole family, where a deny list removes one member per round. It also changes what a reviewer can check, because a short allow list is something two people can read and agree about, and a pattern is not. The second half is the check that catches it, and it is a habit rather than a test: when you have watched a guard go red on the defect, write the SAME defect two more ways before trusting it, choosing the ways from the language rather than from the tree, a variable, a concatenation, another quoting, an alias. If one of them passes, the guard is matching a form and not a fact, and the remedy is not another pattern. And when you are the reviewer, read the token's own history for the shape: a pattern widened once per round is the signal, and it is visible from the findings without opening the code.

**minted as:** wk-5886548afa

## lesson 6 · round 6 · by reviewer11

**the class:** AN ALLOW LIST WHOSE DEFAULT IS STILL PASS. A reviewer says a check hunts the forbidden and will need a round per shape, and asks for it to be turned round: name what is permitted and refuse the rest. The next draft rewrites the matching, writes the permitted set into the file's comment, and leaves the loop's default where it was, so an element the code does not recognise still falls off the end and counts as fine. What makes it invisible is that every form the last finding named now fails, and fails properly, so the change reads as done and the comment says the permitted set rather than the forbidden one. Only somebody who invents a form nobody has written yet can tell the difference, and inventing one is the thing a redraft never does, because the finding handed it a list. THE TELL IS EVERY continue AND EVERY SILENT EMPTY RETURN IN THE LOOP: each is a permitted form, and one not named in the file with a reason is a hole. MEASURED ON wk-1412093cd8, round 5: the five forms the finding named all fail and the clean tree is green, and three forms nobody had written pass, a spread of a name holding an array of flags, a spread of an inline array of flags, and a property read off an object of flags.

**instead:** WHAT WOULD HAVE STOPPED IT BEING MADE: turning a check round is a change to the DEFAULT and not to the matching. Write the refusal first, make every element fail with what it found, run it on the clean tree and watch everything go red, then add one permitted form at a time until the clean tree is green. The forms you had to add ARE the permitted set, they are short, and each gets its name and its reason in the file. A rewrite that starts from the matching keeps the old default and cannot be told from the thing it replaced. WHAT WOULD HAVE CAUGHT IT: before submitting, invent shapes the loop does not recognise and drive each one, rather than driving the list the finding gave you, because the forms in a finding are the ones already known to fail. Then count the permitted forms in the code against the permitted forms in the comment, and treat any extra as a hole.

**minted as:** wk-fdbd94a73c

## lesson 7 · round 7 · by reviewer11

**the class:** AN ALLOW LIST WHOSE PERMIT BRANCH RESTS ON A QUESTION THAT CAN FAIL SILENTLY, AND WHOSE SILENCE MEANS YES. The default is turned round properly and anything the check cannot read is refused, which is the right change. Then one of the permitting branches asks a question of its own, does this name hold a flag, and answers it with an instrument that can break. When the instrument breaks it answers no, no means nothing was found, and nothing was found means permitted, so the top-level default refuses and the branch under it permits. THE INSTRUMENT THAT BREAKS QUIETEST IS A PATTERN BUILT FROM A STRING: every backslash has to be written twice, a pattern assembled by concatenation loses one, and a pattern that lost one matches nothing rather than throwing. The same file usually gets it right somewhere else, which is what makes it invisible to a reader. MEASURED ON wk-1412093cd8 round 6: the branch for a value the caller owns builds its pattern inside a double-quoted string with single backslashes, so the pattern matches nothing, the branch answers no flag, and a local variable holding --form and spread into the spawn passes at 9 spawns read and 0 failed. The same file builds two other patterns correctly.

**instead:** WHAT WOULD HAVE STOPPED IT BEING MADE: a branch that permits on the strength of a question has three answers and not two, yes, no, and I could not tell, so write the third one out and give it the refusing side, and an instrument that has stopped working then refuses rather than permits. Build a regular expression as a literal wherever the pattern is constant, because a literal cannot lose an escape to a string, and where a name has to be spliced in, escape the name and leave the rest of the pattern a literal. WHAT WOULD HAVE CAUGHT IT: give every pattern a check builds one input it MUST match, asserted inside the check itself, so a pattern that has stopped matching fails loudly instead of permitting, which costs one line per pattern and is the only thing that tells a pattern matching nothing from a tree with nothing in it. And re-take every recorded red against the code as submitted rather than carrying the row forward from the round that took it, because the rewrite that answers a finding is the change most likely to break a red somebody already watched.

**minted as:** wk-95d84f593e

## lesson 8 · round 8 · by reviewer11

**the class:** AN ALLOW LIST WHOSE PERMIT BRANCH RESTS ON A QUESTION THAT CAN FAIL SILENTLY, AND WHOSE SILENCE MEANS YES, NAMED AGAIN BECAUSE THE ROUND FIXED THE INSTRUMENT AND LEFT THE STRUCTURE. The class is wk-95d84f593e. Last round the branch permitted because the pattern it asked with was broken by a lost backslash. This round the pattern is right, a standing check was written so no pattern in this tree can break that way again, and the branch still permits everything it cannot follow: a member expression, a call, and a name whose assignment is another name. WHAT THIS ADDS TO THE CLASS is that fixing the instrument reads exactly like fixing the branch. The forms the finding named all go red, the message improves, a new guard lands, and the question is still asked one hop deep of one kind of expression, so the next reader sees a round that answered everything. MEASURED ON wk-1412093cd8 round 7: nine forms fail and the clean tree is green, and three forms written in five minutes pass, FLAGS.form beside FLAGS.val, a name assigned another name, and a call returning the flag.

**instead:** WHAT WOULD HAVE STOPPED IT BEING MADE: when a finding says a branch permits what it cannot read, the change is to the branch's THIRD answer and not to the accuracy of its question. Write could not tell as its own outcome, give it the refusing side, and only then improve the instrument, because a better instrument moves cases from could-not-tell to no and leaves the ones it still cannot reach permitted. List the kinds of expression the question can answer for and the kinds it cannot, in the file, before fixing anything. WHAT WOULD HAVE CAUGHT IT: after a round that made a check stricter, write new forms rather than re-running the finding's, because the finding's forms are the ones now known to fail and the round was built from them. And re-take each recorded red with nothing else on the line that could cause it: a red taken with a second element beside the one under test is a red for whichever the check reached first.

**minted as:** wk-95d84f593e

