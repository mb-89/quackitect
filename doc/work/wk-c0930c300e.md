---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: measuring scripts cannot run
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/main
claimed_at: "2026-09-07T15:00:08Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 6143d4771f56f578769dc50a9c4bd38fa6ac2981
---

## detail

Two measuring scripts sit beside the battery's checks: count-standing.py and count-voice-breaks.py. Neither is a battery check, and each carries the command that runs it in its own docstring.

The tests-via-engine guard refuses any command whose text names that folder, and it refuses more than a run: a cat of one of them and a copy of one out of the tree are both refused with the same words. The engine cannot run them either, because it only knows the .mjs names the battery lists.

So a script kept there for a person to run by hand can be written and never run. The measurement it exists for is taken with a copy made outside the tree, and the tree's copy is never exercised.

## proposed action

Decide where a measuring script that is not a battery check belongs, or let the guard pass a command it can see is not a test run.

The guard is guards.tests_via_engine. The battery's own list of checks is read out of battery.sh by checks-live-in-the-method, which counts only the .mjs files, so a .py beside them is already outside that rule.

## done when

- a measuring script that is not a battery check can be run from the tree, and the command that runs it is the one its docstring names
- a battery check is still refused when run outside the engine: the tests-via-engine guard stays on for the .mjs the battery lists

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A script kept for a person to run by hand can be run, and its measurement is taken with the tree's own copy. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | The script is written and never run from the tree. count-voice-breaks.py carries an environment variable that exists only so a copy outside the tree can find the rules. |  |
| [x] | the ask is small enough to review whole, or it is split first | — |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Both are. The guard is ATestRunByHand, and TestNamingACheckIsNotRunningIt drives it in both directions. |  |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [x] | one test was written first and seen red for the reason expected | Five rows into TestNamingACheckIsNotRunningIt, red on two, each on its assertion: refused=true, wanted false, for python util/checks/count-voice-breaks.py doc/glossary.md. |  |
| [x] | the same test was seen green after the change, and named | Green, with TestACommitNamesItsPaths, TestASearchOverTheTreeGoesThroughTheIndex, TestGitCleanIsARemoval, TestARemovalNeedsARead, TestALoopThatDeletesIsRefused and TestTheTestsAreTheEngines. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | 6143d477 is no object here, so the change reads against HEAD: commit e5bbe127. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-571b650932. criteria-name-a-runnable-command splits on a pipe without reading quotes, which is the defect the engine's own pipeline was fixed for. |  |

