---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: tsc cannot read lsp
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-nancarrow
claimed_at: "2026-09-05T13:17:06Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - bbb70d2892b78f5e3bf98bbb20e877214a9c5aca
---

## detail

Run from src/extension, node node_modules/typescript/bin/tsc --noEmit reports TS2307 on lsp.ts line 2. It cannot find vscode-languageclient/node under the moduleResolution this tsconfig sets. The types are on disk, at node_modules/vscode-languageclient/lib/node/main.d.ts, and tsc names them in the same message.

The build uses esbuild, which bundles and does not typecheck. So the build is green and nothing in the tree typechecks the extension at all.

Found while working wk-391ac2bb07, which does not touch lsp.ts.

## done when

- node node_modules/typescript/bin/tsc --noEmit, run from src/extension, exits 0 and reports nothing

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One setting in one file, src/extension/tsconfig.json. Nothing else changed. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | The one done-when names its command, `cd src/extension && node node_modules/typescript/bin/tsc --noEmit`, decided by exit status and empty output. |  |
| [x] | the basics it stands on exist, or are minted first | typescript and vscode-languageclient are both on disk under src/extension/node_modules. `node node_modules/typescript/bin/tsc --version` prints `Version 5.9.3`. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 11 held the change to the ask, rule 12 put the red first. |  |
| [x] | one test was written first and seen red for the reason expected | Before the change `cd src/extension && node node_modules/typescript/bin/tsc --noEmit` exited 2 on `lsp.ts(2,70): error TS2307: Cannot find module 'vscode-languageclient/node'`. Expected, as node10 resolution cannot read that package's exports map. |  |
| [x] | the same test was seen green after the change, and named | That command printed nothing and exited 0 after the change. `moduleResolution: node16` alone was refused by TS5110, which wants module Node16 too, so both moved. `node build.mjs` still exits 0. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/extension/tsconfig.json only. No .ts file or build script touched. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Token wk-c6d6bd7efc, tracked and open. esbuild strips types, so no check reads this tsconfig. |  |

