---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: two guidance rules overrun
# where the token stands. The process owns these values.
status: open
---

## detail

se lint answers a guidance finding on two files, the same shape wk-1da1791ba0 closed on doc/guidance/work-token.md.

doc/guidance/behaviour.md:30, an item in Actionables runs to 31 words and the schema allows 25. It opens "Stage the paths you edited, by ...".

doc/guidance/methods/reviewing.md:31, an item in Actionables runs to 38 words and the schema allows 25. It opens "Read every hunk of `git diff ...".

Measured at head 143797e on this box: ./RUNME.sh lint answered 407 findings, and those two are among them, each with its file and line.

The write door lets these through, because it refuses only a section a write pushes further past its bound, and guidance is not measured at that door the way a token is. So the bound is answered by lint after the fact, and nobody is stopped from adding a word.

The fix is prose in each file. The rule comes back to 25 words or fewer, and what it drops moves into the chapter its number owns in Discussion, so the reader keeps the sentence. Neither Discussion is near its own 1000-word bound: behaviour.md and reviewing.md draw no Discussion finding today.

Found while closing wk-1da1791ba0, whose criteria named the same lint over the third file.

## proposed action

Shorten the one over-long rule in each file to 25 words or fewer, moving what it drops into that rule's Discussion chapter.

## done when

- se lint answers no finding naming doc/guidance/behaviour.md, decided by: ./RUNME.sh lint from the root
- se lint answers no finding naming doc/guidance/methods/reviewing.md, decided by: ./RUNME.sh lint from the root
- both rules keep what they said, decided by: a reader compares the two rules and their Discussion chapters against the same files at 143797e

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

