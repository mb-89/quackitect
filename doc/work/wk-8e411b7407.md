---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: mint swallowed its markup
# where the token stands. The process owns these values.
status: open
claimed_by: f5927132/worker-tallis
claimed_at: "2026-09-06T12:28:00Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 2ddcfe4cd97b2054ed615a0baaaffc8fcb834441
---

## detail

doc/work/wk-ea3fc56219.md has no proposed action chapter. It has one sentence of proposed action sitting inside its detail, behind two literal tags.

The detail's last paragraph ends with a closing detail tag, written out as text. The line under it opens a parameter tag naming proposed_action, and the sentence after that is the proposed action. Both are text in the file rather than markup anything reads.

So the mint took the whole thing as one detail. A reader sees the tags. The schema sees a token with no proposed action, and the detail runs longer than it looks.

The cause is the call that minted it. A tool call's own markup reached the field's value.

The fix on the file is two lines. Cut the two tags, and move the sentence under a proposed action heading of its own, where the schema's order puts it.

Whether the mint can take markup as a value at all is the second half, and it is worth one look before this closes. A caller that can put a tag in a field once can do it again.

Found while reading that token for its approach, on wk-24cdeae29b.

Measured again while writing this. The mint of this token carried a closing tag in its value, and the engine wrote it into the file with the text after it. So the second half is no guess. The door takes markup as a value today, twice in one hour.

## proposed action

Cut the two literal tags out of the detail and give the sentence its own proposed action chapter.

## done when

- doc/work/wk-ea3fc56219.md carries a proposed action chapter, decided by: ./RUNME.sh lint naming no chapter fault on that file
- no literal tag is left in its detail, decided by: se find --regex over that file for an angle bracket
- the sentence the tags hid is the proposed action, decided by: a reader compares it against the same file at 567bf44

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

