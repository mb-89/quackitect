---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: deleted argument still described
# where the token stands. The process owns these values.
status: open
---

## detail

FOUND REVIEWING wk-8863048da6, which is not this token's parent because nothing here blocks it.

That change deleted the stop argument with its file, src/engine/challenge.go. The prose that described it and the tests that carved out of it were left standing, and they still assert it.

WHERE IT STILL SAYS THE ENGINE ARGUES. challenge_test.go:71 and :89-90 say the engine pushes back twice over work in hand, forty lines under that file's own new header saying the argument was deleted. emptyhandsarenotarguedwith_test.go:9-21 and :44-46 say it again. hook.go:1425 says unbound "keeps the claim and the argument", written in the commit that deleted it. relentneedsaclaim_test.go:83 and stop_test.go:158 both read "See challenge.go.", a file that is gone.

AND TWO TESTS ARE SPARE. TestThePersonsWordIsNotArguedWith and TestEmptyHandsAreNotArguedWith assert a carve-out with nothing left to carve out of. TestAValidClaimStopsAtOnce drives every reason with a token held and TestAClaimWithEmptyHandsIsGrantedAtOnce holds the other half.

THE CHANGE KNEW THE JOB. askedisgranted_test.go:47-55 was rewritten to say the argument is gone. Two of the three files did not get that treatment.

WHAT IT COSTS. A reader asking how a stop is decided is told, by the engine's own tests, that it is pushed back on twice.

## done when

- no file in src asserts a live argument over a claim, decided by: se find --regex 'pushed back on twice|pushing back twice|the only thing the engine ever argues with' over src answering nothing
- no file cites challenge.go, decided by: se find --regex 'challenge\.go' over src answering nothing
- the stop rule is still held after the spare tests go, decided by: se test --propose TestAValidClaimStopsAtOnce --propose TestAClaimWithEmptyHandsIsGrantedAtOnce answering ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

