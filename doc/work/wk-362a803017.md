---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: Unleashed catches God too
# where the token stands. The process owns these values.
status: open
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 11cdd67979bcfd756d87db7e74d9c721c078be42
---

## detail

Found reviewing wk-8797959d3c.

src/engine/pull.go guards the hand-out with Unleashed(r), which src/engine/unbound.go answers true for Unbound and for God. wk-8797959d3c asked about unbound and says nothing about God.

Measured on the tree at a2c87a9e, by a probe that binds a tree to God and pulls: the pull answers wait, hands out no token, and its notice reads "This tree is unbound, so the queue hands out nothing." With those 26 lines reverted and nothing else changed, the same pull hands out a token. So God lost the hand-out, and the agent is told it is on a rung it is not on.

The comment in whatComesNext, in the same file, says GOD IS NOT THIS: every refusal is off there, and a queue that refused would be a new one. So one file now says two things about God and the reachable one is the unwritten one.

## proposed action

Decide what the hand-out does on a God tree and write it at one site. Whichever way it goes, no notice a pull answers names a rung the tree is not on.

## done when

- A pull on a God tree answers what the decision says, and a case named god in TestUnboundTakesTheQueueOffEveryPathThatIsTheQueue drives it, decided by: ./RUNME.sh test --on <id> --propose TestUnboundTakesTheQueueOffEveryPathThatIsTheQueue | jq -e '.ok and ((.unreached // []) | length == 0)'
- No notice a pull answers on a God tree contains the word unbound, and the same test reads the notice on the god case, decided by the same command.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

