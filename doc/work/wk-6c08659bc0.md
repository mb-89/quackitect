---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: client finds no method
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T16:52:51Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 8fdb3416016a0ff60f3191d3a9f8529b4d482b3d
---

## detail

From wk-76463660e3, which put the client's reason into the failure message. The reason it printed is this.

TestAVerbRunsInsideTheEngineAndTheClientPrintsIt in src/engine/liveengine_test.go fails saying: engine: no method root here. This program looked up from where it is for a folder carrying src/processes and found none, so every path under the method would be a guess.

FindRoots takes the method root from the folder the program sits in when no method is named. The suite's engine is cached at os.TempDir()/se-engine-<stamp>, from wk-4f8e7e7ebe, and nothing above that folder carries src/processes. On a real box the engine sits inside the method tree, so the walk finds it and this never bit.

So the client is refused before it reaches the engine, and the test cannot pass while it names no method.

## proposed action

Give the two client commands in that test the method root, --method r.Method, the way a real box gives it by where the program sits. Then check whether any other test drives the shared binary from outside a method tree.

## done when

- the client in that test names a method root, decided by: reading the two exec.Command calls in src/engine/liveengine_test.go
- the test passes with an engine answering, decided by: se test --propose TestAVerbRunsInsideTheEngineAndTheClientPrintsIt answers ok
- no other test drives the shared engine from outside a method tree, decided by: se find for theEngine over src/engine and a reading of each hit

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

