---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: gc sweeps archived blob
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-part
claimed_at: "2026-09-05T15:34:44Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 8659942be10f4b83351438dfc85138e69899c695
---

## detail

A finding on wk-808abd40a4, archive without a ref.

keepInGit in src/engine/archive.go writes the note with git hash-object -w and nothing points at the object it makes. The old archive hung a tag off it, so it was reachable and a gc left it alone. Now no ref and no tree reaches it, and git gc --prune=now, git prune or git repack -ad is free to sweep it.

Where the note was committed the row also names on_branch, which the branch keeps reachable, so the loss is invisible. Where it was not, on_branch is empty by onBranch's own rule and the unreachable blob is the only copy there is. A tracked token minted and closed before anybody committed doc/work is exactly that case, and the close deletes the file from the disk.

The comment on OnBranch says a gc is free to sweep the blob, so the hole is known and nothing holds it.

## done when

- a tracked token whose note the branch never committed is closed, git gc --prune=now is run, and ReadArchived still answers it, proved by a Go test in src/engine
- the test is watched red against the code as it stands today

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One call in keepInGit, and the two comments it made untrue. | one hunk of archive.go |
| [x] | every done-when line is decidable, and names the command where one decides it | Both are, and both name the same test: it archives a tracked note the branch never committed, runs the gc, and reads it back. The second line is the red, watched before the call was added. | `se test --on wk-06a6ca318c --propose TestAnArchivedNoteSurvivesAGarbageCollection` |
| [x] | the basics it stands on exist, or are minted first | The anchor namespace was already there in shape: refs/se/steps holds the snapshots, pushed by nothing and read by nothing. Nothing had to be minted. | `const snapshotRefs = "refs/se/steps/"` |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rule 12 for the order. The design the ref had to fit is on archiveRefs: a ref that must be pushed cannot be the archive. | work-token |
| [x] | one test was written first and seen red for the reason expected | TestAnArchivedNoteSurvivesAGarbageCollection, before the anchor: `a gc took the only copy: fatal: Not a valid object name`. It asserts first that the row names no on_branch, so it cannot pass on the branch's copy. | FAIL |
| [x] | the same test was seen green after the change, and named | It passes, with the two holding the design: TestAnArchivedTokenTravelsOnTheBranch and TestAnArchiveNeedsNoRefItCannotWrite. | five ok |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | One update-ref, its constant, a test file, two comments. | `git diff --stat` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change: the comments said nothing held the object. | archive.go |

