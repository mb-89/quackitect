---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: gate assertion matches prose
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/reviewer-elm
claimed_at: "2026-09-07T15:41:12Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 8152cc12b616afc4a04a8b52df67a964d27f3e00
---

## detail

Found reviewing wk-c7d3a72daa (the gate records nothing).

The last assertion of TestTheTestVerbWritesTheRunItRan, in src/engine/testedgate_test.go, decides criterion 2 by looking for two phrases in the submission's findings:

    if strings.Contains(f.Wrong, "did not pass") || strings.Contains(f.Wrong, "had not finished")

Those are the two sentences TestsRefuseTheClose returns, at src/engine/testedgate.go:358 and :367. Today the match is exact and complete, so the assertion is right. Nothing ties it to the gate.

The damage is what happens on the next edit to those sentences. Reword either one, to "did not succeed" or anything else, and the loop matches nothing, the test stays green, and criterion 2 stops being checked by anything. It would go on passing whether or not the gate holds a passing run.

That is the same shape this very token was written to close. Its own detail says the two older tests "pass whether or not the verb ever writes anything", and this assertion is one rename away from joining them.

There is a wording-independent handle already: TestsRefuseTheClose(r, tok) answers the empty string exactly when the gate is not holding the token. Asserting on that, or on a typed reason rather than prose, cannot be disarmed by an edit to the message.

Not raised as a defect: the test asserts the gate did not hold the token rather than that the submission was accepted. The fixture's token carries no evidence, so a full acceptance cannot be asserted there, and the test's comment says so.

## done when

- the assertion cannot be disarmed by rewording the gate, decided by: reword both sentences in testedgate.go and watch TestTheTestVerbWritesTheRunItRan go red
- the gate's refusal is readable without matching prose, decided by: read what TestsRefuseTheClose answers to its callers
- TestTheTestVerbWritesTheRunItRan and TestASubmissionIsRefusedWhenTheRunWasRed both stay green, decided by: se test proposing both by name

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

