---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: v4 misses logbook package
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-nancarrow
claimed_at: "2026-09-05T15:58:40Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 50d591de6b83313174ca864a920ef45b2f16cf26
  - 4a31a4c0e9a86b8179644d313c73eeb23de21988
  - 4bdb4001d52066772aeaad40064b93e05e18c75a
  - 3d4da1b632d2d618901e2db86034a7bdcfff4628
  - c95f8ee8aaf460386069324cdef1131aaf752a8a
  - 13def0c0dc2158f0fda18e5c6728dc034ebb017c
  - a332adc423cd53865dd0e5087aa495e83203fffe
  - 8a7b896564c022c3c311eb8363e5297b06a7bd95
  - 2ac068cd35239ce36d26937a5a80168650461d7e
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 1e38ea3254c6c005e789e800d9e5e46f995424e0
  - 8ef178538e4e85283ba02368a03be09dd4557a8d
  - 1658d333ce6732a34d038e9f8571ee72102a1caa
  - f4ed0104435b63954777c8e09915657df92bf152
  - f597da89e59092b790d9e7a98d6310c2614e235b
  - dbc5628559b51ca404b3e2d723bbcbd10db98156
  - f2bebb2f1c6ec4308d8bf9c831d803fbc4f75f57
  - 42be21eadbb367346d57c941f5de06cb17c759e2
---

## detail

Found while reviewing wk-6d74c93848, but it is nobody's token: origin/v4 does not build.

src/engine/claimsync.go on 52bdc2fa imports quackitect/engine/internal/logbook. No commit on the branch carries that package: git ls-tree -r --name-only HEAD | grep logbook answers nothing, and src/engine/internal on disk has alive, console, frontmatter, quiet, replaced, runme, treelock, version, voice and yaml, but no logbook. Commit df31079a, "claims ride a branch on the remote side", added the import and never added the package.

MEASURED. A clean copy of HEAD, go build ./... in src/engine, answers: claimsync.go:7:2: package quackitect/engine/internal/logbook is not in std. Every test in the package then answers FAIL [setup failed], so the whole engine suite is dark, not only the claims tests.

THE DAMAGE. Every agent on this branch that builds from a clean checkout gets nothing, and every reviewer who tests against a clean copy of HEAD, which is what they are told to do when the worktree is mid-edit, reads a package-wide setup failure instead of the token's own answer. It cost this reviewer one review's worth of digging.

THE CHECK. A check that builds src/engine from a clean checkout of the branch head, rather than from the working tree, catches an import whose package was never committed.

## done when

- git archive HEAD into an empty folder and go build ./... in src/engine answers no error
- src/engine/internal/logbook is committed on v4, or claimsync.go stops importing it
- a check in util/checks builds src/engine from a clean checkout of the branch head and fails when an import names a package no commit carries

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One import and two names in claimsync.go, two call sites, one line in main.go, and one check. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | The first is `git archive HEAD` into an empty folder and `go build ./...` in src/engine, run here. The second is read off claimsync.go. The third is the new check, run against this root. |  |
| [x] | the basics it stands on exist, or are minted first | The engine's own Log and internal/frontmatter are both on the branch, so what the committed file named can be spelled with what a clone gets. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 12 put the red first, and the check is what goes red. |  |
| [x] | one test was written first and seen red for the reason expected | the-branch-head-builds said `FAIL go build ./... in src/engine, over a clean copy of the head`, naming `package quackitect/engine/internal/logbook is not in std`. Expected, as no commit carries it. |  |
| [x] | the same test was seen green after the change, and named | The same check answers ok over the head this commit makes. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | claimsync.go spells Log, Yes, Split and Parse with what the branch carries, and claimverb.go and main.go match the shapes it declares. The check and its battery line are new. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Token wk-d7629cc9af, on two committed test files the branch cannot build. |  |

