---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: three tools skip guard
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-dvorak
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0f449186ae222b6b9b6a1343ff4a0b25e60823bc
  - e6a9b8d9b3f370842f379d765ab3743c8cec6224
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 5694da62b84e6b31e05ba7675847b87a54ed4d7a
  - 56f3416658ad0a0e6af4f4a2a5900daf4af01d0f
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

From the verdict on wk-3747494a3c, which ended the schema/handler drift for the nine tools in theLane.

src/mcp/main.go:108-137 appends three more tools after laneTools(): se_status, se_answer and se_said. None is in theLane, so TestToolSchemasComeFromTheirStructs never walks them: the test iterates laneTools() and indexes theLane, and its handlers map lists only those nine.

So the drift the token exists to end is still open in the same file. se_status at main.go:112 still writes its inputSchema as a literal map, the one shape the token replaced everywhere else. se_answer at main.go:118 advertises schemaOf(answerArgs{}) while call() hands se_answer to recordAnswer, and se_said at main.go:135 advertises schemaOf(saidArgs{}) against recordSaid: nothing holds those pairs together, so pointing either schemaOf at the wrong struct advertises fields the handler will not decode and is caught by nobody. Both handlers were moved into this door by wk-3747494a3c itself.

Put the three into theLane, or into a second table the same test walks, so tools() has no entry outside the guard.

## done when

- every tool tools() advertises is in a table TestToolSchemasComeFromTheirStructs walks, and its handlers map names a handler for each: go test -C src/mcp -run TestToolSchemasComeFromTheirStructs
- no inputSchema in src/mcp is written as a literal outside schema.go: se find --regex 'inputSchema' --path src/mcp/*.go shows only schemaOf calls
- the test fails when a tool is advertised with no entry in the handlers table, watched red before the fix

## evidence: step 1. ask

the ask is small enough to review whole: three files, one table and its guard. Every done-when names its command: se test on TestToolSchemasComeFromTheirStructs, se find --regex inputSchema --path src/mcp/*.go, and the red watched first. The basics all exist already: theLane, laneTools, schemaOf and the guard.

## evidence: step 2. do

doc/guidance/work-token.md read, and rule 12 ordered the change. Red first: the three tools went into theLane with the handlers table untouched, and TestToolSchemasComeFromTheirStructs failed three times, se_status is advertised and this test knows no handler for it, and so for se_answer and se_said. Green after: src/mcp/TestToolSchemasComeFromTheirStructs ok in 0.05s, with TestThePullDoorCarriesEveryFieldThePayloadHas, TestTheToolSaysWhatTheGuidanceSays, util/checks/mcp-tools and util/checks/refusals-name-a-door green beside it. The change is src/mcp/lane.go, main.go and schema_test.go and nothing else. The cleanup is blankLine, which served the literal descriptions this removes.

## note

tools() advertised twelve tools and the guard walked nine. It read laneTools()
and indexed theLane, so the three appended after it were invisible to it. That
is where the drift it exists to end had survived.

tools() is now laneTools() and nothing beside it. se_status, se_answer and
se_said are three more rows in theLane, their descriptions carried over word for
word, each naming the request it takes so its schema is read off the struct its
handler decodes. se_status gets statusArgs, an empty struct, which is the honest
shape for a tool that takes nothing and replaces the one literal inputSchema
left in the door. showStatus wraps status(r) to give it the two-argument shape
every other handler has.

The guard learned two things. Its handlers table names the three. And it counts
tools() against theLane before the walk, so the next tool declared outside the
table is named here rather than slipping past the way these did.

One rule in it had to give. It failed a tool advertising no properties at all,
which se_status legitimately does. An empty properties object is what schema.go
reads off a struct with no fields. A missing one is a schema that was never read
off a request, and that still fails, as does a request that declares fields and
advertises none.

