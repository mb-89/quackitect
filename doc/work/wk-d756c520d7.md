---
id: wk-d756c520d7
seq: 1000181
type: work
title: config, written twice
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-16
---

## detail

A token that moves a constant into config writes the constant again in its own prose deliverable. wk-d898634fd3 moves the criteria ceiling into config, default eight. Its document criterion requires wording that pins the number in doc/guidance/reviewing.md, where the ceiling already appears twice as a literal. When a token moves a value into config, search for that value over every artefact the token touches, prose included. Require zero hits outside the config file and its default. Write the document criterion as a refusal of the literal. The paragraph must state the rule and contain no digit and no number-word for that value. Related: wk-d898634fd3, wk-888a1e0536.
