---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: verdict verification runs
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-birch
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 1aa7f4768802f9d08f2268458567a781d5f1439d
  - ba372a3ef060ae6b9de61bef83dbb1fd89b5efa3
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - beac94fe09465770739a6030690bc23e9e30796c
  - d14d06f24f8d4f1421dce3c7934c05608fb81137
# how it ended. Only an ended token carries one.
disposition: done
# what it became. They have to exist.
successors:
  - "[[wk-dd7e2ca1a6]]"
---

## detail

Carries the commands the reviewer must run to verify the criteria of two standard tokens. They are wk-526ac833fb and wk-c93aac62be, both at status done awaiting a verdict. The resident engine is stale (build 7f22e1a3.210143). It refuses both se_pull and se_run on a token that has ended. So a reviewer cannot run a criterion's command while naming the token it belongs to. This token exists only to give those runs a name. It changes no file. See wk-65c53d4b97 for the stale binary itself.

## done when

- the diff and criterion commands for wk-526ac833fb and wk-c93aac62be have been run and their output carried into the two verdicts

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One token carrying command runs. It writes one evidence chapter into each of the two notes and touches nothing else. | — |
| [x] | every done-when line is decidable, and names the command where one decides it | It names git diff and the criterion commands of both tokens. Every one ran. The output is in the chapter evidence: verification runs on each note. |  |
| [x] | the basics it stands on exist, or are minted first | Both tokens and their began and ended commits exist, so nothing had to be minted first. | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole through se run. Rule 14 asks every command to run again, fresh answer beside what was written. | — |
| [x] | one test was written first and seen red for the reason expected | None. This token re-runs existing criteria. Two re-runs disagree with what the verdicts wrote, and that is the red. |  |
| [x] | the same test was seen green after the change, and named | Nothing was made green. The disagreement is a finding, wk-dd7e2ca1a6, not a defect this token fixes. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | One evidence chapter added to doc/work/wk-526ac833fb.md and to doc/work/wk-c93aac62be.md, through se apply. | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-dd7e2ca1a6: four incidents these tokens landed in the guidance went missing after they ended. | — |

