---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: verdict verification runs
# where the token stands. The process owns these values.
status: open
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 1aa7f4768802f9d08f2268458567a781d5f1439d
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - beac94fe09465770739a6030690bc23e9e30796c
---

## detail

Carries the commands the reviewer must run to verify the criteria of two standard tokens. They are wk-526ac833fb and wk-c93aac62be, both at status done awaiting a verdict. The resident engine is stale (build 7f22e1a3.210143). It refuses both se_pull and se_run on a token that has ended. So a reviewer cannot run a criterion's command while naming the token it belongs to. This token exists only to give those runs a name. It changes no file. See wk-65c53d4b97 for the stale binary itself.

## done when

- the diff and criterion commands for wk-526ac833fb and wk-c93aac62be have been run and their output carried into the two verdicts

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One token carrying command runs only. It changed no file of its own. | — |
| [x] | every done-when line is decidable, and names the command where one decides it | It names git diff and the criterion commands of both standard tokens, and their output is carried into the two verdicts. |  |
| [x] | the basics it stands on exist, or are minted first | Both standard tokens and their began and ended commits exist, so nothing had to be minted first. | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md and methods/reviewing.md were read, and every command ran again before submitting. | — |
| [x] | one test was written first and seen red for the reason expected | None. This token runs commands and changes no file, so there is nothing to drive red. | — |
| [x] | the same test was seen green after the change, and named | None, for the same reason. The runs are the evidence and are named in the two verdicts. | — |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Empty, and expected to be: this token changed no file. | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-ddb9edee8b restored the evidence tables lost from both notes. The findings are wk-f9233114b0, wk-de2256d35c and wk-72ed0f21e3. | — |

