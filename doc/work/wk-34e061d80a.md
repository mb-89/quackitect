---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: events table re-measured
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: cage
# tokens that have to close before this can start
depends_on:
  - "[[wk-899d2dbc6c]]"
---

## detail

Split out of wk-5e31bc7615, which carried four done-when lines. The first is answered and the other three moved here with their criteria.

wk-5e31bc7615 asked whether the lost measurement can be had. Measured on this box: it cannot. The spike wk-218e541ec2 was a private note on another box, and nothing under doc/work, .se/work or doc/work/archive.jsonl carries it. The cage comment no longer cites it, and util/checks/the-cage-cites-what-is-here.mjs holds that.

Re-driving the events is what is left, and it needs a box this is not. Three things are missing here.

One, this box is driven by one surface and cannot open the editor or the desktop Code tab.

Two, nothing says which surface a record line came from. wk-899d2dbc6c already measured that, so it is the basic this stands on.

Three, the record carries the engine's own kinds and not the harness event names. src/engine/hook.go holds the mapping in its switch on in.Event, so a reader can build the query from the code rather than guessing.

## proposed action

Drive each harness on a box that has all three, and count the record lines the switch in src/engine/hook.go writes per event. Write the table and the query into a note under doc, then point the cage comment at it.

## done when

- the table lives somewhere git carries, with a legend of three marks, fires, silent and not measured
- the three cli cells and a desktop Code tab column carry the not measured mark, and no cell carries a dash the prose then takes back
- the way each count was arrived at is on the note beside it, so a reader can run it again

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

