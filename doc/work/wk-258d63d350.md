---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: check reads untracked leftovers
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-dutilleux
claimed_at: "2026-09-05T17:02:25Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - d2c47378f76203d6893273aa60a6afd2e8453c73
---

## detail

TestNoExportedFunctionHereIsUncalled is red on this box and green on the branch, and the difference is a file no commit carries.

src/engine/frontmatter.go is untracked. git status answers two question marks for it, and git ls-files says it matches no file known to git. It is the flat copy of the package that moved to src/engine/internal/frontmatter, left on disk when the move landed. It declares WriteFront, SplitNote and ParseFront, and nothing in the tree calls any of them.

So the check reports: WriteFront in frontmatter.go is exported, nothing calls it, and it is in no exclusion. Every hand on this box reads that failure and it is nobody's work.

MEASURED, on 54dacf1d: se find --regex "func WriteFront" --path "src/**/*.go" answers one hit, src/engine/frontmatter.go line 104. The same file is the only declaration of SplitNote in the tree.

THE CLASS. The check walks the disk under src, so anything a hand leaves in a shared working tree is read as part of the package. A check that reports on files the branch does not carry says nothing a reader can act on.

This is the sibling of wk-0f91286ac1, where a commit named a package no commit carried.

## proposed action

Read src/engine/frontmatter.go, confirm every name in it has a home under src/engine/internal/frontmatter, and take it off the disk.

Then decide whether the check reads the branch rather than the disk. A clean checkout of the branch head is what every other box builds from, and it is what the check is about.

## done when

- src/engine/frontmatter.go is gone: ls src/engine/frontmatter.go answers no such file
- the check is green with no exclusion added for it: se test --on this token --propose TestNoExportedFunctionHereIsUncalled
- nothing lost the names it held: se find --regex "func (WriteFront|SplitNote|ParseFront)" answers one package each

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

