---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: guard misses inline roots
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-borromini
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - a2e52db87a7c40ae85d2184122c1db15fdb572cf
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 76dd36016be989616dfc7a5e985c92dd7e1fbb84
# how it ended. Only an ended token carries one.
disposition: became
# why it was dropped
reason: Any rule that names the three files this token asked for names sixteen more, so its guard could not be green once the change it sanctioned was done. wk-0e43a3c079 carries the measurement and the whole set.
# what it became. They have to exist.
successors:
  - "[[wk-0e43a3c079]]"
---

## detail

Found while giving the verdict on wk-4e91c7b3f8, which this token names.

TestOneFileCarriesTheFixtureBuilders in src/engine/fixture_test.go walks the suite and fails on a function whose result type is Roots. It sees nothing else.

A fixture root written inline is the same fixture by hand, and the guard is blind to it. Twenty-seven such literals are left in engine test files outside the builder.

Several are plainly fixture roots. owns_test.go line 19 makes two temporary folders and pairs them, which is what apart answers. kindschema_test.go line 26 and cage_test.go line 34 pair one root with itself, which is what aTree answers.

So the seam the token bought can be walked around, and the tree already walks around it in those places. The next test file to want a tree may do the same and nothing will say so.

A few of the twenty-seven are fair. A Roots built from two fixed strings for a pure function is not a fixture, and the guard should let those stand.

## done when

- the guard names an inline Roots literal built from a temporary folder outside fixture_test.go, decided by a red run of TestOneFileCarriesTheFixtureBuilders before the callers move
- the fixture roots in owns_test.go, kindschema_test.go and cage_test.go come from the builder, decided by reading those three files
- a Roots built from fixed strings for a pure function still passes the guard, decided by the guard staying green over onedoorperfolder_test.go and log_test.go

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A fixture written by hand stops passing | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | The next file that wants a tree walks around the seam | the detail |
| [ ] | the ask is small enough to review whole, or it is split first | NOT MET: it names three files, and any rule reaching those three reaches nineteen | wk-0e43a3c079 |
| [ ] | every done-when line is decidable, and names the command where one decides it | NOT MET: the third names log_test.go, which is gone | wk-0e43a3c079 |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected | NOT MET: no rule can be green at the size asked, so none was written | below |
| [ ] | the same test was seen green after the change, and named | NOT MET: nothing was changed | — |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | NOT MET: nothing was changed | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

IT BECAME wk-0e43a3c079, which carries the measurement. Thirty-two Roots literals live outside the builder. Nineteen are the fixture shape, and the three this token named are three of those nineteen.

