---
id: wk-cf928952b6
seq: 1000091
type: work
title: one reviewer turns off
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: became
successors:
  - wk-be13363600
minted_by: main
evidence:
  - outcome
---

## detail

The refusal that forces a reviewer to be spawned is turned off by one live reviewer, however long the queue behind it. reviewerMissing in src/engine/pull.go returns Answer{}, false on the first token in review whose holder is still pulling. So len(waiting) against UnreviewedBeforeBlocked is only reached when no reviewer is alive. Build: count the reviewers still pulling, hold that against the number waiting, and refuse when the waiting outnumber them by more than the configured limit. The refusal says how many more to spawn. The StillPulling rule for a dead holder stays. Check, red today, beside the no-reviewer cases in pull_test.go: one reviewer holds one token with several more submitted, and the worker pull is refused.

## evidence: outcome

Nothing was built under this token. It ended as became, and the counted-reviewer refusal continues in wk-be13363600.
