---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the count nothing counts
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
---

## detail

FOUND REVIEWING wk-8863048da6, which deleted the argument over a claim.

THE ONLY WRITER OF THE claimed: COUNT WENT WITH IT. decideStop used to call countRefusedStop(roots, "claimed:"+actor) and grant the third claim. That call is gone. Nothing increments "claimed:"+actor anywhere in the tree now.

WHAT IS LEFT IS TWO CLEARS OF A COUNT NOBODY KEEPS. src/engine/hook.go:823 clears it on every tool call that is not the stop verb, taking a file lock and an atomic write to set zero to zero. src/engine/hook.go:1495 clears it again on the grant. Five tests clear it too.

AND THE COMMENT ABOVE THE FIRST STILL TEACHES THE DELETED RULE, in the present tense: "AND IT PUTS THE ARGUMENT BACK TO ITS START. Three claims earn a stop over open work, and going back to work between two of them is changing your mind, so the run has to be unbroken." A reader of decidePreToolUse learns a rule the engine no longer has.

THE TOKEN'S CRITERION PASSED OVER THIS. It asked that no code name a claim count, and se find for claimsBeforeAStopIsGranted and TheChallenge is clean, because the identifier went and the mechanism it counted stayed.

## proposed action

Delete the two forgetRefusedStops calls on the claimed: key and the comment above the first.

## approach

Delete the `if !isTheStopVerb(in) { forgetRefusedStops(roots, "claimed:"+actor) }` block and its four-line comment at src/engine/hook.go:818-824, and the lone `forgetRefusedStops(roots, "claimed:"+actor)` at src/engine/hook.go:1495 with the paragraph above it that explains it. Drop the same line from the tests that carry it.

## done when

- nothing writes or clears the claimed: key, decided by: se find --regex for claimed: over src/**
- no comment in src names three claims earning a stop, decided by: se find --regex for Three claims over src/**
- the stop tests still pass, decided by: se test proposing TestAValidClaimStopsAtOnce, TestEmptyHandsAreNotArguedWith and TestAskedIsGrantedOnTheFirstClaim

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

