---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: note cites unminted token
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/reviewer-elm
claimed_at: "2026-09-07T15:35:12Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 5eb39d3e21bfdc3c2fde4ff38385f7bc40eb9f13
---

## detail

Found reviewing wk-4e91c7b3f8 (one fixture builder).

doc/work/wk-4e91c7b3f8.md line 99 ticks the step 2 line "the cleanup the change revealed is in the change, or is a token of its own" and gives wk-2493bf564a as its receipt.

wk-2493bf564a does not exist. It is not doc/work/wk-2493bf564a.md, it is not in doc/work/archive.jsonl, .se/work does not exist on this box at all, and `git log --all --grep=wk-2493bf564a` answers nothing. The note's three other ids (wk-0bed3ea63b, wk-1a22aff298, wk-3747494a3c) all resolve.

The damage: the cleanup that line was ticked for is the one criterion 1 revealed, that "one builder answers the roots the engine tests use, decided by: search the test files for TempDir" names no answer a search can be measured against. The worker read that correctly and said so, then recorded it as carried by a token that was never minted. So the cleanup is in nobody's queue and the ticked line rests on nothing.

Fix: mint the cleanup token for criterion 1's undecidable done-when line, and correct the receipt on wk-4e91c7b3f8's note to name it. Or, if the cleanup is not wanted, untick the line and say so.

## done when

- wk-4e91c7b3f8.md line 99 names a work id that resolves, decided by: look for that id under doc/work or as a row in doc/work/archive.jsonl
- the cleanup criterion 1 revealed is in the queue or the note says it is not wanted, decided by: read the step 2 cleanup row on the note
- a check refuses a note whose receipt names a wk- id that resolves nowhere, decided by: run that check over doc/work

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

