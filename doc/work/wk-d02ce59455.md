---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: every kind schemad
# where the token stands. The process owns these values.
status: closed
author: worker-ivo
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e45b9806277ae5b93a1e42f4baa15d3c99ebce60
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 288d676f2b03ab3615033d969e554749e688fd54
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Two kinds exist, work-token and guidance, and each has a schema that drives its template and its refusals. Nothing is missing today, which is why the rule is cheap to add now: it binds nothing and the third kind cannot arrive without one. The owner wants every markdown the system creates to come from a template. Generated files, the output style, the copilot instructions, AGENTS.md and the projections, already do under another name. Prose nobody mints, the README, dev_guide, the extension's walkthrough pages, stays prose. Consumes wk-7f4983c759 and wk-6dc83f3931.

## done when

- a note declaring a kind with no schema is refused at the mint and by the lint, naming the kind. Run: go test -C src/engine -run TestAKindNeedsASchema
- the two kinds that exist pass unchanged: .bin/se.exe lint

## evidence: 1 refused at the mint and by the lint, naming the kind

MET, by go test -C src/engine -run TestAKindNeedsASchema, new in src/engine/kindschema_test.go. It drives BOTH doors, because a rule taught to one is half a mechanism. A kind refused at the mint still lands if somebody writes the file by hand. A kind the lint refuses stays mintable until the mint is told. RED first: 'the mint wrote a note whose kind has no schema, so nothing can read it back'. The lint half was already green before I touched it, and I say so rather than claiming I built it. LintNotes calls LoadSchema, which answers 'no schema for kind %q', and the finding carries that whole sentence. GREEN after: ok, 0.14s. Both halves also assert the message NAMES the kind. The bare 'no schema' leaves a reader hunting for which kind on the page has not got one. A third case holds the guard honest by minting work-token and requiring it through.

## evidence: 2 the two kinds pass unchanged

MET as far as this machine allows. .bin/se.exe lint answers 4 findings, every one in doc/guidance/methods/retro.md: Actionables holds 17 items and the schema allows 15, two items over the 25-word limit, and Discussion at 1134 words against 1000. None is about a kind or a missing schema. That file is being edited by another actor this session, so the findings are theirs and predate me. NOT VERIFIED THE OTHER WAY: .bin/se.exe is the pre-change binary and se lint takes no --method flag. A binary built from my change in the scratchpad would resolve the wrong method root. I did not rebuild .bin, because the engine is running out of it and another token owns the rebuild.

## evidence: NOT DONE: the final re-run is blocked, and not by me

I cannot re-run the Go test now. Two other actors have left src/engine uncompilable since my green: staffing.go:62 cfg.TokensPerWorker undefined (type Config has no field or method TokensPerWorker), and the same for WorkersAtMost, TokensPerReviewer, ReviewersAtMost, which is wk-a933164f2c one parallel control mid-flight. The other is timeline_test.go:55 got.Timeline undefined (type Collected has no field or method Timeline) plus undefined TimelineEntry and splitLines, which is wk-b5170b82a1 weave one timeline. Neither file is in my diff. My own go build of src/engine succeeded before those landed, and TestAKindNeedsASchema went red then green on that compiling tree. A reviewer should re-run the one command once those two land.

## evidence: on se_test and the battery

se_test could not give a whole ruling on this token at all: util/checks/scripts-are-lf.mjs is in the delta from another actor and it is util/checks/**, which triggers the whole battery. The battery answers 'no sh on this machine'. Naming tests outright got the proposal run instead. A pattern does not work: proposing 'Test' came back unreached and fell through to the battery, so tests have to be named exactly.

## evidence: what changed

Three files. src/engine/token.go: TokenKind, the one place the default kind is named. A Kind field on Token, a kind() method, and the guard in Mint. src/engine/store.go: front() writes asLink(t.kind()) instead of the bare string 'work-token', so the writer and the mint read one answer. src/engine/kindschema_test.go: the test, with a fixture that copies the shipped schemas and refuses if they did not copy, so it cannot pass having checked nothing.

## evidence: step 1. ask

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one rule, two doors, three files touched | not split |
| [x] | every done-when line is decidable, and names the command where one decides it | 1: go test -C src/engine -run TestAKindNeedsASchema. 2: .bin/se.exe lint | both run from the root |
| [x] | the basics it stands on exist, or are minted first | LoadSchema and LintNotes already existed. Only the mint door was missing | nothing minted |

## evidence: step 2. do

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token 13, both halves: the test drives the mint door and the lint door, not one | applied |
| [x] | one test was written first and seen red for the reason expected | TestAKindNeedsASchema said "the mint wrote a note whose kind has no schema, so nothing can read it back" | red before the guard |
| [x] | the same test was seen green after the change, and named | TestAKindNeedsASchema ok. A later re-run is blocked by two other actors, named in the evidence on the pull | green while the tree compiled |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | the engine wrote began |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | the kind was a bare string in the writer. It is TokenKind in one place now | in this diff |

## evidence: why the guard asks about the declared kind

The guard fires on the kind the caller DECLARED, not on the default. That is deliberate and it is the token's own framing: the rule binds nothing today and stops the third kind arriving without a schema. MEASURED before choosing: there are 28 Mint call sites in the suite across bespoke fixtures, and only aTreeWithTheProcesses copies src/schemas. Making the default prove itself at the mint would have refused most of those fixtures and said nothing whatever about kinds. The lint is the door that asks this of every note on disk, whoever wrote it and whether or not it came through the mint.

