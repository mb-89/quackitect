---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: move fixture reads uid
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-nono-two
claimed_at: "2026-09-05T19:48:08Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b43860fa31619ba59dec74650727c33476dd380a
---

## detail

TestAMoveReportsTheFileItCouldNotRewrite in src/engine/move_test.go is red in the battery on this box: `move_test.go:72: the file that could not be saved is not reported: []`. It appeared between .se/tests/battery-20260905-160529.out and battery-20260905-162527.out.

The fixture makes a file 0444 and its folder 0555 and expects writeAtomic to fail. The battery runs as root on the cloud boxes, and root ignores both. Measured here: id -u answers 0, a write through a 0444 file succeeds, and a create in a 0555 folder succeeds. So the save goes through, nothing is unwritten, and the test fails saying the verb swallowed a failure it was never handed.

The verb is right. What it promises is that a save it could not make is reported, and no save failed.

writeAtomic puts its temp file beside the target, named after it, with a dot, a random number and .tmp added. A target whose own name is 250 characters leaves no room for that suffix under the 255 a name may hold, so the create fails with a name too long, for root as for anybody.

## proposed action

Build the fixture out of a name too long to write beside rather than out of permissions.

## done when

- go test -C src/engine -run TestAMoveReportsTheFileItCouldNotRewrite answers ok when run as root and when run as an ordinary user
- the test still fails if MoveFile drops the save error instead of reporting it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The test answers the same on every box. It read the uid before, so one commit was green on a desk and red on a cloud box. | `id -u` answers 0 here |
| [x] | what breaks if it is never done, and not only that it stays undone | A red the verb never caused stays in the battery, and a reader learns to run past it. | battery-20260905-162527.out |
| [x] | the ask is small enough to review whole, or it is split first | One fixture in one test. The verb is untouched. | `git show --stat 46025860` |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one is the test under two uids. Line two is a mutation of MoveFile. Both were run. | `go test . -run TestAMoveReportsTheFileItCouldNotRewrite` |
| [x] | the basics it stands on exist, or are minted first | writeAtomic and put are already in the tree | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 14 sent every reading back through the instrument here. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | Re-measured by mutation. With the save error dropped in MoveFile it failed, saying the file that could not be saved is not reported. | `go test . -run TestAMoveReportsTheFileItCouldNotRewrite` |
| [x] | the same test was seen green after the change, and named | That test passes at origin/v4 as uid 0, and as uid 65534 through setpriv. | the same test, two uids |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | 46025860 is an ancestor of origin/v4. The fixture is a 250 character name. | `git merge-base --is-ancestor` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The chmods went with their cleanup hook. The disk held a stale copy, put back. | `git status` on move_test.go |

