---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: flat yaml version go
# where the token stands. The process owns these values.
status: open
# true when this waits for a person rather than an agent
needs_human: true
# what has to be true before this is worth reading again
ready_when: a hand the harness lets delete removes src/engine/yaml.go and src/engine/version.go, in the commit that lands the move
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 97c92fab665a6aca50d892dc174204cab6668b85
  - dd578950db86408be0c6078f1e53ff69739257a0
  - afb5093eafcc271133f5270f3612ed5df38713d6
  - 1a9334cbf96dd3ef0bd2e29bdf96df6cbfa5307c
  - e297ec094a8c19ff70d93e398c1acd3fb7e859ad
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 990a67052b114508eee521acc1d3de1e36aca0bc
  - f87efe853399bdb009c2d9b5b79dd24e92b0d27d
  - ae917722b4076054a9f246ab5cf412635e314f26
  - 5572c7416243f9a9c21453cc0b0577c3821869b1
---

## detail

Found reviewing wk-40abb881a7. That token moved src/engine/yaml.go to src/engine/internal/yaml and src/engine/version.go's Build to src/engine/internal/version, and every caller now reads yaml.Parse, yaml.Map, yaml.List, yaml.Str, yaml.Strs and version.Build. Commit cf8e1d4e then put src/engine/yaml.go and src/engine/version.go back, because origin/v4 without the move did not build.

With the move in the tree, both flat files are dead weight: se find --regex "\b(ylist|ymap|ystr|ystrs|ParseYAML)\(" --path "src/engine/*.go" answers only lines inside src/engine/yaml.go itself, and nothing reads the package main Build any more. Two readers for one format is how the next fix lands in the copy nobody calls.

The deletion belongs in the same commit that lands the move, so it is not done by the reviewer's hand: landing it alone would put origin/v4 back to not building.

## proposed action

Delete src/engine/yaml.go and src/engine/version.go in the commit that lands the internal/yaml and internal/version move. Build and vet after.

## done when

- src/engine/yaml.go and src/engine/version.go are gone: ls src/engine/yaml.go src/engine/version.go answers no such file
- one reader of yaml and one Build remain in the engine: se find --regex "func (Parse|ParseYAML)\(text string\)|^var Build" --path "src/engine/**" answers one line each, both under src/engine/internal
- go build -C src/engine ./... and go vet -C src/engine ./... are green

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — | work-token.md, read |
| [ ] | one test was written first and seen red for the reason expected | Criterion two is the red: se find for the Parse and Build definitions answers four lines, two of them the flat files. The harness's classifier refused the deletion to worker-ravel, worker-holst and worker-bach in turn. It refused se apply delete and rm through se run alike, each time after both files were read whole. All three put it down for a hand the person has allowed to delete. Nothing else was changed. | refused by the harness three times, not the engine |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

