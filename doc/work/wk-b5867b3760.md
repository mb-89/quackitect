---
id: wk-b5867b3760
seq: "-37"
type: work
title: the notice asks damage
status: imp_done
assignee: cowork
scope: single-step
traced: true
disposition: done
rounds: 1
evidence:
  - outcome
minted_by: cowork
submitted_by: cowork
---

## detail

Every review notice in src/engine/pull.go leads with the owner's leading question, would the product be damaged if this were blessed as it stands. It carries the three rules. No damage means no rejection, a nitpick is corrected by the reviewer, and a moved number is not damage unless the decision rests on it. The two contest sentences, own rules and nothing else, and every measurement reproduced, both go. The notice cites doc/guidance/reviewing.md rather than repeating it.

## done when

- every review notice, spec and implementation, leads with the leading question and carries all three rules, driven in one test that pulls each half and reads the answer
  `rg -q func.TestTheReviewNoticeLeadsWithDamage src/engine && go test -C src/engine -count=1 -run TestTheReviewNoticeLeadsWithDamage$ .`
- the contest sentence is gone from src/engine/pull.go, not joined by a better one
  `rg -q "and every measurement reproduced" src/engine/pull.go && exit 1 || rg -q "damaged if this were blessed" src/engine/pull.go`
- the contest bar is gone from src/engine/pull.go too, not joined by a better one
  `rg -q "own rules and nothing else" src/engine/pull.go && exit 1 || rg -q "damaged if this were blessed" src/engine/pull.go`
- every test named above was watched failing on its own assertion, with the change absent, before it was watched passing, and the evidence names each test and what it said

## evidence: outcome

specNotice and reviewNotice in src/engine/pull.go lead with theLeadingQuestion() and carry the three rules, and both old sentences are gone. TestTheReviewNoticeLeadsWithDamage pulls both halves and was seen red on each clause. go test -C src/engine answers ok.
