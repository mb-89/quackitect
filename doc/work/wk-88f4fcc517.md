---
id: wk-88f4fcc517
seq: 1000016
type: work
title: a retro keeps numbers
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: person
---

## detail

The owner wants long-run statistics without storing every ephemeral item: the retro should produce a statistic for its own period and publish it. Proposed shape, to be agreed before it is built: a retro counts what it is about to take and publishes the counts. It writes them into its folder and into a file that survives the drain. Open: where that file lives, whether two retros in one day make two periods, and whether published means a file, a panel line, or both. The owner also asked to cut the failure rate at every commit touching doc/guidance, so each improvement gets measured. Say what counts as a difference before looking, since the sample either side of one commit is small. Related: wk-1b7c1a2da1, whose per-day numbers depend on this answer.
