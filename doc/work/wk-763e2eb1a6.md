---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: noteMove argues in place
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/main
claimed_at: "2026-09-05T18:51:26Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - fe729906f22cef7104fd2aa2ffaac79fa01e5cd7
  - 0291bf095af007fdfb4abb7f4c8603e6e43ac9f2
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - f73d0108c92cd0e54c12184c53b98423e8cbe307
---

## detail

A finding on wk-72c8216cc2, which moved four arguments out of src/engine/store.go into rationale notes. One argument stays. noteMove carries a MEASURED paragraph reasoning that the line says what happened rather than being inferred from what is in it. It recounts a burn-down that counted a mint by looking for a line with a status and no from, and an ending by looking for a disposition, where neither key was ever written. Both numbers read nought for every day there has ever been, and nothing said so, because nought is a number a burn-down is allowed to answer. That is a decision with a cost and a revisit, which is what a rationale note holds. It was left out of the parent because its criterion names four and widening a criterion mid-token is how a token stops being reviewable.

## done when

- the MEASURED paragraph in noteMove is a rationale note store.go names by link: se find --regex "Why it is this shape" --path src/engine/store.go answers five lines
- the note carries decided, why and costs, and store.go repeats no sentence of it, driven by TestARationaleIsNamedNotRepeated

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One paragraph leaves one function. One note arrives. | 2 files |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one runs se find. Line two runs a named test. | 2 of 2 |
| [x] | the basics it stands on exist, or are minted first | The rationale kind, its schema and the test all shipped. Four notes already use them. | 4 shipped |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Nothing new was decided. The argument already written moved to where an argument belongs. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | The test shipped with the parent and drives both halves. It refuses a note nothing links to, and a sentence still standing where it left. | driven, not written |
| [x] | the same test was seen green after the change, and named | TestARationaleIsNamedNotRepeated is ok over nine notes. se find answers five links, all under store.go. build, vet and gofmt clean. | 5 links |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | store.go loses six comment lines and gains one link. One note is new. | 2 files |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Nothing left over. The remaining capitalised comments in store.go are instructions with one clause of reason. | 0 minted |

