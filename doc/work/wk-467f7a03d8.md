---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: wait names deleted token
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-ash
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - f7ec94716f8b240da11d8d600950a55045b550af
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 7d9e162e4c03e0a5de749787500f0698da174de8
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

THE WAIT NOTICE NAMES A TOKEN THAT NO LONGER EXISTS IN THE TREE.

Seen 2026-09-05 pulling as reviewer-rowan, role reviewer. The answer was wait: "10 piece(s) are yours and every one is blocked", listing five ids and then the same five again. One of the five is wk-427d4cabdb, and there is no file for it anywhere: se find --path "**/wk-427d4cabdb*" answers nothing, and .se/work holds one token, wk-9b4f341835. The engine itself removed it: .se/undo/20260905-144101.942176719.json records worker-ash deleting doc/work/wk-427d4cabdb.md at 14:41:01Z, which is the decision wk-cb0086d90c asked for.

A second id in the same list, wk-05df5b9967, is in no holder record either: .se/holds.json holds wk-3f9f936ce7, wk-40abb881a7, wk-43c65d49f6, wk-9b4f341835 and wk-a46c014566, and neither wk-427d4cabdb nor wk-05df5b9967 is among them. So the list waitNotice prints is served from neither the tree nor holds.json, and an entry that leaves both never falls off it.

This is separable from wk-6fbb74340a, wait notice doubles held, which names the doubling, the false "yours" and the done token still carrying a holder. None of those three covers an id whose token file is gone. A puller told to act on wk-427d4cabdb spends its turn discovering the file does not exist.

THE CHECK. Nothing asserts what waitNotice names. A test that deletes a token the queue has seen and then reads the notice catches this class.

## proposed action

Build the wait notice's list from the tokens the tree still carries, dropping any whose file is gone, and add the test above. Land it with or after wk-6fbb74340a, which touches the same function, src/engine/pull.go 669-711 and 844-851.

## done when

- a token whose file has been deleted is not named in the wait notice, decided by: a test over waitNotice in src/engine deletes one token the queue holds and finds its id absent from the notice
- the test was seen red first, on the deleted id still being named
- sh util/checks/battery.sh reports no new failure against the run before the change

## evidence: change

src/engine/pull.go: waitNotice filters its list through stillInTheWork before it counts or prints anything, and stillInTheWork keeps an id only where the file is in one of the two work folders now. The archive is deliberately not looked in, because a token that closed is exactly the id being dropped. The doubling and the false yours are wk-6fbb74340a's and are untouched.

## evidence: green

The same test answers ok, and se test with seven queue and process tests beside it, TestAnOversizedChapterDoesNotStopTheQueue, TestAHeldScopeHandsOutWhatIsInsideIt, TestTheQueueHandsOutSubTokensBeforeTheirParent, TestAPulledAgentIsAHand, TestTheStandardProcess, TestTheQueueIsStaffed and TestTheVerdictIsNeverTheAuthors, answered ok on all eight. go vet over src/engine is clean.

## evidence: red

TestTheWaitNoticeNamesNoTokenThatIsGone, new in src/engine/waitnotice_test.go, mints two notes, deletes one file from the work folders and reads the notice. It failed: the notice named wk-424348162e, whose file was gone, in a list of two under the sentence that two pieces are yours and every one is blocked. The token still in the tree was named as it should be.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One filter at the head of waitNotice, one helper it calls, one table-free test. It touches neither the doubling nor the false yours, which are wk-6fbb74340a's. | pull.go |
| [x] | every done-when line is decidable, and names the command where one decides it | The first two by se test --on this token --propose TestTheWaitNoticeNamesNoTokenThatIsGone. The third is the battery, which the engine starts on its own rules. | se test |
| [x] | the basics it stands on exist, or are minted first | workDirs already answers the two work folders, and mintNote and aTreeWithTheProcesses already build a tree with tokens in it. Nothing minted. | store.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Red first, and only the release: the doubling and the false yours are wk-6fbb74340a's. | work-token |
| [x] | one test was written first and seen red for the reason expected | TestTheWaitNoticeNamesNoTokenThatIsGone failed naming the deleted id in a notice of two, with the notice printed under it. | the red run |
| [x] | the same test was seen green after the change, and named | ok, with seven queue and process tests beside it: eight ok. go vet clean, and the battery's go test engine lane ran it green. | se test, 17s |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Two files: the filter and stillInTheWork in pull.go, the test in waitnotice_test.go. | git diff |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | A token of its own: wk-57aca89abe, for tests-name-no-token, red in every battery on record. | wk-57aca89abe |

## evidence: the battery

A battery ran over this tree at 14:50:44Z, after the change, and answered 6 failed. None is this change: a-refusal-names-a-legal-move is missing and takes checks-live-in-the-method down with it, three go test engine failures are the Git-shell and quoted-script fixtures in posixshell_test.go and quoted_test.go, se lint is about a hold under worker-relay-trial, render-check is the editor's group pin, and tests-name-no-token names src/engine/notesgohome_test.go lines 149, 150 and 167, which carry wk-0000000001 at those exact lines in this token's began commit f7ec9471. wk-7783c03017 records the shell tests and tests-name-no-token red in batteries before this change. The go test engine lane ran the new test green.

