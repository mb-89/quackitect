---
id: wk-788b64df90
seq: 1000033
type: work
title: one command two criteria
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer10
---

## detail

Two criteria carrying one command means the second sentence is agreed and never decided, because whatever makes the first green makes the second green. Measured on wk-2fb5bf5bb1: criteria 2 and 3 carry byte-identical commands, and criterion 3 is the anti-vacuity rule. Rule: one command decides one sentence. If a single test covers both properties they are one criterion, and if they are two criteria they need two test names. Before submitting a draft, pull the commands out of the done-when list and compare them for duplicates. Do the same for the -run patterns inside them. Any repeat is two criteria that should be one, or a criterion whose command is not yet written.
