---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: three files keep arguments
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T16:20:40Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 2e88d38bbb221b332215655bd7f4aaf311238142
---

## detail

From the verdict on wk-5b51fc14e4, rationales get a kind.

That token's detail names the harm: schema.go, gate.go and store.go carry paragraph-long ALL-CAPS arguments about why a thing is the way it is, and so do the MCP tool descriptions. It built the cure. There is a rationale kind with a schema, a folder, a template, and TestARationaleIsNamedNotRepeated driving the link both ways.

Only the descriptions were treated. doc/rationale holds two notes, a-description-is-an-instruction explaining src/mcp/lane.go and a-field-is-read-or-gone explaining src/schemas/work-token.schema.yaml. None of the three Go files the detail names was touched. src/engine/schema.go carries 6 such openers, src/engine/gate.go 34 and src/engine/store.go 18, and none of the three names a rationale by link.

The token's done-when lines never asked for them, so it closed green with its own motivation standing. Nothing goes red for it either: the test walks the notes already under doc/rationale, so a file that never got a rationale is a file it never looks at.

Wanted is the extraction the detail asked for, one file at a time, and each argument moved rather than copied.

## proposed action

Take src/engine/gate.go first, since it carries the most. Move each ALL-CAPS argument into a rationale under doc/rationale, listing gate.go under explains, and leave the instruction in the comment naming the note by link. TestARationaleIsNamedNotRepeated then covers it without a line of new test. Then schema.go and store.go the same way, each its own token if this grows.

## done when

- src/engine/gate.go names a rationale by link and carries none of its sentences, decided by: go test -C src/engine -run TestARationaleIsNamedNotRepeated ./... answers ok with gate.go among the files it drives
- the instruction left in gate.go still says what to do, decided by: a reader of each touched comment can act on it without opening the note
- the engine still builds and its gate tests pass, decided by: go build -C src/engine ./... answers nothing and go test -C src/engine -run 'Guard|Refus' ./... answers ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

