---
id: wk-84a8b68af9
seq: 79
type: work
title: a guard behind another
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "The class is written in doc/guidance/behaviour.md and the instance is fixed on wk-c22f29af7b, so the token is obsolete."
aborted_from: backlogged
minted_by: reviewer6
---

## detail

A guard standing in front of another guard, with a check that only asks whether the call was refused. Delete the first guard and every check stays green, so nothing holds the message a person reads. Found on wk-c22f29af7b: remove the strings.TrimSpace(p.Learned) == "" block from rejectionIsWhole in src/engine/pull.go and go test ./... still passes. A check on a refusal asserts on its distinguishing phrase, one case per refusal. The check, red today: give TestARejectionNamesTheLessonsToken two cases, the empty-id case on "names no token" and the not-a-token case on the id it was handed.
