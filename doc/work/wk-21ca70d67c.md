---
id: wk-21ca70d67c
seq: 1000110
type: work
title: the record replays
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: cowork
---

## detail

UC-29 and UC-30 are walked by hand, the engine dying mid-session is untested, and nothing can stand in for the harness. The session record under .se/log already holds every call in order. Build a replay stub that reads a recorded session, re-issues its calls against the engine, and asserts the rulings match. Killing the engine mid-replay makes the heartbeat case a test, and the two harness walks recorded once replay forever. A replay asserts rulings and refusals, never full output equality, so it does not freeze defects.
