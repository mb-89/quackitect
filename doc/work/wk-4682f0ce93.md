---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: two tests one gate
# where the token stands. The process owns these values.
status: open
---

## detail

ONE RULE IS GUARDED BY TWO TEST FILES, BECAUSE TWO AGENTS WORKED ONE TOKEN.

wk-34d11d2caf was worked twice on 2026-09-05, on two lines that did not touch until they merged. Neither is an ancestor of the other, so nothing refused the second.

- d56177f landed src/engine/heldknocks_test.go, TestAHeldTokenStillKnocksBeforeItIsHandedBack, three sub-tests: needs a person, waits on another token, waits on nothing.
- 499594b landed src/engine/heldskipsthegate_test.go, TestAHeldTokenWaitingOnAPersonIsSetBack and TestAnOrdinaryHeldTokenIsStillHandedBack.

The second file's coverage is a strict subset of the first's: it asserts the needs_human case and the ordinary case, and does not reach Blocked. Both drive the same branch of nextAmong in src/engine/pull.go, the whyNotNow guard over the tokens an actor already holds.

Measured on this box today: with that guard removed from a scratch copy of HEAD, both files go red on the same branch, and both go green with it. So the two files are one test written twice, and the engine now runs both for every change that reaches pull.go.

WHAT IT COSTS: two files to keep in step when the branch moves, and 73 lines the flat engine counts. the-flat-engine-only-shrinks already reads FAIL on this tree.

Deleting another hand's committed file under this hand's name is what the staging rule forbids, so it is not folded into wk-34d11d2caf.

## proposed action

Keep src/engine/heldknocks_test.go, which covers all three cases including Blocked, and drop src/engine/heldskipsthegate_test.go. Where its fixture says something the other does not, TakeUp and LoadToken rather than recordHold and HeldBy, carry that into the surviving file first.

## done when

- src/engine holds one test over the held-token gate in nextAmong, decided by: se find --regex 'func TestAHeldToken' over src/engine names one file
- the surviving test still covers needs_human, blocked and ordinary, decided by: removing the whyNotNow guard from a scratch copy of pull.go sends the needs_human and blocked cases red and leaves the ordinary case green
- the battery reports no new failure against the run before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

