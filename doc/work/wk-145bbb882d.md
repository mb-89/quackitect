---
id: wk-145bbb882d
seq: "8"
type: work
title: one line per call
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
minted_by: main
evidence:
  - outcome
---

## detail

The log writes a call on PreToolUse and an answer on PostToolUse, so one tool call takes two lines. Collapse them into one. The source is whoever initiated the call. A refusal keeps its own line because a refused call has no answer to fold into.

## evidence: outcome

The guard remembers what was asked in .se/calls.json and folds it into the answer, writing one line per call when the call comes back. The source is the agent and the ok column says whether it worked. TestTheGuardAppendsToTheRunningSession asserts that asking alone writes nothing and the returned call writes one line naming what was asked.
