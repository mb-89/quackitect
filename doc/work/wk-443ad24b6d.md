---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: reading names wrong file
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-fir
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T15:31:55Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 633999d76ea3614a81f97b4bc24657c074da7c90
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 474cbc33ea3867806c23332d13ccaa08795b7bb3
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

doc/rationale/a-description-is-an-instruction.md carries the reading the cut is argued from. It says: at 7f22e1a3 the file it explains declared 43 descriptions carrying 5249 bytes, cut to 11 descriptions carrying 883. It states its own method, a sum of the description literals in the file, taken over the working copy and over git show of that commit.

The method does not answer that number. The file it explains is src/mcp/lane.go, and at 7f22e1a3 that file carries no tool description at all. Every backtick in it is a struct tag. The descriptions were in src/mcp/main.go then, which carries 25 long literals and 8009 bytes at the same commit.

The second reading is stale as well. The working copy sums 21 descriptions and 1158 bytes, against the 883 over 11 the note states in the present tense.

A number carries the command that produced it, and this one carries a command that answers something else. A reader checking the argument finds nothing, and cannot tell a moved file from a wrong claim.

## proposed action

Re-measure both readings with one command that answers both, and name the file each is over. Pin the later one to a commit rather than to the present tense.

## done when

- the note's before and after readings are each answered by the command the note states, over the commit the note names: se test --propose 'TestARationalesReadingReproduces' answers ok

## evidence: change

the second reading was stale, and that half of the detail is right. doc/rationale/a-description-is-an-instruction.md now pins it: at 69667087 the door declares 21 descriptions carrying 2113 bytes, where the note said 11 of 883 in the present tense. The paragraph names the file, states the method, and names the command that takes both readings.

## evidence: finding_corrected

the detail names the wrong file, not the note. src/mcp/lane.go at 7f22e1a3 carries all 43 description keys, and git ls-tree over src/mcp at that commit shows main.go with 5 of them. The test takes the note's own method over lane.go at 7f22e1a3 and answers 43 descriptions of 5249 bytes, which is the note's number to the byte, so the before reading stood and needed no change.

## evidence: green

se test --on wk-443ad24b6d: TestARationalesReadingReproduces ok true, with TestARationaleIsNamedNotRepeated ok true beside it, so the note still fits its schema and still names what it explains.

## evidence: note

the argument survives the re-reading: 43 descriptions to 21, and 5249 bytes to 2113. The door has grown since the cut, which is why each reading is now pinned to a commit.

## evidence: red

before the change the after row failed with lane.go at 69667087 carrying 21 descriptions of 2113 bytes against the note's 11 of 883, and the note not naming that commit. The before row was green from the first run.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one paragraph of the note and one test | none owed |
| [x] | every done-when line is decidable, and names the command where one decides it | se test --on wk-443ad24b6d --propose TestARationalesReadingReproduces, which takes both readings over the commit each names and holds the note against them | ok true |
| [x] | the basics it stands on exist, or are minted first | git carries both commits, and the rationale schema and its check were already there. Nothing was minted | none owed |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | rule 14: a number carries the command that produced it, and here that command is the test the note names | none owed |
| [x] | one test was written first and seen red for the reason expected | the after row failed: lane.go at 69667087 carries 21 descriptions of 2113 bytes and the note said 11 of 883, in the present tense | red, one row |
| [x] | the same test was seen green after the change, and named | TestARationalesReadingReproduces, beside TestARationaleIsNamedNotRepeated | 2 ok |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | the measured paragraph of doc/rationale/a-description-is-an-instruction.md and the new src/engine/rationalereading_test.go | none owed |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | in the change. The detail names the wrong file: lane.go at 7f22e1a3 carries all 43, and the test answers 5249 bytes over it, so only the second reading was stale | none owed |

## evidence: test

src/engine/rationalereading_test.go, TestARationalesReadingReproduces. It reads each file as its commit left it, counts what a description key or a says field carries, in pieces joined by a plus with comments left out, and asserts the note states the same numbers, commits and file.

