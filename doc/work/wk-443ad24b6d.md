---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: reading names wrong file
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T15:31:55Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 633999d76ea3614a81f97b4bc24657c074da7c90
---

## detail

doc/rationale/a-description-is-an-instruction.md carries the reading the cut is argued from. It says: at 7f22e1a3 the file it explains declared 43 descriptions carrying 5249 bytes, cut to 11 descriptions carrying 883. It states its own method, a sum of the description literals in the file, taken over the working copy and over git show of that commit.

The method does not answer that number. The file it explains is src/mcp/lane.go, and at 7f22e1a3 that file carries no tool description at all. Every backtick in it is a struct tag. The descriptions were in src/mcp/main.go then, which carries 25 long literals and 8009 bytes at the same commit.

The second reading is stale as well. The working copy sums 21 descriptions and 1158 bytes, against the 883 over 11 the note states in the present tense.

A number carries the command that produced it, and this one carries a command that answers something else. A reader checking the argument finds nothing, and cannot tell a moved file from a wrong claim.

## proposed action

Re-measure both readings with one command that answers both, and name the file each is over. Pin the later one to a commit rather than to the present tense.

## done when

- the note's before and after readings are each answered by the command the note states, over the commit the note names: se test --propose 'TestARationalesReadingReproduces' answers ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

