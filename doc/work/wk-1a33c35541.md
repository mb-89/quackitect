---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: HEAD does not build
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-part
claimed_at: "2026-09-05T14:53:44Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - d84810ebdd710e7d94354a4b32f7bb0809e4de0a
---

## detail

On a clean copy of the tree at 559fc4a6, taken with git archive HEAD, the engine does not compile.

go vet, run from src/engine, says `main.go:46:43: too many arguments in call to callTheEngine`, and names the two shapes: it has a context first, and the function wants none.

Half of a context threading is committed. main.go passes a context to callTheEngine, and the copy of callTheEngine in the tree takes a string and a slice.

This is not one hand's working tree. It is what anybody gets by cloning, so every box that builds from git is stopped, and no test over src/engine runs at all.

src/engine/results_test.go, committed as part of the same threading, calls runVerbInside with a context as well, and that copy takes none. So the test package is in the same state as the package.

Found while working wk-2ab8c4708a, which does not touch main.go.

## done when

- a clean copy of HEAD compiles, decided by: git archive HEAD into an empty folder, then go vet -C src/engine ./... exits 0
- the whole test package builds there, decided by: go test -C src/engine -run TestNothingMatchesThisName ./ exits 0

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One signature, in one file. It wanted nothing here in the end. | one hunk of client.go |
| [x] | every done-when line is decidable, and names the command where one decides it | Both are, and both were run at four commits: the one the token names, the one that fixed it, the token's own began, and HEAD. | `git archive <c>` unpacked, then `go vet -C src/engine ./...` and `go test -C src/engine -run TestNothingMatchesThisName ./` |
| [x] | the basics it stands on exist, or are minted first | Nothing to mint. The reading needs git and the go toolchain, and the index tag the engine is built with. | `CGO_ENABLED=1 GOFLAGS=-tags=sqlite_fts5` |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rule 12: a check that will not go red is the finding. | work-token |
| [x] | one test was written first and seen red for the reason expected | Red at 559fc4a6, with the message the token quotes: `vet: ./main.go:46:43: too many arguments in call to callTheEngine`. | vet exit 1, test exit 1 |
| [x] | the same test was seen green after the change, and named | Green at 50bc1b2e, "client.go carries the context again", and at HEAD 55f16927: `ok quackitect/engine`. Both done-when lines hold. | vet exit 0, test exit 0 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Empty. 50bc1b2e put the context back before this was taken up. | `git log 559fc4a6..HEAD` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Its own, [[wk-86873d5a30]]. The began snapshot d84810eb does not compile either, on `rows[0].Blob undefined`. | wk-86873d5a30 |

