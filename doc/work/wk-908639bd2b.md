---
id: wk-908639bd2b
seq: "41"
type: work
title: search with ripgrep
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "4"
minted_by: person
evidence:
  - outcome
---

## detail

The agent keeps typing grep while the probe found rg. Refuse a recursive grep over the tree in the guard, naming the tool from the probe's list, and leave a grep on one file alone. Owner ruled .se must stay searchable: .rgignore carries !.se/ and !.se/**/*.jsonl. Put the rule and a measurement of the recursive case in doc/guidance/behaviour.md.

## done when

- A recursive grep over the tree is refused, naming the tool the probe found
  `rg -q func.TestARecursiveGrepIsRefused src/engine && go test -C src/engine -count=1 -run TestARecursiveGrepIsRefused$ .`
- A grep on one named file or standard input is left alone
  `rg -q func.TestAGrepOnOneFileIsLeftAlone src/engine && go test -C src/engine -count=1 -run TestAGrepOnOneFileIsLeftAlone$ .`
- The refusal reads the command, so grep behind a pipe is judged the same
  `rg -q func.TestTheRefusalReadsTheCommand src/engine && go test -C src/engine -count=1 -run TestTheRefusalReadsTheCommand$ .`
- A search with no path named reaches the tokens in .se
  `rg --files | rg -q se.work.wk-`
- The same search reaches the record
  `rg --files | rg -q se.log.session`
- The rule is in doc/guidance/behaviour.md
  `rg -q the.tool.the.probe.found doc/guidance/behaviour.md`
- Every projection target in util/projections.json carries the rule
  `rg -q func.TestTheSearchRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheSearchRuleReachesEveryProjection$ .`
- The measurement carries the recursive grep figure
  `rg -q 261ms doc/guidance/behaviour.md`
- And the rg figure
  `rg -q 40ms doc/guidance/behaviour.md`
- And both commands
  `rg -q LoadConfig doc/guidance/behaviour.md`
- Nothing outside tests names the searching tool
  `rg -q func.TestNothingNamesTheSearchingTool src/engine && go test -C src/engine -count=1 -run TestNothingNamesTheSearchingTool$ .`
- Every test above was watched red first

## evidence: outcome

search.go splits the command on every shell separator, newline included, and refuses a recursive grep naming the tool TheSearcher reads from util/tools.json. .rgignore and behaviour.md carry the negations and the rule. The five tests above were red first, and run-criteria.py answers 11 green.
