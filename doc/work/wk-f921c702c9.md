---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: claim publish stages ignored
# where the token stands. The process owns these values.
status: open
---

## detail

THE CLAIM PATH STAGES THE WORKING TREE, AND GIT REFUSES IT.

Measured on this box at 19:50. se claim --these wk-2109b74545 --take answered: the claim stands here. It could not be committed: git -c: The following paths are ignored by one of your .gitignore files: .se

The same call published cleanly two minutes earlier, so this arrived with a build rather than with the branch.

origin/v4 at 01119cf5 builds the claim commit with hash-object, update-index, write-tree and commit-tree. It touches no working tree, which is what keeps a claim from carrying whatever else is uncommitted. So the fault is in a build made from a tree carrying an in-flight edit.

What it costs: a claim that does not publish is one no other box can see. Every box on this branch takes the same build eventually.

The verb prefix in the message is -c. So the call that failed is a git add behind a flag, rather than the plumbing above.

## proposed action

Find the git add in the claim path and take it out. writeTheClaims on origin builds its commit with hash-object, update-index, write-tree and commit-tree, and never touches the working tree, which is what keeps a claim from carrying whatever else is uncommitted. Whatever replaced that has to go back to plumbing.

## done when

- se claim --these <id> --take publishes, decided by: the answer says published on refs/heads/se/claims rather than naming a path git ignores
- the claim path stages nothing from the working tree, decided by: se find --regex 'add' --path src/engine/claim.go names no git add

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

