---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: store keeps 25 arguments
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-aspen
claimed_at: "2026-09-05T21:04:14Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - f98b5504e266716a20aa68d2b320d2651b82a110
---

## detail

From wk-a53e262ad6, which moved src/engine/schema.go's arguments out and left store.go for a token of its own.

src/engine/store.go opens 30 paragraphs in capitals. Five of them already name a rationale by link: tokens-are-notes-in-two-folders, the-folder-answers-where-a-token-lives, the-line-says-what-happened, measuring-is-separate-from-refusing and the-disposition-says-it-stopped. The other 25 argue in place, so a reader looking for what the store does reads why it is that way first, every time.

They are separate decisions, and they cluster: the hold is engine state and the claim travels, the id is the file name, the body is prose the engine writes and a section it does not know is kept, the record refuses what it cannot read back, the snapshot is read once per process, the file name is the order, and the close is the moment that archives. A rationale allows 250 words in its why chapter, so one note cannot hold them.

## proposed action

Take the clusters one at a time. Write a rationale under doc/rationale listing src/engine/store.go under explains, move the argument into it, and leave the instruction naming the note by link. TestARationaleIsNamedNotRepeated covers each without new test code. Where store.go and schema.go share a decision, name the existing note rather than writing a second: a-chapter-opened-twice-is-a-departure already covers headingsSaidOnce.

## done when

- no comment in src/engine/store.go carries an argument that is not in a rationale, decided by: se find --regex "^\\s*// [A-Z][A-Z ,.:-]{12,}" --path src/engine/store.go answers nothing
- every rationale store.go names is reachable and unrepeated, decided by: go test -C src/engine -run TestARationaleIsNamedNotRepeated ./... reports no subtest naming store.go as failing
- the engine still builds and its store tests pass, decided by: go build -C src/engine ./... answers nothing and go test -C src/engine -run 'Save|Token|Archive' ./... answers ok

## evidence: note

store.go's 25 remaining arguments are eight notes under doc/rationale: the-hold-is-engine-state, a-note-is-written-for-its-readers, a-section-nobody-understands-is-kept, the-record-refuses-what-it-cannot-read-back, the-folders-are-read-once-per-process, the-token-carries-no-time, the-save-is-the-one-door and the-close-archives-the-token. Two existing notes took the rest: a-field-is-read-or-gone now explains store.go for the id and the kind, and the moved file's measured case joined the-folder-answers-where-a-token-lives. The five comments already linked lost their capitals and kept their links. Every non-comment line of store.go is identical before and after, by a diff that drops comment lines. Landed as 868a7b5c on origin/v4. se find could not decide the first criterion here, because this clone's index is 400 commits behind origin. So the same regex ran through awk over the worktree.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one file of comments, eight new notes and two lines on existing ones, and no non-comment line changed |  |
| [x] | every done-when line is decidable, and names the command where one decides it | the regex through awk over the worktree, since this clone's index is behind origin, one Go test read per subtest, and go build with the store tests |  |
| [x] | the basics it stands on exist, or are minted first | the rationale kind, its schema, its folder and its test all ship, and wk-a53e262ad6 left the shape to follow |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read. Rule 11 kept the ten unlinked notes out as wk-2b1d846eb8, rule 12 put the red first, rule 15 answered each line |  |
| [x] | one test was written first and seen red for the reason expected | notes first, store.go untouched. TestARationaleIsNamedNotRepeated failed on ten notes store.go did not name and five sentences it carried |  |
| [x] | the same test was seen green after the change, and named | TestARationaleIsNamedNotRepeated PASS on all fifteen subtests naming store.go at 868a7b5c. go build, go vet, gofmt and the regex answer nothing, the store tests answer ok |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/engine/store.go, eight new and two edited notes under doc/rationale, commit 868a7b5c on origin/v4 |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | none beyond wk-2b1d846eb8, the ten notes outside this file |  |

