---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: gate keeps 30 arguments
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T16:31:26Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0401e7d601a9a49dd3e70608b1a41ccad55ede28
---

## detail

From wk-3a46df35b7, which moved the first of gate.go's arguments out.

src/engine/gate.go opened 34 paragraph-long capitalised arguments. The write gate decision is now doc/rationale/no-token-no-writing.md, and gate.go names it by link and carries none of its sentences.

Thirty openers are left. They are separate decisions: the todo list as a sub-token, the names an actor answers to, a slot handed on between sessions, what makes a session live, and the shape of a refusal.

A rationale allows 250 words in its why chapter, so one note cannot hold thirty arguments. Each cluster is a note of its own, and the comment left behind keeps the instruction and names the note.

## proposed action

Take the clusters one at a time. Write a rationale under doc/rationale listing src/engine/gate.go under explains, move the argument into it, and leave the instruction naming the note by link. TestARationaleIsNamedNotRepeated covers each without new test code.

## done when

- no comment in src/engine/gate.go carries an argument that is not in a rationale, decided by: reading each capitalised opener left in the file
- every rationale it names is reachable and unrepeated, decided by: go test -C src/engine -run TestARationaleIsNamedNotRepeated ./... answers ok
- the engine still builds and its gate tests pass, decided by: go build -C src/engine ./... answers nothing and the guard tests answer ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

