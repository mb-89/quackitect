---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: Wait notice doubles held
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-relay-trial
claimed_at: "2026-09-05T14:41:49Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b75ba8eed629d3587af4654432e3f92800861991
---

## detail

THE WAIT NOTICE SAYS "YOURS" ABOUT WORK THAT IS SOMEBODY ELSE'S, AND SAYS IT TWICE.

Seen 2026-09-05 pulling as reviewer-poplar. It read "12 piece(s) are yours and every one is blocked", listed six ids, then the same six again. There are six, and not one was the puller's. Each was held by another actor on this box, every holder confirmed by naming the token on a run.

Two separable faults, in src/engine/pull.go between 669 and 711.

One, the count doubles. held is appended at 675 inside a loop over wantMine true then false, and the append does not depend on wantMine, so every held token lands twice.

Two, the word is wrong. Line 675 appends whenever the holder is not empty, with no test against actor, while line 851 calls the list "yours". That is the false report the comment at 844 exists to prevent.

It cost this session turns: the stop gate named wk-05df5b9967 as in my hands through three claims after the engine had answered freed on it.

Also seen: wk-a46c014566 and wk-fc6b6c5aa7 are done and still carry a holder.

Nothing asserts what waitNotice names. Born as note wk-9b4f341835.

## proposed action

Move the append out of the wantMine loop so a token lands once. Compare the holder against actor before calling the list the caller's, and skip a token whose status is ended. Then write the missing test: two tokens held by other actors, one held by the caller, one ended token still carrying a holder, read the notice, assert it names the caller's alone and once.

## done when

- a pull whose queue holds three tokens, two held by other actors and one by the caller, names one id in its wait notice and calls it the caller's, decided by: a test over waitNotice in src/engine reads the notice and finds one id
- a token at status done carrying a holder is not named in that notice, decided by: the same test adds one and finds it absent
- the test was seen red first, on the doubling
- the battery reports no new failure against the run before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

