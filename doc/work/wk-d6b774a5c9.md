---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: a sweep strands notes
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-07T11:19:17Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - fb6a181f1c7fef514f005b2879e0b93b28b8c201
---

## detail

A sweep takes notes off the disk and writes their rows into doc/work/archive.jsonl. Reverting that file puts the rows back and leaves the notes gone, so the tokens are stranded: off the disk, on the branch, and named by no row.

MEASURED, 2026-09-07. A hand ran se archive --sweep, saw it rewrite about 180 rows, judged that far beyond one close and reverted the file untouched. It did not revert the deletions, because nothing said there were any. deleted-notes-have-a-row then read 185 notes off the disk with 153 failing, where the same check had read 25 off the disk and 0 failing an hour earlier.

The 153 were restored from origin/v4 and the check reads 0 failed again. Nothing had landed the deletions, so the branch never lost them. That is luck rather than design: any land naming those paths would have taken them off the branch too.

THE SWEEP IS TWO WRITES AND ONLY ONE IS VISIBLE. The rows are a file a hand can see and revert. The deletions are not listed anywhere, so a hand undoing the sweep undoes half of it.

## proposed action

THE OWNER'S RULING, September 2026. A cloud box does not sweep at all. The sweep collects what has closed for the retro and puts it in the archive. That belongs where a person sits and the disk outlives the session. The environment already says which box this is, and the engine reads it as TheHost.

So the sweep is refused where the host is a cloud box, and the refusal names the desk as the place to run it.

## done when

- a sweep is refused where the host reads as a cloud box, and a Go test drives it with the cloud variable set
- a sweep is allowed where the host reads as a desk, proved by the same test with the variable cleared
- the refusal names where to run it instead, so it points at a legal move

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The retro's work stays where a person sits, and a cloud box can no longer delete notes by reading a record it does not hold. | TheHost |
| [x] | what breaks if it is never done, and not only that it stays undone | A sweep here strands the tokens it touches, and the branch keeps them only while nobody lands the deletions. | the 153 measured |
| [x] | the ask is small enough to review whole, or it is split first | — | one function, one branch in the verb |
| [x] | every done-when line is decidable, and names the command where one decides it | Two are Go tests over the host table, one with the cloud variable set and one with it cleared. The third reads the refusal text. | RUNME.sh test --propose TestASweep |
| [x] | the basics it stands on exist, or are minted first | — | TheHost and the host table already stood |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | trivial process |
| [x] | one test was written first and seen red for the reason expected | Red on its assertion at aswepisforadesk_test.go:28, a cloud box was allowed to sweep. Driven again against an implementation that lets one through. | RUNME.sh test --propose TestASweepIsRefused |
| [x] | the same test was seen green after the change, and named | Both green. | TestASweepIsRefusedOnACloudBox, TestASweepRunsOnADesk |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | archive.go and one new test file |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The 153 notes a sweep had stranded were put back on the disk here and then taken off again, on the owner's ruling. The archive is git's, and the disk keeps the list of hashes. | the owner's ruling |

