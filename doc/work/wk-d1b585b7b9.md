---
id: wk-d1b585b7b9
seq: "-31"
type: work
title: the engine nudges
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 1
minted_by: person
evidence:
  - outcome
---

## detail

Owner's shape: when three tokens are open where the walker is and none is in work, the engine suggests spawning a subagent. It says so again at six and at nine, once each. The same for the reviewer when the submitted line is long and nothing is in review. It is a nudge and not a refusal, and declining is fine. The engine remembers the highest step it nudged at per queue and forgets a step once the count falls below it. Owner to decide whether the nudge replaces limits.unreviewed_before_blocked or the wall stays at a higher number.

## evidence: outcome

The nudge is in the pull. stepFor, firstTimeAt and forgetAbove are keyed per role so the two queues are counted apart, and forgetAbove runs for every count. TestAQueueThatFallsAndClimbsIsNudgedAgain drives the fall and climb, and TestTheTwoQueuesAreCountedApart the shared-memory case. Seven defects were put back one at a time and each reddened nudge_test.go. The battery is green.
