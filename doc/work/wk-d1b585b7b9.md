---
id: wk-d1b585b7b9
seq: "-22"
type: work
title: the engine nudges
status: imp_submitted
assignee: main
scope: single-step
traced: true
disposition: done
minted_by: person
---

## detail

A nudge on the way up, once per step, and declining is fine.

THE OWNER'S SHAPE: when three tokens are open where the walker is and none is
in work, the engine says how about you spawn a subagent. At six it says it
again. At nine again. Once each. The same for the reviewer: when the submitted
line is long and nothing is in review, how about another reviewer.

IT IS A NUDGE AND NOT A REFUSAL. Declining is a legitimate answer, because
sometimes work does not split. Nothing is refused and nothing has to be
claimed. The engine says it once and stops.

ONLY ON THE WAY UP, AND ONLY ONCE PER STEP. What the engine remembers is the
highest step it has already nudged at, per queue. It nudges when the count
crosses a step it has not nudged at, and it forgets a step once the count falls
below it. So a queue that empties and fills again nudges again, and a queue
sitting at seven does not nudge every pull.

THAT NEEDS NO CLOCK, which is why it fits: an arrival is a fact and a clock
guesses.

TWO QUEUES, ONE MECHANISM. Open work where the walker is, and submitted work
with nothing in review. Same steps, same rule, two counts.

WHAT THIS REPLACES OR SITS BESIDE: limits.unreviewed_before_blocked is a wall
at one number that keeps firing until somebody obeys it. Decide whether the
nudge replaces the wall or the wall stays as a last resort at a higher number.
The owner has not said, and it is theirs to say.

## evidence: what was built

A nudge on the way up, once per step, and declining is fine. It is your shape and the numbers are yours: three, six, nine.

WHEN IT SPEAKS. The queue is counted before the hand-out, because a pull that gives this agent a token makes it busy by definition and your condition is three open with nothing in work. Counted afterwards the nudge could only ever land on a wait.

ONCE PER STEP, AND ONLY ON THE WAY UP. What is remembered is the highest step already spoken at, per queue. It speaks when the count crosses a step it has not spoken at, and it forgets a step once the count falls below it, so a queue that empties and fills is nudged again and a queue sitting at seven is not nudged on every pull. That needs no clock, which is why it fits: a count is a fact and a clock guesses.

TWO QUEUES, ONE MECHANISM. Open work where the walker is, and work waiting for a reviewer with nothing in review. Same steps, two counts, and they are counted apart, because a nudge spent by one would refuse the other early.

IT REFUSES NOTHING. It rides on the pull's notice and the answer is unchanged, because sometimes work does not split and declining is a legitimate answer. It sits beside the wall rather than replacing it: unreviewed_before_blocked is a refusal at one number that keeps firing until somebody obeys, and this speaks earlier and lets the agent decide.

IT FIRED ON THE REAL QUEUE ON ITS FIRST LIVE PULL, which is the evidence I trust most here: the answer came back carrying 3 open where you are and nothing in hand. How about spawning a subagent.

WATCHED RED, SIX DEFECTS PUT BACK ONE AT A TIME:

  the once-per-step memory taken out            RED
    nudge_test.go:35: something was said at 4 open, which is not a step
  the step forgotten when the count falls        RED
    nudge_test.go:92: a queue that emptied and filled again was not nudged
  the two queues sharing one memory              RED
    nudge_test.go:35: something was said at 4 open, which is not a step
  nothing in hand no longer checked              RED
    nudge_test.go:109: an agent with work in hand was nudged
  the sentence without the count in it           RED
    nudge_test.go:47: the nudge does not say how many are open
  the reviewer counting the worker's queue       RED
    nudge_test.go:126: nothing was said with 3 waiting for a reviewer
  restored                                       GREEN

WHAT I DID NOT DECIDE. The token asks whether the nudge replaces the wall or the wall stays at a higher number. I left the wall where it is and said why above, because turning off a refusal that works is a change to what the engine will let happen, and that is yours rather than mine.

