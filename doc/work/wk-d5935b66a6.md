---
id: wk-d5935b66a6
seq: 1000217
type: work
title: "a regression check's red"
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-32
---

## detail

An evidence criterion says every named check was watched red with the fix absent, over a list that includes a regression check. A regression check is green with the fix absent by construction, so the sentence asks for a red nobody can take. Found on wk-7722faa0b8 round 2: only a wrong fix, a fresh SysProcAttr written over Quietly, moves HideWindow and CreationFlags. When an evidence criterion collects reds over a list, split it by what each check guards. A discriminating check is watched red with the fix absent. A regression check is watched red with a named wrong implementation planted in a copy. Before writing the sentence, run each named check against the tree as it stands, and name a plant for every one already green.
