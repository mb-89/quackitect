---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: clone lacks context commit
# where the token stands. The process owns these values.
status: open
# true when this waits for a person rather than an agent
needs_human: true
---

## detail

The shared clone at /home/user/quackitect cannot build the src/engine test binary, and no Go red under that package can be watched on this box. The cause is the clone, not the code. Its HEAD does not carry commit 735ebc83, which gave TestTheDelta a context parameter. snapshotnevertravelled_test.go passes that context, so the two disagree here and agree at the branch tip. Measured with CGO_ENABLED=1 GOFLAGS=-tags=sqlite_fts5 go test -C src/engine -run TestNothingAtAll ./... run twice. On this clone it answered two errors and exit one. On a detached worktree of the branch tip it answered exit zero and no build failure. Three tokens are parked on this wall and each names a different cause for it: wk-dc6bfb21f5, wk-cf33c85301 and wk-4140e954b7. The working tree also carries uncommitted edits from many hands, so a fast forward here is not a worker's call. Until the clone carries the tip, every Go criterion decided on this box is decided against a tree nobody lands on.

## proposed action

Whoever owns this box brings the clone's HEAD up to origin/v4, keeping the uncommitted work of the other hands. Then rerun the build command above and confirm it answers no build failure, so the parked tokens can be taken up here rather than on a worktree.

## done when

- the clone's HEAD carries commit 735ebc83, decided by: git -C /home/user/quackitect merge-base --is-ancestor 735ebc83 HEAD answering exit zero
- the package builds its tests in the clone, decided by: CGO_ENABLED=1 GOFLAGS=-tags=sqlite_fts5 go test -C src/engine -run TestNothingAtAll ./... answering no build failure
- the uncommitted work other hands hold is still there, decided by: git -C /home/user/quackitect status --porcelain naming the same files it named before

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

