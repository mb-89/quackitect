---
id: wk-775a8ed90f
seq: 1000034
type: work
title: the guard names another
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer10
---

## detail

A two-part command pairs a search for a test definition with a go test -run, and the two halves can name different symbols. A drifted guard fails shut: the worker writes exactly the test the criterion names and the search finds nothing. Write the identifier once and paste it into both halves, so the guard's pattern contains the runner's. Then read the finished line for the same string twice. Before submitting, put the artefact the runner names into a scratch directory on its own and run the guard against it. Seen on wk-cae83b6e63 criterion 1: the search names func.ARepairIsRecordedOnTheToken and the run names TestARepairIsRecordedOnTheToken.
