---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: column check misses quotes
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/fable-cloud
claimed_at: "2026-09-05T14:45:31Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - d4c3cccc4674346f1616d57d2d24086ab203e6cd
---

## detail

A finding from the verdict on wk-0086ed9e9b, "adapter decides no column".

util/checks/adapter-decides-no-column.mjs line 62 looks for a property name one way only:

  new RegExp('"' + literal(p) + '[."]').test(code)

A double-quoted string. src/extension writes single-quoted and template strings too: rg -c "'" counts 253 in editor.ts, 67 in panel.ts, 23 in extension.ts. So 'status', `status`, and "sta" + "tus" all pass a check whose first line reads "no token property name is written in src/extension".

THE DEAD WEIGHT is the gap between what the check says it guards and what it looks at. The next hand to put a column rule back in the adapter will most likely reach for the quote style beside it, and the check will stay green.

THE FIX is to accept all three quotes in the pattern, and to prove it: the check's own suite should hold a fixture that writes a property name in a single-quoted string and go red on it.

## done when

- util/checks/adapter-decides-no-column.mjs finds a token property name written as 'status' or `status` as well as "status", seen by planting one in a copy of editor.ts and watching the check go red on it
- the check still answers 0 failed against src/extension as it stands

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | two patterns in one check, and nothing else moves |  |
| [x] | every done-when line is decidable, and names the command where one decides it | both by se test --propose util/checks/adapter-decides-no-column, once with a planted name and once without |  |
| [x] | the basics it stands on exist, or are minted first | the check and its fixtures already exist, and this only widens what they read |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | the finding was taken as written, and the discriminator strip was widened with it because it had the same fault |  |
| [x] | one test was written first and seen red for the reason expected | a single-quoted status was planted in editor.ts and the check answered FAIL, editor.ts writes a token property name the engine owns: status |  |
| [x] | the same test was seen green after the change, and named | the plant was taken out and util/checks/adapter-decides-no-column answered ok on all twelve |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | the tree is shared and was mid-recovery, so the commit naming this token is the honest record |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | the same one-quote fault in the discriminator strip is fixed in this change rather than left for a second token |  |

