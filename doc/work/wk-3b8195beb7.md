---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: one word for tracked
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-birch
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 2428b8bc7a0189d018c859b47f625c0e72d780eb
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 5a57e55098389ea1aed253a1c69ea7e42d08ea87
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

The tree calls it traced and the owner calls it tracked. Both name the same thing: a token in doc/work, which git carries.

TracedDir is the function, and the guidance and the mint now say tracked. So a reader meets two words for one idea, which the voice rule forbids.

EphemeralDir is the other half and has the same problem. The owner says local, the code says ephemeral, and the folder is .se/work.

A rename is mechanical and the tests name the functions, so it fails loudly if it is half done.

## proposed action

Pick the owner's words: tracked and local.

Rename TracedDir to TrackedDir and EphemeralDir to LocalDir, and every comment that says traced or ephemeral of a token.

Leave the folder names alone. doc/work and .se/work say where, not what.

## done when

- no Go file says traced of a token, in prose or in an identifier: se_find regex [Tt]raced over path src/engine/*.go answers count 0
- no Go file says ephemeral of a token: se_find regex [Ee]phemeral over path src/engine/*.go answers one hit, the ephemeral port range in hookserve.go, which is not a token
- the engine still builds and its tests pass: se test on this token answers ok

## evidence: green after

se find --regex [Tt]raced --path src/engine/*.go answers count 0.
se find --regex [Ee]phemeral --path src/engine/*.go answers count 1, hookserve.go, the port range.
se find --regex Traced|Ephemeral --path src/** answers count 0, so no caller was left behind anywhere in the source.

## evidence: red first

Both criteria were run before any file was touched.
se find --regex traced --path src/engine/*.go answered count 15, in eleven files.
se find --regex ephemeral --path src/engine/*.go answered count 3, in three files.
The fifteen were the two comments in store.go, one in config.go, one more in store.go, eight process fixtures and three lines of store_test.go.

## evidence: se test

Answered not ok. It chose 145 tests and three failed, none of them naming a renamed identifier.
TestALoopThatDeletesIsRefused and TestARemovalNeedsARead both fail because the guard refuses for want of a named token before the removal guard speaks. src/engine/hook.go is modified in this tree by another lane.
src/mcp/TestTheToolSaysWhatTheGuidanceSays could not build: src/mcp/main.go passes map[string]any where nine typed argument structs are wanted. That file is another lane's uncommitted work.
This change touched no guard and nothing under src/mcp, so neither failure is from it.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One rename over fifteen files in src/engine, all of it mechanical. | — |
| [x] | every done-when line is decidable, and names the command where one decides it | Two were not, and both were rewritten before the work. See the evidence chapter the criteria as handed over. |  |
| [x] | the basics it stands on exist, or are minted first | Both functions and every caller exist. The owner's two words are already in the guidance and the mint. | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md read whole. Its rule 12 sent the criteria back before any file was touched. | — |
| [x] | one test was written first and seen red for the reason expected | The criteria are the checks, and both were run first. See the evidence chapter red first. |  |
| [x] | the same test was seen green after the change, and named | Both were run again. See the evidence chapter green after. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Fifteen files in src/engine, no file outside it. See the evidence chapter what changed. | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | A dead traced field in eight process fixtures went with it. Nothing else was found. See the evidence chapter se test. | — |

## evidence: the criteria as handed over

The first named the regex traced, which is case sensitive and cannot see TracedDir. That is the identifier the token is mostly about, so the criterion could go green with the rename half done. It now names [Tt]raced.
The second asked for count 0 on ephemeral. hookserve.go says ephemeral of a port range, which is the standard term and not a token, so count 0 was unreachable without spoiling correct prose. The criterion now names that one hit and why it stays.

## evidence: what changed

store.go carries the rename: LocalDir and TrackedDir, and the four comments that said traced or ephemeral of a token.
archive.go, pull.go and config.go took the callers and two comments.
archive_test.go and mintsayswhere_test.go took the callers.
Eight test process fixtures carried a traced field the Process struct does not declare and the schema does not name. The parser ignored it, so it was deleted rather than renamed.
store_test.go drives the frontmatter parser on an arbitrary boolean key, and that key is now tracked.

