---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: calls need an engine
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-relay-trial
claimed_at: "2026-09-05T20:48:11Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 27f2fc9cf79366529d2f75ded6260f106a19a3e5
---

## detail

A finding on the verdict of wk-9f1d1d0a2d. se query --calls is a verb, and a verb is sent on to the engine that lives over the folder, so over a folder with no engine the built binary answers the refusal at src/engine/client.go:80, "no engine is running over <work>, so there is nothing to run query", and the catalog is not answered. Seen on a worktree of origin/v4 at 13e2594f: the done-when command answered grep exit 1 with no engine, and 0 once one was started. The comment above the answer in src/engine/lane.go says the catalog is about this program rather than about the tree, and the token says a caller holds one call and the engine hands back the rest. Neither holds: the caller must hold start on its own before it can fetch the catalog that lists start, and a fresh tree cannot answer what the program takes.

## proposed action

Answer --calls in the program itself, before a verb is sent on, or as the flag form se --calls beside --bind status and --ping, which the catalog test already drives through the binary. The catalog stays beside the dispatch either way. Hold it with a test that runs the built engine over a folder with no engine, asks for the calls, and reads the catalog with its own fetching call in it. Seen red first against the verb as it stands.

## done when

- the built engine answers the catalog over a folder no engine runs over, decided by: .bin/se.exe --calls --work <fresh dir> | grep -q '"calls"' or the query form doing the same, whichever the taker keeps
- a test in src/engine drives that through the built binary and was seen red first, and go test ./src/engine names it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

