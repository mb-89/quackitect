---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: flat number names nothing
# where the token stands. The process owns these values.
status: closed
claimed_by: 547b9365/worker-relay-trial
claimed_at: "2026-09-05T20:53:43Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - cd227972f552e9aede28642fe740611f4681c28a
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 2d5cb28126cb2020d984e986d4a43474fce2db39
# how it ended. Only an ended token carries one.
disposition: dropped
# why it was dropped
reason: "became wk-a383afdbbc: it landed first as 330d4ee5 and took the number out of the check, so there is no most to set and no commit to name beside it"
---

## detail

A finding on the verdict of wk-9f1d1d0a2d. util/checks/the-flat-engine-only-shrinks.mjs raised most.lines from 29203 to 29378 with the reason that the catalog went in on purpose. But the check counts 29730 lines and 90 files at 13e2594f, the commit that wrote that number, and 29591 and 90 at its parent, so it answered 2 failed on both sides and the raise accounts for nothing: the number is one no tree had. wk-a383afdbbc says the check reddens whoever runs it next rather than the change that grew the package, and that is why the number was already behind. This is the smaller thing: whoever raises the number sets it to what the check counts at that commit, so the check is green where the reason beside it says it should be.

## proposed action

On a worktree of origin/v4, run node util/checks/the-flat-engine-only-shrinks.mjs, set most.files and most.lines to what it counts, and say beside them which commit set them and why the tree is that size. If wk-a383afdbbc lands first and changes what the check asks, fold this into it.

## done when

- node util/checks/the-flat-engine-only-shrinks.mjs answers 0 failed on a worktree of origin/v4 at the commit that sets the number
- the comment beside most names the commit that set it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

