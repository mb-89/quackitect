---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: claim fixture is lapsed
# where the token stands. The process owns these values.
status: open
---

## detail

TestPublishKeepsEarlierClaimsWhenThePushKeepsFailing in src/engine/publishkeepsearlier_test.go is red on the branch head, and it stays red on a tree where the behaviour it asks for holds.

Its notes are written by aClaimNote, which stamps every one claimed_at 2026-09-05T00:00:00Z. A claim lapses after the hours the method sets, three by default, so by the time the test runs every fixture claim is already lapsed. nextClaimsFile keeps only live claims, so the earlier ones are dropped by the rule rather than by the defect, and the test asks for them anyway.

MEASURED. With wk-36ad47c24a's carry-forward in the tree, the test still says `the claims ref does not name wk-earlier-zero`, and the claims file it prints holds only the last claim. The same tree passes TestAPushThatNeverLandsKeepsEveryClaimThisBoxWrote, which stamps its claims at the time it runs.

The fixture wants the clock the test runs at, or the test wants to say that a lapsed claim is dropped on purpose.

## done when

- TestPublishKeepsEarlierClaimsWhenThePushKeepsFailing passes on a clean copy of the head, decided by: go test -C src/engine -run TestPublishKeepsEarlierClaimsWhenThePushKeepsFailing -count=1 ./
- the fixture stamps a claim at a time the test names, so the test says which rule it is about

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

