---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: startup names three programs
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-ives
claimed_at: "2026-09-05T13:31:41Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 32e18ff10a1abe8b12024e2835aab07047c41fa2
---

## detail

src/engine/main.go:136 calls LinkBothNames with a literal list, []string{"se", "se-mcp", "logview"}. util/setup/manifest.json is the tree's list of what it ships, and this is a second copy of it in code.

wk-4ad110f94e ended the same drift in the swap door: buildNext and putInPlace read theBuilds and no longer name a program themselves. Startup is the one place left that still does.

A fourth program added to the manifest is built by the installer, built and put in place by a swap, and then left with only its suffixed name by the engine that starts. The cage names .bin/se with no extension because it travels, so the plain name is the one the hooks reach for, and a program that has only one of its two names fails with nothing saying why.

Nothing is wrong today, because the literal and the manifest happen to agree. What is wrong is that nothing holds them together, and the way this is found is a hook that stops firing.

## proposed action

Read the names from theBuilds at src/engine/main.go:136, the way the swap door now does.

## done when

- src/engine/main.go names no program in a list of its own: se find --regex 'se-mcp' --path src/engine/main.go shows no literal name list
- the names startup links come from theBuilds, the same call the swap door reads
- a test gives a tree a manifest naming a program the literal never had, starts the link step, and finds that program under both its names; watched red before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Two helpers in vehicle.go, two lines in main.go, one test file. | src/engine/vehicle.go |
| [x] | every done-when line is decidable, and names the command where one decides it | Three lines. The first names se find, which now answers count 0. The second is read in the code. The third names a test, seen red and then green. | se_find, se_test |
| [x] | the basics it stands on exist, or are minted first | theBuilds and LinkBothNames both exist, and neither changed. util/setup/manifest.json names the same three programs the literal named. | src/engine/swap.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove the seam: the literal became a function first, so the test could go red. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | TestTheLinkStepNamesWhatTheManifestNames, red: the manifest names quacklint, and the link step left it without its plain name. | se_test, ok false |
| [x] | the same test was seen green after the change, and named | TestTheLinkStepNamesWhatTheManifestNames, ok true, with four neighbours green beside it. | se_test, ok true |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began is 32e18ff1, an object here, and ended comes with this submission. That window catches other hands, so the four staged paths are the change. | git diff --cached --stat |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change: main.go held a second literal list, and it now reads theProgramNames. | src/engine/main.go |

## evidence: step 2, the second list

The detail names one literal, at the call to LinkBothNames. The first done-when asks for more than that: main.go is to name no program in a list of its own.

There was a second. The start-up report that says a program is two different files walked its own se and se-mcp. A third program split between its names went unreported. It reads theProgramNames now, and se find for se-mcp over main.go answers count 0.

