---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: commit will not compile
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-part
claimed_at: "2026-09-05T16:05:52Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 3e2e81a63cb23782e91fc7a0cb4ab4533e50cb25
---

## detail

A finding from the verdict on wk-0086ed9e9b, "adapter decides no column".

Commit dd2fed69 carries hunks that are not that token's work: src/engine/view.go rewritten from ymap/ystr/ylist to yaml.Map/yaml.Str/yaml.List, plus battery.sh's -ldflags moved to quackitect/engine/internal/version.Build. Both belong to the yaml and version extraction (dad9ddbf), not to the adapter.

THE DAMAGE. The commit does not build:

  git archive dd2fed69 | tar -x -C /tmp/d && cd /tmp/d && go build -C src/engine .
  view.go:7:2: package quackitect/engine/internal/yaml is not in std

view.go imports a package dd2fed69 does not carry. git ls-tree -r dd2fed69 -- src/engine/internal/ lists nine files and no yaml. So a bisect landing on this commit cannot build the engine, and the token's diff cannot be read as one change. HEAD builds, because internal/yaml is back by then.

This is the swept-index hazard the tree already knows: staging by name rather than a whole dirty tree keeps a token's commit to a token's work.

## done when

- every commit on v4 from dd2fed69 to HEAD builds on its own: for each, git archive <sha> | tar -x -C a fresh dir, then go build -C src/engine . exits 0
- the engine's commit path stages the paths a token wrote by name, and a check or the commit code refuses a stage that carries a file no verb of this token touched

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Two asks, and neither is one hand's. The first is 67 commits a dozen agents have pushed. The second is a guard in the gate, [[wk-ae2ac9d15f]]. So it is split rather than worked. | 174 commits |
| [x] | every done-when line is decidable, and names the command where one decides it | The first is, and was decided: 67 of 174 commits import a package the commit does not carry, by package logbook 63, yaml 5, version 4. It cannot be made true without rewriting commits every other box has pulled. | `git ls-tree -d --name-only <c> src/engine/internal/` against `git grep -o quackitect/engine/internal/...` |
| [x] | the basics it stands on exist, or are minted first | The second line stands on a commit path in the engine, and there is none: snapshot.go and claim.go each build a temporary index, and the archive never touches the branch. The successor is written against the gate instead. | `git add` reaches the gate |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rule 5: work that moves off takes its criteria with it, so both go to the successors. | work-token |
| [x] | one test was written first and seen red for the reason expected | The reading is red and stays red: 67 of 174 commits. It is red about the past, and nothing this hand writes turns it green. | 67 of 174 |
| [x] | the same test was seen green after the change, and named | No green, which is why the token moved rather than closing done. | no change |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Empty but for this note. | `git diff --stat` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Tokens of their own. [[wk-ae2ac9d15f]] is the guard the second line asks for, and the 63 logbook commits are the live half of wk-0f91286ac1. | two successors |

