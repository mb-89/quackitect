---
id: wk-c6247665a3
seq: "-12"
type: work
title: upstream is judged
status: imp_submitted
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "3"
minted_by: person
---

## detail

Read what the cloud agent pushed to origin/v4 and decide whether it goes in.

THE OWNER'S WORDS: a cloud agent wrote some changes that it thought were useful
for running stuff in the cloud. I found this not very convincing. Check it out
and see if it makes sense. If it does not, do not merge it. If it does, merge
it.

THIRTEEN COMMITS AHEAD. They are about the cage, which is what the cloud runs
the agent in: util/cage, util/setup, and the engine's own naming across
platforms.

THE ANSWER IS A REASON EITHER WAY. Merging is not the deliverable and neither
is refusing. What this owes is a judgment with the evidence under it.

## evidence: If it is taken, this branch carries the newest of them. If it is left, the report says what would have to be true for the answer to be different. One command cannot be true of both outcomes, so the evidence answers it by name

IT WAS TAKEN, so this branch carries the newest of them.

    git merge-base --is-ancestor f4a21c0e HEAD  ->  exit 0

The merge commit is ecb7857b. git detected the source-to-src rename and moved the whole of source/engine and source/viewer into src/ by itself, which is why thirteen commits over a renamed tree cost seven conflicts rather than every file.

THE SEVEN. Four were file location: cage_test.go, twonames_test.go, frame.go and frame_test.go landed under source/ and belong under src/, placed there unchanged. .mcp.json and .copilot/mcp-config.json are theirs, because both are generated from util/cage and taking the absolute paths out of them was the point of the upstream work, while mine carried this machine's paths. util/setup/main.go is both: upstream generalised engineSays to take the args the caller names, because installing now asks the engine two things rather than one, and mine wrapped the call in Quietly so no console window flashes up. Neither replaces the other, so the merge has the general call inside the quiet wrapper.

THE REASONING is in .se/scratchpad/field-report.md under THE VERDICT ON UPSTREAM, with all thirteen named and what each one does, and under THE INHERITED CHECKS, RUN AGAINST THE DEFECT THEY NAME, which is what the reason now rests on rather than on having read them.

## evidence: The whole battery is green afterwards, whichever way it went. It is green today, so it is not a command criterion: one that passes before the work cannot report on the work. The evidence answers it with what the run said

The whole battery, run after the merge and after both cage checks were fixed: all ok, every line.

    go build         ok
    go test          ok   ok  quackitect/engine
    go test mcp      ok   ok  quackitect/mcp
    go test viewer   ok   ok  quackitect/viewer
    se lint          ok
    render-check     ok   0 failed.
    drive-editor     ok   14 message(s) sent. 0 failed.
    engine-args      ok   0 failed.
    engine-args-lifecycle ok   0 failed.
    engine-spawns    ok   8 spawn(s) read. 0 failed.
    one-look         ok   0 failed.
    panel-icons      ok   0 failed.
    no-loose-glyphs  ok   24 glyphs declared, 13 file(s) read. 0 failed.
    no-loose-spawns  ok   7 file(s) read. 0 failed.
    all ok

IT WAS NOT GREEN ON THE FIRST RUN AFTER THE MERGE. go test viewer failed on TestFrameOnAFileThatIsNotThere, which came in with the merge, and it was a real defect rather than a fixture problem: src/viewer/ui.go cut the waiting line at the window width from the right, so a long path lost its file name and left the directories every file on this machine shares. It is shortened from the front now, and when even that will not fit the sentence goes before the name does.

AND IT WAS NOT GREEN A SECOND TIME. Once the cage check could see a forward-slash path, three committed projections were red. Re-projecting with the current build fixed them and a stale engine wrote them back, so both stale engines were stopped. The battery above is the run after that.

## evidence: finding 4 · round 3 · the checks the reason leans on are run

CLOSED, and both halves of what you said were true.

THE CHECK THE CAGE WORK TURNS ON COULD NOT FAIL HERE. TestNoCagedFileNamesTheMachineItWasWrittenOn compared every projected line against filepath.Abs of the root, which on this platform is spelled with backslashes. Every file the cage writes uses forward slashes, because that is what JSON and a shell both want. So it looked for the one spelling nothing writes. On Linux the two are the same string, which is why it passed review upstream and arrived green here.

WATCHED: with this machine's full path written into util/cage/mcp.json in place of {{method}}, with forward slashes exactly as a projection writes one, the check was green.

THE FIX. namesTheMachine answers whichever spelling a text carries, and both cage checks ask it. Then, on the same injection:

    TestNoCagedFileNamesTheMachineItWasWrittenOn   RED
      cage_test.go:57: claude tool lane (.mcp.json) line 6 names this machine as
      C:/Users/ichbi/Desktop/ai/quackitect-v4
                                                  GREEN
    TestNoCommittedProjectionNamesTheMachine       RED
      cage_test.go:83: .mcp.json names this machine as
      C:/Users/ichbi/Desktop/ai/quackitect-v4. Run the engine to write it again
                                                  GREEN

AND IT FOUND A LIVE DEFECT THE SAME MINUTE. Three committed cage files named this machine: .mcp.json, .copilot/mcp-config.json and .claude/settings.json, the last one fourteen times. That is the state 02e8789d describes in as many words, and I had walked straight into it: two engines from an older build were still running, they re-project when guidance changes, and they write what their own build knew how to write.

I re-projected with the current binary, and one of the old engines wrote all three back within seconds. An engine cannot restart itself because whoever started it decides that, which is f4a21c0e. So I stopped both, started 17:33 and 19:35, re-projected, and the three files are right and stay right. Nothing is running an engine here now: the extension starts one when the panel opens and the SessionStart hook starts one in a box with no panel, which is 715c0a8b, and both were going to end with the machine anyway.

WHAT I DID NOT DO, and it is the scope you drew. I did not run all thirteen commits' tests against their defects. The reason the verdict rests on is that the cage work is a rule stated in one place and checked nowhere, applied to the projections, so the checks that reason leans on are the two cage ones. Those are the ones run.

THE RULE IS BEING WRITTEN DOWN RATHER THAN REMEMBERED. wk-8573243384, which you minted, is drafted and its spec is submitted: before a verdict rests on each commit carrying its own test, run the ones the reason leans on against the defect they name. An inherited check arrives already green, which is the easiest place in the world to skip watching it fail, and this one had been through a review and was correct in its reasoning and dead on the platform it landed on.

## done when

- The field report carries the verdict under a heading of its own
  `rg -q THE.VERDICT.ON.UPSTREAM .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The newest of the thirteen is named
  `rg -q f4a21c0e .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- And the oldest, whose parent is the merge base
  `rg -q 4bfcaf32 .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit 1fda1255 is named
  `rg -q 1fda1255 .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit 7d3cd486 is named
  `rg -q 7d3cd486 .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit 6522bcbb is named
  `rg -q 6522bcbb .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit 02e8789d is named
  `rg -q 02e8789d .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit 0b1d0da9 is named
  `rg -q 0b1d0da9 .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit bc75704e is named
  `rg -q bc75704e .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit c1bb44b6 is named
  `rg -q c1bb44b6 .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit 4deaa415 is named
  `rg -q 4deaa415 .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit 1151665a is named
  `rg -q 1151665a .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit d6d7e341 is named
  `rg -q d6d7e341 .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The commit 715c0a8b is named
  `rg -q 715c0a8b .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The verdict is one word the report carries plainly, taken or left, with the reason under it
  `rg -q THE.ANSWER.IS .se/scratchpad/field-report.md`
  **red without** the field report's verdict section, the report itself left in place
  **red said** exit: exit status 1, so rg found nothing and exited one
- The whole battery is green afterwards, whichever way it went. It is green today, so it is not a command criterion: one that passes before the work cannot report on the work. The evidence answers it with what the run said
- If it is taken, this branch carries the newest of them. If it is left, the report says what would have to be true for the answer to be different. One command cannot be true of both outcomes, so the evidence answers it by name

## finding 1 · round 1 · Criterion 3: 'And the oldest of them, so the range is not guessed at' with `rg -q 1151665a .se/scratchpad/field-report.md` · by reviewer5

**wrong:** 1151665a is not the oldest of the thirteen; it is the tenth. `git rev-list $(git merge-base HEAD origin/v4)..origin/v4 | tail -1` answers 4bfcaf32, whose parent 460c56b8 IS the merge base, so nothing in the set is older. `git rev-list --count 1151665a..f4a21c0e` answers 9, against the detail's own THIRTEEN COMMITS AHEAD, which `git rev-list --count v4..origin/v4` confirms is 13. So a worker who satisfies this criterion writes a false range into the field report and the criterion still exits zero: the check cannot fail for the defect it is there to prevent, which is the range being guessed at. Check seen RED today: `rg -q "$(git rev-list $(git merge-base HEAD origin/v4)..origin/v4 | tail -1 | cut -c1-8)" doc/work/wk-c6247665a3.md` exits non-zero now. Swept the other two literals in the same pass: f4a21c0e in criterion 2 is correct (newest, 2026-08-31 19:45:46), and criterion 1's pattern is fine. This is the only wrong literal.

**satisfies:** Criterion 3 names 4bfcaf32, or better derives it: `rg -q "$(git rev-list $(git merge-base HEAD origin/v4)..origin/v4 | tail -1 | cut -c1-8)" .se/scratchpad/field-report.md`. The same check run against doc/work/wk-c6247665a3.md then exits zero, which closes this.

## finding 2 · round 1 · The spec notice: 'A criterion that can be a command has to be one, because a command fails and a sentence does not.' · by reviewer5

**wrong:** Criteria 4 and 5 carry no `runs` at all, and each has a half that is already a command sitting in this tree. Reproduced: `python .se/scratchpad/run-criteria.py doc/work/wk-c6247665a3.md` reports only 3 criteria, 3 red - criteria 4 and 5 are invisible to it, so the gate that refuses a submission on a red command criterion cannot see them. Criterion 4's 'every one of the thirteen is named' is thirteen rg calls. Criterion 5's 'the whole battery is green afterwards' is exactly `sh .se/scratchpad/battery.sh`, which I ran to completion just now and which answers 'all ok', and 'this branch carries those commits' is `git merge-base --is-ancestor f4a21c0e HEAD`. Only the judgement halves - taken or left, one reason for the set, what would have to be true for the answer to be different - are genuinely prose.

**satisfies:** Split 4 and 5 so the mechanical half is a criterion with a command and the judgement half is a criterion without one. Concretely: (a) 'every one of the thirteen is named in the field report' with `for h in f4a21c0e 1fda1255 7d3cd486 6522bcbb 02e8789d 0b1d0da9 bc75704e c1bb44b6 4deaa415 1151665a d6d7e341 715c0a8b 4bfcaf32; do rg -q $h .se/scratchpad/field-report.md || exit 1; done`; (b) 'the battery is green' with `sh .se/scratchpad/battery.sh`; (c) if taken, 'this branch carries them' with `git merge-base --is-ancestor f4a21c0e HEAD`. run-criteria.py then reports 6 criteria instead of 3, and (a) is red today.

## finding 3 · round 2 · done when: And the eleven between them, each by its own short name, so the reader can see that every one was looked at / runs: rg -q 715c0a8b .se/scratchpad/field-report.md · by reviewer4

**wrong:** THE WORDS COVER ELEVEN COMMITS AND THE COMMAND COVERS ONE. The criterion reads 'And the eleven between them, each by its own short name, so the reader can see that every one was looked at', and its runs is rg -q 715c0a8b .se/scratchpad/field-report.md. One hash. The other ten are named in the sentence and checked by nothing, so the whole point of the criterion -- that the reader can see every one was looked at -- is the part that cannot fail. REPRODUCED, and it takes nine lines. .se/scratchpad/reviewer4/field-report-as-the-criteria-allow.md is a document with a heading THE VERDICT ON UPSTREAM, a line THE ANSWER IS left, and the sentence 'I read f4a21c0e, and 4bfcaf32, and 715c0a8b. I did not read the other ten and this document does not name them.' Run this token's five command criteria against it, pattern for pattern, and all five are green. Then count how many of the thirteen it names: 3 of 13. A worker can say in the report that it did not look at ten of them and satisfy every command on the token. AND THE LOOP WAS ALREADY WRITTEN FOR YOU. Round 1's second finding handed over exactly the command that covers the set -- for h in f4a21c0e 1fda1255 7d3cd486 6522bcbb 02e8789d 0b1d0da9 bc75704e c1bb44b6 4deaa415 1151665a d6d7e341 715c0a8b 4bfcaf32; do rg -q $h .se/scratchpad/field-report.md || exit 1; done -- and what shipped is one rg from the middle of that list. EXTENT, one pass over the five commands. The other four are each about a single thing and each checks that thing, so this is the only one whose sentence is wider than its command. I confirmed the two literals in criteria 2 and 3 against the tree rather than taking them: git rev-list --count $(git merge-base HEAD origin/v4)..origin/v4 answers 13, the list is f4a21c0e 1fda1255 7d3cd486 6522bcbb 02e8789d 0b1d0da9 bc75704e c1bb44b6 4deaa415 1151665a d6d7e341 715c0a8b 4bfcaf32, newest first, so f4a21c0e is the newest and 4bfcaf32 the oldest. Round 1's first finding, that criterion 3 named the tenth commit rather than the oldest, is CLOSED and the number is now right. AND THE TWO PROSE CRITERIA ARE RIGHT TO BE PROSE, which is the other half of round 1's second finding and I am not reopening it. 'The whole battery is green afterwards' declines a command with a good reason -- it is green today, and a criterion that passes before the work cannot report on the work -- and the taken-or-left criterion declines one because no single command is true of both outcomes. Both say so in the criterion itself, which is what a decline should look like. AND EVERY COMMAND IS RED TODAY, which is what a draft before its work should look like: python .se/scratchpad/run-criteria.py doc/work/wk-c6247665a3.md answers 5 red, 0 green. They are red because .se/scratchpad/field-report.md, which does exist and is 889 bytes of a different report, names 0 of the thirteen.

**satisfies:** MAKE THE COMMAND COVER THE SET THE SENTENCE COVERS. Replace the single rg with the loop round 1 already wrote, over all thirteen so the newest and the oldest are checked by the same mechanism as the eleven: for h in f4a21c0e 1fda1255 7d3cd486 6522bcbb 02e8789d 0b1d0da9 bc75704e c1bb44b6 4deaa415 1151665a d6d7e341 715c0a8b 4bfcaf32; do rg -q $h .se/scratchpad/field-report.md || exit 1; done. That is red today at 0 of 13 and it goes green only when every one is named, which is what the sentence promises. Criteria 2 and 3 can stay as they are or fold into it; either is honest once the loop exists. BETTER, DERIVE THE LIST RATHER THAN TYPING IT, the way round 1 derived the oldest: for h in $(git rev-list $(git merge-base HEAD origin/v4)..origin/v4 | cut -c1-8); do rg -q $h .se/scratchpad/field-report.md || exit 1; done. Then the criterion cannot go stale if the cloud agent pushes a fourteenth while this token is open, which is a thing that can happen between now and the work being done. Say in the criterion which of the two you chose, because a typed list is a promise that the range is frozen. AND SAY IN THE DRAFT WHAT EACH COMMAND ANSWERED WHEN YOU RAN IT, before the work: five red is the right answer today and it is worth writing down, because a criterion nobody has run is a guess about a file that does not exist yet.

## finding 4 · round 3 · detail: THE ANSWER IS A REASON EITHER WAY. Merging is not the deliverable and neither is refusing. What this owes is a judgment with the evidence under it. / field report, WHY IT MAKES SENSE: Every one of them is a hole a Linux clone or a cloud session actually fell into, and each carries its own test · by reviewer6

**wrong:** THE REASON THE VERDICT RESTS ON IS NOT TRUE OF THE CHECK THE CAGE WORK TURNS ON. The field report's WHY IT MAKES SENSE says "Every one of them is a hole a Linux clone or a cloud session actually fell into, and each carries its own test", and names the class of fault as "a rule stated in one place and checked nowhere, or checked in a fixture rather than on the product's own list. The cage work is that lesson applied to the projections." That is the argument for taking it. The check it points at cannot fail on this platform.

**satisfies:** RUN THE INCOMING CHECKS AGAINST THE DEFECT THEY NAME, AND PUT WHAT THEY SAID IN THE REPORT. Not all of them and not for long: the ones the verdict's reason leans on. For each, break the thing it guards, watch it go red, put it back, watch it go green, and write the two lines into the field report under the commit that brought it. That is the same standard this project now refuses its own submissions for going without, and inherited work is where it is easiest to skip because the check arrives already green.

## lesson 1 · round 1 · by reviewer5

**the class:** A criterion greps for a literal that encodes a claim about the tree, and the literal was never derived by running the command that establishes it. The check then goes green exactly when the deliverable states something false, because rg proves only that the string is present.

**instead:** Derive every literal a criterion asserts - a commit id, a count, a path, a line number - by running the command that produces it BEFORE writing the criterion, and put that command in the criterion. Where the literal can be derived at check time, derive it inside the check instead of pasting it, so the check goes red when the fact moves.

**minted as:** wk-dcb697c1b1

## lesson 2 · round 2 · by reviewer4

**the class:** A criterion whose sentence is about a set and whose command is about one member of it. The sentence carries the intent -- every one was looked at -- and the command carries a single sample, so the check is green for a report that says outright it looked at three of thirteen. It is the same shape as a test that asserts one element of a list and calls the list covered, and it is harder to see in a spec because the sentence beside it reads as though it were the check. The remedy had already been written out in full in the previous round's finding and what shipped was one line from the middle of it.

**instead:** When a criterion says every, each, all, or the rest, the command has to iterate and fail on the first miss -- a loop with || exit 1, not a sample. Read the sentence and the command side by side and ask what the command would let through: if you can write the document that passes it and fails the sentence in under ten lines, write it, and you have your answer. Derive the set in the command where the tree can answer for it, so the criterion cannot go stale between agreeing the draft and doing the work. And when a previous round handed you the exact command, use it or say why not, because taking half of it is indistinguishable from taking all of it in the diff.

**minted as:** wk-10d3cf13cd

## lesson 3 · round 3 · by reviewer6

**the class:** WORK ADOPTED ON THE STRENGTH OF ITS TESTS, WITH NONE OF THEM RUN AGAINST THE DEFECT IT NAMES. Somebody else's branch is read, the commit messages are good, each commit carries a test, and the verdict is taken. "Each carries its own test" is a claim about those tests and it is made by reading them. A test that is read is green; green is also what a test that cannot fail looks like, and guarding something was the whole reason for adopting the work. Inherited work is where this project's own rule -- a check nobody has watched fail is a check nobody has tested -- is easiest to forget, because the check arrives already green and there is no moment at which anybody had to make it go red. On wk-c6247665a3 the central incoming check, TestNoCommittedProjectionNamesTheMachine, compares a committed file against filepath.Abs's backslash spelling of the root while the projector writes filepath.ToSlash's forward-slash spelling, so it cannot fail on Windows: three committed projections name this machine today and it is green, and at the merge commit itself .claude/settings.json named it on fourteen lines. Under it sits the narrower class -- a check comparing two spellings of one thing, a path with either separator, a name in either case -- written in the spelling of the checker rather than the spelling of the artefact.

**instead:** When you take somebody else's work because it is guarded, break what each guard guards and watch it go red before counting it as a reason, then write what it said into the report beside the commit that brought it. One defect per check, one pass; if a check cannot be made to fail in a few minutes, that is the finding rather than a delay. And when a check compares two spellings of the same thing, open the artefact and see which spelling is actually in it, or normalise both sides before comparing -- never write the comparison in the form your own language hands you and assume the file agrees.

**minted as:** wk-8573243384

