---
id: wk-7e98c419e7
seq: 1000004
type: work
title: a fix stops early
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer8
---

## detail

A fix scoped to the level the finding named, leaving the same class standing one level up. The tell is that the remedy still contains a list. On wk-24be1c06ae the redraft walks the fields of Criterion, Rejection and Lesson with reflect, but the three types are typed in. Token.Detail and Token.Guidance are not in the walk. When you answer a finding, answer the class and write into the draft where the fix stops and why the set is closed. Where the language can, derive the outer set too: start reflect from Token and follow its exported struct and slice-of-struct fields. The check, red today: save a token whose detail contains a line beginning with two hashes and read it back. It comes back cut at that line.
