---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: store argues in place
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/main
claimed_at: "2026-09-05T17:25:15Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0134eda156d1a65a2fcf938b5e16ab44fe241905
---

## detail

A follow-up on wk-5b51fc14e4, which made rationale a kind and moved two arguments out. src/engine/store.go still argues in place. Measured today: 765 lines, 131 of them comment, and 18 of those open a paragraph in capitals. The store is the file every reader of a token reaches first, so the cost is paid often. The shape is settled and shipped. A rationale note under doc/rationale carries decided, why, costs and revisit when. The instruction keeps one line naming it, and TestARationaleIsNamedNotRepeated holds the two halves together. Nothing new is decided here. The arguments already written move to where an argument belongs.

## done when

- every argument store.go used to carry is a rationale it names by link: se find --regex "Why it is this shape" --path src/engine/store.go answers four lines
- store.go carries no argument a rationale should hold, judged by a reader over its comments, and the four moved are named in the note
- every argument moved is a rationale note that store.go names by link, driven by TestARationaleIsNamedNotRepeated

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One file loses 46 lines and gains 17. Four notes arrive. | 5 files |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one runs se find. Line three runs a named test. Line two is a reader's judgement and says so. | 3 of 3 |
| [x] | the basics it stands on exist, or are minted first | The rationale kind, the schema and the test all shipped with wk-5b51fc14e4. Four notes already used them. | 4 shipped |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Nothing new was decided. The arguments already written moved to where an argument belongs. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | The test shipped with the parent token and drives both halves. It refuses a note nothing links to, and a sentence still standing where it left. | driven, not written |
| [x] | the same test was seen green after the change, and named | TestARationaleIsNamedNotRepeated is ok over the shipped corpus. build, vet and gofmt are clean. | ok, 8 notes |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | store.go loses 46 lines and gains 17. Four notes are new. | 17 in, 46 out |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | One argument stays. The MEASURED paragraph in noteMove reasons about a burn-down that read nought. It is a fifth rationale. | wk-763e2eb1a6 |

