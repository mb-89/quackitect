---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: battery names two fewer
# where the token stands. The process owns these values.
status: open
---

## detail

Found while adding a check to the battery. checks-live-in-the-method says the battery runs every check in util/checks, and it answers FAIL on the branch today: archive-rows-travel and the-cage-cites-what-is-here sit in util/checks and the for loop in util/checks/battery.sh names neither, so they are checks nobody runs. Both were written to guard something and neither is guarding it in the battery. Run by hand over the branch, the-cage-cites-what-is-here answers 0 failed and archive-rows-travel answers 7 failed of 119 rows read, so listing them is not free: one of them turns the battery red on rows whose object no clone of the branch carries.

## proposed action

List the-cage-cites-what-is-here in the battery. Then decide the seven rows archive-rows-travel refuses, either by repairing them or by ruling what the check should say about a row whose object is on no branch, and list it too.

## done when

- the battery names every check in util/checks: node util/checks/checks-live-in-the-method.mjs answers 0 failed
- sh util/checks/battery.sh answers 0 failed

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Two names added to one line of battery.sh. No check changed. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Two lines, each naming its command. The second cannot be met from this box, and step 2 says why. |  |
| [x] | the basics it stands on exist, or are minted first | Both checks exist in util/checks on origin, and each answers on a worktree of it. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. The judge was watched red before the list moved. |  |
| [x] | one test was written first and seen red for the reason expected | checks-live-in-the-method answered 1 failed on origin: the battery runs every check in util/checks. |  |
| [x] | the same test was seen green after the change, and named | The same check answers 0 failed with both listed, and the-cage-cites-what-is-here answers 0 failed beside it. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began is e9c76d70 and is no object here. The change is one line of battery.sh, on origin as 1d0ea7e9. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-2a0d33d3be, needs_human. archive-rows-travel answers 6 failed on notes held by their closing box alone. |  |