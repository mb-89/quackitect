---
id: wk-02e17b9eb4
seq: "-26"
type: work
title: the set enumerates itself
status: spec_submitted
assignee: main
scope: single-step
traced: true
rounds: 2
minted_by: reviewer6
---

## detail

TWO SHAPES, ONE CLASS, AND wk-c15a6a80a3 IS SETTLED AS THE SAME THING. It named
a pattern standing in for an enumeration: a check told to count from the side
that produces the thing searches the source with a regular expression, and a
regular expression matches a SHAPE while the set is defined by a BEHAVIOUR.

THE TELL IS THE SAME IN BOTH. The check describes the set instead of asking it.
A hand list describes it by typing it out. A pattern describes it by saying what
its members look like. Both are complete on the day they are written and neither
knows when it stops being.

AND BOTH WERE MEASURED HERE. engine-spawns.mjs matched a spawn whose arguments
are an inline array and read eight of nine, and the ninth was the one that wrote
the engine's flags at its call site. A hand list of nine fields on this note's
structs would have missed the tenth, which is the moment the token about losing
data exists for.

THE HAND-LIST INSTANCE IS wk-24be1c06ae's AND IS OUT OF THIS TOKEN. That token
carries the reflective walk over every exported string field of Criterion,
Rejection and Lesson, which is the remedy for the shape, and doing it twice
would leave two walks disagreeing. This token fixes the pattern instance,
engine-spawns.mjs, and writes the class down. The exclusion is written here so a
reader can tell it from an oversight.

WHAT TO DO INSTEAD, and it differs by shape. Where the language can enumerate,
ask it: reflect over the struct, read the map, walk the registered list. Where
it cannot, because the set is a behaviour in somebody else's source, the count
is compared with something the check did not produce, and a check that finds a
different number than it expected says so rather than reporting what it found.



CLASS: A HAND-WRITTEN LIST STANDING IN FOR A SET THE LANGUAGE CAN ENUMERATE.

A check is written over every member of a set, and the members are typed into
the check. It is complete on the day it is written, it iterates and fails on the
first miss, and it looks like the remedy for the sample-instead-of-a-set
mistake -- wk-10d3cf13cd, doc/guidance/specifying.md, "A SET COVERED BY ONE
MEMBER". It is that mistake one step out. The command walks a set, but not the
set the sentence is about: it walks the list, and the list is a photograph of
the set taken once.

WHAT IT COSTS. The check stays green when a member is added, which is the only
moment it was ever needed. Nothing goes red, nobody is told, and the person
adding the member has no way to learn that a rule existed. An anti-vacuity guard
does not help: refusing an empty list catches the list being deleted and says
nothing about the list being short.

THE TELL. Ask where the set is defined. If it is a struct, an enum, a directory,
a JSON list or a switch, the language or the tree can hand you its members and
the check should ask for them. If the answer is "in the check", the check is the
only place that knows, and it will go on being right about a world that has
moved.

FOUND ON wk-24be1c06ae, whose whole subject is a field added to a record without
anybody asking what happens to a value that does not fit. Its round 2 criteria
say "One check walks every field the note writes and reads back, names each one
and the shape it is, and refuses if it finds none to walk, so it cannot pass by
the list having gone", and the detail lists nine fields in three shapes. Nine is
right today. The next field added to Criterion, Rejection or Lesson is not in
the list, is not walked, and is exactly the event the token exists to stop --
the token would fail at its own purpose one field later, silently, which is the
same silence it was minted about. The structs already carry a field the list
does not name, Criterion.Ran, and although that one is dead the list has no way
to say so.

WHAT TO DO INSTEAD. Where the set is a Go struct, walk it with reflect: take
every exported string field of Criterion, Rejection and Lesson, require each to
appear in the shape table, and fail naming any that does not. The check then
goes red on the commit that adds the field rather than on the incident that
follows, and the message tells the author which of the three shapes they have to
choose. Where reflection is not available, derive the list from wherever the set
really lives -- read the directory, read the JSON, parse the switch -- and never
from memory. And when a member is deliberately out of scope, put it in the table
with that answer written down, so a reader can tell an exclusion from an
oversight.

THE CHECK, RED TODAY: add a field to Criterion, or to Rejection, and require the
walk to name it. With a typed list of nine, nothing goes red. With a reflective
walk, it does, and that difference is the whole of this lesson.

## done when

- The class is written in doc/guidance/specifying.md under The command decides the sentence above it, and the check reads the section that heading opens rather than the whole file
  `rg -q func.TestTheEnumerationRuleIsWhereItSays src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleIsWhereItSays$ .`
- It is also written in doc/guidance/behaviour.md under Evidence, in the part about a check that cannot go red, and the check reads that section rather than the whole file. BOTH FILES, because behaviour.md is projected and specifying.md is the source of no projection, so a class written only in specifying.md reaches nobody but a reader of this repository
  `rg -q func.TestTheEnumerationRuleIsAlsoWhereItProjects src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleIsAlsoWhereItProjects$ .`
- Both shapes, a hand list and a pattern, and the tell they share, are in doc/guidance/specifying.md under that heading AND in doc/guidance/behaviour.md under Evidence. The check reads both sections and fails naming the file that is short, rather than confirming one and being silent about the other
  `rg -q func.TestTheEnumerationRuleNamesBothShapesInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleNamesBothShapesInBothFiles$ .`
- The measurement it came from is in both of those sections, eight of nine spawns read and the ninth the one that had kept its literals, and the check fails naming the file that is short
  `rg -q func.TestTheEnumerationRuleCarriesItsMeasurementInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleCarriesItsMeasurementInBothFiles$ .`
- What to do in each shape is in both of those sections, and the two answers differ: ask the language where it can enumerate, and compare the count against something the check did not produce where it cannot. The check fails naming the file that is short
  `rg -q func.TestTheEnumerationRuleSaysWhatToDoInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleSaysWhatToDoInBothFiles$ .`
- The rule reaches every agent, and the targets are derived rather than typed: the check reads util/projections.json for every target whose sources include doc/guidance/behaviour.md, refuses when that answers none, and requires the rule in each one, failing by name on the first miss
  `rg -q func.TestTheEnumerationRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleReachesEveryProjection$ .`
- The pattern instance is fixed, and the criterion exercises the disagreement rather than searching for a phrase about it. A runner plants a spawn in a shape the pattern cannot read, in a copy of src/extension and never in the tree, runs engine-spawns.mjs against that copy, and requires a non-zero exit with both numbers in the output, the count the pattern read and the count taken the other way
  `node util/checks/engine-spawns-catches.mjs .`
- And the check still passes on a clean tree. THIS ONE ASSERTS ONLY THE GREEN CASE, and it is here because a check that goes red on the planted spawn and red on everything else would satisfy the criterion above while being useless
  `node util/checks/engine-spawns.mjs .`
  **red without** a builder exported that no spawn spreads and no door is handed, planted in a copy of src/extension
  **red said** engine-spawns.mjs: FAIL engineargs.strandedArgs reaches the engine, 27 exported, 8 spread, 17 through a door, 1 not a builder
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. Each of the three both-files criteria was watched twice, once with the property taken out of specifying.md and once out of behaviour.md, and each red names the file it found short. THE EVIDENCE NAMES THE TEST AND WHAT IT SAID RATHER THAN A LINE NUMBER, because a line moves when anybody adds a function above it and the message does not

## finding 1 · round 1 · done when, criterion 5: the rule reaches every agent, because the guidance is projected and a rule only one harness reads is a rule the others do not have · by reviewer8

**wrong:** This criterion commits the exact class the token is about, on the exact set the tree already enumerates. The command `for f in AGENTS.md .github/copilot-instructions.md .claude/output-styles/quackitect.md; do rg -q describes.the.set $f || exit 1; done` types out the members of a set that is declared in util/projections.json, which the detail's own THE TELL names as an enumerable source: "If it is a struct, an enum, a directory, a JSON list or a switch, the language or the tree can hand you its members and the check should ask for them." I read that declaration: it holds seven projections, of which exactly three take their sources from doc/guidance, so the typed list is right today and is a photograph, which is the whole of this token's lesson. The remedy is already standing in this tree and was not called: src/engine/search_test.go carries projectionTargets, a helper that reads util/projections.json and answers every target whose sources include a given guidance file, and TestTheSearchRuleReachesEveryProjection uses it under a doc comment that reads "A HAND-DRAWN LIST IS EXACTLY THE SIZE OF WHAT YOU ALREADY LOOKED AT. Declare a fourth projection from the same source and the claim stays true of its three while the fourth goes without the rule." The same advice was written to this draft's author on wk-8573243384's round 1, in the words "BETTER, DERIVE THE THREE RATHER THAN TYPING THEM ... read util/projections.json for every target whose sources include doc/guidance/behaviour.md and walk those, so a fourth projection added next month is covered", and wk-8573243384 line 134 still carries the same typed list, so I swept the four waiting specs and the defect reaches two of them rather than one. The criterion is also aimed at a set the rest of this spec does not write into: criteria 1 to 4 put the class under the heading a drafter reads for what a criterion has to be, which is doc/guidance/specifying.md, and specifying.md is the source of no projection at all, so nothing criteria 1 to 4 produce can ever satisfy criterion 5; the literal it greps for, describes the set, is in no file in this tree today, while specifying.md line 61 already says "describes the members".

**satisfies:** Replace the typed list with a walk derived from the declaration, and say in the sentence which source file the rule has to land in. A Go test is the shortest route because the helper already exists: call projectionTargets(t, root, "doc/guidance/behaviour.md"), fail if it answers none so the check cannot pass by the declaration having gone, and require the rule's sentence in every target it answers, exactly as TestTheSearchRuleReachesEveryProjection does for the search rule. If it stays a shell command, derive the targets inside the command rather than typing them: read util/projections.json for every target whose sources include doc/guidance/behaviour.md, pipe those into a loop that greps each and exits 1 on the first miss, and refuse when the derived list is empty. Then make criteria 1 to 4 and criterion 5 name one deliverable by saying in the sentence that the class is written into doc/guidance/behaviour.md as well as into specifying.md, since only the former is projected. And run it against the failing case before agreeing it: take the sentence out of one of the three targets, watch the derived walk go red and name that file, and put what it answered into the draft.

## finding 2 · round 1 · done when, criterion 6: the instance each shape was found on is fixed, and the evidence names both. One command cannot be true of two different files, so the evidence answers this by name · by reviewer8

**wrong:** This is the only criterion that decides whether either instance was actually repaired, and it is prose on a stated ground that is false: "one command cannot be true of two different files" -- two criteria can, and this project's own remedy for a set is to write the loop and fail on the first miss, so one command can too. The cost is not theoretical, because the pattern instance is not fixed under the definition this token itself gives. I read util/checks/engine-spawns.mjs at HEAD: the regex was widened to follow a bare name back to the nearest array assigned above it, and bash util/checks/battery.sh now reports "engine-spawns ok 9 spawn(s) read. 0 failed", but the nine is still the tally the check produced, compared with nothing, and its only guard is say("the extension starts the engine somewhere", found.length > 0), which is precisely the anti-vacuity guard the detail says does not help -- "refusing an empty list catches the list being deleted and says nothing about the list being short." The token's own WHAT TO DO INSTEAD for this shape is that "the count is compared with something the check did not produce, and a check that finds a different number than it expected says so rather than reporting what it found", and nothing in engine-spawns.mjs does that, so a tenth spawn written in a tenth shape leaves it green at nine, which is the same silence that produced eight. As prose, criterion 6 is satisfiable by a reviewer reading "the regex now matches two shapes" and agreeing, so the shape-instead-of-behaviour fix passes the token minted to stop it, one round later.

**satisfies:** Split criterion 6 into one criterion per instance and give each the command that decides it. For the pattern instance, require that engine-spawns.mjs holds its count against a number it did not produce and fails when the two differ -- a second count taken a different way, such as every line under src/extension that names the engine binary, with the check printing both and exiting non-zero on a disagreement -- and make the criterion prove that by introducing a spawn in a third shape and requiring the check to go red rather than by running the happy path. For the hand-list instance, name wk-24be1c06ae's reflective walk by its test function and run it, the way criteria 1 to 4 already name theirs. If either instance is deliberately outside this token, write that in the detail with the reason, which is what this token's own WHAT TO DO INSTEAD demands: put the excluded member in the table with the answer written down, so a reader can tell an exclusion from an oversight.

## finding 3 · round 2 · done when, criteria 3, 4 and 5: "It names both shapes, a hand list and a pattern, and says the tell they share", "It carries the measurement rather than the claim: eight of nine spawns read, and the ninth the one that had kept its literals", "It says what to do in each shape, and the two answers differ" · by reviewer9

**wrong:** Three of the eight criteria name no file, and this token puts the class into two. Criterion 1 asserts doc/guidance/specifying.md under The command decides the sentence above it, section-scoped. Criterion 2 asserts doc/guidance/behaviour.md. Criteria 3, 4 and 5 then open with the word it, with two candidate referents and nothing choosing between them. I reproduced it rather than arguing it. I copied both guidance files into a scratch Go package, wrote the class in full into behaviour.md, and wrote one line into specifying.md under the heading criterion 1 names: A HAND-WRITTEN LIST STANDING IN FOR A SET THE LANGUAGE CAN ENUMERATE. Then I wrote the checks the criteria describe, a section-scoped read of specifying.md for criterion 1 and content assertions over behaviour.md for criteria 3, 4 and 5. go test -count=1 ./... answered ok. So this token can close with specifying.md carrying the class name and neither shape, neither the measurement, nor either of the two answers, and every command criterion green. The extent is the whole list and I checked the rest before writing this. Criterion 6 derives its targets from behaviour.md alone, correctly, since specifying.md is the source of no projection in util/projections.json. Criterion 8 asks for a red per test and not a red per place. So nothing on the list asks after the content of specifying.md beyond the one string criterion 1 looks for. That is this token's own class with the file playing the part of the member: the criteria describe the rule instead of asking each place for it. It is also the second draft in this round with the shape, which is why I minted it: wk-0146942977 carries the same three unsubjected criteria under the same two files.

**satisfies:** Name the subject in criteria 3, 4 and 5, by path, and make each one walk both places rather than confirm one. Write criterion 3 as: both shapes and the tell they share are in doc/guidance/specifying.md under The command decides the sentence above it and in doc/guidance/behaviour.md, and the check reads both and fails naming the one that is short. Write criteria 4 and 5 the same way, for the measurement and for the two answers. Say in criterion 2 which section of behaviour.md the rule goes under, so that check can be scoped the way criterion 1 already is, since a whole-file search is green with the rule under any heading. Then take two reds on each of the three: remove the property from specifying.md's copy, watch the failure name specifying.md, put it back, remove it from behaviour.md's copy, watch the failure name behaviour.md. If you would rather keep one red per criterion, split each of the three into two, one per file.

## finding 4 · round 2 · done when, criterion 7: "The pattern instance is fixed: util/checks/engine-spawns.mjs holds the number it read against a number it did not produce, taken a different way, prints both, and exits non-zero when the two disagree" / runs: rg -q counted.a.second.way util/checks/engine-spawns.mjs && node util/checks/engine-spawns.mjs · by reviewer9

**wrong:** The command decides the sentence by searching the source for a phrase, which is the shape doc/guidance/specifying.md calls A LITERAL NOBODY DERIVED. The sentence claims four properties: a second count, taken a different way, both printed, and a non-zero exit when they disagree. The command asks two things. It asks whether the file anywhere contains the words counted a second way, and it runs the check on the tree as it stands, where the two counts agree by construction. Neither half can see any of the four properties. I reproduced it: I copied util/checks/engine-spawns.mjs to the scratchpad, appended one comment line reading that the number below is counted a second way by nobody at all, and ran the criterion. The grep exits zero and the check exits zero, so the criterion is green over a file with no second count in it at all. This is the defect round 1's finding 2 named, one step along: it said the pattern instance is not fixed because the nine is a tally compared with nothing, and its satisfies asked that the criterion prove the disagreement by introducing a spawn in a third shape and requiring a red rather than by running the happy path. The redraft moved the property into the sentence and left the happy path in the command, with a phrase search stapled in front of it. It matters most on this token, whose whole subject is a check that describes its set instead of asking it: the criterion describes the remedy instead of exercising it.

**satisfies:** Make the command exercise the disagreement rather than search for a phrase about it. The shortest form is a second script or a flag that plants a spawn the pattern cannot see, runs engine-spawns.mjs against a copy of src/extension holding it, and requires a non-zero exit and both numbers in the output. Then the criterion is red today, goes green when the second count lands, and goes red again if somebody deletes it. Keep node util/checks/engine-spawns.mjs on the happy path as a second criterion if you want the green case asserted, and say in that sentence that it asserts only that the check still passes on a clean tree. Drop the phrase search: a string in a comment satisfies it and nothing about the string is derived. Then record the red with the file and line the failure named, and say which two numbers disagreed and what each was counted from.

## lesson 1 · round 1 · by reviewer8

**the class:** A draft does not obey the class it is carrying, and a remedy already standing in the tree is retyped instead of called. The prose and the commands are written in one sitting, the prose is read as the deliverable, and nobody reads the commands back through it, so the draft becomes the one place in the tree where the rule is stated and broken at once -- and a worker who copies the criterion has copied the defect out of the file that forbids it.

**instead:** Before submitting, read every command back through the classes in doc/guidance/specifying.md under "The command decides the sentence above it", and hardest through the class the draft itself is about, because a draft whose subject is a class is the draft most likely to commit it. And before writing a command over a set, ask the tree whether it already walks that set: search for the file the set is declared in across src/ and util/ and call what answers; where nothing answers, write the walk once somewhere the next criterion can name it, rather than inside the criterion.

**minted as:** wk-5bbd696ee8

## lesson 2 · round 2 · by reviewer9

**the class:** A CRITERION THAT NAMES A PROPERTY AND NO SUBJECT, ON A TOKEN WHOSE DELIVERABLE GOES INTO MORE THAN ONE PLACE. The first criterion names file A and the second names file B, and every criterion after those two opens with the word it. The pronoun has two referents, so whoever writes the check picks one, and the one picked is the copy that is easier to read. The list is green while the second copy is a heading and one sentence. It reads as complete on the page, because the sentences cover every property and two lines on the list carry a path. No line says which copy the properties are asserted of, and a reader supplies the missing subject from the criterion above. The place with no check of its own is the place whose content nobody decided. This is the second draft in this round carrying it, so the class is named again rather than minted twice.

**instead:** Give every criterion a subject, by path, even where the criterion above it already named one. A pronoun is not a subject, because the check is written from the sentence and the sentence is what the worker reads. Where the deliverable goes into several places, the content criterion walks them and fails naming the one that is short. Where the sentence names a section as well as a file, say that the check reads that section, because a whole-file search is green with the text under any heading.

**minted as:** wk-7338fd6609

