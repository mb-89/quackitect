---
id: wk-02e17b9eb4
seq: "-26"
type: work
title: the set enumerates itself
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 7
minted_by: reviewer6
submitted_by: main
evidence:
  - outcome
---

## detail

A check that describes a set instead of asking it goes stale when a member is added. Write the class into doc/guidance/behaviour.md under Evidence, the measurement into doc/guidance/cases.md, and fix util/checks/engine-spawns.mjs. The hand-list instance belongs to wk-24be1c06ae. Related: wk-c15a6a80a3.

## done when

- The class is under the heading When a claim says every, count from the side that produces them, in behaviour.md, and the check fails naming that heading when it is elsewhere
  `rg -q func.TestTheEnumerationRuleIsUnderItsHeading src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleIsUnderItsHeading$ .`
- Both shapes and their shared tell are in that section
  `rg -q func.TestTheEnumerationRuleNamesBothShapes src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleNamesBothShapes$ .`
- The measurement is in cases.md under the same heading, and the check reads only the blocks naming this rule
  `rg -q func.TestTheEnumerationRuleCarriesItsMeasurement src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleCarriesItsMeasurement$ .`
- Both answers are in the behaviour.md section and differ
  `rg -q func.TestTheEnumerationRuleSaysWhatToDo src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleSaysWhatToDo$ .`
- The rule reaches every target derived from util/projections.json
  `rg -q func.TestTheEnumerationRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheEnumerationRuleReachesEveryProjection$ .`
- A runner plants an added spawn in a copy of src/extension and requires a red on the spawn-count line
  `node util/checks/engine-spawns-catches.mjs .`
- The check passes on a clean tree
  `node util/checks/engine-spawns.mjs .`
- Every test above was watched red, and the evidence names the test, the cut and the message

## evidence: outcome

The rule is in behaviour.md and its measurement in cases.md, with five tests in src/engine/enumerate_test.go reading each named block alone. engine-spawns.mjs holds the spawn count against a count taken from the source. engine-spawns-catches.mjs plants an added and a replaced spawn and asserts which line each red came from. bash util/checks/battery.sh answers all ok.
