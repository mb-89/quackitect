---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: se_status count unchecked
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-sibelius
claimed_at: "2026-09-05T16:06:39Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e7a03db80cab14de2e5ac6192e799a43d970060e
---

## detail

A finding on wk-6d74c93848, count wrong results.

Its fourth done-when line is "the two numbers appear together with what is already reported by se_status", and its step 1 evidence names mcp-tools as the command that decides it. mcp-tools does not decide it. util/checks/mcp-tools.mjs:115 calls se_status with empty arguments and only reads the answer for the malformed-argument shapes every call is read for. It asserts nothing about the text that comes back.

So the block src/mcp/roots.go status() now appends — a blank line, then the trimmed output of `se state` — is guarded by nothing. Delete those four lines and the whole battery stays green while the criterion this token was written for is silently false again. The state of play line itself, results N, M wrong, is guarded by TestTheStateOfPlayReadsOneScreen and by results_test.go; only its arrival in the lane's status is not.

Verified here: `./.bin/se state` prints "results 310, 30 wrong", and driving the built .bin/se-mcp with a tools/call for se_status returns an answer ending in the same line, so the change works. The live lane on this box is se-mcp.lane built before the change, which is wk-65c53d4b97, and is why se_status through the tool still answers without it.

## done when

- util/checks/mcp-tools.mjs asserts the se_status answer matches /results \d+, \d+ wrong/, and node util/checks/mcp-tools.mjs says ok for it
- removing the state-of-play block from src/mcp/roots.go status() makes that check fail

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one block of assertions in one check. No source moves. | util/checks/mcp-tools.mjs |
| [x] | every done-when line is decidable, and names the command where one decides it | both are the check's own run, once as it stands and once with the block taken out of status(). | `node util/checks/mcp-tools.mjs .` |
| [x] | the basics it stands on exist, or are minted first | the se_status case was already driven and its answer already held. Nothing was minted. | util/checks/mcp-tools.mjs |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read. Red without the block, green with it. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | on a scratch root at 5a83c225 with se and se-mcp built there. Take the state-of-play block out of status() and that assertion alone fails, saying no results line. | `node util/checks/mcp-tools.mjs <root>` |
| [x] | the same test was seen green after the change, and named | ok on that root with the block in, and ok through se test here. | `se test --on wk-e59cd71335` |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | the check gains a statusAt case found by tool name, and one assertion on the results line. | `git diff --stat` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | none. The commit carries this hunk alone, because the working copy holds another hand's deletion. | the commit |

