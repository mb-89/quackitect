---
id: wk-0302babe41
seq: 1000080
type: work
title: two pills say counts
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-66a28ca311
minted_by: main
evidence:
  - outcome
---

## detail

The work editor's bar says nothing about how much work is in flight. Owner's decision: two pills in the .bs-bar of src/extension/editor.ts, one for in work and one for in review, each carrying a count. Pressing a pill lists the tokens behind the count by title, and pressing a title opens that note. In work counts imp_in_work and spec_in_work, in review counts imp_in_review and spec_in_review. The engine counts and hands over the ids and titles, and the page draws what it was handed. Pressing a title sends the same open message a row's door sends. Related: wk-1b7c1a2da1.

## evidence: outcome

src/engine/view.go: Tally gained Of, a list of TallyOf carrying id and title, filled in the same pass that increments the count. util/views/work.base declares the two counts on both panes. src/extension/editor.ts: tallyPills draws one button per count in .bs-bar and tallyPops draws one popover per count listing titles, and a title sends `{type:'open', id}`. Tests src/engine/tally_test.go (two tests, red first) and util/checks/drive-editor.mjs (fourteen assertions) pass, and bash util/checks/battery.sh answers all ok over 14 checks.
