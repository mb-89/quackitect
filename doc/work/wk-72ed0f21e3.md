---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: rule seven half answered
# where the token stands. The process owns these values.
status: closed
author: worker-zev
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 77a4db189e882686fdfde6aad463d9b805f9f1e9
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - cd29986849012bf618fa30f9dbde6893529d5e51
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Found reviewing wk-c93aac62be, which dropped the chapters for work-token.md rules 6 and 7 to pay for its two new rules against the 1000-word discussion cap. Rule 6 survives that, because chapters 1 and 2 carry both its halves. Rule 7 does not. It still reads "Ask whether a criterion is about this change or about the project. Pin a one-time one". So it asks the reader to sort criteria into two kinds, then says what to do with only one of them. The deleted chapter held the other half. A standing criterion is a rule the project keeps, and it belongs in a check rather than on a token that closes. A search of work-token.md for standing, one-time or belongs in a check returns rule 7 and nothing else, so that instruction is now nowhere in the file. Give rule 7 its second clause in the rule itself, which costs no discussion words, or restore a chapter and pay for it elsewhere.

## done when

- work-token.md says what to do with a project criterion, not only a one-time one: se find --regex 'standing|belongs in a check' --path doc/guidance/work-token.md returns a line carrying the instruction
- work-token.md still lints clean at fifteen rules, none over 25 words, and the discussion under 1000: .bin/se.exe lint

## evidence: criterion 1 the instruction is back

se find --regex over doc/guidance/work-token.md for standing or belongs in a check answered 0 hits before the change, which is the defect. The deleted chapter had taken the instruction with it. It now answers 1 hit, line 29. Rule 7 reads "Ask whether a criterion is about this change or about the project. Pin a one-time one", with a second clause "a standing one belongs in a check". The second clause went into the rule rather than into a restored chapter. It cost no discussion words, and nothing had to be paid for elsewhere.

## evidence: criterion 2 the file still lints

.bin/se.exe lint answered the same four findings before and after, all of them methods/retro.md: 17 actionables against 15, two items over 25 words, and a discussion of 1134 words. None is for work-token.md, so it still holds fifteen rules, none over 25 words, and its discussion under 1000. Rule 7 went from 16 words to 23, inside the cap.

## evidence: what changed and why

Rule 7 asked the reader to sort criteria into two kinds, then said what to do with only one of them. wk-c93aac62be dropped the chapter carrying the other half to pay for two new rules. The lost instruction was that a standing criterion belongs in a check rather than on a token that closes. Putting it in the rule as a second clause restores the pairing without spending discussion words. Rule 6 was checked for the same damage and does not have it: chapters 1 and 2 carry both of its halves.

