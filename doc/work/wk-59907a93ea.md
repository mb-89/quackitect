---
id: wk-59907a93ea
seq: 1000076
type: work
title: the closer is missing
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
minted_by: cowork
submitted_by: main
evidence:
  - outcome
---

## detail

Four eyes become two in one extra command. probeA submits a draft, rewrites assignee to probeB with se work --set, then pulls as reviewer and is handed its own draft. judgeSpec and judge both refuse only when t.Assignee == actor, and field.go lets anybody write assignee. Fix, the smaller of two: record who submitted in a field the engine writes and nobody may type, and check against that instead of assignee. The closer field as Level 1 line 190 accepts it is the larger way. Prove it: the reproduction refused at the reassignment or the review, and a test that carries it.

## evidence: outcome

Token.SubmittedBy in src/engine/token.go is written by submit and submitSpec, refused by field.go, and persisted in store.go. sentBy(t) in src/engine/gate.go asks it in both reviewer queues in next() and in both verdict doors, judge and judgeSpec, with assignee as the fallback. src/engine/closer_test.go carries the reproduction as three tests, red before and green after, plus TestSomebodyElsesSubmissionIsStillReviewable. The closer field itself is not built, and an attempted bypass is still not recorded.
