---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: archive without a ref
# where the token stands. The process owns these values.
status: done
# who did the work step, so the verdict is never theirs
author: worker-glass
claimed_by: aeaf7bd9/worker-glass
claimed_at: "2026-09-05T14:16:07Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 8dbae2c00ab70d8d9b1a919308793de0fd945250
---

## detail

Archiving a closed tracked token writes refs/tags/archive/<id> and pushes it. A cloud box cannot push that namespace: measured on this branch, refs/tags answers HTTP 403 from the session's git proxy. archive.go:189 discards the push result with a bare assignment, and Archive then calls forget, which deletes the file from doc/work. So on a cloud box the tag is written locally, the push is refused silently, the file leaves the tree, and the next branch commit takes it off v4. The content then exists only in a local tag on a machine that is destroyed. Nothing is lost yet, because 122 tags stand locally and 122 on origin, so every close so far ran on a desk. The cloud is about to do the main work. THE TAG IS NOT THE ONLY COPY. The file was on the branch and its content stays reachable in history through the commit before the deletion. The tag is a stable name for something already there.

## proposed action

Stop making a tag. Record the blob in the archive list, which already travels on the branch, and retrieve with git cat-file. Nothing is pushed, so no namespace is refused and any box can archive. Where a push is still attempted anywhere, its failure is read rather than discarded, and the file stays on disk when it fails. The list is what a reader opens either way.

## done when

- a closed tracked token is archived on a box that cannot push a tag, and its content is retrievable afterwards from the branch alone
- no archive path creates or pushes a ref, decided by a search over src/engine for the archive namespace
- a push whose failure would lose content leaves the file on disk, watched red first against a stubbed git that refuses
- the 122 tags standing today are readable through the new list, or the change says why they are left alone
- sh util/checks/battery.sh reports no new failure against the run before the change

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | The Archived row: blob, on_branch, tag, read in that order. | type Archived |
| [x] | every done-when line is decidable, and names the command where one decides it | Four tests named in the do step, plus se find --regex archiveRefs and git ls-remote --tags origin. | the do step |
| [x] | the change is small enough to review whole, or it is split first | 263 insertions and 108 deletions over archive.go and its test, a new 271 line test file, five lines of .gitignore. | git diff --stat |
| [x] | the basics it stands on exist, or are minted first | All stood but one: the list was ignored by .gitignore, so it travelled nowhere. Made to travel here. | git check-ignore -v doc/work/archive.jsonl |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Applied: tests first, watched red. | below |
| [x] | the change follows the approach on the token, or the token says why it departed | It follows it and adds on_branch, because a blob hash-object writes reaches no clone. Watched red there. | func onBranch |
| [x] | se test --on this token answered ok, and what it ran is named | It could not: the package will not build, for untracked files this token does not touch. The suite ran at tip 3ae95149 plus these files in /tmp/glass-wt. Five failures, the same five the tip alone answers. The six tests named here pass. | /tmp/glass-after.out |
| [x] | the note says what changed and why, for a reader who was not here | archiveRefs, ArchiveList, Archived and readArchived each say why. | archive.go |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | treeOfOne and the swallowed push are gone. A reader one close short is wk-11820ecfde. | wk-11820ecfde |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | Read whole first, and I am not the author. | pass |
| [x] | every hunk of git diff began..ended was read, and any not read is named | began one hash, ended none. 5a83c225 read whole. Its test file and .gitignore rode in 13fa0928, read there. None skipped. | pass |
| [x] | every criterion's command was run again, and what it said is named | Clean 5a83c225 tree, as origin/v4 will not build. Vet clean, seven archive tests pass, archiveRefs only read, 69 tags here and 123 on origin. Suite 11 failed, none the archive. | pass |
| [x] | every hunk improves the product, or a finding names the one that does not | Pass. A travelling file replaces a ref no cloud box can push, and the keep precedes the delete. Three findings, no blocker. | pass |
| [x] | every finding is a trivial token naming this one | wk-06a6ca318c gc sweeps the blob. wk-54dd15a1dc no branch row names one. wk-e0d462f854 Where has one test caller. | pass |

