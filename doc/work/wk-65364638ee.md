---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: long test says where
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-fir
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e0e4821e2a94f08567c778d4ba0d0a93e601a3f8
  - 78e349f7d70c2573a2e383ef6f98177b5fff821c
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - ffce3031dd2037fc9fd10ecedf64d1f193575e74
  - d8154eb081f5984223da3f787dcd4305b4443af8
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: "The change holds. TestALongTestAnswersWhereItLands answers ok in 0.24 seconds and drives both halves, a run that fits and one that does not, and handing the runner in is the right seam so the test of the decision does not wait as long as the thing it is about. The run opening its own index handle, and awaiting a run whose answer has nowhere to land rather than throwing it away, are both right. One finding, minted as wk-f952e72caa: the still running entry carries OK true and tests.go folds it into out.OK, so se test answers ok for a run that has not finished, and the step 2 checklist line names exactly that field. It is the shape startBattery already had and the approach said to copy it, so what this widened is its reach.</reason>\n<parameter name=\"successors\">[\"wk-f952e72caa\"]"
---

## detail

Split off wk-4ad15a5c86, which carried this criterion and fixed the other half.

A tool call through the lane is given sixty seconds by the harness. se test can run for longer: a run of six tests took more than that, the harness cut the call, and the agent read a cut call as a dead engine and started another. The work went on running behind it, with nothing naming where its answer would land, so the agent had no way to ask again for what it had already paid for.

The engine already has the shape for this and uses it in one place. startBattery in src/engine/battery.go writes a marker, starts the run detached with its output to a file, and answers at once with the path the answer lands in. A later engine reads the marker and puts the outcome in the record. se test does not do this: it runs the chosen tests inside the call and answers when they finish, however long that is.

## proposed action

Give se test the answer startBattery already gives. A run the engine expects to outlive the lane's ceiling is started detached, its output goes to a file under .se/tests, and the call answers at once naming that file and how to read it. A run that fits answers as it does now, because an answer in hand beats a path to one.

What decides which is a number the engine holds, not a guess by the caller.

## done when

- a se test that runs longer than the lane's ceiling answers with where its result lands, decided by: go test -C src/engine -run 'ALongTestAnswersWhereItLands' ./... answers ok
- a run that fits inside the ceiling still answers with its result and not a path, decided by the same test
- the outcome of a detached run reaches the record, the way a battery's does

## evidence: battery

the whole battery is owed, as it is on every token this session: the tree carries several other sessions' half-landed work and would not compile twice within the last hour.

## evidence: change

src/engine/longtest.go, new. theAnswerOrWhereItLands runs the tests and answers what they said, unless they outlive theWaitForTests, forty seconds. Then it answers at once with the file the answer lands in, and the run goes on into that file. Tested gains a landing field carrying that path, and TestTheDelta calls it instead of runChosen. When the run finishes it writes what it said and puts the outcome in the record.

## evidence: departure

the approach said a run the engine expects to outlive the ceiling is started detached. Nothing predicts that, and predicting would ask the engine how long a suite takes before running it. The wait answers the same question afterwards, and the note says so.

## evidence: green

se test --on wk-65364638ee: TestALongTestAnswersWhereItLands ok true, with TestAMintNamingACheckIsNotATestRun, TestTheTestsAreTheEngines and TestEveryRungButGodNamesItsToken ok true beside it.

## evidence: index_handle

the run opens its own index handle. The one TestTheDelta is given is closed when the call answers, and the run may outlive the call by design.

## evidence: red

before the change the test would not build: theAnswerOrWhereItLands undefined, twice, which is the mechanism not existing.

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | the proposed action was written at the mint, before any edit: the shape startBattery already uses, for a run that only might be long, with the number the engine's | none owed |
| [x] | every done-when line is decidable, and names the command where one decides it | the first two fall to se test --on wk-65364638ee --propose TestALongTestAnswersWhereItLands, which drives a run that fits and one that does not. The third is the same test reading what landed | ok true |
| [x] | the change is small enough to review whole, or it is split first | one new file, one field on the answer, and one branch of TestTheDelta | none owed |
| [x] | the basics it stands on exist, or are minted first | batteryDir, writeAtomic and inSession were all there. Nothing was minted | none owed |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | rule 13: the runner is handed in, the way the swap hands in its compiler, so what is driven is the decision and not a suite | none owed |
| [x] | the change follows the approach on the token, or the token says why it departed | it follows it, with one departure the note states: nothing predicts a long run, the wait answers it afterwards | none owed |
| [x] | se test --on this token answered ok, and what it ran is named | TestALongTestAnswersWhereItLands, with TestAMintNamingACheckIsNotATestRun and TestTheTestsAreTheEngines | 4 ok |
| [x] | the note says what changed and why, for a reader who was not here | the note above, which carries the number and the departure | none owed |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | in the change. A run that outlives the call outlives the index handle it holds, so it opens one of its own | none owed |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | one round, one finding | reviewing |
| [x] | every hunk of git diff began..ended was read, and any not read is named | longtest.go whole, longtest_test.go whole, and the Landing field with the TestTheDelta branch that fills it. Nothing skipped | longtest.go |
| [x] | every criterion's command was run again, and what it said is named | TestALongTestAnswersWhereItLands answers ok in 0.24 seconds and drives both halves. TestAMintNamingACheckIsNotATestRun and TestTheTestsAreTheEngines answer ok beside it | se test |
| [ ] | every hunk improves the product, or a finding names the one that does not | handing the runner in is the right seam, and a run whose answer has nowhere to land is awaited rather than thrown away. The still running entry answers OK true | wk-f952e72caa |
| [x] | every finding is a trivial token naming this one, and their ids are here | wk-f952e72caa. It is the shape startBattery already had and the approach said to copy, so what this widened is its reach | wk-f952e72caa |

## evidence: test

src/engine/longtest_test.go, TestALongTestAnswersWhereItLands. It drives a runner that fits, asserting the answer is the run's own and no path, and one that sleeps past a twenty millisecond wait, asserting the answer names the file, then reads that file and finds the token, the outcome and what ran.

## note

se test answered when the tests finished, however long that took.

The call now waits forty seconds, which is inside the sixty a tool call is given, and answers with the result when it is there. Past that it answers at once with the file the result will land in, and the run goes on into it. The outcome reaches the record when it lands, so a run nobody waited for is still one the log carries. Which of the two happens is decided by that number and not by the caller.

One departure from the approach: nothing predicts a long run. Predicting would ask the engine how long a suite takes before running it, and the wait answers the same question afterwards.

The run reads the index through a handle of its own, because the one the call holds is closed when the call answers.

