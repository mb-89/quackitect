---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: pulled count keyed wrong
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: surface
---

## detail

Found reviewing wk-4bd6379304. ActorsThatPulled, src/engine/doing.go line 200, drops the whole arrival record when a.Session differs from currentSession(r), the engine's own session from the first log line. The record is written by ArrivedAs keyed on ArrivalSession(r), which is the harness session id whenever a SessionStart hook has logged one, and falls back to the engine session only when none has.\n\nSo on the harness wk-4bd6379304 describes, where SessionStart arrives and SubagentStart does not, the two keys differ, ActorsThatPulled answers nil, and the second walk in StaffingOf, src/engine/staffing.go, adds nobody. WorkersHere stays where it was and the guard's ask stays unsatisfiable. WhatIsHappening has the same hole, since it walks the same function.\n\nMEASURED. A probe test that wrote one session record with session harness-a into the log before the puller arrived, then called StaffingOf with the session row and one unregistered worker puller, read ArrivalSession=harness-a, currentSession=20260905-113107, ActorsThatPulled=[], WorkersHere=1. TestAPulledAgentIsAHand passes only because its log holds the engine start record alone, so both keys fall back to the same value.\n\nThe fix is one line: ActorsThatPulled compares against ArrivalSession(r), the key the record is written under, and the test gains a case that logs a harness session record first.

## proposed action

In ActorsThatPulled, filter on ArrivalSession(r) instead of currentSession(r), so the read uses the key the write used. Add a fourth case to TestAPulledAgentIsAHand that writes a session record with a harness id into the log before ArrivedAs, asserting WorkersHere is 2 and the guard does not refuse.

## done when

- ActorsThatPulled answers the pullers of the current harness session when the log carries a SessionStart record with a harness id: a table case in TestAPulledAgentIsAHand logs one before ArrivedAs and asserts WorkersHere is 2 and AStaffShortfall does not refuse. go test -C src/engine -run TestAPulledAgentIsAHand ./...
- the three existing cases still pass. go test -C src/engine -run 'TestAPulledAgentIsAHand|TestTheQueueIsStaffed|TestTheMainAgentCountsAsAWorker|TestOneNumberStaffsEveryRole' ./...

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

