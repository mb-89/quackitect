---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: blocked names wrong cause
# where the token stands. The process owns these values.
status: open
---

## detail

Found reviewing wk-8c3c71d77d.

Its note submits step 2 with "se test --on this token answered ok" unticked, and the blocked line says: "wk-89847112fe left a call to TestTheDelta carrying a context the function does not take. The whole package has not compiled since."

Measured on this box, in worktrees off the branch:

  d9ef4d39^ (e91d34a, wk-89847112fe already closed): go build . in src/engine is clean. Only the test build was red, on "undefined: aTreeWithTheProcesses", which is not the TestTheDelta signature the note names.
  9c5e571^ (86a528a, the branch tip carrying this change): go build . answers "./snapshot.go:143:13: undefined: theSnapshotHere". That is this change's own line. d9ef4d39 was written on a base where the walk was still called theSnapshotHere, and the branch had renamed it theSnapshotToDiff.

So the no-build at the tip was this change's, not wk-89847112fe's, and a separate commit (9c5e571) repaired it. util/checks/the-branch-head-builds names that error in five seconds and passes now.

The damage is the record: a reader chasing a broken branch is sent to a token that did not break it, and a step stayed unticked on a diagnosis nobody had run.

## proposed action

Correct the blocked line on doc/work/wk-8c3c71d77d.md to name the real break and its fix. Consider whether a submission that says the branch does not build should have to name the check that says so, since the-branch-head-builds already answers it.

## done when

- doc/work/wk-8c3c71d77d.md says the tip's no-build was its own call to theSnapshotHere, renamed on the branch, and that 9c5e571 fixed it
- the note names util/checks/the-branch-head-builds as what answers the question, and says it passes now: se test --propose util/checks/the-branch-head-builds
- the step 2 line about se test says what the tree answers today, which is ok for both named tests

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

