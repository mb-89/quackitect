---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: table names no method
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-relay-trial
claimed_at: "2026-09-05T19:48:11Z"
# true when this waits for a person rather than an agent
needs_human: true
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - ef2ca83b87257b01456c6ad9a0409fab84de7dfb
---

## detail

Found reviewing wk-218e541ec2. That spike's whole product is a table of counts, and its note says only that they were counted off .se/log and .se/retro/*/log, joined by entrypoint in the Claude transcripts. No reader can reproduce one number from that. Verified: a script over all 78 log files, counting the quoted event names, answers 0 for thirteen of the fourteen and 1 for StopFailure, because the record carries the engine's own kinds, call, gate, prompt, helper, session, and not the harness's event names. So the mapping from a kind to an event is the measurement, and it is nowhere on the token. The next harness version needs the same count and the method has to be invented again. Put the query or the script on the token.

MEASURED ON A CLOUD BOX, AND IT CANNOT BE DONE HERE. wk-218e541ec2 is closed and archived, so the note this ask names sits behind refs/tags/archive/wk-218e541ec2 and there is nothing on disk to write on.

The other half of the measurement is the entrypoint field in the harness transcripts under the person's home folder. A cloud box clones the tree and nothing else, so half the join is missing here. wk-899d2dbc6c is the fix, and it writes the surface onto the session line so the record answers it alone.

So this wants a desk, or a rewrite saying where the method lives now.

## done when

- the note names, for each column, the exact query or script that produced it, and which log kind stands for which event
- running what it names over the same logs answers the same numbers, or the note carries the new ones
- a reader who was not here can say where a single cell came from

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

