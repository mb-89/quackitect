---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: table names no method
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-dufay-two
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e97bd731ada0b703042f6e107a9b0fa985516c38
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 0a168446dd3b5aafeffc86eea2b4212477deafc9
# how it ended. Only an ended token carries one.
disposition: dropped
# why it was dropped
reason: the note it is about is not in this tree. wk-218e541ec2 closed on another box, and its archive row carries only the branch copy, which has no table.
---

## detail

Found reviewing wk-218e541ec2. That spike's whole product is a table of counts, and its note says only that they were counted off .se/log and .se/retro/*/log, joined by entrypoint in the Claude transcripts. No reader can reproduce one number from that. Verified: a script over all 78 log files, counting the quoted event names, answers 0 for thirteen of the fourteen and 1 for StopFailure, because the record carries the engine's own kinds, call, gate, prompt, helper, session, and not the harness's event names. So the mapping from a kind to an event is the measurement, and it is nowhere on the token. The damage is that the next harness version needs the same count and the method has to be invented again, and nobody can check the numbers the cage comment now rests on. Put the query or the script on the token, and if it earns its place in util/checks it can be run again.

## proposed action

Drop it. The note this is about closed on another box and its content never reached this tree.

The archive row for wk-218e541ec2 carries on_branch and no blob. So the only copy here is what the branch last committed. That copy was read with git cat-file. It holds the detail, the approach and three done-when lines, with every checklist row unticked. There is no note section in it, no table, no column and no cell.

The logs are gone with it. The count the detail describes was taken over 78 log files on that box, and .se/log here holds six.

wk-34e061d80a measured the same loss from the other side. It records that the cage comment no longer cites the spike.

## done when

- the note names, for each column, the exact query or script that produced it, and which log kind stands for which event
- running what it names over the same logs answers the same numbers, or the note carries the new ones
- a reader who was not here can say where a single cell came from

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does | nothing now. The note holding the columns is not in this tree | |
| [ ] | what breaks if it is never done, and not only that it stays undone | nothing further. wk-34e061d80a measured the same loss, and the cage no longer cites the numbers | |
| [x] | the ask is small enough to review whole, or it is split first | one note and one table, small enough, and not here to change | |
| [ ] | every done-when line is decidable, and names the command where one decides it | all three read that note, and reading it is what cannot be done | |
| [ ] | the basics it stands on exist, or are minted first | the basic is the note itself, and this tree carries no copy of it | |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read whole. Rule 11 decided it: the ask cannot be met, and a guess beside it is worse | |
| [ ] | one test was written first and seen red for the reason expected | none. Nothing changed, so no check could redden for the reason this token names | |
| [ ] | the same test was seen green after the change, and named | none, for the same reason | |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | there is no change | |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | none revealed | |

