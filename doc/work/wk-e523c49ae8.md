---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: four tests over eight
# where the token stands. The process owns these values.
status: open
---

## detail

tests-are-not-hotspots went from ok to FAIL between the battery at 14:58 and the one at 15:05. The check allows three tests over eight seconds and four are over it. se ask --sql "SELECT name, path, seconds FROM test WHERE seconds > 8 ORDER BY seconds DESC" answers TestNoExportedFunctionHereIsUncalled at 9.5s, TestTheQueueIsStaffed at 9.4s, TestAnAgentSeenAfterARestartIsHereAgain at 8.9s and TestTheRegisterFollowsAgentsInAndOut at 8.2s. All four sit within one and a half seconds of the line. That reads as the suite drifting on a loaded box rather than one test doing something heavy. Three of the four were untouched by wk-0909cb7db7. The fourth was, by two added StaffingOf calls, which cost nothing.

## proposed action

Read the four against testing rule 13 and decide whether any drives a real tool it could be fed instead. Where the timing is the box and not the test, say so here and leave the check alone.

## done when

- se ask --sql "SELECT count(*) FROM test WHERE seconds > 8" answers 3 or fewer, or this token names which of the four has to be real and why
- the battery reports tests-are-not-hotspots ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

