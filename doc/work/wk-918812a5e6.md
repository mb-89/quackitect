---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: orphan test breaks build
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-birch
claimed_at: "2026-09-05T19:26:17Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 82055326ac1a55529b09aab51d01d267b4c128fe
---

## detail

src/engine/timeline_test.go and src/engine/stateofplay.go are untracked, and the test half names things the source half does not define. Collected has no Timeline field, and TimelineEntry is not declared anywhere. So the src/engine test package does not compile, and every go test over it fails at the build, whatever it was testing. No open token names timeline, so the producing half was never written or was written somewhere this tree cannot see. This blocks every worker who runs se test on the engine, not one token's work. Either the missing half lands, or the test file is parked with a leading underscore until it does.

## done when

- the src/engine test package builds: go vet -C src/engine ./...
- a named engine test runs rather than failing at the build: go test -C src/engine -run TestARationaleIsNamedNotRepeated

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

