---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: orphan test breaks build
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-nancarrow
claimed_at: "2026-09-05T14:28:36Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 82055326ac1a55529b09aab51d01d267b4c128fe
---

## detail

src/engine/timeline_test.go and src/engine/stateofplay.go are untracked, and the test half names things the source half does not define. Collected has no Timeline field, and TimelineEntry is not declared anywhere. So the src/engine test package does not compile, and every go test over it fails at the build, whatever it was testing. No open token names timeline, so the producing half was never written or was written somewhere this tree cannot see. This blocks every worker who runs se test on the engine, not one token's work. Either the missing half lands, or the test file is parked with a leading underscore until it does.

## done when

- the src/engine test package builds: go vet -C src/engine ./...
- a named engine test runs rather than failing at the build: go test -C src/engine -run TestARationaleIsNamedNotRepeated

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Eleven untracked files renamed with a leading underscore. No tracked file moved, and no line of anyone's code changed. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Both name their command, `go vet -C src/engine ./...` and `go test -C src/engine -run TestARationaleIsNamedNotRepeated`, and both were run from the root here. |  |
| [x] | the basics it stands on exist, or are minted first | The Go toolchain passes over a file whose name begins with an underscore, which is what parking rests on. The files the detail named, timeline_test.go and stateofplay.go, are tracked and build. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 12 put the red first, and the two commands are the checks. |  |
| [x] | one test was written first and seen red for the reason expected | `go vet -C src/engine ./...` said `enginefresh.go:114:18: theToolchain.buildEngine undefined`. The test command failed at the build on claimsfile_test.go. Expected, as each orphan names a half the tree does not hold. |  |
| [x] | the same test was seen green after the change, and named | Both answer ok. The vet exits 0, and TestARationaleIsNamedNotRepeated runs and passes. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | No tracked file moved, because all eleven are untracked. wk-57fbc66ca9 lists every one. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Token wk-57fbc66ca9. Parking is a stopgap, and each file is revived or deleted by whoever owns its half. |  |

