---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: claim reread never lands
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-satie-two
claimed_at: "2026-09-05T21:05:18Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 00d5b337ba332a0d771c2ca4587ce6e4c8564a18
---

## detail

Publish in src/engine/claim.go commits a claim on top of the local refs/se/claims and pushes. When the push fails it reads the other box's ref with `git fetch --quiet origin refs/se/claims:refs/se/claims` and writes again on top. That fetch is refused by construction: the local ref now holds this box's own commit, whose parent is what the remote held, so whatever the remote holds now is never a descendant of it, and git rejects a non-fast-forward update of a local ref unless the refspec carries a plus. With --quiet the rejection is not even printed, so the answer reads "The push did not run: git fetch: " and names no reason. Seen on this tree after a claim: the local ref had the remote's commit as an ancestor, and a dry fetch into it answered "[rejected] (non-fast-forward)".

So the rebase path Publish describes has never run. Two boxes racing on the ref leave the loser's claim local for good, and the answer does not say why.

## proposed action

Fetch the far ref into a name of its own, such as refs/se/claims-far or FETCH_HEAD, build the next commit on that parent, and drop --quiet so a fetch that fails says why.

## done when

- a Publish whose first push is refused because the far ref moved reads the far ref and pushes a commit whose parent is the far commit: proved by a Go test in src/engine over two local repositories
- a fetch that fails leaves its reason in Published.Says

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A real git says whether it accepted the calls. A fed git answers whatever it is told, and the calls were right while git refused them. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | Nothing proves the parent. The fetch shape has moved twice already, and a fed test reads the call rather than the refusal. |  |
| [x] | the ask is small enough to review whole, or it is split first | One test file, two tests, and no change to the engine. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | go test -run TestAClaimIsWrittenOnTheFarCommit decides line 1, and TestAFetchThatFailsLeavesItsReason decides line 2. |  |
| [x] | the basics it stands on exist, or are minted first | claimsRef, claimsFile and Publish are all here, and gitAt in archivetravels_test.go is reused rather than written again. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 12 put the red first, rule 13 drove it through both halves. |  |
| [x] | one test was written first and seen red for the reason expected | Put the fetch back over the local ref, quietly, and the first test fails: the far claims were never read, and the answer stops at "git fetch: ". Drop git's reason and the second fails on the empty reason. |  |
| [x] | the same test was seen green after the change, and named | Both pass as the tree stands. TestAClaimIsWrittenOnTheFarCommit in 0.15s, TestAFetchThatFailsLeavesItsReason in 0.03s. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/engine/claimfar_test.go only. The engine already reads the far ref, so claim.go did not move. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Token wk-ba5b3feb23. The branch head builds no test, so both runs were in a worktree. |  |

