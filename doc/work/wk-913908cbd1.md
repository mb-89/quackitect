---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: claim reread never lands
# where the token stands. The process owns these values.
status: open
---

## detail

Publish in src/engine/claim.go commits a claim on top of the local refs/se/claims and pushes. When the push fails it reads the other box's ref with `git fetch --quiet origin refs/se/claims:refs/se/claims` and writes again on top. That fetch is refused by construction: the local ref now holds this box's own commit, whose parent is what the remote held, so whatever the remote holds now is never a descendant of it, and git rejects a non-fast-forward update of a local ref unless the refspec carries a plus. With --quiet the rejection is not even printed, so the answer reads "The push did not run: git fetch: " and names no reason. Seen on this tree after a claim: the local ref had the remote's commit as an ancestor, and a dry fetch into it answered "[rejected] (non-fast-forward)".

So the rebase path Publish describes has never run. Two boxes racing on the ref leave the loser's claim local for good, and the answer does not say why.

## proposed action

Fetch the far ref into a name of its own, such as refs/se/claims-far or FETCH_HEAD, build the next commit on that parent, and drop --quiet so a fetch that fails says why.

## done when

- a Publish whose first push is refused because the far ref moved reads the far ref and pushes a commit whose parent is the far commit: proved by a Go test in src/engine over two local repositories
- a fetch that fails leaves its reason in Published.Says

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One test file, two checks. No source changed, because the refspec was already put right. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Two lines. One runs the far-commit check, the other the reason check, both in src/engine. |  |
| [x] | the basics it stands on exist, or are minted first | aBareOrigin, aClaimNote, mustGit and runGit all exist in claimsrideabranch_test.go. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Both checks were written first, and the one that decides the ask was watched red. |  |
| [x] | one test was written first and seen red for the reason expected | With the old refspec put back it answered that the push did not run, and named the fetch that could not find the remote ref. |  |
| [x] | the same test was seen green after the change, and named | TestTheRereadLandsOnTheFarCommit and TestAFetchThatFailsLeavesItsReason, green, with four claims tests beside them. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began is b055a7f3 and is no object here. The change is one test file, on origin as 7a9e1c49. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The reason check does not tell the two refspecs apart. It holds the second clause and is a guard, which is said here rather than hidden. |  |