---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: executables sit beside source
# where the token stands. The process owns these values.
status: closed
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 1a99aae06cb38040488a097349818a0f47648ac8
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 0f5a4bddfe1acfd7af7ba266beb5c7e6eea3b547
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Five built programs sit in the source folders: src/engine/engine, src/engine/engine.exe, src/viewer/viewer, src/viewer/viewer.exe, src/mcp/mcp.exe. Built programs belong under .bin. The gitignore names the first four and not src/mcp/mcp.exe. Provide one way to build that the installer and a person both call, so nobody types a bare go build. Add a check that fails when an executable exists anywhere outside .bin and names the ones it found. For a compile check, `go build -o /dev/null` and `go vet` leave nothing behind. Do not change how the tree builds while somebody is working in it.

## done when

- git check-ignore src/mcp/mcp.exe exits 0.
- A battery check fails naming executables outside .bin, passes clean.
- One build command, called by the installer, and no binaries under src/.

## evidence: step 1. ask

<!-- restored by hand. The engine deleted these tables on submit, which is wk-7887984486. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Whole. Three criteria, each read off one command. | — |
| [x] | every done-when line is decidable, and names the command where one decides it | git check-ignore src/mcp/mcp.exe for the first, and se test proposing the binaries check for the second and third. | — |
| [x] | the basics it stands on exist, or are minted first | Both exist and neither had to be built. util/checks/binaries-live-in-bin.mjs is the check. util/setup/manifest.json carries the builds list, naming se, logview and se-mcp with their sources. | util/setup/manifest.json:83 |

## evidence: step 2. do

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | work-token |
| [x] | one test was written first and seen red for the reason expected | Green already, so the red was made. I planted src/engine/stray-probe.exe and the check failed: "FAIL src/engine/stray-probe.exe stays out of the source folders", 339 files looked at, 1 failed. | se_test --propose util/checks/binaries-live-in-bin |
| [x] | the same test was seen green after the change, and named | Green twice. Before the probe, ok true in 0.14s, "no executable sits outside .bin". After deletion, exit 0. | se test --on wk-6a41371774 --propose *binaries-live-in-bin* |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | no source changed: the work was already landed |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | util/setup is the installer's build command. A person's door, and a way to replace a running engine, is wk-212909368a, open. This token's last line refused folding it in. | wk-212909368a |

