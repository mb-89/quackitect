---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: two tests one name
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - eeac13d7a47293b5c2ac4380e9de39a55123dd73
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - cb4ada4d9558d9bffa56b3b134e5547e0d061002
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: go vet over src/engine went from redeclared to exit 0, and both tests pass under their two names.
---

## detail

The engine package will not compile at the branch head, so se test answers no build for every token on every box.

Two files declare TestAFetchThatFailsLeavesItsReason: src/engine/claimfar_test.go line 102, landed by 131545f1 for wk-913908cbd1, and src/engine/claimrereadlands_test.go line 88, landed by 7a9e1c49. go vet at head ce3f0bd3 answers: TestAFetchThatFailsLeavesItsReason redeclared in this block.

Two hands wrote the same test for the same behaviour, in two files, minutes apart. Neither is wrong and neither is a copy. claimfar publishes to a remote path that is not there and reads git's own words back. claimrereadlands drives the gitRuns seam and refuses every push and fetch.

So both proofs are worth keeping and only the name collides.

## proposed action

Rename the claimfar one to say what it is about, a remote that is not there, and leave the seam-driven one with the plain name. Nothing else moves.

## done when

- go vet over src/engine at the branch head exits 0, run as CGO_ENABLED=1 GOFLAGS=-tags=sqlite_fts5 go vet .
- both test bodies survive, decided by rg counting two functions whose name ends LeavesItsReason in src/engine
- go test -run LeavesItsReason over src/engine answers ok for both

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The engine package compiles at head again, so se test answers for every token on every box. | vet 0 |
| [x] | what breaks if it is never done, and not only that it stays undone | Every agent tests by hand in a worktree, and what ran is then nobody's record. Three reported doing exactly that. | 3 agents |
| [x] | the ask is small enough to review whole, or it is split first | One rename and a comment, in one file. | 1 file |
| [x] | every done-when line is decidable, and names the command where one decides it | go vet, rg over the two names, and go test -run LeavesItsReason. | 3 of 3 |
| [x] | the basics it stands on exist, or are minted first | Both tests and both files were already on the branch. | 2 files |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | The token asked for a rename and nothing else. Neither test body moved. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | go vet at head 2ede26b3 answered TestAFetchThatFailsLeavesItsReason redeclared in this block. | vet 1 |
| [x] | the same test was seen green after the change, and named | go vet exits 0, and go test -run LeavesItsReason passes both. | 2 of 2 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Six lines added and two removed, in claimfar_test.go. | 55050b0d |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Two hands wrote one test twice because the shared tree reads stale, which is a token already. | wk-c69f2dabc7 |

