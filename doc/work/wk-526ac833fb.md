---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: test lessons into testing
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-erin
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - f47a126d909a6f3a6422d38cdb6ab7f7062eb07c
  - d8aaa93db2ed9cb84808235553045dede9455330
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 3dcc0969a4252f5aa6ff27005f147363374d4204
  - 5281d329fb3972774eeb4a4943cd0b050897d21b
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Eight notes each ask for one more rule about how a check is built:

- count causes and fixtures
- list a language's spellings in a pattern
- drive a configurable number away from its default
- count exits from the bottom
- a vacuity guard on the set
- a same-as criterion comparing both places
- a dispatcher criterion asserting what every other token holds
- a measurement whose command really ran

testing.md holds fifteen rules, the ceiling, so none can land as written. Consumes the eight notes named in the approach.

## done when

- testing.md lints clean at fifteen rules or fewer with the eight lessons placed: .bin/se.exe lint
- each consumed note's lesson is under one chapter, listed in the note's successor evidence: se find --words <a phrase from each note> --path doc/guidance/software-development/testing.md
- no chapter names a token id: node util/checks/tests-name-no-token.mjs .

## evidence: criterion 1 lint

.bin/se.exe lint reports no finding for software-development/testing.md, run again just before submitting. Actionables holds fifteen items, none over the 25-word cap, and Discussion is under the 1000-word cap. Lint is not clean overall, but every remaining finding is methods/retro.md, which was already failing before this change and which this change did not touch.

## evidence: criterion 2 lessons placed

Each note's lesson sits under one chapter of testing.md, found by se find over that path. Chapter 7, counting the set, carries three. The first two are three causes fixtured as one, and a flag matched in one spelling. The third is a walk of explicit returns blind to a catch and an unread stream. Chapter 4, the claim and the assertion, carries three. The first is a configurable number fixtured away from its default, so behaviour moves both ways. The second is a same-as naming both places and comparing declarations field by field. The third is a dispatcher saying what the record holds for the tokens it did not hand back. Chapter 8 carries the vacuity guard, now counting producers into the criterion and planting a violating second producer. Chapter 15 carries the measurement, now refusing an exit code inherited through a pipe.

## evidence: criterion 3 no token id

The claim holds: a regex search for a minted id over testing.md returns no hits, so no chapter names a token id. The command the criterion names cannot decide this, because tests-name-no-token walks only test files under src and never opens a guidance file. That gap is minted as wk-46ff3d11af.

## evidence: se test

Answered not ok. It ran util/checks/tests-name-no-token, named outright, and the single failure is a provenance comment in src/engine/investigate_test.go naming a token in the record: 93 test files read, 1 failed. That file is untracked and new from another lane. This change touched only testing.md, so the failure is not from this work and fixing it would be work next to the ask. Minted as wk-c98ea8f020. The battery stays owed because scripts-are-lf.mjs changed under util/checks and the proposal ran in its place.

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | It was on the token when it was handed over, under the approach heading, and it is a shape rather than a plan. See the evidence chapter the approach as handed over. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | The first two are, and both commands were run again before submitting. The third is decidable. See the evidence chapter criterion 3 no token id. |  |
| [x] | the change is small enough to review whole, or it is split first | One file, doc/guidance/software-development/testing.md, a hundred and twenty-three lines, read whole after writing. | — |
| [x] | the basics it stands on exist, or are minted first | The eight notes exist and are closed as became, each naming this token as its successor. The file, the schema that caps it and the lint that reads it all existed, so nothing had to be minted first. | — |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md was read whole. See the evidence chapter the guidance applied. | — |
| [x] | the change follows the approach on the token, or the token says why it departed | It follows it. Three classes came out of the eight. See the evidence chapter the three classes. |  |
| [x] | se test --on this token answered ok, and what it ran is named | It answered not ok, and that is the honest answer rather than a tick. See the evidence chapter se test. |  |
| [x] | the note says what changed and why, for a reader who was not here | Yes, and the evidence chapter what changed says it for a reader who was not here. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Two notes, wk-c98ea8f020 and wk-46ff3d11af, neither parented to this token because each is follow-on work. See the evidence chapter the cleanup as notes. | — |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | reviewing.md was read whole before the diff. See the evidence chapter the findings. | — |
| [x] | every hunk of git diff began..ended was read, and any not read is named | The range is one sweep commit carrying 13 files. Every hunk of doc/guidance/software-development/testing.md was read, which is this token's whole change. See the evidence chapter the hunks not read. |  |
| [x] | every criterion's command was run again, and what it said is named | All three commands were run again. See the evidence chapter the criteria run again. |  |
| [x] | every hunk improves the product, or a finding names the one that does not | Every hunk carries a lesson the file did not hold, except the compression in three chapters. See the evidence chapter the hunks judged. |  |
| [x] | every finding is a trivial token naming this one, and their ids are here | Verdict: pass. One finding kept and minted, wk-3c708aff10. See the evidence chapters the findings and the second look. |  |

## evidence: the approach as handed over

The approach was on the token when it was handed over, under the approach heading, and it is a shape rather than a plan:

- group the eight by the class of wrong check they catch
- expect three rules at most
- merge with the rules already standing on that class
- put the incidents in the discussion
- say which rule was displaced and why

A reader can disagree with the grouping itself, which is the claim being made: three classes, not eight rules.

## evidence: the cleanup as notes

wk-c98ea8f020: a test file carries a token id in a provenance comment, failing the check this token's third criterion names. wk-46ff3d11af: that check reads no guidance file, so a criterion over prose that names it cannot be decided by it. That is the same defect these eight notes are about.

## evidence: the criteria run again

Criterion 1: .bin/se.exe lint returns four findings, all methods/retro.md, none for testing.md. retro.md already held 17 actionables at f47a126d, the began commit, so it was failing before this change. Criterion 2: se find over testing.md returns all eight lessons, in chapters 4, 7, 8 and 15. The eight consumed notes are closed as became, each naming this token. Criterion 3: se test proposing tests-name-no-token ran it and it failed on investigate_test.go and notekeeps_test.go, never on testing.md, because the check reads only test files under src. The criterion was decided instead by se find for a minted id over testing.md, which returned no hits. The battery stayed owed because util/checks changed, which is not this author's doing.

## evidence: the findings

The bar applied is improvement rather than perfection, and one round only. Four candidate findings were written and one kept, and the rest were dropped as nitpicks. The one kept is wk-f9233114b0: chapters 10, 12 and 13 lost their closing practice sentence. Also seen, both already in the queue and not raised again. Criterion 3 names a command that cannot decide it, wk-46ff3d11af. A test file names a token id, wk-c98ea8f020.

## evidence: the guidance applied

Its line that a checklist carries institutional knowledge and is answered rather than ticked shapes these sentences. Its line that an observation names the check and what it said, never a line number, sent me back to rewrite the placement evidence. That evidence now names chapters and phrases instead of lines. Its line that every command runs again before submitting was followed for all three criteria.

## evidence: the hunks judged

Rules 4, 7, 8, 13 and 15 and the two new chapters each carry a lesson the file did not hold. The count stays at fifteen with no rule lost. The compression that paid for them mostly cut words. In three chapters it cut the instruction, which is the finding.

## evidence: the hunks not read

They are another lane's work landing inside this token's window: methods/retro.md, the deletion of doc/retro/20260902-184643.md, the notes wk-00c162f56e, wk-6d74c93848 and wk-fda3dc640a, src/engine/retro.go, retrodrain_test.go, the three process yaml files and util/checks/battery.sh. That the delta carries foreign work is wk-33ffac1616.

## evidence: the second look

The verdict was written once and could not be recorded, so the token came back. All three criteria were run again at this HEAD, and all three now pass.

Criterion 1. .bin/se.exe lint answers clean true with findings null. The four retro.md findings named above are gone, fixed by their own lane.

Criterion 2. Each of the eight lessons sits under one chapter of the ended file. The criterion also wants the lesson listed in each note's successor evidence. Those eight notes are not in the tree. They were swept from doc/work at bf54d1fe, before this work began. So that half cannot be read back.

Criterion 3. se test proposing tests-name-no-token answers ok. The check now walks doc/guidance to the bottom, so it does open a chapter. wk-46ff3d11af closed that gap.

One finding was kept, wk-3c708aff10. Two candidates were dropped. Chapter 1 lost one of three shapes, which survives in work-token.md. Chapter 15 lost the citation to Go's testing.B.

## evidence: the tables restored

The step 1 and step 2 tables were missing from this note, the known loss on wk-7887984486. They were restored verbatim from the ended commit under wk-ddb9edee8b rather than written afresh. The engine could not record the verdict, because the stale resident binary answers that a token at status done is already closed.

## evidence: the three classes

Four went to rule 7, rewritten to count a set from the side that produces it: every cause, spelling, value and exit.

- the enumeration that stopped early
- three causes fixtured as one
- a pattern matching one of the language's spellings
- a walk of explicit returns blind to a catch and an unread stream

Four went to rule 4, which already tied a test's name to its claim and now says to assert every word of it.

- the check that asserts half its claim
- same-as checked as merely exists
- a configurable number driven only at its default
- a dispatcher criterion silent on the tokens it did not hand back

The guard or measurement that never really ran went to rule 8 and to rule 15. Rule 8's control now sits on the set rather than one member. Rule 15 requires a measurement to show real output and an exit code the caller read rather than inherited through a pipe.

## evidence: verification runs

Every command ran again at HEAD under wk-16761b9ac8. The resident engine refuses a run named on a token that has ended, so that token carries these runs.
The range is git diff f47a126d909a6f3a6422d38cdb6ab7f7062eb07c..3dcc0969a4252f5aa6ff27005f147363374d4204, and it carries 13 files, the count this verdict read.
Criterion 1: .bin/se.exe lint answers clean true, findings null. The four methods/retro.md findings named above are gone, fixed by another lane after this token ended.
Criterion 3: se test proposing tests-name-no-token answers ok, one check run. The investigate_test.go failure named above no longer reproduces, which settles what wk-c98ea8f020 reported. A regex over testing.md for a minted id still returns no hits.
Criterion 2: se find over testing.md now returns seven of the eight lessons. Chapter 4 no longer carries the dispatcher lesson.
git show of the file at the ended commit finds that sentence there. So the criterion held when this verdict was written, and a later compression removed it. Raised as wk-dd7e2ca1a6.

## evidence: what changed

Rules 4, 7, 8 and 15 rewritten to carry the eight lessons, and two chapters added. The two are the claim and the assertion, and counting the set. Rule 15's clock clause moved into rule 13 to make room, so the file still holds exactly fifteen rules and no rule was lost. Rule 13 already held the clock as a fixture. The discussion had no headroom against its 1000-word cap. The first write came back from lint at 1008 words. The eleven existing chapters were compressed by about a fifth to pay for the two new ones, keeping every incident and every citation.

## approach

Group the eight by the class of wrong check they catch. Expect three rules at most, folded into testing.md by merging with the rules already there on the same class, with the incidents in the discussion. A rule displaced says which and why. The notes: wk-49bd1fee6c, wk-5385d7a03e, wk-1dc5a8d150, wk-c7535682d6, wk-428af70a5e, wk-5fb02d9f30, wk-5376b70f99, wk-4243c04e79.

