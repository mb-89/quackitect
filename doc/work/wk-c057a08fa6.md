---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: stop criterion names test
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-chagall
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 92f46bd2b7e0e1c6d4956614118a221ceb2e643b
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 9be1da426f103aa4cdf4a1d7092f181de41b64af
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: "The no-alias case moves out of the tail of TestAStopClaimIsFoundUnderTheHarnessName into TestAClaimUnderAnUnlinkedNameIsFoundUnderThatNameAlone, which the criterion's own pattern reaches. Before the move that name found no test and answered ok having run nothing. After it the test passes, and it fails when StandingClaim is made to answer every claim regardless of name. The test above still passes. Landed on the branch as commit c61c3c69."
# what it became. They have to exist.
successors:
  - "[[wk-00e4785e51]]"
---

## detail

A FINDING WHILE REVIEWING wk-c1d58b91d6.

The third criterion on wk-c1d58b91d6 says a claim under a name no alias links is found under that name alone, decided by go test -C src/engine -run 'TestStopClaim|TestAClaim' ./... answering ok. No test in src/engine is named TestStopClaim, and the eight named TestAClaim are work-claim tests in claim_test.go, claimgate_test.go, challenge_test.go, gitfed_test.go and relentneedsaclaim_test.go, none of which touches a stop claim. se test --on wk-c1d58b91d6 with those two patterns proposed answered unreached for both. The case itself is asserted, but as the tail of TestAStopClaimIsFoundUnderTheHarnessName in src/engine/stopclaimnames_test.go, where StandingClaim(r, "worker-bach") must answer nothing. So the criterion's command runs eight unrelated tests and says ok, and a reader believes the no-alias case is guarded by the command named for it.

## proposed action

Move the worker-bach assertion out of TestAStopClaimIsFoundUnderTheHarnessName into a test of its own in src/engine/stopclaimnames_test.go whose name the criterion's pattern reaches, such as TestAClaimUnderAnUnlinkedNameIsFoundUnderThatNameAlone, so go test -run 'TestStopClaim|TestAClaim' decides what the criterion says it decides.

## done when

- go test -C src/engine -run 'TestAClaimUnderAnUnlinkedName' ./... answers ok and names one test that ran, and it fails when StandingClaim is made to answer every claim regardless of name
- TestAStopClaimIsFoundUnderTheHarnessName still answers ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The command a criterion names runs the test that decides it. | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | A reader keeps believing a case is guarded by a command that runs none of it. | no tests to run |
| [x] | the ask is small enough to review whole, or it is split first | — | one test file |
| [x] | every done-when line is decidable, and names the command where one decides it | Both lines are go test in src/engine, each naming its test. | go test -run |
| [x] | the basics it stands on exist, or are minted first | — | StandingClaim and guidanceTree |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | read |
| [x] | one test was written first and seen red for the reason expected | Run before the move, the criterion's own name reached no test. It answered ok having run nothing. | no tests to run, exit zero |
| [x] | the same test was seen green after the change, and named | It passes after the move. With StandingClaim made to answer every claim regardless of name it fails, so it guards its case. | TestAClaimUnderAnUnlinkedNameIsFoundUnderThatNameAlone |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | c61c3c69 on the branch |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Three git-backed tests fail only under the whole suite, and pass alone. | wk-00e4785e51 |

