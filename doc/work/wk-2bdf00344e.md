---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: every retro test fails
# where the token stands. The process owns these values.
status: open
claimed_by: ae61c33c/worker-sedge
claimed_at: "2026-09-06T10:35:25Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 113000406eecea2e082999a7b62c980ac44a96fb
---

## detail

All five tests of the retro verb fail at the branch tip. So nobody working on the retro can watch a check of their own go green, and the lane guards nothing.

MEASURED IN SEPTEMBER 2026, on a detached worktree at the tip, with the engine test package building cleanly. The command was a go test run in src/engine narrowed to the retro. It answered five failures and no passes.

They are TestARetroCollectsAndDrains, TestARetroLeavesAnotherActorsFolder, TestARetroLeavesTheSourcesEmpty, TestARetroTakesTheRunningSessionToo and TestASecondRetroTakesNothingTwice.

EVERY ONE FAILS THE SAME WAY. The spawned se retro exits one, saying there is no method root here. It looked up from where it is for a folder carrying src/processes and found none, and it asks to be named one with the method flag. So the fixture folder carries no method and the call passes no flag.

One of them adds a second line, saying the session log could not be written because the file was already closed. That may be the same cause or a second one, and it is worth reading beside the first.

This is not the package failing to build, which is wk-4140e954b7 and is about a clone behind the tip. Here the package builds and the five tests run and fail.

## proposed action

My best attempt: the fixture builds a folder with no src/processes in it and the spawned call names no method, so one of the two has to give. Giving the fixture a method root is the smaller of the two and matches what every other spawning test here does. Read the log line about a closed file before deciding, in case it is a second fault wearing the same failure.

## done when

- the five named retro tests pass, decided by a go test run in src/engine narrowed to the retro
- the fixture says where the method root comes from, so a reader knows whether the folder gains one or the call gains the flag
- no other test of the package goes from green to red, decided by a whole go test run in src/engine against the same tip

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

