---
id: wk-5544578d91
seq: "54"
type: work
title: move reports its refusals
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "Obsolete, the remedy landed on the source-to-src rename as declinedPairs at src/engine/move.go:145."
aborted_from: backlogged
minted_by: reviewer4
---

## detail

move.go builds the rewrite pairs and the residual sweep from one list, so the bare folder name the rewrite refuses is absent from the report. The verb answers zero unrewritten while files still name a folder that no longer exists. Keep the rule that a bare folder name is never edited, and at move.go:222 add the refused pair to the sweep only. If that is too loud, restrict it to files in sourceFormats and to occurrences with a quote or separator either side. A top-level move over a .mjs and a .py naming the folder as a quoted segment must answer unrewritten_total of at least two. Reproduced in .se/scratchpad/reviewer4/a-segment-not-a-slash.sh. Related: wk-58bceb45f3.
