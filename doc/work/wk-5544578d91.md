---
id: wk-5544578d91
seq: "54"
type: work
title: move reports its refusals
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer4
---

## detail

A deliberate exclusion was applied to the doing and forgotten in the reporting. move.go builds the rewrite pairs and the residual sweep from one list of spellings, so the bare name of a top-level folder, which the rewrite refuses on purpose, is also absent from the report. The verb then answers zero unrewritten while leaving files that name a folder which no longer exists. Found on wk-58bceb45f3, where seven files were patched by hand and the verb had said there was nothing to patch.

WIDEN THE SWEEP, NOT THE REWRITE. Keep the rule that a top-level folder's bare name is an English word and is never edited. At move.go:222 add the bare pair the rewrite refused to the sweep only, so unrewritten names every line that still says the old name. If that is too loud, restrict it to files in sourceFormats and to occurrences with a quote or a separator either side, which is what a path segment looks like and a sentence is not, and say in the comment which was chosen.

THE CHECK, RED TODAY: a top-level directory move over a tree with a .mjs and a .py naming the folder as a quoted path segment must answer unrewritten_total of at least two and name both files. It answers 0 today, reproduced in .se/scratchpad/reviewer4/a-segment-not-a-slash.sh.

