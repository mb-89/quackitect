---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: proposed action optional
# where the token stands. The process owns these values.
status: closed
author: worker-gale
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 05263b57ffc5ab6f5dd050af9f84055e6131b2c2
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - e34e10c079a42d044c8a1d75133cf88c1399cae0
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: Proposed action moved from required to optional in src/processes/note.process.yaml. TestANoteNeedsNoProposedAction was written first, seen red for the reason expected, and green after. The first done-when named se lint, which cannot decide it, so the decider was corrected and the gap minted as wk-9cff60b794.
# what it became. They have to exist.
---

## detail

The note process requires a proposed action section. The panel mints a note from the typed line, title and detail only. So every note typed into the panel is born red in the problems panel. 114 of the 124 problems came from that. The owner rules it optional. The smallest note is one sentence about something wrong, and whoever decides writes the action at the decide step. Consumes wk-5a87576328.

## done when

- proposed action is optional in note.process.yaml, and a note minted from a title and a detail alone draws no departure from the schema its process narrows. The problems panel reads that schema. Decide it with se test --on wk-97f1251d43 --propose TestANoteNeedsNoProposedAction
- the note process's decide step still asks for what happens to it, so the optional section loses nothing. Decide it with go test -C src/engine -run TestANoteNeedsNoProposedAction

## evidence: step 1. ask

<!-- restored by worker-gale after the submit wiped it. The engine's submit writes the payload's evidence map over the tables it just read off this file, and a se_pull carrying none writes nil. The text below is the text that was on the file when the gate read it and let the token close. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | — |  |
| [x] | every done-when line is decidable, and names the command where one decides it | It is true now, and it was not true as the token arrived, so the first done-when line was repaired before this was ticked. What was asked is unchanged, that a note of a title and a detail alone is accepted. Only the decider was corrected. See the evidence chapter the decider was corrected. | se test --on wk-97f1251d43 --propose TestANoteNeedsNoProposedAction |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [x] | one test was written first and seen red for the reason expected | TestANoteNeedsNoProposedAction was written before note.process.yaml was touched, and it went red for the defect the token describes. See the evidence chapter the test seen red. | se test --on wk-97f1251d43 --propose TestANoteNeedsNoProposedAction |
| [x] | the same test was seen green after the change, and named | The change moved proposed action from required to optional in src/processes/note.process.yaml, two lines, and TestANoteNeedsNoProposedAction then passed in 0.15 seconds. See the evidence chapter the test seen green. | se test --on wk-97f1251d43 --propose TestANoteNeedsNoProposedAction |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Two notes were minted rather than fixed here, wk-9cff60b794 and wk-9773928fc8. See the evidence chapter the cleanup minted as notes. | se work --abort |

## evidence: the decider was corrected

As written the first done-when named se lint as the command deciding whether a note is clean. se lint cannot decide that. It runs LintTokens, LintIcons, LintLimits, LintGuidance and LintProcesses, and none of those runs ValidateNote over a work token. Run against a tree holding a note of a title and a detail alone it answered five findings, every one about doc/guidance/methods/retro.md and doc/guidance/work-token.md and none about the note, and it answered the same before the change, so the line could not have gone red either way. The decider was corrected from a command that cannot see the property to the schema validation the problems panel actually reads. The gap in se lint is minted as wk-9cff60b794 rather than fixed here.

## evidence: the test seen red

TestANoteNeedsNoProposedAction mints a note the way the panel mints one, from a title and a detail. It reads the file back through ValidateNote, which is what the problems panel reads. Run before the change it said: a note of a title and a detail was marked as a problem: it has no proposed action chapter. That is the defect the token describes, seen by the check.

## evidence: the test seen green

Seven neighbours reading the same schemas and processes were run with TestANoteNeedsNoProposedAction and all passed: TestAMintRequiringDoneWhenRefusesWithoutIt, TestANoteThatFitsItsSchemaAnswersNothing, TestAnOversizeDetailIsRefusedOffTheSchema, TestEachDepartureFromTheSchemaIsNamed, TestTheStandardProcess, TestTheStateOfPlayReadsOneScreen and TestTheTemplateDoesNotReshuffleItself. The test also holds the other half of the ask, that the decide step still asks with evidence what happened to the note. So the section being optional loses nothing.

## evidence: the cleanup minted as notes

Neither is what this token asks. wk-9cff60b794 says se lint never validates a work token against its process schema, which is why the first done-when could not be answered. wk-9773928fc8 says se work ignores the -work folder for an untraced process and writes the note into the driving copy's real backlog. That second one bit while checking this token: two probe notes landed in .se/work and were aborted as dropped, wk-6cbf5200a9 and wk-374d3be992, each with a reason naming this token. Both new notes were minted from a title and a detail alone, which is this change working on the real tree.

