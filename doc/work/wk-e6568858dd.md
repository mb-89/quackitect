---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: branch head fails seven
# where the token stands. The process owns these values.
status: open
---

## detail

SEVEN TESTS FAIL ON THE BRANCH TIP AND NO TOKEN OWNS THEM. wk-00e4785e51 names three that flake and says in its own detail that the ones failing either way are a separate matter, naming the retro, the verb door and the fixture builders. This is that matter, measured.

Measured twice on this Linux box, with CGO_ENABLED one and the sqlite_fts5 tag, by `go test ./...` from src/engine in a detached worktree at the tip. Once at commit 6caeff27 and once at 37700f73. Both runs named the same seven, and a worktree at the same tip carrying no change named the same seven again.

TestARetroCollectsAndDrains, TestARetroLeavesAnotherActorsFolder, TestARetroLeavesTheSourcesEmpty, TestARetroTakesTheRunningSessionToo, TestASecondRetroTakesNothingTwice, TestAVerbRunsInsideTheEngineAndTheClientPrintsIt and TestOneFileCarriesTheFixtureBuilders.

TWO SHAPES, AND THEY ARE NOT ONE FAULT. Six answer the same sentence: engine: no method root here, the engine looked up from where it is and found no folder carrying src/processes. Those six run the built engine from a temporary folder, so the reading may say the suite wants a method named rather than that the product is broken.

The seventh is a plain red about the tree: two fixture builders live outside fixture_test.go, aBoxHolding in claimfar_test.go and aConsoleTree in keywordsaid_test.go.

A person landing a change reads this suite. Seven names that are red whatever they do teaches them to skip the list rather than read it.

## proposed action

Decide the two shapes apart first. Run the six by name from a worktree with the method named and see whether they pass, which says whether the six are the suite's setup or the product. Then either give those six their method root or say on the token why they cannot have one. The seventh is a move: aBoxHolding and aConsoleTree go into fixture_test.go, where the check says every builder lives.

## approach

Measure again at the tip before anything else. The seven were counted at 6caeff27 and 37700f73, and the branch has moved a long way since. A name that no longer fails is not this token's to fix.

Then the two shapes are told apart, because they are not one fault.

The six answer "engine: no method root here". They start the built engine in a temporary folder, and the engine looks upward for src/processes. The fixture already holds Roots.Method, so the seam is where it starts the child: the method root is handed over rather than searched for. If that turns them green, the six were the suite's setup, and the token says so.

The seventh is a move and nothing else. aBoxHolding in claimfar_test.go and aConsoleTree in keywordsaid_test.go go into fixture_test.go, where the check says every builder lives.

The seventh lands first, because wk-0e43a3c079 cannot tell a new red from that one until it is cleared.

## done when

- go test ./... from src/engine at the branch tip names none of the seven among its failures
- the token says which of the six were the suite's own setup and which were the product, naming what was changed for each
- the fixture builder check passes: se test --propose src/engine/TestOneFileCarriesTheFixtureBuilders answers ok with nothing unreached

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the approach is on the token before any work, as an interface or a shape a reader can disagree with |  |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the change is small enough to review whole, or it is split first | — |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | the change follows the approach on the token, or the token says why it departed |  |  |
| [ ] | se test --on this token answered ok, and what it ran is named |  |  |
| [ ] | the note says what changed and why, for a reader who was not here |  |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | [[reviewing]] was read and applied | — |  |
| [ ] | every hunk of git diff began..ended was read, and any not read is named |  |  |
| [ ] | every criterion's command was run again, and what it said is named |  |  |
| [ ] | every hunk improves the product, or a finding names the one that does not |  |  |
| [ ] | every finding is a trivial token naming this one, and their ids are here |  |  |

