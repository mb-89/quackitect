---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the spent ticket refusal
# where the token stands. The process owns these values.
status: closed
author: worker-kest
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - d8c7b7e4a7ab6e7df9f1b2c3f95f733f4ceaee85
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 56cfcbc46dfa89cf03827534b0ad1d06e3b53b16
# how it ended. Only an ended token carries one.
disposition: dropped
# why it was dropped
reason: "The mechanism this token is about no longer exists, and the behaviour it asks for is already there. The detail describes a ticket: se work --on armed one write and the write spent it, so a second write was refused. apply.go and gate.go both record that ticket being taken out because the name belongs on the edit, ticket.go went with it, and se apply and se run now take the token on the call. So nothing can be armed or spent and the fixture the criteria ask for cannot be built. The vocabulary is gone too: imp_in_work, ImpOpen and SpecOpen survive only in the retro archive, in tally_test.go fixtures as YAML filter strings, and in stale token text. The substance is already satisfied. whatIsOpen lists what the caller holds first, as \"<id> <title> (in your hands as <name>)\", before the unheld ones. So a refusal names the id in hand rather than sending the agent off to mint another. I saw it first hand this session, in the refusal a Bash call earned. I left src/engine/heldmenu_test.go in the tree. It pins both criteria in today's vocabulary. It asserts the refusal names the held token and says it is held. It asserts that an actor holding nothing is told nothing about hands and is not offered a token somebody else holds. It ran ok. wk-ffc6d3ae85 carries this token's detail word for word and still stands open. It wants the same drop."
---

## detail

After `se work --on <id>`, one product write is allowed and the second is refused with the no-token refusal, which ends by telling the actor to mint one. At that moment the actor holds the token in imp_in_work. The menu lists only ImpOpen and SpecOpen tokens, so the id it must name is missing. Make the refusal distinguish the two states. With a hand and a spent ticket it names the id, says `se work --on wk-xxxx --by main` again, and lists what the actor holds in work. With no hand at all it says what it says today. Check: a fixture arms a ticket, spends it on one product write, and requires the second refusal to carry the id in hand.

## done when

- A fixture arms and spends a ticket. The second refusal names the held id and the se work --on command.
- With no hand the refusal reads as today. The same test shows it.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one file, one test | heldmenu_test.go |
| [ ] | every done-when line is decidable, and names the command where one decides it | no: both name a ticket that no longer exists, so nothing can be armed or spent | apply.go, gate.go |
| [x] | the basics it stands on exist, or are minted first | whatIsOpen and WriteNeedsAToken exist. ticket.go does not | gate.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | rule 12: a check that will not go red is the finding | work-token.md |
| [ ] | one test was written first and seen red for the reason expected | no: whatIsOpen already names the held token, so no red was available | gate.go |
| [x] | the same test was seen green after the change, and named | TestTheMenuSaysWhatIsAlreadyInYourHands, ok | se_test |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began d8c7b7e4, one file added | heldmenu_test.go |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | no: wk-ffc6d3ae85 repeats this detail, still open | wk-ffc6d3ae85 |

