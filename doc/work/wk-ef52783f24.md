---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: rationale note miscounts descriptions
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: cage
---

## detail

Found reviewing wk-5b51fc14e4, which shipped this note. doc/rationale/a-description-is-an-instruction.md, why chapter, line 22, says "Cut to the instruction, 11 descriptions carry 883 bytes." The note names its own method on the next line: a sum of the description literals in the file it explains. Run that method over src/mcp/lane.go and the answer is 13 descriptions carrying 1788 bytes. It was already 13 and 1788 at b3b2bfd1, the token's own ended snapshot, so this is not drift. It was wrong when written. The rationale schema asks the why chapter for "the argument, and what was measured", so a measurement that does not reproduce is dead weight in the first example of the new kind. The token's own step 2 row says "I corrected its numbers, which did not reproduce", so the author caught this class in the token note and left it in the rationale note. Nothing checks a rationale's numbers. TestARationaleIsNamedNotRepeated checks the link and checks for repeated sentences, and reads no figure.

## proposed action

Re-measure src/mcp/lane.go with the method the note already names and write that number, or pin the sentence to a commit the way the line above it pins 7f22e1a3. Then decide whether a rationale's figures should name the command that produces them, as a done-when line does.

## done when

- the line reports what the named method answers over src/mcp/lane.go today, or names the commit it was measured at: se find --regex 'descriptions carry' --path doc/rationale/*.md
- a rationale that quotes a figure names where the figure came from, so the next reader can re-run it: go test -C src/engine -run TestARationaleIsNamedNotRepeated

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

