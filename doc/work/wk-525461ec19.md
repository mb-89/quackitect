---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: empty hands draw nothing
# where the token stands. The process owns these values.
status: open
---

## detail

The owner's rule, given on 2026-09-06: an agent with nothing in hand can despawn, and one holding something is shown with what it holds.

The agents table is declared with source present, and Present is the register. Its comment says so: who is here, which is not who has worked, and it holds an agent that has arrived and pulled nothing yet.

Actors already carries the rule. WhatIsHappening drops a waiting actor with nothing in hand, and its comment says the owner read four of those as nonsense. So the lesson was learned once for one list and the panel was pointed at the other.

The rule belongs at the drawing and not in Present. The staffing count reads len(Present), which oneprocessonerow_test.go asserts, so an agent hidden from that list is an agent the guard cannot count. A spawned worker that has not pulled yet would go uncounted, and the guard would ask for another.

This is separate from [[wk-2f51c2156a]], which is why one process shows twice. This rule holds whichever name wins there.

## proposed action

Draw no row for an agent holding nothing, in the panel rather than in Present, so the staffing count still sees every agent.

## done when

- an agent holding nothing draws no row, decided by: the panel check against a Present carrying one holder and one empty hand
- an agent holding a token draws a row naming it, decided by: the same check
- the staffing count still counts the hidden agent, decided by: WorkersHere against a Present with an empty hand in it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

