---
id: wk-7722faa0b8
seq: "-41"
type: work
title: a quoted criterion mangles
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 1
minted_by: main
submitted_by: main
reviewed_by: rev-33
evidence:
  - outcome
---

## detail

A criterion command carrying a double quote is unrunnable on Windows and reads as unmet. runEvidence in src/engine/pull.go builds exec.Command with cmd, /c and the script, and Go re-quotes the script so cmd never sees the inner quotes. It blocks wk-d898634fd3. Fix the runner: on Windows the script reaches cmd verbatim, and both window flags Quietly sets in src/engine/quiet_windows.go survive. The SysProcAttr write goes through the door that owns it, as TestEveryChildProcessIsStartedQuietly requires. The unix sh -c branch does not change.

## done when

- runEvidence echoing a phrase with spaces in double quotes answers the phrase with its quotes, equal and with no backslash
  `rg -q func.TestAQuotedScriptReachesTheShellWhole src/engine && go test -C src/engine -count=1 -run TestAQuotedScriptReachesTheShellWhole$ .`
- A criterion searching a file for a quoted phrase with spaces is met, and UnmetCriteria answers nothing
  `rg -q func.TestACriterionWithAQuotedPatternIsMet src/engine && go test -C src/engine -count=1 -run TestACriterionWithAQuotedPatternIsMet$ .`
- An unquoted script exiting zero and one exiting non-zero each answer what they did before
  `rg -q func.TestAnUnquotedScriptIsUnchanged src/engine && go test -C src/engine -count=1 -run TestAnUnquotedScriptIsUnchanged$ .`
- After a quoted script the command still has HideWindow and CreationFlags 0x08000000, and the SysProcAttr guard runs in the same command
  `rg -q func.TestTheWindowFlagsSurviveAQuotedScript src/engine && go test -C src/engine -count=1 -run TestTheWindowFlagsSurviveAQuotedScript$ . && go test -C src/engine -count=1 -run TestEveryChildProcessIsStartedQuietly$ .`
- Every test above was watched failing with the fix absent, and the evidence names each test and what it said

## evidence: outcome

TheScriptVerbatim in src/engine/quiet_windows.go, with a no-op sibling in quiet_other.go, sets the field on the struct Quietly made, and runEvidence builds its command through evidenceCommand. Criteria 1 and 2 were watched red with the fix absent, criteria 3 and 4 with a wrong fix planted by .se/scratchpad/quoted-reds.py. The engine suite is green.
