---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: chapter thirteen lost seeds
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-fir
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e77c1747dbae517dd69aa4474820ee06874ebb4e
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 82c8aacbf25baca5588b96c49941e00389db5819
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: Chapter 13 of testing.md now says the test hands in its random source and that a failing case is replayed by its seed. The words were paid for inside chapter 13, and lint is clean.
---

## detail

testing.md chapter 13 lost its closing sentence when wk-526ac833fb compressed the file. The lost sentence says the clock and the random source are fed to the code. So a five second wait is a millisecond, and a failing case is replayed by its seed. wk-f9233114b0 restored the probe sentence to that chapter but not this one. Rule 13 still names clock and randomness as fixtures a test holds. So the rule asks for something the discussion no longer says how to do. Half survives elsewhere: writing-go.md says a five second wait is then a millisecond, under testing/synctest, which is Go-specific and about the clock only. se find for seed over doc/guidance returns no hit at all, so replaying a failing case by its seed is nowhere in the tree. testing.md stands at 998 words against a cap of 1000, so the words have to be bought inside chapter 13.

## proposed action

Restore the seed half of the sentence to chapter 13, paying for the words inside that chapter. The clock half can stay short, because writing-go.md carries it for Go and rule 13 already names the clock.

## done when

- testing.md says how a random source is made a fixture and that a failing case is replayed by its seed. se find --regex 'seed' --path doc/guidance/software-development/testing.md returns a hit
- the file still lints clean: .bin/se.exe lint reports no finding for software-development/testing.md

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one file, one chapter, three replacements |  |
| [x] | every done-when line is decidable, and names the command where one decides it | both name a command and both ran from the root: se find --regex 'seed' --path doc/guidance/software-development/testing.md, and .bin/se.exe lint |  |
| [x] | the basics it stands on exist, or are minted first | all exist: the file, the 1000-word cap at src/schemas/guidance.schema.yaml line 60, and the lint door. Nothing minted |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md read: rule 11, only chapter 13 touched, and the clock half left to writing-go.md |  |
| [x] | one test was written first and seen red for the reason expected | se find --regex 'seed' --path doc/guidance/software-development/testing.md answered count 0 before the change |  |
| [x] | the same test was seen green after the change, and named | the same find answers count 1 at line 128, and .bin/se.exe lint answers clean true, findings null |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | one source file, doc/guidance/software-development/testing.md, three replacements in chapter 13 |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | paid inside chapter 13: lint refused at 1001 words, and two trims there brought Discussion to 1000 |  |

