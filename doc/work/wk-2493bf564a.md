---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: battery selection flake
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-tallis-two
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 783b0499b6503ee8e5a7c5046ee4b288de749be7
  - 45d4dbce4cc8d33ffd5f68b8336d3c98e2bb2fb2
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 2ca71e69ef595d45edefb48999f2b66c19735a44
  - aba6cbb474c63ac4e5594c77660ff02f0cc1b100
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

TestWhenTheWholeBatteryRuns failed once inside a full package run and passed on every run of its own. The failure said a selection of everything did not call for the whole battery. The answer it printed carried two line deltas over lib.go with an empty Chosen. Three runs of the test alone answered ok, and the test door answered ok on it too. A test that goes red under load and green alone is red. So the selection this test asserts is either timing dependent or it reads a state another test in the package writes. Seen on 2026-09-05 in a worktree at 0925cc75 while wk-4e91c7b3f8 was worked.

## done when

- the failure is reproduced by a full package run, or the token says how many runs found nothing, decided by go test -count=1 over src/engine
- what the test depends on beyond its own fixture is named, decided by reading the test and aTreeWithTests for a path or a clock it shares

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | a red run means something again | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | a red that comes and goes teaches a hand to re-run until green | the detail |
| [x] | the ask is small enough to review whole, or it is split first | one lock in one fixture |  |
| [x] | every done-when line is decidable, and names the command where one decides it | both by go test over the four tests that feed | done when |
| [x] | the basics it stands on exist, or are minted first | the fed toolchain and the test both exist |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | testing says a flaky test is a red one, so this was made repeatable before it was fixed | testing.md |
| [x] | one test was written first and seen red for the reason expected | the test existed. Four of eight runs of the four feeding tests failed at tests_test.go:217, with lib.go uncovered and nothing chosen | 4 of 8 red |
| [x] | the same test was seen green after the change, and named | TestWhenTheWholeBatteryRuns, eight of eight green with the same command. The whole package answered its usual seven | 8 of 8 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | one lock and one unlock in toolchainfed_test.go | toolchainfed_test.go |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | none. The seam itself is a package variable, which is a bigger change than this token | — |

