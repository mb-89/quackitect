---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the engine says guarded
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/fable-cloud
claimed_at: "2026-09-05T14:13:08Z"
---

## detail

A session can run with no guard at all and nothing says so. Where the harness root sits above the repository, the settings file never loads and no hook fires.

That happened for a whole session. Every hook-written line in the record belonged to a different session, and every rule built that day was enforced against nobody in the session that built it.

The failure is silent by construction. An agent cannot tell a guard that passed everything from a guard that never ran.

## proposed action

The engine says at session start whether the guard is live, in one line an agent cannot miss, and the record carries it.

## done when

- the engine says at session start whether the guard is live: a test starts one with the hook reachable and one without, and reads a different line each way
- the record carries the same answer: the session log holds one line naming it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one new file of 45 lines and one block in main, beside its test |  |
| [x] | every done-when line is decidable, and names the command where one decides it | both by go test -run TestTheEngineSaysWhetherTheGuardIsLive |  |
| [x] | the basics it stands on exist, or are minted first | listenHooks, hooksURL and the log all exist, and this only says what they already know |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | the test was written first and watched red on two undefined names |  |
| [x] | one test was written first and seen red for the reason expected | red: undefined holdTheDoor and undefined SayTheDoor |  |
| [x] | the same test was seen green after the change, and named | TestTheEngineSaysWhetherTheGuardIsLive ok, with the two hook tests still ok, run over a copy of origin/v4 carrying this change |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | the tree is shared and was mid-edit by four hands, so the commit naming this token is the honest record |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | one cleanup was in the way and is committed separately: origin/v4 itself did not compile, and one call in another hand's test was the whole break |  |

