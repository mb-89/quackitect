---
id: wk-c7535682d6
seq: 1000090
type: work
title: the exit nobody read
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-1
---

## detail

A walk of the explicit returns is offered as every way a thing can fail, and the paths nobody wrote on purpose are not counted. Found on wk-551d5a2008: the detail names three silent returns, and src/extension/extension.ts line 1125 has a fourth, the catch around JSON.parse resolving undefined. askEngine attaches no handler to standard error, where fail and failUnread in src/engine/main.go print, so every non-JSON engine refusal takes that path. When a detail says every way a thing can fail, count exits from the bottom: every return, resolve, catch and fall-through. Add every stream nobody subscribed to. Put the number in the detail and make the check enumerate the same way. Before listing failures, list the channels the code could speak on and ask of each whether this code reads it.
