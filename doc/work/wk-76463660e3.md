---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: client failure says nothing
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-part
claimed_at: "2026-09-05T14:12:50Z"
---

## detail

TestAVerbRunsInsideTheEngineAndTheClientPrintsIt in src/engine/liveengine_test.go fails on a clean copy of the tree, and all it says is `liveengine_test.go:56: the client failed: exit status 1`.

The reason is lost by the test itself. It runs the client with exec.Command(...).Output(), which captures standard output only, and then prints that captured output beside the error. The client writes its refusal to standard error, so the one thing a reader needs is the one thing not printed. exec.ExitError carries it in Stderr, and CombinedOutput would carry it too.

Found while working wk-2083d0e131, which does not touch this test. It fails the same way with and without that token's change, so it is not that change: a copy of the tree at bbb70d2 with nothing added fails it too.

The first ask is the message. What the client is actually refusing is behind it, and belongs to whoever reads the message the fix prints.

## done when

- the failure message of TestAVerbRunsInsideTheEngineAndTheClientPrintsIt names what the client wrote to standard error, decided by running that test on a copy of the tree and reading the line it prints
- the test's own assertion is unchanged in what it accepts, decided by: with the engine answering, the test passes

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One file, and only the reporting in it. What the client refuses is [[wk-03ebebbccb]]. | `git diff -- src/engine/liveengine_test.go` |
| [x] | every done-when line is decidable, and names the command where one decides it | Both are decided by one run on a copy of the tree, unpacked from `git archive` with this file over it. The first reads the line printed, the second wants exit zero. | `CGO_ENABLED=1 GOFLAGS=-tags=sqlite_fts5 go test -run TestAVerbRunsInsideTheEngineAndTheClientPrintsIt -count=1 -v .` |
| [x] | the basics it stands on exist, or are minted first | One, and it is here already: Output fills ExitError.Stderr when Stderr is nil, which is how this file drives the client. Nothing was minted. | `errors.As(err, &exited)` |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 12 set the order, rule 4 the check: it reads the refusal off the stream. | work-token |
| [x] | one test was written first and seen red for the reason expected | TestAClientFailureNamesTheReasonStream, with the old reporting back, said the failure says "exit status 1" and not what the client wrote. | `--- FAIL` |
| [x] | the same test was seen green after the change, and named | TestAClientFailureNamesTheReasonStream passes. TestAVerbRunsInsideTheEngineAndTheClientPrintsIt then prints `it said: engine: no method root here`, and with SE_ENGINE inside the tree both pass. | `ok quackitect/engine` |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | One file from 8c51ad50: the helper, two callers, the test, two imports. | `git diff` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Its own, [[wk-03ebebbccb]]: the shared engine is built under the OS temp root and finds no method root. | wk-03ebebbccb |

