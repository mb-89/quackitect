---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: swap leaves lanes stale
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-elgar
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - df02a72c28a317676e1efe5705810ac76c8da97d
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 35feadb5088333905e1ef0711fdb0f07e5e8f8d7
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: A swap now builds and installs every program the manifest names, so .bin is one set rather than half new. The red test in the tree is green.
# what it became. They have to exist.
successors:
  - "[[wk-80e7a9bd52]]"
---

## detail

From the verdict on wk-212909368a. src/engine/TestASwapBuildsEveryProgramTheManifestNames is RED in the tree right now: "logview is not in .bin after a swap". The manifest half of the swap door is written and tested but never wired in. src/engine/swap.go:142 buildNext builds src/engine alone, and swap.go:236 putInPlace renames the one binary it was handed. theBuilds (swap.go:98), manifestBuild (swap.go:78), nextBinary (swap.go:125) and engineAt (swap.go:132) are reachable only from swap_test.go. So a swap leaves .bin/se-mcp.exe and .bin/logview.exe at whatever build they were, which is the exact defect theBuilds' own comment (swap.go:86-93) records as measured: .bin/se.exe at 13:16 and .bin/se-mcp.exe at 10:45 with src/mcp changed between them, and the standing check for the lane driving code that was not in the tree. Either wire theBuilds through buildNext and putInPlace, or delete the four unwired declarations and the test that asserts behaviour the door does not have.

## done when

- go test -C src/engine -run TestASwapBuildsEveryProgramTheManifestNames is green
- either theBuilds is called from a shipped path, or theBuilds, manifestBuild, nextBinary and engineAt are gone from swap.go
- go vet -C src/engine ./... reports no unused declaration in swap.go

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one file, three functions in it, and the test was already written | reviewed whole, not split |
| [x] | every done-when line is decidable, and names the command where one decides it | se test --propose TestASwapBuildsEveryProgramTheManifestNames, and go vet -C src/engine ./... | both run, step 2 says what they answered |
| [x] | the basics it stands on exist, or are minted first | theBuilds, manifestBuild, nextBinary and engineAt were all written and tested already. The wiring was the only thing missing | nothing was minted |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token 12, red first, and 13, both halves: the build half and the install half now read one list | — |
| [x] | one test was written first and seen red for the reason expected | it was red in the tree before I touched it: swap_test.go:201, logview is not in .bin after a swap | red, watched first |
| [x] | the same test was seen green after the change, and named | TestASwapBuildsEveryProgramTheManifestNames, ok true, with TestASwapToTheSameBuildIsRefused beside it | green |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/engine/swap.go alone | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | engineAt was test-only, so planSwap and handOver now call it instead of building the path again. And wk-80e7a9bd52, for the literal name list left at main.go:136 | — |

