---
id: wk-74558c90da
seq: "57"
type: work
title: one class one wiring
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "The remedy landed on the editor port, where the level-add loop matches .bs-add[data-kind] at src/extension/editor.ts:976, so the token is obsolete."
aborted_from: backlogged
minted_by: reviewer4
---

## detail

A styling class reused as the handle a wiring loop matches on, so the loop reaches controls it was not written for. editor.ts puts bs-add on the level-add buttons and also on Add condition, Add group and Hide all. wireChrome loops over .bs-add and overwrites the Hide all handler. Give the level-add button a class nothing else answers to, or select it by its data attribute. The check, red today: drive-editor.mjs presses Hide all in the Properties popover and requires a columns message with an empty only list and no error. Found on wk-080da4232c.
