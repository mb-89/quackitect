---
id: wk-74558c90da
seq: "57"
type: work
title: one class one wiring
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "The remedy landed on the editor port: the level-add loop matches .bs-add[data-kind] at src/extension/editor.ts:976, so Hide all keeps the handler wireChrome gave it and bs-add stays the styling name. Obsolete."
aborted_from: backlogged
minted_by: reviewer4
---

## detail

A class that is a look, reused as the handle a wiring loop matches on, so the loop reaches controls that are not the one it was written for. editor.ts gives the level-add buttons class bs-add and data-kind, and bs-add is also on + Add condition, + Add group and Hide all. wireChrome then does for (const add of wrap.querySelectorAll('.bs-add')) add.onclick = ..., which overwrites the handler it set for Hide all a few lines earlier in the same function, and the replacement throws because Hide all carries no data-kind.

REPRODUCED in .se/scratchpad/reviewer4/drive-add.mjs: pressing Hide all in the Properties popover throws TypeError: Cannot read properties of null (reading 'querySelector') and no column is hidden. + Add condition and + Add group survive only because wireFilter runs after wireChrome and re-sets them.

WHAT TO DO: give the level-add button a class nothing else answers to, or select it by its data attribute rather than by the shared look class. Keep bs-add as the styling name if it is worth having and stop matching on it.

THE CHECK, RED TODAY: in drive-editor.mjs, open the Properties popover, press Hide all, and require a columns message with an empty only list and no error thrown. It sends nothing today. Found on wk-080da4232c.

