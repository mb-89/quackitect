---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: twin bash is unguarded
# where the token stands. The process owns these values.
status: open
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b2b00c5da74e86db0b2d05173c135c9b739ba8b1
---

## detail

Found reviewing wk-e51c579664. That token taught the shell lookup to pass over the WSL launcher, and src/engine/substitution_test.go was left doing the same lookup by hand. Line 94 asks exec.LookPath("bash") and fatals if it answers nothing, and line 124 runs exec.Command("bash", "-c", ...) as the oracle the whole table is judged against. On this machine that name is the WindowsApps alias. Verified live: se_test on TestBashDecidesWhichCommandsLeaveTheException came back ok false, "bash refused the twin for a bare substitution: exit status 1", carrying WSL_E_WSL_OPTIONAL_COMPONENT_REQUIRED in UTF-16, which is the exact error the token's own detail names. So the guard's best table sits red on the box it was written on, and read as green somewhere else it would be comparing the engine against a launcher rather than against bash. The lookup here should go through theShellAmong, so it finds the sh Git brought or skips honestly.

## done when

- TestBashDecidesWhichCommandsLeaveTheException passes or skips on a Windows box whose only bash is the WSL launcher: go test -C src/engine -run TestBashDecidesWhichCommandsLeaveTheException
- the oracle it runs is the shell theShellAmong answers, not the bare name bash
- where no POSIX shell exists at all it skips saying so, rather than fatals

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | the table is judged against the shell se_run hands a command to, on every box, and a box with none says so | f9a2af95 |
| [x] | what breaks if it is never done, and not only that it stays undone | on a Windows box without WSL the table sits red on the launcher. With WSL it reads green against a shell over another filesystem | f9a2af95 |
| [x] | the ask is small enough to review whole, or it is split first | one lookup and one oracle, in one test file | f9a2af95 |
| [x] | every done-when line is decidable, and names the command where one decides it | the first names go test on TestBashDecidesWhichCommandsLeaveTheException. The second is read off the oracle it runs, and the third off the skip | f9a2af95 |
| [x] | the basics it stands on exist, or are minted first | posixShell, theShellAmong, writeProbe and the idiom in TestARunUsesTheShellGitBrought are all in the tree | f9a2af95 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md read. Rule 14 is why the token went back once, and why it closes now on runs somebody watched | f9a2af95 |
| [x] | one test was written first and seen red for the reason expected | the red is the detail's own run: se_test on the Windows box answered ok false, bash refused the twin, exit status 1, WSL_E_WSL_OPTIONAL_COMPONENT_REQUIRED. A Linux box cannot redden it, because the bare name bash is a shell here | — |
| [x] | the same test was seen green after the change, and named | go test -run TestBashDecidesWhichCommandsLeaveTheException over a worktree of 602d451a: PASS in 0.05s. The same test with PATH holding go and git only still ran an oracle, and it named /bin/sh, so the oracle is what posixShell answers and not the name bash | 602d451a |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/engine/substitution_test.go alone, on origin in f9a2af95. The began hash here is one the closing clone never had, so the diff is read off origin | f9a2af95 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | the build break the first hand met is gone. go build and go vet over src/engine at 602d451a answer clean, and gofmt names no file | 602d451a |

## evidence: the finding

THE CHANGE IS ON ORIGIN IN f9a2af95, AND THE RUNS NOBODY HAD WATCHED ARE
WATCHED NOW.

The oracle no longer runs the bare name bash. theShellThisMachineHas asks
posixShell, which passes the WSL launcher over and finds the shell beside git,
and it skips saying where it looked when this machine has no POSIX shell at all.

What the first hand could not watch was the package refusing to compile under
another refactor in flight. That refactor has landed. At 602d451a go build and
go vet over src/engine answer clean, and the test named on the token passes in
0.05s over a worktree of that tip.

THE ORACLE WAS WATCHED BEING SOMETHING OTHER THAN BASH. A second run gave the
test a PATH holding go and git only, so no bash resolved anywhere. The test
still ran an oracle, and the oracle answered /bin/sh: 1: touch: not found. That
shell is the one shellsBesideGit found walking up from git's folder. The run
failed on touch missing from that bare PATH, which is the harness and not the
change.

THE SKIP BRANCH IS READ, NOT WATCHED, AND HERE IS WHY. On a Linux box git sits
under /usr/bin, and the walk from any git folder reaches /bin/sh within four
steps, so posixShell never answers empty here. The branch is at
theShellThisMachineHas: an empty answer skips with the folders looked in, and
nothing there fatals. A Windows box with neither Git nor WSL is where that
branch runs, and this token had none to hand.
