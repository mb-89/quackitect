---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: twin bash is unguarded
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-aalto
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b2b00c5da74e86db0b2d05173c135c9b739ba8b1
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 001e1d7a7952c0e65ef5ada012ee70d65af9882b
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: The change was already written and is committed at the branch tip. The green is watched now, on a worktree of the tip with the file wk-5f8dda7870 names set aside. The red wants a Windows box and this one is Linux, so it stays unwatched and the token says so.
---

## detail

Found reviewing wk-e51c579664. That token taught the shell lookup to pass over the WSL launcher, and src/engine/substitution_test.go was left doing the same lookup by hand. Line 94 asks exec.LookPath("bash") and fatals if it answers nothing, and line 124 runs exec.Command("bash", "-c", ...) as the oracle the whole table is judged against. On this machine that name is the WindowsApps alias. Verified live: se_test on TestBashDecidesWhichCommandsLeaveTheException came back ok false, "bash refused the twin for a bare substitution: exit status 1", carrying WSL_E_WSL_OPTIONAL_COMPONENT_REQUIRED in UTF-16, which is the exact error the token's own detail names. So the guard's best table sits red on the box it was written on, and read as green somewhere else it would be comparing the engine against a launcher rather than against bash. The lookup here should go through theShellAmong, so it finds the sh Git brought or skips honestly.

## done when

- TestBashDecidesWhichCommandsLeaveTheException passes or skips on a Windows box whose only bash is the WSL launcher: go test -C src/engine -run TestBashDecidesWhichCommandsLeaveTheException
- the oracle it runs is the shell theShellAmong answers, not the bare name bash
- where no POSIX shell exists at all it skips saying so, rather than fatals

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The guard is held against a shell, and not against a launcher that runs nothing. | the oracle |
| [x] | what breaks if it is never done, and not only that it stays undone | The best table the guard has stays red on the box it was written on. | the detail |
| [x] | the ask is small enough to review whole, or it is split first | one lookup and one oracle, in one test file | — |
| [x] | every done-when line is decidable, and names the command where one decides it | the first names go test on TestBashDecidesWhichCommandsLeaveTheException. The second is read off the file, and the third is the skip that test now takes | — |
| [x] | the basics it stands on exist, or are minted first | posixShell, theShellAmong, writeProbe and the idiom in TestARunUsesTheShellGitBrought are all in the tree | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md read. Rule 11, nothing next to the ask, is why the build break below was left alone | — |
| [x] | one test was written first and seen red for the reason expected | STILL NOT WATCHED, and it cannot be here. The red wanted is this test on a WSL-only box, which is a Windows box | see the finding |
| [x] | the same test was seen green after the change, and named | WATCHED NOW. TestBashDecidesWhichCommandsLeaveTheException passes on a worktree of the branch tip | see the finding |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/engine/substitution_test.go, and nothing else | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | the build break is another hand's work in flight and is left to them | — |

## evidence: the finding

THE CHANGE IS WRITTEN, AND THE GREEN IS WATCHED. THE RED IS NOT, AND NO BOX HERE
CAN WATCH IT.

The oracle no longer runs the bare name bash. theShellThisMachineHas asks
posixShell, which passes the WSL launcher over and finds the shell beside git.
It skips saying where it looked when this machine has no POSIX shell at all.

The change is committed and stands at the branch tip. What blocked the watch
before was one hand's refactor in flight, named on this token as quiet_test.go.
That is gone. The block now is sessiontakesmainback_test.go, which calls OpenLog
and Yes after both moved to internal/sessionlog, and that is wk-5f8dda7870. With
that one file set aside on a worktree of the tip, the test passes.

The red wants this test judged against the WSL launcher, and that is a Windows
box. This one is Linux. What holds here is the mechanism the criteria name.

