---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: zero rules pass quietly
status: closed
disposition: done
---

## detail

LoadVoiceRules at voice.go:52 returns no error when the file parses to an empty rule list, so Check finds nothing and every write passes. The unreadable case is loud by ruling at hook.go:526, and the empty case is silent. Refuse to run with zero compiled rules, the same way a check that finds nothing to check refuses. The limits default at voice.go:68 stays.

## done when

- A test loading a zero-rule file sees a refusal, red then green.
- hook.go:526 and voice.go:68 unchanged. go test ./src/engine passes.

