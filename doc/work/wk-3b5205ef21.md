---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: actor is a session
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-brahms
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0992d9c3bf535ee8cc792699807e8e15400fe3ed
  - 17f22ab8a8f75a5ce1ab0b083c7e70a353583afd
  - 243e556c9040154232f6dfcd387ed2ceb3472028
  - 65eb52d5f375fa340d2f9151978e04061e5bcea7
  - 02fbc22ac934a87f775e7d7e600b7b3045211842
  - 5bcbc5979d93852e51d3e9d13d91d194987acca0
  - 842626f4b090e8f2fbce94abccc75de22280820d
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - bcb9138f4ef4e9c5d4d2cb18f67db7e0fa430449
  - e2b380532cbb48b08de8ebc8f5e00a58d23fa080
  - 930b166e6f781d32e1c485d1db1b12a13a44fbb6
  - 16be9d0920717427fe222a3c055e12ba91c7ce0c
  - 598ed57269d554cbf41aaa61c78b750d080fd69c
  - 72343cd8a1b55fad69a7bcc94b15c2d5adcb578d
  - db0c17b77fab62dd0b3ef8be14dfe1c079f2d161
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: Pass. An actor is a session and not a word, and the change makes it so. The register gives the first session main and every other a name of its own, TheActorOf reads it at the hook, and the guard refuses a name a live session of another id holds, at the lane and at the shell. It holds its three criteria and leaves the tree better. One finding, wk-3bfef668b0, which does not withhold the pass.
---

## detail

Two sessions ran at once and both were the actor main. TakeUp puts back everything else that actor holds. So every time one session named a token, the other session's token left its hands. That happened three times in the log, each followed by refused: no token in hand. An agent spent fifteen turns locked out. The name main is a word, and two sessions can say it. Consumes wk-52271812a5.

## done when

- two sessions over one tree hold two tokens under two names and neither puts back the other's: go test -C src/engine -run TestTwoSessionsAreTwoActors
- a take-up under a name a live session of another id holds is refused naming that session: go test -C src/engine -run TestANameAnotherSessionHoldsIsRefused
- the header and the table draw the second session under its own name: node util/checks/panel-draws-the-register.mjs .

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | It is, as a shape: a session id names the session, and a second session becomes main with the id's short form. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Three lines, three commands, all run and green. Two go test names and one node check. Step 2 says what each answered. |  |
| [x] | the change is small enough to review whole, or it is split first | Three source files and one check, one idea in each. |  |
| [x] | the basics it stands on exist, or are minted first | It stands on a shell that runs a command, and this box has none. Already minted: wk-e51c579664. Every criterion here ran through se test. |  |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Red first: both tests watched failing with aSessionName weakened to main. Both halves: lane and shell driven through one rule. |  |
| [x] | the change follows the approach on the token, or the token says why it departed | It follows it. One departure: the refusal is in the guard, not TakeUp, because no verb says which session called. |  |
| [x] | se test --on this token answered ok, and what it ran is named | ok: true. It ran the two tests and the check named here, and six register tests beside them. All green. |  |
| [x] | the note says what changed and why, for a reader who was not here | It names each file, the red, and what the third criterion cannot decide. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The dead shell, already minted as wk-e51c579664. Nothing else. |  |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | Read whole, and applied in two passes. Eight candidates, one kept. |  |
| [x] | every hunk of git diff began..ended was read, and any not read is named | All of this token's hunks read: evidence.go, gate.go, hook.go, sessionisanactor_test.go, panel-draws-the-register.mjs. Not read: the first three take-up windows, and in the fifth lane.go, mcp/main.go, tests.go, battery.sh and four test files, all other tokens' work. |  |
| [x] | every criterion's command was run again, and what it said is named | se test ran all three, all ok. Battery owed under wk-e51c579664. |  |
| [x] | every hunk improves the product, or a finding names the one that does not | Each earns its place. The name is settled once and kept, so it cannot move under tokens already held. The refusal names the session and the name to use. Both doors closed. One finding: gate.go:517 asks Gone without Run, unlike evidence.go:251. |  |
| [x] | every finding is a trivial token naming this one, and their ids are here | wk-3bfef668b0. |  |

## approach

An actor is a session, not a word. The session id the harness sends on every hook names the session. main becomes main-<the id's short form> for a second session over the same tree, with the first keeping main. A take-up by a name that a live session of another id already holds is refused, naming the session. The register already knows the sessions. The gate reads it.

## note

An actor is a session. The register called every session main, so two over one tree were one actor. TakeUp puts back what that actor holds, so each emptied the other's hands.

src/engine/evidence.go: NoteSession asks aSessionName. The first session keeps main. Any other is main with the id's short form. TheActorOf reads it, and src/engine/hook.go asks that where it asked NameOf.

src/engine/gate.go: ANameAnotherSessionHolds refuses a name a live session of another id holds. The guard asks it first, and not TakeUp, because no verb's arguments say which session called. Both doors: theNameACommandActsUnder reads --actor and --by off a shell command.

Red: with aSessionName answering main, both tests failed for the defect.

FINDING. The third criterion cannot go red, because panelHtml derives nothing. The check holds that two rows stay two. TestTwoSessionsAreTwoActors decides the naming.

se run is dead, so the battery is owed. See wk-e51c579664.

