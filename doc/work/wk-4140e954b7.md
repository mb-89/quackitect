---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: engine tests fail building
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-aalto
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 8711e9137c22f4f8dcb980938d02c40265062e26
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 7c6fa5fbef6d7cbae91c98c47a32f7e47a3e8d4e
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: "The hand mid-change is the clone, not either file. At the branch tip TestTheDelta already takes a context, so the two calls stand as written and the build answers exit zero there. This clone's HEAD lacks that commit, 735ebc83, so it alone answers the two arity errors. The ready_when on wk-dc6bfb21f5 now names those errors rather than writeWorkableProcess, which is defined."
# what it became. They have to exist.
successors:
  - "[[wk-7bcb526fb7]]"
  - "[[wk-a2826160cc]]"
---

## detail

The src/engine test binary does not build, so no check under that package can be watched reddening on an assertion. Measured with go test over the package, which answered two errors and nothing else: snapshotnevertravelled_test.go calls TestTheDelta with a context and a nil where the function takes neither. The non-test build is sound, so the engine still builds and swaps. wk-dc6bfb21f5 is parked on this same wall and names a different cause, an untracked arrival_test.go calling writeWorkableProcess, which no longer appears in the answer. A reader of that token would run the build, see its stated cause gone, and still find no red available.

## proposed action

Bring the two calls into line with TestTheDelta as it now stands, or say on the token which hand is mid-change and leave it. Then correct the ready_when on wk-dc6bfb21f5 so it names the cause that is actually there.

## done when

- the package builds its tests, decided by: CGO_ENABLED=1 GOFLAGS=-tags=sqlite_fts5 go test -C src/engine -run TestNothingAtAll ./... answering no build failure
- the ready_when on wk-dc6bfb21f5 names a cause the build answer still shows, decided by: read that token beside the build answer

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A reader of wk-dc6bfb21f5 stops hunting a cause that is already gone. | its ready_when |
| [x] | what breaks if it is never done, and not only that it stays undone | The next hand drops that token again on a wall it cannot find. | the old ready_when |
| [x] | the ask is small enough to review whole, or it is split first | — | one line of one note |
| [x] | every done-when line is decidable, and names the command where one decides it | The first names the build. The second is a read, because no check may name a token. | tests-name-no-token |
| [x] | the basics it stands on exist, or are minted first | — | both notes, both builds |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rule twelve says a check that cannot redden on an assertion is the finding. Both criteria are that. | work-token |
| [x] | one test was written first and seen red for the reason expected | The build is the only check, and it reddens on a build failure alone. It named both calls. | the clone answer |
| [x] | the same test was seen green after the change, and named | The same command on a worktree of the branch tip answered exit zero, with no code changed. | the tip answer |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | one line in wk-dc6bfb21f5 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-7bcb526fb7 carries the same stale wall elsewhere. wk-a2826160cc carries the clone being behind. | two tokens |

## evidence: where this stands

NO CODE CHANGED, AND THE HAND MID-CHANGE IS THE CLONE.

At the branch tip TestTheDelta already takes a context, so the two calls in snapshotnevertravelled_test.go are right as they stand. This clone's HEAD does not carry that commit, 735ebc83, so the same two files disagree here. Dropping the context to satisfy this clone would break the tip.

The build ran twice, with the command the first criterion names. In this clone it answered two errors and exit one. On a detached worktree of the branch tip it answered exit zero.

The second criterion is a read, and it was done. wk-dc6bfb21f5 named an untracked arrival_test.go calling writeWorkableProcess. That file is tracked at the tip, writeWorkableProcess is defined in workableprocess_test.go, and the build answer names neither. Its ready_when now names the arity errors the answer does show.

