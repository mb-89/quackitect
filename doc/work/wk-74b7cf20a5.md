---
id: wk-74b7cf20a5
seq: "-2"
type: work
title: a parent follows children
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-66a28ca311
rounds: "1"
minted_by: person
evidence:
  - outcome
---

## detail

A parent is in work while any child is in work, and it leaves in_work when the last child does. Nothing pulls a parent, so the rule lives in the engine at the one place a state change is written. A parent today looks blocked by its own sub-token, when the sub-token is how the parent is being done.

## evidence: outcome

followChildren in store.go raises a parent from open to in_work when a child goes in work and lowers it when the last child leaves. Both happen only while the holder is empty. The pull at pull.go:419 now asks the holder too, so a parent raised by the rule is never handed out. TestTheQueueHandsBackTheChildAndNotTheParent and TestAHeldParentIsNotLoweredByItsChild cover both findings, and the engine suite passes.
