---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: no stop with notes
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/fable-cloud
claimed_at: "2026-09-05T13:33:50Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 298ee307122a44762f329be3448ce9fb62492b48
---

## detail

THE OWNER'S WORDS: when you have nothing else to do, claim all the notes that are still there and work them in. Then you can stop. The goal is that nothing gets lost when the box vanishes.

The ceiling holds the work at twenty notes, which is the middle of a session. This is the other end: the last thing a cloud box does before it goes quiet.

A stop is where it belongs, because a stop is the box saying it has nothing else to do. Every note still open at that moment is about to be lost, and the agent is the only one who can turn it into a token.

A desk is left alone, the way it is at the ceiling.

## proposed action

Refuse a stop on a cloud box while a note is open, and name the notes in the refusal, so the last act of the box is turning them in.

## done when

- a stop claimed on a cloud box with one open note is refused, and the refusal names that note
- the same stop with no note open is granted
- a desk with notes open is granted, proved by the same test with the variable unset
- a Go test in src/engine drives all three

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one function beside the ceiling it belongs to, and four lines in the stop claim |  |
| [x] | every done-when line is decidable, and names the command where one decides it | all four by go test -run 'TestACloudBoxTurnsThemInBeforeItStops|TestADeskStopsWithNotesInHand' |  |
| [x] | the basics it stands on exist, or are minted first | the note count and the host reading both landed an hour ago on wk-b75b6ebff3 |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | the test was written first and watched red |  |
| [x] | one test was written first and seen red for the reason expected | red: a cloud box stopped with a note in hand, and the note dies with it |  |
| [x] | the same test was seen green after the change, and named | TestACloudBoxTurnsThemInBeforeItStops and TestADeskStopsWithNotesInHand, both ok, with TestTheFirstStopOfASessionIsGranted still ok |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | the tree is shared, so the commit that names this token is the honest record |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | none revealed: the ceiling and this share one reading of the notes |  |

