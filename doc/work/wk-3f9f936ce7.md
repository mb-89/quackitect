---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: lane cannot flag needs_human
# where the token stands. The process owns these values.
status: open
---

## detail

A finding on wk-b75b6ebff3, notes become tracked tokens.

src/engine/notesgohome.go:65-66 tells the held agent the third answer: "you cannot decide it, so mint a tracked token carrying your best attempt and set needs_human on it". The lane cannot do the second half. src/mcp/lane.go:219-230, workArgs, is the whole of what se_work takes: title, detail, process, tracked, proposed_action, done_when, depends_on, parent, on, actor. There is no needs_human, and no set/field/to. The only door to the flag is `se work --set <id> --field needs_human --to true`, declared at src/engine/work.go:48-50, which is a shell call.

THE DAMAGE. Both routes are shut at once. In the lane the flag cannot be set at all, and at a shell the same guard holds the Bash call (see the sibling finding on TooManyNotes and the command). So the answer the owner's words single out, the undecidable note that must still reach a person, is the one answer an agent under this refusal cannot give. It mints the token, cannot flag it, and the token lands where nobody is looking for it.

THE CHECK. Nothing asserts that what a refusal instructs is reachable through the tools that same refusal leaves open. A test that takes each action TooManyNotes names and does it through the lane, with the guard standing, catches this class.

## done when

- se_work carries needs_human, or the refusal in src/engine/notesgohome.go names the door that does exist
- a Go test drives the three answers TooManyNotes names through the tools the guard leaves open, with the guard standing, and each one lands: a note dropped, a tracked token minted, and a tracked token minted with needs_human true
- the needs_human half of that test is seen red before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

