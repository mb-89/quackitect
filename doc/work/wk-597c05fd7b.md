---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: the count already exists
status: closed
disposition: done
---

## detail

A check enumerates a set for its anti-vacuity guard and never holds that enumeration against what it covered. util/checks/engine-args.mjs computes Object.keys(A) over the builders module and asserts only builders.length > 5. src/extension/engineargs.ts exports 26 builders and the runners ask 25, the other being readKind. Hold Object.keys(A) against the builders the run asked and fail naming what is in one and not the other. Name a member deliberately out of scope with its reason in the same table. Related: wk-1412093cd8.

## done when

- engine-args.mjs fails naming any builder exported and not asked, or the reverse.
- An out-of-scope member such as readKind stands in the table with its reason.

