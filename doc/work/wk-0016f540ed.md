---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: cold door misses needs_human
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-glass
claimed_at: "2026-09-05T15:01:41Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 1fbd8b45dd730523ccd39c0d0601b6bb707e1ebc
---

## detail

util/cage/tools.json is what a cold session is shown before the lane is built. It is generated by `.bin/se-mcp --tools > util/cage/tools.json`, and it is behind the lane by one field.

The lane's request for se_work carries needs_human, described as "true when the answer is not yours: your best attempt, and a person reads it first". Rebuilding the stub and running --tools prints that field. The file in the tree does not have it.

So an agent in a cold session is offered a door without needs_human, sends it anyway if it read the warm door, and the harness drops it as unknown. That is the same shape as the tracked field that was found before, where a mint through the door was refused for a field the caller could not send.

Found while working wk-37210ec714, which added take to se_claim and wrote only that one hunk into the file, so this one is untouched and still owed.

## done when

- util/cage/tools.json offers needs_human under se_work, decided by: build src/mcp, run the stub with --tools, and diff its output against the file, which comes back empty
- a test names the gap rather than a person finding it, decided by: a Go test in src/mcp that fails when the cold door and the lane differ on any tool

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

