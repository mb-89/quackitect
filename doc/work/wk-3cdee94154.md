---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: anchor the named exception
status: closed
author: worker-four
began:
  - e15c020af186dea8638952d5f3ce7ea54910cca3
ended:
  - 6ead4ac473ef5b6101d8d2f53dc35c5963c15dc8
disposition: dropped
reason: "The defect this token names is already fixed in the tree: runsTheEngine in src/engine/hook.go anchors on the program via firstWord and isTheEngine and refuses separators and substitutions, and TestTheEngineExceptionIsAnchoredToTheProgram in src/engine/gate_test.go already holds the asked negatives, echo se and the engine plus a second command. The test ran green through the engine door and cannot be made to go red, so there is nothing left for this token to change."
---

## detail

A guard carves out a named exception and matches it as a bare word anywhere in the input. An anchored form of the same test stands in the same file. runsTheEngine splits a shell command into fields and answers true if any field is se or se.exe, so echo se skips the write gate. runsTheEngineWith, twelve lines above, finds the program position first. Rewrite the exception in the narrowest terms that let the allowed case through, using the anchored matcher already there. Add negative tests: an input that mentions the exception without being it, and one that is the exception plus a second command, both refused. Found on wk-4e8eeb76aa.

## done when

- runsTheEngine matches se only in program position via the anchored matcher.
- Tests: echo se refused, se plus a second command refused, red then green.

## evidence: step 1. ask

answered on the token: the ask is small and decidable, and the basics line carries the finding that the fix and its tests already stand

## evidence: step 2. do

not reached: no change was needed. se test --on wk-3cdee94154 --propose TestTheEngineExceptionIsAnchoredToTheProgram answered ok in 0.097s

