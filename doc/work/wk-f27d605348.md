---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the list names wasbin
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-messiaen
claimed_at: "2026-09-05T16:44:20Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - be48264cb0b93b753a2e769d2481acae9d5d2a5c
---

## detail

A finding on wk-40abb881a7. Its approach section lists eleven groups the call graph found, one of them the bullet: wasbin 1, 81. src/engine/wasbin.go was deleted at d74aa58c and is absent from the tree at every began and every ended this token carries: git cat-file -e 28abf9f4:src/engine/wasbin.go, cd0d59ce and b07c52d5 all answer absent. So the measurement the whole split rests on counts a file that was not there when it was taken. The step 1 evidence then reconciles by substitution rather than by correction: it lists eleven names under internal as alive, console, frontmatter, quiet, replaced, runme, treelock, version, voice, yaml and expr, and says only log is still flat. replaced and treelock were never among the eleven the call graph named, and wasbin is named nowhere. ls src/engine/internal answers those eleven directories, so the count matches and the names do not. A reader taking up the next move looks for wasbin.go and finds a comment in vehicle.go:375 pointing at a file that is gone.

## proposed action

The approach section closed with its token, so the list is history and cannot be corrected in place. The correction goes where a reader still reaches it: the comment in vehicle.go, and the token for the one move left.

Drop wasbin, name replaced and treelock, and keep the counts beside each name so the next move can be picked by cost.

## done when

- no comment under src names src/engine/wasbin.go, a file that is gone: se find --regex "wasbin" --path "src/**" answers nothing
- the corrected list reaches the one move left: doc/work/wk-3fb658ea61.md names replaced and treelock and says what each is
- the section itself is not corrected, and this token says why: git cat-file -p on the blob archive.jsonl names for wk-40abb881a7

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one comment line in vehicle.go and one paragraph on wk-3fb658ea61 | git diff --stat |
| [x] | every done-when line is decidable, and names the command where one decides it | the two the token was minted with name a section that is archived, so all three are rewritten to what is still reachable | the done when section |
| [x] | the basics it stands on exist, or are minted first | the section this token aimed at does not. wk-40abb881a7 took its verdict at a4f6d2fc and was archived | ls doc/work/wk-40abb881a7.md |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | rule 11: the ask was impossible as written, so the token says where the correction went instead | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | red: se find over src answered one hit, vehicle.go:375, and ls src/engine/wasbin.go answered no such file | se find --regex "wasbin" --path "src/**" |
| [x] | the same test was seen green after the change, and named | the same find answers count 0, and go build over src/engine exits 0 | se find --regex "wasbin" --path "src/**" |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | one comment in vehicle.go, one paragraph on wk-3fb658ea61, and this note | the frontmatter |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | the archived list still reads wasbin and nothing can change that. The two live readers are corrected | doc/work/archive.jsonl |

