---
id: wk-a8da26e971
seq: 1000061
type: work
title: a manifest, not counts
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: cowork
---

## detail

The collection writes one file, manifest.jsonl, one JSONL line per thing. Each line carries the name in the retro folder, the origin, and the fate: taken, kept and why, or looked for and missing. It replaces index.md, whose counts are derivable from the manifest and whose instruction belongs to the retro method. Only src/engine/retro_test.go reads index.md. Today Collected carries Logs int and Scripts int, and nothing records which name came from the scratchpad and which from the log directory. citedInOpenWork stays as it is, and the manifest covers everything the retro did take.
