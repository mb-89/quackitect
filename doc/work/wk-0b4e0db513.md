---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: battery lists absent check
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-part
claimed_at: "2026-09-05T14:37:08Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0f08f1a4082061fcc1cfeef40d946370cf5d0fd8
---

## detail

A finding from the verdict on wk-0086ed9e9b, "adapter decides no column".

Commit dd2fed69 added two names to the for-loop at util/checks/battery.sh:393. One, adapter-decides-no-column, is that token's own check and lands with it. The other, a-refusal-names-a-legal-move, does not: git ls-tree HEAD -- util/checks/a-refusal-names-a-legal-move.mjs answers nothing, and git log -S on battery.sh names dd2fed69 as the only commit that put it in the list.

THE DAMAGE. The battery is red at HEAD for a check that is not in git, on two lines:

  a-refusal-names-a-legal-move FAIL it is not there, so it did not run
  checks-live-in-the-method FAIL a-refusal-names-a-legal-move is in util/checks

Both come off the same hunk, because checks-live-in-the-method reads the names out of battery.sh's own list. Neither could have printed before dd2fed69. So the next submission citing a green battery has two failures to explain that it did not cause.

Seen with: git archive HEAD | tar -x -C /tmp/t && cd /tmp/t && sh util/checks/battery.sh

THE FIX is either commit the check file under the token that wrote it, or take the name back out of battery.sh until that token lands it.

## done when

- util/checks/battery.sh's for-loop names no check git does not carry: for every name in the list, git ls-tree HEAD -- util/checks/<name>.mjs answers a blob
- sh util/checks/battery.sh over a clean archive of HEAD prints neither 'a-refusal-names-a-legal-move FAIL it is not there' nor the checks-live-in-the-method failure naming it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One name in one list, or one file into git. It wanted neither in the end. | one hunk of battery.sh |
| [x] | every done-when line is decidable, and names the command where one decides it | Both are, and both were run at two commits. The first reads the list out of battery.sh and asks git for each name. The second is the battery over an archive, read for the two lines. | `git ls-tree <c> -- util/checks/<name>.mjs`, and `git archive <c>` unpacked then `sh util/checks/battery.sh` |
| [x] | the basics it stands on exist, or are minted first | The battery, checks-live-in-the-method and the check file are all there. Nothing was minted for it. | `git ls-tree HEAD` answers a blob |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rule 12: a check that will not go red is the finding. | work-token |
| [x] | one test was written first and seen red for the reason expected | Reading battery.sh's list and asking git for each name says at dd2fed69 `MISSING a-refusal-names-a-legal-move`, `1 that git does not carry`. The battery over that archive printed both quoted lines. | exit 1 |
| [x] | the same test was seen green after the change, and named | The same reading at HEAD: `37 named, 0 that git does not carry`. The battery over an archive of HEAD prints `a-refusal-names-a-legal-move ok`. | exit 0 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Empty. fc27d9b1, `wk-885646032c began`, carried the file in before began 0f08f1a4. | `git log --diff-filter=A` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Its own, [[wk-874de0fedb]]: the check asks the disk, not git. | wk-874de0fedb |

