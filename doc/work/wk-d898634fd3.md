---
id: wk-d898634fd3
seq: "-36"
type: work
title: eight criteria splits
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 2
minted_by: cowork
submitted_by: main
reviewed_by: rev-33
evidence:
  - outcome
---

## detail

A draft carrying more criteria than the ceiling is refused at draft submission, and the refusal names the count, the ceiling and the remedy: split. The ceiling is the config key limits.criteria_ceiling, default eight, by owner ruling. Sub-tokens are the split mechanism. doc/guidance/reviewing.md carries the same rule with no digit or number-word. Related: wk-24be1c06ae.

## done when

- A ninth criterion refuses the draft at submission, naming the count, the ceiling and the word split, asserted on the gate's words.
  `rg -q func.TestANinthCriterionRefusesTheDraft src/engine && go test -C src/engine -count=1 -run TestANinthCriterionRefusesTheDraft$ .`
- The same fixture with exactly eight criteria reaches review unrefused.
  `rg -q func.TestEightCriteriaGoThrough src/engine && go test -C src/engine -count=1 -run TestEightCriteriaGoThrough$ .`
- With limits.criteria_ceiling set to three by SetValue, a draft of four is refused naming three and a draft of three goes through.
  `rg -q func.TestTheCeilingMovesWithTheConfig src/engine && go test -C src/engine -count=1 -run TestTheCeilingMovesWithTheConfig$ .`
- The transitional sentences and both ceiling literals leave reviewing.md, replaced by a numberless sentence, in one command failing on the first miss.
  `rg -q "When the gate is built it will" doc/guidance/reviewing.md && exit 1 || rg -q "Until then this rule binds the" doc/guidance/reviewing.md && exit 1 || rg -q "EIGHT CRITERIA IS THE CEILING" doc/guidance/reviewing.md && exit 1 || rg -q "more than eight criteria" doc/guidance/reviewing.md && exit 1 || rg -q "more criteria than the ceiling" doc/guidance/reviewing.md`
- Every test named above was watched failing on its own assertion with the gate absent, and the evidence names each test and what it said.

## evidence: outcome

The gate in src/engine/spec.go reads LoadConfig(r).CriteriaCeiling and refuses above it. TestANinthCriterionRefusesTheDraft, TestEightCriteriaGoThrough and TestTheCeilingMovesWithTheConfig in ceiling_test.go were watched red with the gate absent, at the ceiling, and against a literal 8. The reviewing.md command was watched red on each old fragment and on deleting the rule, swept by .se/scratchpad/ceiling-reds.py.
