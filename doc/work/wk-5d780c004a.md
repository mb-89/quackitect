---
id: wk-5d780c004a
seq: 1000164
type: work
title: the commonest red
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-10
---

## detail

A refusing gate classifies command failures from causes the drafter enumerated from their own examples, and the commonest correct case lands on the wrong side. Found on wk-41069b8234 at round 2: the crash causes and the fixtures are all rg or go. The commonest pre-work red in this tree is `node util/checks/<name>.mjs` on a check not yet written, which exits 1 with MODULE_NOT_FOUND. The detail calls that a crash and refuses it, and the classifier cannot see it, and no fixture drives it. Before agreeing a rule that sorts command failures, count the programs the tree's criteria invoke. Drive one fixture per program, correct case and wrong case. Write the sentence naming what the gate must never refuse before the sentence naming what it refuses.
