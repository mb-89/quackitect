---
id: wk-080da4232c
seq: "35"
type: work
title: sort takes many levels
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-5bec911840
rounds: "2"
minted_by: person
evidence:
  - outcome
---

## detail

Sort and Group by take more than one level, like v3. Today SetSort and SetGroup replace every level with one. The engine keeps a list, and a level is added, removed or rewritten by position. A heading press sets the first level rather than replacing the list. The editor draws a row per level with a way to add one and drop one, and an empty list still draws one blank row. The order of the levels is the order they sort in, as the file says. Checked by driving the page in .se/scratchpad/drive-editor.mjs: two levels added, one removed, and the message reaching the engine names both in order.

## evidence: outcome

The engine keeps a level list for sort and group, and a heading press sets the first level. The editor draws a row per level, with the level-add button selected as .bs-add[data-kind] so Hide all keeps its own handler. drive-editor.mjs adds two levels, removes one, presses Hide all and requires an empty columns message, with a window error listener asserting no handler threw. sh .se/scratchpad/battery.sh answers all ok with drive-editor at 14 messages sent and 0 failed.
