---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: swap says it swapped
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-berg-two
claimed_at: "2026-09-05T17:22:01Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - fc57a426db288ec184f5ba6b87267d7a90509317
---

## detail

Measured twice today, on this box. se --swap answered swapping true, build 6503d294.163715, from a4f6d2fc.161039, and said the calls in flight finish and it takes over. Forty-five seconds later .se/engine.json still named a4f6d2fc.161039, and the engine went on serving the old code. Nothing said the handover had failed. The same thing happened earlier today, cured only by stopping the engine and starting it again. THE DAMAGE. An agent that fixes a guard cannot put the fix in front of itself. I changed the tests guard so a git commit naming a check is not read as a test run, watched the test go green, and then could not commit the change, because the engine still refused it. The swap is the sanctioned door and it answered success. Landing needed a worktree outside the work root. Every engine fix on a busy box meets this. THE CHECK. A swap whose successor does not take over inside its own budget answers that it did not.

## done when

- a swap whose successor never takes over answers swapping false, decided by a test over the swap door with a successor that never writes engine.json
- the answer names how long it waited, decided by that same test reading the field

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One field and one wait loop in one file, plus one test. The stat says 60 added lines. No split was needed. | git diff HEAD --stat -- src/engine/swap.go |
| [x] | every done-when line is decidable, and names the command where one decides it | One Go test decides both. The first line reads said.Swapping, the second parses said.Waited as a duration. | RUNME.sh test --propose TestASwapThatNeverLandsSaysSo |
| [x] | the basics it stands on exist, or are minted first | The test needs to write engine.json and read it back. SayRunning and LoadRunning were both already here, so nothing was minted. | src/engine/swaplands_test.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove red before green. Rule 13 put both halves in the one test. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | afterTheAnswer was neutered to hand the promise straight back. TestASwapThatNeverLandsSaysSo failed with Swapping true and Waited empty. | /tmp/red-web2.txt, ok false |
| [x] | the same test was seen green after the change, and named | With the loop back, TestASwapThatNeverLandsSaysSo answered ok in 0.216s. The package under -run Swap answered ok in 1.758s. | /tmp/green-web2.txt |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Two files. swap.go gains Waited, two budgets, afterTheAnswer and its call site. swaplands_test.go is new. | git diff HEAD -- src/engine |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None revealed. One optional field, and askForASwap was the only caller. | git grep swapAnswer |

