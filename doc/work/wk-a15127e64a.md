---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: a coverage guard tautology
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/main
claimed_at: "2026-09-07T17:42:15Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - dca30a5c8a831c7ff7304ea59d9441aa588a9b53
---

## detail

A finding from the verdict on [[wk-eb0eeb7aa2]], which it blocks not at all. In src/engine/theguardreadsbash_test.go, the closing guard of TestTheGuardAgreesWithBash reads `if driven < len(wrappers)*len(payloads)-unparsed || driven < 30`. Every iteration increments exactly one of driven or unparsed, so driven always equals the product minus unparsed and the first clause can never fire. It reads as a coverage guard and holds nothing; only the literal 30 does any work. Replace it with a check that can fail, such as asserting driven+unparsed equals the product and that unparsed stays under a named ceiling, so a wrapper or payload silently dropped from the alphabet is caught.

## approach

Rewrite the closing assertion in TestTheGuardAgreesWithBash so each clause can fail: assert driven+unparsed equals len(wrappers)*len(payloads), and assert unparsed is at most the number of shapes bash genuinely cannot parse. Keep the run green.

## done when

- the tautological clause is gone and no assertion in TestTheGuardAgreesWithBash is unfalsifiable: se test --propose TestTheGuardAgreesWithBash

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

