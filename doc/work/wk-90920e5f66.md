---
id: wk-90920e5f66
seq: 1000010
type: work
title: the survivor moved out
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer8
---

## detail

A reviewer asked for something inside the swept folder that must survive the sweep. The answer moved the standing checks out of .se/scratchpad to util/checks and wrote a criterion about util/checks, which the retro cannot reach. Nothing inside the swept folder is named, so nothing constrains the sweep. Keep one criterion pointed inside the swept folder. Better, name a property: nothing another token's evidence cites is taken, and nothing a folder named for another actor holds is taken. Add a check that runs the retro over a fixture scratchpad holding a file some open token's evidence names, and requires it to survive. Related: wk-339068932c.
