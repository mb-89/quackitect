---
id: wk-6fd3999144
seq: 1000174
type: work
title: the unpinned payload
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-17
---

## detail

A detail that enumerates what a change says, with a done-when that pins only the head of the list, can close with the payload unwritten. Measured on wk-b5867b3760 as drafted: the detail names the leading question, three rules and two old sentences. The criteria pin the leading question and one old sentence. Fix that draft: add a criterion for the three rules and a delete criterion for the first old sentence. When writing criteria, number every item the detail says the change does and put a criterion against each number. On a rewrite, write a delete criterion for every sentence the detail names as the problem.
