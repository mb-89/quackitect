---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: measure one standing rule
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-ash
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 1de776ac035effdd80b445210a2a04990b0fc7ee
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - dae3bf76316d02c7bd5226c10df4bcee29175d33
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Whether a rule in the standing layer changes what an agent does is unmeasured. The cost half is answered by a counting script that is still there. The effect half has never been run.

The method: take one standing rule whose effect a check can see. Run the same piece of work with the rule and without it, in a separate tree, several runs each way. Count how often the check goes red under each.

One run either way is not evidence, and this refuses it. Two samples of a thing this noisy say nothing, and a number with no spread behind it will be quoted later as though it did.

Run it in a separate tree and mind the mint. A mint against a scratch folder has been reported landing in the real backlog. Check where a token lands before running anything that mints in bulk.

## proposed action

Name one standing rule and a check that can see its effect. Run the work with and without it, several runs each way, in a separate tree.

Write up the rule taken away, the piece of work, the check counted, and the counts each way.

## done when

- one standing rule is named, with a check that can see its effect
- the same work runs with and without that rule in a separate tree, several runs each way
- the write-up names the rule, the work, the check and the counts each way
- where a minted token lands is checked before the runs, so the real backlog is not polluted

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One rule, one check, ten runs. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | No line named a command. The counts are decided by the counting script, in the chapter the counts each way. Where a mint lands is decided by the refused mint, in the chapter of that name. |  |
| [x] | the basics it stands on exist, or are minted first | The standing layer is projected into the output style, and the voice limit sits in the rules file the engine reads. Both stand, so nothing was minted first. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 is answered below rather than ticked, because nothing here changes behaviour. |  |
| [ ] | one test was written first and seen red for the reason expected | Not met. This measures rather than changes, so no defect exists to go red. The instrument was validated instead, over voice.md. |  |
| [ ] | the same test was seen green after the change, and named | Not met, for the same reason. The counts are in the chapter the counts each way. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | One file, the counting script, and this write-up. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | It is wk-c0930c300e. The guard refuses every command naming the folder those scripts sit in. |  |

## evidence: the counts each way

The counting script over the ten outputs.

| run | with the rule | run | without it |
|---|---|---|---|
| A1 | 0 over of 30 | B1 | 7 over of 32 |
| A2 | 0 over of 29 | B2 | 6 over of 26 |
| A3 | 2 over of 37 | B3 | 9 over of 27 |
| A4 | 0 over of 22 | B4 | 5 over of 24 |
| A5 | 0 over of 34 | B5 | 10 over of 20 |

With the rule: 2 sentences over the limit in 152. One run of five goes red.

Without it: 37 over in 129. Five runs of five go red.

The spread does not overlap. The worst run with the rule breaks it twice, and the best run without it breaks it five times. So the rule changes what the agent writes, and the check can see it.

## evidence: the rule and the check

The rule is voice.md 1: one sentence, one idea, at most 25 words. It is in the standing layer, projected into the output style every agent carries on every turn.

The check is the counting script now in the tree beside the battery's checks. It reads the limit from the rules file the engine holds, rather than carrying a number of its own. It splits sentences the way the engine does, dropping headings and table rows.

Run over voice.md it reads no sentence over the limit. That is the instrument agreeing with prose the tree already holds to this rule.

## evidence: the runs

A separate tree under the temporary folder, outside the project.

Arm A carries the projected output style as it stands, 52 numbered rules. Arm B carries the same file with voice rule 1 taken out and the rest renumbered, 51 rules. Nothing else differs.

Each run is a fresh folder with its arm's style copied in. One headless agent gets one fixed prompt. It asks for prose, for a maintainer, on why a queue that stops at one unwritable item is worse than one that passes over it. Five runs each way, alternating, so the hour belongs to neither arm.

The agents confirm which arm they were in. The style asks each to say how many rules it read. Arm A said 52 five times. Arm B said 51 four times and once said nothing. Neither arm ever said the other's number.

## evidence: where a mint lands

Checked before the runs, because a mint against a scratch folder has landed in the real backlog before.

A mint aimed at the scratch tree was refused. No engine runs over that folder, so there was nothing to run work. Nothing was written anywhere. The tracked backlog held 67 notes before and 67 after, and the private one 38 before and after.

The hazard is a scratch folder inside the tree. The walk goes up to the nearest marker, so such a folder answers the project root instead. This tree sits under the temporary folder, which the walk does not climb out of.

The arms carry no engine and no marker of their own, so an agent in one cannot reach the record at all.

