---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: a flag hides removers
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-berg-two
claimed_at: "2026-09-05T17:03:37Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 1ca68d2de6de500fc80d50039214f31b1f5250e2
---

## detail

Found reviewing wk-2ef79848eb. src/engine/removal.go removerAt walks past a word only while runner() says so, and returns -1 at the first word that is neither a runner nor a remover. Every flag is such a word, so one flag between the runner and the remover turns the guard off. Verified live in this tree: `xargs rm unseen.go` is refused (the test asserts it), and `xargs -n1 rm src/engine/no-such-file-xyz.go` ran, exit 123, with no refusal at all. `xargs -I{} rm {}`, `sudo -u me rm f`, `git -C dir rm f` and `sh -c "rm f"` are the same class, and `xargs -I{}` is the ordinary spelling. The damage is that the guard's own named door is one flag from open, so nothing stops the incident it was written for. The walk should skip a flag while looking for the program, and sh/bash/powershell -c should have its argument read as a command.

## done when

- xargs -n1 rm and xargs -I{} rm on an unread file inside the tree are refused naming the file: go test -C src/engine -run TestARemovalNeedsARead
- sudo -u someone rm and git -C dir rm on an unread file are refused: the same test
- the allowed cases still pass: git status, git add, and a sentence carrying the word rm are not refused

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One walk, three done-when lines, one test. Read whole in one sitting. | the token |
| [x] | every done-when line is decidable, and names the command where one decides it | All three lines are decided by the command the token writes: go test -C src/engine -run TestARemovalNeedsARead. | ok true |
| [x] | the basics it stands on exist, or are minted first | remover, runner, pipeline and the read set all exist. Nothing had to be minted first. | se find, src/engine/removal.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 sent me to put the old walk back. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | The test was in the tree at take-up. With removerAt back to the runner-only walk it fails on xargs -n1 rm, allowed. | go test, FAIL |
| [x] | the same test was seen green after the change, and named | TestARemovalNeedsARead, run here through the RUNME door, ok true. | ok true |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | valueFlags and the flag walk are on origin/v4 and in began 1ca68d2d. | git show, count 1 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The shell case is in the change. sh and bash are runners, and -c is no value flag. | removal.go |

## evidence: step 2, how the red was taken

The flag walk and the test were already in the tree when I took the token up. A reviewer should know I did not type them.

So I earned the red. In a detached worktree at HEAD I put removerAt back to the walk the token describes. It returns -1 at the first word that is neither a runner nor a remover.

There the test failed on its own message. A removal run through another program was allowed, and it named xargs -n1 rm on the unread file. Nothing else was touched.

The same test is green in this tree, with the allowed cases beside it.

