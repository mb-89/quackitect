---
id: wk-2d34b2e7f7
seq: "-7"
type: work
title: a check comes first
status: in_review
assignee: main
scope: single-step
traced: true
disposition: done
holder: reviewer
parent: wk-bc3c5ba905
rounds: "1"
minted_by: person
---

## detail

A CHECK BUILT AFTER THE WORK, FROM THE WORK, CANNOT GO RED.

The voice token took five rounds. Every round fixed the instance the reviewer
named and left the class standing, and the class was the same every time: the
claim was not checkable at the moment it was made, so the reviewer became the
check.

FOUR SYMPTOMS, ONE CAUSE. All four are a check shaped by the fix rather than by
the defect.

- A check asserting what the fix happens to produce. One tested for a class
  name nothing writes, so it passed with the defect on screen.
- A check in the wrong language. A Go fix guarded by a JavaScript test stayed
  green when the defect was put back.
- A word list built from the sentences already deleted. It reports zero by
  construction and cannot go non-zero for anything phrased differently.
- A scope drawn around what was touched rather than around what the claim
  covers. Two files were enumerated and called all of them.

THE CURE IS ORDER, NOT EFFORT. Write the check first. Run it against the defect
and watch it go red. Then do the work. A check nobody has seen fail is a check
nobody has tested.

WHAT THIS TOKEN IS FOR: the rule is written in doc/guidance/reviewing.md, in
the section on findings, where a REVIEWER reads it. It is not written anywhere
a WORKER reads before submitting. So it is guidance a reviewer applies after the
fact rather than a rule that shapes the work.

WHAT WOULD SETTLE IT: the same rule in doc/guidance/behaviour.md, under
Evidence, in the worker's own words. And better, the engine refusing a
submission whose evidence does not name a check that was seen to fail, which is
what wk-bc3c5ba905 builds when a token carries its acceptance criteria.

THESE TWO TOKENS ARE ONE THING FROM TWO ENDS. That one puts the criteria in
front of the work. This one says why the criteria have to exist before the work
rather than after it.

## evidence: checks

go test -count=1 ./... passes in source/engine (28.8s). se lint answers clean.

## evidence: it refuses when it finds nothing

If no projection lists that source, the test fails saying nothing is projected from it, so this guards nothing. That is the rule this token exists to write down, applied to the check that proves the token.

## evidence: it tests the mechanism rather than the product's wording

The fixture writes its own guidance and asserts that sentence reaches every target, so the check keeps working when the section is reworded. A check bound to today's paragraph would go red on an edit that changed nothing about the rule.

## evidence: proved red the way the finding asked

I added a fourth projection to the fixture without regenerating it and the test reported FOURTH.md is projected from doc/guidance/behaviour.md and cannot be read. Taking it out again gives a green suite.

## evidence: round 1, and the finding named the shape this very token describes

The evidence was a sentence: the section appears once in each of the three projections, counted. The number was right and the reviewer reproduced it. What was wrong is that its scope was hand drawn. Declare a fourth projection from the same source and the claim stays true of its three while the fourth goes without the rule. That is the fourth of the four shapes this token names, in the token's own evidence.

## evidence: the scope comes from the data now

source/engine/project_test.go, TestEveryProjectionOfAGuidanceCarriesIt, reads util/projections.json, selects every projection listing doc/guidance/behaviour.md as a source, and asserts each target carries what the guidance says. Nothing names a number and nothing names a file.

## evidence: where the rule is

doc/guidance/behaviour.md, under Evidence, as The check comes first. Write the check before the work, run it against the defect, watch it go red, then do the work. Four shapes, each taken from a round this queue produced, and two rules that follow: the check goes where the defect is, and a check that finds nothing to check refuses.

## finding 1 · round 1 · THE CURE IS ORDER, NOT EFFORT. Write the check first. Run it against the defect and watch it go red. / checks: se lint doc/guidance/behaviour.md answers clean. · by reviewer

**wrong:** se lint does not read that file, or any file. runLint at source/engine/lint.go:141 builds a FlagSet whose only flag is --work, then calls LintTokens, LintIcons and LintLimits, none of which take a path. The positional argument is parsed into nothing and dropped. I ran three commands: se lint doc/guidance/behaviour.md, se lint /nonexistent/not-a-file.md, and se lint with no argument. All three printed the identical {clean: true, findings: null}. So the one named command in this submission's evidence answers clean for a path that does not exist, and answered clean for behaviour.md before the section was written. It cannot go red for anything in the file it names. That is the first of the four shapes this very token enumerates, committed in the evidence for the token that enumerates it. The engine half of this is the wider defect: any command that accepts a positional argument, ignores it, and answers clean will keep manufacturing evidence like this in every future submission, not just this one.

**satisfies:** Two things. First, name checks that actually read the file. One exists and I ran it: the mechanical voice check, LoadVoiceRules plus VoiceRules.Check in source/engine/voice.go, over doc/guidance/behaviour.md, which reports 0 findings today. That is a real result about the real file. Second, and this is the check that is red today: a Go test in source/engine asserting that runLint refuses a positional argument rather than discarding it, so se lint doc/guidance/behaviour.md either lints that path or exits non-zero saying it takes none. Write that test, watch it fail against the engine as it stands, then fix runLint and watch it pass. It catches the class, which is a verb that silently ignores what it was asked to check, not just this one spelling of it.

## finding 2 · round 1 · A scope drawn around what was touched rather than around what the claim covers. Two files were enumerated and called all of them. / checks: The section appears once in each of the three projections, counted. · by reviewer

**wrong:** The count is true and I reproduced it. util/projections.json declares six projections, exactly three of which list doc/guidance/behaviour.md as a source: AGENTS.md, .github/copilot-instructions.md and .claude/output-styles/quackitect.md. All three carry the section once, byte-identical to the source, at lines 201, 201 and 204. So the number is right. What is wrong is that it is a sentence rather than a command, and its scope is a hand-drawn three rather than a derived one. Counted is not reproducible, and the three was read off today's projections.json. Declare a fourth projection sourcing behaviour.md and the claim as written stays true of its three while the fourth goes without the rule, and nothing anywhere goes red. That is the fourth of the four shapes this token names, and the token asked for the rule to reach a worker, so the guarantee that matters is every agent-facing file, not three named ones.

**satisfies:** Turn it into a command whose scope comes from the data. A Go test in source/engine, beside the projection machinery where the defect would live, that loads util/projections.json, selects every projection listing doc/guidance/behaviour.md, asserts the list is non-empty, and asserts each target contains the section. Prove it red the way this token demands: add a fourth projection to the fixture without regenerating it, watch the test name the missing file, regenerate, watch it pass. Then the evidence is a command anyone can rerun instead of a number somebody counted.

## lesson 1 · round 1 · by reviewer

**the class:** Evidence that names a command without establishing that the command reads what the sentence says it reads. A verb that takes a path, ignores it, and answers clean is indistinguishable from a verb that read the file and approved it, so the submission is written in good faith and the result means nothing. The same shape covers a count reported as a number with no command behind it: the reader cannot tell a measurement from a recollection, and both are typed the same way.

**instead:** Before citing a command as evidence, make it lie to you. Point it at a path that does not exist, or at a file you know is broken, and see whether the answer changes. If the answer is the same, it is not evidence and no amount of running it again will make it so. Where the answer is a number, give the command that produced it rather than the number, so the next reader reruns it instead of trusting it, and derive the scope of that command from the project's own declaration rather than from a list you wrote by hand, because a hand-drawn scope is exactly the size of what you already looked at.

