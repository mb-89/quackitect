---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: schema denies the claim
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - d0011ff70bab2c7f3e690e034e216fbcd6520719
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - fe7bfc497ea0e0093edf552a6b5b89545657690c
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: The schema lint over doc/work went from 283 findings to 75, and none of them is about a claim.
---

## detail

A BASIC wk-c6e5db7c51 STANDS ON.

That token asks for every work token to be read against its schema. Measured first, as it asks: LintNotes over doc/work answers 283 findings, and 208 of them are two lines the engine itself writes.

src/schemas/work-token.schema.yaml sets additionalProperties false and declares neither claimed_by nor claimed_at. se claim writes both onto every token it takes. So 104 tokens each carry two fields their own schema refuses.

The schema says in a comment that the holder is not a field here, because a hold is the engine's and lives under .se. A claim is not a hold. It belongs to a box rather than an agent, and it travels on the branch, which is the whole reason it is written on the token.

Until this is declared, turning the schema lint on adds 208 findings that no token author caused.

## done when

- LintNotes over doc/work names neither claimed_by nor claimed_at, decided by a test that counts findings carrying those words over the real folder
- a token carrying a claim reads clean against the schema, decided by a test over one such token
- the schema says why a claim is a field and a hold is not, decided by a reader

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The schema lint over the tokens becomes readable. Three findings in four were the engine's own writing. | 283 to 75 |
| [x] | what breaks if it is never done, and not only that it stays undone | wk-c6e5db7c51 cannot land. Turning the lint on adds 208 findings no token author caused, and buries the 75 that are real. | 208 noise |
| [x] | the ask is small enough to review whole, or it is split first | Two field names, in a schema and in two process files. | 3 files |
| [x] | every done-when line is decidable, and names the command where one decides it | One test over a claimed token, one over the schema's own words, and the count over the real folder. | 3 of 3 |
| [x] | the basics it stands on exist, or are minted first | LintNotes and the process narrowing were both already here. | 2 of 2 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Measured before changing anything, as the parent token asks, and measured again after. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | Both tests red: a claimed token refused by its own schema on both fields, and the schema carrying none of the three words. | 5 red |
| [x] | the same test was seen green after the change, and named | TestAClaimOnATokenIsNotAFinding and TestTheSchemaSaysWhyAClaimIsAField pass, with every schema, process, lint, mint and claim test. | 2 of 2 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Two properties in the schema, two field names in each process, and one test file. | f79621e8 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change. Declaring it in the schema alone left it refused, because the process narrows the schema and lists its own fields. | 3 files |

