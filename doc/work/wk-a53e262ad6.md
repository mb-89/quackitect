---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: schema and store arguments
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-aspen
claimed_at: "2026-09-05T20:47:06Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - c8763901c1f82648aafb1682df077b265afe3ddb
---

## detail

From wk-3a46df35b7, whose detail names three files and whose done-when lines covered only one.

src/engine/schema.go opens 6 paragraph-long capitalised arguments and src/engine/store.go opens 18. Neither names a rationale by link, so TestARationaleIsNamedNotRepeated never looks at either file and nothing goes red for them.

The first extraction is doc/rationale/no-token-no-writing.md, which explains src/engine/gate.go. It is the shape to follow: the argument moves whole, the comment keeps the instruction, and the note is named by link.

gate.go's remaining arguments are wk-2d6e10c446. This token is the other two files.

## proposed action

Take schema.go first, since it carries 6. Write a rationale per decision under doc/rationale, listing the file under explains, move each argument into it, and leave the instruction naming the note. Then store.go the same way, and split it off if 18 arguments prove to be more than one token.

## done when

- src/engine/schema.go and src/engine/store.go each name a rationale by link and carry none of its sentences, decided by: go test -C src/engine -run TestARationaleIsNamedNotRepeated ./... answers ok with both files among those it drives
- the instruction left in each touched comment still says what to do, decided by: a reader of the comment can act on it without opening the note
- the engine still builds, decided by: go build -C src/engine ./... answers nothing

## evidence: note

schema.go opened five capitalised arguments at origin's tip, one fewer than the detail counted, and already named two rationales. The five are now four notes under doc/rationale: a-required-field-is-checked-by-presence, a-chapter-opened-twice-is-a-departure, which also covers the one-rule-one-chapter walk, a-section-may-name-a-prefix and a-finding-is-named-by-its-path. Each comment they came out of keeps what to do and names its note by link. Landed as 07516f58 on origin/v4. store.go already names five rationales and opens 25 more arguments in eight clusters, so it is wk-86bc02c843, as the proposed action allowed. TestARationaleIsNamedNotRepeated is red on the tip for ten notes nobody linked, none of them about these two files. That is wk-2b1d846eb8.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one file of comments and four notes. store.go is split off as wk-86bc02c843 |  |
| [x] | every done-when line is decidable, and names the command where one decides it | one Go test, one reading of the comments left behind, and go build. The test cannot answer ok on the tip for reasons outside this token, wk-2b1d846eb8, so it was read per subtest |  |
| [x] | the basics it stands on exist, or are minted first | the rationale kind, its schema, its folder and its test all ship. The exemplar no-token-no-writing is now named a-write-names-its-token |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read. Rule 11 kept store.go out, rule 12 put the red first, rule 15 answered each line |  |
| [x] | one test was written first and seen red for the reason expected | the four notes landed first, schema.go untouched. TestARationaleIsNamedNotRepeated then failed on four notes schema.go did not name and three sentences it carried |  |
| [x] | the same test was seen green after the change, and named | TestARationaleIsNamedNotRepeated answers PASS on all six subtests naming schema.go at 07516f58. go build, go vet and gofmt answer nothing, and go test -run 'Schema\|Note\|Lint' answers ok |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/engine/schema.go and four notes under doc/rationale, commit 07516f58 on origin/v4 |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | store.go's arguments are wk-86bc02c843. The ten unlinked rationales are wk-2b1d846eb8 |  |

