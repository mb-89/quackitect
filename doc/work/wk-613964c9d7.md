---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: a person edits queries
status: closed
disposition: done
---

## detail

The person's words: the query should be just a file on disk, and I should be able to edit it. util/views/work.base is a file in the method root, but nothing tells a person where it is and nothing reloads when they save it. A person needs the path named where they look, the pane redrawn when the file changes, and a bad edit answered with what is wrong. The editor already watches util/parameters.json and rebuilds on a save.

## done when

- the editor names the path to util/views/work.base
- saving it redraws the panes, like util/parameters.json
- a bad edit says what is wrong, view kept

