---
id: wk-b42c0e9a53
seq: 1000059
type: work
title: the retro writes checks
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: cowork
---

## detail

A write into util/checks/ is refused as a destination, and the refusal names .se/scratchpad/ as where it goes instead. The retro is the one moment the guard opens. It judges each scratchpad script, promotes what earns its place into util/checks/ in any form, then clears the rest. This corrects wk-964cd3a1d4, which moved the standing checks out of the scratchpad to survive a blind sweep. One-time correction: the whole util/checks/ folder goes back into .se/scratchpad/ with `se move` rewriting references, and the first retro judges it script by script. util/checks/trycmd has no extension and se move leaves such files unrewritten. Do this as the first act of the first retro, not while open tokens carry criteria naming util/checks/ paths. Decide whether a retro must refuse while any work is open rather than only while somebody holds a token, since escape leaves tokens open.
