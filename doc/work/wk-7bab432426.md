---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: Search guard breaks promise
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-ravel-two
claimed_at: "2026-09-05T19:05:46Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 9a63929d50ecd2815ed6d6ca0e265ef985da7c98
---

## detail

THE SEARCH GUARD REFUSES A SEARCH OUTSIDE THE TREE, AND ITS OWN REFUSAL SAYS IT WILL NOT.

Its last line reads: OUTSIDE THIS TREE THE DISK IS YOURS: a search naming a path outside it is not refused. Measured here on 2026-09-05, through se run: a call naming rg, a pattern and the path /root/.ccr/README.md was refused with exactly that message. The path is outside the tree.

So the guard reads the tool name and refuses, without asking where the paths point. A reader who does what the refusal tells them gets the same refusal again, which is the worst shape a guard can have: it teaches nothing and there is no next move.

It cost a criterion here. wk-5b5ede3926 asks that a command naming rg run through the engine and resolve, and the only way left to answer it was command -v rg, which proves the PATH and not the run.

The guard is right about the tree, and that half should not move. A search over the tree goes through the index, and se find is the door.

## proposed action

Ask where the paths point before refusing. A search whose paths all lie outside the work root goes through. One naming a path inside it, or naming none, is refused as now and still names se find. The refusal's last line then stops being a promise the guard breaks.

## done when

- a search naming a path outside the tree runs through the run verb, decided by: rg with a pattern and a path under /root or /tmp answers rather than being refused
- a search naming a path inside the tree is still refused and still names se find, decided by: the same call with a path under src/ answers with the index refusal
- both cases are a test in src/engine, seen red first on the outside case
- the battery reports no new failure against the run before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One guard, one question. The change is one function and one table of cases in src/engine, reviewable in a single diff. | — |
| [x] | every done-when line is decidable, and names the command where one decides it | All four are decided by go test in src/engine. The first three by TestASearchOverTheTreeGoesThroughTheIndex and TestASearchOutsideTheTreeIsLeftAlone. The fourth by go test ./..., compared with the same run before the change. | — |
| [x] | the basics it stands on exist, or are minted first | It stands on ASearchOverTheTree, pathsAmong and anyInside, all already in src/engine/search.go, and on answerHook, which the table test drives. Nothing was missing. | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove the red first, and rule 11 sent the pipeline defect to its own token. | — |
| [x] | one test was written first and seen red for the reason expected | Three cases went into TestASearchOverTheTreeGoesThroughTheIndex before any change. Two went red, refused=true where false was wanted. | — |
| [x] | the same test was seen green after the change, and named | TestASearchOverTheTreeGoesThroughTheIndex passes, all seventeen subtests. TestASearchOutsideTheTreeIsLeftAlone still passes. Run in a worktree at HEAD. | — |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | The began hash 9a63929d50 is not an object here, so git answers bad object. The change is search.go and searchguard_test.go. | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The word split reads quotes now. pipeline still does not, so a separator inside a quoted pattern cuts the command. Minted as wk-8c76f768c1. | — |

