---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: claim gate reddens ghosts
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-webern-two
claimed_at: "2026-09-05T21:35:17Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - c59766b8b4be6453ece00ae89ea44e968ee21361
---

## detail

Three tests in src/engine/goneputsdown_test.go are red on the claim gate. TestAnAgentThatGoesPutsDownItsWork, TestATurnsEndPutsItsHelpersWorkDown and TestAHelperCannotStopHoldingOpenWork each fail at aGhost, where TakeUp answers that the token travels and this box holds no claim on it.\n\nThe fixture does claim. mintStandard in standard_test.go calls Claim with Claimant of main and fails the test if that errors. So NoClaimHere reads an empty ClaimedBy on a token the fixture just claimed.\n\nThose three tests decide criteria 1, 2 and 4 of wk-1c9dc4ef28, so that token cannot be reported either way while they are red. The claim gate is wk-239cae9216, held and in flight, so this is written down rather than worked on from a second pair of hands.

## proposed action

Whoever holds wk-239cae9216 decides it. Either the gate exempts a fixture the same run claimed, or mintStandard claims in a way the gate reads. Then run those three tests.

## done when

- the three named tests in goneputsdown_test.go pass: go test -C src/engine -run 'TestAnAgentThatGoesPutsDownItsWork|TestATurnsEndPutsItsHelpersWorkDown|TestAHelperCannotStopHoldingOpenWork' answers ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | Three tests decide criteria on wk-1c9dc4ef28, and it can be reported either way once they answer. | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | That token stays unreportable, because the tests deciding it say nothing about its change. | the detail |
| [x] | the ask is small enough to review whole, or it is split first | No change was needed. The gate this waited on has landed. | git diff HEAD --stat |
| [x] | every done-when line is decidable, and names the command where one decides it | One go test run over the three named tests. | go test -run the three |
| [x] | the basics it stands on exist, or are minted first | The gate is wk-239cae9216. It closed done and is in the archive, so it is no longer in flight. | doc/work/archive.jsonl |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. This token asks for no test of its own: it records three that were red and waits on the gate. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | None was written, and none was seen red here. The red is the one this token was minted to record, measured by the hand that wrote it. | the detail |
| [x] | the same test was seen green after the change, and named | All three answered ok in 5.0s: TestAnAgentThatGoesPutsDownItsWork, TestATurnsEndPutsItsHelpersWorkDown and TestAHelperCannotStopHoldingOpenWork. | /tmp/enggate |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Nothing but this note. The cure is in the claim gate's own change. | git diff HEAD |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None. The run was on a copy outside the tree, because another lane's file stops src/engine building. | /tmp/enggate |

