---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: claim gate reddens ghosts
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-linden
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 7c55f89ac259d88f6e0059bb1b2efef1f839137a
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - fbe909679d2bf5b7c72f4f83d53419f82d3716cd
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Three tests in src/engine/goneputsdown_test.go are red on the claim gate. TestAnAgentThatGoesPutsDownItsWork, TestATurnsEndPutsItsHelpersWorkDown and TestAHelperCannotStopHoldingOpenWork each fail at aGhost, where TakeUp answers that the token travels and this box holds no claim on it.\n\nThe fixture does claim. mintStandard in standard_test.go calls Claim with Claimant of main and fails the test if that errors. So NoClaimHere reads an empty ClaimedBy on a token the fixture just claimed.\n\nThose three tests decide criteria 1, 2 and 4 of wk-1c9dc4ef28, so that token cannot be reported either way while they are red. The claim gate is wk-239cae9216, held and in flight, so this is written down rather than worked on from a second pair of hands.

## proposed action

Whoever holds wk-239cae9216 decides it. Either the gate exempts a fixture the same run claimed, or mintStandard claims in a way the gate reads. Then run those three tests.

## done when

- the three named tests in goneputsdown_test.go pass: go test -C src/engine -run 'TestAnAgentThatGoesPutsDownItsWork|TestATurnsEndPutsItsHelpersWorkDown|TestAHelperCannotStopHoldingOpenWork' answers ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [x] | the ask is small enough to review whole, or it is split first | One criterion, and it is a test run. Nothing was changed to meet it. | 1 criterion |
| [x] | every done-when line is decidable, and names the command where one decides it | The criterion names its command. It was run as three runs, one test each, because the shell reads the pipe in that pattern as a second command. | 3 runs |
| [x] | the basics it stands on exist, or are minted first | The claim gate this waited on, wk-239cae9216, is archived done, so what it stands on is in the tree. | the gate |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token read. Rule 14 is why this reports the runs rather than a reading of them. | work-token |
| [x] | one test was written first and seen red for the reason expected | No test was written. The three named tests carry the red this was minted from, and one passes even at the snapshot taken when this was taken up. | no red |
| [x] | the same test was seen green after the change, and named | All three pass on origin/v4 at 89850363, run one at a time and verbose: RUN then PASS, exit 0 each. | 3 PASS |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | No diff is this token's. The gate work met the criterion before this was taken up. | no diff |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Nothing changed, so nothing was revealed. | none |

