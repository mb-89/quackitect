---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: shortfall tests contradict source
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-06T19:59:34Z"
# true when this waits for a person rather than an agent
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 6e4da6945cdad6019c47d1dcf6279a479b3f8478
---

## detail

Three test files assert the staffing shortfall holds every working tool. The source holds the pull and nothing else, on the owner's own words. Both cannot be right, and the tests have been red since the source changed.

WHAT THE SOURCE SAYS. src/engine/staffing.go:204 is headed "SO IT HOLDS THE PULL AND NOTHING ELSE", and quotes the owner: even if you are bound, I do not want the agent to have to spawn subagents just because I want to talk to him. heldDuringShortfall at line 221 carries se_pull and Bash, and Bash is narrowed at line 239 to a command that is itself a pull.

WHAT THE TESTS ASSERT. src/engine/nolegalmove_test.go:48 fails where Bash, Write, Edit, se_apply, se_run and se_test each go through a shortfall. src/engine/pulledisahand_test.go:114 and src/engine/staffinghands_test.go:68 both call AStaffShortfall with se_apply and expect a refusal. Under the current source se_apply is never held, so all three are red on every run.

A SECOND DISAGREEMENT INSIDE THE SAME TEST. nolegalmove_test.go:57 also fails because the shortfall refused a shell pull naming worker-one. The guard reads the calling actor as main and holds any pull command, so the main agent cannot run a spawned worker's pull at a shell. That is the escape the refusal's own last line names.

MEASURED in September 2026 on a cloud box, twice, with identical output both times. These are not flakes.

## proposed action

A person says which design stands, because the answer is a ruling and not a reading.

MY BEST ATTEMPT, and why. The source is right and the tests are stale. Its comment records a deadlock the old design caused: a list of what was allowed refused se_pull, Read and the stop, so an agent could neither work, nor look, nor pull, nor stop. That reasoning is written down and dated, and nothing in the tests answers it.

So the three files are rewritten to assert the current rule: the pull is held, everything else goes through. Each keeps its own subject.

THE SHELL PULL IS A REAL HOLE EITHER WAY. A pull naming another actor is the move the refusal asks for, and the guard refuses it. That is worth its own token whichever design stands.

## done when

- a person has said which design stands, and the token records the answer. ANSWERED: the source stands. Their words are quoted in staffing.go: a bound agent should not have to spawn subagents just to be spoken to. In September 2026 they told this box to settle what it could settle itself
- the three named test files agree with the source, and all three pass: se test naming TestTheCageHasNoStateWithNoLegalMove, TestAPulledAgentIsAHand and TestAHandThatWentHomeIsNotAHand answers ok
- the shell pull naming another actor is a token of its own, and this one names it: wk-725b3914e7

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | Three files stop asserting a rule the engine dropped, so a red in them means something again. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | Three tests red on every run, measured twice with identical output. A suite with standing reds is one nobody reads. |  |
| [x] | the ask is small enough to review whole, or it is split first | Two lines and one block. The hole it revealed is split off as wk-725b3914e7. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | By se test on this token, naming the three. |  |
| [x] | the basics it stands on exist, or are minted first | The guard and its three tests all existed. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token, read. The tests were carried to the mechanism rather than the mechanism to the tests. |  |
| [x] | one test was written first and seen red for the reason expected | Not a new test. The three were already red, each on the assertion the source had dropped, and that is the reading. |  |
| [x] | the same test was seen green after the change, and named | se test on this token: TestTheCageHasNoStateWithNoLegalMove, TestAPulledAgentIsAHand and TestAHandThatWentHomeIsNotAHand green, with two others. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | nolegalmove_test.go, pulledisahand_test.go, staffinghands_test.go. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | A token of its own, wk-725b3914e7. A shell pull naming another hand is held, and that is the move the refusal asks for. |  |

