---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: publish test reads notes
# where the token stands. The process owns these values.
status: open
---

## detail

TestPublishKeepsEarlierClaimsWhenThePushKeepsFailing is red on origin/v4, measured at 7395c3b1. It was written when the claims ref carried one note per claim, and the ref now carries a single claims file with a line per live claim. The test still asks the ref for note paths, so it fails on a shape that changed under it rather than on the behaviour it was written to hold. That behaviour is still worth holding: a push refused three times must not eat the claims made before it. The question the test asks does not change, only where it reads the answer.

## proposed action

Rewrite the assertions against the claims file. Ask which ids the file names rather than which paths the tree holds, on the local ref and on the remote, the way publishkeepsearlier_test.go was already rewritten once for the same reason.

## done when

- TestPublishKeepsEarlierClaimsWhenThePushKeepsFailing passes on a worktree of origin, decided by: go test -run TestPublishKeepsEarlierClaimsWhenThePushKeepsFailing in src/engine
- the test still goes red when the recovery path throws an earlier claim away, decided by: the same run with the recovery move restored answers FAIL

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

