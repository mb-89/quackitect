---
id: wk-967b04b877
seq: "63"
type: work
title: criteria about live data
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: reviewer4
---

## detail

A criterion written as a standing test over the project's own live data, rather than over the change. wk-61af3a054e's criteria assert facts about doc/work as it stands: every backlogged token names a decision, no token is left where nobody can act on it. A one-time clearing then becomes a permanent rule, so the check goes red the next time anybody mints a backlogged token, and it silently redefines what backlogged means. se work --backlog says backlogged is visible, and not work anybody is doing; the criterion makes it waiting on a decision.

MEASURED: 10 backlogged tokens today, 0 carrying a WAITS ON line, and 5 of the 10 were minted today as lesson tokens by a reviewer, each with a named remedy and a red check. None of them waits on a decision.

WHAT TO DO: when a criterion is about the state of the project rather than about the change, say whether it is a one-time assertion or a standing rule. If it is one-time, pin it to the tokens that existed when the work started, by id or by a snapshot. If it is a standing rule, say what it means for every future token of that kind and check it against the ones that exist today before agreeing it.

Found on wk-61af3a054e.

