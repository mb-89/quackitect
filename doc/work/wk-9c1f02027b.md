---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: battery takes a context
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-holly
claimed_by: 547b9365/worker-holly
claimed_at: "2026-09-05T19:50:40Z"
# the token this is a part of. It cannot close while this is open
parent: [[wk-697f9876cf]]
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 983634eab0c7319066657363f484bfcee80402bb
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - bc04665e01b98a863315ebc2487e76d801a3f6ee
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: startBattery takes a context first and starts nothing when it has already ended, saying so and writing no marker. The started battery is not killed by it, and the comment says why. runTest passes c.ctx and TestTheDelta hands it down. Landed on origin/v4 as 735ebc83ab86be1b568d8c8c28d58efff5cb13d5. The change was made on a worktree of origin/v4 rather than in this clone, which is 339 commits behind and has no call.ctx to pass; the note says so under where it was done.
---

## detail

startBattery SPAWNS AND TAKES NO CONTEXT.

wk-697f9876cf asks that every spawn take the context that owns it. startBattery is the last one, at src/engine/battery.go. It starts the whole battery detached and answers where the output will be.

Its caller is TestTheDelta in src/engine/tests.go, which is a verb handler and has no context of its own, so the parameter cannot be added without giving the verb layer one. Four tests call it as well.

WHAT THE CONTEXT MEANS HERE IS NOT WHAT IT MEANS FOR A SERVER. The battery is started detached on purpose: it builds the engine and puts a new one over this tree, so waiting for it inside the process it replaces is waiting for itself. A context that killed the child would undo that. The context decides whether to start one, the way the handover's does, and it does not follow the child.

That distinction is why this is its own token rather than a line in a wider one.

## proposed action

startBattery takes a context as its first parameter and starts nothing when it has already ended, answering the cancellation. The verb layer passes the one it is given. The started battery is not killed by it, and the comment says so.

## done when

- startBattery takes a context as its first parameter, decided by: se find --regex 'func startBattery' answers a signature naming context.Context first
- a cancelled context starts no battery, decided by: a test in src/engine cancels and sees startBattery answer the cancellation with no run marker written

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Two production files and one new test. The rest is the call sites the signature moves, one argument each. | commit 735ebc83 |
| [x] | every done-when line is decidable, and names the command where one decides it | Line 1 is read off the signature. Line 2 is TestACancelledContextStartsNoBattery, which asserts the answer and the absent marker. Both by go test on v4. | batterycancel_test.go |
| [x] | the basics it stands on exist, or are minted first | The context already existed on origin/v4: call carries ctx and runVerbInside takes one. Nothing had to be minted. | verbs.go, client.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rules 11 and 12. The guard decides whether to start, and does not follow the child. | battery.go |
| [x] | one test was written first and seen red for the reason expected | Written first. Red: too many arguments in call to startBattery. A spawn with no context to refuse it. | the red run |
| [x] | the same test was seen green after the change, and named | Both green, with 21 neighbours. Same seven suite failures before and after. | go test, v4 worktree |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Not in this tree. This clone is 339 commits behind v4 and has no call.ctx, so it cannot be made here. It landed as 735ebc83. | where it was done |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None. The seven failures predate it. | 735ebc83 |

## evidence: where it was done

The engine snapshots this tree, and the work did not happen in this tree. That gap is deliberate and worth writing down.

This clone sits 339 commits behind origin/v4. In src/engine alone, 171 files differ. The token asks the verb layer to pass the context it is given. On origin/v4 that layer already has one: call carries ctx, and runVerbInside takes one. Here neither exists.

A change built here would have had to invent that thread first. It would not be the change that lands, and its diff would describe work nobody asked for. So it was made on a fresh worktree of origin/v4, checked there, and pushed.

v4 moved once mid-flight, from a6d8bc30 to 42a14fca. None of those commits touched the fourteen files. The patch was re-applied on the new tip and rechecked before the push, which landed as 735ebc83.

The seven suite failures are the same on an untouched worktree of that tip. Each one is a tree that is no method root, so they are the checkout and not the change.

