---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: rule four length unjustified
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: reviewer-rowan
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 9815ef7b4691b49bb685a524845906cd4b3d5f52
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - bb2e28e36772fcb010d3faa49cccc7ff3ab0bd89
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

doc/guidance/work-token.md rule 4 reads "Match on what the check holds at run time: a whole identifier written once into both halves, or a length as a number." and carries an asterisk. Its chapter, "## 4. What a check may match on", explains only the identifier half and the rendered-text handle. The incident that justified the length clause stood at commit edd983fa1bae0d1f12fede3c76664668ad854cc2, in the chapter "A criterion that cannot fail": a boundary drawn in something the check did not hold when it ran, so the match fell to a floorless prefix that could not fail. wk-c93aac62be cut that sentence to bring the Discussion under its word cap and kept the clause, so half an asterisked rule has no chapter and a reader cannot tell what "a length as a number" guards against. Restore one sentence to chapter 4 covering the length clause, or drop the clause from rule 4. From the verdict on wk-c93aac62be.

## done when

- chapter 4 of work-token.md covers the length clause, or rule 4 no longer carries it: se find --regex 'length' --path doc/guidance/work-token.md
- .bin/se.exe lint stays clean, so the Discussion is under its word cap: .bin/se.exe lint

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One sentence into one chapter of one markdown file, reviewable whole as a diff of doc/guidance/work-token.md. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Both are. Criterion 1 is se find --regex 'length' --path doc/guidance/work-token.md, read as two hits rather than one: the rule and a chapter sentence. Criterion 2 is .bin/se.exe lint, the only thing that decides the 1000-word cap on Discussion. |  |
| [x] | the basics it stands on exist, or are minted first | The file, the schema's maxWords of 1000 on Discussion, and the lint that enforces it all exist. Nothing had to be minted. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md read whole; it is also the file changed. Only the lesson the token names was added. |  |
| [x] | one test was written first and seen red for the reason expected | se find --regex 'length' over work-token.md answered 1 hit, the rule at line 28. No chapter carried the word. |  |
| [x] | the same test was seen green after the change, and named | The same se find now answers 2 hits, line 28 and line 82 in chapter 4. .bin/se.exe lint is clean. se test ran 17 go tests, all ok. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | One file. Chapter 4 gains the length sentence, paid for by tightening six sentences elsewhere. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Nothing new minted. The incidents later lanes cut from chapters 3, 5 and 12 have wk-dd7e2ca1a6. |  |

