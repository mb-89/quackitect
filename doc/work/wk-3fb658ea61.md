---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: log moves under internal
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/worker-birch
claimed_at: "2026-09-07T10:31:16Z"
# tokens that have to close before this can start
depends_on:
  - "[[wk-40abb881a7]]"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - f2af8f44ecc69b951c5bc5bb077b3e180e09adfa
---

## detail

src/engine/log.go is the last of the eleven groups wk-40abb881a7 measured that is still flat in package main. It is 251 lines and reaches nothing outside itself, so it is a package by the same test as the ten already under src/engine/internal.

It was left out of the expr move on wk-40abb881a7 because of who holds its callers. The names it puts in package main are Log, Record, OpenLog, ContinueLog, OpenExistingLog, RetireCurrent, Current, Yes and No, and they are read from 84 files. Three are claim.go, claimsync.go and claimverb.go, which another hand holds. A move that renames a name they read edits their files under them.

MEASURED, on 62c62a65: a word-boundary count of those nine names over src/engine, log.go and log_test.go aside, answers 84 files and 328 uses.

The name logbook is already spoken for by wk-0f91286ac1, about an import naming a package no commit carries, so this token says which name it takes.

THE MEASURED LIST IS ONE NAME OUT OF DATE, and wk-40abb881a7 is archived, so it is corrected here. Its bullet wasbin is the group now called replaced: d74aa58c renamed src/engine/wasbin.go to internal/replaced/replaced.go. treelock is under internal and was never one of the eleven, because it was born there at dad9ddbf.

## proposed action

Wait until the claims work is put down, then move src/engine/log.go to a package under src/engine/internal named for what it holds, and qualify its callers.

Drive the caller edits from the compiler rather than from a search: build with -gcflags=-e, take each undefined name at its own line and column, and qualify it there. That is how the expr move was done on wk-40abb881a7.

## approach

There is nothing left to move. wk-c5b92df740 did it, on a box this clone cannot see. Commit 37700f7 is not in this repository, so the citation is corrected to merge 6dfb7ea, which is what carries the move here. It added src/engine/internal/sessionlog, 251 lines beside a 106 line test, and no commit this clone holds ever carried src/engine/log.go.

Read against the tip, this token's four criteria already answer. log.go is gone and its package is under internal. The name taken is sessionlog rather than logbook, so wk-0f91286ac1 is untouched.

So the shape here is a reading rather than a change. Whoever takes it runs the four commands and names what each answered. Then it closes as work another hand did, rather than as a move done twice.

One thing could still be outstanding. A caller reading any of the nine names unqualified would be a compile error rather than a search, so the build decides it.

## done when

- src/engine/log.go is gone and its package is under src/engine/internal: ls src/engine/internal and ls src/engine/log.go
- no import cycle is introduced: go build -C src/engine ./... and go vet -C src/engine ./... both answer nothing
- the engine tests pass after the move: se test --on this token answers ok true
- the package name is not logbook while wk-0f91286ac1 is open: se find --regex "internal/logbook" answers nothing

## note

WHAT CHANGED. Nothing in the engine. The move this token asks for is already in the tree, made on another box under wk-c5b92df740 and carried here by merge 6dfb7ea. This hand ran the four criteria and corrected the approach's commit citation.

WHAT EACH CRITERION ANSWERED. ls src/engine/log.go answers no such file. ls src/engine/internal lists sessionlog among fifteen packages. go build and go vet over src/engine both exit zero and print nothing. The three sessionlog tests run green. se find over internal/logbook finds no import.

WHAT THE NAMES BECAME. OpenLog, ContinueLog and OpenExistingLog are sessionlog.Open, Continue and OpenExisting. The other six kept their names, and 356 qualified uses stand across the engine.

A CRITERION IS AWKWARD. se test on this token reaches no Go test, because the delta is one markdown file, so the three were proposed by name.

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The flat package sheds 251 lines and the log gets its own boundary. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | package main keeps nine names hundreds of call sites read, and the flat engine stops shrinking. |  |
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | It is, written by the hand before this one. This hand corrected its citation: 37700f7 is in no history here. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Two name ls, one go build and go vet, one se test, one se find. All were run. |  |
| [x] | the change is small enough to review whole, or it is split first | — |  |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [x] | the change follows the approach on the token, or the token says why it departed | It follows. The approach called for a reading, and the only edit is this token's own file. |  |
| [x] | se test --on this token answered ok, and what it ran is named | ok true with nothing unreached, over TestTheFileRotatesAndTheSessionDoesNot, TestAnEarlierSessionIsSetAsideNotOverwritten and TestRotatingWithoutAnEngineKeepsWhatWasThere. Nothing could be watched going red, because the move was in the tree before take-up. |  |
| [x] | the note says what changed and why, for a reader who was not here | The note names who made the move, what each criterion answered, and what the nine names became. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None to mint. The one finding is a criterion wording, and the note carries it. |  |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | [[reviewing]] was read and applied | — |  |
| [ ] | every hunk of git diff began..ended was read, and any not read is named |  |  |
| [ ] | every criterion's command was run again, and what it said is named |  |  |
| [ ] | every hunk improves the product, or a finding names the one that does not |  |  |
| [ ] | every finding is a trivial token naming this one, and their ids are here |  |  |

