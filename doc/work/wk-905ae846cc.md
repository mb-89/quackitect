---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: go-cmp is a dependency
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-webern-two
claimed_at: "2026-09-05T21:03:00Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 2e8a1201642980356a0bfbdb46b588f4abd2402b
  - 176de0b4c63f1581a4ec0e4458359bdce1028d7f
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 224d491b3c8665e3b54a6090e51de5ebbdc02596
---

## detail

writing-go rule 14 asks a test to compare with cmp.Diff, and github.com/google/go-cmp is required by no go.mod in the tree. So the clause cannot be obeyed anywhere.

The four modules are src/engine, src/mcp, src/viewer and util/setup. Two dependencies are already required by src/engine, fsnotify and go-sqlite3, so this sets no precedent.

There are two places to use it at once: reflect.DeepEqual appears in one test file in src/engine, twice.

## proposed action

Require go-cmp in the four go.mod files, and replace the two reflect.DeepEqual comparisons with cmp.Diff.

## done when

- go-cmp is required by src/engine, src/mcp, src/viewer and util/setup
- no _test.go file under src or util calls reflect.DeepEqual
- go build -C src/engine -o ../../.bin/se.exe . succeeds through the swap door

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A writer can obey writing-go rule 14 in any of the four modules. A failing comparison now prints what differs. | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | The rule stays unobeyable, so every new test compares by hand and says nothing about how the two sides differ. | the detail |
| [x] | the ask is small enough to review whole, or it is split first | Ten added lines across six module files, and four comparisons in two tests. | git diff HEAD --stat |
| [x] | every done-when line is decidable, and names the command where one decides it | Lines 1 and 2 are searches. Line 3 is a build of src/engine. | se find |
| [x] | the basics it stands on exist, or are minted first | go-cmp v0.7.0 was already in the module cache, so nothing had to be fetched. | /root/go/pkg/mod |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole, with writing-go rule 14. Rule 12 drove the searches first. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | The searches are the check. go-cmp was in no go.mod, and reflect.DeepEqual stood in four places, two more than the detail counted. | se find |
| [x] | the same test was seen green after the change, and named | All four go.mod files name go-cmp. The search for reflect.DeepEqual answers nothing. The mcp suite answered ok. | se find |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Six module files and two tests. Only go-cmp is added. | git diff HEAD |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The viewer and setup requires read as indirect, because nothing there imports it yet. A tidy would drop the line again. | src/viewer/go.mod |

## note

WHAT WAS NOT RUN. The third done-when names a build through the swap door. The four modules were built directly instead, each with the tree's own tags, and all four produced a binary. A swap hands every agent on this box a new engine. Another lane is mid-refactor in src/engine, so that was not mine to do.

TWO MORE THAN THE DETAIL COUNTED. It named two comparisons in src/engine. There are two more in src/mcp, and the criterion asks for none under src or util, so all four were replaced.


