---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: guidance reword reds test
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b856dbcbe70b60bb2bb2893894e5b53916941e51
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 06a36f0645a86e9a28ef4918c0a8d84b4586d336
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: TestTheWorkTokenGuidanceNamesTheTwoQuestions passes at the head, and the guidance names both questions in the words the ask criteria use.
---

## detail

Found reviewing wk-2b8f45c1e7, which put two criteria on the tracked ask and a test on the guidance that names them.

src/engine/askanswerswhy_test.go, in TestTheWorkTokenGuidanceNamesTheTwoQuestions, asserts doc/guidance/work-token.md contains the strings "what is gained by doing it" and "what breaks if it is never done". Those were the words rule 1 and the discussion carried when wk-2b8f45c1e7 landed at 1c970535.

wk-54936ec0f0 then trimmed the guidance, at 0f465154 and 3d2fd6c3. Rule 1 lost its sentence, and the discussion line became "the ask says what is gained and what breaks if nobody does it". Neither asserted string survives.

At origin v4, run under the installer's cgo.env, the test fails on both strings. TestTheTrackedAskAnswersWhy and TestAMintedTokenCarriesTheWhyRows still pass, so the processes are intact and only the guidance drifted.

The branch head carries a red test. Either the guidance says the two questions in the words the ask uses, or the test is taught the shorter wording. The first is better, because the ask criteria and the guidance should read alike.

## proposed action

Put the two questions back into doc/guidance/work-token.md in the words the ask criteria use, keeping the file inside the bounds wk-54936ec0f0 set. If the shorter wording is wanted instead, change theQuestionsTheAskOwes in src/engine/askanswerswhy_test.go and the process criteria together, so all three read alike.

## done when

- TestTheWorkTokenGuidanceNamesTheTwoQuestions passes at the branch head, decided by: go test -run TestTheWorkTokenGuidanceNamesTheTwoQuestions in src/engine
- the guidance and the ask criteria name the two questions in the same words, decided by: read doc/guidance/work-token.md beside src/processes/standard.process.yaml

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The guidance and the ask criteria say the two questions in one wording, so a reader holding a token to its ask reads one. | 1 wording |
| [x] | what breaks if it is never done, and not only that it stays undone | The branch head carries a red test, and every agent that runs the suite reads a failure that is nobody's change. | 1 red |
| [x] | the ask is small enough to review whole, or it is split first | Two lines in one file. | 2 lines |
| [x] | every done-when line is decidable, and names the command where one decides it | The named test, and the guidance read beside the process files. | 2 of 2 |
| [x] | the basics it stands on exist, or are minted first | The wording is already in both process files, so this copies rather than invents. | 2 of 2 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | The first of the two options was taken, so the guidance moved and the test did not. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | At the tip, the test named both strings as missing from the guidance. | 2 red |
| [x] | the same test was seen green after the change, and named | It passes, with TestTheTrackedAskAnswersWhy and TestAMintedTokenCarriesTheWhyRows. | 3 of 3 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Two lines swapped in chapter 1, and the Discussion reads 996 against 1000. | 36bbe7d9 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | One correction to this detail. The words went before wk-54936ec0f0 touched the file, because de8edd7c already lacked both. | de8edd7c |

