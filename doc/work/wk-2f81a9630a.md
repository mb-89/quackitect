---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: three files are unformatted
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/main
claimed_at: "2026-09-07T16:27:01Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 4f114fb0d819a0c6eea4ad172f4fc2bc2cecd19c
---

## detail

Found landing wk-d924167a0a, on a file this box was already editing.

gofmt -l over src and util names three files the formatter would rewrite: src/engine/battery.go, src/engine/tests.go and src/engine/internal/keyword/keyword_test.go. All three are struct field alignment, and none of it came from the change that found them.

This token was minted saying nothing in the battery reads gofmt. That was wrong, and measuring it is what showed so. se lint reads the formatter, and .bin/se lint names all three files today. Each says gofmt would rewrite this file. The guard is there and it is working.

What is true is that nobody has answered it. The battery's se lint step is red on this box for another reason. golangci-lint refuses to load its config, because the Go it was built with is older than the target. So a formatting finding lands inside a step that is red anyway, among six refusals about a linter.

The linter wall is wk-8374ec5d2f and is not this. This is the three files.

## proposed action

Format the three files, and leave the guard as it is.

## done when

- gofmt -l over src and util names nothing, decided by: run it from the root
- se lint reports no finding saying gofmt would rewrite a file, decided by: run .bin/se lint and read its findings
- the engine's own tests still pass over the files that moved, decided by: se test proposing the tests those files carry

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

