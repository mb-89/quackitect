---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: checks miss new modules
# where the token stands. The process owns these values.
status: open
# what has to be true before this is worth reading again
ready_when: ready when the tree is quiet enough to run the battery, on a clone at the branch tip. The battery starts an engine and watches the tree, and this box carries ten agents at once. Two of the three criteria are decided by a battery run, so neither can be decided here. This clone is also behind the tip, where src/filter and the check that now reads it live.
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 10f64854719ed081c20720586e644248d52d680a
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 0b82087160c750111173203ec85be0da0f04899e
---

## detail

util/checks/no-loose-glyphs.mjs held a hand-written list of folders and did not name src/filter, so four hundred lines that moved out of the viewer went unread. wk-106911cbea added the folder. That is the smallest answer and not the last one.

MEASURED IN SEPTEMBER 2026, on a worktree at the branch tip. A gear was written into src/filter/filter.go, in code rather than in a comment. With the list as the branch had it, the check said thirteen files read and none failed. With src/filter on the list it said fourteen files read and one failed, naming the file and the line.

TWO MORE LISTS OF THE SAME SHAPE. util/checks/battery.sh at line 378 runs the viewer suite and knows no filter module. util/checks/tests-name-no-token.mjs at line 25 walks three named folders. Each goes stale the next time a module is born, and it goes stale quietly. The check answers green over a folder it never opened, which reads the same as a folder with nothing wrong in it.

THE ANSWER THAT CLOSES THE CLASS is to find the modules by walking the tree for a go.mod. The note on wk-db6bdc8872 already says that is what every reader of the modules does, so a hand-written list is the odd one out.

## proposed action

My best attempt: one walk of the tree for go.mod answers all three lists, and each check reads that walk rather than a list of its own. The two engine and viewer entries that name single files, like editor.ts and panel.ts, stay named, because those are a choice about which files draw and not a list of modules.

## done when

- no check names a module folder by hand, decided by reading the folder lists in util/checks/no-loose-glyphs.mjs, util/checks/battery.sh and util/checks/tests-name-no-token.mjs
- a module born after the change is read without editing a check, decided by adding a folder carrying a go.mod and one Go file and running the battery
- the battery is green over the tree, decided by running util/checks/battery.sh

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

