---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: cloud card section misordered
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-dutilleux
claimed_at: "2026-09-05T16:33:10Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b16b1b936632e7c94847bec7609650fad43d5d26
---

## detail

A finding on wk-b75b6ebff3, notes become tracked tokens.

util/cage/cloud-runner.md gained actionable 10 and a Discussion section for it. The section went in above `### 9. The chat is where they read you`, so the Discussion now runs 1 to 8, then 10, then 9. The Actionables list above it is in order, so the two halves of the card disagree about where 9 and 10 are.

THE DEAD WEIGHT. The card is the one thing a cloud agent reads before it does anything, and it is read by number: the actionables are numbered so the discussion can be found. A reader following 9 to its discussion walks past 10 to get there, and the next person adding an actionable copies the placement.

THE CHECK. No check reads this file's shape. The headings under Discussion are numbered and so are the actionables, so a check that the discussion headings ascend, and that every actionable number has a heading, is a few lines and holds for every card in util/cage.

## done when

- ### 10 in util/cage/cloud-runner.md sits below ### 9
- a check reads every card in util/cage and fails when a Discussion heading number is lower than the one above it
- the check is seen red against the file as it stands now and green after the move

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one card reordered and one half added to one check | read whole |
| [x] | every done-when line is decidable, and names the command where one decides it | all three are decided by the same run. It prints the numbers in the order the card discusses them, so the first line is read off the same output | ./RUNME.sh test --on wk-954d674f30 --by worker-dutilleux --propose the-cards-reach-their-box |
| [x] | the basics it stands on exist, or are minted first | the-cards-reach-their-box already reads the cage cards and the battery already names it, so no new file and no edit to the contested battery.sh | se find --path util/cage/* |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token, in the prompt | — |
| [x] | one test was written first and seen red for the reason expected | the new half went red before the card was touched: FAIL, cloud-runner.md discusses its points in the order 1, 2, 3, 10, 9, 5, 4, 7. Worse than the token said, which named only 10 above 9 | 1 failed |
| [x] | the same test was seen green after the change, and named | the-cards-reach-their-box, ok true. The headings now read 1, 2, 3, 4, 5, 7, 9, 10 | RUNME test |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | sections 4, 5, 7, 9 and 10 move into order, prose untouched, and the check gains its ordering half | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | actionables 6 and 8 have no section. Outside this done-when, so wk-cbc9d0854e carries it | minted |

