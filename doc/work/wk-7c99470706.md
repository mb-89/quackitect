---
id: wk-7c99470706
seq: "-35"
type: work
title: one round then repair
status: imp_open
assignee: cowork
scope: single-step
traced: true
depends_on:
  - wk-925ca24643
  - wk-00e222004d
rounds: 1
minted_by: cowork
submitted_by: cowork
---

## detail

A single-step token gets at most one spec rejection round: from the second failing spec round the engine refuses a plain rejection. The refusal itself grants rung two on a single-step token, a stated change to the ladder's trigger for this scope. The refused reviewer then holds full authority and may repair the draft or send the work where it judges right. The person stays reachable at rounds-per-rung further failures after the grant. Multi-step tokens and the implementation half keep the ladder default. Say in one line how the person is reached on this cell, or that the cap replaces rung three there. Related: wk-925ca24643 for the authority a repairing reviewer holds, wk-00e222004d for the repair path.

## done when

- A second spec rejection on a single-step token is refused, and the refusal names the two doors that stay open: repair the draft with the ladder's authority, or escalate to the person. The fixture drives one spec rejection, sends a second, and asserts on the refusal's own words, asking the gate and not the source.
  `rg -q func.TestASecondSpecRejectionOnSingleStepIsRefused src/engine && go test -C src/engine -count=1 -run TestASecondSpecRejectionOnSingleStepIsRefused$ .`
- A multi-step token keeps the ladder and so does the implementation half everywhere: a second spec rejection on a multi-step token is taken, and an implementation rejection on a single-step token is taken too.
  `rg -q func.TestAMultiStepTokenKeepsTheLadder src/engine && go test -C src/engine -count=1 -run TestAMultiStepTokenKeepsTheLadder$ .`
- The refused rejection leaves a door open, driven: the fixture drives one spec rejection on a single-step token, sends the second, takes the refusal, then sends the repair verdict the refusal names and requires it accepted.
  `rg -q func.TestTheRefusedRejectionLeavesADoorOpen src/engine && go test -C src/engine -count=1 -run TestTheRefusedRejectionLeavesADoorOpen$ .`
- Every test named above was watched failing on its own assertion, with the rule absent, before it was watched passing, and the evidence names each test and what it said.
