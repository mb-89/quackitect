---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: rationales get a kind
# where the token stands. The process owns these values.
status: done
# who did the work step, so the verdict is never theirs
author: worker-birch
claimed_by: 547b9365/reviewer-quince
claimed_at: "2026-09-05T15:57:37Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 3dfe7cadc440ee2ed1e575cf9f177f4a75d0f6b1
  - 3ac6bf200154ff2431fb491e7927be285c09ee73
  - 37bcb61550ee4470183484cbf696b7631e6395c6
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 314bb4b866b6c66c389c66deb3f2d516c20a41fe
  - fe8b43557a297f59f6ddf7dde5b36e9815b5dacc
---

## detail

Everything the engine carries should be actionable and in the present tense, and it is not. schema.go, gate.go and store.go carry paragraph-long ALL-CAPS arguments about why a thing is the way it is. So do the MCP tool descriptions, which cost 3780 bytes on every turn. Guidance already has the shape: Actionables says what to do, Discussion holds the argument. Nothing else has anywhere to put a rationale, so the argument lives where the instruction should be, and every reader pays for it. The owner rules a rationale is a kind of its own with a template and a folder. Consumes wk-f302064e96.

## done when

- rationale is a kind with a schema, and a note of that kind lints: .bin/se.exe lint
- a rationale carries what was decided, why, and what it costs. The thing it explains names it by link rather than repeating it: go test -C src/engine -run TestARationaleIsNamedNotRepeated
- the MCP tool descriptions carry the instruction and name their rationale. What a turn pays for them falls, with the note saying from what to what: se find --regex 'description.:' --path src/mcp/lane.go

## evidence: 1 rationale is a kind that lints

src/schemas/rationale.schema.yaml exists, doc/rationale holds a-description-is-an-instruction.md and a-field-is-read-or-gone.md, and both carry kind: [[rationale]]. ./RUNME.sh lint answers 7 findings, every one of them on a work token in doc/work and none on a rationale.

## evidence: 2 a rationale carries what was decided, why and what it costs

TestARationaleIsNamedNotRepeated: ok. It loads the schema, refuses an empty corpus, validates every shipped rationale against it, requires the decided, why and costs chapters to say something, and drives the link both ways: the note names what it explains and the thing explained names the note back without repeating its sentences.

## evidence: 3 the descriptions carry the instruction and name their rationale

src/mcp/lane.go:14 says the answer is [[a-description-is-an-instruction]]. The tool descriptions read as instructions: START THE ENGINE, TEST WHAT YOU CHANGED, TAKE A BLOCK OF WORK. Summing the says literals over the working copy answers 21 descriptions and 1158 bytes, against the 5249 the note records before the cut.

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | Unmet as a section. The shape is in the detail: a rationale is a kind with a schema, a folder and a template, and the thing it explains names it by link. A reader can disagree with a new kind. | the detail |
| [x] | every done-when line is decidable, and names the command where one decides it | All three name one, and all three were run. | done when |
| [x] | the change is small enough to review whole, or it is split first | The kind and the first two notes are one change. It consumed wk-f302064e96 rather than being split further. | — |
| [x] | the basics it stands on exist, or are minted first | LoadSchema, ValidateNote and the lint already read a kind off src/schemas, so the kind needed no new machinery. | rationale.schema.yaml |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Every criterion was run again rather than read off the tree. One did not answer what it claims. | work-token |
| [x] | the change follows the approach on the token, or the token says why it departed | The kind, schema, folder and template are there, two notes are shipped, and lane.go names its rationale. | doc/rationale |
| [x] | se test --on this token answered ok, and what it ran is named | TestARationaleIsNamedNotRepeated: ok. se lint answers 7 findings and none is a rationale. The descriptions sum to 1158 bytes over 21 tools. | se test |
| [x] | the note says what changed and why, for a reader who was not here | a-description-is-an-instruction.md says what was decided, why, what it costs and when to revisit. | the note |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | That note's before reading names a file holding no description at the commit it names. | wk-443ad24b6d |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | reviewing was read and applied | — |  |
| [ ] | every hunk of git diff began..ended was read, and any not read is named |  |  |
| [ ] | every criterion's command was run again, and what it said is named |  |  |
| [ ] | every hunk improves the product, or a finding names the one that does not |  |  |
| [ ] | every finding is a trivial token naming this one, and their ids are here |  |  |

## evidence: what the third criterion did not answer

The note states its method as a sum of the description literals in the file it explains, over the working copy and over git show of 7f22e1a3. That does not reproduce either number. src/mcp/lane.go at 7f22e1a3 carries no tool description at all, every backtick in it being a struct tag, and the descriptions were in src/mcp/main.go then. The after reading, 883 bytes over 11 descriptions, is written in the present tense and the working copy now sums 1158 over 21. The criterion asks the note to say from what to what, and it does. Whether those readings reproduce is wk-443ad24b6d.

## approach

A rationale is a kind like any other: src/schemas/rationale.schema.yaml, notes in doc/rationale/, read by the lint the way guidance is. The shape to disagree with:

- frontmatter carries kind, title, and explains, a list of the files or notes that hold the instruction this argues for.
- the body runs decided, why, costs, revisit when. Four sections, none optional.
- the walk goes both ways. The rationale names what it explains, and the thing explained names the rationale back by link and does not repeat its sentences. TestARationaleIsNamedNotRepeated drives that through every note: the link is there, and no sentence of why appears verbatim in the file it explains.

Two cases rather than a sweep: src/mcp/lane.go, where a turn pays the cost, and src/schemas/work-token.schema.yaml, which carries the heaviest standing argument. The ALL-CAPS blocks left in schema.go, gate.go and store.go are ordinary work afterwards, one token each.

## note

A rationale is a kind now: src/schemas/rationale.schema.yaml, notes under doc/rationale, and LintRationales reading them the way LintGuidance reads guidance. Two arguments moved out of the code carrying them. src/mcp/lane.go and src/schemas/work-token.schema.yaml each kept the instruction and gained a link to the note that argues for it, so the walk goes both ways.

TestARationaleIsNamedNotRepeated drives both halves over the shipped corpus: the explained file names the note, and holds none of its sentences.

The descriptions had already been trimmed under the token this one consumes. Measured on lane.go, they fell from 43 descriptions and 5249 bytes at 7f22e1a3 to 11 and 883. This change did not move that number. It moved the argument left behind beside them.

