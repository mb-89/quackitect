---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: oversized evidence blocks verdict
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-ash
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0d72acb3c2524106c8aa733748eb972953cf041e
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 1996fd4a1624932f7a2faaafa3e70f9f91f07cab
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

The reviewer pull answers wait rather than handing out a token. The record will not take a write, because an evidence section runs past the words the schema allows.

Two standard tokens owed a verdict carry step tables restored verbatim from their ended commits. Restored verbatim they exceed the cap the schema puts on an evidence section.

So the engine cannot write a reviewer hold onto the note, and hands out no token at all. One oversized section stops the whole reviewer queue.

## proposed action

Trim the offending sections to fit, keeping what was built and what each check said.

Then decide whether a section that will not fit should stop the queue, or refuse only the write that made it.

## done when

- the reviewer pull hands out a token rather than answering wait
- no evidence chapter of either token runs past the words the schema allows: the save refuses one that does

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One mechanism, one engine file and one test. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Neither line named a command, so both are named in the evidence chapter the two criteria decided. |  |
| [x] | the basics it stands on exist, or are minted first | overWords, overCaps and proseThatFits already stand, so nothing was minted first. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove the red first, rule 15 minted the cleanup. |  |
| [x] | one test was written first and seen red for the reason expected | TestAnOversizedChapterDoesNotStopTheQueue failed saying the queue answered wait, evidence step 2. do runs to 439 words and the schema allows 200. |  |
| [x] | the same test was seen green after the change, and named | The same test answered ok, with seventeen others named beside it. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | The engine wrote both. The change is pull.go and the new queuepasses_test.go, and nothing else. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | It is wk-d4609788a3: take opens a stretch before the save, so a refused write still leaves a snapshot object. |  |

## evidence: the two criteria decided

The first. Five reviewers hold a token each in the engine's hold store: hazel, larch, maple, rowan and juniper. The reviewer pull hands out rather than answering wait.

The second. Every chapter of every note under doc/work and .se/work was counted the way overWords counts, comments stripped. No evidence chapter runs past two hundred words. The longest is exactly two hundred, in wk-c93aac62be. The two the detail names are inside it.

And the save still refuses one that is not. The test writes four hundred and thirty-nine words into a chapter, through the file rather than the door. Then it calls SaveToken and requires an error naming the chapter and its bound. It got one.

## evidence: what was decided

The proposed action asked whether a section that will not fit should stop the queue, or refuse only the write that made it.

It refuses only the write. The cap stays and the save keeps holding it. A record that took whatever was handed to it would fill with tickets nobody reads. What does not stand is one such token answering for every other. So take now says whether the record took the write, and next passes over what it would not take. The wait at the end names each one with its refusal, so the chapter to shorten can be found.

The prose half needed no trimming. Every chapter of both tokens was already inside the cap when this was taken up, measured at the began snapshot this token carries.

