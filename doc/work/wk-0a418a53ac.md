---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: folded blobs travel nowhere
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-varese
claimed_at: "2026-09-05T16:00:25Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - ac73be528f9141fa591afcf363ff12073eb2f420
---

## detail

Folding a tag's object into an archive row makes the row name an object, and does not make that object travel. The list still does not read on a box that was never handed the tags.

writeArchiveRows gives a row naming a tag and no blob the object that tag holds. wk-54dd15a1dc ran that fold over the branch's 66 unfolded rows and committed the result, which is what its criteria asked. Every row names a blob now, and the blob is the object the tag reaches. Nothing else reaches it.

MEASURED, on this box. A clone of this repository made with --no-tags carries no tags, and `git cat-file -e <blob>` in it answers missing for every folded row tried, eight of eight, wk-0016f540ed and wk-0086ed9e9b among them. The clone holds the whole branch, so this is not a shallow clone missing history.

So archive-rows-name-an-object is green over a list whose rows a clone cannot read. Naming a blob is not the same as the blob being there.

What travels is on_branch, the note's bytes as a commit on the branch carried them. Two of the 78 rows have one. For a token whose note was committed before it closed, that blob is in every clone, and the row's disposition carries what the closing lines said.

The smallest case: clone this tree with --no-tags and run se archive --id on any folded row.

## done when

- a row a clone can read is told apart from a row that only names an object, decided by: a check or a Go test that resolves each row's object in a repository holding no tags and refuses a row whose objects are all absent there
- every row of doc/work/archive.jsonl reads back in a clone made with --no-tags, decided by: clone the tree with --no-tags, then se archive --id <id> for each row answers the note rather than an error
- a row that can be given an on_branch is given one, decided by: for each row with no on_branch, git rev-parse <commit>:doc/work/<id>.md over the branch's history either answers a blob that is then on the row, or answers nothing for every commit

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one fold in writeArchiveRows, one check, one test. Criterion 2 does not close here and moves to wk-17f90ad848 with its command | split at the end |
| [x] | every done-when line is decidable, and names the command where one decides it | line 1 by `node util/checks/archive-rows-travel.mjs .`, line 3 by `./RUNME.sh archive --sweep` and reading on_branch off the list. Line 2 by `git clone --no-tags --no-local file:///home/user/quackitect` and `git cat-file -e` on each row's objects | run from the root |
| [x] | the basics it stands on exist, or are minted first | onBranch, writeArchiveRows and the tag fold all existed, and the sweep already calls the writer | nothing minted |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read. Rule 5: criterion 2 cannot close for six rows, so it moves to a token carrying its command | — |
| [x] | one test was written first and seen red for the reason expected | TestTheFoldGivesARowTheCopyThatTravels red: the fold left the row naming "" where the branch carried a blob | FAIL |
| [x] | the same test was seen green after the change, and named | that test ok, with TestAnArchivedNoteSurvivesAGarbageCollection. archive-rows-travel refuses six rows and passes 79 | 6 named |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | writeArchiveRows folds on_branch through onBranchEver, which walks the full history. The check and one test are new | one commit |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | six rows the branch never carried cannot travel at all, and the answer is a decision | wk-17f90ad848 |

