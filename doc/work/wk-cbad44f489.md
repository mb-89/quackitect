---
id: wk-cbad44f489
seq: 1000050
type: work
title: a sphere moves underneath
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: main
---

## detail

A REVIEWER LOSES ITS WORK WHEN A NEWER ONE TAKES THE SPHERE, AND NOTHING WARNS FIRST. MEASURED, by reviewer10 this session: it had prepared a rejection on wk-24be1c06ae with two findings and a minted lesson token, and the verdict was refused with this spec is not with you, because reviewer11 had claimed the reviewer sphere while the review was being written. The findings were re-derived later at full cost. THE REFUSAL IS RIGHT AND THE MOMENT IS WRONG. One sphere has one reviewer is a rule worth keeping. What is wrong is that a reviewer learns it only when it hands in, after the reading, the reproducing and the writing are all done, and the engine's answer at that point is stop. THE SHAPE THAT MAY WORK, and it wants agreeing before it is built. A reviewer that is about to lose a token is told on its next call rather than at the verdict, the way the guard already tells an agent other things on every tool call. Or the sphere is not taken from a reviewer that is still pulling, the way the investigate already asks whether a holder has stopped. WHAT IS UNCERTAIN. Whether a second reviewer arriving should be refused, queued, or given a different sphere, and that is the owner's call because it decides how many reviewers can run at once. AND THE COST IS THE ARGUMENT FOR FIXING IT: a lost review is the most expensive thing this queue produces, because it is the one piece of work nobody can tell was lost.

