---
id: wk-61d6fa0484
seq: 1000160
type: work
title: here holds both halves
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
evidence:
  - outcome
minted_by: main
submitted_by: main
reviewed_by: rev-40
---

## detail

The here group in util/views/work.base names imp_open and imp_in_work only, so a draft in spec_open or spec_in_work is in no group a person looks at. The owner said: here should also list spec open. Widen here to the four states and add a check that asks the engine for them.

## done when

- here answers yes to every state a worker is handed or holds and no to every other, both sets read from HandedOut and HeldBy, as one command
  `rg -q func.TestHereHoldsEveryStateAWorkerIsHandedOrHolds src/engine && go test -C src/engine -count=1 -run 'TestHereHolds' .`
- the view file names the four states, why it names them rather than asking, and what holds the hand list
  `rg -q "spec_in_work" util/views/work.base && rg -q "HandedOut" util/views/work.base`
- the whole tree still answers
  `bash util/checks/battery.sh`

## evidence: outcome

The here filter in util/views/work.base names the four states with a comment pointing at src/engine/here_test.go. That file adds TestHereHoldsEveryStateAWorkerIsHandedOrHolds and TestHereHoldsNothingSomebodyElseIsHolding, built from HandedOut and HeldBy. The battery answered all ok.
