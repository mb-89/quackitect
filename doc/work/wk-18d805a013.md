---
id: wk-18d805a013
seq: 1000088
type: work
title: the table follows work
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: main
---

## detail

wk-2b78b911b1 carries a table of verdicts derived from the notes at check time, so ordinary work turns TestTheSecondBacklogCountIsDerived red and somebody retypes the table. Decide between two fixes. Freeze the table at the snapshot sha, or compare the detail against the section headings inside the token, as criterion 1 does. Same class on criterion 6: its recorded red cites drain_test.go:553, so adding lines above the assertion turns TestEveryRedSaidResolves red. doc/guidance/reviewing.md says to name what a check said rather than where it was. Both checks must stop failing for those two acts. They must still fail for a wrong count and for a citation that never pointed at an assertion.
