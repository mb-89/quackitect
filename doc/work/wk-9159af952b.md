---
id: wk-9159af952b
seq: 1000203
type: work
title: the panel hides work
status: backlogged
assignee: human
scope: single-step
traced: true
minted_by: main
---

## detail

`se work --on wk-71a9ec3d53 --by main` answers status imp_in_work, holder main, and the owner's panel shows nothing in work. Find which of four it is, measured, then fix it. The pills in src/extension/editor.ts count `status == imp_in_work || status == spec_in_work` over the view's filters. If the count is right and the row is hidden, it is the grouping, and if nought, the query or data. The view's here group already includes imp_in_work. The third column shows the assignee and not the holder, so nothing on the page may say who is working now. Then the fix is a holder column. Also check whether the panel refreshes while an agent works or only on the person's action.
