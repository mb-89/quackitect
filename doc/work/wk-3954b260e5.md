---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: expr keeps its contains
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-sibelius
claimed_at: "2026-09-05T16:39:39Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 89d7366512f4d1ef5ff1e0c45cc06a445f1573da
---

## detail

A finding on wk-40abb881a7. src/engine/internal/expr/expr.go:314 defines func contains(list []string, s string) bool, a seven line linear scan. The same commit, 69dc3aed, replaced its two callers outside the package with slices.Contains: src/engine/filterbuild.go:264 and src/engine/view.go:462, and it added a slices.Contains at src/engine/view.go:894 and src/engine/view_test.go:507. So the change decided that slices.Contains is the way this tree spells the operation, and then left the hand rolled copy behind for its one remaining caller, the list branch of the contains method in the same file. That is dead weight the change itself revealed: seven lines duplicating the standard library, in a package that was just given a boundary so a reader can read it whole.

## proposed action

Read the contains method's list branch through slices.Contains, delete func contains, and add slices to the import block. go build -C src/engine ./... and go vet -C src/engine ./... after.

## done when

- src/engine/internal/expr/expr.go defines no contains: se find --regex "func contains" --path src/engine/**/*.go answers nothing
- the contains method still answers for a list: se test with TestWhatAFilterCanSay, which evaluates subs.contains("wk-1") over a list value, answers ok true
- the tree still builds and vets: go build -C src/engine ./... and go vet -C src/engine ./... both answer nothing

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one function deleted, two call sites read through slices.Contains, one import added. | `git diff -- src/engine/internal/expr/expr.go` |
| [x] | every done-when line is decidable, and names the command where one decides it | a search for the first, one test for the second, build and vet for the third. All three were run. | below, one row each |
| [x] | the basics it stands on exist, or are minted first | slices.Contains is the standard library, and this tree already spells the operation with it. Nothing was minted. | src/engine/view.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read. The search was the red. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | none was written: nothing new is claimed. The red is the criterion's own search, which answered line 314 before and nothing after. The test grips the branch too, failing on a clean copy with false in place of the call. | `se find --regex 'func contains'` |
| [x] | the same test was seen green after the change, and named | TestWhatAFilterCanSay ok. | `se test --on wk-3954b260e5` |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | expr.go loses func contains, gains slices, reads both call sites through slices.Contains. | `git diff --stat` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | the detail names one caller and there are two. The binary walk called it too, so the delete needed both. | `git diff -- expr.go` |

