---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the identity door untested
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-sibelius
claimed_at: "2026-09-05T15:15:41Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - a4c3beaeff328e3ebea2d5cb6e5ac692fc06ebe0
---

## detail

Found reviewing wk-120d7c9685.

Four of that token's criteria are written as writes at the door: a tracked file is refused, .se is taken, a machine field keeps its stamp, a username is refused. Every test it landed drives identityMaterial on its own and nothing drives decidePreToolUse.

The .se exemption is the one this costs. It is not in identityMaterial at all. It rests on where the block sits in src/engine/hook.go, inside the branch guarded by underPrivate. Move the block one brace out and .se writes start being refused, and every test in the tree stays green.

## proposed action

One table over decidePreToolUse: a write of prose carrying an ISO date into doc/work, the same write under .se, and a write whose only stamp is a frontmatter field. It asserts the decision, not the pattern.

## done when

- a test in src/engine drives decidePreToolUse over those three writes and passes
- the .se row goes red when the identity block is moved out of the underPrivate branch
- go test ./src/engine answers ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one table of three rows over one function, in one test file. | src/engine/identitydoor_test.go |
| [x] | every done-when line is decidable, and names the command where one decides it | the first two are, by the command below. The third asks the whole package, and no token can answer it while the branch tip will not build. | `go test ./src/engine -run TestTheIdentityDoorDecidesWrites` |
| [x] | the basics it stands on exist, or are minted first | decidePreToolUse, underPrivate and identityMaterial all existed. Nothing was minted. | `se find --regex 'func decidePreToolUse'` |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read. Red first, then green. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | on a clean copy at 5a83c225, where hook.go and the test match the tip exactly. Move the identity block out of underPrivate and only the .se row reddens. | `go test ./src/engine -run TestTheIdentityDoorDecidesWrites` |
| [x] | the same test was seen green after the change, and named | green as the branch has it, in TestTheIdentityDoorDecidesWrites. | same |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | it is not. identitydoor_test.go was in the tree at began, so the diff holds the note alone. | `git log -1 -- src/engine/identitydoor_test.go` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | not this token's. The package answers twelve failures, none identity, and the tip does not build. | `go test ./src/engine` |

