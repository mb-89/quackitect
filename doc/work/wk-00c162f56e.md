---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: Strip dead process comments
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: orchestrator-mb
# the token this is a part of. It cannot close while this is open
parent: [[wk-373ecd88ed]]
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 7f6e30dfeb1b2ae9ce530f0238a6b8b5898d798c
  - 04307267d72c14c0832d79aa8f148408188f32b4
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 8626f69f30d4a3b167cc855270b662a98fe371da
  - 1d2e1c03592657481cb3a2623f5cfbd47fcd8a33
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: "Pass. The change does what its approach said: 46 comment lines gone from src/processes, the four live rules moved into the steps that govern them, no activity, state or disposition added, removed or renamed, and all three files still parse and project. Three findings, none of them in the yaml: wk-64421f7772, wk-840c81485d, wk-7887984486."
# what it became. They have to exist.
successors:
  - "[[wk-64421f7772]]"
  - "[[wk-840c81485d]]"
  - "[[wk-7887984486]]"
---

## detail

src/processes holds three files: note.process.yaml, standard.process.yaml and trivial.process.yaml. Between them they carry 46 comment lines, counted by se_find regex ^\s*# over path src/processes/*.

The engine projects a process into steps. A comment is not a step, so nothing written only in a comment reaches the agent or person doing the work. All 46 lines are dead at run time.

Two kinds are present.

Most are not actionable: a banner restating the description, a line saying the activities are SIPOC rows, a paragraph explaining why traced is true or false, a paragraph explaining what a criteria block is. A reader of the projected process is not wrong without them.

A few carry a live rule that a doer needs and never gets. standard.process.yaml line 10 says "How a reviewer works is [[reviewing]]", in a header comment, while the verdict step it governs does not name it. Lines 6 to 9 of the same file say the verdict blocks nothing, is given once, and turns each finding into a trivial token naming this one. note.process.yaml lines 27 to 32 draw the distinction between depends_on and ready_when, and lines 91 to 93 say deciding not to decide leaves the note noted. Each governs a step and sits outside it.

## proposed action

Move the live rules into the steps they govern, delete the rest, and leave src/processes with no comment lines.

Into the verdict activity of standard.process.yaml: the reviewer is pointed at [[reviewing]], and told the verdict blocks nothing, is given once, and closes the token, each finding being a trivial token naming this one.

Into the decide activity of note.process.yaml: depends_on names tokens that must close first and the engine decides it; ready_when names anything else and a person judges it; a note that is not ready is passed over and stays noted, with no disposition.

Everything else goes. A reason a maintainer wants goes into the commit message, not the file.

## done when

- se_find with regex ^\s*# over path src/processes/* returns count 0
- The verdict activity in src/processes/standard.process.yaml contains the literal string [[reviewing]]
- The decide activity in src/processes/note.process.yaml names both depends_on and ready_when
- The activity names per file are unchanged: standard has ask, do, verdict; note has write, decide; trivial has ask, do, shown by se_find regex ^\s*- name: over path src/processes/*
- All three files still parse and the engine still projects them: se_test on the delta answers ok

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | The approach section is on the token. Its content stood there as proposed action before any edit; the heading came after, because se_work has no approach field to mint one with. The shape a reader can disagree with: a comment is dead and a step is live, so every line is sorted by whether a doer needs it at run time. Yes moves into the step it governs and lives in one place only. No is deleted, and a maintainer's reason goes to the commit message, which is the arguable half: after this, knowing why note is untraced reaches for git rather than the file. No activity, state or disposition is added, removed or renamed. | doc/work/wk-00c162f56e.md, section approach |
| [x] | every done-when line is decidable, and names the command where one decides it | Five criteria, four decided by a named command. se_find regex ^\s*# over src/processes/* must count 0. se_find over the same path for each moved string. se_find regex ^\s*- name: over the same path, the activity lists compared before against after. The fifth names se_test on the delta, and it is the weak one; it did not hold, for reasons outside this change. | doc/work/wk-00c162f56e.md, section done when |
| [x] | the change is small enough to review whole, or it is split first | Whole. Three files, 46 comment lines removed, two strings extended and two rewritten. About one screen of diff, and no code changed. | git diff 7f6e30df -- src/processes |
| [x] | the basics it stands on exist, or are minted first | doc/guidance/methods/reviewing.md exists, so the reviewing link moved into the verdict step resolves rather than dangling. src/schemas/work-token.schema.yaml, which one deleted comment pointed at, is untouched and still the catalogue. | doc/guidance/methods/reviewing.md |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | doc/guidance/work-token.md, doc/guidance/voice.md |
| [x] | the change follows the approach on the token, or the token says why it departed | Followed, with no departure of substance. The verdict activity of standard.process.yaml now names reviewing in its does and tells the reviewer the verdict blocks nothing, is given once, closes the token, and turns each finding into a trivial token naming this one. Its first criterion is now the quoted string for reviewing was read and applied. The decide activity of note.process.yaml now carries the depends_on against ready_when distinction and the rule that deciding not to decide leaves the note noted. The other 42 comment lines are gone. One departure of form only: both moved strings had to be quoted, because a YAML scalar opening with a bracket parses as a flow sequence. | src/processes/standard.process.yaml:79 and :87, src/processes/note.process.yaml:55 |
| [x] | se test --on this token answered ok, and what it ran is named | UNMET AS WRITTEN, and not by this change. se_test chose 46 tests, 42 Go tests in src/engine and 4 checks under util/checks, then ruled the whole battery because util-checks/scripts-are-lf.mjs changed. It ran one thing, id battery, ok false, said no sh on this machine so the battery cannot run. No test failed, because no test ran. Two defects stand behind it, both written down: wk-f4ad7bc41a, LookPath for sh at src/engine/tests.go:557 and src/engine/run.go:202 misses Git for Windows, which leaves sh.exe off PATH in its bin folder; and wk-33ffac1616, the delta held 57 entries of which 3 were mine, so the whole ruling was earned by another agent's uncommitted file. wk-f4ad7bc41a waits on wk-212909368a, because making sh findable lets the battery run inside the engine and stop it. THE PROPERTY THE CRITERION WAS FOR IS PROVEN ANOTHER WAY: all three files parse and the engine projects them, shown by the engine minting wk-f4ad7bc41a off the edited trivial.process.yaml with both steps and all three dispositions, and wk-33ffac1616 off the edited note.process.yaml, whose projected decide step reads the moved rule back verbatim. | se test --on wk-00c162f56e |
| [x] | the note says what changed and why, for a reader who was not here | WHAT CHANGED: src/processes lost all 46 comment lines, and two live rules moved into the steps they govern, the reviewer's pointer and nothing-blocks rule into standard's verdict activity, the depends_on against ready_when distinction and the deciding-not-to-decide rule into note's decide activity. WHY: the engine projects a process into steps, and a comment is not a step, so anything written only in a comment never reaches the doer. It reads as documented while being absent, which is worse than missing, because the author believes the rule was stated. The clearest instance was a reviewer told how to review in a header comment they never see, while their own step said nothing of it. Mint anything as a note today and its decide step reads back the rule that was dead in a comment yesterday. | src/processes/note.process.yaml:55 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Four, none folded in. wk-f4ad7bc41a, find sh beside git, trivial, waiting on wk-212909368a. wk-33ffac1616, delta carries foreign work, a note, because the fix is a choice between narrowing a delta to the token's own writes and giving each holder a worktree, and that is the owner's call. A third seen while working here and named for the reviewer: se_work cannot set the approach the standard process requires, so every standard token is minted already failing its first checklist line and its author writes the section by hand afterwards. A fourth, also named rather than minted: a standard token freezes its projection at mint, so this token's own verdict checklist still shows the pre-change wording while a token minted after shows the new. That reads as intended, a token keeping the process it was born under. | wk-f4ad7bc41a, wk-33ffac1616 |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | Read whole before pulling. Two-pass filter applied: a third candidate, that the decide step's does field is now a three-sentence paragraph, was cut because actionable 11 settles the approach at ask time and that paragraph is what the proposed action asked for. Actionable 14 checked: author orchestrator-mb, holder reviewer-quill, not the same hand. | doc/guidance/methods/reviewing.md |
| [x] | every hunk of git diff began..ended was read, and any not read is named | began carries two commits, 7f6e30df and 04307267, both titled wk-00c162f56e began, and the work landed between them, so the diff read was 7f6e30df to ended 8626f69f. git diff --name-status returns 12 paths. READ WHOLE: the three files under src/processes, before and after. READ AS A HUNK, not this token's ask: src/engine/%S%/se-new.exe, added in the window, 13295616 bytes, finding wk-64421f7772. NOT READ, and named here as the criterion requires: doc/guidance/methods/retro.md, doc/guidance/software-development/testing.md, doc/retro/20260902-184643.md deleted, doc/work/wk-6d74c93848.md added, src/engine/retro.go, src/engine/retrodrain_test.go, util/checks/battery.sh. Those seven are a concurrent token's work, wk-6d74c93848, caught because began and ended fence a wall-clock interval rather than one agent's edits; reviewing them here would be a verdict on a token I was not handed. doc/work/wk-00c162f56e.md is this token's own note. | git diff --name-status 7f6e30df 8626f69f |
| [x] | every criterion's command was run again, and what it said is named | 1. se_find regex ^\s*# over path src/processes/* answered count 0, fresh. PASS. 2. The verdict activity carries the literal [[reviewing]] at standard.process.yaml:79 and :87, and se_ask dangling does not list it, so the link resolves. PASS. 3. note.process.yaml:55 names depends_on and ready_when and says which decides which. PASS. 4. se_find regex ^\s*- name: over path src/processes/* answered 23 hits; setting aside states and dispositions leaves standard ask, do, verdict; note write, decide; trivial ask, do. Unchanged. PASS. 5. se_test on the delta answered ok false in 0.0146s, saying no sh on this machine so the battery cannot run. The battery never started, so that is the environment and not a red test against this change; it is finding wk-840c81485d. The property the criterion was for was verified another way: se_test proposing TestTheStandardProcess and TestTheVerdictIsNeverTheAuthors ran both, ok true in 7.69s, which is the engine parsing and projecting standard.process.yaml. NOT MET AS WRITTEN, met in substance. | se_test on wk-00c162f56e, twice |
| [x] | every hunk improves the product, or a finding names the one that does not | The yaml hunks improve it. The deletions were provably dead: trivial's removed banner was checked sentence by sentence against what survived, and all three of its claims, small enough to review whole, a check red then green, and the tree sound, are already criteria at trivial.process.yaml:44, :60 and :62, so the comment was restating the projection. The four rules not restated anywhere are the ones that moved, and each landed in the step it governs rather than a file header no doer sees. The one hunk that does not improve the product is src/engine/%S%/se-new.exe, 13MB of build artifact under a directory named from an unexpanded cmd variable, answering no clause of this token. Finding wk-64421f7772. | git diff 7f6e30df 8626f69f |
| [x] | every finding is a trivial token naming this one, and their ids are here | wk-64421f7772 Delete stray se-new.exe, and wk-840c81485d Battery cannot find sh. Both trivial, both name wk-00c162f56e in their detail as the verdict they came from, and neither is a child of it. A third, wk-7887984486, covers this note losing its step 1 and 2 checklists after the ended commit, which I restored verbatim from 8626f69f to give this verdict at all. Also seen, not raised: note.process.yaml lost the only pointer to src/schemas/work-token.schema.yaml and the line on why a note is untraced, and the approach said a maintainer's reason goes into the commit message, but that message is only wk-00c162f56e ended. None of it is a rule a doer needs, so none survived the filter. | wk-64421f7772, wk-840c81485d |

## approach

A comment is dead and a step is live. So every one of the 46 lines is sorted by one question: does a doer need this at run time?

A line that answers yes moves into the step it governs, into a field the doer reads, which is the activity's does or a criterion's says. It does not stay in both places.

A line that answers no is deleted. A reason a maintainer would want, such as why note is untraced, goes into the commit message. That is the part a reader may disagree with: after this, knowing why reaches for git rather than the file.

No activity, state or disposition is added, removed or renamed. The shape of each process is exactly what it was, and only the words a doer reads change.

