---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: one build door
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-willow
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 3b2678528c205edb6645ded1925113395b76357d
  - 40b0bcd1e4bb7cbb6674a07ffaf39df5dec3cf4c
  - 73006b0521d0a71ed7fe720698a5ef5b8e82cde6
  - fe97356ae0dca5bc7c21631dbfac1857f2f5cdbd
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 58e3b422a0d59d5955c822da7a7b6c51034806af
  - 3c9fa1e1733c7d226a05764f55f4f79b18655428
  - fd281d3af602f3081c0f32b188cfd1a80ae40a52
  - 9679d18630c3a798376c4df05707579f1133bb58
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Two agents rebuild and swap the engine when their change lands. se test with a whole ruling runs the battery inside the living engine, which stops that engine. Every swap rotates the log, severs the other agent's in-flight socket calls, and with tests via the engine makes tests impossible to finish. On 2026-09-03 three se test runs were severed unrecorded and the owner read a log window that kept starting over. The six-step rebuild ritual was typed by hand thirty-four times because Windows locks a running binary. Consumes wk-b8fd638ad7, wk-747d6019d6 and wk-a29aecd059.

## done when

- a swap through the engine keeps the log session and severs no in-flight call: go test -C src/engine -run TestASwapKeepsTheSessionAndTheCalls
- se test with a whole ruling answers before the battery runs and the outcome lands in the record: go test -C src/engine -run TestTheBatteryRunsOutsideTheEngine
- a go build aimed at .bin inside the tree is refused and told the door: go test -C src/engine -run TestTheBuildDoorIsTheEngines
- the battery itself uses the door rather than stopping the engine by hand: grep -c 'se.exe --stop' util/checks/battery.sh answers 0

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | The approach section was on the token before the work. It names a queued swap, a kept session, a detached battery, a refused build. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Three lines name a go test -run against a named test. The fourth names a grep count that answers 0. |  |
| [ ] | the change is small enough to review whole, or it is split first | Put back unworked by worker-four: the change swaps the running engine binary and moves the battery outside it, and this hand is barred from rebuilding .bin and from running the battery, so neither half could be driven or verified from here. It needs a hand with the build door open. |  |
| [x] | the basics it stands on exist, or are minted first | The log, the socket, .bin/was and the test door all existed. None needed minting. |  |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read in full. Rule 12, red first, could not be met. |  |
| [x] | the change follows the approach on the token, or the token says why it departed | Every clause is wired: socket.go swap, planSwap --version check, drainCalls, handOver with log.go, tests.go startBattery, main.go RecordFinishedBattery, hook.go ABuildRunByHand. Departed on red first, because the change was in the tree at take-up. |  |
| [x] | se test --on this token answered ok, and what it ran is named | se test ran TestASwapKeepsTheSessionAndTheCalls, TestTheBatteryRunsOutsideTheEngine and TestTheBuildDoorIsTheEngines, and answered ok. |  |
| [x] | the note says what changed and why, for a reader who was not here | The note section says what changed, why, and what this hand did. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | SweepOrphanedWrites and SweepWhatWasReplaced are in the change. Nothing else was revealed. |  |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | Read in full. Author worker-willow, not this hand. |  |
| [ ] | every hunk of git diff began..ended was read, and any not read is named | Not met. se run is dead here: batteryShell picks the WSL stub, so no git diff. See wk-d6e1c2e0c4. Files read whole from the tree instead. Deleted hunks unread. |  |
| [x] | every criterion's command was run again, and what it said is named | All four run again and hold. Also red: TestASwapBuildsEveryProgramTheManifestNames. |  |
| [x] | every hunk improves the product, or a finding names the one that does not | Pass. The queued swap, verified binary, kept session and detached battery earn their place. Three do not: swap.go's unwired manifest half, batteryPassed, the shell lookup. |  |
| [x] | every finding is a trivial token naming this one, and their ids are here | wk-4ad110f94e, wk-02e2f3ac8f, wk-d6e1c2e0c4. Not taken: each needs the build door. |  |

## approach

The engine is the one door to its own replacement. A swap is a queued engine act. The caller asks, and the engine builds the next binary beside the current one. It checks the binary answers --version, drains in-flight calls, records the swap, and hands over to the new process. The new process reopens the same log session rather than rotating it. se test with a whole ruling does not run the battery inside the engine. It answers the plan at once, runs the battery detached with its output to a file under .se/tests/, and the next engine records the outcome at start. A go build aimed at .bin inside the tree is refused and told the door.

## note

WHAT CHANGED. Three acts around a live engine now go through the engine.

- swap.go builds the next engine to .bin/se.next, checks it answers --version, drains the calls in flight, and hands over. handOver passes the log session on, so the record does not start over.
- battery.go starts the whole battery detached and answers at once. RecordFinishedBattery puts the outcome in the record at the next start.
- ABuildRunByHand refuses a build aimed at .bin in this tree and names se --swap. It is wired in hook.go.
- battery.sh asks for the swap rather than stopping the engine.

WHY. Two agents rebuilding at once cut each other's socket calls, and a battery inside the engine severed its own answer.

WHAT THIS HAND DID. The change was in the tree at take-up, so nothing could be watched going red. Each criterion was run green instead.

