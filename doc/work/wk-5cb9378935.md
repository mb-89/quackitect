---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: chapter eleven lost practice
# where the token stands. The process owns these values.
status: closed
author: worker-zev
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e54833d01b615df18225798684969b61aed0a2c4
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - d65e4861ccf192babba2dbbd7138e71f81c6af85
# how it ended. Only an ended token carries one.
disposition: done
# what it became. They have to exist.
successors:
  - "[[wk-1c3f3107db]]"
---

## detail

Reviewing wk-526ac833fb. Its compression of testing.md cut the closing practice from four chapters. wk-f9233114b0 was raised for three of them and has closed: chapters 10, 12 and 13 now end on a practice sentence again. Chapter 11, Fakes stubs and mocks, was missed and still ends on where to put the fake. The sentence cut was that the real thing is met in one test of that size, and everything above it runs on the fake. That is the rule for how many tests may touch the real dependency. Rule 11 does not carry it: it says only to fake at a boundary you own with a fake that behaves. se find --regex 'real thing|met in one test|runs on the fake' --path doc/guidance/** returns no hits, so the practice is nowhere in the tree. Chapter 13 also lost the clock and seed sentence and got back only the probe sentence. Rule 13 still names clock and randomness as fixtures.

## proposed action

Restore the closing sentence to chapter 11 the way wk-f9233114b0 restored chapters 10, 12 and 13. Pay for the words inside that chapter if the Discussion cap needs it.

## done when

- testing.md chapter 11 ends on the practice for how many tests meet the real dependency: se find --regex 'real thing|the real dependency' --path doc/guidance/software-development/testing.md returns a hit
- the file still lints clean: .bin\se.exe lint reports no finding for software-development/testing.md

## evidence: criterion 1 the practice is back

se find --regex 'real thing|the real dependency' over testing.md answered 0 hits before and 1 after. Line 97, chapter 11, now ends on one test of that size meets the real thing, and everything above it runs on the fake. The wording came from the ended commit rather than from memory. git diff between wk-526ac833fb's began and ended hashes over this file shows the cut line as a minus. The cut line reads: the real thing is met in one test of that size, and everything above it runs on the fake.

## evidence: criterion 2 the file still lints

.bin/se.exe lint answered the same four findings before and after, every one methods/retro.md. None is for software-development/testing.md, so it holds fifteen rules, none over 25 words, and its Discussion under 1000.

## evidence: the chapter thirteen half, checked and minted

The detail's second remark was checked rather than repeated. Chapter 13 did lose its clock and seed sentence and got back only the probe sentence. The clock half survives outside this file: writing-go.md line 99 says a five second wait is then a millisecond, under testing/synctest. The seed half does not survive anywhere: se find for seed over doc/guidance returns no hit. That is wk-1c3f3107db, not fixed here because it is a different chapter and this token's done-when names only chapter 11.

## evidence: the words were paid for inside the chapter, at net zero

testing.md stood at 998 words of a 1000 cap, so two words were the whole budget and the practice wanted sixteen. Three sentences of chapter 11 were tightened by exactly what the fourth costs. The Discussion still measures 998, so nothing else in the file paid.

The mock definition's told what to answer and expect became is scripted, since the next sentence says what scripted means. A test over a mock became A mock. The clock told when to tick example went, leaving the in-memory store, because rule 13 and chapter 13 both carry the clock. Named here so a reader can disagree with the trade rather than discover it.

