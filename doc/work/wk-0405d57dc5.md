---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: merge the tests group
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/main
claimed_at: "2026-09-07T17:59:06Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - f8da214c3ef43ac25c5f6fc98386a2d6cb104e88
---

## detail

The tests group is closed. Every token the queue hands out is done, the retro has run, and doc/work/groups/tests.done is on the branch. What is left in the bucket waits on a person.

The branch group/tests carries about twenty commits that trunk does not. Nothing there reaches a reader until it is merged, and a cloud box is reclaimed when the session ends.

The card says merge back when the group is empty, and leave the branch standing. The delete answers 403 from here and always will.

## proposed action

Merge group/tests into trunk through the door that does not touch the shared index, and push trunk. Say the branch is ready to sweep and leave it standing.

## done when

- trunk carries every commit of group/tests: git log trunk..group/tests answers nothing
- the shared working tree is where it was: git status --porcelain names no file the merge staged
- the branch still stands on origin, and the answer says it is ready to sweep

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | 107 commits of closed work reach a reader instead of dying with the box. | git rev-list |
| [x] | what breaks if it is never done, and not only that it stays undone | The group's work stays on a branch nobody merges, and the box is reclaimed. | the detail |
| [x] | the ask is small enough to review whole, or it is split first | One merge and one push. | the approach |
| [x] | every done-when line is decidable, and names the command where one decides it | Two are git commands, the third is read on origin. | git log |
| [ ] | the basics it stands on exist, or are minted first | The merge is refused by the harness, not the engine. A worktree at origin/v4 stands at /tmp/merge-v4, and git merge in it is denied. | a person |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

