---
id: wk-ca0b0641e7
seq: "24"
type: work
title: work owns its views
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "2"
evidence:
  - outcome
minted_by: person
---

## detail

The engine resolves util/views and util/icons.json from its own location rather than from --work. A reviewer isolated with --work still wrote into the live util/views/work.base twice. ViewPath reads r.Private("views") and r.Method plus util/views. Decide what --work owns. The comment on ViewPath already claims a view file under the work folder wins and the method's copy is the fallback. The same question covers util/icons.json.

## evidence: outcome

ViewPathToWrite answers a path under the work folder, and all seven edits leave the method's file byte-identical. The read still falls back for a folder that ships no view. under takes filepath.Rel on cleaned absolute paths and refuses a neighbour, the shape inRoot and underPrivate already use. TestANeighbourIsNotAChild puts the work folder at proj and the method at proj-v4 in one temporary folder and was proved red with the prefix test back. icons.go already reads the work folder first and the method second. sh .se/scratchpad/battery.sh answers all ok.
