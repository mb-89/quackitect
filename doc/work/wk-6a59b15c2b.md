---
id: wk-6a59b15c2b
seq: "-3"
type: work
title: unreviewed work blocks
status: submitted
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "2"
minted_by: person
---

## detail

More than three tokens submitted with nothing in review, and the pull refuses.

NOTHING STARTS A REVIEWER. The engine says spawn a reviewer if you have not, in
a notice, and a notice is a suggestion. Seven tokens sat submitted with none in
review, and the queue went on handing out work.

THE REFUSAL IS THE ONLY THING THAT WORKS. Every rule the agent was asked to
remember it eventually forgot. The hold refuses, the owed answer refuses, and
those two hold. A notice does not.

WHAT IT SAYS: how many are submitted, which ones, and the one thing that clears
it, which is a reviewer pulling. It clears the moment a token moves into review,
so an agent that spawns one is unblocked by the reviewer's first pull rather
than by anything it does itself.

THE NUMBER IS A PARAMETER. Three is the owner's number, said out loud while
this was being built: more than three submitted with nothing in review means
the reviewer is not running. It sits in util/parameters.json. Zero turns it off.

IT MAY BE MADE SMALLER AND NEVER LARGER, because raising it weakens a guard and
a guard an agent can loosen is not a guard. A person tightens it here and
loosens it by changing the declared default, which is theirs.

A REVIEWER IS NEVER REFUSED. It is the thing that clears the refusal, and
refusing it would be a trap nobody could escape.

## evidence: both refuse now

LintLimits returns a finding naming util/parameters.json and the error when LoadTree fails. LintIcons does the same when the file cannot be read and when it cannot be parsed. Both were fixed, because the defect was in both and fixing one would have left the other silent, which is the difference between fixing the instance and fixing the class.

## evidence: checks

go test -count=1 ./... passes in source/engine (27.0s). se lint answers clean against the real tree.

## evidence: proved red

I put the silence back in both functions and the test failed four times: LintLimits and LintIcons, missing and unparseable. Restoring them gives a green suite.

## evidence: round 2, and the finding used this project's own rule against it

LintLimits and LintIcons both returned nothing when the declaration was missing or would not parse, so se lint answered clean precisely when the file it guards was broken. The finding is right that this is not an imported rule: A CHECK THAT FINDS NOTHING TO CHECK REFUSES is in doc/guidance/behaviour.md and in all three projections, so it is in the system prompt of every agent here.

## evidence: the check the finding asked for

source/engine/lint_test.go, TestALinterThatCannotReadTheDeclarationRefuses, runs both linters against a method root with the declaration missing and again with it replaced by { this is not json }, and asserts each returns a finding saying it cannot be read. TestALinterThatCanReadSaysNothingAboutIt asserts neither says that about a file that is there, so the fix cannot pass by making every run complain.

## evidence: what round 1 established still holds

The reviewer reproduced all of it and said so: the leftover truncation is gone and AskToStop passes the whole slice with its true length, TestAStopRefusalCountsWhatIsHeldAndSaysHowManyMore goes red with the truncation restored, LintLimits catches all three classes it claims, parameters.json is coherent with max equal to default on all three limits, a limit of zero turns the refusal off, and a reviewer is never refused. None of that was redone.

## finding 1 · round 1 · WHAT IT SAYS: how many are submitted, which ones, and the one thing that clears it. The submission's own evidence: the notice and the stop check both list a few things and say how many more, that was written twice, so it is now one function, briefly, and the stop check calls it. · by reviewer

**wrong:** The stop check now under-reports and no longer says how many more. AskToStop at source/engine/pull.go:632-641 still runs the old shortening inline and then calls the new function on the result: n := len(mine); if n > 3 { mine = mine[:3] }; lines := ...; if n > 3 { lines += and %d more }; and the return is fmt.Sprintf(..., len(mine), briefly(mine)). The old code was left standing and only its two arguments were swapped, so mine is truncated to three before briefly sees it and len(mine) is read after the truncation. Two things follow. The count is the truncated one, so an agent holding seven pieces is told it holds three. And briefly can never add its tail, because len(all) > len(shown) is false when all is already three, so the and N more line the evidence claims is gone. The local lines still holds the correct old text and nothing reads it. git diff on this function shows exactly the swap: the return went from n, lines to len(mine), briefly(mine) and nothing above it changed. I proved it rather than reading it. In an isolated copy I minted seven open tokens for main and called AskToStop. It answered: You hold 3 piece(s) of work that nothing else will do, then three ids and nothing else. Before this token it would have said seven and and 4 more. The whole suite is green with this on, so nothing in source/engine covers the shortening in AskToStop. Extent, searched in one pass: briefly has two callers, pull.go:159 and pull.go:641. The one at 159 passes the whole waiting slice and is correct, and the notice's own count is right. AskToStop at 641 is the only site with the leftover, and it is the only one that was refactored onto briefly.

**satisfies:** Delete the four leftover lines, n, the truncation, lines and its tail, and pass the whole slice and its true length: fmt.Sprintf(..., len(mine), briefly(mine)) with mine untouched. Then a check that catches the class rather than this line, in the package the defect is in. A test in source/engine that mints more open tokens than briefly shows, calls AskToStop, and asserts both halves: that the count in You hold N piece(s) is the number actually held, and that the text carries and N more. I have run that test against the tree as it stands and it is red now on both assertions, logging You hold 3 piece(s) with seven held and no and 4 more. It goes green once the truncation is removed. Assert both halves, not only the tail, because the count and the list are two separate ways this same mistake shows and a test for the tail alone would pass with the count still wrong. The same test shape is worth pointing at reviewerMissing's notice too, since both callers should say how many more from one owner and only one of them is covered today.

## finding 2 · round 1 · More than five tokens submitted with nothing in review, and the pull refuses. THE NUMBER IS A PARAMETER. Five is a guess about how much unreviewed work is tolerable, and a guess belongs in util/parameters.json where a person can move it. · by reviewer

**wrong:** The number that shipped is three, not five, and no evidence section says why it changed. util/parameters.json declares limits.unreviewed_before_blocked with default 3, and TheFloor() in source/engine/config.go declares the same number a second time as UnreviewedBeforeBlocked: 3. reviewerMissing blocks when len(waiting) > limit, so the engine refuses at four waiting rather than at six, which is what the token's first sentence asks for. The second half is worse than a wrong default, because the parameter carries narrow: smaller. narrow() in config.go rejects any stored value above the declared default with this may be made smaller and never larger, so a person cannot move the number to the five the token itself names. The clause says a person can move it, and they can only move it down. I proved this rather than reading it. In an isolated copy I wrote 5 into the private store and read the value back: the value in force is 3, from the method, because this may be made smaller and never larger, and LoadConfig answers 3. The declared max of 100 on that parameter is dead for the same reason: with narrow smaller and a default of 3 nothing above 3 is reachable, so the range a person sees in the panel is 0 to 100 and the range they can actually set is 0 to 3. Extent, searched in one pass over util/parameters.json: limits.heartbeat_seconds has the same shape, default 5 with max 60 and narrow smaller, so its max is unreachable too, but that one predates this token and is not this token's to fix. limits.ready_budget_ms is coherent, its max equals its default. Within this token, unreviewed_before_blocked is the only new parameter and the only one the clause names a number for.

**satisfies:** Ship five. Set default to 5 in util/parameters.json and UnreviewedBeforeBlocked to 5 in TheFloor(), so the engine refuses at six waiting the way the first sentence reads, and so the number the token names is the number in force. If three is genuinely the better guess, that is a change to the token and it belongs in the token's detail with the reason, not in the code with the token still saying five. Then decide what narrow should be here and make the declared range tell the truth: either drop narrow so a person can raise it as the clause says they can move it, and keep max 100 meaningful, or keep narrow smaller and set max to the default so the panel does not offer a range nothing can reach. Then a check that catches the class rather than the value. A test in source/engine that asserts the two declarations of this number agree, TheFloor().UnreviewedBeforeBlocked equals the default that LoadTree reads for limits.unreviewed_before_blocked, and that a person storing the largest value the parameter's own max allows gets that value back from LoadConfig. I have run the second half against the tree as it stands and it is red now: asked for 5, got 3, from the method, because this may be made smaller and never larger. Both halves matter. The agreement half catches the two-declarations-of-one-number problem, which nothing checks today and which is silent while they happen to match. The reachability half catches a declared range a person cannot use, which is the class heartbeat_seconds is already in.

## finding 3 · round 2 · NOTHING STARTS A REVIEWER. Seven tokens sat submitted with none in review, and the queue went on handing out work. THE REFUSAL IS THE ONLY THING THAT WORKS. · by reviewer

**wrong:** The refusal does not fire in the state this queue was in one hour ago. reviewerMissing at source/engine/pull.go:151 returns not-blocked the instant it sees any token with status in_review, on the comment 'One token in review is a reviewer reading, and that is enough.' That sentence is false, and this project has already lived it being false: the previous reviewer's process died holding wk-98493ee2d8, that token sat in_review with a holder nobody was behind, and eleven tokens piled up submitted while the queue kept working. A status is not a reviewer. It is what a reviewer leaves behind when it dies. I proved it rather than reading it. In an isolated copy I parked one token in_review with holder reviewer, submitted eleven more, minted one open piece of work, and pulled as a worker. The pull answered work and handed the token out, with the notice 'Do this token.' Eleven waiting, nobody reading, and the queue went on handing out work, which is the sentence this token was minted from, word for word. Extent, searched in one pass: this is the only escape in the function, and it is unconditional. Nothing anywhere times out an in_review token, and Tokens carries no held-since, so there is currently no fact in the record the guard could consult to tell a reviewer that is reading from one that is gone. That absence is the finding, not an aside.

**satisfies:** The escape has to require evidence of a live reviewer rather than a status a dead one left behind. The cheapest correct shape, and it needs no new machinery: currentSession already exists at source/engine/pull.go:172, and the engine already reclaims a held token when a reviewer arrives, so it already knows what a fresh hold looks like. Make the in_review escape conditional on the holding token having been taken in the current session, or on the holder having moved within a window, and let a hold older than that stop counting as a reviewer reading. If that needs a held-since stamp on the token or a moment in the record, add it, and say so, because a guard whose escape cannot decay is a guard with an off switch anybody can leave flipped. The check that is red today, and I have run it: mint one token, set it in_review with a holder, submit more than the limit, mint one open token, pull as a worker, assert the answer is wait and the notice carries NO REVIEWER IS RUNNING. Against the tree as it stands it answers work. It goes green when a stale hold stops clearing the guard, and it catches the class rather than the instance, because it is written about a hold nobody is behind and not about any particular reviewer.

## finding 4 · round 2 · a check that catches the class: LintLimits in source/engine/lint.go, run by se lint ... and se lint answers clean. · by reviewer

**wrong:** LintLimits answers clean when it has checked nothing. At source/engine/lint.go:109 it calls LoadTree and returns nil on any error, so a missing or unparseable util/parameters.json produces no findings and se lint prints clean. I ran it three ways against a method root I built: with the real parameters.json it returns 0, which is correct; with parameters.json absent it returns 0; with parameters.json replaced by the text { this is not json } it returns 0. LintIcons at lint.go:53 and lint.go:62 has the identical shape, returning nil when the file cannot be read or cannot be parsed, so the whole of se lint goes quiet together. The guard this token added therefore stops working precisely when the file it guards is broken, and says clean while it does. This is not my rule imported from outside: A CHECK THAT FINDS NOTHING TO CHECK REFUSES is in doc/guidance/behaviour.md and in all three projections, so it is in the system prompt of every agent in this project. The rest of this submission is not in doubt and should not be redone. I reproduced all of it: the four leftover lines are gone and AskToStop passes the whole slice with its true length; TestAStopRefusalCountsWhatIsHeldAndSaysHowManyMore is real and I proved it red by putting the truncation back, at which point it reported 'You hold 3 piece(s)' with seven held; LintLimits genuinely catches all three classes it claims, which I proved by restoring heartbeat max to 60, unreviewed max to 100, and a declaration disagreeing with the engine, and it named each one; parameters.json is coherent, max equals default on all three limits; limit <= 0 really does turn it off; role == RoleReviewer really is never refused; and se lint and the engine suite are clean.

**satisfies:** Make both linters refuse instead of returning nil. When LoadTree or the icons read fails, return a Finding3 naming the file and the error, so se lint exits non-zero and says it could not read what it was asked to check. The check that is red today, and I have run its measurement: a test in source/engine that builds a method root with no util/parameters.json and asserts LintLimits returns at least one finding, and a second with unparseable json asserting the same. Both return zero against the tree as it stands. Point the same test at LintIcons, because the defect is in both and a fix to one leaves the other silent, and that is the difference between fixing the instance and fixing the class.

## lesson 1 · round 2 · by reviewer

**the class:** An escape hatch written as a state flag rather than as evidence that somebody is still there. A status like in_review means a live worker only for as long as the worker lives; it survives the process, so the flag outlasts the fact it stands for and the guard is left permanently open by the very failure it was built for. The same shape appears wherever a check reports success for a state in which it did nothing: a linter that returns clean because it could not read its input is the same mistake, since absence of findings is being used as evidence of absence of defects.

**instead:** Make the thing that opens a guard decay, and make the absence of input loud. When a guard has an escape, ask what the escape looks like the moment the actor behind it disappears, and if the answer is that it stays open, tie it to something with a clock on it: a hold taken this session, a heartbeat inside a window, a stamp that goes stale. When a check cannot read what it was asked to check, that is a finding and never a pass. Test both by taking the actor away rather than by taking the defect away: kill the holder, delete the input file, and see whether the guard still says everything is fine.

