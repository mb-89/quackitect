---
id: wk-6a59b15c2b
seq: "-3"
type: work
title: unreviewed work blocks
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "5"
evidence:
  - outcome
minted_by: person
---

## detail

More than three tokens submitted with nothing in review, and the pull refuses. It says how many are submitted, which ones, and that a reviewer pulling clears it. It clears the moment a token moves into review. The number is the owner's three, in util/parameters.json, zero turns it off, and it may be made smaller and never larger. A reviewer is never refused, since it is the thing that clears the refusal.

## evidence: outcome

The stop refusal counts what is held and names the holder, with the number three in the declaration and the engine. A holder with an entry is still pulling while the queue has moved less than the staleness since its last pull. A holder with no entry is trusted until the session has run past the staleness. The notice says which fact it is, using HasPulled. TestAHoldCarriedAcrossARestartIsNotCalledStopped was proved red, TestAStaleHoldIsNotAReviewerReading now pulls past the staleness before asserting, and sh .se/scratchpad/battery.sh answers all ok.
