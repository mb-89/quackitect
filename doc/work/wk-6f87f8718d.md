---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: archive words are literal
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-nancarrow
claimed_at: "2026-09-05T16:31:01Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 7279aa5a34bf2350cc9e7e70aeda2b50871d626c
  - 55cb12fc248439a41427bf94bedf7ab12b688088
  - a28b8a6f6a5472ad9e3c50f79df90e296c721942
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 75ae57116f7fd245bdf188bd9ef57cae4d80ae18
  - 53c427cb3a9378850246c0a15f5484a0e1cf463e
---

## detail

From the verdict on wk-887e9a126a, the engine archives.

se find --words means FTS5 over the index, and the flag's own help says so: terms in FTS5 syntax, a AND b, a NOT b. se find --archive --words means something else. src/engine/archive.go FindArchived does want = regexp.QuoteMeta(p.Words), so the words are matched as one literal string, operators and all.

Measured on this tree. se find --words 'undo AND pops' answers two hits. se find --archive --words 'undo AND pops' answers zero, because no archived line carries the characters "undo AND pops". The archived token wk-00b78575d6 is titled "undo pops anothers apply" and is right there under refs/tags/archive/wk-00b78575d6.

The damage is that the archive door is the whole reason the archive is not write-only, and it answers nothing for a query the tree answers. A searcher reads zero hits as "that work does not exist" rather than "that flag means something else here", and closed work is exactly the work nobody remembers well enough to doubt the answer.

## done when

- se find --archive --words answers the same question as se find --words, or refuses the syntax it cannot honour rather than matching it literally: a test searches both doors for a term carrying an FTS operator and asserts they agree, seen red first
- the help says which door means what where they differ: se find --help read at a prompt

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One branch in FindArchived, the words going to the function the tree door already uses, and one test. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | The first is the test by name, TestTheArchiveAnswersWordsTheWayTheTreeDoes. The second is read in the usage find.go prints, which names the archive door and what --path does there. |  |
| [x] | the basics it stands on exist, or are minted first | findDB and the FTS5 table the tree door reads are both here, so the archive can be asked the same question by the same code. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. The change was in the tree from an earlier hand, so the red was made again. |  |
| [x] | one test was written first and seen red for the reason expected | On a copy of the head with the words quoted into a regular expression again, the test said `the tree answered 2 hits for "undo AND pops" and the archive 0`, which is the detail's own measurement. |  |
| [x] | the same test was seen green after the change, and named | TestTheArchiveAnswersWordsTheWayTheTreeDoes passes over the head as it stands. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | FindArchived sends words to findArchivedWords, which builds the same FTS5 table over the archived text. find.go's usage names the door. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None beside it. --path over the archive was already refused rather than ignored. |  |

