---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: landed tests name ids
# where the token stands. The process owns these values.
status: open
claimed_by: f5927132/worker-tallis
claimed_at: "2026-09-06T12:03:30Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 65f641a7f10d808b673f5dd9f05153e90fec6229
---

## detail

util/checks/tests-name-no-token answers 5 failed, measured through se test on this box after wk-c456e72a75 closed the nineteen it named.

Both files are on origin/v4 now. src/engine/batterydone_test.go names wk-857e0a4f99 at lines 57, 71, 86 and 105, and was added by 6239efc. src/engine/godhandsout_test.go names wk-cda42858cd at line 17, and was added by bcc0dfe.

wk-c456e72a75 left these five out, and said why: no commit held either file when it was written, so they were another hand's work in flight. Both landed while that token was worked. They are landed and ownerless now, which is the rule that put the other nineteen in scope.

The rule is that a test must not name a token the record carries, in code or in a comment. An invented id is one character repeated, so wk-1111111111 reads as a fixture and no minted id looks like it.

This class has now been closed four times, by wk-57aca89abe, wk-89f874cf61, wk-a8b817198a and wk-c456e72a75. It comes back red on whatever was written since, because nothing refuses an id at the door where a test is written. Do not soften the check.

## proposed action

Reword each of the five so it keeps its subject and loses the id, and write any id a line still needs as one character repeated.

## done when

- util/checks/tests-name-no-token answers 0 failed, decided by: ./RUNME.sh test --on <this token> --propose util/checks/tests-name-no-token
- util/checks/tests-name-no-token.mjs is unchanged, decided by: git diff names no file under util/checks
- the tests in both files still pass, decided by: ./RUNME.sh test --on <this token> --propose TestABatteryAnswersOnDone TestGodIsHandedWorkAndUnboundIsNot

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

