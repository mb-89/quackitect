---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: gate argues in place
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-satie-two
claimed_at: "2026-09-05T20:12:52Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - dac64299a3c737a73b791ab959ae01a87b6bb4cc
---

## detail

A follow-up on wk-5b51fc14e4, which made rationale a kind and moved two arguments out. src/engine/gate.go is the heaviest of the three files that still argue in place. Measured today: 602 lines, 179 of them comment, and 31 of those open a paragraph in capitals. A reader looking for what the gate does reads why it is that way first, every time. The shape is settled and shipped. A rationale note under doc/rationale carries decided, why, costs and revisit when. The instruction keeps one line naming it, and TestARationaleIsNamedNotRepeated holds the two halves together. Nothing new is decided here. The arguments already written are moved to where an argument belongs.

## done when

- gate.go opens no paragraph in capitals: se find --regex "^// [A-Z][A-Z ,-]{15,}" --path src/engine/gate.go answers nothing
- every argument moved is a rationale note that gate.go names by link, driven by TestARationaleIsNamedNotRepeated

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A reader of gate.go meets what it does. gate.go went from 602 lines to 472, and each argument is one link away. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | Every reader of the gate keeps paying for the argument before reaching the rule, and a decision nobody can revisit stays wherever it was typed. |  |
| [x] | the ask is small enough to review whole, or it is split first | Comments only. Every non-comment line is identical, 351 before and 351 after, by a diff that drops comment lines. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Line 1 is the se find regex over gate.go. Line 2 is TestARationaleIsNamedNotRepeated. |  |
| [x] | the basics it stands on exist, or are minted first | The rationale kind, its schema and that test all shipped with wk-5b51fc14e4. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 12 put the red first, rule 15 answered each line. |  |
| [x] | one test was written first and seen red for the reason expected | The seven notes landed first, gate.go untouched. TestARationaleIsNamedNotRepeated then failed on seven notes gate.go does not name, and on seven sentences it still carried. se find answered 29 capital openers. |  |
| [x] | the same test was seen green after the change, and named | That test now answers ok in 4.9s and go vet is silent, both in a worktree at HEAD, because other hands broke the package here. se find answers nothing. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | gate.go comments, and seven notes under doc/rationale. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None found. Arguments indented inside functions stay, because the criterion names the ones opening a line. |  |

