---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: settled answer drops token
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-linden
claimed_at: "2026-09-05T21:38:35Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e4123bd40b0efc18b9cd8cb678d8a097f0226b2b
---

## detail

The approach on wk-963dbf6898 says a submission through any other door is 'answered as settled: the token it named, the disposition it carried, and no second token'. What landed at src/engine/pullverb.go:86 is

    a = Answer{Pull: AnswerSettled, Notice: p.ID + " is settled. ..."}

with no Token and no disposition. Measured with a probe on the change: the answer came back pull="settled", token=<nil>, and the only place the id appears is inside an English sentence in the notice.

So a caller that reads the answer as JSON, which is what se pull prints, has to parse prose to learn which token settled, and the test that guards it at src/engine/shellsubmits_test.go:38 asserts a substring of that sentence rather than the token.

## proposed action

Carry the settled token in the answer, the way every other answer that names a token does, and let the notice be prose.

## done when

- a shell submission answers with Token naming the id that settled, proved by a Go test in src/engine that reads said.Token.ID rather than the notice text

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

