---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: can display be refused
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-ligeti-three
claimed_at: "2026-09-05T17:08:37Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 4e86240a3f23bc16872f546b11c19f0706341bb1
---

## detail

Spike: whether a refusal from the event that fires while assistant text is displayed is honoured is undocumented. Fire the event, return a refusal, and watch whether the words still reach the person. Write the answer into doc/levels/level-0-design.md as a ruling either way, replacing the sentence that stands there as SPIKE. If it cannot be measured, say so and say what was tried, and the spike still closes.

## done when

- The event fires during display, a refusal returns, and what the person saw is recorded.
- The SPIKE line in the level-0 doc becomes a ruling, or says what was tried.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One question, one ruling, one comment block. | 1 file |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one is decided by a search of the installed cli.js. Line two names a doc no clone carries, so it is answered by what was tried. | 2 of 2 |
| [x] | the basics it stands on exist, or are minted first | The harness bundle is on this box with its own event list. The level-0 doc is missing, and wk-794865775f carries it. | wk-794865775f |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. A fact about the harness has no red in this tree. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | A search of the installed cli.js for MessageDisplay answered no match, exit 1, on claude-code 2.1.42. The ruling had called that event documented. | rg exit 1 |
| [x] | the same test was seen green after the change, and named | The ruling now rests on the harness's own event list. Both tests in the file passed before and after. | 2 pass, 2 pass |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began 4e86240a is not an object here, so the span reads nothing. The change is one comment. | 1 file |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The SPIKE line stands in a doc no clone carries. wk-794865775f takes the ruling there. | wk-794865775f |

