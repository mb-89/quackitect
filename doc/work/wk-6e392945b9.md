---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: can display be refused
# where the token stands. The process owns these values.
status: open
# what has to be true before this is worth reading again
ready_when: a box that carries dev_guide/levels/level-0-design.md on disk and a person at the harness to watch the display takes it
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 4e86240a3f23bc16872f546b11c19f0706341bb1
---

## detail

Spike: whether a refusal from the event that fires while assistant text is displayed is honoured is undocumented. Fire the event, return a refusal, and watch whether the words still reach the person. Write the answer into doc/levels/level-0-design.md as a ruling either way, replacing the sentence that stands there as SPIKE. If it cannot be measured, say so and say what was tried, and the spike still closes.

## done when

- The event fires during display, a refusal returns, and what the person saw is recorded.
- The SPIKE line in the level-0 doc becomes a ruling, or says what was tried.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | small, and not this box's. Tried once from a cloud session: doc/levels does not exist here. The file the ask names is dev_guide/levels/level-0-design.md, which .gitignore line 21 keeps off git and which is not on this disk. The row is in git at 1da8c0d0^:doc/levels/level-0-design.md, headed OPEN, whether the message-display event can refuse, SPIKE already recorded |  |
| [ ] | every done-when line is decidable, and names the command where one decides it | line 1 wants a harness with a person watching the display while the Notification hook, which util/cage/claude-settings-local.json wires over HTTP, answers a refusal. A cloud session has nobody watching, so it was not fired. Line 2 wants the file, which is not here |  |
| [ ] | the basics it stands on exist, or are minted first | the two basics are the owner's box and the owner's eyes, so the token is parked on ready_when rather than closed on a measurement nobody made |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

