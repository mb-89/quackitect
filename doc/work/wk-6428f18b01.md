---
id: wk-6428f18b01
seq: "-34"
type: work
title: accept with findings
status: imp_done
assignee: cowork
scope: single-step
traced: true
disposition: done
minted_by: cowork
submitted_by: cowork
evidence:
  - outcome
---

## detail

A third verdict, accept_with_findings: the work proceeds and the findings are owed at the next gate. On an implementation it is refused, since the closer is not built.

## done when

- On a spec the token goes to implementation open with the findings on it
  `rg -q func.TestAcceptWithFindingsOpensTheWork src/engine && go test -C src/engine -count=1 -run TestAcceptWithFindingsOpensTheWork$ .`
- A submission answering none of the findings is refused, naming each
  `rg -q func.TestTheObligationsAreOwedAtSubmission src/engine && go test -C src/engine -count=1 -run TestTheObligationsAreOwedAtSubmission$ .`
- On an implementation it is refused, saying the closer is not built
  `rg -q func.TestAnImplementationAcceptWithFindingsIsRefused src/engine && go test -C src/engine -count=1 -run TestAnImplementationAcceptWithFindingsIsRefused$ .`
- It refuses the drafter, a wrong holder, an unrewatched red, and empty findings
  `rg -q func.TestAcceptWithFindingsCarriesTheSameRefusals src/engine && go test -C src/engine -count=1 -run TestAcceptWithFindingsCarriesTheSameRefusals$ .`
- Each test above was watched failing with the verdict absent

## evidence: outcome

judgeSpec in src/engine/pull.go gained the branch, sharing the accept guards, and judge refuses it. All four tests watched red with the branch cut.
