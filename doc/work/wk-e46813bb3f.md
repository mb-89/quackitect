---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: cover build ignores seam
# where the token stands. The process owns these values.
status: open
---

## detail

The toolchain seam in src/engine/testmap.go declares buildCover, realToolchain fills it, and nothing calls it. coverBinary runs go test -c through exec directly, a few lines above. So aFedToolchain feeds runOne only, and every test that takes aTreeWithTests really compiles a module. That is the cost the seam was added to stop, and its comment says the compiler is fed and not run.

Measured at head d50af797: se find --regex buildCover --path src/**/*.go answers three lines, one declaration and two literals, and no call.

Found while working wk-7c41d9e206, which changes what coverBinary's caller does with a build failure and does not touch the build itself.

## done when

- coverBinary builds through theToolchain.buildCover, decided by: se find --regex theToolchain[.]buildCover --path src/**/*.go names a call in testmap.go
- a test that feeds the toolchain compiles nothing, decided by: a fed build counter above zero and no go test -c in the run

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

