---
id: wk-7f0b46d99f
seq: "-9"
type: work
title: criteria are seen red
status: imp_submitted
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-bc3c5ba905
minted_by: person
---

## detail

A criterion is not agreed until somebody has watched it fail.

THE OWNER'S RULE: before a worker submits, it has to have seen the test red.
Not red by some random thing. Red by the assertion it built, with the fix
absent. The reviewer then does the same: it checks the test is red, and if it
is not, or the test is not good, the work does not open for implementation.

RED BY ABSENCE IS NOT RED. go test -run on a name that does not exist answers
ok, no tests to run, and exits zero. A draft went to review with eight command
criteria and every one of them passed with nothing built and no test written. A
linter given a path it ignores does the same. So does a search over a folder
that is not there. Each one reports success for a run in which it asserted
nothing.

WHAT THE ENGINE HOLDS. A criterion carries what was seen when it failed and
what was taken away to make it fail. A criterion with a command and no red
observation is a criterion nobody has tested, and the gate refuses a submission
that rests on one.

TWO PLACES IT IS OBSERVED, and they are different observations. The worker sees
it before the work, with the fix absent. The reviewer sees it again, by putting
the defect back or by building the tree without the fix, and the verdict says
which it did. Neither one alone is enough: the worker's proves the check can
fail, and the reviewer's proves the check is still the one that guards the
behaviour after the work landed.

WHERE IT BITES FIRST. A spec whose criteria cannot go red does not open for
implementation. That is the cheapest place to catch it, because nothing has
been built yet to argue about.

This breaks down the token that put criteria on a token, so what a criterion is
is already agreed. What is added here is that a criterion has to have been seen
to fail.

WHERE THE OBSERVATION GOES, and this is not a detail. A criterion is stored as
a list item with its command under it, and the note is re-rendered from the
parsed token every time it is saved. Prose written into the done when section
is dropped on the next save. Three drafts said in that section that every
command had been run and seen red, and all three lost the sentence between
being written and being read.

So the observation is a field on the criterion and not a paragraph near it. A
criterion carries what was seen when it failed. A criterion with a command and
no red observation is one nobody has tested, and the gate refuses a draft that
rests on one.

## evidence: what was built

A criterion carries what was taken away to make it fail and what it said when it did, and the engine refuses on both sides of that.

THE FIELDS. Criterion.Without and Criterion.Red, src/engine/token.go, with Watched() answering whether somebody has seen this one fail. A criterion with no command is not watched and is not asked to be: it is answered by name in the evidence and a reviewer judges the answer.

THEY ARE FIELDS AND NOT PROSE, WHICH IS THE WHOLE POINT OF THE TOKEN. The note is re-rendered from the parsed token on every save, so a paragraph in the done when section is dropped the next time anything writes the note. Three drafts said in that section that every command had been run and seen red and all three lost the sentence between being written and being read. They are written under the command as **red without** and **red said**, src/engine/store.go, and read back by the same leads.

TWO GATES, IN TWO PLACES, BECAUSE THEY ARE TWO DIFFERENT OBSERVATIONS.

AT THE SPEC: CriteriaThatAlreadyPass, src/engine/spec.go, runs every command criterion and refuses the draft if any exits zero. A criterion that passes before the work cannot report on the work, and the spec is the cheapest place to catch one because nothing is built yet to argue about. That is what red by absence is: go test -run on a name that does not exist answers ok, no tests to run, and exits zero. So does a linter given a path it ignores, and a search over a folder that is not there. Wired at src/engine/pull.go in submitSpec.

AT THE SUBMISSION: UnmetCriteria refuses a command criterion that is not Watched, and the refusal says take the work away, run it, and record what was absent and what it said. Running a command once and seeing green says the command exits zero. It says nothing about whether the command could ever exit anything else.

WHAT THE GATE FOUND THE MOMENT IT LANDED. Four existing tests failed, all for the right reason: their fixture spec carried Runs: exit 0, which is exactly a criterion that passes before the work. The fixture now carries a red command at the spec stage and an observation at the submission stage, which is what the gate asks of a real token.

AND IT IS ALREADY IN USE. Twelve of the thirteen criteria on wk-20c6c329fd carry their observation, and both on wk-61af3a054e do. .se/scratchpad/observe-red.py writes what it watched into watched.json and carry-red.py puts each one under the command it belongs to, so the recording is mechanical rather than remembered.

WHAT IS NOT HERE, and it is the half the detail names second: the reviewer's own observation, made after the work landed by putting the defect back. Nothing in the engine records that yet. A reviewer does it and says so in the verdict, which is where it has been happening all session. It wants its own token and this is not it.

WATCHED RED, ALL SIX, in a git worktree at the commit under test so a reviewer working in the main tree was not running against a half-applied change:

WATCHING THE SIX CHECKS ON wk-7f0b46d99f FAIL, EACH ON ITS OWN ASSERTION.

Run in a git worktree at the commit under test, so a reviewer working in the
main tree was not running against a half-applied change.

THE FOUR THAT ASK THE GATE TO REFUSE. The defect put back was the gate itself,
one at a time: the CriteriaThatAlreadyPass block taken out of submitSpec, the
Watched check taken out of UnmetCriteria, and the two observation fields present
on the struct but neither written to the note nor read back from it.

TestASpecWhoseCriteriaAlreadyPassIsRefused     RED
    red_test.go:30: a draft whose criterion already passes went to review: "wait"
                                               GREEN
TestASubmissionOnAnUnwatchedCriterionIsRefused RED
    red_test.go:71: a submission on an unwatched criterion was taken: "wait"
                                               GREEN
TestHalfAnObservationIsRefused                 RED
    red_test.go:99: a criterion carrying what was taken away, and nothing about what was seen was taken
                                               GREEN
TestTheObservationSurvivesTheNote              RED
    red_test.go:144: what was taken away read back as ""
                                               GREEN

THE TWO THAT ASK THE GATE TO LET SOMETHING THROUGH. A gate that refuses
everything passes every check that asks it to refuse, so these are the ones that
catch a gate drawn too wide, and they need their own red.

TestASpecOfProseCriteriaIsNotRefusedForHavingNoCommand  RED
    with the c.Runs == "" skip taken out, so a prose criterion is run as a command
    red_test.go:55: a spec of prose criteria was refused: 1 of the 1 criteria pass with the work not done
                                               GREEN
TestAWatchedCriterionGoesThrough               RED
    with Watched() returning false always, so no criterion can ever be watched
    red_test.go:114: a watched criterion was refused: 1 of the 1 criteria this token agreed are not met
                                               GREEN

WHAT THE GATE THEN FOUND IN THE TREE. Four existing tests failed the moment it
landed, and all four for the right reason: their fixture spec carried
Runs: exit 0, which is a criterion that passes before the work. The fixture now
carries a red command at the spec stage and an observation at the submission
stage, which is what the gate asks of a real token.

    TestADraftIsAgreedBeforeTheWorkStarts
    TestASubmissionThatMeetsNoCriterionIsRefused
    TestAReviewerCannotStopHoldingADraft
    TestADraftRunsTheFourVerbs
    TestAgreeingADraftOpensImplementation

Then go test ./... on the whole engine: ok, 38.9s.

