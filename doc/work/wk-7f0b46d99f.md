---
id: wk-7f0b46d99f
seq: "-20"
type: work
title: criteria are seen red
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-bc3c5ba905
rounds: 8
evidence:
  - outcome
minted_by: person
submitted_by: main
---

## detail

A criterion is not agreed until somebody has watched it fail. Owner: before a worker submits it must have seen the test red by its own assertion with the fix absent, and the reviewer checks again. Red by absence is not red, since go test -run on a missing name exits zero. A criterion carries what was seen when it failed and what was taken away, as fields and not prose. The done when section is re-rendered on save. The gate refuses a draft or submission resting on a command criterion with no red observation. The reviewer re-watches by putting the defect back or building without the fix, and the verdict says which.

## evidence: outcome

The gate refuses a submission or accept resting on a command criterion with nothing re-watched. The re-watch loop in src/engine/pull.go builds on, every criterion the token carries, and asking, the subset the gate is about, with owed as len(asking). A key outside asking is kept and counted for nothing, a key naming no criterion is refused, and the refusal lists what would settle it. src/engine/answered_test.go drives the paid-plus-extra, unpaid-from-outside, nothing-owed and key-naming-nothing cases, and the battery is green.
