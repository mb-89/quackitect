---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: battery criterion decides nothing
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
# true when this waits for a person rather than an agent
needs_human: true
claimed_by: 7a7c633a/main
claimed_at: "2026-09-07T16:56:49Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 36b584025c544822645906ce678302a2a9bfa6e2
---

## detail

A finding on wk-4ad15a5c86, engine dies under load.

Its third done-when line is "sh util/checks/battery.sh reports no new failure against the run before the change". No agent inside this tree can decide it.

Measured here on 2026-09-05. Run as written, sh util/checks/battery.sh is refused by the tests guard with "THE ENGINE OWNS THE TESTS", which names one door instead: se test --on the token. Through that door the battery runs only when the delta trips a whole rule, and se test --plan --on wk-4ad15a5c86 answers whole false, so the battery does not run. .se/tests holds battery output from 14:02, which is the run before the change, and none after it.

THE DAMAGE. The reviewer is told to run every criterion's command. This one is refused by the tree and unreachable through the door the refusal names, so the token closes with a done-when nobody answered. The same sentence is boilerplate on many tokens, so the same hole is repeated.

THE CHECK. Nothing asserts that a command a done-when names is a command the guards let through. A check reading the commands out of doc/work and running each past the guard catches the class.

## proposed action

Either give the test verb a way to ask for the whole battery on purpose, so the criterion has a door, or stop writing the raw battery.sh call into done-when lines and write the engine's door instead. A person picks, because the first widens the verb.

## done when

- a done-when line asking for the battery names a call the guards let through, decided by: that call run from the root answers rather than being refused by the tests guard
- a check reads every command named in a doc/work done-when line and reports one the guards refuse, decided by: that check answers ok on a tree where none is refused and fails on one where a raw battery.sh line stands
- the failing half of that check is seen red before the change

## evidence: commits

6bc570f0 the check reads a criterion that is a command. On group/tests.

## evidence: criterion 2, the failing half

util/checks/criteria-name-a-runnable-command is the check the line asks for, and it was already in the tree. It read a backticked command, or one after decided-by, or one after the last colon. The standing shape carries none of those, so it passed over every raw battery line and answered ok. It reads the line itself now, where the first word is a program this tree runs.

## evidence: criterion 3

seen red, and precisely: 193 open notes read, 428 commands judged, 16 failed. Fifteen notes carry a raw battery line, and the sixteenth names go test over src/engine.

## evidence: criterion 2, the green half

not met. It asks for a tree where no criterion names a refused command, and fifteen open notes belonging to other hands carry one. Rewriting them is the fork this token's own proposed action puts to a person.

## evidence: criterion 1 is the fork

it asks for a done-when line that names a call the guards let through. Which call that is has not been decided. Either the test verb gains a door for the whole battery, which widens it. Or the line is rewritten to name the door that exists. That choice is the person's, and this token says so.

## evidence: a whole line is not always a command

judging every line whole reported two sentences about files as commands, at wk-36c8a26220 and wk-17f90ad848. Each opens with a path under util/checks, which the guard refuses in a command and which states a fact in a criterion. So the whole line is read only where its first word is an interpreter, a searcher, or go.

## evidence: the check is not in the battery

it is one of the four wk-0bed3ea63b carries, listed nowhere in battery.sh. So it is red on its own rather than a wall every agent waits behind.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

