---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: inline fixture roots measured
# where the token stands. The process owns these values.
status: open
# tokens that have to close before this can start
depends_on:
  - "[[wk-e6568858dd]]"
# the token this is a part of. It cannot close while this is open
parent: [[wk-259aa1500b]]
---

## detail

THE GUARD wk-259aa1500b ASKED FOR NAMES MORE THAN THE THREE FILES THAT TOKEN SANCTIONED. This carries the measurement and the scope it turned out to need.

TestOneFileCarriesTheFixtureBuilders fails on a function whose result type is Roots and sees nothing else. A Roots literal written inline is the same fixture by hand.

Measured at the branch tip, by reading every Roots literal in src/engine outside fixture_test.go. Thirty-two of them, in three shapes. Nineteen are assigned to a variable and built from something computed, which is the fixture shape. Eleven are handed straight to a function. Four are built from string literals alone.

THE THREE THE PARENT NAMED ARE THREE OF THE NINETEEN. The other sixteen are in twelve more files: archivedreadsclosed, archivetravels, claimfar, formatlint, hook, index, owns three more times, readonce, retro, runmeargs, socket twice and swap twice.

So any rule naming the parent's three names sixteen more, and the parent asked for three files to move. Its guard could not be green once the change it sanctioned was done.

TWO THINGS THE PARENT SAID ARE STALE. Its third criterion names log_test.go, which wk-c5b92df740 moved into sessionlog. That case is logprivate_test.go now, built from strings alone. And the guard is already red at the tip under its present rule, for two builders wk-e6568858dd names, so a new red cannot be told from that one until it is cleared.

## proposed action

Say the rule first, as a sentence a reader can disagree with: a Roots literal assigned to a variable, whose fields are not all string literals, is a fixture root and belongs to the builder. A literal handed straight to a function, and one built from strings alone, are not. Then clear wk-e6568858dd so the guard is green under its present rule, extend the guard to that sentence and watch it redden on nineteen, and move all nineteen onto the builder. Split the move by file if one change is too large to review whole.

## done when

- the guard names an assigned Roots literal whose fields are not all string literals, watched red on the nineteen sites before any caller moves: go test -C src/engine -run TestOneFileCarriesTheFixtureBuilders
- no Roots literal outside fixture_test.go is assigned to a variable from something computed, decided by the same run answering ok
- a Roots built from string literals still passes, decided by the guard staying green over logprivate_test.go and onedoorperfolder_test.go
- a Roots handed straight to a function still passes, decided by the guard staying green over onedoorperfolder_test.go and schema_test.go

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the approach is on the token before any work, as an interface or a shape a reader can disagree with |  |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the change is small enough to review whole, or it is split first | — |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | the change follows the approach on the token, or the token says why it departed |  |  |
| [ ] | se test --on this token answered ok, and what it ran is named |  |  |
| [ ] | the note says what changed and why, for a reader who was not here |  |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | [[reviewing]] was read and applied | — |  |
| [ ] | every hunk of git diff began..ended was read, and any not read is named |  |  |
| [ ] | every criterion's command was run again, and what it said is named |  |  |
| [ ] | every hunk improves the product, or a finding names the one that does not |  |  |
| [ ] | every finding is a trivial token naming this one, and their ids are here |  |  |

