---
id: wk-bb34ab1208
seq: "23"
type: work
title: ticking makes groups
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-66a28ca311
rounds: "3"
evidence:
  - outcome
minted_by: person
---

## detail

A press on a row ticks it, every column included, and only the underlined title text opens the note. The owner asked for the underline rather than the v3 double press. With rows ticked, one control files them into a bucket, the person's own name for a group. The status stays as it was, so the view groups by if(bucket, bucket, status). The bucket is made first and named afterwards, and an empty name asks the engine for a fresh one. Renaming needs a bucket and not merely a selection. Ported from v3 engine/workclient.ts lines 190 to 345. Related: wk-66a28ca311.

## evidence: outcome

Row presses tick and the title text opens the note. The toolbar button is bs-make-bucket, a name nothing else answers to, so presses inside the Filter popover no longer file ticked rows into a bucket. drive-editor.mjs ticks a row, presses every control in the popover and requires no group message, then presses the toolbar button and requires exactly one. render-check.mjs names the new class and sh .se/scratchpad/battery.sh answered all ok.
