---
id: wk-8573243384
seq: "-27"
type: work
title: inherited checks are watched
status: spec_open
assignee: main
scope: single-step
traced: true
rounds: 5
minted_by: reviewer6
---

## detail

CLASS: WORK ADOPTED ON THE STRENGTH OF ITS TESTS, WITH NONE OF THEM RUN AGAINST
THE DEFECT IT NAMES.

Somebody else's branch is read, the commit messages are good, each commit
carries a test, and the verdict is taken. "Each carries its own test" is a claim
about those tests, and it is made by reading them. A test that is read is a test
that is green. Green is what a test that cannot fail looks like too, and the
whole reason for adopting the work was that it guards something.

THE SAME RULE THIS PROJECT ALREADY APPLIES TO ITS OWN WORK. A check nobody has
watched fail is a check nobody has tested, and the engine now refuses a
submission that rests on one. Inherited work is where the rule is easiest to
forget, because the check arrives already green and there is no moment at which
somebody had to make it go red.

FOUND ON wk-c6247665a3, the judgment on thirteen upstream commits. The verdict's
reason is that every one of them is a hole a Linux clone actually fell into and
each carries its own test. The central one is 6522bcbb's
TestNoCommittedProjectionNamesTheMachine, src/engine/cage_test.go, whose whole
subject is that a committed cage file must not name the machine it was written
on -- the exact fault the upstream work exists to fix, a Windows path reaching a
Linux box.

IT CANNOT FAIL ON WINDOWS. It asks strings.Contains(committed, root), where root
is filepath.Abs(...) and therefore
"C:\\Users\\ichbi\\Desktop\\ai\\quackitect-v4", and the projector writes
paths through filepath.ToSlash, so the file says
"C:/Users/ichbi/Desktop/ai/quackitect-v4". Two spellings of one path, and the
check looks for the one nothing writes.

MEASURED AT ecb7857b, THE MERGE COMMIT, and pinned there rather than to the word
today. Three committed projections named this machine in the spelling the
projector uses -- .claude/settings.json, .mcp.json and .copilot/mcp-config.json
-- and go test was green over all three. .claude/settings.json named it on
fourteen lines, so the check arrived green over a live violation in the same
commit that introduced it. At HEAD it names it on none.

WHAT TO DO INSTEAD. When you adopt work because it is guarded, break what each
guard guards and watch it go red before counting it as a reason. One pass, one
defect per check, and the reason for taking the work then rests on something
somebody saw. If a check cannot be made to fail in a few minutes, that IS the
finding.

AND THE NARROWER LESSON UNDER IT, WHICH IS PART OF THE DELIVERABLE AND HAS A
CRITERION OF ITS OWN: a check that compares two spellings of the same thing -- a
path with either separator, a name in either case, a URL with or without its
trailing slash -- must be written in the spelling the producer writes, or
normalise both sides. Ask what the artefact actually contains, by opening it,
rather than what your own language hands you.

IT IS THE HALF THAT PREVENTS RATHER THAN CATCHES, which is why it is not
dropped. The five steps say how to catch the mistake, one check at a time, after
somebody has already leaned on it. This one tells whoever writes the check to
open the artefact before choosing what to compare, which is the thing that was
not done upstream. A rule that shipped only the catching half would teach the
more expensive of the two.

HOW THE MEASUREMENT IS WRITTEN, AND THE OTHER ROUTE WAS REFUSED. The two
spellings cannot appear in the prose. This file is projected into three
harnesses and TestNoCagedFileNamesTheMachineItWasWrittenOn refuses a projected
file that names this machine, so the criterion asking for the literal path and
the battery are two states this token cannot have at once. Moving the rule out
of a projected file was the other route offered and it is refused here, because
the whole point of WHERE IT GOES is that every harness reads it. So the fact is
written as the two functions that produce the two spellings, filepath.Abs and
filepath.ToSlash, and the check requires both names inside the section. A
function name is not a topic word: nothing else in that section would carry it,
which is what separates this from the noun-search round 1 rejected.

THE FIX HERE LANDED IN 2dd4c19d AND NOTHING OF IT IS OWED. That commit put both
spellings into namesTheMachine in src/engine/cage_test.go, both tests call it,
the three cage files were re-projected, and no {{mcp}} placeholder survives. An
earlier draft of this paragraph told a worker to watch that check go red today,
and it is green on nothing, which reads either as a check that cannot fail or as
a worker looking in the wrong place. This token owes the rule and owes no code.


THE REMEDY IS A RULE AN AGENT READS, NOT A CHANGE TO A PROGRAM. Nothing in the
engine can tell an inherited check from one written here, and a rule that asked
it to would be a word list fitted to the branch we happened to merge. What can
be written down is what to do before leaning on somebody else's test.

WHERE IT GOES. doc/guidance/behaviour.md, under the section that already says
the check comes first, because this is the same rule at the moment the check
arrives already green rather than at the moment it is written.

WHAT IT SAYS. Before a verdict rests on the reason that each commit carries its
own test, run the ones the reason leans on against the defect they name: break
what the check guards, watch it go red, put it back, watch it go green, and
write the two lines into the report under the commit that brought it. Not all of
them and not for long. The ones the verdict's reason turns on.

AND WHY IT IS WORTH THE TIME, WITH THE MEASUREMENT THIS CAME FROM. Thirteen
commits were adopted on that reason. The check the cage work turns on compared
every projected line against this machine's root as filepath.Abs spells it, with
backslashes, and every file the cage writes uses forward slashes. It looked for
the one spelling nothing writes. On Linux the two are the same string, which is
why it passed review upstream and arrived green here. It could not fail on this
platform for the defect it exists to catch, and the moment it was fixed it found
three committed cage files naming this machine.


THE CRITERION ABOUT REACHING EVERY AGENT WAS AN OR AND NOT AN AND, which is the
finding round one raised and it was right. rg -q PATTERN a b c exits zero when
ANY of the three matches, so a rule written into one harness file would have
turned it green while the other two had nothing. It walks the three now and
fails on the first miss.

THE CLASS IS IN THE METHOD, in doc/guidance/specifying.md beside A SET COVERED
BY ONE MEMBER: the tell is whether the tool's exit code is about any or about
every, and where it is about any the command is one member's however many you
list.

AND THE LIST ITSELF WAS THE SECOND HALF, which round two raised and it was right
as well. A loop over three typed harness files is a photograph of a set that
util/projections.json declares, seven projections of which three take their
sources from doc/guidance. Declare a fourth from behaviour.md and the criterion
stays green with the rule missing from the harness that does not have it, which
is the state its own sentence forbids. The targets are derived now, by the
helper this tree already carries, projectionTargets in src/engine/search_test.go.


THREE CRITERIA SAID THREE THINGS EACH AND THEIR COMMANDS DECIDED ONE WORD, which
is round one's finding and it is right. A search for the word backslashes turns
green on any sentence in the file, including one carrying none of the three
facts. A search over the whole file cannot see which section a rule is in, and
this file has many sections. MEASURED AT a1fdc0f3, the commit this redraft is
taken at: wc -l answers 243, a count of lines opening with a hash answers 13,
six of them section headings and six sub-headings under the document title. The
two numbers this paragraph used to carry, a hundred and ninety lines and eleven
headings, were true of no commit: the file last held 11 heading lines at
ee592d7c when it was 175 lines, and its nearest approach to 190 was 195 lines at
5e023176, where it held 12. The argument does not need either number, so the
sentence no longer carries one.

IT MATTERS MORE HERE THAN USUAL BECAUSE THE DELIVERABLE IS PROSE. Where the
thing being made is a paragraph, the string and the claim are the only two
things there are, so a check on the string is the whole check.

SO EACH CRITERION IS ONE CLAIM AND ITS CHECK READS THE SECTION. A test reads
doc/guidance/behaviour.md, finds the heading the rule belongs under, takes the
section that heading opens, and asks its own question of that text. One
criterion, one question, one section.

## done when

- The rule is written under the heading about the check coming first, and not merely somewhere in the file. The check reads the section that heading opens and finds the rule inside it
  `rg -q func.TestTheInheritedCheckRuleIsWhereItSays src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleIsWhereItSays$ .`
- It says what to do rather than what to feel, and the section carries every step the detail names under WHAT IT SAYS. THE NUMBER IS NOT IN THE SENTENCE, because a count beside a list is two things that can disagree and this one did, saying four over a list of five. The check holds the list: one assertion per step, failing by name on the step that is absent
  `rg -q func.TestTheInheritedCheckRuleSaysWhatToDo src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleSaysWhatToDo$ .`
- It carries the measurement it came from rather than the claim alone: that the check compared against the root as filepath.Abs spells it while the projector writes every path through filepath.ToSlash, and that the two are one string on the platform it passed review on. THE TWO SPELLINGS ARE NAMED BY THE FUNCTIONS THAT PRODUCE THEM AND NOT WRITTEN OUT, because this file is projected and the cage guard refuses a projected file that names this machine. The check requires both function names inside the section, which a topic word cannot satisfy
  `rg -q func.TestTheInheritedCheckRuleCarriesItsMeasurement src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleCarriesItsMeasurement$ .`
- The narrower lesson is in that section too, and it is the half that prevents rather than catches: a check comparing two spellings of one thing is written in the spelling the producer writes or normalises both sides, and you ask what the artefact contains by opening it. The check looks for something only that rule would carry rather than for the noun that names the subject
  `rg -q func.TestTheInheritedCheckRuleSaysToOpenTheArtefact src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleSaysToOpenTheArtefact$ .`
- The rule reaches every agent, and the harnesses are derived rather than typed: the check asks util/projections.json for every target whose sources include doc/guidance/behaviour.md, refuses when that answers none, and requires the rule in each one, failing by name on the first miss. projectionTargets in src/engine/search_test.go already answers that question and is called rather than retyped
  `rg -q func.TestTheInheritedCheckRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleReachesEveryProjection$ .`
- It is scoped rather than universal, and the section says so in as many words: the checks a verdict's reason leans on, not every test on a branch, and a sentence naming the cost of the universal reading. The check reads that section and asks for the scope and the cost
  `rg -q func.TestTheInheritedCheckRuleIsScoped src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleIsScoped$ .`
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. For the steps criterion the red is taken once per step, each step removed from a copy of the section in turn, and the failure names that step. For the projection walk the change taken away is the rule, removed from one derived target, and the red names that file. THE EVIDENCE NAMES THE TEST AND WHAT IT SAID RATHER THAN A LINE NUMBER, because a line moves when anybody adds a function above it and the message does not

## finding 1 · round 1 · done when: The rule reaches every agent, because the guidance is projected and a rule only one harness reads is a rule the others do not have / runs: rg -q inherited.check AGENTS.md .github/copilot-instructions.md .claude/output-styles/quackitect.md · by reviewer6

**wrong:** THE COMMAND IS GREEN IN THE ONE CASE THE CRITERION'S OWN SENTENCE FORBIDS. Criterion 4 reads "The rule reaches every agent, because the guidance is projected and a rule only one harness reads is a rule the others do not have", and its command is rg -q inherited.check AGENTS.md .github/copilot-instructions.md .claude/output-styles/quackitect.md. rg exits zero when ANY of the paths matches, not when every one does. I ran it rather than assuming it: three files, the pattern written into the first only, rg -q answers exit 0; with the pattern in none of them, exit 1. So the criterion goes green with the rule in one harness and missing from two, which is the exact state the sentence says is a rule the others do not have. THE CASE THAT MATTERS IS THE ONE IT CANNOT SEE. All three targets are projections of the same two sources -- util/projections.json gives AGENTS.md, .github/copilot-instructions.md and .claude/output-styles/quackitect.md each as doc/guidance/voice.md plus doc/guidance/behaviour.md -- so writing the rule into behaviour.md is not the risk. The risk is a re-projection that did not reach all three, which is a live shape in this tree: 2dd4c19d's own message says two engines from an older build were re-projecting and one wrote a stale file straight back. A partial projection is exactly what criterion 4 exists for and exactly what rg -q cannot report. IT IS wk-10d3cf13cd's CLASS BY A NEW ROAD, and that is why it reads as safe. There the sentence was about a set and the command named one member, visible on the page. Here the command names all three, so it looks like the remedy, and the tool ORs them one level down. A reviewer sees three filenames and stops, because three filenames is what walking a set looks like. EXTENT, one pass over the five criteria: this is the only command that names more than one path, so it is the only one with this shape, and criteria 1, 2 and 3 have a different problem which is my second finding.

**satisfies:** WRITE THE LOOP AND FAIL ON THE FIRST MISS: for f in AGENTS.md .github/copilot-instructions.md .claude/output-styles/quackitect.md; do rg -q inherited.check "$f" || exit 1; done -- and say in the criterion that it fails on the first miss, so the next reader does not have to work out what rg does with several paths. BETTER, DERIVE THE THREE RATHER THAN TYPING THEM, the way this project already derives a commit range: read util/projections.json for every target whose sources include doc/guidance/behaviour.md and walk those, so a fourth projection added next month is covered without anybody remembering. Say in the criterion which of the two you chose, because a typed list of three is a promise that there are three. AND RUN IT AGAINST A CASE THAT OUGHT TO FAIL BEFORE AGREEING IT, which is the thing that would have caught this: take the pattern out of two of the three and run the command. The rg form stays green. The loop form goes red and names the first file that lost it. Put what each answered into the draft.

## finding 2 · round 1 · done when: The rule is written where an agent reads it, in the section about the check coming first / runs: rg -q inherited.check doc/guidance/behaviour.md AND done when: It carries the measurement it came from rather than the claim alone / runs: rg -q backslashes doc/guidance/behaviour.md · by reviewer6

**wrong:** THREE CRITERIA SAY THREE THINGS EACH AND THEIR COMMANDS DECIDE ONE WORD. Criterion 2 says "It carries the measurement it came from rather than the claim alone: the spelling that was looked for, the spelling every cage file writes, and that the two are one string on Linux" -- three separate facts -- and its command is rg -q backslashes doc/guidance/behaviour.md. One occurrence of the word backslashes anywhere in a 190-line file turns it green, in any sentence, including one that carries none of the three facts. Criterion 1 says the rule is written "in the section about the check coming first" and "says to run an inherited check against the defect it names before a verdict rests on it", and its command is rg -q inherited.check over the whole file. It cannot see which section -- doc/guidance/behaviour.md has eleven headings and the one meant is line 17, The check comes first -- and it cannot see what the rule says. Two of its three clauses are decided by nothing. THIS IS THE SHAPE THE PROJECT ALREADY NAMES, doc/guidance/specifying.md, A LITERAL NOBODY DERIVED: the search proves the string is present and says nothing about whether the claim is true. It matters more here than usual because the deliverable IS prose, so the string and the claim are the only two things there are, and a command that cannot tell them apart is checking nothing at all. I CONSTRUCTED THE DOCUMENT THAT PASSES AND FAILS THE SENTENCE, which takes three lines: add to the Stopping section at the foot of behaviour.md the sentence "An inherited check written on a machine with backslashes is not our concern here, and we do not watch it go red and then green." That contains inherited check, backslashes and watch it go red and then green, so criteria 1, 2 and 3 all go green, and the rule is not in the right section, carries none of the measurement and tells nobody to do anything. CRITERION 3 IS THE ONE THAT NEARLY WORKS, because watch.it.go.red.and.then.green is a distinctive phrase rather than a common word, and I am not asking you to change it beyond anchoring it the way the others need.

**satisfies:** ANCHOR THE SEARCH TO THE SECTION, AND SEARCH FOR THE CLAIM RATHER THAN A WORD IN IT. For the section: rg -q -A 30 '^### The check comes first' doc/guidance/behaviour.md and require the phrase inside that window, or give the rule its own heading and grep the heading, which is simpler and is what the neighbouring rules in that file already do -- each of the four is its own ### heading. For the measurement: three facts want three patterns, one per fact, joined so the criterion fails on the first miss -- the backslash spelling, the forward-slash spelling, and that the two are one string on Linux. Grep for something only a true sentence would carry, such as the two spellings written out, and not for the noun that names the topic. AND CHECK IT THE WAY THE CRITERION ITSELF DEMANDS: write the three-line decoy above into a copy of behaviour.md, run all five commands against the copy, and put the count in the draft. If it answers five green, the commands are not deciding the sentences, and the count is the shortest way to say so. That is the same instrument this token is about to write into the guidance for everybody else, applied to itself.

## finding 3 · round 2 · detail: THE FIX HERE, AND THE CHECK, RED TODAY ... That is red today on three files · by reviewer8

**wrong:** The detail's instruction paragraph describes a tree that has not existed for eleven commits, and I reproduced every number in it. It says "THE FIX HERE, AND THE CHECK, RED TODAY: compare against filepath.ToSlash(root) as well as root, in both TestNoCommittedProjectionNamesTheMachine and TestNoCagedFileNamesTheMachineItWasWrittenOn. That is red today on three files. Then re-project them so they carry no machine path, and watch it green. The two generated mcp files also carry an unexpanded {{mcp}} placeholder as the command." All of it is already done, by commit 2dd4c19d "The inherited cage checks are run against the defect they name, and one was dead": src/engine/cage_test.go now holds namesTheMachine, which walks both spellings -- for _, spelling := range []string{root, filepath.ToSlash(root)} -- and both tests call it. I counted the files rather than trusting the paragraph: .claude/settings.json named this machine on fourteen lines at ecb7857b and on none at HEAD, .mcp.json and .copilot/mcp-config.json name it on none, and no {{mcp}} survives in either at HEAD or at ecb7857b. So the check is green on zero files rather than red on three, and 2dd4c19d is eleven commits before 694def52, the commit that wrote this redraft, so the paragraph was already false when round 1's detail was carried forward into round 2. The cost is not tidiness: a worker told to watch a named check go red will find it green, and the two readings available then are that the check cannot fail -- the class this very token is about -- or that they are looking in the wrong place. The same paragraph is also the clause no criterion covers: all six criteria are about the guidance prose, so nothing decides whether this instruction was carried out, was already carried out, or was dropped, which is exactly how a stale instruction survives a round.

**satisfies:** Re-take every reading in the detail before this goes out again, not only the parts a finding touched, and then do one of two things with this paragraph. Either delete it and say in its place that the fix landed in 2dd4c19d, naming the commit, so a reader can tell work already done from work still owed; or, if any of it is still owed, say which part and give it a criterion whose command decides it. And date the measurements that stay: the historical reading is the interesting one and it is true, so pin it to the commit it was read at -- "at ecb7857b .claude/settings.json named this machine on fourteen lines and the check was green" -- rather than to the word today, which is a timestamp that ages without anybody noticing. The same pass applies to "MEASURED, TODAY: three committed projections name this machine ... and go test is green over all three", which is false at HEAD in the present tense and true at ecb7857b.

## finding 4 · round 2 · done when, criterion 4: The rule reaches every agent, because the guidance is projected and a rule only one harness reads is a rule the others do not have · by reviewer8

**wrong:** Criterion 4 types the members of a set this tree already enumerates, so it is green in a state its own sentence forbids. The sentence is "The rule reaches every agent, because the guidance is projected and a rule only one harness reads is a rule the others do not have", and the command is for f in AGENTS.md .github/copilot-instructions.md .claude/output-styles/quackitect.md; do rg -q inherited.check $f || exit 1; done. The loop fixes round 1's any-not-every defect and I confirmed it does fail on the first miss, so that half is answered; what is left is the list. I read util/projections.json: it declares seven projections, of which exactly three take their sources from doc/guidance, so the typed three are right today and are a photograph of a set that is declared in a file the check could simply read. Declare a fourth projection from doc/guidance/behaviour.md and the criterion stays green with the rule missing from the harness that does not have it, which is word for word the state the sentence says must not exist. The remedy is already written in this tree and was not called: src/engine/search_test.go carries projectionTargets, which reads util/projections.json and answers every target whose sources include a given guidance file, used by TestTheSearchRuleReachesEveryProjection under a doc comment reading "A HAND-DRAWN LIST IS EXACTLY THE SIZE OF WHAT YOU ALREADY LOOKED AT. Declare a fourth projection from the same source and the claim stays true of its three while the fourth goes without the rule." This is not a new opinion: round 1's own satisfies said "BETTER, DERIVE THE THREE RATHER THAN TYPING THEM ... read util/projections.json for every target whose sources include doc/guidance/behaviour.md and walk those". Extent, in one pass over the four specs now waiting: wk-02e17b9eb4 carries the identical typed list at line 100, so this reaches two of the four and not one.

**satisfies:** Derive the targets from util/projections.json instead of typing them. The shortest route is a Go test, because the helper exists: call projectionTargets(t, root, "doc/guidance/behaviour.md"), fail if it answers none so the check cannot pass by the declaration having gone, and require the rule's phrase in every target it answers, exactly as TestTheSearchRuleReachesEveryProjection already does. If it stays a shell command, read the declaration in the command -- select every projection whose sources include doc/guidance/behaviour.md, pipe the targets into the loop, keep the || exit 1, and refuse when the derived list comes back empty. Either way, run it against the case that ought to fail before agreeing it, which is the step that decides this: add a fourth projection sourced from behaviour.md to util/projections.json, or take the phrase out of one existing target, and watch the derived form go red and name the file while the typed form stays green. Put what each answered into the draft.

## finding 5 · round 2 · done when, criterion 5: It is scoped rather than universal: the checks a verdict's reason leans on, and not every test on a branch · by reviewer8

**wrong:** Criterion 5 is prose in a spec that has just demonstrated, three times over, that this exact claim can be a command, and it is the clause most likely to be dropped. It reads "It is scoped rather than universal: the checks a verdict's reason leans on, and not every test on a branch. The evidence says why, because a rule nobody can afford is a rule nobody keeps". Its first half is a claim about a string in a named section of doc/guidance/behaviour.md, which is precisely what criteria 1, 2 and 3 decide by running a Go test that finds the heading, takes the section it opens, and asks its own question of that text; only the second half, whether the evidence explains why, needs a reader. So the deliverable's scoping sentence is the one part of it that no command decides, and scope is the part a drafter loses first: a rule written as "run an inherited check against the defect it names" reads complete, passes criteria 1, 2 and 3, and is the unaffordable universal version the detail spends a paragraph refusing -- "Not all of them and not for long. The ones the verdict's reason turns on." I checked the tree for the state this would let through: doc/guidance/behaviour.md line 17 opens The check comes first and carries nothing about inherited checks at all today, so the section will be written from scratch, and nothing in the criteria makes the writer choose between the scoped rule and the universal one.

**satisfies:** Split it, and give the checkable half a command in the shape the other three already use: a test, say TestTheInheritedCheckRuleIsScoped, that takes the section the heading opens and requires it to carry the limit -- the checks the verdict's reason leans on, and not every test on the branch -- searching for something only a scoped sentence would contain rather than for the word scope. Leave the second half, that the evidence says why, as the prose clause a reviewer judges, and say in the sentence that it is the half no command decides. Then test the command the way this token's own subject demands: write the universal version of the rule into a copy of behaviour.md, run all the criteria against the copy, and if the scoping one stays green it is not deciding its sentence. Put that count in the draft.

## finding 6 · round 3 · done when, criterion 3: "It carries the measurement it came from rather than the claim alone: the spelling that was looked for, the spelling every cage file writes, and that the two are one string on the platform it passed review on. All three facts, and the check asks for each" · by reviewer9

**wrong:** The criterion asks the guidance to carry a value this tree already refuses to let into that file. The two spellings are two spellings of this machine's root, and doc/guidance/behaviour.md is a source of three projections, AGENTS.md, .github/copilot-instructions.md and .claude/output-styles/quackitect.md. src/engine/cage_test.go carries TestNoCagedFileNamesTheMachineItWasWrittenOn, which assembles each projection from its sources and fails on any line holding the root in either spelling, and TestNoCommittedProjectionNamesTheMachine, which reads the committed targets and does the same. I reproduced it rather than reasoning about it. I copied doc, util, src/engine and the three targets into a scratch tree, confirmed both guards green there, then wrote one sentence into doc/guidance/behaviour.md under The check comes first carrying that copy's root in both spellings. TestNoCagedFileNamesTheMachineItWasWrittenOn went red on all three projections at cage_test.go:59, naming the line. So criterion 3 green and the battery green are two states this token cannot have at once. The other reading, naming the separator rather than the path, is the one round 1 refused in as many words: its satisfies asked for the two spellings written out and not for the noun that names the topic. The criterion does not say which reading it means, so the worker meets the contradiction at the keyboard and settles it alone, and the settlement that makes both pass is the loose search this token spent a round removing. The guard that refuses it is the same guard the token's whole story is about.

**satisfies:** Decide what stands in for the literal and write it into criterion 3, because a drafter can change a criterion and a worker cannot. One route: keep the two spellings out of the prose and put the fact in a form the guard permits, such as the two separators named beside the function that produces each one, filepath.Abs and filepath.ToSlash, and have the check require both function names in that section rather than the word backslashes. Another: keep the literals and move the rule out of a projected file, which changes where it goes and so wants saying in the detail. Whichever you take, say in the criterion how the check tells the substitute from a topic word, since that is the half round 1 rejected. Then run it before agreeing it: write the sentence into a copy of the tree, run the two cage guards in that copy, and put what they answered into the draft. They answered green before I wrote the sentence and red on three projections after it.

## finding 7 · round 3 · done when, criterion 2: "It says what to do rather than what to feel: break what the check guards, watch it red, put it back, watch it green, and write the two lines under the commit that brought it. All four steps, in that section" · by reviewer9

**wrong:** The criterion lists five steps and then says four, so the check decides which one it drops. Its own sentence enumerates break what the check guards, watch it red, put it back, watch it green, and write the two lines under the commit that brought it. That is five. The detail says the same five under WHAT IT SAYS. A worker writing TestTheInheritedCheckRuleSaysWhatToDo reads the count before the list, asserts four phrases, and the criterion is green with one step missing from the guidance. The step most likely to go is the fifth, because it is the only one that leaves anything behind: without it the rule tells somebody to run a check and never to record what they saw, which is the whole product this token is minted for. This is doc/guidance/voice.md under Claims, derive a count and never retype one, applied to a criterion instead of to a note. Extent, one pass over the six criteria: two of them carry a number, criterion 2 and criterion 3, and criterion 3 recounts, since it says all three facts over a list of three. Criterion 2 is the only one whose number disagrees with its own list.

**satisfies:** Count the list and write what it answered: all five steps, or drop a step from the list if one of them is not being asked for. Better, take the number out of the sentence and let the check hold the list, so the two cannot disagree: say the section carries every step the detail names under WHAT IT SAYS, and have the test assert one phrase per step and fail naming the step that is absent. Then watch it red once per step rather than once for the criterion: take each of the five out of a copy of the section in turn and check that the failure names that step.

## finding 8 · round 3 · detail: "A search for the word backslashes turns green on any sentence in a hundred and ninety lines, including one carrying none of the three facts. A search over the whole file cannot see which section a rule is in, and this file has eleven headings" · by reviewer9

**wrong:** Two present-tense readings of doc/guidance/behaviour.md were carried into this redraft without being taken again, and both are wrong about the tree. I counted rather than trusting them. At HEAD, b5daecb6, the file is 243 lines and carries 13 lines beginning with a hash, twelve of them below the document title. I walked its history: it last carried 11 heading lines at ee592d7c, when it was 175 lines, and the nearest it came to 190 lines was 195 at 5e023176, where it carried 12. So no commit ever held both numbers, and neither describes the file the rule is about to be written into. This is round 2's own lesson on this token, wk-4c3586254a, one round later and in the same section of the same detail: re-take every number and every present tense before submitting a redraft, not only the parts a finding touched. Round 2's finding 3 named one paragraph, the red today instruction, and that paragraph was fixed properly and pinned to ecb7857b. These two sentences sit four paragraphs below it and were left as they were. It matters because the numbers are the argument: they are what says a whole-file search is too loose, and a reader who recounts them finds the file half as long again as the sentence claims and stops trusting the paragraph that carries the reasoning.

**satisfies:** Re-take both readings and paste what the commands answered, with the commit they were taken at, the way the ecb7857b paragraph in this same detail already does. The argument does not need either number, so the cheapest fix is to drop them and say the file has many sections and a whole-file search cannot tell which one a rule is in. If you keep them, run wc -l and a count of heading lines at the commit you are drafting from and write both answers down beside the sha. And make the pass the lesson asks for rather than the pass the finding asks for: read every sentence of the detail that carries a number or a present tense, and say in the evidence how many you re-took and which ones moved.

## finding 9 · round 4 · done when, criterion 3: "It carries the measurement it came from rather than the claim alone: the spelling that was looked for, the spelling every cage file writes, and that the two are one string on the platform it passed review on. All three facts, and the check asks for each" · by reviewer10

**wrong:** Criterion 3 came back byte for byte as round 3 rejected it. I compared the strings rather than reading them: the criterion on the list and the text finding 6 quotes in its own clause are the same string, which I checked with a substring test over the note. Round 3 said that criterion asks the guidance to carry a value a standing check keeps out of that file, and its lesson, minted as wk-b999022075, is that class. Nothing on the token moved for it: not the criterion, and not a sentence in the detail deciding which reading is meant. I reproduced the contradiction rather than taking it from the finding. I copied src/engine, doc, util and the three projection targets into a scratch tree and ran TestNoCagedFileNamesTheMachineItWasWrittenOn and TestNoCommittedProjectionNamesTheMachine there, and both passed. I then wrote one sentence under The check comes first in that copy's doc/guidance/behaviour.md carrying that copy's own root in both spellings, the backslash one and the forward-slash one. TestNoCagedFileNamesTheMachineItWasWrittenOn failed three times, once per projection, naming agent protocol in AGENTS.md, copilot instructions in .github/copilot-instructions.md and claude output style in .claude/output-styles/quackitect.md, each with the line it found and the spelling it found there. So criterion 3 read literally and the battery are two states this token cannot have at once. Criterion 4 makes that worse rather than better: it now derives its targets from util/projections.json, which answers exactly those three, so the criterion that would carry the literal into every projected harness is the one this round strengthened. And criterion 3 still does not say which reading it means, the literal path or the separators named, so the worker meets the contradiction at the keyboard and settles alone what a drafter could have settled here, which is word for word what round 3's lesson says happens next.

**satisfies:** Change criterion 3, because a drafter can change a criterion and a worker cannot, and say in the detail which route was taken and why the other was refused. Round 3 already offered two and either will do. Keep the two spellings out of the prose and put the fact in a form the guard permits, such as the two separators named beside the functions that produce each one, filepath.Abs and filepath.ToSlash, with the check requiring both function names inside the section rather than a topic word. Or keep the literals and move the rule out of a projected file, which changes WHERE IT GOES and wants saying there. Whichever you take, say in the criterion how the check tells the substitute from a topic word, since a search for the noun that names the subject is what round 1 rejected. Then run it before agreeing it, the way I did: write the sentence you intend into a copy of the tree, run the two cage guards in that copy, and paste what they answered into the draft. Mine answered green before the sentence and three failures after it, one per projection, each naming the file and the spelling.

## finding 10 · round 4 · detail: AND THE NARROWER LESSON UNDER IT, and WHAT IT SAYS, against done when, criteria 1 to 6 · by reviewer10

**wrong:** The detail names two rules and the criteria ask for one. WHAT TO DO INSTEAD names the adoption rule, break what each guard guards and watch it go red before counting it as a reason, and then names a second under it: a check that compares two spellings of the same thing, a path with either separator, a name in either case, a URL with or without its trailing slash, must be written in the spelling the producer writes or normalise both sides, and ask what the artefact actually contains by opening it. I read all six criteria against that sentence. Criterion 1 asks for placement. Criterion 2 asks for every step the detail names under WHAT IT SAYS, and WHAT IT SAYS names the five adoption steps and nothing about spellings. Criterion 3 asks for the measurement the rule came from, which is the cage path and not a rule about comparing spellings. Criteria 4 and 5 ask for reach and for scope. Criterion 6 asks for reds on the five tests above. So nothing on the list decides whether the narrower lesson is written, was judged out of scope, or was forgotten, and the detail says nothing either, which is the shape this token's own round 2 lesson names, wk-4c3586254a: anything the detail still instructs gets a criterion or it is not being asked for. It matters more than an ordinary omission here. The five steps say how to CATCH the mistake, one check at a time, after somebody has already leaned on it. The narrower lesson is the half that would stop it being made, because it tells whoever writes the check to open the artefact and see what it contains before choosing what to compare, which is the thing that was not done upstream. A rule that ships only the catching half teaches the more expensive of the two.

**satisfies:** Decide it and write the decision down. If the narrower lesson is part of the deliverable, add it to WHAT IT SAYS so criterion 2's list carries it and the check asserts it as one more step, or give it a criterion of its own in the shape the other five use: a test that takes the section the heading opens and requires the rule about two spellings of one thing, searching for something only that rule would carry rather than for a topic word, and failing by name when it is absent. If it is out of scope, say so in the detail beside WHERE IT GOES with the reason, the way the detail already tells work already done from work still owed for 2dd4c19d. Either way take its red the way criterion 6 asks for the others: cut that rule out of a copy of the section, run the check, and record the test and what the failure said.

## finding 11 · round 5 · done when, criterion 3: "The check requires both function names inside the section, which a topic word cannot satisfy", against the same criterion at round 4, which ended "All three facts, and the check asks for each" · by reviewer10

**wrong:** The rewrite answers the finding and drops the clause that said what the check has to ask for. The old criterion 3 is in the record twice, quoted verbatim in the clause of finding 6 and again in the clause of finding 9, and it ended: All three facts, and the check asks for each. The new one ends: The check requires both function names inside the section, which a topic word cannot satisfy. I put the two sentences side by side rather than reading the new one alone. The three facts are all still in the sentence, correctly: the root as filepath.Abs spells it, the projector writing every path through filepath.ToSlash, and the two being one string on the platform it passed review on. What is gone is the only line saying the check asks for each of them, and the line that replaced it names two strings. So a test that finds filepath.Abs and filepath.ToSlash inside the section satisfies everything criterion 3 says about the check, and the third fact ships or does not ship according to what the worker felt like asserting. That third fact is the load-bearing one: it is the whole explanation of why an inherited check arrived green, and without it the section says two functions spell a path differently and never says why that got past a review. This is the defect the detail says this redraft fixed, one criterion along. Its own paragraph reads THREE CRITERIA SAID THREE THINGS EACH AND THEIR COMMANDS DECIDED ONE WORD, and the closing paragraph reads SO EACH CRITERION IS ONE CLAIM AND ITS CHECK READS THE SECTION, one criterion, one question, one section. Criterion 3 carries three facts and specifies the question for one of them. I checked the extent over the other six before writing this: criteria 1, 4, 5 and 6 each say what the check asks in terms that cover their whole sentence, criterion 2 hands the list to the check explicitly, and criterion 7 is the reds. Criterion 3 is the only one whose stated check is narrower than its stated claim. Separately, and this is not a finding but the reason I am confident the rest of the round is sound: I reproduced the route the redraft chose. I copied src/engine, doc, util and the three projection targets into a scratch tree, wrote the function-name form of the sentence under The check comes first, and ran TestNoCagedFileNamesTheMachineItWasWrittenOn and TestNoCommittedProjectionNamesTheMachine there. Both passed, where the literal-path form failed three times last round. The decision is right and it is written down on the criterion.

**satisfies:** Put back a clause saying what the check asks for, covering the whole sentence rather than the part the last finding was about. The shortest form keeps everything the redraft added: the two spellings are named by the functions that produce them and not written out, because this file is projected and the cage guard refuses a projected file that names this machine, AND the check asks for each of the three facts, requiring both function names and a sentence saying the two are one string on the platform it passed review on, none of which a topic word can satisfy. If the third fact is deliberately not being asked for, say that in the criterion with the reason, because the detail's own rule is that a deliberate exclusion is written down. Then take the red per fact rather than per criterion, the way criterion 7 already asks of the steps criterion: remove each of the three from a copy of the section in turn, run the check, and record which fact each failure named.

## lesson 1 · round 1 · by reviewer6

**the class:** A COMMAND THAT LOOKS LIKE IT WALKS A SET AND DOES NOT, BECAUSE THE TOOL'S EXIT CODE IS ABOUT ANY AND NOT ABOUT EVERY. This is wk-10d3cf13cd's class, A SET COVERED BY ONE MEMBER, reached by a new road and therefore much harder to see. There the sentence was about a set and the command named one member, plain on the page. Here the command names all the members, so it reads as the remedy already applied, and the tool ORs them one level down: rg -q PATTERN a b c exits zero when one of the three matches, which I ran rather than assumed. A reviewer sees three filenames and stops, because three filenames is what walking a set looks like. The same trap wears other clothes -- grep -l over several paths, test -f a -o -f b, a for loop with no || exit 1, a pipeline whose status is the last command's rather than the worst. On wk-8573243384 the criterion says a rule only one harness reads is a rule the others do not have, and its command is green in exactly that state. Beside it sits the older shape, a literal nobody derived: three criteria whose sentences carry three claims each and whose commands grep for one word, on a deliverable that is prose, where the string and the claim are the only two things there are.

**instead:** Run every command criterion against a case that ought to FAIL before agreeing it, not only against the tree as it stands. Make one member fail and watch the command go red; if it stays green, the command is not walking the set and the number of paths in it was never the question. Where a tool ORs, write the loop yourself with || exit 1 and say in the criterion that it fails on the first miss, so the next reader does not have to know what the tool does with several paths; better, derive the members from wherever the set is really defined rather than typing them. And when the deliverable is prose, the fastest test of a command criterion is to write the document that passes it and fails the sentence. If that takes under ten lines -- it took three here -- the criterion is checking a word and not a claim, and the remedy is to anchor the search to the place the claim has to be and to search for something only a true sentence would carry.

**minted as:** wk-dbfb8efd7a

## lesson 2 · round 2 · by reviewer8

**the class:** A redraft carries its old measurements forward without taking them again, so a round two token describes the tree of round one. The findings are answered and the parts nobody objected to are left alone, which is right about the argument and wrong about the facts: a detail's numbers are readings of a tree that moved while the round took, and a paragraph saying "red today" or "three files" carries a timestamp nothing prints. The token then instructs a worker to repair what somebody already repaired and to watch a named check go red that is green, and a stale instruction with no criterion over it cannot be told from a live one.

**instead:** Re-take every number and every "today" in the detail before submitting a redraft, not only the parts a finding touched: run the command the sentence rests on, count the lines again, open the file at the line it names. Say when each reading was taken by pinning it to the commit it was read at rather than to the word today, and where the thing has since been fixed, say so and name the commit that fixed it, so a reader can tell work already done from work still owed. And anything the detail still instructs gets a criterion or it is not being asked for, because an instruction living only in the detail is decided by nobody.

**minted as:** wk-4c3586254a

## lesson 3 · round 3 · by reviewer9

**the class:** A CRITERION THAT SPECIFIES THE CONTENT OF A FILE ANOTHER CHECK ALREADY GUARDS, WITHOUT ASKING WHAT THAT CHECK REFUSES. The deliverable is prose. The last round said a search for a topic word decides nothing, so the next draft asks for the evidence itself: the literal path, the literal command, the literal value the argument turns on. That is the right answer to the finding. It is also the exact thing a standing check keeps out of that file, and nobody asked, because a guard over a file is not something a drafter reads while writing sentences. The two checks then disagree and only one of them is on the list. The token's criterion goes green when the literal is there, the battery goes red for the same state, and the battery is not a criterion, so the draft is agreed with the contradiction inside it. The worker meets it at the keyboard and settles it alone, and the usual settlement is to weaken the criterion until both pass, which is the shape the previous round rejected.

**instead:** Ask what already reads the file before specifying what goes into it. For a guidance file that is the projector and every check over its targets, and the question takes one search. It is the drafter's question, because a worker cannot change a criterion that has been agreed. Run the battery over a draft of the sentence, in a copy, before the criterion is agreed: a criterion about prose is cheap to try, so write the paragraph, run the checks, and see what refuses it. And where a literal is refused, say in the criterion what stands in for it and how the check tells that substitute from a topic word, rather than leaving the worker the contradiction and no way out of it.

**minted as:** wk-b999022075

## lesson 4 · round 4 · by reviewer10

**the class:** A REDRAFT THAT ANSWERS EVERY FINDING WHOSE REMEDY IS AN EDIT AND LEAVES THE ONE WHOSE REMEDY IS A DECISION. A round hands back several findings. Some say what to type: drop a number, re-take a count, derive a list from the file that already declares it. One says the draft has to choose between two routes and that neither is written yet. The edits get made, the redraft comes back looking answered because the diff touches every criterion a finding named, and the criterion that needed the decision comes back byte for byte as it was. It hides because the round looks productive: more findings were closed than left, and an untouched criterion carries no marker saying anybody considered it, so the next reviewer cannot tell a decision that was taken and rejected from one nobody made. The tell is in the satisfies rather than in the criterion: a remedy that opens with one route and another, or with either and or, is asking for a judgment, and a judgment is the thing an edit pass has no step for. Measured on wk-8573243384, round 4. Findings 7 and 8 each asked for one edit and both were made, correctly, and I re-ran every number they touched. Finding 6 asked the drafter to decide what stands in for a literal a standing check keeps out of the file, and criterion 3 came back as the same string, which I checked by comparing it with the text the finding quotes.

**instead:** Two halves, and the first is what stops it being made. Before writing anything, sort the round's findings into edits and decisions and take the decisions first, while nothing has been typed and the draft is still cheap to move. Write each decision into the token as a sentence saying which route was taken and why the other was refused, even where the criterion barely changes, because a decision that leaves no sentence behind cannot be told from an oversight, and the next round spends itself finding that out. A finding whose satisfies offers a choice is not answered by any edit. The second half is the check that catches it, and it is mechanical. Before submitting a redraft, put each criterion a finding named beside the text that finding quoted and list the ones that came back byte-identical. Any criterion on that list is unanswered until the draft says in words why it should stand as it is. One pass, and it runs on the two things the record already holds.

**minted as:** wk-b062e097c9

## lesson 5 · round 5 · by reviewer10

**the class:** A REWRITE THAT ANSWERS THE FINDING AND DROPS A CLAUSE STANDING BESIDE IT. A finding names one defect in one criterion. The remedy is not an insertion but a rewrite, because the criterion is one sentence, and the sentence comes back saying the new thing correctly and no longer saying one of the old things. It is invisible from the new draft alone: the criterion reads well, it answers the finding, and nothing on the page marks the hole, because a clause that is gone leaves no trace where it was. It is invisible from the finding too, since the finding is about the part that changed. The clause most likely to go is the one saying what the CHECK has to ask for, because a rewrite concentrates on what has to be TRUE and the check's half reads like an aside. What is left is a sentence naming several properties and a check specified for one of them, which is the drift doc/guidance/specifying.md is about, arrived at by repair rather than by drafting. Measured on wk-8573243384, round 5: the old criterion 3 ended All three facts, and the check asks for each, and the new one names three facts and specifies the check for two strings.

**instead:** Two halves, and the first is what stops it being made. Rewrite a criterion beside its old text rather than over it: the record already holds the old wording, quoted in the clause of the finding that rejected it, so put the two on screen together and list what the new sentence no longer asks for before you call it done. A finding is about one clause, and every other clause in that criterion is still agreed, so anything that leaves has to leave on purpose and with a reason written next to it. The second half is the check that catches it, and it needs no memory: before submitting, for each criterion a finding named, take every property in the quoted old sentence and find it in the new one, and list the ones you cannot. Anything on that list is either restored or explained in the draft. Do it last, after the rewrite reads well, because reading well is exactly when the missing clause stops being noticeable.

**minted as:** wk-9b464d7365

