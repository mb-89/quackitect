---
id: wk-8573243384
seq: "-27"
type: work
title: inherited checks are watched
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 6
evidence:
  - outcome
minted_by: reviewer6
submitted_by: main
---

## detail

Work adopted on the strength of its tests, with none of them run against the defect it names. On wk-c6247665a3 thirteen upstream commits were adopted because each carries a test, and TestNoCommittedProjectionNamesTheMachine in src/engine/cage_test.go could not fail on Windows. The code fix landed in 2dd4c19d and nothing of it is owed. This token owes a rule in doc/guidance/behaviour.md under the section that says the check comes first. Before a verdict rests on inherited tests, break what each guards and watch it go red, then put it back and watch it go green. Related: wk-c6247665a3.

## done when

- the rule is written under the heading about the check coming first, and the check reads that section
  `rg -q func.TestTheInheritedCheckRuleIsWhereItSays src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleIsWhereItSays$ .`
- the rule says what to do, one assertion per step, read from the rule's own opening line to the end of the section
  `rg -q func.TestTheInheritedCheckRuleSaysWhatToDo src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleSaysWhatToDo$ .`
- it says what a green means: a check that will not go red is the finding
  `rg -q func.TestTheInheritedCheckRuleSaysWhatAGreenMeans src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleSaysWhatAGreenMeans$ .`
- it carries its measurement as the function names filepath.Abs and filepath.ToSlash and a sentence that the two are one string on the platform it passed review on
  `rg -q func.TestTheInheritedCheckRuleCarriesItsMeasurement src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleCarriesItsMeasurement$ .`
- the narrower lesson is in the section: write the check in the spelling the producer writes or normalise both sides, and open the artefact
  `rg -q func.TestTheInheritedCheckRuleSaysToOpenTheArtefact src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleSaysToOpenTheArtefact$ .`
- the rule reaches every projection of doc/guidance/behaviour.md named in util/projections.json, using projectionTargets in src/engine/search_test.go
  `rg -q func.TestTheInheritedCheckRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleReachesEveryProjection$ .`
- it is scoped to the checks a verdict's reason leans on, and the section names the cost of the universal reading
  `rg -q func.TestTheInheritedCheckRuleIsScoped src/engine && go test -C src/engine -count=1 -run TestTheInheritedCheckRuleIsScoped$ .`
- every test above was watched failing on its own assertion with the change absent, once per step and once per fact, naming the test and what it said

## evidence: outcome

doc/guidance/behaviour.md carries the rule under the check-comes-first section and it is projected into every target. src/engine gains the seven TestTheInheritedCheckRule tests the criteria name and the battery is green.
