---
id: wk-42888299f3
seq: "-31"
type: work
title: the header says holding
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
evidence:
  - outcome
rounds: 3
minted_by: main
submitted_by: main
reviewed_by: rev-33
---

## detail

The panel header in src/extension/panel.ts says nothing about the agent. Owner: show what state you are in, or what token you are working on, in the header. The engine answers one row per actor that pulled this session, off the record. Each row draws the first true of stopped with its reason, working, reviewing, else waiting, plus the id and title of any token in hand. The hold from .se/hold.json is drawn once for the tree, and the extension writes what it was handed. Related: wk-1b7c1a2da1.

## done when

- The engine answers what each actor is doing, in the order stopped, working, reviewing, waiting, driving one actor with two states true and one with none.
  `rg -q func.TestTheEngineSaysWhatEachActorIsDoing src/engine && go test -C src/engine -count=1 -run TestTheEngineSaysWhatEachActorIsDoing$ .`
- The hold is answered once for the tree and not per actor, with two actors known.
  `rg -q func.TestTheHoldIsAnswerdForTheTreeAndNotPerActor src/engine && go test -C src/engine -count=1 -run TestTheHoldIsAnswerdForTheTreeAndNotPerActor$ .`
- The engine answer is among what panelHtml is handed at its one call site, render in src/extension/extension.ts.
  `node util/checks/panel-is-handed-the-state.mjs .`
- The answer carries the id and title of the token in hand, or says there is none, driving the engine into each case.
  `rg -q func.TestTheAnswerNamesTheTokenInHand src/engine && go test -C src/engine -count=1 -run TestTheAnswerNamesTheTokenInHand$ .`
- The panel header draws the state and token it was handed, rendered from two different answers.
  `node util/checks/panel-says-holding.mjs .`
- The new check is named in the battery.
  `rg -q panel-says-holding util/checks/battery.sh && node util/checks/checks-live-in-the-method.mjs .`
- Every test named above was watched failing on its own assertion with the change absent before it was watched passing.

## evidence: outcome

se --doing answers the rows from src/engine/doing.go with TheStates ordered stopped first. doingArgs in engineargs.ts feeds lastDoing to panelHtml in render, redrawn after readDoing in postValues. Stop claims are one per actor in src/engine/stop.go, guarded by TestTwoActorsEachKeepTheirClaim. All eight reds were watched.
