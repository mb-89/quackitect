---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: spawned work never pushed
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-06T17:24:58Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - ff19a285f4f131dd3d2987779a929b78523e7594
---

## detail

Three agents were spawned onto the claims queue, closed nine tokens between them, and pushed none of the code. Every change they made sits uncommitted in this working tree, which is reclaimed when the session ends.

MEASURED in September 2026. git diff against origin/v4 answers src/engine/pull.go with the whole lane mechanism in it: TheLaneHere, TheLaneWorkedIt, laneInWords and theVerdictIsNotYours, about ninety lines. Its token wk-4ae19a7f0d is closed done. The same holds for the bad-claims-line change and for the lint narrowing.

WHAT THE RECORD SAYS AGAINST WHAT GIT HOLDS. The tokens read closed and the archive rows are pushed, so the work is finished as far as any reader can tell. The code is on one box's disk. That is the shape the cloud card already names: six archived notes name an object no clone can read, because each was closed on a box that never pushed the tag.

WHY THE HANDS DID NOT PUSH. Each was told to pull and work what it was handed, and nothing in that instruction says to land. The land door is a shell script, and an agent that never reads the card does not find it.

## proposed action

Land what the spawned hands wrote, one commit per token, naming the token in the message. Their tests are named in their own submissions and run again before each lands.

The order matters. src/engine/pull.go carries both the lane change and this session's own, and the tree is behind the branch, so the landing comes before the merge rather than after it.

The instruction a spawned worker is given wants the landing in it. That is a second token, because this one is the rescue and that one is the prevention.

## done when

- git diff --stat origin/v4 over src/engine names no file, so nothing a spawned hand wrote is left only on this box
- the tests each spawned token named are green before its landing: se test naming TestALaneThatWorkedItGivesNoVerdict and the bad claims line test answers ok
- each landing names the token whose work it carries, and git log --grep answers one commit per token

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

