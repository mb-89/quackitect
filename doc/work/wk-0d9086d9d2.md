---
id: wk-0d9086d9d2
seq: 1000127
type: work
title: the note it rewrote
status: imp_done
assignee: cowork
scope: single-step
traced: true
disposition: done
rounds: 3
minted_by: rev-9
submitted_by: cowork
evidence:
  - outcome
---

## detail

A criterion pins which token a call answers with and never the note the same call rewrites. Found on wk-386169824b round 3: the put-down branch in src/engine/pull.go wrote a hardcoded ImpOpen copied from the neighbouring branch, and criterion 5 passed either reading. Write the class into the dictionary of criterion drift in doc/guidance/specifying.md, above the set-covered-by-one-member entry. Remedy: for every token a fixture writes, assert by loading it back off disk. Before agreeing the criterion, finish the sentence "and afterwards this one reads" for each.

## done when

- The drafting method carries the class as one entry headed PINS THE ANSWER AND NEVER THE REWRITE, and the check compares the heading's line with the anchor's line, failing with both numbers.
  `rg -q func.TestTheRewriteEntrySitsBeforeTheSetEntry src/engine && go test -C src/engine -count=1 -run TestTheRewriteEntrySitsBeforeTheSetEntry$ .`
- The entry carries the remedy on one line inside its range.
  `sed -n '/PINS THE ANSWER AND NEVER THE REWRITE/,/A SET COVERED BY ONE MEMBER/p' doc/guidance/specifying.md | rg -q "loading each written token back off disk"`
- The entry carries the preventing half inside its range.
  `sed -n '/PINS THE ANSWER AND NEVER THE REWRITE/,/A SET COVERED BY ONE MEMBER/p' doc/guidance/specifying.md | rg -q "afterwards this one reads"`
- The entry carries its measured case inside its range, in the past tense, pinned to the round.
  `sed -n '/PINS THE ANSWER AND NEVER THE REWRITE/,/A SET COVERED BY ONE MEMBER/p' doc/guidance/specifying.md | rg -q "wrote a hardcoded ImpOpen"`

## evidence: outcome

doc/guidance/specifying.md carries the entry above the anchor with the remedy, the preventing sentence and the round 3 case. TestTheRewriteEntrySitsBeforeTheSetEntry was watched red with the entry absent and again with it planted below the anchor. The three sed searches were watched red on the empty range.
