---
id: wk-5ef1fa8f6b
seq: 1000214
type: work
title: A detail prescribes code
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-30
---

## detail

The detail of wk-7722faa0b8 prescribes setting cmd.SysProcAttr in runEvidence in src/engine/pull.go, and TestEveryChildProcessIsStartedQuietly in src/engine/quiet_test.go refuses any .SysProcAttr assignment outside src/engine/quiet_windows.go. Do not widen isTheDoor to admit pull.go. Before a detail names a file and a line, run the tree's own checks against that line on a copy. Here that is `go test -C src/engine -run TestEveryChildProcessIsStartedQuietly`. A detail says what has to become true and names the constraint, never the assignment. The fix owed is that the script reaches cmd verbatim while both window flags survive, and where the write goes is the worker's decision. Related: wk-7722faa0b8.
