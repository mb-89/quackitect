---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: Delete stray se-new.exe
# where the token stands. The process owns these values.
status: closed
author: worker-bell
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 21c80655e6d2a4ded3316b49e66a471b2aa03416
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 665c1c00c1bcc7ed191680bed5b960cc23375c2b
# how it ended. Only an ended token carries one.
disposition: became
# what it became. They have to exist.
successors:
  - "[[wk-cdb6961359]]"
---

## detail

Finding from the verdict on wk-00c162f56e (Strip dead process comments). Not a child of it. That token is closed.

The tree at that token's ended commit 8626f69f30d4a3b167cc855270b662a98fe371da carries src/engine/%S%/se-new.exe, blob cfe8311764bc8ce190712585c3063c560f7e5020, 13295616 bytes. git diff --name-status 7f6e30dfeb1b2ae9ce530f0238a6b8b5898d798c 8626f69f30d4a3b167cc785270b662a98fe371da shows it as A, so it landed inside that token's window. It is not in HEAD (7f22e1a3), so it lives only on the engine's began/ended lane.

Two things are wrong with it and both are dead weight. The directory name %S% is an unexpanded cmd variable: something ran `go build -o %S%\se-new.exe` through a shell that does not expand %S%, and cmd made a literal directory out of it. And a 13MB build artifact is committed at all, when util/checks/binaries-live-in-bin exists to say binaries do not live outside bin.

The damage is that every clone and every diff over that lane carries 13MB of nothing, and a reviewer reading git diff began..ended for any token in that window has to explain a binary hunk that belongs to no ask. The build site that writes %S% will do it again on the next build.

## done when

- src/engine/%S% does not exist at HEAD nor in the engine's lane tip. git ls-tree -r HEAD and git ls-tree -r <lane tip> both return nothing for the path
- the build site that wrote it is found and fixed, so the variable expands or the output goes to bin. grep the tree for se-new.exe and name the file that writes it
- util/checks/binaries-live-in-bin passes over the tree, and the run is named

## evidence: step 1. ask

<!-- restored after submitting: the engine's submit wrote the payload's map over these tables. Same text, put back by hand. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | — |  |
| [x] | every done-when line is decidable, and names the command where one decides it | All three name their command and all three were run. Line one: git ls-tree -r HEAD and git ls-tree -r 21c80655e6d2a4ded3316b49e66a471b2aa03416, neither carrying src/engine/%S%. Line two: se_find regex se-new over the tree. Line three: util/checks/binaries-live-in-bin through se_test. Line three is decidable and it answers no, for a file that is not this token's stray, which is why this token closes as became. |  |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [x] | one test was written first and seen red for the reason expected | The check is the test: util/checks/binaries-live-in-bin, through se_test. Seen red, under evidence: the check red and green. |  |
| [x] | the same test was seen green after the change, and named | Not green over the whole tree, and that is the answer rather than a tick. The runs are under evidence: the check red and green. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

## evidence: src/engine/%S% does not exist at HEAD nor in the engine's lane tip

Met. git ls-tree -r HEAD carries no src/engine/%S% path. git ls-tree -r 21c80655e6d2a4ded3316b49e66a471b2aa03416, the began snapshot the engine wrote when I took this token, carries none either. The worktree has none: dir /b /s src\engine\se-new.exe answers nothing.

## evidence: the build site that wrote it is found and fixed

Met, and the answer is that there is no build site. se_find regex se-new over the tree names three things and none of them writes that path: this token's own note, and .se/scratchpad/reswap.sh, which builds .bin/se-new.exe, the right place and a different file. The stray came from a one-off command I ran through se_run at 19:45, go build -C src/engine -o %S%\se-new.exe ., where set S=... sat on the same cmd line. cmd expands a variable when it parses the line, so %S% was still literal and cmd made a directory of it. Nothing in the tree repeats it. The guard that catches the class already exists and does catch it, shown red above. What let it through is that the battery cannot run for want of sh, which is already minted as wk-840c81485d.

## evidence: util/checks/binaries-live-in-bin passes over the tree

NOT met, and this is why the token closes as became rather than done. The check goes from 336 files, 2 failed to 335 files, 1 failed. The failure this token is about is gone. The one left is src/mcp/mcp.exe, tracked in git at HEAD, which no sweep of a leftover can answer because removing it changes what the project ships. It is minted as wk-cdb6961359, which carries this line.

## evidence: the check red and green

The stray had already been deleted before this token was minted, by me, under wk-2f780ad15b, so there was nothing left to make it fail. To see it red for the reason expected I put an empty file back at exactly that path and ran it: FAIL src/engine/%S%/se-new.exe stays out of the source folders, a built program sits beside source. 336 files looked at, 2 failed.

Removing the stray took the run from 336 files, 2 failed to 335 files, 1 failed, and the FAIL naming src/engine/%S%/se-new.exe is gone. The one that remains is src/mcp/mcp.exe, which is tracked in git at HEAD, is not this token's stray, and is now wk-cdb6961359. So this token's own class is clean and the tree-wide pass its third done-when asks for is not, which is why the disposition is became.

