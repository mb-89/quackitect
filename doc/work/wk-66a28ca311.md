---
id: wk-66a28ca311
seq: "10"
type: work
title: port the v3 editor
status: submitted
assignee: main
scope: single-step
traced: true
disposition: done
evidence:
  - toolbar
  - columns
  - editing
  - measured against v3
minted_by: main
---

## detail

TAKE THE CODE FROM v3 AND ADAPT IT. Do not rewrite it from a description. C:/Users/ichbi/Desktop/ai/quackitect-v3/deliverable/engine/tables.ts, baseui.ts and basesclient.ts are the source. Read them and port them, keeping their structure and their comments, and change only what v4's engine makes different.

WHAT MUST BE THERE, from baseui.ts:386-397. A toolbar above the table and outside the scrolling pane: the view name, the row count, Filter as an and-of-ors builder, Sort carrying both group-by and sort-by level lists, Properties as a column tick list with a find box and Hide all, and a toggle to the raw .base text. A pager below the table.

Help is a detail and never a button. Clicking a control's label puts its help in the details pane, and there is no question mark anywhere.

Column widths and order are written back into the .base file by dragging. Clicking a heading replaces every sort level rather than adding one.

THE COUNTS COME OFF THE BAR. Every group already shows its own count, and a number in two places is a number that disagrees with itself.

WHAT NOT TO COPY: v3 lost every hand-fold on refresh, and several of its handlers query the document rather than their own pane, so they hit the left one whichever pane the control was in.

## evidence: columns

Dragging an edge writes columnSize. Dragging is on a grip and the grip is not the header, so resizing does not sort. Clicking a heading replaces every sort level rather than adding one: a header that added a level would make the two controls disagree about what is in force. The last column takes what is left, so the table fills its pane.

## evidence: editing

Double-click begins, Enter commits, Escape discards. A cell says whether it can be edited and why not when it cannot, because a cell that silently ignores a double-click reads as a broken table. The engine decides what may be written: fifteen cases in field_test.go say which fields are a person's and which are the engine's.

## evidence: measured against v3

MEASURED: se view --file work --pane left --width title=360 and --sort title --direction DESC both landed in the left view. The file's 17 comment lines survived and the query read back widths title=360 and sorted by title DESC. A change patches the lines it names and nothing else moves.

48 render checks pass against a live query, including the filter builder with its serialised operator vocabulary, the condition rows, both templates, the join word, the pager, the typed page size and the column grip.

THE STYLING IS OURS. v3 drew its table's buttons differently from every other button in the product. One rule styles every button here.

NOT COPIED: v3 lost every hand-fold on refresh, and several of its handlers query the document rather than their own pane, so they hit the left one whichever pane the control was in. Folds survive here, and every handler is scoped to its own pane.

Both suites pass. What I have not done is look at it on a screen.

## evidence: toolbar

Ported from baseui.ts with its structure and its comments. The view name and the row count, Filter as an and-of-ors builder, Sort carrying both the group-by and sort-by level lists, Properties as a column tick list with a find box and Hide all, a toggle to the raw .base text, and the pager below the table.

It sits above the pane that scrolls, so it stays put while rows go past. Help is a detail and never a button: there is no question mark anywhere.

Rules kept rather than reinvented: an empty level list still draws one blank row, or there is nothing to clone and Add does nothing. The find box is this pane's own, because two panes are drawn side by side. One unreadable filter row makes the whole group raw. An operator already in the file is offered even where the inferred type would not offer it. An empty value box is an unfinished row and never a test for the empty string. The two templates are what Add clones, and a template's content is not in the document so it is never collected.

TWO THINGS HIDE A ROW and they share one attribute: a closed group and a page it is not on. Both are computed in one pass, because two handlers writing the hidden flag would fight and the loser would be whichever ran second.

