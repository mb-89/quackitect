---
id: wk-c2164f937d
seq: "27"
type: work
title: open work blocks
status: closed
assignee: main
scope: single-step
traced: true
disposition: became
successors:
  - wk-d1b585b7b9
evidence:
  - outcome
minted_by: person
---

## detail

When more tokens than the limit are open with nobody working on them, the pull refuses, the same shape as the reviewer rule. What counts: tokens that are open and not held by anybody. It refuses the pull and nothing else, since refusing every call would block the call that spawns the worker. It says how many are open, which ones, and that spawning a worker clears it. The number is limits.open_before_blocked, beside the review one, zero turns it off, and it may be made smaller and never larger. The owner asked for 9999 as the off switch and zero is used instead, because the declared default is the ceiling. Related: wk-d1b585b7b9.

## evidence: outcome

The token became wk-d1b585b7b9, which carries the rule forward.
