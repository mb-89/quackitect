---
id: wk-4aa225110e
seq: 1000231
type: work
title: zombies reset at boot
status: spec_open
assignee: main
scope: single-step
traced: true
minted_by: main
---

## detail

Owner: what is in work right now might be zombies from last session, and when the session starts they should reset. If the tool does not reset them, that is a bug. At session start, every token in an in-work state whose holder is a dead session goes back to open. Look at what is in work now and verify each one is being worked. If the engine does not reset them itself, fix that. wk-71a9ec3d53 round 1 finding 2 shows a reclaim loop in src/engine/pull.go that strands imp_in_work tokens whose holder went away. If that fix is this fix, settle this token by citing it.
