---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: projections carry chapter titles
# where the token stands. The process owns these values.
status: closed
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 92c6795f402032b54b1617b98f40fae60f2cb113
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 840935b2cb2727986641b8980c5f962fc42c2798
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

util/projections.json now projects only the Actionables chapter of each guidance file, headed by the file's title. The old engine on a machine that has not rebuilt writes the whole files instead. Add a check that AGENTS.md carries exactly the Actionables chapters of its sources and nothing else, so a stale build is caught at the battery.

## done when

- node util/checks/<name>.mjs . fails on old-assemble AGENTS.md, passes on new.
- The check runs from battery.sh.

## evidence: step 1. ask

<!-- restored by hand. The engine deleted these tables on submit, which is wk-7887984486. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Whole. One new check, and one word added to the battery's list. | — |
| [x] | every done-when line is decidable, and names the command where one decides it | se test proposing the check decides the first. Reading battery.sh:318 decides the second. | — |
| [x] | the basics it stands on exist, or are minted first | Both exist. util/projections.json already carries section and sources_from, so the check derives what to expect rather than hard-coding it. battery.sh:318 already holds an explicit list where a check that is missing counts as a failure, not a skip. | util/projections.json, util/checks/battery.sh:318 |

## evidence: step 2. do

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | work-token |
| [x] | one test was written first and seen red for the reason expected | The check was green on the clean tree, so the red had to be made. See evidence: the planted red. | se test --on wk-78af255c33 --propose *projections-carry-chapters* |
| [x] | the same test was seen green after the change, and named | Green twice, before the plant and again after the revert: overall ok=True, util/checks/projections-carry-chapters ok=True, 2 projections read, 0 failed. | same command |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | util/checks/projections-carry-chapters.mjs is new. util/checks/battery.sh:318 gained one name |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | ONE CORRECTION TO THE ASK, and it is in the change. See evidence: one correction to the ask. | util/projections.json |

## evidence: the planted red

I planted a Motivation chapter into .github/copilot-instructions.md, which is the shape a stale build writes, and ran it again. FAIL: "it carries 1 heading(s) no source puts there, the first being ## Motivation". That is the failure the token asks to be caught. The plant was reverted at once.

## evidence: one correction to the ask

The token says AGENTS.md and no such file is in the tree. The chaptered projections are .github/copilot-instructions.md and .claude/output-styles/quackitect.md. The check names neither: it reads both off util/projections.json, so it follows a rename or a third target without being edited.

