---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: guidance draws search line
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: main
# the token this is a part of. It cannot close while this is open
parent: [[wk-c9c5fcf3b9]]
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 4d60164bdb8d19fed953d103ecd9b39f00bfa452
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - d3f9b1dbb1e6d177bee13c0e86050cc29dce167a
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Rule 8 in doc/guidance/driving-the-engine.md says a shell's rg or grep aimed inside the tree is refused. It does not say where the guard draws the line.

The guard refuses the recursive form and leaves a search on one named file alone. So grep on a path is fine and rg over a folder is not.

An agent that reads the rule as written reaches for grep on a file and gets it. It then reaches for grep -r and pays a refused call to learn the difference. Seen twice in one session, once through se_run and once through the harness.

A refusal costs a call and a turn. The rule already exists. The boundary is what is missing.

## proposed action

Add one sentence to the rule 8 section of doc/guidance/driving-the-engine.md, beside the sentence about refusals. It says the recursive form is refused and a search on one named file is not.

The count of numbered actionables does not move, so no projection changes.

## done when

- the rule 8 section says the recursive form is refused and one named file is not: se_find regex recursive over path doc/guidance/driving-the-engine.md answers one hit
- the chapter still numbers 11 actionables: se_find regex ^11\. over that path answers one hit and regex ^12\. answers none

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one sentence in one file | — |
| [x] | every done-when line is decidable, and names the command where one decides it | both are se_find with a regex and a path, and both were run | the answers are below |
| [x] | the basics it stands on exist, or are minted first | rule 8 and its section already exist, and this adds to them | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | the token names the cost seen twice, and one criterion a command decides | — |
| [x] | one test was written first and seen red for the reason expected | the first criterion's own command answered count 0 before the edit | se_find regex recursive |
| [x] | the same test was seen green after the change, and named | the same command answers one hit, at line 102 | se_find |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began 4d60164bdb8d19fed953d103ecd9b39f00bfa452 | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | one thing is left open and named below, rather than guessed at here | — |

## evidence: what is left open

The guard also refused grep -v reading a pipe, with no file named. It let git status piped into grep -c through in the same session.

So the boundary has a second edge, and I could not tell from two samples where it lies. The sentence claims only what both samples agree on: the recursive form is refused and one named file is not.

Whoever reads the guard's own condition can say the rest in one more sentence.

## evidence: what the checks said

The sentence is at doc/guidance/driving-the-engine.md:102. se_find for recursive over that file answered 0 before the edit and 1 after.

The chapter still numbers 11 actionables. se_find for a line starting 11. answers one hit at line 33, and for 12. answers none.

The projection is unaffected, because the sentence sits in the case and not in the numbered rule. util/checks/projections-carry-chapters ran green in 0.102s.

