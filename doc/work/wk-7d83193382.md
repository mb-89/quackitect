---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: edit skips content checks
status: closed
disposition: done
---

## detail

hook.go:495 declares the writing tools as Write, Edit, NotebookEdit and MultiEdit, but the payload struct at hook.go:41 carries only FilePath, Path, Command and Content. An Edit arrives as old_string and new_string, so ti.Content is empty and the voice check at hook.go:521 and the private original check at hook.go:547 skip. Owner ruled this is to be fixed. First write two failing tests beside the Write case at hook_test.go:190. An Edit whose new_string carries a semicolon in a .md path must deny naming the rule. An Edit whose new_string is a private original must be refused. Then add NewString for Edit, Edits for MultiEdit and the NotebookEdit shape, and feed the guards every member joined, not the first. Add a check that walks the writes map at hook.go:495 and asserts each tool name has a payload case yielding content.

## done when

- Test: Edit new_string with semicolon into .md denied naming the rule, red then green.
- Test: private-original new_string refused, red then green.
- A check walks the hook.go:495 map, failing tools without content payload cases.

