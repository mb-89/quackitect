---
id: wk-be13363600
seq: "-32"
type: work
title: one reviewer disables it
status: imp_done
assignee: cowork
scope: single-step
traced: true
disposition: done
rounds: 1
minted_by: cowork
submitted_by: cowork
reviewed_by: rev-31
evidence:
  - outcome
---

## detail

reviewerMissing in src/engine/pull.go is turned off by one live reviewer through an early return, and never counts spec_submitted. Count what HandedOut(RoleReviewer) answers, remove the early return, and split dead-holder detection out.

## done when

- Every state HandedOut(RoleReviewer) answers is counted
  `rg -q func.TestTheRefusalCountsWhatIsWaiting src/engine && go test -C src/engine -count=1 -run TestTheRefusalCountsWhatIsWaiting$ .`
- A live reviewer is worth exactly the one token it took
  `rg -q func.TestALiveReviewerIsWorthTheOneItTook src/engine && go test -C src/engine -count=1 -run TestALiveReviewerIsWorthTheOneItTook$ .`
- The refusal says how many wait and how many are reading
  `rg -q func.TestTheRefusalSaysHowManyAreReading src/engine && go test -C src/engine -count=1 -run TestTheRefusalSaysHowManyAreReading$ . && rg -q func.TestTheReviewerRefusalSaysHowManyMore src/engine && go test -C src/engine -count=1 -run TestTheReviewerRefusalSaysHowManyMore$ .`
- Dead holder detection is its own function, standing tests green
  `rg -q func.TestADeadHolderIsCaughtOnItsOwn src/engine && go test -C src/engine -count=1 -run TestADeadHolderIsCaughtOnItsOwn$ . && rg -q func.TestAStaleHoldIsNotAReviewerReading src/engine && go test -C src/engine -count=1 -run TestAStaleHoldIsNotAReviewerReading$ . && rg -q func.TestSomethingWaitingWithNoHolderIsNotCalledHeld src/engine && go test -C src/engine -count=1 -run TestSomethingWaitingWithNoHolderIsNotCalledHeld$ . && rg -q func.TestAHoldCarriedAcrossARestartIsNotCalledStopped src/engine && go test -C src/engine -count=1 -run TestAHoldCarriedAcrossARestartIsNotCalledStopped$ .`
- A reviewer is never refused
  `rg -q func.TestAReviewerIsNeverRefusedByTheQueue src/engine && go test -C src/engine -count=1 -run TestAReviewerIsNeverRefusedByTheQueue$ .`
- Every test in the engine package still passes
  `go test -C src/engine -count=1 .`
- Every test above was watched red

## evidence: outcome

reviewerMissing counts what HandedOut(RoleReviewer) answers plus dead reads, with no early return, and deadReads is the dead-holder detection. Five new tests were watched red and the seven standing tests are untouched.
