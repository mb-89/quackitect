---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: abort loses its reason
status: closed
disposition: done
---

## detail

Abort took (id, why, by) and its one caller passed (id, by, why), so every abort wrote the actor's name into the reason field. A token dropped with a paragraph explaining why recorded reason: person. Two adjacent string parameters, and nothing in the language or the tests could tell them apart. Found by reading a token I had just dropped.

## done when

- Abort takes one named struct. go build ./src/engine passes.
- Taker reads call sites: no adjacent same-typed strings.

