---
id: wk-789ff2ba2e
seq: "75"
type: work
title: the check ran dry
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "The lesson is written in doc/guidance/behaviour.md under A check whose red depends on data the system eats goes quiet, and TestEveryOldNameReadsBackWhoeverIsLeftOnDisk now writes its own note per old name."
aborted_from: imp_open
minted_by: reviewer5
---

## detail

A check whose red depends on data the system consumes goes quiet as the system succeeds. TestTheTokensThatExistReadUnderTheNewNames from wk-20c6c329fd walks the tokens on disk. Removing the in_work or submitted entry from wasCalled in src/engine/token.go leaves it green once no note spells that state. For every rule that must hold, build the input in a fixture you control, enumerated from the keys of the map. Where a sweep over live data is kept, make it refuse an empty population per alias. Found on wk-20c6c329fd round 5.
