---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: keywords reach the engine
# where the token stands. The process owns these values.
status: done
# who did the work step, so the verdict is never theirs
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 5c0783adbecd8b2701e45a2eed85838d856dd39b
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 48398d47f512494b195d143aa8b337725a8bac58
---

## detail

The cloud has no panel. The only surface a person has on a box they are not sitting at is the chat. Nothing the sidebar offers can be reached there.

The engine already receives what the person wrote, and from the harness rather than from the agent. A message that starts a turn arrives in the hook payload. One written mid-turn is copied out of the harness transcript on the next tool call, or at the stop if the agent goes quiet.

Both are written to the record as user and prompt, and nothing else in the record carries that pair. So the engine can tell what a person said from what an agent said, without trusting the agent to be honest about it.

## done when

- a message that is only the keyword changes the control it names, decided by: send one and read the state
- the same keyword inside a sentence changes nothing, decided by: send one and read the state
- the change names the keyword and what it moved in the record, decided by: read the record after
- a keyword an agent writes rather than a person changes nothing, decided by: a test that plants one in a tool call

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A person away from their desk gets the sidebar back. | the six console flags |
| [x] | what breaks if it is never done, and not only that it stays undone | A cloud box cannot be steered while it runs. | the detail |
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | it names the input, the match and what is written | the approach |
| [x] | every done-when line is decidable, and names the command where one decides it | each by sending a message, then reading the state or the record | the done when |
| [x] | the change is small enough to review whole, or it is split first | — |  |
| [x] | the basics it stands on exist, or are minted first | the engine records both routes already | heard.go, hook.go |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Five checks were written first and two were watched red. | keywordsaid_test.go |
| [x] | the change follows the approach on the token, or the token says why it departed | It follows it. The match is on the harness routes only, and the said verb does not call it. | heard.go, hook.go |
| [x] | se test --on this token answered ok, and what it ran is named | 12 Go tests green. The battery failed the same 5 before and after, less the flat one this fixed. | battery-20260906-072019 |
| [x] | the note says what changed and why, for a reader who was not here | The approach says the shape, and the package comment says why only a bool. | internal/keyword |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change. The flat package had to shrink to take this, so the record reader moved out with it. | internal/said |

## approach

The engine matches a keyword against what the person said, and against nothing else. A record of user and prompt is the only input it reads.

The whole trimmed message has to be the keyword. A mention inside a sentence is not one, which is what keeps the guidance describing these from firing them.

What matched and what it moved goes in the record, so a change nobody expected can be attributed rather than guessed at.

Nothing is relayed by the agent. The text comes from the harness, so an agent cannot forge one, and no guard has to be written to stop it trying.

