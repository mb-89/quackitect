---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: the harness has tools
status: closed
disposition: done
---

## detail

The search rule in behaviour.md is written in shell terms. The harness Grep tool satisfies it by a route the rule and the guard cannot see. Name the harness Grep tool in behaviour.md as a way of satisfying the rule. Have the guard treat a Grep tool call the same as an rg call. Do not widen the probe behind util/tools.json to reach it, since a harness tool is not a program on the machine. Then make one pass over the guidance for other rules that name a command rather than an act. Read, Edit, Write and Bash open the same hole.

## done when

- behaviour.md names the Grep tool, and a test shows the guard treats Grep like rg.
- tools.json probe not widened.
- Rules naming a command, not an act, fixed or tokened.

