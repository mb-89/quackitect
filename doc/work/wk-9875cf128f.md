---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: context count reads path
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/main
claimed_at: "2026-09-06T06:52:24Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 18ecc2b5d42b8a2e123735fbde143a220fbceadd
---

## detail

A FINDING WHILE WORKING wk-98df32b772.

pathsAmong in src/engine/search.go knows six flags that take a value: -e, -g, -t, -T, --regexp and --glob and --type. Every other value-taking flag leaves its value on the list, and a bare word that is not a flag is read as a path.

Measured in this tree, over a file outside the work root:

rg -n "func runningPath|type Running struct" -A 12 /tmp/wk98/src/engine/watch.go

It was refused with THE TREE IS INDEXED. The 12 after -A is the context count. It is bare and relative, so anyInside answers true, and the outside-the-tree path beside it never decides anything.

Same shape as wk-7bab432426 and wk-8c76f768c1: a guard ruling on words it has not finished reading. The refusal names a rule the command does not break.

## done when

- rg with -A and a number, over a path outside the work tree, is not refused: a case added to the search guard's test in src/engine, seen red first
- the same holds for -B, -C and -m, each a case in that test
- a search naming a path inside the tree is still refused: the existing cases in that test stay green

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

