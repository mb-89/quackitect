---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: note carries two approaches
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-webern-two
claimed_at: "2026-09-05T18:50:04Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 2b6b96470fa3873307d35fcaed33ce55b0d203af
  - 86116f003ac7821f6ae92ab3da19225e11b22e66
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 7035ba030ed617898eab5fdca5b7ae54f26c7195
---

## detail

Found reviewing wk-5bba8e497a. doc/work/wk-5bba8e497a.md carries two "## approach" sections, at line 85 and line 95.

They disagree. Line 91 says both the state of play and se query name a parked token. Line 101 says parked work has one surface, the state of play. A reader cannot tell which approach the work followed, and the step 1 evidence cites "the approach section" as though there were one.

It is the only note in doc/work with a repeated heading, measured by a uniq -d over every "## " line.

Nothing catches the class. readBody at src/engine/store.go:595 appends an unknown heading to Kept, so a second section with the same name round-trips forever and the writer is never told. No check under util/checks reads token headings.

The fix is two parts. Merge the two sections on that note into one that says what was built. Then add a check that refuses a token note carrying one heading twice.

## proposed action

One approach section per token note, and a check that refuses a repeated heading.

## done when

- doc/work/wk-5bba8e497a.md carries one approach section, and it does not contradict itself. Decided by: sed -n 's/^## approach$/x/p' doc/work/wk-5bba8e497a.md | wc -l, which answers 1
- no note under doc/work carries the same second-level heading twice. Decided by the new check, run over the tree
- the check names the offending file and the repeated heading. Decided by running it on a note edited to carry two headings, where it fails and names both

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Two parts. One note section merged, and one check of 76 lines reading one folder. | git diff HEAD --stat |
| [x] | every done-when line is decidable, and names the command where one decides it | The first is a sed and a wc that answers 1. The second and third are the new check, run over the tree and over the note carrying two headings. | RUNME.sh test --propose notes-say-each-heading-once |
| [x] | the basics it stands on exist, or are minted first | The engine finds any mjs under util/checks by itself, so the check needed no wiring. The battery keeps a list of its own, so the name went in there too. The note was gone from doc/work, taken by an archive sweep in flight, and was restored from HEAD to be fixed. | util/checks/battery.sh |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove red first. Rule 13 named the two halves and drove both. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | The check was written, then the note restored with both sections. It failed naming the file and the heading, twice over. 141 read, 1 failed. | /tmp/red3-web2.txt |
| [x] | the same test was seen green after the change, and named | After the merge it answered ok. The battery ran it over 139 notes with 0 failed. | /tmp/green3-web2.txt |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Four files. The note keeps one approach, the check is new, battery.sh gains its name, and this note carries the answers. | git diff HEAD |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None. checks-live-in-the-method failed only on the check being uncommitted. | battery output |

