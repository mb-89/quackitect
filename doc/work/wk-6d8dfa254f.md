---
id: wk-6d8dfa254f
seq: 1000172
type: work
title: a predicate reads twice
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-16
---

## detail

When a detail decides a branch with a word like absent, gone or stale, name the function that answers it in the same sentence. Where the tree answers the word in more than one way, the detail names the function or the criteria carry one fixture per reading. Count the live population under each reading before calling the difference theoretical. Seen on wk-d51f434d37 at draft review: absent is answered twice in src/engine/arrival.go, by Arrived and by StillPulling. Its criterion 3 fixture never arrives the remembered reviewer, so an implementation reading Arrived passes and strands every token whose reviewer arrived and stopped. Related: wk-73017d4153.
