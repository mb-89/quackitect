---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: guidance runs past bounds
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-ravel-two
claimed_at: "2026-09-06T06:48:59Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e548c032e4f65e5e8f5bb63b842175d8e09b783c
---

## detail

se lint answers two findings on doc/guidance/work-token.md. An item in Actionables runs to 38 words where the schema allows 25, and that is rule 1. Discussion runs to 1142 words where the schema allows 1000.

Both arrived through one commit. Rule 1 grew a sentence about what a tracked ask answers, and chapter 1 grew a paragraph under it. Chapter 12 grew six lines saying a build failure is no red. The write door let each through, because it refuses a section a write pushes further past its bound and the guidance is not measured at that door the way a token is.

Measured at head 46d003d8: se lint names both, with the file and the line.

The fix is prose. Rule 1 comes back under 25 words with its second sentence moved into chapter 1, and the Discussion loses what it repeats until it is under 1000.

Found while working wk-7c41d9e206 and wk-88f5e77313, whose commits carry the chapter 12 half and the hunk another hand had in flight.

## done when

- se lint answers no finding naming doc/guidance/work-token.md
- every rule in Actionables is 25 words or fewer, decided by: se lint after the change
- the rules keep what they said, decided by: a reader compares the rules against the same file at 46d003d8

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A clean lint. The two findings sat on the file every hand reads before writing a token. | — |
| [x] | what breaks if it is never done, and not only that it stays undone | The guidance stays over its own bounds while holding others to theirs, and a lint nobody can get to zero teaches readers to skim it. | — |
| [x] | the ask is small enough to review whole, or it is split first | One file, one prose diff, no code. | — |
| [x] | every done-when line is decidable, and names the command where one decides it | Two by se lint, which prints the file and line. The third by diffing the Actionables block against 46d003d8. | — |
| [x] | the basics it stands on exist, or are minted first | It stands on se lint's guidance rule and the schema's word bounds, both in the tree. 46d003d8 is an object here. | — |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. It is the file changed. Rule 12 put the red first. | — |
| [x] | one test was written first and seen red for the reason expected | se lint is the check the criteria name. Against the file as HEAD had it: an item at 38 words against 25, Discussion at 1142 against 1000. | — |
| [x] | the same test was seen green after the change, and named | se lint, run again in the same call with the changed file back, named no finding on work-token.md. | — |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | doc/guidance/work-token.md alone. satie-two left most of it uncommitted. | — |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Chapter 9's cut left a double blank, fixed here. Commit 6f4b48fd then put the file back over both bounds on rule 3, which is wk-54936ec0f0. | — |

