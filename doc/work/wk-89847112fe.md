---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: delta misses bad object
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/main
claimed_at: "2026-09-05T20:16:49Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - e193a44462590c9cf8c39727fbcd2ff8b7516dd9
---

## detail

A FINDING WHILE WORKING wk-40abb881a7.

deltaSince in src/engine/tests.go diffs the tree against the token's last began snapshot. That snapshot is a commit under refs/se/steps, which no push carries. A token taken up on one box and worked on another has a began hash the second box never had, and git diff on a full hash it does not hold says fatal: bad object <hash>. deltaSince reads the error for Could not access, bad revision, unknown revision and ambiguous argument, and bad object is none of those, so se test --on that token answers an error and nothing runs. Measured on wk-40abb881a7 on this box: git diff: fatal: bad object c2682c671c7ab75306367de55d36104c0ec51b96, while every other tracked token tested. The take-up added no snapshot here, because openStretch sees a stretch already open (three began, two ended) and does not open another.

## proposed action

Treat bad object the way bad revision is treated: no history to diff against, so the untracked list is the delta. Better still, when the last began is not an object here, fall back to the newest began this box does hold, and then to HEAD, and say in the answer which since was used.

## done when

- se test --on a token whose last began hash is not an object in this clone answers a plan rather than an error, decided by a test over deltaSince in src/engine that hands it a hash git does not hold
- the answer names the since it fell back to, decided by the same test reading Tested.Since

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The engine's own test door opens for work that came from another box. Until now it answered an error and ran nothing. | 1 door |
| [x] | what breaks if it is never done, and not only that it stays undone | Every travelled token has to be tested by hand, outside the engine, so what ran is nobody's record. Agents did that all day. | tested by hand |
| [x] | the ask is small enough to review whole, or it is split first | One helper, one call site, one word added to a list. | 3 hunks |
| [x] | every done-when line is decidable, and names the command where one decides it | Both name one test, which hands the delta a hash git does not hold and reads Since back. | 2 of 2 |
| [x] | the basics it stands on exist, or are minted first | deltaSince, Tested.Since and the began list were all in the tree. | 3 of 3 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | The proposed action asked for the fallback rather than only tolerating the error, and both are in. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | TestASnapshotThisBoxNeverHadStillTests went red on both cases with the quoted error: git diff, fatal, bad object. | 2 of 2 red |
| [x] | the same test was seen green after the change, and named | Both cases pass. Every Delta and RunByHand test passes with them, and go vet is clean. | 0 failures |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Three hunks in tests.go and one new test file. | 2 files |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change. bad object went into the tolerated list as well, so a caller naming a hash itself still gets a plan. | 1 word |

