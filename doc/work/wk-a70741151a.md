---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: criteria name untouched files
# where the token stands. The process owns these values.
status: open
---

## detail

A finding on wk-4f8e7e7ebe. The token's own commit, 72594c7a, carries one file: doc/work/wk-4f8e7e7ebe.md. Its last began..ended span, 2261f453..ad9b42ba, carries only the ended marker. So the verdict step's line, every hunk of git diff began..ended was read, reads a span with no code in it, while the four criteria are all about src/engine/enginefresh.go and the TestMain in src/engine/enginebin_test.go. The worker says so plainly and names where the code came from: snapshot fc27d9b1, wk-885646032c began, 162 files. That is a take-up snapshot, not somebody's change, so 125 lines deciding which engine every subprocess test drives entered the tree without ever being read as a change by anyone. This verdict is the first read they have had, and it found one defect: wk-0a4ee0c1fa. A reviewer who took the span at its word would have found nothing, because the span holds nothing.

## proposed action

Make the shape visible where a reader meets it. On the standard process's verdict step, a token whose began..ended span touches none of the files its criteria name says so on a named line and names the commit that does carry the change, so the reviewer reads that instead of the empty span. Decide whether that line belongs in src/processes/standard.process.yaml or in doc/guidance/methods/reviewing.md before writing it.

## done when

- a reviewer meeting an empty span is told where the change is: the verdict step of src/processes/standard.process.yaml, or reviewing.md, carries a line naming the case
- the line names what to do rather than only what happened: it says the token names the commit carrying the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

