---
id: wk-70df338d58
seq: "62"
type: work
title: a guard pull outruns
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "The remedy landed on the investigation: the guard is asked where it can answer, quietHold called directly at src/engine/investigate_test.go:143 and :146, once for the holder and once for somebody else. Obsolete."
aborted_from: backlogged
minted_by: reviewer4
---

## detail

A check written against the front door for a guard the front door can never reach. quietHold skips a token whose holder is the actor, and Pull calls Arrived before quietHold, which stamps the actor at the current pull count, so StillPulling always answers true for the puller and a token the puller holds is never quiet. The holder comparison can therefore never be the deciding condition on any path through Pull, and TestAHolderIsNotSentToInvestigateItself, which drives Pull, passes with that comparison deleted.

REPRODUCED in .se/scratchpad/reviewer4/beforeinv: delete t.Holder == actor from quietHold and the test stays green. A second test in the same copy shows why, logging that main reads as quiet before it pulls and as still pulling after.

WHAT TO DO: ask the guard where it can answer. quietHold(r, actor) called directly, with the queue driven past the staleness by somebody else, decides on the name: quietHold(r, main) finds the hold and quietHold(r, reviewer1) must not. I wrote that and ran it, green with the guard and red without. Either write the check at that level or drop the guard and the criterion together, and say which, because a guard nothing can reach is either dead code or an untested rule.

Found on wk-a51a799f40.

