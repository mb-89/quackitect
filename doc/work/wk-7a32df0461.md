---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: restore the eaten archive
# a person decides this, and the queue does not hand it to an agent
needs_human: true
# what has to be true before this is worth reading again
ready_when: ready when a box may write doc/work/archive.jsonl. The harness refuses that write, not the engine. It was reproduced here down to an edit that changes nothing. Every agent handed this reaches the same refusal. So a person says which box does it, or lifts the rule.
# where the token stands. The process owns these values.
status: open
claimed_by: f5927132/worker-gibbons
claimed_at: "2026-09-06T12:05:12Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - a340a1a9fd01cd76f5472fc116dbfae80ee67aa8
---

## detail

THE ARCHIVE THE BRANCH STILL CARRIES IS 380 ROWS, AND THE LIST ON THE DISK IS 15.

Measured 2026-09-06. git cat-file -p 084367a59c148aade7d72de11a506b7bb0d26b63:doc/work/archive.jsonl reads 380 rows. doc/work/archive.jsonl read 12 here, then 15 as other hands closed into it. That is the close wk-fa2dd32c33 named. The close is guarded now and the rows it ate were never put back.

WHAT IT COSTS. se lint answers 306 findings of the shape "names wk-XXXX, and no token and no archive row answers to it", over 200 ids in 168 live notes. 164 of the 200 had a note under doc/work that a commit deleted: 112 are named by a row in the 380, and 51 more can have a row derived from the note blob HEAD reaches. wk-b8161b13b5 cannot, its note being in no commit HEAD reaches. The other 36 never had a note here and are wk-cf33c85301's.

BUILT AND CHECKED HERE, AND REFUSED. .se/scratchpad/rebuild_archive.py answers 178 rows, 178 of them naming an object git rev-list --objects HEAD reaches, and none naming a token whose note is live. It takes only the stranded ids, because restoring all 380 writes 154 rows over live notes, which is wk-ffaac73ec8's question.

The write is refused by the harness rather than the engine: se apply on that path is denied down to an edit whose old and new are identical, while se apply --dry on it and se apply on other files are allowed. wk-7873576bbb hit the same wall.

## proposed action

On a box whose sandbox permits the write, rebuild doc/work/archive.jsonl: keep the rows on disk, add the row from 084367a:doc/work/archive.jsonl for each stranded id that has one, and mint {id, title, process, disposition, on_branch} for each that has none, taking the fields from the note blob and leaving disposition empty where the note carries none, since many read status: backlogged. Then check every row names an object git rev-list --objects HEAD reaches, and that no row names a token whose note is live under doc/work.

## done when

- doc/work/archive.jsonl names every stranded id whose note HEAD reaches, decided by: se lint answers no "no token and no archive row" finding naming one of them
- node util/checks/archive-rows-travel.mjs . exits 0, and so does node util/checks/archive-rows-name-an-object.mjs .
- no row names a token whose note is live under doc/work, decided by: comparing the row ids against doc/work/wk-*.md
- the battery reports no new failure against the run before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

