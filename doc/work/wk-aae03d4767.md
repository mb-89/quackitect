---
id: wk-aae03d4767
seq: 1000225
type: work
title: the bar filters rows
status: spec_open
assignee: main
scope: single-step
traced: true
minted_by: main
---

## detail

Split out of wk-1b7c1a2da1. The heading line carries a filter box, and what a person types in it narrows what the panes draw without writing the view file. Decision owed: the syntax is the log's, but its reader is src/viewer/filter.go in module quackitect/viewer, unreachable from the engine and the extension. Pick one: move the reader into a package both modules import, or name the engine invocation that runs it, with arguments built in src/extension/engineargs.ts. Or rule for src/engine/expr.go, which is a second syntax and has to be argued against the person's words. Level 0 rules in doc/levels/level-0-design.md lines 361-362 that an invalid expression does not empty the view, says so, and keeps the last good filter. The criteria have to name the text typed, the rows still drawn and the rows that go, and carry those two rulings.
