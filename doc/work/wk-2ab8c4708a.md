---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: shell writes leave delta
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-nancarrow
claimed_at: "2026-09-05T14:41:57Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - bf35b17b9babbb115f1629838c8b289a67f90a0e
  - 1aaf18130f8c9ec118468bf6e0544e26da8901c5
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 1627e7b0a65dc0fad75acc1bd8237f2365495640
---

## detail

Found reviewing wk-70dde20ba7, "delta is the token's".

src/engine/tests.go, in TestTheDelta: if wrote, proven := WhatThisTokenWrote(r, on); proven { out.Delta = onlyWhatItWrote(delta, wrote) }.

tokenwrote.go states the invariant in words: "A write the engine cannot prove is one it will not silently drop." The code holds it only all-or-nothing. proven goes true on the first apply on record, and every file the same token wrote by a shell command under se run is then dropped from the delta with nothing said.

MEASURED, on a clean copy of HEAD (git archive HEAD, then src/engine): a token that applied one.md and then wrote byshell.md with os.WriteFile answered delta map[one.md:true], whole false, why_whole empty. byshell.md is a real change nothing chose a test for and nothing in the answer names.

An agent that runs a generator, a formatter or a sed under a token gets a narrower battery than its change earns, and reads green.

## done when

- a Go test in src/engine drives a token that applies one file and writes a second outside an apply, and se test's answer for it either carries the unproved write or names it
- the sentence in tokenwrote.go and the code agree: either the invariant is narrowed in words to the empty-record case, or the code stops dropping unproved writes

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One field on the answer, one function beside the narrowing, two lines in TestTheDelta, and one test. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | The first is the new test by name, TestAShellWriteIsNamedRatherThanDropped. The second is read by whoever reviews, in tokenwrote.go, where the words and the code sit together. |  |
| [x] | the basics it stands on exist, or are minted first | The apply journal, WhatThisTokenWrote and the delta are all here. The test stands on aTreeWithTests, aTokenTaking and wrote, which deltaisthetokens_test.go already carries. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 12 put the red first, rule 13 kept both halves. |  |
| [x] | one test was written first and seen red for the reason expected | TestAShellWriteIsNamedRatherThanDropped said `the answer names nothing of the write no apply journalled`, and printed an answer whose delta was one.md alone. Expected, as byshell.md is in no journal. |  |
| [x] | the same test was seen green after the change, and named | On a copy of the tree at HEAD it passes, with left_out naming byshell.md, and TestTheDeltaIsWhatThisTokenWrote passes beside it. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | tokenwrote.go carries the words and leftOut. tests.go gains the field and one call. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Token wk-1a33c35541. A copy of HEAD does not compile, because main.go hands callTheEngine a context it does not take. |  |

