---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: starting is not passing
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-06T18:37:18Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - a1d25d7ecabad08c0de4f0036457ff32d8087fc0
---

## detail

Commit 6239efc made a started battery answer pending rather than ok, and said so in the code: STARTING IS NOT PASSING. Three tests that read the old meaning were left as they were.

src/engine/battery_test.go:27, src/engine/batterycancel_test.go:48 and src/engine/batteryshell_test.go:76 each assert got.OK and report the battery did not start. A started battery now answers OK false and Pending true, so all three fail on every battery run.

MEASURED, September 2026: the battery on this box reported go test engine FAIL with these three and nothing else.

THIS IS THE PATTERN THE OWNER NAMED. A mechanism changed and what still referred to it was left behind. The change was right and the readers of it were not carried along.

## proposed action

Each of the three asserts got.Pending rather than got.OK, and says a start answers pending rather than that it did not start.

## done when

- the three tests assert Pending, and no test in src/engine reads got.OK off startBattery
- se test names the three green

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The battery goes green on its own suite, so the next red in it means something. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | Three reds every battery, so a fourth is read as the usual three. |  |
| [x] | the ask is small enough to review whole, or it is split first | Four assertions in three files. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | se find over src/engine for got.OK, and se test on this token. |  |
| [x] | the basics it stands on exist, or are minted first | ran.Pending existed before this. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token, read. The change is the three readers, and nothing beside them. |  |
| [x] | one test was written first and seen red for the reason expected | The three were already red, and the battery's page is the reading: each said the battery did not start on an answer that says it did. |  |
| [x] | the same test was seen green after the change, and named | se test on this token: all seven battery tests green, including the three. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | battery_test.go, batterycancel_test.go and batteryshell_test.go. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change: the cancelled-context test read ok alone, which a started battery would have passed. It now reads neither ok nor pending. |  |

