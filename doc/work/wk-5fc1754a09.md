---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: A criterion already green
# where the token stands. The process owns these values.
status: open
---

## detail

Found reviewing wk-8797959d3c, which minted wk-cda42858cd.

wk-cda42858cd's first done-when reads: src/engine/pull.go asks Unleashed at one site for the hand-out, decided by se find with regex Unleashed over path src/engine/pull.go returning two hits, the hand-out and AskToStop.

Run on the tree at a2c87a9e, before any of that token's work: exactly two hits, the hand-out in answerFor and the one in AskToStop. The duplicate site the token exists to remove is in whatComesNext and reads LoadBinding(r).At == Unbound, which that regex never matches. So the criterion is green before the work and cannot redden.

Its proposed action says keep the cut closest to the queue. The cut closest to the queue is the one in answerFor, which is the one carrying the God reach and the notice, so a hand following the token literally keeps both.

## proposed action

Rewrite wk-cda42858cd's first done-when to match what both sites hold at run time rather than one spelling of one of them: a count of the guards on the hand-out however each is written, or a test that reads the one notice an unbound pull answers. Say in the proposed action which site is kept and why.

## done when

- wk-cda42858cd's first done-when is red on the tree at a2c87a9e and green once the duplicate is gone, decided by running the command that criterion names on both trees and comparing the two answers.
- wk-cda42858cd's proposed action names the site it keeps by its function, not by its position.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

