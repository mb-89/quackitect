---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: Dead claim strands note
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-06T20:13:12Z"
# true when this waits for a person rather than an agent
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - baa964671e91e38b433a5e68823dcb0733a10f0f
---

## detail

A NOTE A DEAD BOX CLAIMED CANNOT BE TURNED IN BY ANYONE, AND THE STOP GATE BILLS IT TO WHOEVER IS HERE.

doc/work/wk-ac18ea020a.md is a note at status noted, claimed by aeaf7bd9/reviewer-nono at 13:58:37Z. That box is gone, and the claim lapses about three hours later.

Measured here on 2026-09-05. A stop refused twice with "1 NOTE(S) ARE STILL OPEN, AND THEY DIE WITH THIS BOX", naming wk-ac18ea020a. A claim on that id answered "it is claimed by aeaf7bd9/reviewer-nono, wait for that claim to lapse". So the gate demands the note be turned in, and the claim refuses the only call that turns it in. No session on this box satisfies both.

Two faults, and they are separable.

One, the count is not true. The gate says the note dies with this box. This one does not. It lives in doc/work, which git carries, and its own detail records that an earlier session moved it there from .se/work for that reason. The gate counts by kind, note, rather than by where the file sits.

Two, a claim outlives the box that took it. A reclaimed box cannot release what it holds, so every note it claimed is frozen for the rest of the window. That is the shape of the claims problem on wk-4759d90994: a cloud box cannot publish, and it cannot release either.

The note's content is not urgent. It carries a verdict for wk-fc6b6c5aa7 that reviewer-hazel is giving live, so dropping it loses nothing.

## proposed action

Decide which half to fix. The narrow fix stops counting a note against the gate once it sits in doc/work. The gate's reason for counting is that notes die with the box, and a tracked one does not. The wider fix lets a claim be broken when the box holding it is gone, rather than only when the window runs out. A person picks, because the wider one changes what a claim means.

THE NARROW HALF IS DONE, AND THE WIDE HALF IS NOT. This token's own done-when lines describe only the narrow one, so they pick it.

THE WIDE HALF IS LEFT ON PURPOSE. The owner ruled in September 2026 that a box is one actor and a claim is the box's. Breaking a claim early changes when it lapses, which is theirs to settle. wk-4759d90994 carries it.

## done when

- a note in doc/work claimed by an absent box does not refuse a stop, decided by: a stop naming decision answers granted while doc/work/wk-ac18ea020a.md stands as it is
- a note still in .se/work does refuse a stop, decided by: the same call with an untracked note open answers with the open note count
- both cases are a test in src/engine, seen red first on the doc/work case. All three lines are TestANoteOnTheBranchDoesNotRefuseTheStop, which drives one note through both places
- the battery reports no new failure against the run before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The gate counts what actually dies here, so its demand can always be met. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | No session on the box satisfies both the gate and the claim. Measured: a stop refused twice over a note nobody could turn in. |  |
| [x] | the ask is small enough to review whole, or it is split first | One condition in one function. The wide half is left to wk-4759d90994. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | All three by se test on TestANoteOnTheBranchDoesNotRefuseTheStop. |  |
| [x] | the basics it stands on exist, or are minted first | noteAt and TrackedDir both existed. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token, read. The narrow half only, as the done-when lines ask. |  |
| [x] | one test was written first and seen red for the reason expected | The test drives the note in both places and asserts the refusal in one. |  |
| [x] | the same test was seen green after the change, and named | TestANoteOnTheBranchDoesNotRefuseTheStop, green, with five neighbours over a worktree of origin/v4. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | notesgohome.go, anoteonthebranch_test.go, notesgohome_test.go, refusalflagsexist_test.go. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change. Six call sites drove TooManyNotes with se_apply, which heldDuringShortfall no longer names. Two were red on the branch and four could not redden. The twin of wk-6a09907c72. |  |

