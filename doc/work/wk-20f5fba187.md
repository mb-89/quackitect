---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: note misreports readable snapshot
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/main
claimed_at: "2026-09-07T15:43:58Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 81f2b211e80d7cad90f5a4609bcd166345587dad
  - bbeaeaaef9d63a07b50252a07712b0bac6332590
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - ac066fae6a15b2f2b5b1c0267962efd65163a969
---

## detail

Found reviewing wk-c7d3a72daa (the gate records nothing).

doc/work/wk-c7d3a72daa.md line 120 says: "7b85042f is no object in this clone, so the change reads against HEAD."

It is an object in this clone, and it always was. `git cat-file -t 7b85042f` answers commit. It is held by the local ref refs/se/steps/7b85042fb562, and its parent e7ba69b9 is on group/tests, so the engine wrote it on this box when the work was taken up. Nothing fetched it in later.

The span it dismisses is the clean one. `git diff 7b85042f 7ffca29b` is exactly this token's change and nothing else: doc/work/wk-c7d3a72daa.md and src/engine/testedgate_test.go, two files. The second began snapshot, 07cb4edb, is the one that cannot be used, because its span to ended sweeps in other agents' work from the shared tree.

The damage is on the review rather than the code. [[reviewing]] tells a reviewer to read every hunk of the named span, and a note saying the span is unreadable sends them to HEAD instead. reviewing.md is explicit about the cost: a reviewer who takes such a claim at its word has reviewed the note. Here the authoritative diff was sitting there and the note said it was not.

The sentence looks carried over from wk-4e91c7b3f8, where it is true: 78a9007e really is absent there. It was not rechecked here.

Fix: say which snapshot is unreadable after asking git, or drop the sentence. A note claiming a snapshot is no object should have run `git cat-file -t` on it first.

## done when

- wk-c7d3a72daa.md no longer calls 7b85042f absent, decided by: git cat-file -t 7b85042f and compare with the note
- a note claiming a snapshot is no object is right about it, decided by: git cat-file -t on each id such a sentence names
- a check refuses a note that calls a snapshot absent when git resolves it, decided by: run that check over doc/work

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A reviewer reads the span the token names. The clean diff was sitting there and the note said it could not be read. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | A reviewer who takes the claim at its word reviews the note rather than the change, which is the cost reviewing names outright. |  |
| [x] | the ask is small enough to review whole, or it is split first | — |  |
| [x] | every done-when line is decidable, and names the command where one decides it | All three, and git cat-file -t decides the first two outright. |  |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [x] | one test was written first and seen red for the reason expected | The check on a probe note saying began is 7b85042f and is no object here. It refuses it and names the id git answers for. |  |
| [x] | the same test was seen green after the change, and named | an-absent-snapshot-is-absent answers ok over the tree, and green on the same probe naming 78a9007e, which git does not resolve. It judges the claim rather than flagging any claim. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | 81f2b211 and bbeaeaae both resolve here, and this note is written under the second. The change is commit bafb8030. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The six notes carrying the wrong sentence are all swept, and their text is not reachable in this clone. Nothing here can edit them. |  |

