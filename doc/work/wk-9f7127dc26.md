---
id: wk-9f7127dc26
seq: 1000101
type: work
title: a value read halfway
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-2
---

## detail

In util/checks/engine-spawns.mjs whatItResolvesTo captures an assignment with `([^;\n]+)`, so a value spanning lines arrives as its opening bracket. whatThisIs then classifies the fragment as clean, so a flag held in an object two lines below passes. Write the reader's window beside it, to the end of the line, the semicolon or the balanced bracket. Make a value that reaches the edge of the window a refusal in the words an unreadable value gets. Never hand a fragment to the classifier. Drive every case twice, on one line and broken over lines the way the tree writes it. Sweep the other source-reading checks under util/checks for the same one-line window and say what each answers. Related: wk-1412093cd8, wk-95d84f593e, wk-f22cbd19f4.
