---
id: wk-50805e98a1
seq: 56
type: work
title: sets belongs with property
status: aborted
assignee: main
scope: single-step
traced: true
disposition: dropped
reason: "The remedy already landed: src/engine/viewedit.go carries sets over only when the property is unchanged, and drops it when the property changes, so a heading can no longer name one field while a drop onto it writes another. TestAGroupingKeepsWhatADropWrote at src/engine/viewedit_test.go:484 drives it. Obsolete."
aborted_from: backlogged
minted_by: reviewer4
---

## detail

State carried across a rewrite that was a fact about the value being replaced. viewedit.go SetLevel rewrites a level in place and keeps the old level's sets: levels[at] = level{property: property, sets: levels[at].sets, direction: direction}. sets names the field a row dropped onto that level's heading is filed under, and it is a fact about the OLD property. When the property changes it is no longer true, and the heading then names one thing while a drop onto it writes another.

REPRODUCED in .se/scratchpad/reviewer4/sets-heading.sh: a view grouping by if(bucket, bucket, status) with sets: bucket draws headings where a drop files a bucket. After se view --group assignee the file says property: assignee with sets: bucket, and the page draws a heading named main, grouped by assignee, where a drop writes bucket = main. A bucket named after a person, and the row does not move under the heading it was dropped on.

WHAT TO DO: carry sets over only when the property is unchanged, and drop it when the property changes, because a level with no sets draws headings nothing can be dropped onto, which is the honest state for a grouping nobody has said how to file into.

THE CHECK, RED TODAY: rewrite a grouping level IN PLACE with a different property and require sets to be gone; rewrite it in place with the same property and require sets kept. TestAGroupingKeepsWhatADropWrote only ever appends a level at a new position, so it cannot see the in-place branch at all. Found on wk-080da4232c.

