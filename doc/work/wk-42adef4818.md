---
id: wk-42adef4818
seq: 1000018
type: work
title: an observation names words
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
depends_on:
  - wk-40ba94619e
rounds: 3
evidence:
  - outcome
minted_by: person
submitted_by: main
reviewed_by: rev-40
---

## detail

A recorded observation carries a line number, which moves whenever anybody adds a function above it. The owner said: why are you baking line numbers into assertions. An observation names the test and what it said, the file may stay, and the line goes. util/checks/count-observations.py goes into version control. Related: wk-40ba94619e, wk-2b78b911b1.

## done when

- a recorded observation names the test and what it said, matched by the prefix of each Errorf and Fatalf format string under src cut at its first verb
  `rg -q func.TestAnObservationNamesItsTest src/engine && go test -C src/engine -count=1 -run TestAnObservationNamesItsTest$ .`
- the checker refuses an observation naming a test the tree does not declare
  `rg -q func.TestARedSaidNamingNoTestIsCaught src/engine && go test -C src/engine -count=1 -run TestARedSaidNamingNoTestIsCaught$ .`
- every observation in doc/work and .se/work names a test the tree declares, unmatched ones counted and left, baseline derived from a checkout by util/checks/count-observations.py
  `rg -q func.TestNoObservationNamesALine src/engine && go test -C src/engine -count=1 -run TestNoObservationNamesALine$ .`
- the method says it in doc/guidance/specifying.md and doc/guidance/behaviour.md under Evidence, each check reading only that section
  `rg -q func.TestTheObservationRuleIsInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheObservationRuleIsInBothFiles$ .`
- the rule reaches every projection of behaviour.md named in util/projections.json
  `rg -q func.TestTheObservationRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheObservationRuleReachesEveryProjection$ .`
- every command a live criterion names is a file git tracks
  `rg -q func.TestEveryCommandACriterionNamesIsTracked src/engine && go test -C src/engine -count=1 -run TestEveryCommandACriterionNamesIsTracked$ .`
- the whole battery is green afterwards
  `sh util/checks/battery.sh`
- every test above was watched failing on its own assertion with the change absent, the evidence naming the test and what it said

## evidence: outcome

src/engine gains the six tests the criteria name, the rule sits in both method files and every projection, and util/checks/count-observations.py is tracked.
