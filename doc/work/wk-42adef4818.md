---
id: wk-42adef4818
seq: 1000018
type: work
title: an observation names words
status: spec_open
assignee: main
scope: single-step
traced: true
depends_on:
  - wk-2b78b911b1
rounds: 1
minted_by: person
---

## detail

AN OBSERVATION RECORDS A LINE NUMBER, WHICH IS AN ASSERTION ABOUT SOMETHING NOBODY CONTROLS. THE OWNER'S WORDS: why are you even baking line numbers into assertions, that is insane, this is again that thing where you make assertions about something that you have no control over and then somebody changes line numbers and then it is not fixed anymore. HOW IT GOT THERE. The Go runner prints file, line and message on one line, and the recorded red is a copy of that whole line. The line number was never chosen, it was carried along, which is how a fragile claim gets into a record without anybody deciding to put it there. WHAT IS DURABLE AND WHAT IS NOT. The message is durable, because it is a string in the source. The test name is durable. The file is nearly durable. The line number moves whenever anybody adds a function above it, and it moved twice in one session while this token's own work was going on. IT BUYS NOTHING. The line exists so a reader can find the assertion. A reader can find the assertion by searching for the message, which is what a person actually does, and which keeps working after an edit. AND I MADE IT WORSE. TestEveryRedSaidResolves, added on wk-2b78b911b1, requires every recorded red to name a line that carries an assertion. That check institutionalises the fragile half: it goes red on an edit that changed nothing, which is a check that cries about the tree rather than about the work. THE SHAPE THAT MAY WORK. A recorded red names the test and what it said. The checker finds the assertion by its words rather than by its address, and fails when no test in the tree says that. wk-2b78b911b1's own criterion 6 is rewritten the same way. WHAT IS UNCERTAIN. Whether the file name stays, which is nearly free and helps a reader, and what to do with the observations already recorded with line numbers in them, since rewriting a record is not something this project does lightly.

THE SHAPE TAKEN, AND IT IS THE SECOND OF THE THREE THE DETAIL NAMES. The file
name stays and the line number goes. A file is nearly durable and it costs
nothing to keep. A line moves whenever anybody adds a function above it, and it
moved twice in one session while this token's own subject was being worked on.

IT WAITS ON wk-2b78b911b1, WHICH IS THE ONE COUPLING. That token's sixth
criterion names TestEveryRedSaidResolves, the check this one replaces, so
changing the check while that token is being reviewed would take the ground out
from under its reviewer. This work starts when that token closes, and rewriting
its criterion to name the new check is part of this token rather than a repair
somebody makes on the way past.

AND THE BACK CATALOGUE IS MIGRATED RATHER THAN LEFT. A record half in one form
and half in another is a record a reader has to guess at, and the checker cannot
be written until one form is the only form. The migration is mechanical: for
every observation that names a file and a line, find the test whose assertion
carries that message and write the test's name in place of the address. Where no
test in the tree carries it, the observation is left as it stands and named in
the evidence, because rewriting a claim nobody can verify is worse than leaving
a stale one.

## done when

- A recorded observation names the test and what it said, and the checker finds the assertion by its words rather than by its address: it takes the message, requires some line under src to carry it, and requires the test it names to exist. IT REPLACES TestEveryRedSaidResolves, which asserts the address and goes red on an edit that changed nothing
  `rg -q func.TestAnObservationNamesItsTest src/engine && go test -C src/engine -count=1 -run TestAnObservationNamesItsTest$ .`
- The checker refuses an observation naming a test the tree does not declare, which is the failure a line number could never catch: a message copied from a run that no longer exists
  `rg -q func.TestARedSaidNamingNoTestIsCaught src/engine && go test -C src/engine -count=1 -run TestARedSaidNamingNoTestIsCaught$ .`
- No observation anywhere in the record still names a line: the check walks every token in doc/work and .se/work, reads every red said, and fails naming the token and the observation that carries an address
  `rg -q func.TestNoObservationNamesALine src/engine && go test -C src/engine -count=1 -run TestNoObservationNamesALine$ .`
- The method says it, in doc/guidance/specifying.md under the heading about what a criterion has to be and in doc/guidance/behaviour.md under Evidence, and each check reads the section that heading opens rather than the whole file, failing by name on the file that is short
  `rg -q func.TestTheObservationRuleIsInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheObservationRuleIsInBothFiles$ .`
- The rule reaches every agent, and the harnesses are derived rather than typed: the check asks util/projections.json for every target whose sources include doc/guidance/behaviour.md, refuses when that answers none, and requires the rule in each one
  `rg -q func.TestTheObservationRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheObservationRuleReachesEveryProjection$ .`
- The whole battery is green afterwards. THIS IS A STANDING RULE OVER THE PROJECT rather than an assertion about this change: it is true before the work and has to be true after
  `sh util/checks/battery.sh`
  **red without** the package made not to build, in a copy of the tree
  **red said** battery.sh: go build FAIL, and 10 failed at the end of the run
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. For the record-wide check the change taken away is one observation rewritten to name a line again, and the red names that token. THE EVIDENCE NAMES THE TEST AND WHAT IT SAID, which is the form this token exists to establish, so it is written in the form it argues for

## finding 1 · round 1 · done when, criterion 1: "the checker finds the assertion by its words rather than by its address: it takes the message, requires some line under src to carry it" and criterion 3: "No observation anywhere in the record still names a line" / detail: AND THE BACK CATALOGUE IS MIGRATED RATHER THAN LEFT · by reviewer10

**wrong:** The remedy swaps an address for a literal the producer never writes, and I measured how far that reaches rather than reasoning about it. Criterion 1 says the checker takes the message and requires some line under src to carry it. Criterion 3 says no observation anywhere in the record still names a line. A Go test message is nearly always produced by Errorf or Fatalf with format verbs, so what the runner printed and what the source holds are two different strings by construction, not two spellings of one. I walked both stores, took every recorded red said, and counted. There are 62 of them. 32 carry a file and line address. Of those 32, the message text appears literally somewhere under src for 6, and does not for 26. The failures are ordinary ones, not odd cases: the engine knows 10 states and the owner named 11 followed by a rendered slice, a token with nothing said starts at imp_open, work a reviewer took is imp_submitted. Every one of them came out of a format string. So criterion 1's checker refuses 26 of the 32 observations it is being written to accept, and criterion 3 cannot be met by the migration the detail describes. The detail says the migration is mechanical, find the test whose assertion carries that message and write the test's name in place of the address, and for 26 of 32 no test carries that message as text. The detail also has a policy for that case, where no test in the tree carries it, the observation is left as it stands and named in the evidence, and criterion 3 forbids exactly what that policy permits, with no exception in its sentence. So the token cannot close: the criterion and the detail's own migration rule disagree about 26 rows of the record, and the only ways to make criterion 3 green are to rewrite claims nobody can verify, which the detail refuses, or to delete them. The second cost is worse than the first and it is permanent. Criterion 3's check walks doc/work and .se/work and runs inside the standing battery, so once it is agreed the whole tree goes red for every reviewer the next time anybody records an observation whose message happens to carry something shaped like an address, on a token they have nothing to do with.

**satisfies:** Decide what a durable observation is against what the runner actually produces, and write the answer in the detail with the count. The reading is in this finding: 62 observations, 32 with an address, 6 whose message is a source literal and 26 whose message is not, taken over doc/work and .se/work. The shape that survives that count is the test name, which is durable and IS a literal in the source, so let the checker require the test the observation names to exist and to be a test, and let the message be the reader's half rather than the checker's. If the message is to be checked at all, check it the way a format string allows: take the leading run of the message up to the first rendered value and require some Errorf or Fatalf under src to carry that prefix, and say in the criterion that this is a prefix match on a format string and why a whole-message match cannot work. Then give criterion 3 the exception the detail already has, naming it: no observation names a line except those the migration could not resolve, which are listed in the evidence with the reason, and the count of them is derived rather than typed. And say whether criterion 3 is a one-time assertion about this migration or a standing rule over every future observation, because it runs in the battery and doc/guidance/specifying.md asks a drafter to say which. Take the reds on the cases that decide it: run the checker over all 62 before agreeing the draft and paste what it answered, and rewrite one migrated observation back to an address and watch the record-wide check name that token.

## finding 2 · round 1 · done when, criterion 6: "The whole battery is green afterwards" · by reviewer10

**wrong:** The criterion has no command and one command decides it: bash util/checks/battery.sh, which exists, is what every agent on this tree runs, and exits zero when it is green. doc/guidance/specifying.md says a criterion that can be a command is one, because a command fails and a sentence does not. Leaving it as prose is not free, and I read the engine rather than guessing at the cost. UnmetCriteria in src/engine/spec.go answers a criterion with no command by requiring an evidence section named after the criterion's own sentence, so the worker writes a section called The whole battery is green afterwards and asserts it in words, and a reviewer judges a sentence where an exit code was available. Extent, over the seven criteria here: this is the one, since criteria 1 to 5 carry commands and criterion 7 is the reds, which genuinely cannot be a command. It is also the second draft minted today with this exact line: wk-1b7c1a2da1 carries the same criterion in the same words with no command, which I rejected for it earlier, so the phrasing is travelling between drafts.

**satisfies:** Give it the command, bash util/checks/battery.sh, and keep the sentence. Say in it whether it is a standing rule over the project or an assertion about this change, which doc/guidance/specifying.md asks a drafter to say. Then take its red the way criterion 7 asks of the others: break one thing the battery covers in a copy of the tree, run it, record the check that failed and what it said, and put it back.

## lesson 1 · round 1 · by reviewer10

**the class:** A REMEDY THAT SWAPS A FRAGILE ADDRESS FOR A LITERAL THE PRODUCER NEVER WRITES. A record keeps a pointer that breaks when the thing under it moves, a line number, an index, an offset. The right diagnosis is reached, that the pointer is an assertion about something nobody controls, and the remedy is to keep the CONTENT instead and look it up by its words. Then the content turns out not to exist as words anywhere: it was rendered, not written. A test message comes out of a format string, a log line out of a template, a rendered page out of a component, so what the record holds and what the source holds are two different strings by construction rather than two spellings of one. The remedy reads as obviously more durable than what it replaces, which is why nobody counts. It is worse than leaving the pointer, because the old form was merely stale and the new one cannot be produced at all, so the migration stalls on rows nobody can convert and the check written for the new form refuses the record it was written to accept. Measured on wk-42adef4818: of 62 recorded observations, 32 name an address, and the message text of 26 of those 32 appears nowhere under src, because every one came out of an Errorf.

**instead:** Two halves, and the first is what stops it being made. Before replacing a pointer with content, go to the thing that PRODUCED the content and ask whether it holds it as text. A format string does not, a template does not, and a renderer does not, so in those cases the durable handle is the name of the producer, the test, the template, the function, and not the words it emitted. Choose the handle from the producer's side rather than from the record's side, because the record shows you the output and the output is the one thing that was never written down anywhere. The second half is the check that catches it, and it is a count you can run before writing a line of the remedy: take every row already in the record, apply the new rule to each, and count how many it can convert. Paste that count into the draft. A migration whose rule converts a minority of the rows it is about is a rule chosen from the examples somebody happened to look at, and the count is the only thing that says so before the work starts.

**minted as:** wk-89f6c3d7a2

