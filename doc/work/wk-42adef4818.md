---
id: wk-42adef4818
seq: 1000018
type: work
title: an observation names words
status: spec_submitted
assignee: main
scope: single-step
traced: true
depends_on:
  - wk-2b78b911b1
minted_by: person
---

## detail

AN OBSERVATION RECORDS A LINE NUMBER, WHICH IS AN ASSERTION ABOUT SOMETHING NOBODY CONTROLS. THE OWNER'S WORDS: why are you even baking line numbers into assertions, that is insane, this is again that thing where you make assertions about something that you have no control over and then somebody changes line numbers and then it is not fixed anymore. HOW IT GOT THERE. The Go runner prints file, line and message on one line, and the recorded red is a copy of that whole line. The line number was never chosen, it was carried along, which is how a fragile claim gets into a record without anybody deciding to put it there. WHAT IS DURABLE AND WHAT IS NOT. The message is durable, because it is a string in the source. The test name is durable. The file is nearly durable. The line number moves whenever anybody adds a function above it, and it moved twice in one session while this token's own work was going on. IT BUYS NOTHING. The line exists so a reader can find the assertion. A reader can find the assertion by searching for the message, which is what a person actually does, and which keeps working after an edit. AND I MADE IT WORSE. TestEveryRedSaidResolves, added on wk-2b78b911b1, requires every recorded red to name a line that carries an assertion. That check institutionalises the fragile half: it goes red on an edit that changed nothing, which is a check that cries about the tree rather than about the work. THE SHAPE THAT MAY WORK. A recorded red names the test and what it said. The checker finds the assertion by its words rather than by its address, and fails when no test in the tree says that. wk-2b78b911b1's own criterion 6 is rewritten the same way. WHAT IS UNCERTAIN. Whether the file name stays, which is nearly free and helps a reader, and what to do with the observations already recorded with line numbers in them, since rewriting a record is not something this project does lightly.

THE SHAPE TAKEN, AND IT IS THE SECOND OF THE THREE THE DETAIL NAMES. The file
name stays and the line number goes. A file is nearly durable and it costs
nothing to keep. A line moves whenever anybody adds a function above it, and it
moved twice in one session while this token's own subject was being worked on.

IT WAITS ON wk-2b78b911b1, WHICH IS THE ONE COUPLING. That token's sixth
criterion names TestEveryRedSaidResolves, the check this one replaces, so
changing the check while that token is being reviewed would take the ground out
from under its reviewer. This work starts when that token closes, and rewriting
its criterion to name the new check is part of this token rather than a repair
somebody makes on the way past.

AND THE BACK CATALOGUE IS MIGRATED RATHER THAN LEFT. A record half in one form
and half in another is a record a reader has to guess at, and the checker cannot
be written until one form is the only form. The migration is mechanical: for
every observation that names a file and a line, find the test whose assertion
carries that message and write the test's name in place of the address. Where no
test in the tree carries it, the observation is left as it stands and named in
the evidence, because rewriting a claim nobody can verify is worse than leaving
a stale one.

## done when

- A recorded observation names the test and what it said, and the checker finds the assertion by its words rather than by its address: it takes the message, requires some line under src to carry it, and requires the test it names to exist. IT REPLACES TestEveryRedSaidResolves, which asserts the address and goes red on an edit that changed nothing
  `rg -q func.TestAnObservationNamesItsTest src/engine && go test -C src/engine -count=1 -run TestAnObservationNamesItsTest$ .`
- The checker refuses an observation naming a test the tree does not declare, which is the failure a line number could never catch: a message copied from a run that no longer exists
  `rg -q func.TestARedSaidNamingNoTestIsCaught src/engine && go test -C src/engine -count=1 -run TestARedSaidNamingNoTestIsCaught$ .`
- No observation anywhere in the record still names a line: the check walks every token in doc/work and .se/work, reads every red said, and fails naming the token and the observation that carries an address
  `rg -q func.TestNoObservationNamesALine src/engine && go test -C src/engine -count=1 -run TestNoObservationNamesALine$ .`
- The method says it, in doc/guidance/specifying.md under the heading about what a criterion has to be and in doc/guidance/behaviour.md under Evidence, and each check reads the section that heading opens rather than the whole file, failing by name on the file that is short
  `rg -q func.TestTheObservationRuleIsInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheObservationRuleIsInBothFiles$ .`
- The rule reaches every agent, and the harnesses are derived rather than typed: the check asks util/projections.json for every target whose sources include doc/guidance/behaviour.md, refuses when that answers none, and requires the rule in each one
  `rg -q func.TestTheObservationRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheObservationRuleReachesEveryProjection$ .`
- The whole battery is green afterwards
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. For the record-wide check the change taken away is one observation rewritten to name a line again, and the red names that token. THE EVIDENCE NAMES THE TEST AND WHAT IT SAID, which is the form this token exists to establish, so it is written in the form it argues for

