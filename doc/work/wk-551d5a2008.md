---
id: wk-551d5a2008
seq: 1000063
type: work
title: the sidebar mints nothing
status: spec_open
assignee: main
scope: single-step
traced: true
rounds: 1
minted_by: person
---

## detail

THE LINE EDIT IN THE SIDEBAR DOES NOT MINT. THE OWNER'S WORDS: the slash detail check, I just tried it, and it does not seem to work, I do not see the token, it does not seem like if I entered work in the sidebar it enters the system. MEASURED BY THE PERSON WHO USES IT, while trying wk-bae93614a3, which is a token assigned to them asking for exactly this control to be checked. WHY IT MATTERS MORE THAN ITS SIZE. The sidebar line is how a person mints work, and it is the one control a person uses most. A control that silently does nothing is the defect the controls-reach-the-engine token exists for, arriving at the one place nothing drives: engine-args.mjs drives mintArgs against the real binary and drive-editor.mjs drives the editor page, and the sidebar panel's own line edit is driven by neither. WHERE TO LOOK, and it is a list of places rather than a diagnosis, because the diagnosis is the work. mintArgs in src/extension/engineargs.ts builds the arguments and returns undefined when it cannot read the line, and a caller that drops that is a mint that vanishes. mintWork in src/extension/extension.ts calls it and returns early on undefined without telling anybody. panel.ts draws the line and sends the message. And the kind picker sends SS and a middle dot and T, which mintArgs reads with readKind. FIND OUT WHICH AND SAY SO, because a token that reads a control does not exist and a token whose message was dropped are different faults with different fixes. AND WHATEVER IT IS, IT ENDS WITH A CHECK THAT DRIVES THE PANEL THE WAY drive-editor.mjs DRIVES THE EDITOR, because this control has no driver at all and that is why nobody knew. A CONTROL THAT FAILS SILENTLY IS THE WORST KIND: the person typed the work, watched it vanish, and had no way to tell whether the engine refused it or nothing was sent.

READ RATHER THAN GUESSED, AND THE ANSWER IS THAT EVERY FAILURE ON THAT PATH IS
SILENT. The chain is panel.ts sending a run message with the text and the kind,
extension.ts executing the command with them, mintWork building the arguments,
and askEngine spawning the engine. THREE PLACES RETURN WITHOUT TELLING ANYBODY.
mintArgs answers undefined when the line is empty and mintWork returns on it.
askEngine resolves undefined when there is no work root or no binary, and
mintWork reads out?.error off undefined, which is undefined, so nothing is
shown. And a spawn that errors resolves undefined the same way. Only one path
speaks: an engine that answers with an error field.

SO THE DIAGNOSIS IS NOT WHICH ONE FIRED. It is that a person cannot tell, and
neither can I from here. Whichever of the three it was, the person typed the
work, watched it vanish, and had no way to learn whether the engine refused it
or nothing was sent. THE FIX IS THAT NONE OF THEM IS SILENT, and then the cause
names itself the next time.

AND THE CONTROL IS DRIVEN, because that is why nobody knew. engine-args.mjs
drives mintArgs against the real binary and drive-editor.mjs drives the editor
page. The sidebar panel is driven by neither, so the one control a person uses
most is the one control nothing presses.

## done when

- Every way the mint can fail says so where the person is looking, rather than returning without a word: an empty line, no work root, no binary, a spawn that errors, and an engine that refuses. The check drives each one and requires a message naming which
  `rg -q func.TestEveryWayAMintFailsSaysSo src/extension && node util/checks/drive-panel.mjs .`
- The panel is driven the way the editor page is: the page is loaded into a DOM, the line is typed into, the key is pressed, and what it sent is read back, so a control that sends nothing is caught by the check rather than by a person
  `node util/checks/drive-panel.mjs .`
- What the line sends reaches the engine as a call the engine answers, driven through the real binary rather than against a description of it, and the token it mints comes back from the engine rather than being assumed
  `node util/checks/drive-panel.mjs .`
- The slash rule wk-bae93614a3 asks about is driven rather than read: a line with a slash mints a token whose title is the words before it and whose detail is everything after, and a line with no slash mints all title and no detail
  `node util/checks/drive-panel.mjs .`
- The whole battery is green afterwards. THIS IS A STANDING RULE OVER THE PROJECT rather than an assertion about this change
  `sh util/checks/battery.sh`
  **red without** the package made not to build, in a copy of the tree
  **red said** battery.sh: go build FAIL, and 10 failed at the end of the run
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. The red is taken once per way it can fail rather than once for the check, and the evidence names the test and what it said rather than a line number

## finding 1 · round 1 · done when, criterion 1 / runs: "rg -q func.TestEveryWayAMintFailsSaysSo src/extension && node util/checks/drive-panel.mjs ." · by reviewer12

**wrong:** THE GUARD IS A GO SEARCH POINTED AT A TYPESCRIPT DIRECTORY, SO IT CANNOT MATCH ANYTHING THE WORK WILL WRITE. src/extension is TypeScript: agent.ts, controls.ts, editor.ts, engineargs.ts, extension.ts, keys.ts, liveness.ts, panel.ts and build.mjs. I ran the guard rather than reading it, and against the tree as it stands it exits 1. Then I asked the question that decides this, which is whether the work could ever make it exit 0. The pattern needs the four letters func, then exactly one character, then Test. A TypeScript declaration reads function TestEveryWayAMintFailsSaysSo, which puts five characters between the two, so it does not match. I drove that case rather than reasoning about it: piping function TestEveryWayAMintFailsSaysSo through the same pattern exits 1. The guard was copied from the Go criteria elsewhere in this queue, where src/engine really does hold func TestX. THE FAILURE IS THE ONE THAT FAILS SHUT. The worker writes exactly the check the criterion names, the search finds nothing, and the criterion is red with the work done. The two ways out are a decoy string carrying the guard's spelling, which is worse than no guard, or editing the criterion, which a worker may not do. So it is discovered at the keyboard by somebody with no authority to fix it, which is the class wk-775a8ed90f names. AND IT IS RED TODAY FOR THE HONEST REASON TOO, that nothing is built yet, so no gate can tell the two reds apart. THE EXTENT IS ONE CRITERION AND I READ ALL SIX COMMANDS TO SAY SO: criterion 1 is the only one carrying an rg guard, criteria 2, 3 and 4 run node util/checks/drive-panel.mjs, criterion 5 runs the battery, and criterion 6 is prose.

**satisfies:** TAKE THE GUARD FROM THE LANGUAGE THE CHECK IS WRITTEN IN, OR DROP IT AND LET THE CHECK REFUSE FOR ITSELF. The guard exists because go test with a run pattern nothing matches exits zero. node util/checks/drive-panel.mjs does not have that hole. A missing file makes node exit non-zero, and util/checks/battery.sh already counts a missing check as a failure rather than a skip, which is written into the file in as many words. So the shortest answer is to drop the rg half and require the check to refuse when it finds nothing to drive, the way the other node checks in util/checks do. If a guard is still wanted, write it against what a TypeScript file actually holds, and say so in the sentence. THE CHECK, RED TODAY: write the one declaration the work will write into a scratch file, run the guard against it, and paste what it answered. Against function TestEveryWayAMintFailsSaysSo the guard as written exits 1, which is a red for the fix rather than for the defect, and that is the whole finding.

## finding 2 · round 1 · done when, criteria 1, 2, 3 and 4, whose commands are all "node util/checks/drive-panel.mjs ." and identical between 2, 3 and 4 · by reviewer12

**wrong:** FOUR CRITERIA AND ONE EXIT CODE, SO THREE SENTENCES ARE AGREED AND NEVER DECIDED. I pulled the commands out of the done-when list and compared them rather than reading down the page. Criteria 2, 3 and 4 carry byte-identical commands, node util/checks/drive-panel.mjs followed by a dot. Criterion 1 carries the same command behind the rg guard. One file's exit code decides four different claims. The four claims really are different, which is why they are four lines. Criterion 1 is that every way the mint can fail says so, and it names five ways. Criterion 2 is that the panel is driven through a DOM the way the editor page is. Criterion 3 is that what the line sends reaches the real binary and the token comes back from the engine. Criterion 4 is the slash rule, the words before the slash becoming the title and everything after becoming the detail. A drive-panel.mjs that loads the page, types the line and reads back what was sent makes criterion 2 true and exits zero, and criteria 1, 3 and 4 are then green with none of their properties asserted. THE COST LANDS ON THE SUBTLER THREE. Criterion 3's real-binary requirement is the thing this token's own detail says is missing, and criterion 1's five silent paths are the whole of the fix. Nothing on the page marks the duplicate, because it is visible only by putting the commands side by side. AND THE FILE DOES NOT EXIST YET, so no reader can appeal to what it happens to assert. I checked: there is no util/checks/drive-panel.mjs in the tree.

**satisfies:** ONE COMMAND DECIDES ONE SENTENCE, SO GIVE EACH CRITERION ITS OWN EXIT CODE. Either split drive-panel.mjs into named cases the command selects, one per criterion, with each sentence naming the case that decides it, or write them as separate files, the way engine-args.mjs and engine-args-lifecycle.mjs already sit side by side in util/checks and in the battery. If one run really does cover all four properties, then they are one criterion and the other three sentences belong inside it, and the token should say that rather than listing four. AND TAKE THE RED PER CASE RATHER THAN PER FILE, which criterion 6 already asks for on the failure paths: drive each case with its own property absent and record which case named it. THE CHECK, AND IT IS ONE LINE OVER THE DRAFT ITSELF: pull the commands out of the done-when list and compare them for duplicates before submitting. Any repeat is either two criteria that should be one, or a criterion whose command has not been written yet. Do the same for any run pattern inside a command, because two different commands driving the same pattern is this defect wearing different clothes.

## lesson 1 · round 1 · by reviewer12

**the class:** TWO CRITERIA, ONE COMMAND, SO THE SECOND SENTENCE IS AGREED AND NEVER DECIDED, WHICH IS wk-788b64df90 NAMED AGAIN AND WITH A LARGER INSTANCE. That class was measured on a pair. This is four: on wk-551d5a2008 criteria 2, 3 and 4 carry byte-identical commands and criterion 1 carries the same command behind a guard, so one exit code stands for four claims. WHAT THE SIZE ADDS IS THAT THE DUPLICATE STOPS LOOKING LIKE A SLIP. One repeated command reads as a copy nobody noticed. Four reads as a deliberate plan to write one driver, which is a reasonable plan and is not the same thing as a gate. The driver may well assert all four properties, and nothing on the token requires it to, so the first version that makes the easiest claim true turns the other three green. AND THE FILE DOES NOT EXIST YET, which removes the last defence: a reader cannot look at what the check happens to assert, because there is nothing to look at. MEASURED ON wk-551d5a2008: six criteria, three identical command strings, and the property the detail says is missing today, that the line reaches the real binary, is one of the three that no exit code of its own decides.

**instead:** Before submitting a draft, pull the commands out of the done-when list and compare them for duplicates. Compare the run patterns inside them too, because two different commands driving the same pattern is the same defect. Any repeat is either two criteria that should be one, or a criterion whose command has not been written yet. Where the command names a file that does not exist, ask what the first working version of it would assert, and which sentences would go green without being asserted at all.

**before it:** Write the command as you write each criterion, rather than in a pass at the end. When you are about to reuse the command above, stop and ask which of the two sentences it decides. One command decides one sentence, and a second sentence costs one more named case.

**minted as:** wk-788b64df90

