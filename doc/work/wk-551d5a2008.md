---
id: wk-551d5a2008
seq: 1000063
type: work
title: the sidebar mints nothing
status: spec_open
assignee: main
scope: single-step
traced: true
minted_by: person
---

## detail

THE LINE EDIT IN THE SIDEBAR DOES NOT MINT. THE OWNER'S WORDS: the slash detail check, I just tried it, and it does not seem to work, I do not see the token, it does not seem like if I entered work in the sidebar it enters the system. MEASURED BY THE PERSON WHO USES IT, while trying wk-bae93614a3, which is a token assigned to them asking for exactly this control to be checked. WHY IT MATTERS MORE THAN ITS SIZE. The sidebar line is how a person mints work, and it is the one control a person uses most. A control that silently does nothing is the defect the controls-reach-the-engine token exists for, arriving at the one place nothing drives: engine-args.mjs drives mintArgs against the real binary and drive-editor.mjs drives the editor page, and the sidebar panel's own line edit is driven by neither. WHERE TO LOOK, and it is a list of places rather than a diagnosis, because the diagnosis is the work. mintArgs in src/extension/engineargs.ts builds the arguments and returns undefined when it cannot read the line, and a caller that drops that is a mint that vanishes. mintWork in src/extension/extension.ts calls it and returns early on undefined without telling anybody. panel.ts draws the line and sends the message. And the kind picker sends SS and a middle dot and T, which mintArgs reads with readKind. FIND OUT WHICH AND SAY SO, because a token that reads a control does not exist and a token whose message was dropped are different faults with different fixes. AND WHATEVER IT IS, IT ENDS WITH A CHECK THAT DRIVES THE PANEL THE WAY drive-editor.mjs DRIVES THE EDITOR, because this control has no driver at all and that is why nobody knew. A CONTROL THAT FAILS SILENTLY IS THE WORST KIND: the person typed the work, watched it vanish, and had no way to tell whether the engine refused it or nothing was sent.

READ RATHER THAN GUESSED, AND THE ANSWER IS THAT EVERY FAILURE ON THAT PATH IS
SILENT. The chain is panel.ts sending a run message with the text and the kind,
extension.ts executing the command with them, mintWork building the arguments,
and askEngine spawning the engine. THREE PLACES RETURN WITHOUT TELLING ANYBODY.
mintArgs answers undefined when the line is empty and mintWork returns on it.
askEngine resolves undefined when there is no work root or no binary, and
mintWork reads out?.error off undefined, which is undefined, so nothing is
shown. And a spawn that errors resolves undefined the same way. Only one path
speaks: an engine that answers with an error field.

SO THE DIAGNOSIS IS NOT WHICH ONE FIRED. It is that a person cannot tell, and
neither can I from here. Whichever of the three it was, the person typed the
work, watched it vanish, and had no way to learn whether the engine refused it
or nothing was sent. THE FIX IS THAT NONE OF THEM IS SILENT, and then the cause
names itself the next time.

AND THE CONTROL IS DRIVEN, because that is why nobody knew. engine-args.mjs
drives mintArgs against the real binary and drive-editor.mjs drives the editor
page. The sidebar panel is driven by neither, so the one control a person uses
most is the one control nothing presses.

## done when

- Every way the mint can fail says so where the person is looking, rather than returning without a word: an empty line, no work root, no binary, a spawn that errors, and an engine that refuses. The check drives each one and requires a message naming which
  `rg -q func.TestEveryWayAMintFailsSaysSo src/extension && node util/checks/drive-panel.mjs .`
- The panel is driven the way the editor page is: the page is loaded into a DOM, the line is typed into, the key is pressed, and what it sent is read back, so a control that sends nothing is caught by the check rather than by a person
  `node util/checks/drive-panel.mjs .`
- What the line sends reaches the engine as a call the engine answers, driven through the real binary rather than against a description of it, and the token it mints comes back from the engine rather than being assumed
  `node util/checks/drive-panel.mjs .`
- The slash rule wk-bae93614a3 asks about is driven rather than read: a line with a slash mints a token whose title is the words before it and whose detail is everything after, and a line with no slash mints all title and no detail
  `node util/checks/drive-panel.mjs .`
- The whole battery is green afterwards. THIS IS A STANDING RULE OVER THE PROJECT rather than an assertion about this change
  `sh util/checks/battery.sh`
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. The red is taken once per way it can fail rather than once for the check, and the evidence names the test and what it said rather than a line number

