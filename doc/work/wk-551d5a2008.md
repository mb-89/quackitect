---
id: wk-551d5a2008
seq: 1000063
type: work
title: the sidebar mints nothing
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 2
evidence:
  - outcome
minted_by: person
submitted_by: main
reviewed_by: rev-31
---

## detail

The line edit in the sidebar does not mint, and the owner saw the work vanish with no message. Every failure on the chain through panel.ts, mintWork and askEngine in src/extension/extension.ts is silent. None may stay silent, and util/checks/drive-panel.mjs drives the panel line the way drive-editor.mjs drives the editor. Related: wk-bae93614a3.

## done when

- every undefined answer in mintWork and askEngine says something first, the set found by reading, not listed
  `node util/checks/drive-panel.mjs --says-why .`
- the panel line is driven: typed into, Enter pressed, what it sent read back
  `node util/checks/drive-panel.mjs --sends .`
- what the line sends reaches the real engine, judged by the exit code Unread in src/engine/verbs.go
  `node util/checks/drive-panel.mjs --reaches .`
- the slash rule is driven: words before the slash are the title, the rest the detail
  `node util/checks/drive-panel.mjs --slash .`
- the flagless call drives every case, refuses with none, reads the root from argv[2], and battery.sh names it
  `node util/checks/drive-panel.mjs . && node util/checks/checks-live-in-the-method.mjs .`
- the whole battery is green afterwards
  `sh util/checks/battery.sh`
- every case above was watched failing on its own assertion, once per case

## evidence: outcome

mintWork and askEngine speak on every undefined answer, panel.ts sends on Enter, mintArgs cuts at the slash, and util/checks/drive-panel.mjs drives all four cases from battery.sh.
