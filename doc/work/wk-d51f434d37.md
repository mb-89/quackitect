---
id: wk-d51f434d37
seq: "-38"
type: work
title: the reviewer stays
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 2
evidence:
  - outcome
minted_by: cowork
submitted_by: main
---

## detail

A token that comes back goes to the reviewer who judged it before. Absent is answered by StillPulling with the staleness window, and four eyes still wins. Related: wk-925ca24643.

## done when

- a returning submission goes to the reviewer who judged it, asked of the queue
  `rg -q func.TestAReturningSubmissionGoesToItsReviewer src/engine && go test -C src/engine -count=1 -run TestAReturningSubmissionGoesToItsReviewer$ .`
- a fresh submission takes any reviewer
  `rg -q func.TestAFreshSubmissionTakesAnyReviewer src/engine && go test -C src/engine -count=1 -run TestAFreshSubmissionTakesAnyReviewer$ .`
- a gone reviewer frees the token once the queue moves past the window, by StillPulling
  `rg -q func.TestAGoneReviewerFreesTheToken src/engine && go test -C src/engine -count=1 -run TestAGoneReviewerFreesTheToken$ .`
- the memory never overrides four eyes
  `rg -q func.TestTheMemoryDoesNotOverrideFourEyes src/engine && go test -C src/engine -count=1 -run TestTheMemoryDoesNotOverrideFourEyes$ .`
- a stopped reviewer frees the token
  `rg -q func.TestAStoppedReviewerFreesTheToken src/engine && go test -C src/engine -count=1 -run TestAStoppedReviewerFreesTheToken$ .`
- every test above was watched failing on its own assertion with the memory absent

## evidence: outcome

KeptForItsReviewer in src/engine/pull.go ends in StillPulling and the pull obeys it. The five tests live in src/engine/stays_test.go.
