---
id: wk-04c42c534b
seq: "34"
type: work
title: two panes seam
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-66a28ca311
minted_by: person
evidence:
  - outcome
---

## detail

The second pane shows at boot when it should not, its button does nothing, and nothing marks where the panes meet. The markup ships the pane and the seam with hidden set, so find what displays them rather than adding a second thing that hides them.

## evidence: outcome

Nothing removed the attribute: .pane-wrap and .seam set display, which beats the browser's own [hidden] rule. The shared stylesheet now says what hidden means with an important rule, which also fixes the clipped group and rename buttons in the toolbar. The seam is four pixels of the panel border colour, and drive-editor.mjs requires pane and seam to start hidden, open together and close. one-look.mjs checks the rule over the stylesheet text, and dragging the seam is left to wk-cff23cb661.
