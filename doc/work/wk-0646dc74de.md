---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: open notes vanish silently
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-berio-three
claimed_at: "2026-09-05T19:13:15Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - a6f0d82af5da2e4c053d39e21251e7104e3e2bf8
---

## detail

Measured on this box at 19:10. git status over doc/work showed 112 note files deleted from the working tree and not committed. 91 of them had a row in doc/work/archive.jsonl, so those deletions are the close doing its job and were committed. The other 21 had no archive row and no archive tag. Twenty of them still existed in git HEAD, every one status open with no disposition. So twenty pieces of open work had been taken off the disk with nothing saying who did it or why. Nothing refuses this and nothing reports it. A close deletes a note and writes an archive row, and those two are meant to move together. A deletion with no row is either a close whose archive write failed, which NotArchived is supposed to surface, or a hand or a sweep removing live work. Either way the queue silently shrinks. I restored all twenty from HEAD. Had anyone committed those deletions first, twenty open tokens would have left the backlog and nobody would have known.

## done when

- a note deleted from doc/work with no archive row is reported: a check under util/checks answers the ids and fails when there is one
- the check is named in util/checks/battery.sh and answers nothing on a clean tree

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One new check of 147 lines, and one name added to the list the battery loops over. | git show 0925cc75 --stat |
| [x] | every done-when line is decidable, and names the command where one decides it | The first is decided by node util/checks/deleted-notes-have-a-row.mjs, which prints ids and exits 1. The second by sh util/checks/battery.sh, whose lane has to read ok. | RUNME.sh test --propose deleted-notes-have-a-row |
| [x] | the basics it stands on exist, or are minted first | git, node and doc/work/archive.jsonl are here, and 121 rows were read off the list. The engine finds any mjs under util/checks itself. | util/checks/battery.sh |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove the red first, rule 14 the re-runs at ec1b82ff. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | A worktree at ec1b82ff lost wk-01bf8b5137.md with no row. The check named it, exit 1. | /tmp/vanish |
| [x] | the same test was seen green after the change, and named | Restored, it said 0 note(s) off the disk, 0 failed. The battery lane read ok. | /tmp/battery-ber3.log |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began a6f0d82a. The new check and one name on the list. 0925cc75 also took another agent's battery.sh edit. | 0925cc75 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None mine. archive-rows-travel is open as wk-17f90ad848. | battery |

