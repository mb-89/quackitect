---
id: wk-41069b8234
seq: 1000128
type: work
title: the engine judges red
status: imp_done
assignee: cowork
scope: single-step
traced: true
disposition: done
rounds: 2
minted_by: cowork
submitted_by: cowork
evidence:
  - outcome
---

## detail

CriteriaThatAlreadyPass skips a run that errors, so a criterion red by crash walks through the spec gate like one red by assertion. Tell three causes apart where the green check runs. Red by crash is exactly rg exiting two, go saying build failed, and go panicking, the last two read from output. A missing file is not a crash, so node on an unwritten script and rg over a missing path go through. Red by dead pattern is a must-be-gone search whose finding half matches nothing. Only the canonical spelling is judged: rg -q, a double-quoted pattern, a path, then && exit 1 ||. A criterion carrying its watched red is skipped by all three judgements.

## done when

- A draft whose criterion crashes is refused at the gate naming the criterion and the cause, with each of the three causes driven in its own fixture through the gate
  `rg -q func.TestACrashRedRefusesTheDraft src/engine && go test -C src/engine -count=1 -run TestACrashRedRefusesTheDraft$ .`
- A draft carrying a canonical must-be-gone search whose finding half matches nothing is refused naming the pattern, while a reordered spelling and a presence guard go through unjudged
  `rg -q func.TestADeadPatternRefusesTheDraft src/engine && go test -C src/engine -count=1 -run TestADeadPatternRefusesTheDraft$ .`
- A draft red by a failing assertion, a clean not-found, node on an unwritten check file, or a node check that throws reaches review, and a criterion carrying its watched red is skipped
  `rg -q func.TestAnAssertionRedGoesThrough src/engine && go test -C src/engine -count=1 -run TestAnAssertionRedGoesThrough$ .`
- Every test above was watched failing with the gate unchanged, and the evidence names each test and what it said

## evidence: outcome

crashCause at src/engine/spec.go:132 reads exit two and the output for build failed and panic, and skips exit two on No such file or os error. mustBeGone at spec.go:152 recognises exactly one spelling. TestACrashRedRefusesTheDraft and TestADeadPatternRefusesTheDraft were watched red with their branch cut, and TestAnAssertionRedGoesThrough against a classifier mutated to refuse any red. bash util/checks/battery.sh answers all ok.
