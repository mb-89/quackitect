---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: branch tip fails building
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-borromini
claimed_at: "2026-09-06T09:35:25Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - cae9568ece6489f8a833678dc44c43edebbde9d2
---

## detail

The branch tip does not build the src/engine test binary, so no red under that package can be watched on a clean worktree of it either. src/engine/sessiontakesmainback_test.go is in package main and calls OpenLog and Yes unqualified. Both moved to src/engine/internal/sessionlog, where Yes now lives at line 189, and nothing in package main defines either. Measured on a detached worktree of the tip at 2b96d1e6, with CGO_ENABLED=1 GOFLAGS=-tags=sqlite_fts5 go test -C src/engine -run TestNothingAtAll ./... It answered two errors, both in that file, naming OpenLog and Yes undefined. Setting the file aside made the same command answer exit zero, which is how the red for wk-910b6e4000 was watched at all. The check named the-branch-head-builds exists and did not stop this landing.

## proposed action

Bring sessiontakesmainback_test.go onto the sessionlog package, importing it and calling its OpenLog and Yes, so the tip builds its tests again. Then read why the-branch-head-builds let it land, and say on the token whether that check reads this package at all.

## done when

- the tip builds its tests, decided by: on a detached worktree of the branch tip, CGO_ENABLED=1 GOFLAGS=-tags=sqlite_fts5 go test -C src/engine -run TestNothingAtAll ./... answering no build failure
- TestASessionTakesMainBackAndDrawsOneRow runs rather than being deleted, decided by: go test -C src/engine -run TestASessionTakesMainBackAndDrawsOneRow -v naming it and not answering no tests to run
- the token says whether the-branch-head-builds reads the engine test binary, decided by: read that check beside the token

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

