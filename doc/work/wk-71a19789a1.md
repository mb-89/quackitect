---
id: wk-71a19789a1
seq: 1000118
type: work
title: state writes are torn
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: cowork
---

## detail

Owner ruling: one door to every interface, and here that means one door to the disc. Only the token store writes a temp file and renames it, at store.go:394 and :408. Every other piece of state is a bare os.WriteFile. Sites: watch.go:76, owed.go:205, owed.go:304 and :323, nudge.go:153, vehicle.go:87, tools.go:124, and stop.go:102. Build one writer that takes a path and bytes, writes a temp file beside the target, syncs and renames. Build one reader that degrades a partial or unparseable file to the empty value and says so in the log. Add a check that enumerates callers of os.WriteFile and os.Create under src/engine and refuses any that writes under the private folder without the door. Put a bare os.WriteFile back and watch the check name it.
