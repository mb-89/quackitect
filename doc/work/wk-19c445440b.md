---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: restart strands the proxy
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b7938dbf5f58fec141f00320dc9dd2c1676499a2
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - cfaa061e58f375bce2f04656164b8ce6213c91c5
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: The diagnosis names a dead HTTPS_PROXY and the live port, and says nothing when the variable is right.
---

## detail

A FINDING AFTER A CONTAINER RESTART.

Outbound HTTPS on a cloud box goes through an agent proxy on a local port, named in HTTPS_PROXY. A restart moves the port and leaves the variable behind, so every git call reaches a port nothing listens on.

Measured on this box. HTTPS_PROXY said http://127.0.0.1:33243 and nothing answered there. /root/.ccr/README.md named 37347, and its status endpoint answered at once. git ls-remote through 33243 said: unable to access, failed to connect to 127.0.0.1 port 33243. The same call through 37347 answered the branch head.

Nothing said so. se claim reported the failed push as its own trouble, five agents were spawned into it, and RUNME.sh --diagnose says it reports the network.

.se/scratchpad/proxy.sh reads the live port out of the README and confirms it against the status endpoint. That is the whole mechanism, and it belongs in the diagnosis rather than a scratchpad.

## done when

- RUNME.sh --diagnose names a dead proxy port and the live one, decided by running it with HTTPS_PROXY set to a port nothing listens on
- the diagnosis is silent about the proxy when HTTPS_PROXY names the live port, decided by the same run without the override
- the engine's own git calls reach the live proxy: moved to wk-07c2d80710, a change in the engine and not in the diagnosis

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A box whose pushes all fail says why in one line, instead of every caller blaming itself. | 1 line |
| [x] | what breaks if it is never done, and not only that it stays undone | The engine reports a dead port as its own trouble, and I spawned five agents into it before I looked. | 5 agents |
| [x] | the ask is small enough to review whole, or it is split first | Split. The cure in the engine is wk-07c2d80710, and this is the measurement. | 1 file |
| [x] | every done-when line is decidable, and names the command where one decides it | Two runs of the diagnosis, one with a dead port and one with the live one. | 2 of 2 |
| [x] | the basics it stands on exist, or are minted first | The README names the port and the status endpoint confirms it. | 2 of 2 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | The third done-when moved off with its criterion, to wk-07c2d80710. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | With HTTPS_PROXY on a dead port, the diagnosis said nothing about it. | 0 lines |
| [x] | the same test was seen green after the change, and named | The same run names the dead port, the live one, and calls it a DEFECT. A run with the live port says nothing. | 3 and 0 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | One helper and two hunks in diagnose.mjs. | 5aa1f35a |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | A token of its own. Saying the port is dead does not cure the engine's own git calls. | wk-07c2d80710 |

