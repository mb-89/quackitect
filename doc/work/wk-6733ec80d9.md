---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: editor classes want rules
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-alder
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T17:31:43Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 53989660155c7299d75dfb2d621d7505ac8f6ca9
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 5835f6743ccc15fe0ff10cd736656dc69693960d
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

From wk-894fb72f52, which asked the same question of the panel and answered it.

That token said four classes carried no rule. On this tree the panel has none: every class it emits and every class its source writes is named by its stylesheet, which the new third half of util/checks/drawn-classes-have-rules.mjs now holds.

The editor page is the other half of the same defect, and it is bigger. Measured here, twenty-three classes it emits have no rule at all: bs-add-cond, bs-add-group, bs-code-toggle, bs-collapse-all, bs-cond-tpl, bs-conds, bs-dir, bs-drop, bs-drop-cond, bs-expand-all, bs-group-tpl, bs-groups, bs-hide-all, bs-levels, bs-make-bucket, bs-prop-name, bs-rename, bs-row, bs-tick, bs-tool, fold, locked and name. The source writes two more that the page here never draws: bs-group-raw and tally.

The standing check asks the editor page only about the classes a script toggles, and about inline elements given a width, so all twenty-three are green today.

## proposed action

Read the twenty-three back off the page first, because a hand list is complete on the day it is written. Give each one a rule, or take the class off the markup where nothing was ever meant to draw it. Then widen the panel half of the check to ask the editor page the same question.

## done when

- every class the editor page emits has a rule, decided by: the third half of util/checks/drawn-classes-have-rules.mjs asked over the editor page as well as the panel
- the widened half is seen red by deleting one existing editor rule, and green when it is put back
- node util/checks/drive-editor.mjs answers ok, so nothing the page does was broken by the styling

## evidence: note

The hand list was read back off the page first. It was already stale. Nineteen classes on this tree, not twenty-three. Seven had been ruled since it was written. Those are bs-conds, bs-groups, bs-levels, bs-prop-name, fold, locked and name. One had joined it, pinned.

The nineteen: bs-add-cond, bs-add-group, bs-code-toggle, bs-collapse-all, bs-cond-tpl, bs-dir, bs-drop, bs-drop-cond, bs-expand-all, bs-group-raw, bs-group-tpl, bs-hide-all, bs-make-bucket, bs-rename, bs-row, bs-tick, bs-tool, pinned, tally.

Eighteen were given a rule, in six groups. Each rule is a decision the sheet was already making somewhere and had never said here. A tool on the bar holds its size. A button that is only a mark is as narrow as the mark.

The nineteenth came off the markup. tally was written by tallyHtml, which the index says nothing calls. It is the one class no rule could be written for. The sheet cannot decide how a mark looks that never reaches a page. The dead writer went.

The widening is a pair, not a second copy. The panel half is now one function, and the two pages are rows of a table it is driven through.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one check half widened into a pair, one stylesheet given eighteen rules. The diff is editor.ts and drawn-classes-have-rules.mjs |  |
| [x] | every done-when line is decidable, and names the command where one decides it | all three by node util/checks/drawn-classes-have-rules.mjs and node util/checks/drive-editor.mjs from the root, the second with a rule deleted first |  |
| [x] | the basics it stands on exist, or are minted first | editorHtml and the panes the engine answers with are what the check already builds. Nothing needed minting |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | item 13. The pair is tabled, one function driven through each row. Item 1 is why the list was re-measured |  |
| [x] | one test was written first and seen red for the reason expected | no rule yet: 85 classes, nineteen named. With .bs-gap deleted: bs-gap is drawn and never named. Only the new half went red |  |
| [x] | the same test was seen green after the change, and named | drawn-classes-have-rules ok, editor half 84. drive-editor ok, 0 failed. render-check, one-look, drive-panel, panel-icons ok. tsc exit 0. The battery's one FAIL is another hand's untracked test |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | editor.ts and drawn-classes-have-rules.mjs, nothing else |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | dead tallyHtml deleted, writer of the unrulable class. Left alone: a class here is both look and handle, which drive-editor and render-check assert |  |

