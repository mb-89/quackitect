---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: editor classes want rules
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T17:31:43Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 53989660155c7299d75dfb2d621d7505ac8f6ca9
---

## detail

From wk-894fb72f52, which asked the same question of the panel and answered it.

That token said four classes carried no rule. On this tree the panel has none: every class it emits and every class its source writes is named by its stylesheet, which the new third half of util/checks/drawn-classes-have-rules.mjs now holds.

The editor page is the other half of the same defect, and it is bigger. Measured here, twenty-three classes it emits have no rule at all: bs-add-cond, bs-add-group, bs-code-toggle, bs-collapse-all, bs-cond-tpl, bs-conds, bs-dir, bs-drop, bs-drop-cond, bs-expand-all, bs-group-tpl, bs-groups, bs-hide-all, bs-levels, bs-make-bucket, bs-prop-name, bs-rename, bs-row, bs-tick, bs-tool, fold, locked and name. The source writes two more that the page here never draws: bs-group-raw and tally.

The standing check asks the editor page only about the classes a script toggles, and about inline elements given a width, so all twenty-three are green today.

## proposed action

Read the twenty-three back off the page first, because a hand list is complete on the day it is written. Give each one a rule, or take the class off the markup where nothing was ever meant to draw it. Then widen the panel half of the check to ask the editor page the same question.

## done when

- every class the editor page emits has a rule, decided by: the third half of util/checks/drawn-classes-have-rules.mjs asked over the editor page as well as the panel
- the widened half is seen red by deleting one existing editor rule, and green when it is put back
- node util/checks/drive-editor.mjs answers ok, so nothing the page does was broken by the styling

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

