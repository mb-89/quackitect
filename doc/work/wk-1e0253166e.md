---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: two files one fallback
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-relay-trial
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 7f5f912e6b9520646a9a4f90d53b539179a142cd
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 470d4fd5c2c9fcf1407895f5b3c3dd31654e1fae
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Found reviewing wk-8c3c71d77d, snapshots do not travel.

That token added src/engine/snapshotstravel_test.go, whose first test, TestTheDeltaFallsBackWhenTheSnapshotIsNotHere, covers theSnapshotToDiff. Its note says the function "has no test at all". On origin/v4 it has two, in src/engine/deltamissesbadobject_test.go: TestADeltaAgainstAHashThisBoxDoesNotHold and TestTheSinceFallsBackToASnapshotThisBoxHolds. The clone the work was done on is far behind origin, so the claim was true of that tree and not of the branch.

What lands is one function's fallback held in two files, with two invented hashes for the same idea: nowhereHash in the older file, elsewhere in the newer. Verified on a worktree of origin/v4 with this token's three files applied: all four tests pass, and reverting theSnapshotToDiff to the unchecked read reddens one test in each file.

Only one assertion is new: began entirely absent, so the floor answers HEAD and the delta is not empty. Move that into deltamissesbadobject_test.go beside the other two, drop the elsewhere constant for nowhereHash, and leave snapshotstravel_test.go holding the reviewer's notice, which is what its name is about.

## done when

- theSnapshotToDiff's fallback is held in one file, with the HEAD floor case kept: go test ./src/engine -run 'TestTheSince|TestADelta|TestTheDeltaFallsBack'
- no second invented hash constant names the same idea: read the consts in src/engine/deltamissesbadobject_test.go and src/engine/snapshotstravel_test.go

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One test added to deltamissesbadobject_test.go and one branch in its fixture helper. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Two lines. One runs three tests by name, the other reads the consts in the two files. |  |
| [x] | the basics it stands on exist, or are minted first | theSnapshotToDiff, aTreeWithTests and openTheIndex all exist. Nothing had to be minted. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. The check was written first and watched red before it was kept. |  |
| [x] | one test was written first and seen red for the reason expected | Watched red with the floor emptied: the delta was read against the empty string and the floor is HEAD. |  |
| [x] | the same test was seen green after the change, and named | TestTheSinceFallsBackToHeadWithNoTakeUp, green, with TestADeltaAgainstAHashThisBoxDoesNotHold and TestTheSinceFallsBackToASnapshotThisBoxHolds beside it. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began is 7f5f912e and is no object here. The change is one file, on origin as eaf0a278. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The second file the ask names is not on origin, so there is no second const to drop. Only the missing case landed. |  |

