---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: two files one fallback
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-relay-trial
claimed_at: "2026-09-05T19:20:22Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 7f5f912e6b9520646a9a4f90d53b539179a142cd
---

## detail

Found reviewing wk-8c3c71d77d, snapshots do not travel.

That token added src/engine/snapshotstravel_test.go, whose first test, TestTheDeltaFallsBackWhenTheSnapshotIsNotHere, covers theSnapshotToDiff. Its note says the function "has no test at all". On origin/v4 it has two, in src/engine/deltamissesbadobject_test.go: TestADeltaAgainstAHashThisBoxDoesNotHold and TestTheSinceFallsBackToASnapshotThisBoxHolds. The clone the work was done on is far behind origin, so the claim was true of that tree and not of the branch.

What lands is one function's fallback held in two files, with two invented hashes for the same idea: nowhereHash in the older file, elsewhere in the newer. Verified on a worktree of origin/v4 with this token's three files applied: all four tests pass, and reverting theSnapshotToDiff to the unchecked read reddens one test in each file.

Only one assertion is new: began entirely absent, so the floor answers HEAD and the delta is not empty. Move that into deltamissesbadobject_test.go beside the other two, drop the elsewhere constant for nowhereHash, and leave snapshotstravel_test.go holding the reviewer's notice, which is what its name is about.

## done when

- theSnapshotToDiff's fallback is held in one file, with the HEAD floor case kept: go test ./src/engine -run 'TestTheSince|TestADelta|TestTheDeltaFallsBack'
- no second invented hash constant names the same idea: read the consts in src/engine/deltamissesbadobject_test.go and src/engine/snapshotstravel_test.go

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

