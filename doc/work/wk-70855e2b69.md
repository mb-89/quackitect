---
id: wk-70855e2b69
seq: "40"
type: work
title: three panes not two
status: closed
assignee: main
scope: single-step
traced: true
disposition: became
parent: wk-66a28ca311
successors:
  - wk-010a2f3684
evidence:
  - outcome
minted_by: person
---

## detail

The generic editor has the number two written into it: a left, a right, a button that shows the second, and one seam. The owner wants three, so the editor stops knowing how many. The view file declares the panes and the page draws one per declaration with a seam between each pair. The control shows and hides panes by name. Property columns and nested rows are built here in the generic editor, and the work editor gets them by being two of these. Check by driving the page: three declared panes draw three, the control shows and hides each, and a seam sits between each pair. Related: wk-66a28ca311, wk-010a2f3684.

## evidence: outcome

Closed as became wk-010a2f3684, which carries the pane count, the per-pair seams and the show-and-hide by name.
