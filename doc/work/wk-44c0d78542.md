---
id: wk-44c0d78542
seq: 1000212
type: work
title: quick fixes, recorded
status: backlogged
assignee: cowork
scope: single-step
traced: true
minted_by: cowork
---

## detail

Record of five direct fixes the person authorized outside the cage, all shipped with the tree's checks green. The pinned groups box in .top now keeps at most half the pane and scrolls past that. A group-heading fold now redraws the pager and jumps the window to the opened group's first row. extension.ts sends counts with every body and editor.ts redraws the pills and their pop lists from them. A sift box in the bar speaks the KQL syntax ported from src/viewer/filter.go and writes nothing to the view file. By the person's ruling the groups all stay however empty they sift. In the engine hook describe() now squashes a multi-line command to one line under the 200-char cap, pinned by TestAMultiLineCommandIsRecordedWhole. VS Code needs a window reload and the cage should rebuild .bin/se for the hook change to land.
