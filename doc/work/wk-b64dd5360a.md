---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: dead yaml version twins
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-aspen
claimed_at: "2026-09-05T21:33:07Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 8f3d3c26c967f9ef53a2f64d7d967fc808f185e3
---

## detail

A finding on wk-40abb881a7, engine splits at seams.

src/engine/yaml.go lines 32 to 227 define ParseYAML, ymap, ylist, ystr and ystrs in package main, and src/engine/version.go line 4 defines var Build. Every caller in the tree reads quackitect/engine/internal/yaml and internal/version instead: se find over src/engine answers no call to any of the five and no bare read of Build. The two files are dead twins of the packages the token moved them into.

They came back in commit cf8e1d4e, Put back yaml.go and version.go, deleted by a shared index, which landed at 11:11 while the move sat uncommitted in the same tree and its internal packages were untracked. HEAD on its own needed them; the tree with the move does not.

The damage is two definitions of one thing, which is the namespace problem the token exists to end, and a stamper still written -X main.Build would stamp a variable nothing reads and the engine would report unstamped.

The order matters: the internal packages and the rewired callers land first, and then these two files go. Deleting them before that breaks HEAD's own build, which is how they came to be put back.

## proposed action

Once src/engine/internal/yaml and src/engine/internal/version are committed with their callers, git rm src/engine/yaml.go and src/engine/version.go. Then add a check, no-flat-twins, that fails when a file under src/engine/internal/*/ shares its basename with a file in src/engine/, so the class is caught the next time a shared index or a put-back reintroduces one.

## done when

- src/engine/yaml.go and src/engine/version.go are gone: ls src/engine/yaml.go src/engine/version.go answers no such file
- the engine builds and vets without them: go build -C src/engine ./... and go vet -C src/engine ./...
- a check refuses a file under src/engine/internal/*/ whose basename is also a file in src/engine/: run it against a copy with yaml.go put back and it fails

## evidence: note

The two flat files were already gone at the tip. d52d8dc2, "the flat yaml and version files go, the internal ones stay", landed them under wk-3a7e490a07 before this token was taken up. ls answers no such file for both, and go build and go vet -C src/engine answer nothing. This token adds the check: util/checks/no-flat-twins.mjs refuses a .go file under src/engine/internal/<pkg>/ whose basename is also a file in src/engine/, naming both. It refuses when it finds no internal package to compare. Against the tip it answers 16 files compared, 0 failed. Against a copy with yaml.go put back from d52d8dc2^ it answers FAIL, naming src/engine/internal/yaml/yaml.go and src/engine/yaml.go, exit 1. battery.sh lists it, and checks-live-in-the-method answers 0 failed on the committed tree. Landed as 75dbc751 on origin/v4.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one check and one line in battery.sh. The deletion was already d52d8dc2 |  |
| [x] | every done-when line is decidable, and names the command where one decides it | ls, go build and go vet, and node over a copy with yaml.go put back |  |
| [x] | the basics it stands on exist, or are minted first | the internal packages and their callers are on the tip, and the battery runs every .mjs it lists |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read. Rule 12 ran the check red before the battery listed it, rule 4 matches on the basename the tree holds |  |
| [x] | one test was written first and seen red for the reason expected | no-flat-twins over a copy with yaml.go put back: FAIL, naming internal/yaml/yaml.go and src/engine/yaml.go, exit 1. Over a copy with no internal: refuses, exit 1 |  |
| [x] | the same test was seen green after the change, and named | no-flat-twins over the tip: 16 compared, 0 failed. checks-live-in-the-method 0 failed, scripts-are-lf 0 failed, go build and go vet silent |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | util/checks/no-flat-twins.mjs and one line of battery.sh, commit 75dbc751 on origin/v4 |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | none found |  |

