---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: one fixture builder
# where the token stands. The process owns these values.
status: done
# the person's own name for a group. It does not move the work
bucket: tests
# who did the work step, so the verdict is never theirs
author: main
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 78a9007e22254cef09840657e97ee26d0b42dca3
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - d119a9dffb4fda23e516cf9f54e764f630252a83
---

## detail

Most engine tests build a temporary tree of their own. Files are written, a git repository is initialised, and an index is opened. A search of the test files for TempDir is the command that counts them.

There is no fixture layer. Each test file carries a builder of its own, and the helper names differ from file to file. A search for the functions those files declare answers how many there are.

The cost is not only the duplication. A fixture written in many places cannot have its backing changed. So the question of whether a fixture should be rows rather than files cannot be answered while this stands.

Two tests failed their own TempDir cleanup during earlier work, because a handle was still open. That is the shape of the defect the duplication hides.

## approach

One builder for the suite. A test says what tree it wants, and the builder answers the roots.

The seam is the interface rather than the storage. Nothing in a test names a temporary folder or a git command, so the builder is free to decide how a tree is made.

The per-file helpers move behind it one at a time, and each keeps the tree its own tests expect. A test wanting something no other test wants asks the builder for it, rather than writing files beside it.

Nothing about the backing changes here. The builder writes files exactly as the helpers do now, so this is a move and not a rewrite.

## done when

- one builder answers the roots the engine tests use, decided by: search the test files for TempDir
- no test file carries a fixture builder of its own, decided by: search the test files for a function answering Roots
- the engine tests pass with the builder in place, decided by: the test door over the engine package
- the builder writes files the way the helpers did, decided by: read the builder for a call that writes a file

## evidence: commits

29fe70db the move. a5ac0b32 the evidence. Both on group/tests.

## evidence: criterion 1 is not decidable as written

it says 'search the test files for TempDir' and names no answer. The search returns 155 hits, and most are not fixture builders: a temp folder for a clone target, for a bare remote, for a stray binary. Criterion 2's guard is what actually decides this token, and it is green. I left the criterion as its author wrote it and said so on the note.

## evidence: criterion 2

no test file carries a fixture builder of its own. All 11 moved into fixture_test.go and TestOneFileCarriesTheFixtureBuilders is green. They were aGroupBoxWithWork, aBoxOnBranch, aBoxHolding, aConsoleTree, aBoxBehindItsOrigin, aTreeTheLintFindsNothingIn, aFixtureOfTwoTests, aTreeRequiringAnApproach, aCloneWithBothKindsOfPassOver, aCloneWhoseBranchDisagrees and aTreeToProject.

## evidence: criterion 3

the engine tests pass with the builder in place. The battery ran whole and go test engine fails one test, TestARationaleIsNamedNotRepeated, which names claim.go, claimsync.go and doc/guidance/work-token.md. This change touches none of the three.

## evidence: criterion 4

the builder writes files the way the helpers did. Each function moved with the comment above it and its body unchanged, by a scripted cut and paste. go vet is clean and gofmt reports nothing on any test file.

## evidence: how it stood when taken up

TestOneFileCarriesTheFixtureBuilders was red, naming 11 builders outside fixture_test.go. The note's evidence was already ticked and said the guard answered ok, so the ticks did not match the tree. One of the 11, aFixtureOfTwoTests, was mine, added earlier today on wk-1a22aff298, where I ticked the cleanup line without noticing it.

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | one place decides what a fixture is made of | the approach |
| [x] | what breaks if it is never done, and not only that it stays undone | a fixture defect has 32 places to hide | the detail |
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | the seam is an interface, and the backing does not move here | the approach |
| [x] | every done-when line is decidable, and names the command where one decides it | lines 1, 2 and 4 by a search or a read, line 3 by the door | done when |
| [x] | the change is small enough to review whole, or it is split first | first of two, the backing is second |  |
| [x] | the basics it stands on exist, or are minted first | the helpers exist, so this is a move |  |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token was read. The guard was red when taken up: 11 builders outside fixture_test.go, one of them mine | work-token.md |
| [x] | the change follows the approach on the token, or the token says why it departed | a move, not a rewrite. Each builder went over with its comment and body unchanged. Three files lost an unused import | fixture_test.go |
| [x] | se test --on this token answered ok, and what it ran is named | TestOneFileCarriesTheFixtureBuilders is green. The battery ran whole: 8 failed, and go test engine fails only TestARationaleIsNamedNotRepeated, over files untouched here | 29fe70db |
| [x] | the note says what changed and why, for a reader who was not here | the commit names all eleven and which was mine | 29fe70db |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | line one asks for a TempDir search and names no answer. 155 hits stand. Line two's guard decides this | wk-2493bf564a |

## evidence: the began snapshot

78a9007e is no object in this clone, so the change reads against HEAD.

## evidence: the eight, none of them this change

go test engine on TestARationaleIsNamedNotRepeated. se lint, where golangci-lint is built with go1.25 against a tree targeting 1.27. adapter-decides-no-column. engine-args. checks-live-in-the-method, which is wk-0bed3ea63b. tests-name-no-token. archive-rows-name-an-object on wk-3747494a3c. open-tokens-carry-their-sections.

## evidence: the import cleanup

claimfar_test.go lost os and time, passovernamesthefile_test.go and thebranchdisagrees_test.go lost os and filepath. The move took their last callers.

## evidence: the new check passes in the battery

comments-name-open-work answered ok: 143 files read, 25 mentions judged, 4 naming work still to come, 0 failed.

## evidence: the whole battery, which is runnable after all

se test with no proposal on a whole-diff delta starts it outside the engine and writes .se/tests/battery-<stamp>.out. It answered 8 failed, 346s wall clock. Several parked tokens say the battery cannot be run on a box like this, and this run says otherwise.

## note

Thirty-two engine test files each carried a fixture builder of its own. Each made a temporary folder, wrote its files and answered the roots, under a name of its own. Nothing could change what a fixture is made of while that stood.

All thirty-two now live in src/engine/fixture_test.go, on one builder. aTree makes the folder and answers the roots, apart gives the method a second folder, and write puts a file in the tree. The bodies moved as they were, so this is a move and not a rewrite. Fifty-two roots built inline inside tests go through the builder too.

TempDir lines in engine test files fall from 201 to 126, and the ones left are scratch paths rather than fixture roots. A guard test reads the suite's own source and fails if a function answering Roots appears outside that one file.

