---
id: wk-f63ec37f11
seq: "-2"
type: work
title: one icon table
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "5"
evidence:
  - outcome
minted_by: person
---

## detail

Every icon in one table, and nothing else carries a glyph. A control, a heading or a button names an icon, and util/icons.json decides what that name looks like. A name nobody declared draws itself as the name, so the face says which entry is missing. The face is an icon and the word moves into the tooltip as the first thing on hover. Owner named the marks. The power ring for start and stop, stacked lines for the log, a house for home, a full-width plus for init. A hand and the robot it stops for the hold, a hammer for work, and gears only for configuration.

## evidence: outcome

util/icons.json holds every mark and panel.ts and editor.ts name them, with the table passed into panelHtml. Three guards stand. se lint names any control whose icon the table lacks. no-loose-glyphs.mjs refuses any character above U+007F in a file that draws unless the table declares it. render-check.mjs refuses any icon name reaching a slot as a word. Each guard refuses an empty table or file list, numeric character references are decoded before the scan, and the CRLF comment-stripping defect was fixed. The battery is all ok with no-loose-glyphs at 24 glyphs declared, 12 files read, 0 failed.
