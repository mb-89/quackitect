---
id: wk-f63ec37f11
seq: "-2"
type: work
title: one icon table
status: submitted
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "2"
minted_by: person
---

## detail

Every icon in one table, and nothing else carries a glyph.

A control, a heading or a button names an icon, and util/icons.json decides
what that name looks like. So the same mark is the same mark in the sidebar and
in the editor, and changing one is one edit in one file.

A NAME THAT RESOLVES TO A GLYPH TODAY CAN RESOLVE TO A FILE TOMORROW, and
nothing that uses the name has to change. That is the point of the name.

A NAME NOBODY DECLARED DRAWS ITSELF. A blank leaves a button a person cannot
see, and the name on the face says which entry is missing from the table.

THE FACE IS AN ICON AND THE WORD MOVES INTO THE TOOLTIP. A tooltip reads
"<the word that was on the button> / <what it already said>", so the word a
person loses from the face is the first thing they find on hover.

The owner named the marks: the power ring for start and stop, stacked lines for
the log, a house for home, a full-width plus for init, a hand and the robot it
stops for the hold, and a hammer for work. Not gears, which belong to
configuration.

## evidence: a fourth check in the same file, fixed for the same reason

The last column takes what is left named the column assignee, so it went red the day the owner ticked a fourth property and assignee stopped being last. It asserts the rule now: whichever column is last carries no width. Proved red by declaring a width for the real last column and removing the guard in head(), then green with the guard restored and the width still declared. My first attempt to prove it failed because the view carried assignee: 71 from a column the owner had dragged and my edit matched 110.

## evidence: and a second defect the finding did not know about

My first fix compared each word against the TABLE'S OWN KEYS. That can never catch a missing entry, because deleting one takes it off both sides of the comparison at once. It now compares against the names THE SOURCE ASKS FOR, read from the icon("...") call sites in editor.ts. That is the real question: did a name the code asked for reach the page as a word.

## evidence: and a third, which is why it said ok for an hour

The pattern that reads those call sites was written through one language into another and carried a literal control character at its front, so it matched nothing and the check reported no failures while a bare filter Filter sat on a button. The check now refuses when it finds no icon() call at all, which is the rule this project already carries: a check that finds nothing to check refuses.

## evidence: checks

go test -count=1 ./... passes in source/engine and source/viewer. se lint answers clean.

## evidence: proved red in both shapes

Deleting filter from util/icons.json, which renders filter Filter on a button, makes the check fail. Deleting split, which renders a bare name in a slot of its own, also makes it fail. Both go green when the entry is restored. The two shapes are the mark-beside-a-word case the finding named and the bare-mark case the earlier proof used.

## evidence: round 2, the whole-content comparison

The finding was right. The check compared a slot's whole trimmed content against the table's names, so four buttons drawing a mark beside a word were invisible to it: filter Filter, sort Sort, columns Properties and plus Group. The finding also caught two miscounted numbers in the same submission, four excluded classes where the code held five, and 67 checks where the run printed 65.

## evidence: the numbers, taken from the runs

render-check.mjs prints 64 assertions and 0 failed. panel-icons.mjs prints 12 and 0 failed. no-loose-glyphs.mjs reports 24 glyphs declared and 0 failed. live-tool-list.py reports 1804 characters of description and 4613 with the schemas.

## evidence: three guards

se lint names any control whose icon the table does not hold. no-loose-glyphs.mjs refuses any declared glyph found as a literal in a file that draws. render-check.mjs refuses any name the source asked for reaching an icon slot as a word.

## evidence: what I cannot verify

THE SIDEBAR ON THE OWNER'S SCREEN. panel-icons.mjs renders panel.ts through the extension's own resolution and asserts every declared name draws as its mark, and it goes red when the resolution is skipped. But a VS Code window holds the built extension until it is reloaded, and only the owner can do that. I am claiming the code resolves them, not that their window shows them.

## evidence: what the token built

util/icons.json holds every icon the system draws, with its glyph, its code point and what it is for. A control, a heading or a button names an icon and never carries one. The engine resolves names in LoadTree, the extension resolves them in its own loadTree beside the declaration, the editor takes the table off the query answer, and source/viewer reads it by walking up from the log it watches. grep -c '&#' editor.ts answers 0.

## finding 1 · round 1 · Every icon in one table, and nothing else carries a glyph. A control, a heading or a button names an icon, and util/icons.json decides what that name looks like. · by reviewer

**wrong:** source/extension/editor.ts still declares glyphs. Line 739, inside the sort-direction click handler, reads: dir.textContent = asc ? '▲' : '▼'; Those are literals, not icon(name), and the table already names both marks (up U+25B2, down U+25BC). The button is drawn correctly at line 247 with icon("up") / icon("down"), so the table decides the mark until a person clicks it, and after one click the mark comes from the source instead. I proved the divergence rather than reading it: in an isolated copy of the tree I set up to U+2191 and down to U+2193 in util/icons.json and re-rendered. The page carried the new arrow four times on the sort buttons, and still carried ▲ once and ▼ three times, one of them the literal at line 739 that the page ships inside its own script block. render-check.mjs answered 0 failed with that divergence on the page. So changing one entry in the table is not one edit: the up and down marks need a second edit in editor.ts. The submission's own evidence cannot see this. grep -c '&#' editor.ts counts HTML entities and these are literal characters, so it answers 0 with the defect present. The render check's icon-slot assertion looks only inside class=fold, pin, pin on and bs-type, and this button is class=bs-dir, so it is passed over along with every other icon slot on the page: bs-tool (filter, sort, columns, query), second (split), bs-drop-cond (remove), bs-prev, bs-next. Its name-shaped test is also /^[a-z][a-z.]{2,}$/, which cannot match the two-letter name up. Extent, searched in one pass over source and util for every character above U+007F: editor.ts line 739 is the only glyph left in the extension, and source/viewer/record.go lines 121 and 123 are the same class in the log window, where func (r Record) Mark() returns the literals ✓ and ✗. util/icons.json's own $comment claims the same mark is the same mark in the sidebar, the editor and the log window, and the log window's marks are not in the table.

**satisfies:** Line 739 sets textContent from the table, the way line 247 already does, so the handler and the first render read the same entry. Then a check that catches the class rather than this line. It goes in the language the defect is in: a check over source/extension/*.ts that reads util/icons.json and fails if any glyph the table declares appears as a literal in the source. I have run that check against the tree as it stands and it is red now, reporting editor.ts:739 declares the glyph ▲ which the table names up, and ▼ which the table names filter and down. editor.ts carries no other character above U+007F, so the same check goes green once line 739 calls icon(). Two things it needs beyond the literal search: it must not fire on a character used as data rather than as a mark, which is the one false positive it found, extension.ts:1056 splitting on ·, so scope it to editor.ts and panel.ts or exclude the names it is not about; and the render check's icon-slot assertion should read the class list from the icon-bearing classes the page actually emits rather than from four of them, so a missing entry for split, remove, previous, next, filter, sort, columns, query, up or down is seen too. For the log window, either add the tick and the cross to the table and have Mark() name them, or say in the token that the viewer is out of scope and correct icons.json's $comment, which currently claims it.

## finding 2 · round 2 · A NAME NOBODY DECLARED DRAWS ITSELF. A blank leaves a button a person cannot see, and the name on the face says which entry is missing from the table. The submission's own claim: the render check now reads every span and button the page emits and compares the content against every name in the table the engine sent. · by reviewer

**wrong:** It compares the whole content, so any slot where a mark sits beside a word is invisible to it. .se/scratchpad/render-check.mjs:101-108 collects slots with /<(?:span|button)[^>]*class="([^"]*)"[^>]*>([^<]*)</g and then tests names.has(m[2].trim()). For a button whose body is a mark and a word the content is the two together, so the set lookup misses and the slot passes. I constructed the case rather than reasoning about it. In an isolated copy I deleted the filter entry from util/icons.json and rendered. The page ships data-help="filter">filter Filter<, the bare name on the face, which is the exact thing this check is named for, and render-check answered 0 failed. no-loose-glyphs.mjs also answered 0 failed, because it reads source and not the page. se lint answered clean, because LintIcons reads util/parameters.json and knows nothing about the editor's slots. So a missing table entry reaches a button face with every check in the project green. Extent, searched in one pass over every icon() call in source/extension: there are fourteen slots and four are in this family, all in editor.ts. Line 145 ${icon("plus")} Group, line 150 ${icon("filter")} Filter, line 151 ${icon("sort")} Sort, line 152 ${icon("columns")} Properties. The other ten hold the mark alone and the check does see them, which is why deleting pin or split is caught and deleting filter, sort, columns or plus is not. The worker's proved-red used split, one of the ten, so the family that was already the blind spot in round one is still the family nobody has put the check against. Two numbers in the submission do not survive a recount, and both read as more careful than they are. Four classes are excluded because they hold data rather than marks: the set at line 102 holds five, name, count, bs-view-name, bs-ticked and bs-join. 0 failed across 67 checks: render-check prints 65 ok lines and 0 FAIL today. Everything else in the submission reproduces, and I checked it: no-loose-glyphs.mjs goes red for the round-one defect when I put dir.textContent = asc ? the up arrow : the down arrow back into editor.ts, and the round-one fix itself holds end to end. With up set to U+2191 and down to U+2193 in the table the page carries no U+25B2 at all and the click handler reads data-up and data-down, so the arrow after a click now comes from the table. panel-icons.mjs prints 12 ok and 0 failed. source/viewer's suite is green and TestTheMarksComeFromTheTable covers both the table path and the written fallback. The engine suite is red right now on TestTheFloorAgreesWithTheDeclaration, about limits.unreviewed_before_blocked, which is another token being reworked in the same tree while I read; the four icon tests pass and I am not counting that failure against this token.

**satisfies:** Compare each whitespace-delimited word of the slot content against the table's names rather than the whole trimmed content, so a mark beside a word is read as two things and the name is seen. The four capitalised words beside these marks, Group, Filter, Sort and Properties, do not collide with any name in the table, and the classes that hold free text are already excluded, so splitting on whitespace does not cost a false positive here. Then put the check against this family before trusting it, because that is what was skipped both rounds. Delete filter from the table, render, and watch the check go red on filter Filter; restore it and watch it go green. Do the same for plus, sort and columns, which are the other three of the shape, and keep split or pin as the bare-mark case so both shapes are covered. Better still, and this is what closes the class rather than these four lines: loop the deletion over every name the table declares, render once per name, and assert the check goes red each time. That is cheap because the page is pure, it needs no new list to be kept in step with editor.ts, and it is the only form of this check that cannot quietly stop covering a slot somebody adds later. While the numbers are being corrected, say five excluded classes and count the checks the run actually prints.

