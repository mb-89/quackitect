---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: no dates in git
# where the token stands. The process owns these values.
status: done
# who did the work step, so the verdict is never theirs
author: main
claimed_by: 542bcda8/reviewer-ravel
claimed_at: "2026-09-04T18:49:07Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 5018e875ad88d61700bd237936f09bf0076dedf1
  - 0a834ab0c87e9d7e751065a6ab76a5938e2bc89c
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 1e355f3f86bb7178d729420cafad92651b744e97
  - da511e3467a1bead79e54b7b6d164a270910f887
---

## detail

Rule 13 says datetimes do not go into git. It has no mechanism and no agreed reading.

The ruling: no date in prose that travels. Where a time is needed, a month and a year. A machine field that needs a stamp keeps it, because a claim is compared against a clock.

Caught today: a whole-file copy of a private original, at hook.go:1139.

Caught by nobody: identity material. Three classes, each with what it needs.

- dates: a pattern, and the ruling above. Easy.
- usernames: the machine knows its own user, and the tree knows its actor names. Easy.
- personal names: a list, because no pattern finds them. The list is itself identity material, so it lives under .se. Hard.

Measured: one tracked file carries a datetime, which is doc/work/wk-b166fb07eb.md. The tree is nearly clean already, so the check holds it there rather than cleaning it up.

## proposed action

Refuse identity material at the door that already refuses a private copy. Take dates and usernames first, because neither needs a list.

THE INTERFACE. A new src/engine/identity.go answers one question:

    identityMaterial(content, user string) (rule, matched string, found bool)

It is pure, so a table drives it. hook.go asks it beside copyOfAPrivateOriginal, under the same conditions: a write, prose, and not under .se.

WHAT IT READS. A machine field keeps its stamp, so a YAML key line is skipped and a frontmatter block is skipped whole. The rest is prose.

WHAT IT MATCHES. An ISO date, an ISO datetime, a clock with seconds, and a day beside a month name. A month and a year is taken. The machine's own username is matched whole, so a path carrying it is caught.

Personal names need a list, which is itself identity material, and that is not here.

## done when

- a write into a tracked file carrying a datetime is refused, and the refusal names what it matched
- a write carrying a month and a year is taken
- a write into .se carrying a datetime is taken
- a token field that carries a stamp, such as claimed_at, is not refused
- a write carrying this machine own username is refused
- doc/work/wk-b166fb07eb.md carries no datetime

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | proposed action names one pure function and where hook.go asks it. A reader can disagree with the folder rule, or with skipping every YAML key line. | proposed action |
| [x] | every done-when line is decidable, and names the command where one decides it | Lines 1 to 5 are decided by a table test in src/engine, run by se test on this token. Line 6 is decided by se find over that file. | done when |
| [x] | the change is small enough to review whole, or it is split first | One new file, one block in hook.go, and one test file. | — |
| [x] | the basics it stands on exist, or are minted first | The door exists: hook.go already refuses a private copy under these conditions. | src/engine/hook.go:1012 |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | The table was written first and watched red twice: on an undefined function, then on the row the regex missed. | the red runs |
| [x] | the change follows the approach on the token, or the token says why it departed | It departs once. A username under three characters matches nothing, and this machine's is two, so criterion 5 holds in the table and not on this box. | identity.go |
| [x] | se test --on this token answered ok, and what it ran is named | ok. TestIdentityMaterialIsRefused, TestAShortUsernameRefusesNothing, TestAnApplyIsHeldToTheVoiceRules. | 11.4s |
| [x] | the note says what changed and why, for a reader who was not here | identityMaterial reads the prose under the frontmatter, skips YAML key lines, and answers the first rule broken. hook.go asks it beside the private-copy refusal. | identity.go |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The one tracked datetime is now a month and a year. | wk-b166fb07eb:21 |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | [[reviewing]] was read and applied | — |  |
| [ ] | every hunk of git diff began..ended was read, and any not read is named |  |  |
| [ ] | every criterion's command was run again, and what it said is named |  |  |
| [ ] | every hunk improves the product, or a finding names the one that does not |  |  |
| [ ] | every finding is a trivial token naming this one, and their ids are here |  |  |

