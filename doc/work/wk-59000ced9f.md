---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: done token finds author
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-fir
claimed_at: "2026-09-05T15:23:14Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 6467583aa25898240c3b2140ebd3a154fb0c8711
  - a24e198f7fe3c41e946a819d0302ed56c401cc98
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 723faa9251e593d103d156cc1144d52612ad4c49
---

## detail

A worker pull hands the author its own done token, and the only move offered is the verdict. Seen on wk-4cb6df2b99: worker-satie submitted the do step, status went to done and author to worker-satie. The next pull with role worker handed the same token back with the step 3 checklist, and a pull with id and reason answered a finding wanting a disposition. standard says the verdict is by a reviewer, never the author, and by role reviewer. So a worker who has submitted has no legal move but to grade its own work or hold the token.

## proposed action

A pull with role worker skips a token in a state whose next activity is role reviewer, and a pull by the author skips its own token there. The submission puts the token down, so the author's next pull does not find it in hand.

## done when

- a worker pull after its own submission answers wait or another token, decided by: a test in src/engine that submits the do step as one actor and pulls again as that actor with role worker, and reads no token with status done and author that actor
- a reviewer pull by the author is refused with a reason naming the author rule

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

