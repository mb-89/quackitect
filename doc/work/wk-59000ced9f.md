---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: done token finds author
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-berg-two
claimed_at: "2026-09-05T16:41:51Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 6467583aa25898240c3b2140ebd3a154fb0c8711
  - 1b4563c727d28e4c112a6f457dfe46c085a7a9a1
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 723faa9251e593d103d156cc1144d52612ad4c49
---

## detail

A worker pull hands the author its own done token, and the only move offered is the verdict. Seen on wk-4cb6df2b99: worker-satie submitted the do step, status went to done and author to worker-satie. The next pull with role worker handed the same token back with the step 3 checklist, and a pull with id and reason answered a finding wanting a disposition. standard says the verdict is by a reviewer, never the author, and by role reviewer. So a worker who has submitted has no legal move but to grade its own work or hold the token.

## proposed action

A pull with role worker skips a token in a state whose next activity is role reviewer, and a pull by the author skips its own token there. The submission puts the token down, so the author's next pull does not find it in hand.

## done when

- a worker pull after its own submission answers wait or another token, decided by: a test in src/engine that submits the do step as one actor and pulls again as that actor with role worker, and reads no token with status done and author that actor
- a reviewer pull by the author is refused with a reason naming the author rule

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Two done-when lines, one test, one helper in pull.go. Read whole in one sitting. | the token |
| [x] | every done-when line is decidable, and names the command where one decides it | Both are decided by TestAWorkerPullDoesNotHandTheAuthorItsOwnDoneToken, run through ./RUNME.sh test --propose on this token. | ok true |
| [x] | the basics it stands on exist, or are minted first | Pull, TakeUp, LoadToken, roleAt and refuse all exist. Nothing had to be minted first. | se find, src/engine/pull.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 sent me to the pre-fix tree rather than trust a green. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | The test was in the tree at take-up. At pre-fix snapshot 723faa9 it failed: the author was handed its own done token. | go test, FAIL |
| [x] | the same test was seen green after the change, and named | TestAWorkerPullDoesNotHandTheAuthorItsOwnDoneToken, run here through the RUNME door. The ran list holds it alone, ok true. | ok true |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | ownVerdictOffTheHand is absent at 723faa9 and present at began 1b4563c. My commit adds the answered token. | grep count 0 then 3 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | This token file was untracked and missing from origin/v4. The commit carries it. | git status, ?? |

## evidence: step 2, where the change came from

The test and ownVerdictOffTheHand were already in the tree when I took the token up. A reviewer should know I did not type them.

So the red was earned, not assumed. I built a detached worktree at 723faa9, the put-down snapshot, which has neither half. I copied only the test in and ran it there.

It failed on the line that names the defect. After naming the token again, the worker was handed its own done token. The first assertion, straight after the submission, already passed there. So the take-up through se run is the half that was broken.

The same test is green in this tree.

