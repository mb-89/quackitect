---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: sweep says rendering still
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-linden
claimed_by: 547b9365/worker-linden
claimed_at: "2026-09-05T19:30:31Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 282c81dde7f35c388517f67dfe0a3377f4230f05
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 0faa6840a36a241a825597406afec4a5f032dd35
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Found reviewing wk-808abd40a4 (archive without a ref).

That token turned doc/work/archive.jsonl from a rendering of the tags into the record, and its own doc comment on WriteArchiveList says so: "A row naming a blob has no tag to be rebuilt from, so the list is where it lives." Its new test TestTheArchiveListRebuildsFromTheTags proves the rest of it: delete the list, call WriteArchiveList, and the file does not come back.

The comment on SweepClosed (src/engine/archive.go, around line 396) was left saying the opposite: "THE LIST IS LEFT RIGHT WHETHER OR NOT ANYTHING MOVED. A sweep with nothing to sweep is how the list is put back after it is deleted, and that is the whole claim that it is a rendering." That is no longer true and the code under it is worse than stale: SweepClosed ends with an unconditional WriteArchiveList(r), so a reader who follows the comment and sweeps to put a lost list back writes a list holding only what the tags carry, and every row naming a blob is gone for good.

Either the comment goes and the unconditional rewrite goes with it, or the sweep learns to refuse to write a list shorter than the one it read.

## proposed action

Rewrite the SweepClosed comment to say the list is the record. Drop the trailing unconditional WriteArchiveList(r), or guard it so a write that would shorten the list is refused. Cover with a Go test in src/engine that sweeps after the list is taken away and asserts no blob row was lost.

## done when

- the SweepClosed comment no longer claims the list is a rendering that a sweep puts back
- a sweep cannot replace an existing archive.jsonl with one holding fewer rows, or the unconditional WriteArchiveList at the end of SweepClosed is gone
- watched red first: a test that deletes the list, sweeps, and asserts the rows naming a blob are still readable, failing before the change
- TestASweepStillFilesWhatIsFinished and TestTheArchiveListRebuildsFromTheTags still pass

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Two files: the comment and its guard in archive.go, and the test that watches the loss. | 2 files |
| [x] | every done-when line is decidable, and names the command where one decides it | Lines one and two are read off SweepClosed. Line three is the new test. Line four names two tests no file in this tree carries, so the Archiv, Sweep, Tidy and Close runs that hold those claims were run instead. | 4 runs |
| [x] | the basics it stands on exist, or are minted first | The list is already the record, and the tags fold in through keepInList, so nothing had to be minted first. | keepInList |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token read. The test takes t.Parallel and a tree of its own, and prints what was lost. | work-token |
| [x] | one test was written first and seen red for the reason expected | With the list deleted the sweep wrote one holding the tagged row alone, exit 1. | 1 FAIL |
| [x] | the same test was seen green after the change, and named | TestASweepPutsBackNoListItCannotRebuild passes, before and after the rebase. The Archiv, Sweep, Tidy and Close runs pass, gofmt and vet clean. | exit 0 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Not this clone, which is far behind origin/v4. It landed as a0df9c00. | a0df9c00 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change. WriteArchiveList said the same thing one function up, so it now says where the fold happens. | WriteArchiveList |

