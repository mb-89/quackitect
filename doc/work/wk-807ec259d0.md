---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: state verb passed over
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-holly
claimed_by: 547b9365/worker-holly
claimed_at: "2026-09-05T20:31:50Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0da8ae46e8599c8168e05a177c1466398181afb6
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 0bdb5a670c05c51fa6c09ad954e4703a7eb27a5f
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: "The check now maps se_status to the state verb, passes over state's --json by name with its reason, and holds every advertised tool to naming a verb, with se_start named as the one that runs none. Seen red by dropping the map entry and green with it back. Landed on origin/v4 as ed890f9b61935e25298dfe7899cb7a55f9a8949b. On its first day it found a real drift that predates it: se_work has no successors field for the work verb's --successors, written up as wk-9ae1f1c5c1."
# what it became. They have to exist.
successors:
  - "[[wk-9ae1f1c5c1]]"
---

## detail

A finding on the verdict of wk-a0804eaf08. util/checks/lane-carries-every-flag.mjs holds each engine verb to the lane tool named se_<verb>, and passes over a verb with no tool of that name: "if (!tools.has(tool)) continue; // a verb the lane does not wrap is not its business". But se_status wraps the state verb under another name, so state's flags (--json, "answer the structure rather than the screen") are never held to se_status, silently, and the comment's reason is wrong for it. The check says it closes the drift between verb and tool, and a tool named unlike its verb stays outside it. se_start is the one tool that runs no engine verb, and nothing says so.

## proposed action

Give the check a verb-to-tool map beside its renames, state to se_status, and a named pass-over for state's --json with the reason that the lane always answers the structure. Then hold the other way too: every se_ tool in util/cage/tools.json names a verb, with se_start listed as the one that does not and why. Seen red by dropping the map entry.

## done when

- node util/checks/lane-carries-every-flag.mjs on a worktree of origin/v4 prints a line for se_status and one for se_start, and neither is a FAIL
- the same check fails when state is taken out of the verb-to-tool map, and passes with it back

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One file. Three named constants, one line in the forward pass, and a loop for the other direction. | commit ed890f9b |
| [x] | every done-when line is decidable, and names the command where one decides it | Both decided by node util/checks/lane-carries-every-flag.mjs on a worktree of origin/v4. Line 2 runs it with the map entry dropped. | the three runs |
| [x] | the basics it stands on exist, or are minted first | Nothing to mint. The check already read tools.json and the verbs' flags. It needed a map, not an instrument. | the check |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rules 11 and 12. Every pass-over is named with its reason, which is the rule this check already held. | the check |
| [x] | one test was written first and seen red for the reason expected | The check is the test. Red by dropping the map entry: FAIL se_status names the status verb, exit 1. | the three runs |
| [x] | the same test was seen green after the change, and named | Entry back: a line for se_status, one for se_start, neither a FAIL, exit 0. Five neighbouring checks exit 0 too. | the three runs |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Not in this tree, which is 339 commits behind v4. Landed on v4 as ed890f9b. | where it was done |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | A token of its own, wk-9ae1f1c5c1. | the red it found |

## evidence: the red it found

The commit message says 0 failed, and that was true at 7344ad5f, where the runs above were made. v4 moved to b0f98d45 while this was landing. At that tip the check answers `FAIL se_work can say --successors`, 1 failed.

That red is not this change. It reproduces on a clean worktree of b0f98d45 with nothing of mine applied. It arrived with 5e94c17c, which gave se work --abort its --successors and never gave the lane's se_work the field.

So the check found a real drift on its first day holding that ground. It is written up as wk-9ae1f1c5c1, tracked, so another box can take it.

## evidence: the three runs

All on a worktree of origin/v4, from `node util/checks/lane-carries-every-flag.mjs <root>`.

With the change, at 7344ad5f: `ok se_status is answered the structure, so state --json is not its to send`, `ok se_start runs no engine verb, and that is what it is for`, and `ok se_status names the state verb`. 0 failed, exit 0.

With state dropped from the map: `FAIL se_status names the status verb`. 1 failed, exit 1. That is the reverse pass doing the work. Dropping the entry only makes the forward pass go quiet again, which is the silence this token is about.

With the entry put back: 0 failed, exit 0.

no-loose-glyphs, scripts-are-lf, checks-live-in-the-method, no-lone-escape and refusals-name-a-door all exit 0 over the changed file.

## evidence: where it was done

This clone is 339 commits behind origin/v4. The check was changed on a fresh worktree of origin/v4, run there, and pushed as ed890f9b. The pushed tree was read back, and it matches the worktree the runs were made in.

