---
id: wk-6504f63cc7
seq: "3"
type: work
title: editor shows counts
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
minted_by: main
evidence:
  - outcome
---

## detail

The work editor shows counts: how many open, how many backlogged. That is the specific editor's job, not the generic one's.

## evidence: outcome

A count is a named filter the view declares, so the generic editor stays generic. util/views/work.base declares four and se query answers them over what the view selects. The work editor bar shows open, in work, in review and backlogged. The render check confirms the counts and the total reach the page.
