---
id: wk-61af3a054e
seq: "-8"
type: work
title: the backlog is cleared
status: imp_in_review
assignee: main
scope: single-step
traced: true
disposition: done
holder: reviewer6
rounds: "3"
minted_by: person
---

## detail

Take every backlog item that needs no judgment and do it.

THE OWNER'S WORDS: go into the backlog and think what is in the backlog that
does not need judgment, and then you do that too, so you clear out the backlog.

WHAT NEEDS JUDGMENT STAYS. A token that asks for a decision only the owner can
make is not cleared by doing it. It is read, and the ones that turn out to be
decisions are left with a sentence saying which decision they wait on.

THE REST ARE DONE. A lesson with an obvious remedy, a rule already agreed and
not yet written down, a check that was named and never built.


THE BACKLOG AS IT STOOD WHEN THIS STARTED, written here so the work has a fixed
list to be judged against. A check that compares the evidence with the LIVE
backlog outlives its token: the evidence is written once and the backlog goes on
changing, so the check goes red the next time anybody mints a backlogged token,
forever. Both halves of what this is judged against are inside the token.

- wk-097b2cac17
- wk-10d3cf13cd
- wk-12b43e820b
- wk-24eeb4f3d4
- wk-35e11bbe73
- wk-4349bc412e
- wk-4a1ee98339
- wk-4b67d7126a
- wk-4d0912e39c
- wk-4e8eeb76aa
- wk-50805e98a1
- wk-5544578d91
- wk-70df338d58
- wk-74558c90da
- wk-7f4983c759
- wk-967b04b877
- wk-be358bd3f3
- wk-ccd563ec25
- wk-d1b585b7b9
- wk-dcb697c1b1
- wk-de69604cfd
- wk-f4731bc18b


WHAT HAPPENED TO EACH ONE, and this is the other half the check reads.
Both halves are inside this token, so the check stops changing when the token
closes.

WHAT THIS IS. The list this is judged against is the snapshot inside
wk-61af3a054e, twenty-two ids, fixed when the work started. Eleven are aborted,
one is settled as a duplicate, and ten are still backlogged with what each waits
on. Four of the twenty-two are ephemeral and live in .se/work rather than
doc/work, which is why a reader looking only in doc/work finds eighteen.

WHAT ABORTING MEANS HERE. A token asking for something that is already true is
obsolete, and obsolete is one of the endings an abort carries. The reason names
the line that proves it, so a reader who finds the aborted token is sent to the
code rather than left wondering.

ABORTED, BECAUSE THE REMEDY ALREADY LANDED.

wk-24eeb4f3d4 one class one meaning. The toolbar button that files ticked rows
is bs-make-bucket, src/extension/editor.ts:152, and bs-group names the filter
group and nothing else. The handler cannot reach a filter control.

wk-74558c90da one class one wiring. The level-add loop matches
.bs-add[data-kind], src/extension/editor.ts:976, so Hide all keeps the handler
wireChrome gave it and bs-add stays the styling name.

wk-5544578d91 move reports its refusals. declinedPairs, src/engine/move.go:145,
gives the residual sweep the spellings the rewrite refuses, so the report names
a file the rewrite left alone.

wk-70df338d58 a guard pull outruns. The guard is asked where it can answer:
quietHold is called directly, src/engine/investigate_test.go:143 and :146, once
for the holder and once for somebody else.

wk-ccd563ec25 one pass hides rows. candidates() skips a row a folded parent took
away, src/extension/editor.ts:1131, in the same pass as the other two.

wk-f4731bc18b learned: containment asked as a string question. under(),
src/engine/query.go:156, asks filepath.Rel and refuses a result of two dots or
one starting with two dots and a separator, which is the shape the lesson named.

ABORTED, BECAUSE FOUR LESSONS WERE ONE LESSON.

wk-10d3cf13cd, wk-4a1ee98339, wk-967b04b877 and wk-dcb697c1b1 each named a way a
criterion's command fails to decide the sentence above it. They are one class,
and four tokens waiting to be done one at a time is the thing the owner said to
stop doing. wk-f0c30a2363, minted after this snapshot was taken, is the fifth
shape and went the same way.

The class is now written where a drafter reads it, in doc/guidance/specifying.md
under "The command decides the sentence above it": borrowed, a literal nobody
derived, a set covered by one member, a live fact demoted to prose, and a
one-time assertion written as a standing rule. The last two are one question
asked from both sides, so they are stated together.

TWO OF THEM ALSO HAD AN INSTANCE, and both instances are already fixed.
wk-c6247665a3 now carries one command criterion per commit, thirteen of them.
wk-61af3a054e pins its assertion to the snapshot above.

SETTLED AS A DUPLICATE.

wk-4349bc412e a warm model says what wk-4b67d7126a the engine stays running
says: a long-lived engine holding in memory the state that is a file today. One
of them stays and it carries the successor, so a reader who finds the settled
one is sent to the live one.

STILL BACKLOGGED, AND WHAT EACH WAITS ON.

wk-097b2cac17 draw the process. SIPOC over this whole system. The owner asked
for it by name, and it is a drawing the owner agrees rather than one to hand in.

wk-12b43e820b the record stays small. Where the line falls between doc/work and
the scratchpad is the owner's line to draw.

wk-4b67d7126a the engine stays running. A resident engine on a port replaces
five processes per tool call with one, and it changes what every file the engine
writes is for.

wk-4e8eeb76aa a token gates writes. The token says in as many words that it is
to discuss before building, because a guard that refuses too much is one
somebody turns off.

wk-de69604cfd a reviewer that waits. The token names what would settle it:
measure how long a submission waits in practice. The owner has said which way
they lean, and a measurement nobody has taken is not a decision.

wk-7f4983c759 a template per type. What shape a work note has to keep is the
owner's, and an engine refusing a note that does not fit is a gate the owner has
to want.

wk-d1b585b7b9 the engine nudges. The owner gave the shape and the numbers, three
six and nine, once each. It is buildable, and it changes what the engine says to
every agent on every pull, so it goes when the owner can watch it happen.

wk-35e11bbe73 an investigation is answered. Two things: a walker that has looked
can say so, and the staleness of ten pulls is wrong for a reviewer that is
reading. The second is a number about how patient the engine is, and the owner
picks it.

wk-4d0912e39c drain names what stays. It is about wk-339068932c, the retro
command, which is still at spec. It waits on that token rather than on a
decision of its own.

wk-50805e98a1 sets belongs with property. A grouping level rewritten in place
keeps sets from the old property, so a heading names one field and a drop onto
it writes another. The remedy is named and small. It waits because it touches
the editor, and the editor is mid-port on wk-66a28ca311.

wk-be358bd3f3 answer every finding. The token names where the fix belongs: the
engine knows the findings on a token, so it can refuse a submission whose
evidence answers fewer sections than there are open findings. That is a new
refusal at the gate, and a new refusal is the owner's to want.

TWO MORE, JUDGED ALTHOUGH THEY ARE NOT ON THE SNAPSHOT.

wk-d2f6e959ef written reads back. Aborted: TestAFilterReadsBackAsWhatWasBuilt,
src/engine/filterbuild_test.go:14, drives the round trip the token described.

wk-09f0384f90 a command is statements. Left backlogged. The newline goes in the
separator list, which is one line. The here-doc is the judgment: refusing a
script that quotes a search is a false positive the owner has a view on, because
the detail already says a refused permitted case sends somebody looking for a
workaround.



WHY THE FIRST CRITERION SAYS DETAIL AND NOT EVIDENCE, and this is a change to
what was agreed. As agreed it read "named in this token's evidence", and no
command could ever decide that sentence. The engine runs the command criteria at
the submission gate and writes the submission's evidence onto the token
afterwards, src/engine/pull.go:283 then :289, so a check reading the token's
evidence reads a token that has none and is red on every submission including a
correct one.

THE INTENT IS KEPT AND THE INSTRUMENT IS MOVED. What the criterion was for is
that both halves sit inside the token, so the check freezes when the token
closes and does not follow a backlog that goes on changing. The detail does that
and the evidence cannot. Nothing else about the criterion moves.

A REVIEWER HAS TO AGREE THIS. A criterion agreed in a spec review is not mine to
edit alone, and the refusal at the gate says so in as many words: every
criterion met, or a reviewer agreeing a change to them. This is the second.

## evidence: Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. The evidence says what was seen each time.

Both checks were watched failing on their own assertion, in a git worktree at the commit under test, so a reviewer working in the main tree was not running against a half-applied change. .se/scratchpad/observed-red-backlog.txt is what was seen, and the observation is on each criterion.

THE FIRST ONE WENT RED ON A REAL DEFECT AND NOT AN INJECTED ONE, which is worth reading. It said 22 ids on the list, 22 passed over. I had appended the verdicts to the detail under ## sub-headings, and a ## heading ends the detail, so LoadToken returned a detail that stopped before the first verdict. The check found that before I did. Green at 22 of 22 with the headings written as the uppercase leads the rest of the note uses.

THE SECOND WAS WATCHED RED TWICE, once for each thing it claims, because a refusal that never fires and a comparison that cannot fail are two defects and one red does not cover both. With the empty-list refusal written as len(named) < 0 so it never fires: a detail with no list was accepted. With the continue taken out, so a list line is searched as well as counted: it passed over [], and wk-2222222222 is the one nothing answers.

AND THE FIRST CRITERION SAYS DETAIL WHERE IT SAID EVIDENCE, WHICH IS A CHANGE TO WHAT WAS AGREED and needs your say. As agreed no command could ever decide it: the engine runs the command criteria at src/engine/pull.go:283 and writes the submission's evidence onto the token at :289, so a check reading the token's evidence reads a token that has none and is red on every submission including a correct one. What the criterion was for is that both halves sit inside the token, so the check freezes when the token closes rather than following a backlog that goes on changing. The detail does that and the evidence cannot. Nothing else about it moves, and the reasoning is written on the token under WHY THE FIRST CRITERION SAYS DETAIL AND NOT EVIDENCE.

## evidence: For each one the evidence says what happened: done, or left backlogged with the reason it is still there, and the reason is a sentence about that token rather than a form of words

Every one of the twenty-two on the snapshot is named in the token's detail with what happened to it, under WHAT HAPPENED TO EACH ONE. Twelve are settled and ten are still backlogged.

SETTLED, AND WHY EACH ONE IS. Six named a defect whose remedy had already landed, so each abort names the line that proves it rather than saying obsolete and leaving a reader to look: wk-24eeb4f3d4 at src/extension/editor.ts:152, wk-74558c90da at :976, wk-ccd563ec25 at :1131, wk-5544578d91 at src/engine/move.go:145, wk-70df338d58 at src/engine/investigate_test.go:143, and wk-f4731bc18b at src/engine/query.go:156. Four more named one class of mistake with four shapes, and four tokens waiting to be done one at a time is the thing the owner said to stop doing, so the class went into doc/guidance/specifying.md under The command decides the sentence above it, where a drafter reads it. wk-4349bc412e says what wk-4b67d7126a says and is settled as a duplicate, carrying the successor so a reader who finds it is sent to the live one.

STILL BACKLOGGED, AND EACH REASON IS ABOUT THAT TOKEN. wk-097b2cac17 is a drawing the owner agrees rather than one to hand in. wk-12b43e820b asks where the line falls between doc/work and the scratchpad, which is the owner's line. wk-4b67d7126a changes what every file the engine writes is for. wk-4e8eeb76aa says in as many words that it is to discuss before building. wk-de69604cfd names what would settle it, a measurement nobody has taken. wk-7f4983c759 is a gate the owner has to want. wk-d1b585b7b9 changes what the engine says on every pull, so it goes when the owner can watch it. wk-35e11bbe73 turns on a number about how patient the engine is. wk-4d0912e39c waits on wk-339068932c, which is still at spec. wk-50805e98a1 has a named remedy and waits because the editor is mid-port on wk-66a28ca311.

TWO MORE WERE JUDGED THOUGH THEY ARE NOT ON THE SNAPSHOT, because they were minted after it was taken and leaving them unread would have been the thing this token exists to stop. wk-f0c30a2363 is the fifth shape of the same lesson and went with the other four. wk-d2f6e959ef I aborted as obsolete and that abort was wrong: I cited a test that could not fail for the defect. The abort stands, because an ending is not written over, and its reason now says the abort was wrong and names wk-5bec911840 as where the work went.

## done when

- Every id on the list above is named in this token's detail beside what happened to it, so none was passed over without being read. Both halves are inside this token, so the check stops changing when the token closes
  `rg -q func.TestEveryBacklogItemWasRead src/engine && go test -C src/engine -count=1 -run TestEveryBacklogItemWasRead$ .`
  **red without** nothing put back by hand: the detail's verdicts were behind a ## sub-heading, and a ## heading ends the detail, so LoadToken returned a detail that stopped before the first one
  **red said** backlogread_test.go:77: 22 ids on the list, 22 passed over, with one line naming each id the detail said nothing about
- That check refuses when the list is empty or the token cannot be read, so it cannot pass by finding nothing to compare
  `rg -q func.TestTheBacklogCheckRefusesAnEmptyList src/engine && go test -C src/engine -count=1 -run TestTheBacklogCheckRefusesAnEmptyList$ .`
  **red without** the refusal, its condition len(named) == 0 written as len(named) < 0 so it never fires; and separately the continue, so a list line is searched as well as counted
  **red said** backlogread_test.go:84: a detail with no list was accepted; and backlogread_test.go:101: it passed over [], and wk-2222222222 is the one nothing answers
- For each one the evidence says what happened: done, or left backlogged with the reason it is still there, and the reason is a sentence about that token rather than a form of words
- Every test named above was watched failing on its own assertion, with the change absent, before it was watched passing. The evidence says what was seen each time.

## finding 1 · round 1 · done when: The check refuses when it finds no backlogged token to judge, so it cannot pass by the backlog being unreadable / runs: rg -q func.TestEveryBacklogItemSaysWhyItWaits src/engine && go test -C src/engine -count=1 -run TestEveryBacklogItemSaysWhyItWaits$ . · by reviewer4

**wrong:** TWO CRITERIA CARRY THE SAME COMMAND, so the second cannot fail for what it says. The runs line on criterion 1 and criterion 2 are byte-identical: rg -q func.TestEveryBacklogItemSaysWhyItWaits src/engine && go test -C src/engine -count=1 -run TestEveryBacklogItemSaysWhyItWaits$ . -- and python .se/scratchpad/run-criteria.py doc/work/wk-61af3a054e.md prints it twice, which is how I noticed. Criterion 2 says something the command cannot see: whether that test refuses when it finds no backlogged token to judge. Once the test exists and passes, both criteria are green, with or without the anti-vacuity guard. So the one criterion whose whole job is to stop the check passing vacuously is itself the vacuous one. IT IS RED TODAY FOR THE WRONG REASON. All three commands are red, and all three are red only because the rg guard says the function is absent. That proves the test does not exist; it says nothing about whether the assertion can fail once it does. This project has already settled that an absent test is no red at all -- it is the finding wk-a51a799f40 was sent back on twice -- and the same rule applies here, at the stage where it is cheapest. EXTENT, one pass over the four. Criterion 3 has no command and is right not to: it asks what the evidence says, which is prose. Criterion 4 has its own command. So this is one duplicate and the fix is one line, but it is the line that decides whether the whole check can pass on an unreadable backlog.

**satisfies:** GIVE CRITERION 2 A COMMAND THAT CAN FAIL FOR ITS OWN CLAIM. The claim is that the check refuses when there is nothing to judge, so the command has to make there be nothing. Two shapes work with what is here. Write the reading as a helper that takes the folder, and add a second test -- TestTheBacklogCheckRefusesAnEmptyBacklog -- that calls it on an empty temporary directory and requires an error; then criterion 2's command names that test. Or keep one test and have it fail loudly on a zero count, and pin the criterion to the sentence: rg -q 'so this guards nothing' on the test file, beside the run. The first is stronger and the tree already has the shape -- TestNoNoteCarriesATime fails with 'no note was read, so this guards nothing' when its directory is empty. AND SAY IN THE DRAFT THAT EACH COMMAND WAS RUN BEFORE THE WORK STARTED and what it answered, so the next round is not spent establishing that red-because-absent is not red.

## finding 2 · round 1 · WHAT NEEDS JUDGMENT STAYS ... the ones that turn out to be decisions are left with a sentence saying which decision they wait on. / done when: Every token that is still backlogged names the decision it waits on, on a line beginning WAITS ON. · by reviewer4

**wrong:** THE CRITERION IS A STANDING RULE OVER LIVE DATA, and the detail describes a one-time clearing. 'Every token that is still backlogged names the decision it waits on' is written as a test that reads doc/work, so it does not stop being true or false when this token closes: it goes red the next time anybody mints a backlogged token without that line, forever. AND IT REDEFINES A WORD THE ENGINE ALREADY OWNS. se work --help says --backlog means 'mint it backlogged: visible, and not work anybody is doing'. The criterion makes backlogged mean 'waiting on a decision', which is a narrower thing, and nothing in the engine enforces or documents that narrowing. The detail itself is looser than the criterion: it says the ones that turn out to be decisions are left with a sentence, which allows a backlogged token that is not a decision. The criterion forbids it. Those two do not agree and the criterion is what the work is judged against. MEASURED, against this tree, today, by reading every note in doc/work: 10 tokens are backlogged and 0 of them carry a WAITS ON line. Five of the ten -- wk-24eeb4f3d4, wk-50805e98a1, wk-5544578d91, wk-70df338d58, wk-74558c90da -- were minted today by a reviewer as lesson tokens, and each one carries a named defect, a reproduction, a remedy and a red check. None waits on a decision. Writing WAITS ON on any of them would be a false sentence, and closing or opening them to satisfy the check would be moving work to satisfy a test. AND THE ENGINE NOW MAKES MORE OF THEM ON EVERY REJECTION. A rejection whose lesson names no token is refused -- I hit that refusal today, verbatim: 'mint a token for the lesson with se work, backlogged or open as you judge'. So the population this criterion governs grows every time a reviewer rejects anything, and backlogged is the natural half of that choice.

**satisfies:** SAY WHICH IT IS, A CLEARING OR A RULE, AND WRITE THE CRITERION TO MATCH. If it is the clearing the detail describes, pin it to what exists now: name the backlogged ids in the draft, or have the check read a list the work writes down at the start, and assert that each of THOSE is closed, opened, or backlogged with its reason. That is a criterion this token can satisfy and finish, and a token minted tomorrow does not turn it red. If it is meant as a standing rule, then it is a change to what backlogged means and it needs saying out loud in the detail, plus an answer for the kind of backlogged token that is not waiting on anything -- a lesson with a remedy and a red check, of which there are five right now. Either carve those out by naming the shape, or say that a lesson token is minted open rather than backlogged and change the refusal's wording to match, because the refusal offers both today. THE CHECK EITHER WAY: run it against the ten backlogged tokens that exist as I write this, before agreeing the draft, and say what it answered. Ten red is a finished clearing waiting to happen; ten red forever is a rule nobody can keep.

## finding 3 · round 1 · done when: Nothing that was backlogged is left in a state nobody can act on: every one is closed, or open with somebody holding it, or backlogged with its reason. · by reviewer4

**wrong:** ONE OF THE THREE STATES THE CRITERION ALLOWS IS ONE THE ENGINE NEVER PRODUCES. Criterion 4 says every one is 'closed, or open with somebody holding it, or backlogged with its reason'. There is no open token with a holder in this engine. I searched every assignment to Holder in src/engine in one pass: every write that sets Open sets the holder empty in the same statement -- pull.go:347, pull.go:456, pull.go:717 and arrival.go:177 are all t.Status, t.Holder = Open, "" -- and the only writes that set a holder set InWork or InReview with it (pull.go:736, pull.go:666) or SpecInReview (pull.go:656). A token being worked on is in_work, not open. SO THE CRITERION CANNOT BE MET AS WRITTEN, and the way it will be met is by quietly reading 'open' as 'open or in_work', which is a decision the draft should make rather than the worker. The distinction matters for exactly the thing this token is about: an open token with no holder is work nobody has started, which is the state the criterion was presumably written to forbid.

**satisfies:** NAME THE STATES THE ENGINE HAS. The three that mean somebody can act are: closed; in_work with a holder; and backlogged with its reason. If open with nobody holding it is also acceptable -- it is, it is the ordinary waiting-to-be-picked-up state and the queue hands it out -- then say so and drop the holder clause. Write the criterion against the status names the engine actually writes, and add the command that reads them, so it fails when a token is left in a state the list does not name. As it stands the command exists but the sentence it is checking describes a state machine this project does not have.

## finding 4 · round 2 · WHAT NEEDS JUDGMENT STAYS ... the ones that turn out to be decisions are left with a sentence saying which decision they wait on. / done when: Every token that is still backlogged names the decision it waits on, on a line beginning WAITS ON. · by reviewer4

**wrong:** UNTOUCHED AND UNMENTIONED. The draft came back with criterion 2 given a test of its own, which closes finding 1 and closes it properly. Finding 2 has no answer of any kind: not fixed, not attempted, not declined with a reason, which would have been a legal answer. The detail is byte-identical to the round before and so is this criterion, word for word: 'Every token that is still backlogged names the decision it waits on, on a line beginning WAITS ON, so nothing sits there because nobody looked at it.' THE FINDING STANDS, and the measurement has moved the wrong way. Re-read every note in doc/work as I write this: 13 tokens are backlogged and 1 carries a WAITS ON line. It was 10 and 0 an hour ago. The three new ones are lesson tokens minted by this reviewer under the engine's own rule -- a rejection whose lesson names no token is refused, and the refusal says 'mint a token for the lesson with se work, backlogged or open as you judge'. Each carries a named defect, a reproduction, a remedy and a red check. None of them waits on a decision, so the only ways to make this criterion green are to write a false sentence on them or to move them out of the backlog to satisfy a test. AND THE WORD IS STILL THE ENGINE'S. se work --help says --backlog means 'mint it backlogged: visible, and not work anybody is doing'. This criterion makes it mean 'waiting on a decision'. The detail is looser than the criterion and still is: it says the ones that turn out to be decisions are left with a sentence, which allows a backlogged token that is not one. The criterion forbids it, and the criterion is what the work is judged against.

**satisfies:** Unchanged from the last round, because nothing has been done to it. SAY WHICH IT IS, A CLEARING OR A RULE. If it is the clearing the detail describes, pin it to what exists when the work starts -- the ids, or a list the work writes down first -- and assert that each of THOSE is closed, opened, or backlogged with its reason. Then the token can be finished and a token minted tomorrow does not turn it red. If it is meant as a standing rule, say so in the detail, say what it means for every future backlogged token, and answer for the kind that is not waiting on anything: a lesson with a remedy and a red check, of which there are now four. Either carve that shape out by name, or say a lesson token is minted open rather than backlogged and change the refusal's wording to match, since it offers both today. AND RUN IT BEFORE AGREEING IT. Point the rule at the 13 backlogged tokens that exist and put the count in the draft. Thirteen red is a clearing waiting to happen. Thirteen red forever is a rule nobody can keep, and the draft cannot tell which it is asking for until somebody counts.

## finding 5 · round 2 · done when: Nothing that was backlogged is left in a state nobody can act on: every one is closed, or open with somebody holding it, or backlogged with its reason. · by reviewer4

**wrong:** ALSO UNTOUCHED AND UNMENTIONED, and byte-identical: 'Nothing that was backlogged is left in a state nobody can act on: every one is closed, or open with somebody holding it, or backlogged with its reason.' There is no open token with a holder in this engine, and I searched every assignment to Holder in src/engine again against the tree as it stands: every write that sets Open sets the holder empty in the same statement -- pull.go:347, pull.go:456, pull.go:717, arrival.go:177, all t.Status, t.Holder = Open, "" -- and the only writes that set a holder set InWork (pull.go:736), InReview (pull.go:666) or SpecInReview (pull.go:656) with it. A token somebody is working on is in_work. So a test written to this sentence can only be made green by reading 'open' as something the word does not mean here, and that reading is a decision the draft should make rather than the worker at the keyboard and the reviewer afterwards.

**satisfies:** Unchanged. NAME THE STATES THE ENGINE HAS. The ones that mean somebody can act are closed; in_work with a holder; and backlogged with its reason. If open with nobody holding it is also fine -- it is, it is the ordinary waiting-to-be-picked-up state and the queue hands it out -- then say so and drop the holder clause. Write the criterion against the status names the engine writes, so the command that reads them can fail when a token is left in a state the list does not name.

## finding 6 · round 3 · done when: Every token that is backlogged is named in this token's evidence, so none was passed over without being read / runs: rg -q func.TestEveryBacklogItemWasRead src/engine && go test -C src/engine -count=1 -run TestEveryBacklogItemWasRead$ . · by reviewer4

**wrong:** STILL A CHECK OVER LIVE DATA, ONE STEP FURTHER OUT. The criterion is much better than it was: the WAITS ON demand is gone, so a backlogged token no longer has to claim it is waiting on a decision, and that half of the last finding is closed. What is left is the half that makes the criterion outlive its token. 'Every token that is backlogged is named in this token's evidence' is a comparison between two things, one of which goes on changing after the work is finished. The evidence is written once. The backlog is not. THE TEST LIVES IN src/engine, so it runs on every go test ./... and therefore inside sh .se/scratchpad/battery.sh, which is what every submission in this project cites as its evidence. The first backlogged token minted after this one closes is not named in wk-61af3a054e's evidence, so the check goes red, and it goes red for somebody who did nothing wrong. What happens then is that whoever is holding the next token deletes or weakens this check to get their battery green, which is worse than not having written it. MEASURED, RATHER THAN PREDICTED, on the two facts the argument rests on. The backlog moves during the life of one token: I counted 10 backlogged notes in doc/work when I first read this draft and 13 an hour later, and 7 of the 13 were minted today by me alone, because the engine refuses a rejection whose lesson names no token and its own refusal text says to mint one 'backlogged or open as you judge'. And the battery really does run the engine's whole test package: battery.sh line for go test is go test -C src/engine -count=1 ./..., with no filter. So the population this criterion compares against grows every time any reviewer rejects anything, and the comparison is made again on every submission. WHAT IS CLOSED, and I checked it rather than taking it. Criterion 2 now names its own test, TestTheBacklogCheckRefusesAnEmptyBacklog, so the anti-vacuity claim no longer shares a command with the thing it is guarding: finding 1 from round 1 is closed. The criterion that allowed 'open with somebody holding it' is gone, which is the right answer to round 1's finding 3, since the engine writes no such state. And criterion 4 now asks for every test to have been watched failing on its own assertion, which is the sentence this queue keeps having to add. python .se/scratchpad/run-criteria.py doc/work/wk-61af3a054e.md answers 2 red, 0 green: both commands are red, and both are red because the function is absent, which is what a draft before its work looks like.

**satisfies:** COMPARE THE EVIDENCE WITH ITSELF, NOT WITH A LIST THAT KEEPS GROWING. The work reads the backlog once and writes down what it found; the check should read what was written down and verify it, so nothing minted afterwards can make it false. Concretely: have the evidence carry the ids it read, and have TestEveryBacklogItemWasRead require that each id it names still exists and is either closed or backlogged with a reason on its note. That is a check about this token's own work, it can fail -- name an id and leave it untouched and it goes red -- and a token minted tomorrow is simply not in the list. IF YOU WANT THE STANDING GUARANTEE AS WELL, that nothing sits in the backlog unread, that is a different token and a different mechanism: it belongs to the engine, which knows when a token was last touched, not to a test comparing a folder against one note. Say so in the detail and mint it, rather than trying to get it out of this criterion. AND BEFORE AGREEING THE DRAFT, RUN THE COMPARISON YOU ARE ABOUT TO SPECIFY against the backlog that exists -- 13 notes as I write this -- and put the count in the draft, so the next reader can see what the check will be looking at rather than having to imagine it.

## lesson 1 · round 1 · by reviewer4

**the class:** A criterion written as a standing test over the project's own live data rather than over the change. Every one of these four is a fact about doc/work as it stands, so the moment the work closes they go on being checked against tokens nobody had in mind when the draft was written. A one-time clearing becomes a permanent rule, a word the engine already defines gets quietly narrowed, and the criterion stops being something this token can finish. And because the criteria are red today for being absent rather than for being false, none of them was ever asked what the data says: run against the ten backlogged tokens that exist, the first one is ten out of ten red, five of them for tokens that are not waiting on anything at all.

**instead:** When a criterion asserts something about the project's own records rather than about the diff, decide out loud whether it is a one-time assertion or a rule for all future work, and write that decision into the draft. A one-time assertion gets pinned to what exists -- the ids, or a list the work writes down first -- so the token can be finished. A rule gets stated as a rule, with what it means for every future item of that kind, and with a carve-out for the kinds it was not written for. Either way, run it against the data that exists before agreeing it, and put the count in the draft: a criterion nobody has pointed at the real records is a guess about them. And when a criterion narrows a word the tool already defines, quote the tool's own definition beside it, because the disagreement is invisible until somebody hits it.

**minted as:** wk-967b04b877

## lesson 2 · round 2 · by reviewer4

**the class:** A round that answers the finding cheapest to fix and is silent about the rest. Finding 1 was one line in the draft and it was closed properly. Findings 2 and 3 needed a decision, and they came back with the detail and both criteria byte-identical and no sentence anywhere saying they had been considered. Declining a finding with a reason is a legal answer here; saying nothing is not, because a submission that answers some findings reads exactly like one that answers all of them. The evidence names what was done and nothing names what was not, so the gap is invisible until the reviewer re-runs its own reproduction. This is the second time in two rounds today: wk-bb34ab1208 closed the pinned-rows finding and said nothing at all about the class-name one.

**instead:** Answer every finding by name, including the ones you are not fixing. One section per finding, headed with its number, and its body is either closed, with what proves it, or not taken, with why and what owes it. Before submitting, list the last round's findings, tick each one against a section, and re-run the reviewer's own reproduction wherever it named one, because the reviewer will. When a finding asks for a decision rather than an edit, that is the one most likely to be skipped and the one most expensive to skip: the edit is a line, the decision is the round. And if this keeps happening, it belongs in the engine rather than in a habit -- it already knows what findings a token carries, so it can refuse a submission whose evidence answers fewer of them than are open.

**minted as:** wk-be358bd3f3

## lesson 3 · round 3 · by reviewer4

**the class:** The same class as the last round's lesson, wk-967b04b877, one step further out, which is why it points at the same token. A criterion that compares something the work writes once against something the project keeps changing is a criterion that stops being about the work. The first shape demanded a sentence on every backlogged token; that was dropped, and what replaced it still reads the live backlog and still turns red when somebody mints a token months later. The tell is that the criterion has two sides and only one of them is frozen when the token closes.

**instead:** When a criterion compares two things, ask of each side whether it stops changing when the work is done. If one side keeps moving, the criterion is a standing rule wearing a criterion's clothes, and it belongs somewhere that can maintain it -- the engine, a linter, a token of its own -- rather than in a test that ships inside the suite every future submission has to keep green. Freeze the moving side into the work's own output and check that instead: the list the work wrote down, not the folder it read it from. And when a check will live in the shared suite, ask who goes red when it fails: if the answer is somebody who did nothing wrong, they will delete it, and a deleted check is worse than one never written.

**minted as:** wk-967b04b877

