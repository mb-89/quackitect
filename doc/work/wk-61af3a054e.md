---
id: wk-61af3a054e
seq: "-8"
type: work
title: the backlog is cleared
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "4"
minted_by: person
evidence:
  - outcome
---

## detail

Owner's decision: go into the backlog and do everything that needs no judgment. A token that waits on a decision is left with a sentence saying which decision. The snapshot judged against, fixed when the work started, is inside this token so the check stops changing when the token closes. Related: wk-097b2cac17, wk-10d3cf13cd, wk-12b43e820b, wk-24eeb4f3d4, wk-35e11bbe73, wk-4349bc412e, wk-4a1ee98339, wk-4b67d7126a, wk-4d0912e39c, wk-4e8eeb76aa, wk-50805e98a1, wk-5544578d91, wk-70df338d58, wk-74558c90da, wk-7f4983c759, wk-967b04b877, wk-be358bd3f3, wk-ccd563ec25, wk-d1b585b7b9, wk-dcb697c1b1, wk-de69604cfd, wk-f4731bc18b.

## done when

- Every id on the list above is named in this token's detail beside what happened to it
  `rg -q func.TestEveryBacklogItemWasRead src/engine && go test -C src/engine -count=1 -run TestEveryBacklogItemWasRead$ .`
- That check refuses when the list is empty or the token cannot be read
  `rg -q func.TestTheBacklogCheckRefusesAnEmptyList src/engine && go test -C src/engine -count=1 -run TestTheBacklogCheckRefusesAnEmptyList$ .`
- For each one the evidence says what happened: done, or left backlogged with a reason about that token
- Every test named above was watched failing on its own assertion before it was watched passing

## evidence: outcome

Of the 22 on the snapshot, counted by .se/scratchpad/count-verdicts.py: 10 aborted, 1 settled as a duplicate, 11 still backlogged. Six aborts name the line proving the remedy landed. Four lessons went into doc/guidance/specifying.md under The command decides the sentence above it, and wk-4349bc412e duplicates wk-4b67d7126a. TestEveryBacklogItemWasRead in src/engine/backlogread_test.go answers 22 ids on the list, 0 passed over, and TestTheBacklogCheckRefusesAnEmptyList was watched red twice. Reds are in .se/scratchpad/observed-red-backlog.txt.
