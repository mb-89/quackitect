---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: god comment misreads unbound
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/reviewer-wren
claimed_at: "2026-09-06T16:30:47Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 94e68c976c6fcd5c260112aea308a8fd737e069a
  - eb7255f876d750f9f1b7fc7a2bf4037615780b8e
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 2a53ca3157da4080ce6b6295e9b5b5ecfacc1ad6
---

## detail

A finding on wk-85d5b0ec27, god silences the stop.

src/engine/hook.go:1423-1424, the last paragraph of the god check's header, says:

  // UNBOUND IS UNTOUCHED. It keeps the claim and the argument, and only the
  // queue stops being a reason to refuse a stop.

Neither half is true in the tree it landed in. Fifteen lines below it, at hook.go:1446, the unbound branch grants the stop outright with no claim standing, and TestGodSilencesTheStopHook asserts exactly that ("an unbound agent was asked for a reason by a queue that chose it nothing"). The argument is gone from the whole file: src/engine/challenge.go was deleted in the same commit, 95b9423.

The two paragraphs were written apart and landed together, so decideStop now opens with a comment that its own next comment contradicts: "THIS IS NOT GOD. God skips the whole hook above." A reader consulting the file for what each rung does reads the wrong answer at the first place they look, which is the one thing a comment this long exists to prevent.

The sentence was true of the token that wrote it. It is the tree that moved under it.

## proposed action

Rewrite the closing paragraph of the god header at hook.go:1423-1424. Say what now separates the two rungs: both skip the claim, and god skips the hook while unbound is recorded as an ordinary stop. Or drop the paragraph, since the unbound comment below already says it. No code changes.

## done when

- the god header at src/engine/hook.go says nothing about unbound that the unbound branch below it contradicts, decided by: reading the two comment blocks in decideStop against each other
- no claim survives in the file that the argument still stands, decided by: se find --regex 'the argument' over src/engine/hook.go
- the stop tests still pass, decided by: se test naming TestGodSilencesTheStopHook and TestAStopWithNoClaimIsRefusedHoweverOftenItIsAsked

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A reader of decideStop gets the right answer where they look first. | The god header stops bounding a rung it no longer describes. |
| [x] | what breaks if it is never done, and not only that it stays undone | The header contradicts its own next comment fifteen lines down. | It says unbound keeps the claim; the branch grants the stop with none standing. |
| [x] | the ask is small enough to review whole, or it is split first | One comment paragraph, dropped. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | One by reading the two blocks against each other, one by se find, one by se test. | se find --regex 'the argument' --path src/engine/hook.go |
| [x] | the basics it stands on exist, or are minted first | The header, the unbound branch and both named tests were already in the file. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token, read. |  |
| [ ] | one test was written first and seen red for the reason expected | Not met. A dropped comment cannot redden an assertion. The two tests the token names guard the behaviour instead. |  |
| [x] | the same test was seen green after the change, and named | TestGodSilencesTheStopHook and TestAStopWithNoClaimIsRefusedHoweverOftenItIsAsked, both ok, se test on this token answering ok. se find --regex 'the argument' over hook.go answers none. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | On the tip inside 171ab25, which another hand landed from this shared tree while holding hook.go. | land.sh answered NOTHING TO LAND. |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | None. The argument block the detail also names was rewritten by that other hand in 62e6aea and 171ab25, not by this token. |  |
