---
id: wk-0146942977
seq: "-29"
type: work
title: half a mechanism ships
status: imp_done
assignee: main
scope: single-step
traced: true
disposition: done
rounds: 1
minted_by: reviewer8
evidence:
  - outcome
---

## detail

A detail names two halves, the producing half gets built and evidenced, and the auditing half is built nowhere. Found on wk-7f0b46d99f round 2, which owns the mechanism. Write the class into doc/guidance/specifying.md under The command decides the sentence above it and doc/guidance/behaviour.md under Evidence. Put both halves in the evidence even when one answer is nothing yet.

## done when

- The class is in specifying.md under that heading, and the check reads that section only.
  `rg -q func.TestTheHalfMechanismRuleIsWhereItSays src/engine && go test -C src/engine -count=1 -run TestTheHalfMechanismRuleIsWhereItSays$ .`
- It is also in behaviour.md under Evidence, subsections included, read as that section only.
  `rg -q func.TestTheHalfMechanismRuleIsAlsoWhereItProjects src/engine && go test -C src/engine -count=1 -run TestTheHalfMechanismRuleIsAlsoWhereItProjects$ .`
- Both instructions are in both sections, failing by the file that is short.
  `rg -q func.TestTheHalfMechanismRuleSaysWhatToDoInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheHalfMechanismRuleSaysWhatToDoInBothFiles$ .`
- Both sections say silence is the answer that fails and nothing yet is an answer.
  `rg -q func.TestTheHalfMechanismRuleNamesTheAnswerThatFailsInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheHalfMechanismRuleNamesTheAnswerThatFailsInBothFiles$ .`
- Both sections carry the measurement: the token, built nowhere, the two failed observations.
  `rg -q func.TestTheHalfMechanismRuleCarriesItsMeasurementInBothFiles src/engine && go test -C src/engine -count=1 -run TestTheHalfMechanismRuleCarriesItsMeasurementInBothFiles$ .`
- The rule reaches every projection of behaviour.md in util/projections.json, refusing when none.
  `rg -q func.TestTheHalfMechanismRuleReachesEveryProjection src/engine && go test -C src/engine -count=1 -run TestTheHalfMechanismRuleReachesEveryProjection$ .`
- Every test above was watched red with the change absent, once per file, naming the test, the cut and the message.

## evidence: outcome

The class is in both guidance sections and reaches AGENTS.md. The six tests named above were each watched red by cutting the sentence from one file at a time.
