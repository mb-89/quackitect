---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: criteria name refused commands
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
claimed_by: 7a7c633a/main
claimed_at: "2026-09-07T14:11:31Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 9884497f5fc1633bf8acebe2b7527f12d7ac046c
---

## detail

Found reviewing wk-5bba8e497a.

doc/work/wk-5bba8e497a.md:45 says "Decided by: go test -C src/engine -run TestAParkedTokenLeavesTheQueueAndIsNamed -count=1 ./". Run in an agent lane on 2026-09-06 it is refused outright: "THE ENGINE OWNS THE TESTS, AND IT DECIDES WHAT RUNS", pointing at se test. So the criterion names a command no agent that has to decide it may run, and the reviewer decides it by translating the command instead.

That is how a verdict comes to record a run that never happened. The verdict already on this note says "I ran the test. It answered ok, 2.851s" under a go test heading, which is not a call this lane can make.

It is a class, not one line. doc/work/wk-191014c633.md:30 names a bare go test too, while wk-391ac2bb07, wk-fa4edbe5aa and wk-faeaddd8a7 name se test --propose and wk-43db3cdcbc names ./RUNME.sh test. Nothing catches the difference: no check under util/checks reads a done-when line.

The fix is two parts. Rewrite the two criteria that name a bare go test as the se test call that decides them. Then add a check that refuses a done-when line whose command the guards refuse, which today is go test, rg and grep over the tree.

The regex this token first named as its own decider cannot reach zero. It matches this note's detail and two of its criteria. It also matches wk-191014c633, which is closed, and a closed note is history whose criteria were decided under the guards of their day. So the criterion names the check instead.

## proposed action

A criterion names a command the guards let an agent run, and a check refuses one that does not.

## done when

- no open note under doc/work names a bare go test in a done-when line. Decided by: se test --propose criteria-name-a-runnable-command answers ok
- a check refuses a done-when line naming a command the guards refuse. Decided by running that check over the tree, where it exits zero
- the check names the file, the line and the command it refused. Decided by running it on a note edited to carry 'Decided by: go test ./...', where it fails and names all three

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A decider runs the command written under the criterion. Today they translate it into one the lane allows, and a translated run is one nobody can repeat. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | A verdict records a run that never happened. wk-5bba8e497a's says it ran the test and got ok in 2.851s, under a go test heading no lane can call. |  |
| [x] | the ask is small enough to review whole, or it is split first | — |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Criterion one was not. Its regex matches this note's own detail and two of its criteria. It also matches wk-191014c633, which is closed. It now names the check. |  |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — |  |
| [x] | one test was written first and seen red for the reason expected | criteria-name-a-runnable-command answered 41 failed over the tree. Measured: grep -c engine-args behind a pipe answers 2 through se run, and the check called it refused. |  |
| [x] | the same test was seen green after the change, and named | It answers ok now: 213 open notes read, 442 commands judged, 0 failed. On a probe note reading go test ./..., it exits 1 naming the file, the line and the command. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | 9884497f is no object here, so the change reads against HEAD: cf5dee77 and 25a4a699. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The check's own path rule was the cleanup, and it is in the change. wk-191014c633 is closed and keeps its go test. |  |

