---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: refusal names shell only
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/reviewer-ligeti-two
claimed_at: "2026-09-05T14:55:09Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 6364d0e3bae7389f70922b930776143e60aea6e2
  - d9d6f8d0d5a06d65b9bb94547f95acc0c7e49817
  - 2e24dd3ba94795005fc4d0726b091a5b5c5d0a9e
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - c6fa35b86694aae03fea747e8c5adc4131b91594
  - 9f8005fd9b4f547d60f92cc5a5e128d209562a46
---

## detail

A finding on wk-239cae9216, "a claim gates tracked".

src/engine/claim.go:234 refuses an unclaimed tracked token with "Claim it and take it in one call: se claim --these <id> --take". That spelling exists only at the shell. src/mcp/lane.go:150, claimArgs, carries actor, next, these, as, release, list and whoami, and no take. So an agent on the tool lane reads a refusal naming a door it cannot open.

The damage is measured, not guessed. On this box a reviewer was refused three times today with that sentence, and each time paid two calls, se_claim and then the verb again, because the one call the refusal names is not callable from the lane.

The check that catches the class: a test that every refusal naming an engine verb names one both doors carry, or claimArgs gaining Take so the sentence is true on both.

## done when

- claimArgs in src/mcp/lane.go carries take, and se_claim {these, take} claims and takes up in one call
- a test drives the lane path: cd src/engine && go test -run TestAClaimCanTakeTheTokenUp -count=1 . still passes, and a new case covers the lane spelling

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One field on the request, one flag on the call it builds, one entry in the cold door, and one test file. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | The second names its command, and it was run here. The first is read off the same run, through the two lane tests by name, TestTheLaneOffersTheTakeTheRefusalNames and TestTheLaneCallCarriesTake. |  |
| [x] | the basics it stands on exist, or are minted first | The verb already carries take. claimverb.go defines the flag and TestAClaimCanTakeTheTokenUp drives it, so only the lane could not spell what the refusal names. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 13 held both halves, the door that offers take and the call that carries it. |  |
| [x] | one test was written first and seen red for the reason expected | TestTheLaneOffersTheTakeTheRefusalNames said `se_claim does not offer take, and the refusal an agent reads names it`, and the same of the cold door. Expected, as claimArgs had no such field. |  |
| [x] | the same test was seen green after the change, and named | It passes with TestTheLaneCallCarriesTake, which reads the call the lane builds. TestAClaimCanTakeTheTokenUp still passes, and so does all of src/mcp. |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/mcp/lane.go for the field and claimArgv, one entry in util/cage/tools.json, and the new test. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Token wk-0016f540ed. The cold door is behind on needs_human too, which this token did not sweep in. |  |

