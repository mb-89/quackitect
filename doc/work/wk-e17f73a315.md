---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: receipts name another commit
# where the token stands. The process owns these values.
status: open
---

## detail

Every receipt cell in steps 1 and 2 of doc/work/wk-1c77b9a6fb.md says be90e0e9. That commit belongs to wk-2f51c2156a and touches no file under src/mcp.

git show --name-only be90e0e9 lists doc/work/wk-2f51c2156a.md, two minted tokens, src/engine/evidence.go and src/engine/sessiontakesmainback_test.go. A reader following the receipt lands in another token's work.

The receipt column exists so a reader checks a claim without asking the author. Ten cells here send that reader somewhere else. The change itself is sound and sits uncommitted in the working tree, which is why no commit receipt exists.

Found reviewing wk-1c77b9a6fb.

## proposed action

Write a receipt a reader can follow. A file or a count serves where no commit carries the work, so src/mcp/claimtakes_test.go beats a hash from another token.

A check holds the class. For every receipt cell shaped like a git hash, ask that the commit touches a file this token also touches. Where the token names no commit of its own, refuse the hash.

## done when

- no receipt cell in doc/work/wk-1c77b9a6fb.md names be90e0e9, decided by: se find --regex be90e0e9 --path doc/work/wk-1c77b9a6fb.md answers nothing
- every receipt on that token names a file, a count or a command a reader follows to the change under review

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

