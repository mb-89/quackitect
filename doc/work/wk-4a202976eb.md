---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: bugs stay in bucket
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-06T17:13:50Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 380e186131dad7c88718359eb6c33c8525bd87ac
  - f2d8149023064834078f210841d46a23ba5ef24a
  - d2982114167e7352be0c5d62677e787238f36c93
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 29860ad448506a469f0ec3faaa189a40c905755f
  - 909aa027c99c5be2d11c77e05c833591f52615bd
---

## detail

An agent that finds a small bug while working a bucket has no written permission to fix it, so it files the bug somewhere tidy and moves on. The next hand then rediscovers everything the finder already knew.

THE OWNER'S RULING, September 2026, in their words. A bug fix created while working on a certain bucket goes into the same bucket. If you find a bug fix and it makes sense to fix it early, you may put it in the bucket you are working and pull it instantly and fix it first. That is allowed, and it is preferred over putting it somewhere else for somebody else to do. If the bug is big and non-trivial, do not.

WHAT IT SETTLES. Two questions an agent asks and the guidance answers neither. Which bucket does a token minted mid-work carry, and may the finder drop what it holds to work it now.

WHY THE BUCKET MATTERS HERE. A person narrows the queue to one bucket and an agent drains it. A token minted with no bucket is invisible to that queue, so work found inside the bucket falls out of it. That was measured on this box: three tokens minted while draining claims could not be handed back to the hand that wrote them.

MEASURED THE OTHER WAY TOO. The pull's notice bug was found while proving an unrelated fix, and fixing it took one edit. Filed for another hand it would have cost that hand the whole reading first.

## proposed action

A section in the work-token guidance, where an agent already reads about minting. Four sentences.

A bug found while working a bucket is filed into that bucket, not a tidier one. The finder may put down what it holds, pull the new token at once, and fix it first. That is preferred over leaving it for a hand that has to rediscover it. The bound is triviality: a fix you can make right away, and a big or non-trivial one is filed and left.

It goes in the guidance rather than in a card, because a card is read once at session start and this is needed mid-work.

## done when

- doc/guidance carries the ruling in the chapter an agent reads before minting a token, and the words bucket and preferred both appear in it
- the projection is rerun so the standing rules an agent is handed carry the new lines: the projected output-style file contains the sentence
- the guidance lint answers no finding on either file the rule touches: se lint names neither work-token.md nor backlog.md
- the projected output style carries a Backlog chapter, and the rule is in it

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The finder fixes what it found, holding the reading already. The bucket keeps the work a person is draining. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | Work found inside a bucket falls out of it. Three tokens minted here could not be handed back to the hand that wrote them. |  |
| [x] | the ask is small enough to review whole, or it is split first | One actionable and one discussion section, and then the split the owner asked for. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | Two by se find over the guidance and the projected file. One by se test on this token. |  |
| [x] | the basics it stands on exist, or are minted first | The guidance chapter and the projection both existed. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token, read, and this token adds a line to it. |  |
| [ ] | one test was written first and seen red for the reason expected | Not met, and no test belongs here. The change is prose in the guidance. The checks that read it are the ones named below. |  |
| [x] | the same test was seen green after the change, and named | deleted-notes-have-a-row and notes-say-each-heading-once are green. open-tokens-carry-their-sections is red on 12 older tokens, none of them this one, and it is filed. | Re-read at submission: se lint names neither work-token.md nor backlog.md, and se -project rewrites neither projected file, so the four done-when lines all answer. |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Landed as 6f3fc61, then eb663f0 for the split into work-token.md and backlog.md. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Two. se_work cannot set a bucket, so every token needed a hand edit of its front matter. And driving-the-engine.md carries the same crowding, filed as wk-b4cf2f938e. |  |

