---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: second build answer unattributed
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-aalto
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 6beaa5ad2a2d4b211f57260938d692b930d02918
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - a0c7b26f3539cac571d1ea6a74e33db03c54a06d
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: runChosen now keeps what the build said beside the binary it could not make, and hands it to every later test in that folder. The red and the green were watched on a detached worktree of the branch tip, because neither this clone nor the tip builds the package as it stands. It is on v4 as 9ea3b43c.
# what it became. They have to exist.
successors:
  - "[[wk-5f8dda7870]]"
---

## detail

A finding from the verdict on wk-7c41d9e206, which made a build failure answer as a build and name the hand that wrote the breaking file.

Only the first chosen test in a package gets that name. In src/engine/tests.go, runChosen remembers a failed package as an empty binary in bins, and every later test in the same folder is answered by aBuildFailure with an empty said. With no compiler output, filesBuildNamed in src/engine/nored.go finds no file, so theHandsThatBrokeIt answers that the record cannot say whose write it was.

Seen at the door on this box. Two tests proposed over one broken package: the first answer named src/engine/snapshotnevertravelled_test.go and the token that wrote it, the second named no file and no hand. Both were refused as reds, so the rule itself holds and only the attribution is lost.

The fix is to carry the first build error for the package beside the empty binary, and to hand it to every later test in that folder.

## done when

- every chosen test over one broken package is answered with the file and the token the build error names, decided by: propose two tests in the broken package and read both answers
- a test drives two chosen tests over one broken package and asserts the hand is named in both, decided by: se test on the new test

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A hand handed the second answer is told which file broke the build, and whose write it was. | the new test |
| [x] | what breaks if it is never done, and not only that it stays undone | It hunts a break through a tree many hands share, already named in the answer above it. | the old answer |
| [x] | the ask is small enough to review whole, or it is split first | — | ten lines and one test |
| [x] | every done-when line is decidable, and names the command where one decides it | The first reads two answers. The second names the test that drives both. | the go test run |
| [x] | the basics it stands on exist, or are minted first | — | aBuildFailure, and the fixture |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Rule twelve. The red is an assertion on the second answer, and not a build failure. | work-token |
| [x] | one test was written first and seen red for the reason expected | TestEveryTestOverABrokenPackageNamesTheHand failed on TestB, which named no file and no hand. | nored_test.go |
| [x] | the same test was seen green after the change, and named | It passes, and TestABuildFailureIsNoRedAndNamesTheHand beside it still passes. | the same command |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | tests.go and nored_test.go |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-5f8dda7870 carries the tip failing to build, which is why the red needed a file set aside. | minted |

## evidence: where this stands

THE RED AND THE GREEN WERE WATCHED ON A DETACHED WORKTREE OF THE BRANCH TIP.

se test could not decide either criterion on this box. The clone does not build the package, which is wk-a2826160cc, and the tip does not either, which is wk-5f8dda7870. So the door the second criterion names was shut.

With sessiontakesmainback_test.go set aside, the tip built and the red was watched on an assertion. The second answer, over TestB, named no file and no hand. After the change both answers name half.go and the token that wrote it.

The whole package was run twice, once at the tip and once with this change. Both answered the same eight failures, so this change introduced none. The file that was set aside is back, and no commit carries its removal.

