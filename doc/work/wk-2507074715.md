---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: nothing runs race detector
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/main
claimed_at: "2026-09-05T16:17:46Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 5ea4c6149f08d6b5b3159905094bed3f3c23cd8c
  - 9b507c143a6e3a038821eac42cd6e38f087d39d1
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - f7b806f97804ea6b4677d1dcd20df932e1621d56
---

## detail

A finding on wk-4ad15a5c86, engine dies under load.

That token exists because concurrent calls from several agents were dropped, and its change is concurrency: src/engine/tests.go:228 runOrLand starts two goroutines and hands one a shallow copy of the answer, and src/engine/engineload_test.go:41 fires twenty calls from four goroutines beside a writer flapping the running record.

Nothing in this tree runs the race detector. se find --regex "-race" over src/** and util/** answers nothing, so no lane, no check and no battery step passes it.

THE DAMAGE. The one class of defect this token was minted against is the class go test -race catches, and the tree has no instrument for it. A future change to runOrLand, to the model's fields or to theLoad counters lands with the same tests green and no reader any wiser. Run by hand here, go test -C src/engine -race -run 'ConcurrentCallsAreAllAnswered|ALongTestAnswersWhereItLands' . answered ok in 7.317s, so this is a missing guard and not a live defect.

THE CHECK. A step in util/checks that runs the engine's Go tests once under -race, or the go lane running it, catches the class.

## proposed action

Run the engine's Go tests under -race once, either as a check in util/checks that the battery carries or as a flag the go lane passes. Decide where it sits by what the battery's wall clock can carry, since -race costs about a factor of three here.

## done when

- the engine's Go tests are run under the race detector by something the tree runs on its own, decided by: se find --regex "-race" --path "util/**" answers at least one line
- that run is green on today's tree, decided by: the new check answers ok on src/engine with no race report in its output
- a deliberate race in a scratch test is seen caught by it before the change lands

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One function and one start line in battery.sh. | 2 hunks |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one runs se find. Line two runs the lane. Line three plants a race and watches it caught. | 3 of 3 |
| [x] | the basics it stands on exist, or are minted first | The battery already builds a test binary once and runs lanes in parallel. This is the same shape. | build, start |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | A check that cannot go red is the finding, so the detector was shown catching a planted race first. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | A copy of src/engine in /tmp gained a test whose two goroutines increment one int. Under -race it answered WARNING: DATA RACE and exited 1. A copy, so the tree kept no planted defect. | 1 race, exit 1 |
| [x] | the same test was seen green after the change, and named | engine_race over the real tree answered exit 0 and reported no race. | exit 0 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Two hunks in util/checks/battery.sh. | 1 file |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Five tests fail under race for reasons of their own, none about concurrency. The lane judges races. | 5 pre-existing |

