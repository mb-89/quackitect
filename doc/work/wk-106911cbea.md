---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: glyph check misses filter
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-alvar
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 051f6fa6eb76dde68c2636bc62ea743f013d2e1f
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 1c1579904ce372986f8852d6c90f12f3dc3f8acf
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

A finding from the verdict on wk-db6bdc8872, which moved the log syntax reader out of src/viewer into the new module src/filter.

util/checks/no-loose-glyphs.mjs holds a hand-written list of folders where a mark may be drawn, at line 34. It names src/extension and src/viewer. It does not name src/filter.

What is gained by doing it: the check sees the four hundred lines that moved, including FilterHelp, which is help text printed to a terminal.

What breaks if it is never done: a loose glyph written into the reader is drawn on a person's screen and no check says so. The note on wk-db6bdc8872 says the only hand-written list was the battery gofmt line, and this is a second one.

The same class was also seen at util/checks/battery.sh line 378, which runs the viewer suite and knows no filter module, and util/checks/tests-name-no-token.mjs line 25, which walks three named folders.

The smallest answer is to add src/filter to the list. The answer that closes the class is to walk the tree for a go.mod, which is what the note already claims every reader of the modules does.

## done when

- util/checks/no-loose-glyphs.mjs covers src/filter, decided by: read the DRAWS list in that file
- the check answers green over the tree with the reader in src/filter, decided by: run the no-loose-glyphs check

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | The check reads the four hundred lines that moved out of the viewer. | measured |
| [x] | what breaks if it is never done, and not only that it stays undone | A mark drawn in the reader reaches a screen and no check says so. | measured |
| [x] | the ask is small enough to review whole, or it is split first | One line on one list, and the wider class is wk-5feb2d8738. | one line |
| [x] | every done-when line is decidable, and names the command where one decides it | One is read from the list, the other is the check run. | both run |
| [x] | the basics it stands on exist, or are minted first | src/filter and the check are both on the branch tip. | read |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | The work-token guidance came with the pull and shaped this answer. | read |
| [x] | one test was written first and seen red for the reason expected | A gear in src/filter/filter.go went unseen, thirteen files read and none failed. | red |
| [x] | the same test was seen green after the change, and named | The check then failed on that file and line, and greened once the gear came out. | green |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | Landed at the branch tip as commit a090ec5f, one file. | a090ec5f |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The other two hand-written lists are wk-5feb2d8738. | wk-5feb2d8738 |

