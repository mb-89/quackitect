---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: RUNME.sh carries CR
# where the token stands. The process owns these values.
status: closed
author: worker-mira
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 1a39e5f7fb3191c2dc396a6ccc253b6e89efddd1
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - de7138fee0b10a1154107647f20b52a92749981f
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

util/checks/scripts-are-lf fails: "RUNME.sh is LF ... it carries a carriage return at byte 86, and sh reads that as part of the line, so the script dies on Linux". Byte 86 ends line 2, which reads "# planted for the battery proof: this line ends in a carriage return". git diff -U0 -- RUNME.sh shows that line as an uncommitted hunk at line 2, planted on purpose by earlier battery work. The check has been red since it was planted, and no session since has answered it green.

A finding from wk-faeaddd8a7, which edits RUNME.sh further down the same file and is otherwise unrelated to it. Counting every byte equal to 13 in RUNME.sh answers 1, at byte 86, so nothing else in the file carries one and that single planted line is the whole failure.

Either the proof it was planted for has been taken and the line goes. Or the proof still wants it, and scripts-are-lf is told this one line is planted. Which of the two is not decidable from the line itself, so whoever planted it decides.

## done when

- util/checks/scripts-are-lf answers ok for RUNME.sh, through se test
- counting bytes equal to 13 in RUNME.sh answers 0, or scripts-are-lf.mjs names the planted line as allowed and says why

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One line of RUNME.sh removed, nothing else. | whole |
| [x] | every done-when line is decidable, and names the command where one decides it | Both name one. Line 1 is se_test propose scripts-are-lf. Line 2 is a count of bytes equal to 13 in RUNME.sh. Both were run from the root before and after, red then green. | se_test, byte count |
| [x] | the basics it stands on exist, or are minted first | The check and the named-test door both exist, and nothing was minted. The one basic the token called missing, the planter's judgement, was found written down rather than asked for: the finding below carries the reading. | scripts-are-lf.mjs |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | doc/guidance/work-token.md read whole. Rule 12, red first, is the row below. Rule 14 is why each row quotes the check and re-runs both commands. | work-token.md |
| [x] | one test was written first and seen red for the reason expected | No new test was written. The existing check was seen red before the change, for the defect. | se_test, ok:false |
| [x] | the same test was seen green after the change, and named | The same check, unedited, answered ok:true after the change. | se_test, ok:true |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | Not true of this tree, and saying so is honest. My change is one line. | reviewer: read RUNME.sh line 2 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | Nothing owed. All six scripts judged pass and RUNME.sh carries no carriage return. | none |

## evidence: the finding

The line goes, decided from the record not guessed. HEAD counts 0 CRs, so the plant was uncommitted only. .gitattributes pins *.sh text eol=lf and git warns CRLF will be replaced next time it touches the file, so the tree was already configured to erase it. An allowlist is refused by the check's own header, "this fails the battery the moment one carries a CR anyway, however it got there". And this tree plants in a copy and undoes after: testing.md ch.8, wk-86a1c09e11, wk-6868ca3074, wk-b60c6912a1, and wk-4700fc5ccd's done-when reads "planting undone after" and is closed done. The proof was taken. The undo was missed.

## evidence: the restore and the step 1 summary

Restored under wk-aa0e2ea529 from .se/undo/20260903-204304.962691000.json, the pre-write copy of the last se_apply before the close. One later apply is not in the journal, which keeps only a sha256 of what an apply produced. On this token it ticked the began..ended row and changed 0.127s to 0.132s, as the submitted summary kept below records. The summary, as the engine wrote it at close:

Small enough whole: one line of RUNME.sh removed. Both done-when lines are decidable and each names a command: se_test propose scripts-are-lf, and a count of bytes equal to 13 in RUNME.sh. Both run from the root, red before and green after. Nothing minted. The one basic the token called missing, the planter's judgement, was found written down rather than asked for.

## evidence: the step 2 rows in full

Red first. Read exactly, with the qualification the tick does not carry. NO NEW TEST WAS WRITTEN: the check that decides this token, util/checks/scripts-are-lf, already existed. Writing a second would duplicate it and be work next to the ask. What rule 12 asks for did happen. The check was run before the change was made and watched go red, red for the defect and not for the fix. se_test propose scripts-are-lf answered ok:false, "FAIL RUNME.sh is LF ... it carries a carriage return at byte 86", with the other five scripts ok and RUNME.sh the only failure of six.

Green after. util/checks/scripts-are-lf, unedited, through se_test propose scripts-are-lf: ok:true in 0.127s, "6 script(s) judged. 0 failed." The byte count of 13 in RUNME.sh went 1 to 0 across the same change.

The began..ended row. NOT TRUE OF THIS TREE, and saying so is honest. began is 1a39e5f7 and the tree is shared: se_test's own delta lists over a hundred files other workers wrote and I never opened. Mine is one line: git diff -- RUNME.sh shows the line-2 hunk gone and wk-faeaddd8a7's hunks from line 20 untouched.

## evidence: the step 2 summary at close

This table is the state at 20:43:04, one apply before the close. The author then ticked the row above and re-ran the check. The summary, as the engine wrote it at close:

work-token.md read whole. No NEW test was written: the deciding check already existed and a second would duplicate it. Rule 12's substance was met, the existing check run before the change and watched red for the defect: se_test ok:false, "FAIL RUNME.sh is LF ... carriage return at byte 86", the only failure of six. Green after, same check unedited, ok:true 0.132s, 6 judged 0 failed. The began..ended row is ticked under protest: as an equality it is false on this shared tree. My change is one hunk inside that range.

