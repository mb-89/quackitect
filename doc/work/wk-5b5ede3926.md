---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: the run exports tools
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-sibelius
claimed_at: "2026-09-05T16:48:38Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 731d0fdd3c8f8828d64234282fca3c4726ae1fc6
  - e9530341acdcc4789edf674976a5aa4d7e591571
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 8f026da50b3c0bcdc4a128d8f1a03f7ce81be1ed
---

## detail

A command through se run inherits the parent's PATH, so about twenty-five recorded calls carry cd and export PATH by hand. Two failures reported as engine defects were only a missing rg. The engine already probes the tools (the pull answer lists each name, path and version), so the run verb can hand the child that environment instead of every command carrying it.

## proposed action

Where se run builds the child process, prepend the directory of every probed tool to the child's PATH. Then rg, node, go and git resolve without a command exporting anything.

## done when

- a test drives the run verb's environment builder with a probe fixture and sees each tool's directory on the child PATH
- a command naming rg with no export runs through se run and resolves, tried once on this machine by the taker
- the test was seen red for the reason expected before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one block in shellCommand and one test over it. | `git diff -- src/engine/run.go` |
| [x] | every done-when line is decidable, and names the command where one decides it | two by the test below, and the middle one by running rg through the verb on this box. | `se test --on wk-5b5ede3926` |
| [x] | the basics it stands on exist, or are minted first | LoadProbe, writeProbe and PathWithTools all existed. Nothing was minted. | src/engine/tools.go |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read. Red first, then green. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | on a clean copy of HEAD with the probe block out of shellCommand, the test fails on all three lines, saying the child PATH is empty. | `go test -run TestShellCommandPutsEveryProbedToolOnPath` |
| [x] | the same test was seen green after the change, and named | green. And rg --version answered through se run with nothing exported, the middle line. | `se test --on wk-5b5ede3926` |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | the block was in the tree at began. The test grows to two tools in two places, reads PATH itself, and holds the parent's behind. | `git diff` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | none. run.go carries another hand's edit, so the commit names the test and note. | the commit |

