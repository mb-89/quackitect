---
id: wk-054bf77924
seq: "11"
type: work
title: fix the controls
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
evidence:
  - outcome
minted_by: main
---

## detail

The control group is not right yet. The hold still draws as a line edit rather than a toggle in the built panel the person runs. Check it against what is on screen, not against the source. Also: every control in a row takes one height, the picker reads as a dropdown, and the line edit mints on Enter.

## evidence: outcome

The hold renders as `<button class="toggle" id="hold">` in the tree and the built bundle, and two render checks assert it is a button and not a field. `--row-h` is declared once and buttons, the line edit and the picker all take it. The line edit mints on Enter and keeps nothing. The picker shows the short form closed and the meaning open, with a caret.
