---
id: wk-054bf77924
seq: "11"
type: work
title: fix the controls
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
evidence:
  - hold
  - heights
  - mint
minted_by: main
---

## detail

The control group is not right yet. The hold still draws as a line edit rather than a toggle in the built panel the person is running, which is either a stale bundle or the type is not reaching. Check it against what is on screen and not against the source. Also: every control in a row takes one height, the picker reads as a dropdown, and the line edit mints on Enter.

## evidence: heights

--row-h is declared once and buttons, the line edit and the picker all take it. A control a few pixels taller than the one beside it reads as a mistake.

## evidence: hold

It renders as <button class="toggle" id="hold"> in the current tree and in the built bundle. Two render checks assert it: the hold is a button, and the hold is not a field. It draws as a line edit only in a bundle built before the toggle type existed, because a panel that does not know a type falls through to the field path.

## evidence: measured against v3

I could not check it against what is on screen, because I cannot see the screen. What I checked is the source and the bundle the person runs. If it is still a line edit after a reload, that is a fault this evidence does not cover.

## evidence: mint

The line edit mints on Enter and keeps nothing. The picker shows the short form closed and what it means open, with a caret. The work row asks for five columns and adds no filler.

