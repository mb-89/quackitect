---
id: wk-98493ee2d8
seq: "-4"
type: work
title: boot opens no windows
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "1"
evidence:
  - outcome
minted_by: person
---

## detail

No console window opens when the system boots. Windows gives a console to a process started from one that has none. The extension spawns nine children on boot and the engine probes tools by their version command. One door per program. Quietly(cmd) in the engine and the stub sets HideWindow and CREATE_NO_WINDOW on Windows, and a local spawn wrapper in the extension sets windowsHide. A Go test per Go package and a script for the extension refuse to pass when they find nothing to check.

## evidence: outcome

Quietly wraps every exec.Cmd construction in src/engine, src/mcp and util/setup, and util/setup's runCmd writes into the attribute the door made rather than replacing it. The extension's spawn wrapper is the only line in src/extension naming child_process. The Go check asserts every exec.Command(, exec.CommandContext( and &exec.Cmd{ is an argument of Quietly( and no line outside the door assigns SysProcAttr. `go test -count=1 -run Quietly` passes in all three packages and node .se/scratchpad/no-loose-spawns.mjs reports 5 files read, 0 failed.
