---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: engine note repeats itself
# where the token stands. The process owns these values.
status: open
---

## detail

Found reviewing wk-711bbd91ec, "checks read stale engines".

checkEngineNote (src/engine/tests.go:778) is called per check at src/engine/tests.go:636, inside runChosen's loop over picks. It calls residentStale, which calls engineToRun, which walks src/engine and stats every .go file. suiteEngine is guarded behind engineKnown and runs once a suite; this walk is not, and runs once per check. The battery declares thirty-seven checks.

It also says the same thing twice. suiteEngine's said already carries whyBuilt when it passed the resident over, and the stale clause appends whyBuilt again. One engine-args run on this tree, one string:

  handed .../.se/tests/se.fresh, built 1m34s ago from the tree: .../src/engine/hook.go is newer
  than .../.bin/se, built 2m20s ago. The engine over this tree is older than its source:
  .../src/engine/hook.go is newer than .../.bin/se, built 2m22s ago. A check that asks it reads
  the old build, so a failure here may be its age and not the change's. Swap first: se --swap --built

Two ages, 2m20s and 2m22s, for one binary: the halves are stamped seconds apart because residentStale takes its own time.Now(). One answer disagrees with itself about how old .bin/se is.

Deciding the resident's age once beside suiteEngine, and appending the stale clause only when suiteEngine did not already say it, drops the walk to one and the age to one.

Also: when suiteEngine answers "" for both engine and said, a method root with no src/engine, the note is the bare string "handed " and names nothing.

## done when

- the resident's age is decided once a suite rather than once a check
- one run's engine sentence gives .bin/se one age, not two
- the stale clause does not repeat a sentence suiteEngine's said already carried
- se test --propose TestACheckOverAStaleEngineSaysSo TestACheckIsHandedTheFreshEngineAndNamesIt answers ok

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

