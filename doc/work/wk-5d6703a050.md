---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: filter landing says depth
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
claimed_by: 7e7f0da1/main
claimed_at: "2026-09-06T16:11:56Z"
# tokens that have to close before this can start
depends_on:
  - "[[wk-b417a9c58a]]"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - df1070dcaaf301b2a3f66b03b960fabc54879840
---

## detail

A person who narrows the queue on a cloud box gets no confirmation that anything happened, and no count of what is left.

MEASURED on 2026-09-06. The owner sent KEYWORD:QUEUE_FILTER=bucket:claims twice. The first send moved nothing, because the engine was still building and nothing was listening. The second landed. Neither send told the agent so, and neither said how deep the narrowed queue was. The owner had to ask, and the agent had to count through the index by hand and answer 22.

THE PANEL ALREADY DOES THIS. util/parameters.json:179 declares a depth control beside the filter box, whose title says "With a filter set it counts what matters, so a bucket can be watched empty". A desk gets that for free. A cloud box has no panel, so the same number has to arrive as words.

WHERE IT LANDS. KeywordSaid in src/engine/keywordmoves.go:203 is the one door a keyword comes through, and it answers with the line the agent is shown. That is where the depth belongs, for the QUEUE_FILTER keyword only.

THE RELAY IS THE OTHER HALF. The person cannot see the number unless the agent says it in the chat, so the line has to ask for that, the way the answer guard asks for an answer.

## proposed action

KeywordSaid, when the keyword it landed is QUEUE_FILTER, appends the depth and the instruction to relay it: what the expression was, how many tokens the queue would now hand out, and to say that number to the person in the chat.

The depth comes from the one door wk-b417a9c58a establishes, not from a fourth count of its own.

An expression that will not read is the case worth saying out loud, because it filters nothing and looks like a filter. The line says so where that happens, rather than reporting the unfiltered depth as though the narrowing worked.

## done when

- three tokens and an expression matching one leave TheDepthAfter saying "1 token": se test naming TestTheFilterLandingSaysTheDepth answers ok
- KeywordSaid answers the word it matched and nothing else, so the line is a second function beside it, and hook.go prints it where the prompt lands
- the same line tells the agent to say the number to the person, and a check on the word chat or relay in it passes
- an expression that will not read answers a line saying it filtered nothing, and does not report the unfiltered depth as the narrowed one
- a keyword that is not QUEUE_FILTER answers the line it answers today, unchanged: the existing tests in src/engine/keywordsaid_test.go stay green

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

