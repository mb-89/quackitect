---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: fallback diff is unnarrowed
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-birch
claimed_at: "2026-09-05T19:21:12Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 7eca0a1bb4d217abf19d84633bc68dbe84154b96
---

## detail

Found reviewing wk-8c3c71d77d, snapshots do not travel.

snapshotsNotHere in src/engine/snapshot.go ends: "Diff against <hash> instead, which is what se test reads its delta against." It is not what se test reads. TestTheDelta narrows the delta through theTokensOwn and, when it cannot, puts the reason in WhyWhole. The sentence hands the reviewer the unnarrowed diff and calls it the same thing.

On a box several agents share, that diff is every open token's work. Measured on wk-8c3c71d77d itself: its began..ended carries 19 files, of which 4 are the token's. The other 15 are wk-a8b817198a's serveHooks and serveModel context work and three notes. A reviewer told to diff against one commit and given no warning reads those hunks as the token's, which is the failure this token exists to prevent, one sentence further on.

The fix is one clause: say that the diff it names is the whole tree's and not this token's, or name what se test names, which is the delta after theTokensOwn.

## done when

- snapshotsNotHere either narrows what it names to the token's own files or says in the message that the diff is the whole tree's: read the string in src/engine/snapshot.go
- a Go test in src/engine holds the wording, so the sentence cannot drift back: go test ./src/engine -run TestAReviewerIsToldWhichSnapshotsAreNotHere

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

