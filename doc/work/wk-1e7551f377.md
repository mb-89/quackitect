---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: engine-args lint red
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-holly
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 635d7b0c45516f921ed200a1d7d02a903cccfded
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 132a2134ce30396f9cfb91398f1e25f9677f504f
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: "The check's expectation was the wrong half. se lint exits non-zero when it has findings, which is the verb answering about the tree it read, not a refusal of the call. The tree it reads is the method, so the line went red whenever anything in the method tripped a lint rule. se --config never has findings, so it stayed green beside it. The line now judges the call with wellFormed, the same instrument every line above uses."
---

## detail

util/checks/engine-args fails on one line: "se lint still answers", saying the refusal is refusing a call that has nothing left over.

It is red on the committed engine. The check drives .bin/se.exe, which serves c5c6796d.120110, so the binary predates today's work and the failure is not in it.

Every other line of that check is green, including the same shape for se --config and se --tree.

So se lint refuses a call that should answer, and only se lint does. The other two verbs take the same argument and answer.

## proposed action

Read what the check hands se lint on that line, and what the refusal reads as left over.

Fix whichever half is wrong: the verb's argument parsing, or the check's expectation.

## done when

- util/checks/engine-args runs green: se_test proposing that check answers ok
- the line that failed is named in the evidence with what it said before and after

## evidence: green after

se_test proposing engine-args: ran [{id util/checks/engine-args, kind check, ok true, seconds 3.4923868}], ok true.

## evidence: next to the ask, not touched

the detail says se --tree takes the same shape and answers. It does not. --tree appears only in the stray loop, so no line drives it with nothing left over. That is a coverage gap in the same check, left alone.

## evidence: red first

On engine 0f226364.162039, over a throwaway work folder and a method with no util/parameters.json, se lint --work W answered {"clean": false, "findings": [...]} and exited 1. Over that one call the two predicates said: old code === 0 -> FAIL se lint still answers, new wellFormed().ok -> ok. Seen naturally red as well, with this tree as the method while doc/guidance/software-development/testing.md ran to 1001 words.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | — |  |
| [x] | every done-when line is decidable, and names the command where one decides it | the first by se_test proposing engine-args, which answers ok or names the failing line. The second by reading the evidence, which quotes the line before and after |  |
| [x] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | doc/guidance/work-token.md, read whole. 11: the gap next to the ask is written down, not fixed. 12: red first |  |
| [x] | one test was written first and seen red for the reason expected | the line was `say(..., code === 0, ...)`. On engine 0f226364.162039, `se lint --work W` answered `{"clean": false, "findings": [...]}` and exited 1. Over that call, `old code === 0 -> FAIL se lint still answers`, `new wellFormed().ok -> ok` |  |
| [x] | the same test was seen green after the change, and named | util/checks/engine-args, by se_test proposing engine-args: `{"id": "util/checks/engine-args", "ok": true}`. The line reads `say(..., answer.ok, ...)` over `wellFormed(fine)` |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | none in the changed file. No line drives se --tree with nothing left over, next to the ask |  |

## evidence: the line that failed

util/checks/engine-args.mjs, "se lint still answers". Before: say("se " + fine.join(" ") + " still answers", code === 0, "the refusal is refusing a call that has nothing left over"). After: say("se " + fine.join(" ") + " still answers", answer.ok, ...) over const answer = wellFormed(fine).

