---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: tests name minted ids
# where the token stands. The process owns these values.
status: open
---

## detail

util/checks/tests-name-no-token reads 284 files and answers 24 failed, measured through se test on this box.

Nineteen are in files git tracks, so they are landed and ownerless:

claimonatoken_test.go:22, 48, 63, 91, 116. enginefindsproxy_test.go:18. flagvalue_test.go:17 twice and 18. holdoutlivestheprose_test.go:18, 25. lintreadswork_test.go:20, 21, 39, 55, 77, 79. unboundpull_test.go:20, all under src/engine. And doc/guidance/methods/reviewing.md:66, which is prose, not a test: the check walks guidance too now.

The other five sit in two files no commit holds yet, batterydone_test.go and godhandsout_test.go. They belong to the hands writing them and are not this ask.

The rule is that a test must not name a token the record carries, because a test that does depends on the record. An invented id is one character repeated, so wk-1111111111 reads as a fixture and wk-0000000001 does not.

This class has been closed three times, by wk-57aca89abe, wk-89f874cf61 and wk-a8b817198a, and is red again each time on files written since. A check that is always red is one nobody reads. Do not soften the check.

## proposed action

Reword each comment so it keeps its subject and loses the id, and write any id a line still needs as one character repeated. Leave the two untracked files to their authors.

## done when

- util/checks/tests-name-no-token names no file git tracks, decided by: se test --propose util/checks/tests-name-no-token
- util/checks/tests-name-no-token.mjs is unchanged, decided by: git diff names no file under util/checks

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

