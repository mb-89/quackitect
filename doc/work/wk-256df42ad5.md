---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: resident engine gates closes
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
# true when this waits for a person rather than an agent
needs_human: true
---

## detail

A submission is refused for reds nobody in the delta caused, on a box where the rule that excuses them is already in the tree and already green.

se pull refuses wk-4a202976eb naming util/checks/deleted-notes-have-a-row. That check names doc/work/wk-8863048da6.md, doc/work/wk-dffbf05720.md and doc/work/archive.jsonl. The delta of wk-4a202976eb is doc/guidance/work-token.md, doc/guidance/backlog.md and doc/work/wk-4a202976eb.md. Not one named path is in it. open-tokens-carry-their-sections is the same shape, twelve older tokens, none in the delta.

src/engine/testedgate.go already rules that a check naming only files outside the delta is the project's red, and src/engine/failureoutsidethedelta_test.go covers it. Both are landed on origin/v4 at 24fff2e. The verdict a submission is judged on is written by a resident process older than that change, so the ruling never runs.

It is not one token. Every open token whose delta touches doc/work is chosen for these two checks, and every one of those submissions is refused for the same twelve older notes.

## proposed action

Restart the resident engine, pid 7239 here, so the process answering is the one .bin/se holds. The restart is the owner's, and wk-65c53d4b97 already says so.

MEASURED 2026-09-06, four ways. The unit test passes: se test --propose TestAFailureOutsideTheDeltaDoesNotRefuseTheClose answers ok. thisTokensFailure copied verbatim outside the tree, handed the refused run's own JSON, answers false for both failing checks, so the record should be true; the engine wrote false. .bin/se is not the stale part: go version -m reads Build=09-06-1751, and RUNME.sh rebuilds it on every call here. And nothing in .bin/se writes that record: a copy of src/engine with a line appended to a file inside RecordTheRun, built and run against this work root, moved the record's timestamp and never created the file. ps puts the resident at pid 7239, started 17:38:00, built near 16:13, before the ruling landed.

## done when

- a submission of a token whose only failing checks name no path in its delta is not refused, and the pull's answer is quoted
- the process answering se test reports the build .bin/se holds: se --version and go version -m .bin/se read the same build, and both are quoted
- se test --on wk-4a202976eb records ok true while deleted-notes-have-a-row and open-tokens-carry-their-sections are still red on other hands' notes

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | Closes stop being judged by code that predates the ruling. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | Finished work cannot be submitted. wk-4a202976eb was refused twice for twelve older notes it never touched. |  |
| [x] | the ask is small enough to review whole, or it is split first | One process, restarted. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | By se pull's answer, by se --version against go version -m .bin/se, and by se test --on wk-4a202976eb. |  |
| [x] | the basics it stands on exist, or are minted first | NO, and that is why needs_human is set. The restart is the owner's, and wk-65c53d4b97 parks on it. |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

