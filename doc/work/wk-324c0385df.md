---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: split halves want approaches
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-chagall
claimed_at: "2026-09-06T07:50:30Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 069d4fee303ae38353d64296d0c5daf1b5f73c5b
---

## detail

A finding from the verdict on wk-6a0b7f2d38, one filter reader. That token split into wk-db6bdc8872 and wk-d7950a4397, and its step 2 evidence says both halves carry the approach unchanged. Neither file holds an approach heading. The standard process names approach a required section, so both halves are open standard tokens missing one. Whoever pulls a half gets a detail and a done-when list, and no shape a reader could have disagreed with before the work. Write an approach section into each, drawn from the approach on wk-6a0b7f2d38 and narrowed to the half it governs. A check reading every open standard token for its required sections would catch the class, and none of the checks in util/checks does that today.

## done when

- doc/work/wk-db6bdc8872.md carries an approach section naming the module shape and the viewer as its caller, decided by: se find --regex '^## approach' --path doc/work/wk-db6bdc8872.md
- doc/work/wk-d7950a4397.md carries an approach section naming what replaces the engine reader, decided by: se find --regex '^## approach' --path doc/work/wk-d7950a4397.md
- a check refuses an open standard token missing a required section, decided by: run it and read what it says

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | Two open halves carry a shape a reader can disagree with. | the two files |
| [x] | what breaks if it is never done, and not only that it stays undone | Whoever takes a half gets a done-when list and no design. | the detail |
| [x] | the ask is small enough to review whole, or it is split first | — | one check, two sections |
| [x] | every done-when line is decidable, and names the command where one decides it | Two lines are se find over one file. The third is running the check. | se find and node |
| [x] | the basics it stands on exist, or are minted first | — | the process declarations |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | read |
| [x] | one test was written first and seen red for the reason expected | The new check named twenty-four open standard tokens carrying no approach, the two halves among them. se find found no heading in either. | exit one, twenty-four failed |
| [x] | the same test was seen green after the change, and named | Both files carry one now, se find finds it, and the check names neither. It answers twenty-two, every one another token. | open-tokens-carry-their-sections.mjs |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | one commit on the branch |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The other twenty-two, and the check's place in the battery, are a token of their own. | wk-f353de1280 |

