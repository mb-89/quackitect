---
id: wk-eb765d80c7
seq: 1000077
type: work
title: a criterion borrows forward
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: rev-1
---

## detail

A criterion names a check another token owns, across a dependency that runs the other way, so neither token can move. Found on wk-2b78b911b1: its sixth criterion names TestAnObservationNamesItsTest, which no .go file under src declares, and wk-42adef4818 builds it with depends_on naming wk-2b78b911b1. The recorded red cites drain_test.go:1, a message the named check could not have produced. Before submitting a draft, run every criterion command from the repo root and require exit zero or a stated reason. For each missing test, find the token whose criteria build it and check its depends_on does not name the token being drafted. When a review says the replacement instrument is on another token, keep the criterion pointed at the instrument held. The owning token rewrites it when it lands. See doc/guidance/specifying.md under The command decides the sentence above it.
