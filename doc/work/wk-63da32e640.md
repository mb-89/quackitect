---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: gate token went missing
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-nono-two
claimed_at: "2026-09-05T18:49:55Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - b92a0140a8254074fd0122279f62a68bf0788831
---

## detail

Found reviewing wk-5b51fc14e4. Its step 2 row says "One token each, as the approach said: wk-88f5e77313 for gate.go, wk-72c8216cc2 for store.go, wk-190889611b for schema.go." Commit 468a2c45 does add all three files. Two of them landed and closed: archive.jsonl carries wk-72c8216cc2 "store argues in place" done, and wk-190889611b "schema argues in place" done. The third is nowhere. doc/work/wk-88f5e77313.md does not exist, archive.jsonl does not name it, and se find over the tree hits only the sentence claiming it was minted. src/engine/gate.go is 25104 bytes and still carries the ALL-CAPS arguments the token was for. So the one piece of the promised cleanup that has not been done is also the one piece no longer in the queue. Nothing catches this class. LintTokens reads the tokens that exist and never asks whether a token id named in evidence still resolves.

## proposed action

Mint the gate.go rationale token again with the same scope wk-72c8216cc2 and wk-190889611b had, so the third file is in the queue with the other two. Then add the lint rule, so a token id named in a note and resolving to nothing is a finding rather than silence.

## done when

- gate.go has a live token for its arguments, open or archived, and the id resolves: se find --regex 'gate' --path doc/work/*.md
- a token id written in evidence resolves to a token or to an archive row, and the lint says so when it does not: .bin/se.exe lint

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | One rule in lint.go with two helpers beside it, and one test. The mint the detail asks for turned out to be unnecessary. | `git show --stat 8511331a` |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one is a find over doc/work, which answers one hit. Line two is the new test and the lint door, both run. | `se find --regex 'gate argues in place' --path 'doc/work/*.md'` and `se lint` |
| [x] | the basics it stands on exist, or are minted first | Tokens, TheArchive and ArchiveList were all there. Nothing was minted to stand on. | `git show origin/v4:src/engine/archive.go` |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. Rule 12 drove red first. Rule 11 stopped a second gate token. | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | It failed first, saying the lint was silent about an id reaching nothing. | `go test . -run TestATokenIdNamingNothingIsAFinding` |
| [x] | the same test was seen green after the change, and named | It passes, with the holder lint test beside it. The package answers eight failures with the change and eight without. | `go test . -count=1` |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | lint.go gains theIDsThatOpen and idsNamingNothing. A note on the disk resolves, and an archive row resolves. | `git show --stat 8511331a` |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | No mint was needed. doc/work/wk-88f5e77313.md is there and open. The rule answers 53 findings, carried by wk-7d2d36115f. | `se lint` answers 58 |

