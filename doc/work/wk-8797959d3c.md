---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: Unbound stops at staffing
# where the token stands. The process owns these values.
status: done
# who did the work step, so the verdict is never theirs
author: main
claimed_by: f5927132/main
claimed_at: "2026-09-06T10:32:06Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 414241e35cc55e37cc08f27bc270d825a55b1d47
  - ae99cb26a9e90166a1f8c9b9ade1039f4c80eb66
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - a2c87a9e11de7b981c257ed8731fb22b14d6958b
---

## detail

Unbound is the queue off and every other rule on. The staffing guard asks Unleashed. The pull's hand-out and the stop judge do not, and both are queue behaviour.

Measured on 2026-09-06 on an unbound cloud box. A pull that closed one token handed out another. The stop judge then refused a stop twice over that same token.

So an unbound session was handed work it never asked for, then held by the work it was handed.

## done when

- A pull on an unbound tree hands out no token, and a test drives a close through an unbound tree and reads no token back.
- A stop on an unbound tree is granted while a token is held, and a test drives it and reads the grant.
- A pull on a bound tree hands out a token as today, and a test drives it and reads one back.
- A stop on a bound tree with work held is refused as today, and a test drives it and reads the refusal.

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | An unbound session works what was asked, and can stop when it is done. |  |
| [x] | what breaks if it is never done, and not only that it stays undone | A person takes the queue off for one thing, is handed another, then held over it. |  |
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | The approach names both cut sites and says a bound tree is untouched. |  |
| [x] | every done-when line is decidable, and names the command where one decides it | All four by se test naming TestUnboundTakesTheQueueOffEveryPathThatIsTheQueue. |  |
| [x] | the change is small enough to review whole, or it is split first | Two guards, one file, 26 lines. |  |
| [x] | the basics it stands on exist, or are minted first | Unleashed and SetBinding are in unbound.go. |  |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token. Rule 12 put the red first. |  |
| [x] | the change follows the approach on the token, or the token says why it departed | Yes. answerFor and AskToStop, and nothing else moved. |  |
| [x] | se test --on this token answered ok, and what it ran is named | TestUnboundTakesTheQueueOffEveryPathThatIsTheQueue, green. Red first in both predicted places. |  |
| [x] | the note says what changed and why, for a reader who was not here | The commit message carries it. |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | wk-cda42858cd, two guards for one rule. |  |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | [[reviewing]] was read and applied | — |  |
| [ ] | every hunk of git diff began..ended was read, and any not read is named |  |  |
| [ ] | every criterion's command was run again, and what it said is named |  |  |
| [ ] | every hunk improves the product, or a finding names the one that does not |  |  |
| [ ] | every finding is a trivial token naming this one, and their ids are here |  |  |

## approach

Ask Unleashed at the two places that are the queue, as the staffing guard already does.

In answerFor, after a submission has settled and before whatComesNext. Closing work is the record rather than the queue, so it still runs. Only the hand-out stops.

In AskToStop, before it walks what the actor holds. The queue did not choose that work and will not choose the next.

Nothing else moves. The write gate, the voice rules and the schema caps all stand, and a bound tree behaves as it did.

