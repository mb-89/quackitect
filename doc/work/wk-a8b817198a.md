---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: test names real token
# where the token stands. The process owns these values.
status: open
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 5ad2ff2bf15bbf2de06175f492ea609b2d4e9019
---

## detail

tests-name-no-token is red on one file.

  FAIL src/engine/deltamissesbadobject_test.go:22 names wk-40abb881a7, which is a token in the record
  212 file(s) read. 1 failed.

The rule the check holds is that a test must not name a token the record carries, because a test that does depends on the record. util/checks/tests-name-no-token.mjs tells a fixture from a real id by shape: one character repeated, so wk-1111111111 and wk-aaaaaaaaaa read as invented and need no list.

The line is a comment, not an assertion, so nothing in the test breaks. What breaks is the check, and a check that is red is one nobody reads: the next real violation lands in the same line of output as this one. That argument is the whole of wk-05df5b9967's ancestor, check stays red always, which fixed the same class in notesgohome_test.go.

This is the only failure the check reports. src/engine/queuehandsunclaimed_test.go carried the same fault and was cleared under wk-89f874cf61, which is where this was found.

Do not soften the check. The id changes, not the rule.

## proposed action

Reword the comment at src/engine/deltamissesbadobject_test.go:22 so it says what it measured without naming a minted id, the way queuehandsunclaimed_test.go now does. Where the line needs an id at all, write one in the shape the check calls invented: one character repeated.

## done when

- no test file names an id the check reads as minted, decided by: se test --propose util/checks/tests-name-no-token answers 0 failed
- the comment still says what it measured, decided by reading the diff: the sentence keeps its subject and loses only the id
- util/checks/tests-name-no-token.mjs is unchanged, decided by: git diff names no file under util/checks

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | nine lines in six test files, each a comment or a fixture id, under one rule | de508c25 |
| [x] | every done-when line is decidable, and names the command where one decides it | the first names the check, the second the diff, and the third git diff over util/checks | de508c25 |
| [x] | the basics it stands on exist, or are minted first | the check is on origin and reads one character repeated as invented. The model file named is not on origin, so its wording was read off the local copy | de508c25 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | work-token.md read. Rule 11 is answered in the finding | de508c25 |
| [x] | one test was written first and seen red for the reason expected | the check over a worktree of 0e2c9ad9: 244 file(s) read. 9 failed | 0e2c9ad9 |
| [x] | the same test was seen green after the change, and named | the same check after the change: 244 file(s) read. 0 failed. go vet is clean, TestACloudBoxTurnsItsNotesIn passes | de508c25 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | six test files under src/engine, on origin in de508c25. began is a snapshot origin does not hold | de508c25 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | the eight lines beside the ask are the same fault, in the same commit. Nothing under util/checks changed | de508c25 |

## evidence: the finding

THE ASK NAMED ONE LINE, AND THE CRITERION NAMED 0 FAILED. ON ORIGIN THE TWO
WERE NINE LINES APART.

The detail was written against a tree where line 22 was the check's only
failure. Over origin's tip the check read nine: line 22, three fixture ids in
notesgohome_test.go, and five provenance comments in foursessionstores,
headingsonce, shellsubmits and stopclaimsession. The three fixture ids were
decided once already, under the ancestor token the detail names, and that fix
never reached origin. The five comments are the class the detail describes.

Taking line 22 alone would have left the check red, which is the harm the
detail names, so all nine went in one commit. Each comment keeps its subject
and loses the id. Each fixture id is one character repeated. The check itself
is untouched: git show --stat de508c25 names six files under src/engine and
nothing under util/checks.

