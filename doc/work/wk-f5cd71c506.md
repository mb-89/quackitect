---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: HoldStates has no caller
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: tests
---

## detail

Found working wk-e523c49ae8, on the test that names exported functions nothing calls.

src/engine/holdhere.go:22 declares HoldStates, and nothing in this tree names it. The only other mention is its own comment two lines above, which says: HoldStates is the order, so a check walks this list rather than one typed here. No check walks it. se find over the whole index answers two hits, the declaration and that comment.

So the function is a claim about a behaviour that is not there, which is the class TestNoExportedFunctionHereIsUncalled exists to name. It was not named, because that test was reading the tree in a way that answered called for it. Measured while making the test faster: the old rule and the new one both answer uncalled, and the test has been red on this and three others.

The three others are handed to the lint's table as values, and are excused there for that reason. This one is not handed to anything.

The answer is one of two. Either the check the comment promises is written, and it walks HoldStates. Or the function goes, and whatever wants the order reads theHold.States() the way HoldStates does.

## proposed action

Decide between writing the check the comment promises and deleting the function, then do that and take the excuse out of lesson_test.go.

## done when

- HoldStates is called by something, or it is gone, decided by: se find --words HoldStates over the tree
- the excuse for HoldStates is out of lesson_test.go, decided by: read the reached map in that file
- TestNoExportedFunctionHereIsUncalled is green, decided by: se test proposing it by name

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

