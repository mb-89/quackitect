---
id: wk-25feb4418f
seq: "-7"
type: work
title: a folder moves
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
parent: wk-58bceb45f3
rounds: "1"
evidence:
  - outcome
minted_by: person
---

## detail

The move verb moves a folder. It refused one, saying to move its files one at a time, and renaming source to src would have been 115 moves. Two rules, each failing without the other. A folder is spelled with its separator, src/ rather than source, so every path under it changes and the English word does not. A prefix expects a word after it, so the rule that stops doc/old.md matching inside doc/old.mdx must not refuse every path under a folder. Found by doing the rename the owner asked for.

## evidence: outcome

refPairs in src/engine/move.go adds a second, bare pair when a directory's path contains a separator. It does not for a top-level folder, and the comment says why. The residual report is asked by every spelling the rewrite used and sweeps every file whatever its format. TestANestedFolderIsRewrittenWhenNamedBare and TestTheReportNamesWhatWasNotRewritten were both proved red. `go test -count=1 ./...` passes in src/engine.
