---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: activity counts as working
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-chagall
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 3d490e716d254c6f430b2671c92e46f17c0be5b4
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - be6c224a8caad3d865b4443bb2de0c6383f31513
# how it ended. Only an ended token carries one.
disposition: dropped
# why it was dropped
reason: The change this token asks for already landed. Staleness reads the record through HasGone, not a pull count, and StillPulling is gone from the tree. Both done-when lines are already tests in git, TestAHolderThatCallsWithoutPullingIsNeitherSweptNorCalledGone and TestAHolderNothingHasBeenHeardFromIsGone, and both run green at the branch tip unchanged. No check can be written that goes red for the reason this token expects.
---

## detail

The investigate flow judges a holder stale by pulls alone. A worker deep in one long proof arrives rarely by design, so it stands accused while it is working.

Seen twice. One worker was writing its token test file and running the test door minutes before it was called stale after 48 quiet rounds. Another recognised the same shape against itself and declined to pull, so as not to strip a live hand.

StillPulling counts arrivals and nothing else. The token carries a trace the engine already writes and nobody reads on this path.

## proposed action

Staleness reads the token own trace beside the pulls. A write under the held token, a run naming it, or its file changing counts as still working.

The investigate notice then fires only when both the pulls and the token trace have gone quiet.

## done when

- a holder that wrote under its token recently is not called stale: a test drives a quiet pull count with a recent write and reads no notice
- a holder quiet on both counts is still called stale: the same test drives both quiet and reads the notice

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A holder deep in one long token keeps its hold. | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | Nothing now. The pull count it accuses was already replaced. | gone.go |
| [x] | the ask is small enough to review whole, or it is split first | — | one guard |
| [x] | every done-when line is decidable, and names the command where one decides it | Both lines are decided by go test in src/engine. | gonebysilence_test.go |
| [x] | the basics it stands on exist, or are minted first | — | HasGone and quietHold |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | read |
| [ ] | one test was written first and seen red for the reason expected | No check can go red. StillPulling is gone from the tree, so the accused reason cannot occur. | rg finds none |
| [x] | the same test was seen green after the change, and named | Both named tests run green at the branch tip, with nothing changed. | TestAHolderThatCallsWithoutPullingIsNeitherSweptNorCalledGone and TestAHolderNothingHasBeenHeardFromIsGone |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | no change |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | — | none |

