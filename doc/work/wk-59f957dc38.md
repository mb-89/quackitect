---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: stop argument test fails
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-ravel
claimed_by: aeaf7bd9/worker-ravel
claimed_at: "2026-09-05T11:27:24Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - ba0dc9fac4b48f50de3160818b22b8817d9504f7
  - 9d0ab993c0b653ebbc0d487f2d609d45d93eb46c
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - c0d80ec30730523762eb9b38c968f631a98ad279
  - d9abbba230f5f80ffec6f59b028a80dd5d546669
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

Finding from the verdict on wk-239cae9216 (a claim gates tracked). Its fifth criterion says the engine package is green, and on this box it is not.

TestAClaimIsArguedWithAndTheThirdIsGranted fails, in the battery and alone through se test: challenge_test.go:52 "claim 1 was taken at face value", with an empty stop answer. The test takes up a claimed token as main, claims asked, and expects the stop to be argued with while work is held. The stop path (src/engine/hook.go:1329) argues only when TheyHold answers work, and TheyHold reads holds from the store, which loadHolds (src/engine/holdstore.go:45) throws away when the store's session is not the current one. Nothing on that path reads a claim, so the claim gate is not the cause; the hold store's session rule or the test's log setup is where to look.

Damage if the test is right about the runtime: an agent holding open work claims a reason and is granted the stop on the first ask, so the two arguments the owner asked for never happen.

Also failing on this box, and probably the box rather than the code: TestTheBatteryRunsOutsideTheEngine and TestTheBatteryRunsOnTheShellGitBrought, each "the battery this test started has not finished" at 3.1s, where a go build here takes 7s.

## proposed action

Find why TheyHold answers nothing for the test's actor after TakeUp, fix the store or the test, and look at the battery tests' fixed wait.

## done when

- cd src/engine && go test -run TestAClaimIsArguedWithAndTheThirdIsGranted -count=1 . is green
- the two battery tests are green on this box, or their wait is measured against the build time rather than a fixed three seconds, and the token says which

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | Two findings on one token, a stop test red and two battery tests red, and both are decided by running the four tests named below through se test in one call. | se_test |
| [x] | every done-when line is decidable, and names the command where one decides it | Line one names go test -run TestAClaimIsArguedWithAndTheThirdIsGranted, run through se test: ok true, 0.72s. Line two names the two battery tests: ok true, 0.05s and 0.04s. | se_test, ok true |
| [x] | the basics it stands on exist, or are minted first | — | the engine, the hold store and the stop hook all exist |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | work-token.md |
| [x] | one test was written first and seen red for the reason expected | NO RED COULD BE MADE. The test claimed asked, which the stop hook grants whatever is in hand, so the empty answer was the rule working, not the hold store. Commit 7c5b7d11 made the tests claim broken and added TestThePersonsWordIsNotArguedWith. It predates this token's first began hash. | git show 7c5b7d11 |
| [x] | the same test was seen green after the change, and named | TestAClaimIsArguedWithAndTheThirdIsGranted, TestThePersonsWordIsNotArguedWith and both battery tests, ok true, twice. THE BATTERY TESTS ARE GREEN HERE, so the wait stands: it waits on an echo script, not a build. | se_test, ok true |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | only this note: the fix was in the tree |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | — | none |

