---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: Where has only tests
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-messiaen
claimed_at: "2026-09-05T16:53:16Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 05a9f47dba59a81656ada8ae66de347d50bbb4ec
---

## detail

A finding on wk-808abd40a4, archive without a ref.

func (a Archived) Where() in src/engine/archive.go names the first of blob, on_branch and tag that is set. Its one caller in the whole tree is src/engine/archive_test.go:158, where the test uses it to predict the path FindArchived reports.

FindArchived does not use it. It takes the second answer from readArchived, which is the object that actually answered rather than the first one named, and readArchived carries its own copy of the same order. So the product holds two statements of the precedence, one of which nothing in the product reads, and they can be made to disagree: a row whose blob is set but swept answers from on_branch, and Where still says blob. The test would then pass on a prediction that is wrong.

TestNoExportedFunctionHereIsUncalled is the check for this class and it does not see it, because it reads exported functions and Where is a method. It fails today on ParseYAML alone, before and after this change.

## done when

- Where is gone and archive_test.go asks readArchived what the reader asks: se find --regex "Where\(\)" --path "src/**/*.go" answers nothing
- TestNoExportedFunctionHereIsUncalled names methods as well as functions, watched red on a method planted for the purpose: se test --propose TestNoExportedFunctionHereIsUncalled
- the archive still answers a search with the object that answered: se test --propose TestTheArchiveAnswersASearch

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | three files: a method deleted, a prediction rewritten, and one regex in the check | git diff --stat |
| [x] | every done-when line is decidable, and names the command where one decides it | the first two named none, so each names one now, and the third holds the archive search | the done when section |
| [x] | the basics it stands on exist, or are minted first | readArchived already answers which object answered, so the prediction has something to ask | src/engine/archive.go |

The first line offered two shapes and took the first. readArchived answers the object that answered, not the first one named. Where could not say that, so it could not be the one statement.

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | rule 12: the check was planted with a method nothing calls and watched name it | doc/guidance/work-token.md |
| [x] | one test was written first and seen red for the reason expected | red: PlantedForTheRed in archive.go is exported, nothing calls it, and it is in no exclusion | TestNoExportedFunctionHereIsUncalled |
| [x] | the same test was seen green after the change, and named | the plant is gone from the failures and TestTheArchiveAnswersASearch is ok true. WriteFront still stands, and it is wk-258d63d350 | se test --on this token |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | archive.go, archive_test.go and lesson_test.go | the frontmatter |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | two, both tracked: the untracked leftover that reddens the check, and the rule that a test is not a caller | wk-258d63d350, wk-a85e0910f2 |

