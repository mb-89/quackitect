---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: god silences the stop
# where the token stands. The process owns these values.
status: closed
# the person's own name for a group. It does not move the work
bucket: claims
# who did the work step, so the verdict is never theirs
author: worker-bay
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0c2def2f2f801111bc86dba6535782eac6209a54
  - 44c140d4e7df5e505ac1f1cb756b157ea33fca2a
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - 1358be5b7fb0ea6969d63b6f54cc9a61dccbd1cd
  - 66ded755e1c28a91d492c137112dbb563c9ee39e
# how it ended. Only an ended token carries one.
disposition: done
# why it was dropped
reason: "Passed. The god check is what the token asked for, the criteria hold, and the two findings are on words rather than code: wk-7e1d675911 and wk-bdce520f18."
---

## detail

God mode is every refusal off, and the stop hook still fires in it.

decideStop in src/engine/hook.go reads the rung nowhere. So a stop in god still needs a claim, and a claim over open work is still argued with three times before it is granted. Every other guard is already off: decidePreToolUse returns at its god check before any of them is reached.

THE OWNER'S WORDS: in god mode, really, the stop hook doesn't fire. You are not supposed to keep running in the loop unsupervised. It is only to fix stuff.

That is the whole reason the rung exists. God is turned on when something in the engine is in the way, with a person watching. An engine that argues with that person's agent about whether it may stop is the engine in the way, in the one state built to take it out of the way.

The person's own controls are the exception and stay. The hold and the ask are enforced above the god check in decidePreToolUse, deliberately, so an override cannot silence whoever is holding the work down. The stop hook is the engine's own rule rather than theirs, so it goes.

## proposed action

decideStop asks the rung once, at its top, and returns when it is god.

1. The check goes at the top of decideStop, the way the one in decidePreToolUse sits above every guard it disables.
2. It reads NoGuardsAtAll, which is the function that answers god and today has no caller outside tests. So the god rung gains its first real behaviour through the name already written for it.
3. The comment says what god is for, and that the person's hold and ask are not affected, so a reader does not take this as everything being off.

Unbound is untouched. It keeps the claim and the argument, and only the queue stops being a reason, which is already true.

## done when

- a stop in god mode is granted with no claim standing, decided by: a Go test driving decideStop on a god tree with work in hand and reading the answer for a block
- a stop needs a claim when bound and needs none when unbound, decided by: the same Go test over both rungs
- NoGuardsAtAll has a caller outside the tests, decided by: se find --regex for NoGuardsAtAll over src
- the existing stop tests still pass, decided by: se test naming the stop and asked-is-granted tests

The second criterion first read that unbound still needs a claim. The owner moved unbound while this token stood open, to a bound agent needing a good reason to stop and an unbound agent not, and that landed in the same commit as this work, so the criterion now says what the test asserts.

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | God becomes what it is named for: the engine out of the way. | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | The engine argues with the person fixing it. | the detail |
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | One check at the top of decideStop, reading NoGuardsAtAll. | proposed action |
| [x] | every done-when line is decidable, and names the command where one decides it | Three by one Go test, one by se find. | done when |
| [x] | the change is small enough to review whole, or it is split first | One check, one record line. | hook.go |
| [x] | the basics it stands on exist, or are minted first | NoGuardsAtAll was written and never called. It is called now. | unbound.go |

## evidence: step 2. do

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. The test landed first, red on its god line alone. | the new test |
| [x] | the change follows the approach on the token, or the token says why it departed | It follows it exactly: one check at the top of decideStop, reading NoGuardsAtAll. | hook.go |
| [x] | se test --on this token answered ok, and what it ran is named | Ok: TestGodSilencesTheStopHook, TestAskedIsGrantedOnTheFirstClaim, TestAStopClaimPassesEveryGuard, TestAStopWithNoClaimIsRefusedHoweverOftenItIsAsked, TestGodModeRefusesNothingAndIsNotSpoken. | se test |
| [x] | the note says what changed and why, for a reader who was not here | The header at decideStop names what god is for and what it does not touch. | hook.go |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | A token of its own, wk-98c5a03545. The change was already in the tree at HEAD 95b9423 while this token stood open, so the criteria were run again rather than the work redone. | wk-98c5a03545 |

## evidence: step 3. verdict

<!-- read every hunk, run every criterion, and say whether each part improves the product. How a reviewer works is [[reviewing]]. Your verdict blocks nothing. You give it once and the token closes on it. Every finding you have is a trivial token you mint naming this one. -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | [[reviewing]] was read and applied | Read whole. Rule 14 refuses the author, worker-bay. I am reviewer-wren. | [[reviewing]] |
| [x] | every hunk of git diff began..ended was read, and any not read is named | The span holds no code: began 0c2def2f is another box's. It landed in 95b9423. I read both hunks whole, hook.go:1409-1430 and godsilencesthestop_test.go. Not read: the Unbound branch and challenge.go, not this token's. | 95b9423 |
| [x] | every criterion's command was run again, and what it said is named | Green, green, hook.go:1426 is the only hit outside a test, five stop tests pass. Two decides against its own criterion. | se test |
| [x] | every hunk improves the product, or a finding names the one that does not | The check sits where the approach put it. Its comment fails: unbound keeps neither claim nor argument. | wk-7e1d675911 |
| [x] | every finding is a trivial token naming this one, and their ids are here | wk-7e1d675911, the stale paragraph. wk-bdce520f18, the second criterion. | wk-bdce520f18 |

