---
id: wk-c9301a00e5
seq: 1000073
type: work
title: the engine recycles agents
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: cowork
---

## detail

A trigger fires, the engine tells every agent to finish, and each is cleared and starts with an empty context, so the machine runs overnight. The engine adds finish what you hold and stop to every answer the way nudge.go speaks once per threshold. The agent puts down with se work --put-down or closes what it holds and claims a stop, and se stop grants it. The clear belongs to the harness, done by the extension restarting the agent or by a person, and whatever clears must tell the engine. The trigger runs every six hours, configurable as a parameter beside the other limits, with a button in the panel to fire it now. Build the round cap in wk-837169b6f1 first, because a clear fixes context and not a token that cannot converge. Proof: fire the trigger by hand with three agents holding work, and every one puts down and stops with the queue unchanged.
