---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: shell pull held
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
---

## detail

Named by wk-6a09907c72, which left it rather than widening itself.

The staffing shortfall holds the pull and nothing else, so the main agent spawns hands instead of doing the work. The refusal names its own escape: a spawned agent is here the moment it pulls.

ON A BOX WITH NO LANE THAT PULL IS A SHELL CALL. AStaffShortfall in src/engine/staffing.go reads the calling actor, which is main, and holds any Bash command that runs the engine and is a pull. So the main agent typing the worker pull is refused the one move the refusal asked for.

MEASURED, September 2026, twice with identical output. src/engine/nolegalmove_test.go asserted that a shell pull naming worker-one goes through, and it does not.

THE COMMAND ALREADY SAYS WHOSE PULL IT IS. It carries --actor worker-one, so the guard can tell a pull for another hand from the main agent pulling more work for itself.

## proposed action

A shell pull whose --actor names somebody other than the caller goes through the shortfall. A shell pull with no actor, or with the caller own name, stays held.

## done when

- AStaffShortfall lets a Bash pull naming another actor through, and still holds one naming main or naming nobody
- a Go test drives both, and the assertion in nolegalmove_test.go that names this token is turned back into the going-through case

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

