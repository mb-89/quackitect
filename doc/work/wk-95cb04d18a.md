---
id: wk-95cb04d18a
seq: "4"
type: work
title: a move verb
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
rounds: "1"
evidence:
  - outcome
minted_by: main
---

## detail

A move verb: rename or move a file and fix every reference to it. v3 had one and it was useful.

## evidence: outcome

move.go applyPairs replaces an occurrence only where neither side runs on into a name, using the same runsOn the report uses. runsOn treats a slash on the left as part of a longer path. The occupied-destination check asks os.SameFile before refusing, so a case-only rename goes through. TestALongerNameIsADifferentFile and TestACaseCorrectionIsARename were each verified red with the defect put back, TestAMoveRefusesWhatItMustNotDo still passes, and the whole engine suite passes.
