---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: lint doubles the battery
# where the token stands. The process owns these values.
status: open
claimed_by: aeaf7bd9/worker-chagall
claimed_at: "2026-09-06T08:11:06Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - ea51fc761c25abea0fe79206732e0d941f3da64a
---

## detail

Found while giving the verdict on wk-8ec7483730, which this token names.

That token put the Go tools behind se lint, at src/engine/lint.go line 196. It left the battery's own copies in place. util/checks/battery.sh runs gofmt and four go vet lines, and then runs se lint at line 423.

So go vet now runs twice over four modules in every battery run, and golangci-lint runs over five modules inside the same suite. Nothing new is learned on the second pass.

The change argues against this itself. The comment on TheGoModules says a list here and the battery's own list are one fact in two places.

The battery is a wall agents wait behind, and clearing it is already open work.

A second thing, smaller. se lint answers clean true whenever no finding came back, at line 198. A box that could not run golangci-lint still reads clean, and only the refused list says otherwise.

## done when

- no Go tool runs twice in one battery pass, decided by reading util/checks/battery.sh for a gofmt or go vet line beside the se lint line
- se lint does not answer clean where a program was refused, decided by a test that hides golangci-lint and reads the clean field
- the whole battery still reports the same failures it did before, decided by running it and comparing

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | Four lanes come off the wall agents wait behind, and clean stops lying. | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | A box whose tools will not start reads clean, and every wait is longer. | the detail |
| [x] | the ask is small enough to review whole, or it is split first | — | one line and four lanes |
| [x] | every done-when line is decidable, and names the command where one decides it | Two are go tests in src/engine. The third is the battery, run twice and compared. | go test and battery.sh |
| [x] | the basics it stands on exist, or are minted first | — | LintGo and TheGoModules |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | read |
| [x] | one test was written first and seen red for the reason expected | The battery test named all four go vet lines. The clean test found a tree with no findings, two programs refused, and clean answered true. | exit one |
| [x] | the same test was seen green after the change, and named | Both pass, beside the four lint and format tests that were already there. The battery ran on each tree and answered twenty-one failures, the same set. | TestTheBatteryRunsNoGoToolTheLintAlreadyRuns and TestTheLintIsNotCleanWhenAProgramWasRefused |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | one commit on the branch |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | gofmt is no duplicate and stays, measured against the golangci default set. Moving it behind the verb is its own token. | wk-2e9721e324 |

