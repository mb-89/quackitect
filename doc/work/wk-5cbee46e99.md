---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: closing shuts the door
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: claims
# true when this waits for a person rather than an agent
needs_human: true
---

## detail

A close now names the paths a hand has to land, on wk-48eee4caa6. The next call is refused, because that token has ended and se run wants one that is open.

MEASURED, 2026-09-07, on wk-48eee4caa6 closing itself. The notice read: land every path this close wrote, doc/work/wk-48eee4caa6.md and doc/work/archive.jsonl. The land through se run naming that same token answered: wk-48eee4caa6 already ended as done, name a token that is open, or mint one. A note was minted to run one command.

The refusal is well formed and names a legal move, so this is friction rather than a wall. It falls on every close, on every box, at the one step where work is lost, and it is paid by minting something nobody wanted.

Which shape is right is not mine to pick, so this carries my best attempt and waits for a person.

## proposed action

Let se run take a token that ended within the last few minutes, for a command that only lands. That keeps the gate, since a token cannot be worked after it closes, and it opens the one door the engine has just asked the hand to walk through. The other shape is for the close to land its own paths, which is smaller for the agent and larger for the engine, because land.sh is deliberately a hand's door and not the engine's.

## done when

- a land naming a token that closed a moment ago runs rather than being refused, and a Go test drives both the land and an ordinary command still refused
- a token that closed long ago is still refused, proved by the same test
- the settled notice names the door it expects the hand to use

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

