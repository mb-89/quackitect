---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: god mode drops itself
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: lifecycle
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - f88450daabbb2189415744af2f4910d77437c96c
---

## detail

The tree was put in god mode from the button, and it came back to bound a moment later. Nothing was clicked.

The ruling: the only thing that ends god mode is another click on the button. Nothing else may put the rung back.

Four places could write the binding without being asked:

- a lapse or a timeout on the rung
- a projection or an engine start restoring a default
- a swap
- the extension, if it writes the rung when it redraws

This is the class where state is reset by somebody else, with nothing saying who did it or why. The binding is the most dangerous instance of it. A rung that drops itself puts guards back on a person who took them off.

## approach

The binding records the name of whatever last wrote it, beside what it says. Every writer names itself, so a drop can be attributed rather than guessed at.

Then a test drives each candidate writer over a tree in god mode, and reads the binding after. A writer that puts the rung back without a click is the defect, and the test names which one it is.

## done when

- the binding carries the name of its last writer, decided by: read the binding after a change from the button
- a swap over a tree in god mode leaves god mode standing, decided by: a test that swaps and reads the binding
- an engine start over a tree in god mode leaves god mode standing, decided by: a test that starts and reads the binding
- a projection over a tree in god mode leaves god mode standing, decided by: a test that projects and reads the binding

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | Guards taken off stay off. They were lasting seconds. | the detail |
| [x] | what breaks if it is never done, and not only that it stays undone | Every restart re-arms the guards under a person mid-fix. | the detail |
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | It is on the note. Binding already carries By. | the approach |
| [x] | every done-when line is decidable, and names the command where one decides it | Line 1 reads binding.json. Lines 2 to 4 name a test each. | controlslastonesession_test.go |
| [x] | the change is small enough to review whole, or it is split first | One file holds the rung and one caller sets it. | se find SetBinding |
| [x] | the basics it stands on exist, or are minted first | Binding carries By and Session already, so none was minted. | src/engine/unbound.go |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read whole. Rule 12 drove red first. Rule 13 drove both halves, the restart and the person leaving. | doc/guidance/work-token.md |
| [x] | the change follows the approach on the token, or the token says why it departed | It departed. main.go is the only caller of SetBinding, so no writer put the rung back. The read did. | the note section |
| [x] | se test --on this token answered ok, and what it ran is named | It ran TestOnlyAClickPutsTheRungBack and answered ok true. Three new and four older control tests answered ok in 6.8s. | the test door |
| [x] | the note says what changed and why, for a reader who was not here | The note names the read, the stamp and the blanked name. | the note section |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | The helper modelling a person leaving writes what the harness writes now. | controlslastonesession_test.go |

## note

WHAT CHANGED. The rung asks the harness which session it is in, not the engine. TheHarnessSession answers the session record the log carries, or nothing where it carries none. A rung falls only when that answer names a different session.

WHY. The rung was stamped with the engine run. A start retires the log and opens a fresh one under a new run. So every restart read as a person leaving, and put the guards back on somebody who had taken them off. A swap and a projection never did. Both were driven anyway.

AND THE NAME STAYS ON THE WAY DOWN. SetBinding blanked By on the way to bound, so a drop could only be guessed at.

WHERE IT DEPARTED. The approach expected a writer. Nothing writes the rung but the button.

