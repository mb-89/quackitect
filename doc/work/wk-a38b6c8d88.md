---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: the general case unreached
status: closed
disposition: done
---

## detail

src/engine/nudge.go calls forgetAbove only inside `if step == 0`, so the general partial forget only ever assigns zero. The detail of wk-d1b585b7b9 says the engine forgets a step once the count falls below it. A queue nudged at six, drained to four and refilled to six is not nudged again. Move forgetAbove above the `if step == 0` return. Add a test that nudges a queue at six, drops it to four, puts it back to six and requires a second nudge. Where a call site pins a helper's argument, move the call out of the guard or replace the computation with the constant.

## done when

- forgetAbove in nudge.go runs for every step, not only step == 0.
- Test: nudge at six, drain to four, refill, require a second nudge, red then green.

