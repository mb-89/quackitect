---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: lint hold rule renoised
# where the token stands. The process owns these values.
status: open
---

## detail

holdersIn in src/engine/lint.go answers 13 findings on this tree. Measured with ./.bin/se lint and .se/tests/se.fresh, which agree: 404 findings, 13 of this shape. The four lines wk-4fbce27e2e names are gone from that list, so this is the same rule renoised by prose written since.

They fall in two classes and the answer differs.

Eight are sentences about how the engine works, quoted from tokens: wk-2346d5827d, wk-51858264b4 twice, wk-5d6e638e96, wk-5f56e97cdb, wk-9935549f19, wk-bd6c665ee8, wk-e2809c026c. The widest of them names a check rather than an actor, and two findings turn on that alone.

Five name an actor against a token: wk-2346d5827d, wk-6cc09dedd4, wk-7a498f6a2b, wk-e48f60dd68, wk-fa4edbe5aa. There the rule is right and the note is what should change, because the sentence goes stale the moment the session rolls.

A note about this rule cannot quote the lines it is about without tripping it, which is why the ids are named and the sentences are not.

## proposed action

Add the first class to the clean map in holderinprose_test.go, taking the check-not-an-actor case as one rule rather than eight lines. Correct the sentence in each note of the second class.

## done when

- ./.bin/se lint answers no finding of the hold shape for any id the detail lists in the first class
- TestATokensProseNamesNoHolder still fails when claimsAHold is stubbed to return false

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

