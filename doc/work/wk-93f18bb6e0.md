---
id: wk-93f18bb6e0
seq: "7"
type: work
title: agents get names
status: closed
assignee: main
scope: single-step
traced: true
disposition: done
minted_by: main
evidence:
  - outcome
---

## detail

The actor column shows a hash like ab974d849cbc3990b, which means nothing to a reader. Level 0 already records an agent's type when the harness starts it, so derive a speaking name from that: reviewer-1, scribe-2.

## evidence: outcome

An agent gets a speaking name when the harness starts it, the kind and a number, so two reviewers are reviewer-1 and reviewer-2. evidence.go carries nextName and NameOf. NameOf is what the record shows and the harness identity is still the key. The guard names the actor once at the top rather than at each event.
