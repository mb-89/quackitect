---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: rationales name nobody back
# where the token stands. The process owns these values.
status: open
claimed_by: 547b9365/worker-relay-trial
claimed_at: "2026-09-05T20:59:32Z"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 393bc0f951afd025c73baa7e9a0dc80874e70563
---

## detail

Found working wk-a53e262ad6. On origin/v4 at 07516f58, go test -C src/engine -run TestARationaleIsNamedNotRepeated ./... answers FAIL, and none of the failing subtests is about that token's files.

Ten notes under doc/rationale list files under explains that do not name the note back by link: a-hook-is-taken-only-when-level-0-owns-the-decision, a-program-is-named-never-a-command-line, a-scope-cannot-be-left-while-its-tokens-are-open, every-call-carries-an-identity, every-token-names-its-closer, one-source-projected-everywhere, one-tree-holds-every-parameter, the-floor-is-look-and-shout, the-guard-scrutinises-rather-than-forbids and the-installer-needs-the-network-and-nothing-else-does. The files they name include src/engine/token.go, pull.go, abort.go, hook.go, evidence.go, project.go, config.go, unbound.go, gate.go, guards.go, src/processes/standard.process.yaml, util/projections.json, util/parameters.json, util/setup/main.go and RUNME.sh.

So the test that keeps a rationale joined to its instruction is red on the tip, and a worker cannot read a fresh red of their own out of it. Every rationale landed since is checked by a test nobody can watch go green.

## proposed action

For each of the ten notes, put a one-line link to it in every file its explains lists, at the comment or key that carries the instruction it argues for. Where a file still carries a sentence of the note's why chapter, move the sentence out. Where a note explains a file that has no comment to carry a link, such as a json file, decide whether the explains entry is wrong and say so on the token.

## done when

- every rationale is named back by every file it explains, decided by: go test -C src/engine -run TestARationaleIsNamedNotRepeated ./... answers ok
- no explains entry was dropped without a sentence on this token saying why, decided by: reading the token's evidence against git diff began..ended over doc/rationale

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one line per link, 21 links in 18 files, reviewed whole in one diff | 35616c6b |
| [x] | every done-when line is decidable, and names the command where one decides it | the test by name, and the diff over doc/rationale, which is empty | 35616c6b |
| [x] | the basics it stands on exist, or are minted first | the tip did not build, snapshot.go called a renamed function, repaired first in 9c5e5714 | 9c5e5714 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read, and each row below names its check and what it said | 35616c6b |
| [x] | one test was written first and seen red for the reason expected | TestARationaleIsNamedNotRepeated on a worktree of 9c5e5714: 21 lines of does not name, and none of still carries | 9c5e5714 |
| [x] | the same test was seen green after the change, and named | the same test ok on 35616c6b, with the-cage-cites-what-is-here, scripts-are-lf and projections-carry-chapters at 0 failed | 35616c6b |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | began is 393bc0f9 here, and the change is 35616c6b on 9c5e5714, 18 files, none under doc/rationale | 35616c6b |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | no explains entry dropped: the json files carry the link in $comment, projections.json gained one | 35616c6b |