---
id: wk-8b5b57101b
seq: 1000075
type: work
title: a drop meets nothing
status: backlogged
assignee: main
scope: single-step
traced: true
minted_by: main
---

## detail

A token being called off cannot be ended through the pull, because submit in src/engine/pull.go runs UnmetCriteria before it reads p.Disposition. The disposition is written at pull.go:307, after the refusal at pull.go:302. On wk-e59b1f134b the pull refused a dropped submission twice, first in everyFindingAnswered and then for all seven criteria, and the owner deleted the file. The gate stays as it is for done and became. Decide: either a dropped submission skips the criteria and leans on the reason, or the refusal names se work --abort as the door. Decide the same for the findings gate in the same breath. Check, red today: a Go test in src/engine mints a token with one impossible criterion, submits it dropped with a reason, and expects acceptance.
