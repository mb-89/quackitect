# iteration breadcrumb — read only by the guidance resolver, never by the engine
[iteration]
type    = "default"
rigor   = "systematic"
version = "i0008_trust_hardening"

# logs_dir = "D:\\somewhere"   # optional override; default = %LOCALAPPDATA%\quackitect\logs\<workspace-slug> (see 'quack version')
