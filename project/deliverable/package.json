{
  "name": "se-engine",
  "version": "4.3.0",
  "private": true,
  "type": "module",
  "description": "The se MCP server: caged-agent tool lane, call log, state machine kernel.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.ts\"",
    "serve": "node engine/bin/se-mcp.ts --root ../..",
    "build": "node esbuild.mjs"
  },
  "dependencies": {
    "@lydell/node-pty": "^1.1.0",
    "@parcel/watcher": "^2.6.0",
    "@vscode-elements/elements": "^2.5.1",
    "@vscode/ripgrep": "^1.15.9",
    "@xterm/xterm": "^5.5.0",
    "marked": "^15.0.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.6",
    "@types/node": "^24.0.0",
    "esbuild": "0.28.1",
    "typescript": "^5.6.0"
  }
}
