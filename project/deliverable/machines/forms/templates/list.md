---
id: template-list
statement: A list editor — one item per row; a new row appears as the last one fills.
editor: list
line_pattern: "^- .+"
line_help: one item per line, each line a dash
placeholder: "one item — Enter adds the next"
---

# list

The editor IS a list: one row per item. Stored as markdown, `- <item>`
per line.

- Lead each item with its key word.
- Keep one thought per item; a growing item wants splitting.
- An empty list is a claim too: one row saying `none`.
