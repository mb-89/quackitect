---
id: template-list
statement: A list — one item per line, each line a dash.
editor: text
line_pattern: "^- .+"
line_help: every line starts with a dash and carries one item
---

# list

One item per line. Start each line with `- `. No prose paragraphs.

- Lead each item with its key word.
- Keep one thought per item; a growing item wants splitting.
- An empty list is a claim too: write `- none`, never a blank.
