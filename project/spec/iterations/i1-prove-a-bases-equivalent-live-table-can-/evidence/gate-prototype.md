---
form: gate-prototype
by: agent
signed_off: 2026-08-10T19:34:57.474Z
authors: agent
files:
---

# Evidence form / gate-prototype

## current_situation

Three spikes ran, three experiments stand with their fold keys answered, fold-back is signed, and the promotion filter yields one entry. The register's hot assumptions are probed, closed or deferred with their until.

## buildable

yes — the winner's two load-bearing bets are now measured rather than believed: trunk reads cost 2.0 ms per file in the batch-reader shape (exp-trunk-read-cost), and the walk resumed from files alone five times in one day (exp-kill-and-resume). The one falling spike (exp-latency-ledger) fails a quality the build must fix, not a premise the build rests on — its fix vehicle is chartered.

## round_0_verify

- evidence vs claims: three seeded refs, three experiment nodes, each carrying question, timebox, form, faked, fallback, verdict and a dated measurement; both probed register entries carry the same finding in their own bodies, and the fold view matches the nodes it derives from
- types: the tree typechecks — the commit hook blocked nothing this round
- lint: biome green on every write this round; seven standing style infos in forms.ts predate the round
- tests: battery 1073 of 1073 green at e568bb2, before the walk resumed

## round_1_validate

- exercised against the goal: M6 exists to derisk the build, and the build's riskiest ground is now measured — the three hottest spikeable unknowns settled or bounded
- missing: the hard-kill and host-swap resume variants — deferred on the record with the POSIX until, never silently
- wrong: nothing found wrong; the one falling spike fails a quality the build must fix, not a premise the build rests on
- out of scope: the async ticket desk that fixes the latency breaches — chartered in the backlog, not this iteration
- prior art: the spike discipline follows meth-spike-tracer's sources — tracer bullets, walking skeleton, the lean experiment card

## round_2_red_team

- the resume kills were sanctioned restarts, never an OS kill mid-write => named on the experiment's faked field and deferred with the POSIX probe, not hidden
- the latency ledger reused a measurement from earlier today => same log, same day, still current — and the verdict is the uncomfortable one, falls
- the trunk-read probe used small method files, a large canvas may cost more => the batch shape scales with bytes; the promotion names the SHAPE, not a number, and the trigger on the decision watches the cost
- one hand ran everything => the brownfield acceptance from gate-candidates stands, and every measurement is re-runnable from its recorded setup

## raid_additions

- none — the spikes updated three standing entries and added nothing new

## verdict

pass — the riskiest assumptions are validated by evidence or parked on the record; the design is buildable as evidenced; one promotion (the batch-reader trunk-read shape) enters M7 through this gate

## follow_up

The bless is the owner's. After it, M7 opens: author-tests and plan-build, with the batch-reader promotion and the fitness candidates as standing inputs.

## anything_else

