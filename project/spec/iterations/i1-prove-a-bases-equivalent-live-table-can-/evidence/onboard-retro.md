---
form: onboard-retro
by: agent
signed_off: 2026-08-06T16:51:27.286Z
checked: Read software, Read ux, Read retro, Read template-free-form, Read template-per-item, Read template-list, Do the survey, Establish the interval
authors: agent
files:
---

# Evidence form / onboard-retro

## current_situation

First iteration of the product, standing at M0. The forms lane, the generic editors, the thumbs and the self-healing pages were built and corrected live today. Four notes pend; the drain's judgment guard blocks parking them here (the seam bug below).

## field_feedback

A full day of live field feedback from the owner, each ruling built the same day: the state's details ARE its form; templates are generic editors taking arguments from the form; submit is the one checking act and belongs to the active state; gates end in a bless or dismiss from a hand above their rung; stale page shells heal themselves on the engine's new life.

## notes_drained

- note-0382c485f421: backlog — three cache layers, three manual reloads; ready when the surface-freshness work starts.
- note-daeb55214242: backlog — any operation past one second goes non-blocking with a toast; ready when the panel work resumes.
- note-9f4e89c58212: backlog — a live overview of running agents with progress; ready with the update-system rework.
- note-bb42c7890b4b: backlog — a signature restored by file edit survives below an unblessed gate; checked bed6c1f, which corrected one file by hand and built no mechanism.

## call_log_mined

- first retro: no earlier boundary, so the window is the whole bootstrap log, 2486 calls
- recurring refusal classes today: stale patches after the fixer's reflow, and update briefs refused as chained lists
- one live seam refusal: judgment drains inside onboard-retro — the inRetro guard matches the name alone
- the day's dominant failure shape: stale page shells after engine reloads, cured mechanically with the life stamp

## waste_leads

- three symptom fixes shipped before the stale-shell root was found — diagnose the shell before the button next time
- the retro seam guard was written before same_as existed and nobody re-checked it against the generated states

## process_stale

Checked against the field this session, not from memory. The requirements method was rebuilt from ISO/IEC/IEEE 29148:2018, the INCOSE Guide to Writing Requirements, Mavin's EARS, and the 2024-2026 literature on LLM-assisted requirements — the last of which is genuinely new input, and it changed the method card: fabricated precision and plausible-but-unverifiable statements are named failure modes with a source-grounding rule against them.

WHERE WE DEPART FROM THE BOOKS, DELIBERATELY: 29148 demands singular requirements. The owner ruled that requirements written for agents need detail, and that a thousand atomic rows is the worse failure. So a node carries one CONCERN with binding facets in a Detail table, and the split rule is verification, not grammar. That is recorded in the method card with the standard it overrules named.

NOT COMPARED: nothing was measured against a running requirements tool. Nobody here has driven Jama, Polarion or DOORS this period, and the standing note on that says field experience is not citable — so the comparison is owed, not made.

## follow_up

- fix inRetro to honor the same_as reference (note-6176aac68897), unbound, after this walk
- re-drain the three parked notes once judgment drains are legal in onboard-retro
- the kickoff proposes the change size next

## promotions

- The matrix rows earned promotion and took it: M3_10's register field became typed references with `covers: use-case`, its set criteria became six per-item questions, and it now demands meth-requirement-authoring as an entry read — all in the rigor matrix itself, so the next product inherits them.
- machines/items/requirement.md is new and upstream: it did not exist when this iteration opened, and it carries its own mechanical checks in frontmatter.
- The template-declared checks engine is upstream: generic code in conformance() applies them, so any item template may declare rules and both hands trip them.
- meth-requirement-authoring.md is new and upstream, carrying the fan-out heuristic and the owner's singularity ruling with the standard it overrules named.
- Staying local: the 145 requirement rows and the qualities chain are this product's content, not method.
- What the promotion cost: editing the matrix mid-walk moved the pin's hash and stripped thirteen sign-offs whose own demands never changed — filed as an engine defect, not a reason to stop promoting.

## anything_else

The register was clustered three times this session under the owner's sharpening test: demands that never fail independently in the real world are one requirement carrying facets. 218 rows became 145 — 95 folded into 37 themed requirements. Sixteen use cases still carry more than five rows and were looked at; the fan-out heuristic in meth-requirement-authoring now says the check always runs even where the fold does not.
