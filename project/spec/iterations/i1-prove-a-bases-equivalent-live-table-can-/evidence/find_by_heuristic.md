---
form: find_by_heuristic
by: agent
signed_off: 2026-08-09T11:36:09.030Z
authors: agent
files:
---

# Evidence form / find_by_heuristic

## current_situation

Eight rules against five clusters, run whole. Forty cells, and the misses are written down rather than left blank.

Three rules bit hard enough to mint an option. Five suggested something already done, or nothing at all, and both of those are recorded as such.

## applies

yes

## sweep

| group what changes together, separate what changes apart | the partition itself is this rule applied, and it produced five clusters | nothing new |
| make the common case cheap, make the rare case possible | the walk | the reading is re-owed whole on every restart, and the restart is the common case |
| one source of truth, everything else derives | the account | three requirements exist only to keep two stores in step |
| push decisions to the last responsible moment | M4's own order already does this, weights before candidates so they cannot be tuned | nothing new |
| make the illegal unrepresentable, not merely checked | the walk | almost every defence here is a check with a remedy, which is the second-best shape |
| small interfaces between big parts beat the reverse | the account consumes from six functions and shows to one surface, which is the shape the rule wants | nothing new |
| if it must be remembered, it must be recorded | already the whole point of the account | nothing |
| the default should be the safe thing | the walk | a fill without submit SAVES silently, and the silent save reads exactly like a refusal |

## options

- opt-unrepresentable-not-checked
- opt-reading-credit-survives-the-engine
- opt-one-store-for-what-happened

## follow_up

- THE EIGHTH RULE BIT AND WAS NOT MINTED. "The default should be the safe thing" points straight at the submit flag: a fill without it saves and does not stamp, which cost four round trips this morning. The fix was documentation, and the rule says the DEFAULT is wrong rather than the documentation. That is a cell nobody has drawn, and it is left open deliberately rather than folded into a note.
- THREE RULES FOUND THE DESIGN ALREADY RIGHT, and that is worth as much as a hit. The partition, the M4 ordering and the account's fan-in each survive a rule that could have condemned them.
- THE CATALOGUE IS EIGHT RULES LONG AND SHOULD GROW BY EVIDENCE. Two rules earned their place today by biting on measured pain: the reading re-owe, and the silent save. Neither is in the catalogue as a rule; both are instances of ones that are.

## anything_else

