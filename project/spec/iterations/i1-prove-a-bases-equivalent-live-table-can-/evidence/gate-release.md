---
form: gate-release
bless: blessed by human
by: agent
signed_off: 2026-08-11T14:36:27.370Z
authors: agent
files:
---

# Evidence form / gate-release

## current_situation

The package stands: dist/quackitect-3.0.0.zip, assembled by engine/bin/package.ts, claim signed with the works check yes. Gate-validation is blessed. This gate ships the version on the bless.

## market_block

Not declared to market - the tier does not apply this iteration (meth-market-tier).

## round_0_verify

- evidence vs claims: the package claim names a file the engine itself checked on disk (the file-ref law), and the works answer cites its runs in the call log (job-msorbhdk-5)
- types: the typechecker runs in the battery preflight - green 2026-08-11
- lint: biome check green at the battery, zero errors, seven pre-existing infos in unrelated files
- tests: the battery 1088 of 1088 green on the recut trunk including the new file-ref pin, and the scoped help run 13 of 13 after the RUNME edits

## round_1_validate

- exercised against the goal: the archive was USED - extracted outside the repo, npm install added 30 packages clean, preflight green from the extracted tree, README renders the product name
- missing: the fresh-machine install run (RUNME plus VS Code, reach the desk) - already owed with the ramp-up demonstration on raid-issue-must-demos-owed; nothing else known missing
- wrong: nothing known wrong - the brand-path defect RUNME carried (reading project/brand where nothing lives, so a fresh install greeted as se and an export renamed nothing) was found BY this step and fixed on trunk 63f3ba43
- out of scope: finalize-docs and ship-review wait outside the matrix (owner ruling 2026-08-11), and the version-stamp consolidation is parked as note-4c4a3f8bb8a5
- prior art: editor extensions normally ship as marketplace VSIX with signing and auto-update, which ours sheds; ours ships a repo-shaped zip with a one-time installer because the product is a folder - engine, method, cage and records-home - not only an extension, and the export shares the package's README template so the two front doors cannot fork

## round_2_red_team

- the works check never ran the installer, so yes may overclaim => steelman: a package nobody installed is unproven; answer: the check used the package for real (extract, install dependencies, preflight green) and says plainly what it did not run - the fresh-machine run stands named on the register where the bless sees it
- the zip packs the live tree, so record or session files could ride into a release => the packager excludes .git, .se, worktrees, node_modules and project/spec by NAME, the same list the export uses, and the shipped zip was rebuilt from the synced record tree after trunk landed
- a gate with no evidence field of its own could wave anything through => the gate depends on the package state, whose claim the engine checks on disk; the dropped packaged field was a second copy of a signed claim, not a check - the kill-criterion was the extracted package failing preflight, and it did not fail

## raid_additions

- none — the fresh-machine gap already stands under raid-issue-must-demos-owed

## verdict

pass with overrides — the package stands and was used: extracted, installed, preflight green, product name rendered. The one override is the fresh-machine install run, named per the register and owed with the ramp-up demonstration. The bless ships the version and is the owner's.

## follow_up

On the bless, 3.0.0 is shipped and the walk reaches the terminal; the retro fires beyond it (a needs-retro note opens the next start). The owed runs - the fresh-machine install and the two person-shaped demonstrations - close raid-issue-must-demos-owed when performed.

## anything_else

