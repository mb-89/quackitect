---
form: gate-motivation
by: agent
signed_off: 2026-08-05T18:43:47.126Z
authors: agent
files:
---

# Evidence form / gate-motivation

## current_situation

Every feeder of this gate stands signed — the packet, the register, the pressure test — and the gate law verified it mechanically before this stamp was attempted. The one interestingness discussion closes here; past this gate the vision is axiomatic.

## vision_scope_stated

The packet stands signed in full: the big idea (architecture decides whether technology is good; input decides architecture), the to-be world, the goal system with ruled conflicts, the Moore pitch for teams — every field redlined line by line with the owner before its submit. Scope and non-goals are drawn from the signed delta: the input end in full, the output end deliberately open, never commercial, never software-only.

## problem_agreed

The problem is agreed and evidenced, not asserted: the output field is crowded while the input-and-architecture middle stands open; thin input becomes slop; an early architecture error costs orders of magnitude (Boehm); ungoverned agents fail at both ends of the strength scale. Every claim cites a reference note in the signed as-is.

## prior_art_positioned

The check RAN at this gate, and the red team counter-probed it — six categories, each read for what it sheds:

- Spec tools (spec-kit, Kiro, Tessl, OpenSpec, BMAD): generate FROM a spec; none forces the spec's own quality or the architecture step.
- Agent runtimes: fleet policy and telemetry — who ran what, never whether the work earned its way.
- Requirements-management suites — surfaced by the red team's counter-probe (@[[ref-rm-suites]]): Jama, Polarion, DOORS Next and Codebeamer force requirement QUALITY at write time (INCOSE/EARS scoring before save) and hold approval workflows with live traceability. The honest concession: they occupy the input-quality corner — for human authors, at the wording level, over an artifact store. They govern no agent, encode no drawn method, and their record is requirements plus approvals, not the decision trail as the spec. This method DESCENDS from that lineage and encodes it for agents.
- Approval layers — a live 2026 category FOUND BY THIS CHECK (@[[ref-approval-layers]]): human gates over agent ACTIONS — payments, deletions, escalations — with audit trails, pushed by the EU AI Act, ISO 42001 and the NIST framework. They gate transactions, not engineering: no drawn method, no evidence forms, no decision record as spec. The regulatory demand for demonstrable human oversight is a tailwind for the record half of this product.
- Academic: spec-anchored drift enforcement is the closest cousin of the record half, without adjudication provenance; adversarial-convergence methodology overlaps the red-team rounds as method, not product.
- The middle NARROWS but stands open: nobody couples forced design input, an encoded method, and a governed agent on one evidence-and-approval record. Each neighbor holds one corner.

Ruled at this gate and BUILT the same evening: every review round carries a prior-art item — a missed neighbor keeps getting chances to surface. The deep requirements-level check stays later in the walk by design.

## success_measurable

The value props carry pass lines a machine can check: any past decision answers its why in one click from the record; no output state is reachable past an unsigned architecture gate; an unattended walk stops at gates and nowhere else; the person's time goes to judging, not process-writing. Signals named: zombie questions in reviews drop, and this product keeps building itself.

## risks_logged

The register stands typed and signed: eight entries in the six-column table — kind, impact, likelihood, owner, revisit — led by the supervision paradox and the thin evidence base. The pressure test's two unclean answers resolve INTO standing entries, not past them.

## round_0_verify

- evidence vs claims: every packet field read back from its signed form; the five value props resolve as references and each answers the value-prop template
- types: green — tsc clean at the landing commit
- lint: green — biome clean through today's commits
- tests: green — 775 of 775, preflight green, at 641c0c3

## round_1_validate

- exercised against the goal: the whole M1 chain was walked by the machinery it describes, every form typed, guided and signed — the process proved itself producing its own founding packet
- missing: nothing standing — the vision-level prior-art check ran at this gate and survived its own red-team round; the deep requirements-level check stays later by design
- wrong: the first position claimed nobody-in-the-middle; the counter-probe narrowed it to nobody-couples-the-three — corrected in place
- out of scope: the output end, held out by the signed non-goals
- prior art: the check RAN and was COUNTER-PROBED — six categories scanned, two neighbors found and referenced (the approval-layer category, the RM-suite class); the verdict stands in prior_art_positioned

## round_2_red_team

- STEELMAN the self-serving vision: the product proves itself on itself, n of 1 => conceded and registered — the thin-evidence entry owns it with a measure-on-landing trigger; the packet claims belief with named evidence, never proof
- STEELMAN the waterfall rebrand: forced input phases are waterfall with new paint, and specs drift from intent => waterfall was right about the ORDER and wrong about the AFFORDABILITY — it died because keeping spec, trace and documentation in sync was human toil, so drift was inevitable; the LLM removes exactly that cost, an agent runs overnight keeping the trace current and re-opening signed history when inputs change (lived three times today); the whole spec-driven field is this realization, and this product adds the input end and the record that field still lacks
- STEELMAN the strongest counter: the next model generation architects well WITHOUT a harness => the counter dies on INFORMATION grounds, not capability grounds: design input is knowledge about YOUR product — intent, constraints, domain decisions — and that data is not in any model's weights, because it cannot be; it must be provided, structured by an encoded method, and recorded; a stronger model produces better output from the same input, which RAISES the value of input quality; the thin residual is a future model eliciting input conversationally — and elicited input still needs the method and the record, which is this product; the KILL-CRITERION — a model that knows your product's intent without being told — is impossible in principle, not merely unobserved
- STEELMAN the check itself: one evening web probe by the interested party cannot carry a universal "nobody occupies the middle" — the searcher chose the categories, and a motivated skeptic finds what the choice hid => the red team ran the counter-probe at the strongest unexamined neighbor, the requirements-management suites, and it LANDED: Jama scores requirements against INCOSE and EARS rules before save, a real occupant of the input-quality corner (@[[ref-rm-suites]]); the position is amended — the claim narrows from nobody-in-the-middle to nobody-couples-the-three; the every-round prior-art rule is the standing mitigation, and the KILL-CRITERION is concrete: a tool that forces design input by an encoded method AND drives a governed agent AND records adjudication as the spec — found, the middle closes and this vision re-aims
- residual attacks on the machinery, not the frame => the manual fan coverage and the interim table editor are both registered with owners; neither touches the frame's worth

## verdict

pass — the frame is interesting on its own terms: an open middle, an evidenced cost, a proven method carried by a tireless agent, and a register that owns its honest weaknesses. The prior-art check ran at this gate and the red team counter-probed the check itself; the claim came out NARROWER and stronger — the requirements-management suites hold the input-quality corner for human authors, the approval layers hold action-gating, and nobody couples forced design input, an encoded method, and a governed agent on one record. The regulatory tailwind strengthens the record half. The pressure test folded cleanly. The bless is the owner's.

## follow_up

- the owner's bless makes the vision axiomatic; a rejection names what to redo
- past the gate, M2 begins: the context draws, and the deep prior-art state follows with its wired guidance
- the queued engine bundle (form-lane designs, reference lint, row sync, walk-all fans) lands at the next unbound stand

## choice

iterations/i1/log-risks
