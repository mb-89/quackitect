---
form: derived-house
by: agent
signed_off: 2026-08-09T13:08:21.049Z
authors: agent
files:
---

# Evidence form / derived-house

## current_situation

[[cand-derived-house]] carries its name, its statement, its five picks and why it was drawn. The other three sections are empty, which is exactly what build_chart leaves behind.

The baseline it varies is the system as it stands today: a token machine, evidence forms over a trace corpus, a worktree per record, an installer, and a note store on disk.

## built

Three sections written into [[cand-derived-house]], and nothing minted beside it.

- How it works — the seams. The position becomes the only remembered state; everything else is recomputed from the corpus, git carries the record lifecycle, and the note store collapses into the log.
- What it costs — the rebuild is measured at 322 nodes, 465 ms cold and 119 ms warm on 2026-08-09. Growth past a few thousand nodes is NOT KNOWN and is recorded as a risk rather than a number.
- What it leans on — four beliefs, and one of them is already false. Reading credit lives outside the corpus, so this candidate loses it on every restart. That happened four times this afternoon, seven documents each time.

Nothing here is scored. The comparison belongs to evaluate-set.

## follow_up

The four remaining lines compose next: [[cand-signalled-line]], [[cand-living-form]], [[cand-thin-worktree]] and [[cand-pushed-step]]. They are independent, so none waits on this one.

When all five stand, the bar on the machine's end releases and the walk carries to cut-criteria.

## anything_else

## read

change, the next expedition's; second call to finish; Has the way of
