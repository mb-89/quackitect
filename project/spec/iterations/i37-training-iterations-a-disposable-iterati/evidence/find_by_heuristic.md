---
form: find_by_heuristic
by: agent
signed_off: 2026-08-19T17:51:22.715Z
authors: agent
files:
---

# Evidence form / find_by_heuristic

## current_situation

i37 stands at find_by_heuristic, the fifth finder of enumerate-space. Four finders are signed with sixteen options between them.

Eight catalogue rules are held against the one cluster this record owns. Six bite, one bites weakly, one finds nothing.

Four of the hits yield an option.

## applies

yes — the catalogue is short and the cluster is one, so the sweep is complete rather than sampled.

## sweep

| Group what changes together; separate what changes apart. | cluster-the-benchmark-run | BIT WEAKLY, and it confirms partition-functions rather than adding anything. All eight functions live and die with one binding, so they change together. The one that changes apart is choosing, which runs before a binding exists — the same thin link the cluster already names. |
| Make the common case cheap; make the rare case possible. | cluster-the-benchmark-run | BIT. The common case is a person naming an iteration. The rare case is a draw by size with a recorded seed. find_without already asked whether the rare case earns its machinery, and this rule says build the common one cheaply and leave the rare one possible rather than absent. |
| One source of truth; everything else derives. | cluster-the-benchmark-run | BIT HARD, and it is the strongest hit in the sweep. Three lists decide what a lane verb may see and they disagree. The conditional concealment needs one predicate that every path-taking verb consults. |
| Push decisions to the last responsible moment. | cluster-the-benchmark-run | BIT AGAINST SOMETHING ALREADY RULED, which is worth recording. The stop point is chosen when a run starts. This rule argues it could be chosen while the run is walking — stop when the budget runs out rather than at a named gate. The owner ruled configurable with the whole walk as default, and nothing here is strong enough to reopen it. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-benchmark-run | BIT HARD. The ceiling is a check today, and a check fails open when it errors. A history that has no future in it cannot be asked for a future commit at all. |
| Small interfaces between big parts beat the reverse. | cluster-the-benchmark-run | NOTHING. The cluster's interface to the rest of the machine is already one flow in and one flow out, and its internal seams are all within one lifecycle. |
| If it must be remembered, it must be recorded. | cluster-the-benchmark-run | BIT, and it sorted the report's conditions into two kinds nobody had separated. The matrix hash, the se version, the rewind commit and the iteration id are all derivable later. The model, the effort and the harness are properties of a session and vanish with it. |
| The default should be the safe thing. | cluster-the-benchmark-run | BIT. The register already has the ceiling failing closed per request. This pushes it earlier: a run that cannot establish its own ceiling never binds, so one refusal names one cause instead of a report full of them. |

## options

- [[opt-a-reachable-future-commit-is-made-unrepresentable-rather-than-checked]]
- [[opt-one-visibility-rule-is-the-single-source-and-every-verb-derives-from-it]]
- [[opt-a-run-that-cannot-establish-its-own-ceiling-refuses-to-start]]
- [[opt-the-conditions-are-recorded-because-they-cannot-be-recovered]]

## follow_up

- The last two finders run next: probing and transforming. Then build_chart, converge and the rest of M4.
- THE UNREPRESENTABLE CEILING IS THE MOST IMPORTANT OPTION IN THE STATE and it may not be buildable through the lane's existing verbs. That is a question for run-spikes at M6 rather than for the box.
- The single-visibility-rule option is the same thing the standing work token asks for, so this iteration's need and the token's fix are one piece of work rather than two.
- The conditions split — derivable versus unrecoverable — answers the trim question find_without raised and should be carried into the box as an argument rather than as a separate row.

## anything_else

ONE RULE BIT AGAINST SOMETHING ALREADY RULED, AND IT IS RECORDED RATHER THAN QUIETLY DROPPED.

Push decisions to the last responsible moment argues that a stop point should be decided while the run walks — stop when the budget is spent — rather than chosen before it starts.

THE OWNER RULED CONFIGURABLE WITH THE WHOLE WALK AS DEFAULT. Nothing found here is strong enough to reopen that, and the case against reopening is good: a run that stops at an unpredictable point cannot be paired with anything, and pairing is the whole product.

IT IS WRITTEN DOWN BECAUSE A SWEEP THAT SILENTLY SKIPS ITS DISAGREEMENTS IS NOT A SWEEP. If the budget ever becomes the binding constraint, this row is where the argument already sits.

THE SECOND THING WORTH SAYING: two of the four options are about the ceiling, and both are about it failing quietly rather than about it working. That is the third state in a row where the design's attention goes to the silent failure rather than the loud one, which is the right instinct for an instrument.
