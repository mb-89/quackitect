---
form: cut-criteria
by: agent
signed_off: 2026-08-19T18:46:34.324Z
authors: agent
files:
---

# Evidence form / cut-criteria

## current_situation

i37 stands at cut-criteria. All three candidates are composed and the drawing that holds them is authored.

The state offers 109 standing requirements as rows. This iteration's own three fitness candidates are NOT among them, which is recorded below rather than worked around.

Three rows survive the cut. Everything below the line is out by position.

## cuts

1. [[req-a-wrong-act-never-passes-silently]] — IN. The only standing criterion the three curves genuinely separate on. The guarded run makes the wrong act unexpressible; the other two catch it with a check plus a control.
2. [[req-fallen-condition-named]] — IN. The refusing run turns a fallen condition into a refusal to bind, which is a different answer from the other two and is what this criterion grades.
3. [[req-walk-survives-host-swap]] [cutoff] — IN, and last. The conditions cell divides the candidates, and the model, the effort and the harness are the three no host records.
4. [[req-first-green-needs-a-red]] [cut: all three curves inherit the same M7 behaviour from the re-walked iteration, so it grades none of them against another]
5. [[req-call-answers-in-one-second]] [cut: a benchmark run is not a user-facing surface and no curve changes any answer's latency]
6. [[req-newcomer-leaves-able-to-ask]] [cut: no curve touches what a newcomer meets]
7. [[req-resume-needs-no-person]] [cut: the owner ruled a person triggers every run, so resuming without one is not a property any candidate is trying to have]

## follow_up

- evaluate-set scores the three candidates against what survived here, plus this iteration's own three criteria.
- THE DECIDING AXIS IS ONE THING: how much is paid so the guard cannot fail quietly. Every surviving row grades some part of it.
- ONE CELL SWAP IS ALREADY NAMED and evaluate-set should consider it as a fourth curve: the refusing run with the thin run's conditions cell.

## anything_else

THIS ITERATION'S OWN THREE CRITERIA ARE NOT ON THE OFFERED LIST.

The rows here are 109 standing requirements. `req-a-bound-run-resolves-no-commit-newer-than-its-rewind-point`, `req-a-benchmark-report-carries-the-conditions-of-its-run` and `req-the-benchmark-history-is-unreadable-while-a-run-is-bound` are absent, though all three were weighed at derive-criteria and all three carry `fitness: true`.

THAT LOOKS LIKE THE SAME DEFECT THE OWNER NAMED on 2026-08-19, when they said a bug in the comparison count was being fixed by another agent. Recorded here rather than routed, because deciding which record a finding belongs to is the owner's.

WHAT IT MEANS FOR THIS CUT. The three criteria that actually decide between these candidates could not be ranked in this field. They are ranked on their own nodes, where derive-criteria put them, and the order stands: the ceiling first, then the conditions, then the concealment.

SO THE SURVIVING THREE ROWS ARE NOT THE WHOLE DECISION SET. They are the standing criteria that still bear on it, and evaluate-set has to read both.

WHY THE CUT IS SO DEEP. 106 of 109 rows are out. This iteration adds one narrow mechanism to a large product, so almost nothing standing differentiates between three curves that vary in ten cells. A shallow cut here would pad the evaluation with rows that score identically for all three, which reads as thoroughness and is noise.
