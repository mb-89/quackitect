---
form: gate-architecture
bless: blessed by agent
by: agent
signed_off: 2026-08-20T07:31:14.239Z
reopened: "2026-08-20T07:31:11.829Z — a feeder re-signed above it after the v3 merge moved the rigor matrix and the M6 spikes moved the winner"
authors: agent
files:
---

# Evidence form / gate-architecture

## current_situation

gate-architecture, reopened 2026-08-19 because the M6 spikes moved the winner and the whole M5 chain re-signed beneath it.

WHAT CHANGED SINCE THE FIRST BLESS. The ceiling is structural rather than checked. The rewind is a three-way split. Three at-risk rulings are discharged. The concealment moved from a deferred dependency into this iteration's scope by owner ruling.

WHAT DID NOT CHANGE. Three elements, four interfaces, eight functions, every crossing named, and no boundary moved.

## round_0_verify

- evidence vs claims: every claim this milestone carries is now measured rather than argued. 1723 files at depth 1 with i33's start commit unresolvable, against a control of 71. 4229 microseconds for the ancestry test that is no longer needed. 1281 of 1282 calls attributable. Four exclusion lists, one empty.
- types: `npx tsc --noEmit` clean after the packaging cherry-pick, the only code change on this branch so far.
- lint: no code authored by M5. Every corpus write passed the engine's reader at the boundary.
- tests: none, and none earned. The battery belongs to verification.

## round_1_validate

- exercised against the goal: the structure survived its own prototype milestone with no seam moving. Two nodes changed content; no element boundary and no interface was added or removed.
- missing: nothing this milestone owed. How a run is reached was answered at decompose-structure and stands.
- wrong: the millisecond bound on if-benchmark-binding-to-guard was the agent's own invention, found by the owner asking where it came from, and is replaced by a derived bound with its divisor named as unmeasured.
- out of scope: nothing. The visibility rule moved INTO scope by owner ruling rather than out of it.
- prior art: unchanged and applied. The calibration transfer caught nothing this round because the round produced no absence-shaped measurement.

## goals_served

- A benchmark run re-walks a named archived iteration from the commit before that iteration started.: SERVED AND MEASURED. The two-command fetch stands the tree; 1723 files at depth 1 on i33.
- The lane refuses to resolve any commit that is not an ancestor of the run's rewind point, so the original answers are unreachable while the run is bound.: SERVED BETTER THAN ASKED. Nothing is refused because nothing is there. i33's own start commit does not resolve in the fetched tree.
- A run is chosen by iteration id, or drawn by size, and a draw records its seed so it repeats.: SERVED AND NARROWED by the owner's ruling that a person triggers every run.
- Runs cycle through the archive rather than repeating the last one, and the reports folder is the only scheduler state.: SERVED IN STRUCTURE. if-benchmark-report-to-binding is the only backward crossing and is read before the binding opens.
- A run fills a benchmark-run item template, and the filled report is the only thing committed.: SERVED. el-benchmark-report owns it and the template costs no engine change.
- The benchmarks folder is concealed while a run is bound and visible everywhere else.: NOW IN SCOPE rather than deferred. Measured cost: four call sites across three files.
- Where a run stops is configurable, and the whole walk is the default.: SERVED BY RULING, two fields on the report.
- vp-rigor-without-toil gains one success criterion measuring whether the machine carries more of the weight over time.: STILL NOT SERVED, and now three gates old. It is authored where value is derived, which this column has not reached.

## bound_breaches

- if-agent-harness-to-entrypoint: two breaches now, both the same fault. The lane restarted twice and silently dropped the owner's autonomy grant each time, coming back at tactical. The second cost four calls of inspecting a signed gate form before the dial was checked. Captured as note-ef85e0c86b5e and note-a02771bee06a. No third occurrence since the dial was restored.

## round_2_red_team

- STEELMAN: the gate is being re-blessed on evidence that overturned its own first ruling, which means the first bless was wrong => the first bless was correct on the evidence available and said so, and wrote the falsification condition that this round met. A gate that cannot be overturned by its own prototype milestone is a rubber stamp.
- STEELMAN: three risks were discharged in one round without new mitigation work => they were discharged by removing the mechanism they hinged on. There is no test to fail open and no per-call cost, so the risks have no subject left.
- The winner's incremental report is still untested and now ships => named in the declaration as inherited rather than endorsed, and it gets M7's first test.
- The visibility rule entering scope grows this iteration after two gates => it does, by owner ruling, and the cost was measured before the ruling rather than after: four call sites, three files.
- The eighth goal has now survived three gates unserved => it cannot be served before the state that derives value, and calling it out at every gate is the honest alternative to pretending otherwise.

## raid_additions

- [[raid-ar-the-benchmark-history-is-unreadable-while-a-run-is-bound]]

## verdict

pass — the structure survived its prototype milestone with no seam moving, and every claim it carries is now measured rather than argued.

WHAT THE BLESS ENDORSES: three elements, four interfaces, the structural ceiling, the three-way rewind, and the visibility rule in scope.

BLESSED BY THE AGENT under the owner's grant of 2026-08-19, which was widened the same day to cover every gate in this iteration including gate-implementation.

NO DISSENT SURVIVES THIS ROUND. The one recorded at the first bless was the evidence-grade override, and M6 answered it.

## follow_up

- gate-prototype closes M6, then M7 opens.
- TRUNK IS MERGED BEFORE author-tests, by owner instruction. It carries things this build needs.
- M7 BUILDS THREE THINGS: the benchmark run itself, the benchmark-run item template, and the visibility rule that four call sites need.
- THE FIRST TEST M7 WRITES should cover the incremental report, which is the one cell of the winner that carries no evidence.

## anything_else

THIS GATE HAS NOW BLESSED THE SAME MILESTONE TWICE WITH DIFFERENT CONTENT, and the record shows why that is the process working rather than failing.

The first bless carried a dissent: the winner's ceiling was checked rather than structural, and if the M6 spike showed a truncated history readable, the ruling should be revisited.

THE SPIKE RAN. The ruling was revisited. The chain re-signed beneath this gate rather than being edited under it.

WHAT THAT COST: four reopens, each with its reason on the record. What it bought: a design whose central mechanism was measured before anything was built against it.

THE ALTERNATIVE WAS AVAILABLE AND CHEAPER. Amend the wording, leave the signatures, ship the checked ceiling. It would have produced a benchmark that spends 4229 microseconds per resolution guarding against a commit it could simply not have fetched.
