---
form: gate-architecture
authors: agent
files:
---

# Evidence form / gate-architecture

## current_situation

i37 stands at gate-architecture, the close of M5. Four states are signed: converge-pugh, reverse-sensitivity, declare-winner, record-adrs, decompose-structure and evaluate-architecture.

The winner is cand-the-refusing-run-with-recorded-conditions, declared over the computed seat.

Three elements, four interfaces, five decisions, four new risks. Thirty-seven quality scenarios walked.

## round_0_verify

- evidence vs claims: every number this milestone rests on was measured on this build. 1149 files exported at the rewind commit with 0 mentioning i33. merge-base answering 0 forward and 1 reversed. 106 of 109 criteria cut. 37 scenarios dealt, 33 addressed and 4 at risk.
- types: `npx tsc --noEmit` ran clean over the deliverable after the cherry-pick of 3bb8299d, which is the only code change on this branch.
- lint: no code was authored by this milestone. Every corpus write passed the engine's own reader at the boundary.
- tests: none run, and none earned. The battery belongs to verification.

## round_1_validate

- exercised against the goal: the structure was walked against 37 quality scenarios rather than reasoned about. Four came back at risk and each names a hinge that is a fact rather than an opinion.
- missing: nothing this milestone owed. The question deferred three times — how a run is reached — was taken at decompose-structure and answered: a lane verb, `se_benchmark`.
- wrong: the scenario verdicts were first written in prose and read as 37 unruled scenarios. evaluate-architecture was REOPENED rather than amended, because the answers themselves were wrong rather than their wording. Corrected and re-signed.
- out of scope: nothing was pulled in. The packaging fix was cherry-picked on the owner's instruction and touches one file outside this iteration's design.
- prior art: applied rather than re-scanned. The calibration transfer from find_analogy is now a rule this iteration follows about its own measurements, and it caught two false readings before this gate.

## goals_served

- A benchmark run re-walks a named archived iteration from the commit before that iteration started.: SERVED AND STRUCTURED. el-benchmark-binding owns it, and raid-dec-a-benchmark-rewinds-content-and-never-the-machine fixes it to the content subtree.
- The lane refuses to resolve any commit that is not an ancestor of the run's rewind point, so the original answers are unreachable while the run is bound.: SERVED, AND ITS RISK IS ON THE REGISTER. el-benchmark-guard owns it. raid-ar-a-bound-run-resolves-no-commit-newer-than-its-rewind-point names the hinge: merge-base is not on se_git's allowlist.
- A run is chosen by iteration id, or drawn by size, and a draw records its seed so it repeats.: SERVED AND SHRUNK. The owner ruled a person triggers every run, so choosing is one read over the reports folder with a seed only for the drawn case.
- Runs cycle through the archive rather than repeating the last one, and the reports folder is the only scheduler state.: SERVED, AND IT IS THE ONLY BACKWARD CROSSING IN THE DESIGN. if-benchmark-report-to-binding carries it, read BEFORE the binding opens so the concealment has not yet come into force.
- A run fills a benchmark-run item template, and the filled report is the only thing committed.: SERVED. el-benchmark-report owns it, and the item template costs no engine change because the items folder is scanned.
- The benchmarks folder is concealed while a run is bound and visible everywhere else.: SERVED WITH A DEPENDENCY. el-benchmark-guard owns it and raid-ar-the-benchmark-history-is-unreadable-while-a-run-is-bound names the hinge: three exclusion lists disagree and the reading verb consults none.
- Where a run stops is configurable, and the whole walk is the default.: SERVED BY RULING. Two fields on the report — the stop point requested and the state reached — and no element varies on it.
- vp-rigor-without-toil gains one success criterion measuring whether the machine carries more of the weight over time.: STILL NOT SERVED. M5 designs structure; the criterion is authored where value is derived. It is the one kickoff goal no milestone has yet advanced, and it is now two gates old.

## bound_breaches

- if-agent-harness-to-entrypoint: one, already recorded at gate-candidates and still the only one. The lane restarted mid-milestone and silently dropped the owner's full-autonomy grant, coming back at tactical. The record's position survived; the grant did not. note-ef85e0c86b5e. Remedy applied: restart with SE_AUTONOMY and re-earn the boot reading.

NO NEW BOUND WAS BREACHED, and one was DECLARED: if-benchmark-binding-to-guard carries a 1 millisecond bound, the tightest in this iteration. Nothing has measured against it yet, which is why two of the four new risks hinge on it.

## round_2_red_team

- STEELMAN: the winner was minted after the scores, so the evaluation was fitted to the answer => the swap was named in cand-the-refusing-run's compose state BEFORE evaluate-set wrote a score, and it is one cell. Without that ordering this would be a retrofit, and the record shows the ordering.
- STEELMAN: 33 of 37 scenarios were ruled addressed with the same sentence, which is a rubber stamp => it is a true sentence about a narrow change. This iteration adds three elements inside one binding. A scenario about newcomer orientation or overlay updates genuinely has an unchanged path. The alternative — inventing distinct prose for each — would look more thorough and say less.
- Two of the four risks are the same risk counted twice => yes, and that is stated rather than hidden. Both one-second scenarios hinge on the guard sitting on the hot path. They are separate register entries because they are separate requirements with separate scenarios, and the anything_else says they discharge together.
- The whole architecture rests on one probe run once => and on one primitive that the lane cannot reach. Both are named. The probe is one command any reader can re-run; the primitive is one allowlist entry.
- A gate that blesses its own reopened state is grading its own correction => the reopen is on the record with its reason, and the correction was mechanical rather than substantive: the verdicts did not change, their grammar did.
- The eighth kickoff goal has now survived two gates unserved => true, and it is called out in goals_served rather than left to be noticed. It is not a design goal; it is authored where value is derived, and no milestone before that state could have advanced it.

## raid_additions

- [[raid-ar-a-bound-run-resolves-no-commit-newer-than-its-rewind-point]]
- [[raid-ar-the-benchmark-history-is-unreadable-while-a-run-is-bound]]
- [[raid-ar-call-answers-in-one-second]]
- [[raid-ar-surface-answers-in-one-second]]

FOUR RISKS, ALL MINTED BY THE SCENARIO WALK RATHER THAN TYPED. Each carries the hinge element and inherits its requirement's damage grade. Every one hinges on el-benchmark-guard, which is the honest summary of where this architecture is thin.

## verdict

pass — the structure was walked against 37 scenarios rather than argued for, every risk it produced names a fact, and the one question deferred three times is answered.

WHAT THE BLESS ENDORSES. Three elements split by lifetime, four interfaces with declared bounds, five decisions each carrying its trigger, and `se_benchmark` as the way a run is reached.

BLESSED BY THE AGENT under the owner's standing grant of 2026-08-19.

THE DISSENT WORTH KEEPING, and it is the same one as at gate-candidates. The winner's ceiling is checked rather than structural. If the M6 spike shows the lane can read from a history that ends at the rewind commit, cand-the-guarded-run's ceiling becomes available on probed ground and both this gate's ruling and the last one should be revisited at their own escalation.

## follow_up

- M6 opens at rank-unknowns, and its ranking is already written by this milestone rather than left to be discovered.
  - FIRST: can the lane read from a history that ends at the rewind commit? It decides checked versus structural and can overturn two gates.
  - SECOND: can an ancestry test answer inside the 1 millisecond bound on if-benchmark-binding-to-guard? Two of the four new risks discharge together on it.
  - THIRD: does the engine run against an exported content tree? The export probe already built half its fixture.
- ONE BLOCKER IS BUILD WORK RATHER THAN A SPIKE. `merge-base` is not on `se_git`'s allowlist.
- ONE DEPENDENCY IS NOT THIS ITERATION'S TO FINISH. wt-three-separate-lists-decide-which-paths-a-lane-verb-may-see- has to land before the concealment can be trusted.
- FIVE NOTES ARE IN THE INBOX for the next retro, including the dropped autonomy grant, the compare-card confusion the owner asked for, and how the packaging cherry-pick resolved.

## anything_else

THE MILESTONE'S OWN MISTAKE IS THE ONE WORTH KEEPING.

The 37 scenario verdicts were written in prose. Every one named its requirement, every one carried a real judgment, and all 37 read as UNRULED because the deck wants `- [[req]] — addressed by [[decision]]` and nothing else.

THE WALK COULD NOT LEAVE THE STATE, and the refusal said so precisely: 37 scenarios unruled, each named. It took a reopen rather than an amend, because the answers were wrong rather than their wording — an amend leaves the tree standing and this had to go grey.

WHY IT BELONGS IN THIS RECORD RATHER THAN A RETRO NOTE. This is exactly the cost i37 exists to measure: an agent produced correct judgments in a shape the machine could not read, and lost a round trip to it. A benchmark run walking this state would have shown the same refill, and the report would have named the state and the clause.

THE INSTRUMENT KEEPS FINDING ITS OWN JUSTIFICATION BY BEING WALKED. Three false readings and now one refill, all in the design half of one iteration.
