---
form: gate-motivation
bless: blessed by agent
by: agent
signed_off: 2026-08-13T10:39:05.011Z
authors: agent
files:
---

# Evidence form / gate-motivation

## current_situation

The delta is built and green. Six mechanisms landed: the reading credit, the claim pool, the mechanical size parse, the frontier reopen, the scaffold entry guard, and the per-size field trim.

The minor column itself was re-authored under this iteration rather than before it. That is unusual. The owner accepted it explicitly, on the grounds that this is the first minor ever walked and the first walk is where the shape gets found.

What remains is the record: the requirements, the design specs and the trace that say what was built and prove it.

## vision_scope_stated

The resident vision is axiom and stays out of scope. What this gate judges is the delta alone: is the walk's durability worth this iteration's cost.

## problem_agreed

Yes, and it was reported from use rather than inferred.

- Reading was re-owed on every engine restart. Observed at every boot of this session until the fix; the first boot afterwards came up with nothing owed.
- A declared build step was walked through in silence. i3 passed specify-build, seeded nothing, and build-steps reported itself done.
- The walk stood in eight states at once after a size change. The owner opened the mirror and saw M0's gate and M3's requirements both live.

Each was found by using the thing, not by reading the code.

## prior_art_positioned

NOT COMPARED, and here is why. This is an internal harness with no comparable product I have run. A vendor's feature list would be evidence a feature is claimed, never that it beats what stands here, and a comparison written from one is fabrication.

What can be said honestly: the mechanisms borrowed are ordinary ones. Content-keyed read caching, token markings on a state machine, and a per-column compile of a shared row set. None is novel; the work is in wiring them to refuse rather than to warn.

## success_measurable

Four checks, all run.

- Boot comes up with no reading owed. Observed live after the credit fix.
- A placeholder sub-machine refuses entry. Pinned by scaffold-entry.test.ts; the refusal names the seeding state.
- A reopen leaves one token on a sequential chain. Pinned by four cases in tokens.test.ts; observed live re-entering i3.
- A trimmed form serves fewer questions. draft-vision served one field where it declares four. Observed live, not only tested.

The battery stands at 1140 of 1140, 119 suites, preflight green.

## risks_logged

Two, both new today and both unexercised.

THE TWELVE STRIKES ARE UNTESTED. Each struck row now carries an escalation rule instead of a question. No minor has yet hit one.

THE TRIM HAS TWO USERS. draft-vision and frame-delta. Every other row serves its whole form wherever it applies, so the mechanism is proven but barely deployed.

## round_0_verify

- evidence vs claims: every claim in this gate names a file, a test file or a live observation. The one claim NOT proven by a walk is the scaffold entry refusal — scaffold-entry.test.ts asserts it ships by reading session.ts, and says so in its own comment.
- types: clean. The typechecker runs on every patch through the lane and refused three times today, each time correctly.
- lint: clean. biome reports 7 infos, all pre-existing string-concatenation suggestions in engine/forms.ts and one test helper, none introduced here.
- tests: 1140 of 1140, 119 suites, preflight green. Three reds surfaced during the change and all three were tests pinning the OLD minor shape; they were re-authored to the new ruling, not deleted.

## round_1_validate

- exercised against the goal: yes. All three defects were reported by the owner from use, on the day each bit, with the symptom described before any cause was known.
- missing: the entry refusal has no walking test. It is pinned by inspection only, and that is named in the test file rather than hidden.
- wrong: nothing found wrong. The one correction made mid-walk was my own test regex, which used \\s and swallowed the next list item.
- out of scope: the patch column, the mirror's rendering of a reopened step, and retiring the method-write refusal. All three were raised and all three were left, deliberately.
- prior art: NOT COMPARED, and here is why. This is an internal harness with no comparable product anyone here has run. A vendor feature list would be evidence a feature is claimed, never that it beats what stands here.

## round_2_red_team

- striking twelve states removes twelve places where "this is actually a major" could surface => real, accepted, and recorded as an issue with a trigger. The counter is that those states were answered "unchanged" without looking, which teaches that forms can be satisfied without looking. Neither side is proven; the first minor that must escalate settles it.
- the field trim moves a judgment from the walker to the row author => real, and that IS the point — a judgment made once and reviewable beats one made fresh every walk. The residual danger is a badly-authored omit, mitigated because absent means asked, so it needs a deliberate wrong edit rather than an oversight.
- decompose-structure now carries alone the escalation duty M2 and M5 used to share => accepted. Its own note already carried the rule before today, so this concentrates load rather than inventing it.
- the trim has only two users, so the mechanism is proven but barely deployed => accepted, and named in follow_up rather than dressed as coverage.
- gate-motivation is being blessed by the same hand that built the work => true at autonomy 1, by the owner's standing ruling. The mitigation is that the rounds cite sources a reader can follow, so the reasoning is checkable even where the signature is not independent.

## raid_additions

- [[raid-iss-minor-lost-its-looking-places]]
- [[raid-asm-an-omit-is-authored-honestly]]

## verdict

pass — the delta is real, reported from use three times, and built with enforcement rather than a warning. The reservation is recorded and not waved: the minor column lost twelve looking-places today and nothing has yet tested whether the escalation rules replace them. That is an issue in the register with a trigger, not a reason to hold the gate.

## follow_up

Three things ride forward from this gate.

THE STRIKES ARE UNTESTED. Twelve rows now carry escalation rules instead of questions, and no minor has hit one. Recorded as raid-iss-minor-lost-its-looking-places, with the first mid-walk escalation as its trigger.

THE TRIM HAS TWO USERS. draft-vision and frame-delta. Every other row still serves its whole form wherever it applies. Recorded as raid-asm-an-omit-is-authored-honestly.

THE ENTRY REFUSAL WANTS A WALKING TEST. Today it is pinned by inspection, which proves it ships and not that it fires.

## anything_else

