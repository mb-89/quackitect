---
form: onboard-retro
by: agent
signed_off: 2026-08-13T08:15:14.435Z
authors: agent
files:
---

# Evidence form / onboard-retro

## current_situation

i3 is bound and standing at its onboarding retro. The retro window opened 2026-08-12T15:52:54Z and holds 1065 calls.

The inbox is empty for the first time in this record's life. It stood at 47 when the retro opened.

The parked backlog stands at 201 after the walk. Four of its notes moved into i3's own scope, because they carry the design for exactly the three things i3 exists to fix.

Three method changes landed on trunk before this record was entered. Requirements now carry a behaviour model where they demand behaviour over time. The package row now owes an emit_back list. idle.md no longer demands a handover that nothing reads.

The claim pool was fixed and specified earlier the same day, unbound. A second machine can now claim an iteration on a product that never had a claims branch.

What stands against the record: the debt sweep run from inside a record sees two of three debts and says nothing about the third, and the memory drain cannot run at all through the lane.

## field_feedback

The owner drove this session directly, so the field feedback is theirs and it arrived live rather than as a report.

Six things came back.

- The autonomy rung buttons take over a second to show their feed line.
- The log still prints the old numeric autonomy value instead of the rung name.
- The claim system did not work in the real world. A cloud agent worked i8 with no claim recorded anywhere.
- Seven atomic requirements for one small feature reads as ceremony, and the one demand that mattered was missing.
- SE-C-134 blocks often, and leaving an iteration to change method is a time waste.
- A retro should render where the time went, so hotspots get fixed rather than guessed at.

Every one was captured as a note. Every one is now drained.

Three were acted on the same day. The claim pool was fixed and specified. The requirements method gained its behaviour-model rule. The emit-back was built into the package row.

## notes_drained

- done: 34 notes, each drain naming the version-plan section or the file that already carries it
- obsolete: 1 note, whose own ruling was that there is nothing to build
- carried: 2 notes, being idle.md's stale handover demand and the weak-model register entry now minted
- backlog: 10 notes, each parked with its re-entry condition named
- inbox: zero, down from 47 when this state opened
- backlog walked: all 207 parked notes read by title and re-entry condition
- backlog pulled: 4 notes into i3, carrying the design for all three charter items
- backlog closed: 2 notes drained done, the contract rule and the pre-push battery gate
- backlog kept: 201 notes whose conditions name iterations not yet started

## call_log_mined

- The window opens 2026-08-12T15:52:54Z and holds 1065 calls with 61 failures, a 5.7% failure rate.
- se_run stands at 3 calls of 1065, which is 0.28%. The method's own baseline is 3249 of 28612 on 2026-08-07, which was 11.35%.
- Each of the three shell calls names a verb the lane lacks: copy a file inside the repo, undo the last commit, read a subagent's host-persisted output.
- se_update fails 46 times in 326 calls, a 14.1% failure rate. It is the dominant refusal by a wide margin.
- SE-C-133 fired 31 times. The stall check refuses narration every five calls during a uniform sweep, and a 47-note drain is exactly that shape.
- SE-C-120 fired 11 times. It also auto-applied a chained brief into three phantom checklist items during this retro, which is note-a96bd6685275 recurring.
- 134 calls broke the one-second rule.
- se_seed_iteration is slow every single time: 19 of 19 over a second, and the seed push to the remote is the suspect.
- se_pull breaks the one-second rule in 6 of 29 calls, on the hottest path there is.
- The agent voids were NOT computed. No verb reports them, and doing it by hand means eleven queries to produce one number.
- Tool audit: nothing looks obsolete. se_file_move, se_shoot and se_web_fetch are rare but right. se_update is the one worth improving, and its failure rate says so.

## waste_leads

- The SE-C-134 escape cost five calls to make a one-call edit. Four of the five did no work.
- The debt sweep run from inside a record sees two of three debts, and says nothing about the third.
- The 47-note drain cost one call per note. There is no batch drain.
- se_survey returns the full 24-iteration list on every page of a backlog walk, so walking 207 notes reshipped it five times.
- The memory drain could not run at all, because the memory lives outside the root.

## promotions

- meth-requirement-authoring gained a behaviour-model section, so an ordered exchange or a lifecycle rides the requirement as text
- the requirement item template gained the matching Behaviour block with its skeleton comment
- M3_10_write-requirements gained a behaviour_modelled set question, forcing the look and recording it either way
- M9_20_package gained the emit_back evidence field, which is the output phase's debt to the shared method
- meth-emit-back was written whole, holding both ends of that seam
- M0_10_onboard-retro now names itself the catching end and points promotions at the previous record's emit_back
- machines/states/idle.md lost a demand that told the agent the engine refuses to end without a handover
- every one of these was made UNBOUND before i3 was entered, so all of them already stand on trunk
- i3's own machine has not been walked past its first state, so it has no local change left to promote

## process_stale

No external state-of-the-art scan was run this retro, and that is a gap rather than a pass.

What WAS compared, and against what.

- The se_run share was compared against the method's own recorded baseline of 2026-08-07. It fell from 11.35% to 0.28%.
- The requirements method was checked against the sources it already cites: ISO/IEC/IEEE 29148:2018, the INCOSE writing guide, and Mavin's EARS. The behaviour-model rule added today sits beside them rather than against them, because none of the three covers what exists before a first trigger.
- The debt register was compared against its own triggers, and both visible debts were re-affirmed with the look dated on the entry.

What was NOT compared, and why.

No outside practice was surveyed for the retro method itself, for the narration lane, or for the claim mechanism. Doing that properly needs citable sources read end to end, and this retro spent its length on a 47-note drain and a 207-note backlog walk.

The honest statement is that the process has not been checked against the state of the art since the sources listed in meth-requirement-authoring were gathered. That check is owed, and naming it here is worth more than a blank.

## follow_up

- the reading credit keys to document content hashes and survives a reload, carrying note-61b267004f20 and note-6fc953ffcdc8
- a red claim standing at the route's objective serves its fill instead of the standing-still wait, carrying note-1f2cabfb9d0a
- one verb answers why a state is grey, carrying note-936e7a2d67d5 and replacing the grey-probe shell cluster
- the emit_back list is filled at package, naming what this record learned about the shared method

## anything_else

