---
form: probe-assumptions
by: agent
signed_off: 2026-08-13T08:40:29.511Z
authors: agent
files:
---

# Evidence form / probe-assumptions

## current_situation

Nine assumptions stand in the register this record can see, and all nine were looked at.

One turned FALSE, on evidence gathered in this same session rather than on argument. raid-corpus-stays-small named its own trigger, and that trigger fired.

Two hold. One of those is half probed, and the unprobed half decides a design inside this iteration.

Six are honestly unprobed or scheduled, each with its reason written on the node rather than left blank. None was called done for being uncomfortable to check.

WHAT THE STATE COULD NOT SEE is the more serious finding. i2's assumptions are absent from this worktree, so a state whose whole job is probing every standing assumption silently probed a subset.

## probes

- raid-corpus-stays-small: FALSE, and it is now an issue. Its own trigger read "when a pull takes longer than a second". Measured this window: 6 of 29 se_pull calls past one second, /widget/machine at 3285 ms with 3275 ms inside the machine phase, and 134 of 1065 calls over the rule. The kind is changed and the id kept.
- raid-asm-every-condition-can-say-what-it-wants: PARTIALLY PROBED, holds so far. Five condition types stand under machines/conditions and every one carries a note saying what it wants. The weight-against-dial case answers itself. The computed pile is not counted, and that half decides the verb's contract.
- raid-strip-types-flag-holds: HOLDS, unchanged since 2026-08-07. Node gave type stripping Stability 2 and made it default from v23.6.0. No new check was invented to look busy.
- raid-adjudication-provenance-gap: NO PROBE, and none will be run. Closed by owner ruling 2026-08-07 because the product does not want finer provenance than git already gives.
- raid-lane-works-on-posix: UNPROBED, reason unchanged. The only honest check is a full battery on macOS or Linux, and no such machine is reachable from this session.
- raid-asm-refusals-recover-a-weak-model: SCHEDULED, never probed. The check is one prepared session with a weak model, which the owner holds.
- raid-asm-grey-verb-distinct-from-se-help: SCHEDULED. The check is putting both verb signatures side by side, and se.help does not exist yet.
- raid-asm-session-identity-survives-a-reload: UNPROBED, deliberately. The check needs an se_reload, which would disturb a bound record to answer a question that is not yet blocking. It runs before the credit's storage shape is chosen.
- raid-asm-line-endings-do-not-move-under-us: UNPROBED. The check mutates the tree to answer a design question, so it belongs beside the storage design in this same iteration.
- i2's assumptions: NOT PROBED, and the state could not see them. This record's spec corpus is frozen at seed time, so raid-asm-remote-serializes-claims and its siblings are absent from this worktree. Nothing reported the gap; it was found by hand.

## follow_up

- run the session-identity probe before the reading credit's storage shape is chosen, since a different token changes the design rather than the code
- count the computed pile of grey guards, which is the half of the condition probe that decides the verb's contract
- name the cause behind raid-corpus-stays-small in i12, where its charter already reaches
- re-read req-call-answers-in-one-second, which now rests on an entry known false
- probe i2's assumptions from trunk, since this record cannot reach them

## anything_else

## fallout

- req-call-answers-in-one-second now rests on an entry known false. Its source_refs name raid-corpus-stays-small, which is an issue as of today.
- cand-derived-house assumed the same smallness at gate-candidates on 2026-08-09, measured then at 322 nodes with 465 ms cold and 119 ms warm. The register has grown from 146 requirements to 177 since.
- engine/session.ts recordDone and engine/paths.ts methodFilesIn are the two readers the entry names as unbounded. Neither is bounded by anything but the corpus.
- i12's charter already reaches the work, so it has a home. What it lacked until today was a register entry saying the assumption underneath it is false.
- THE ISSUE ITSELF NOW HAS NO SWEEP. The retro sweeps debts only, so an entry that becomes an issue leaves every mechanism that would re-read it. Recorded as its own note.
