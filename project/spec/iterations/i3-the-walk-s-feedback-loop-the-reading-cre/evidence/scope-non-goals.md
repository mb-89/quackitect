---
form: scope-non-goals
by: agent
signed_off: 2026-08-13T10:36:42.045Z
authors: agent
files:
---

# Evidence form / scope-non-goals

## current_situation

The six items above are built and green: 1140 tests, 119 suites, preflight green.

What remains inside this iteration is the record itself - the requirements, the design specs and the trace that say what was built and prove it.

The minor column was re-authored under this iteration rather than before it. That is unusual and the owner accepted it explicitly: this is the first minor ever walked, and the first walk is where the shape gets found.

## scope

WHAT THIS ITERATION TAKES ON

- Reading credit survives a reload. A document read once is not re-owed after the engine restarts.
- The claim pool opens on its first claim. A missing claims branch is minted by the engine, not by hand.
- The change size is parsed mechanically. One extractor serves both the gate's check and the recorded size.
- A reopen places its tokens on the frontier. The walk stands where it can actually work, not on every reopened step.
- A placeholder drawing cannot be walked into. A seeded sub-machine the authoring state has not written refuses entry.
- A form can be trimmed per change size. The row marks which questions a size does not ask, and the engine drops them.

THE THREAD THROUGH ALL SIX: the machine must enforce what it declares, and must not lose what it earned.

## non_goals

WHAT THIS ITERATION LEAVES

- The patch column. The owner ruled it out of scope today; only minor was re-authored.
- The mirror's rendering of a reopened step. A step reopened but not stood in still draws like one never walked. Noted, not built.
- Retiring the method-write refusal. It cost four detours today, and it stays until the fan-out that replaces it exists.
- Trimming the gates. Nine gates became seven at minor by striking two, not by shortening any gate's own form.
- The scope toggle on the editors. Unplanned, and named as such.

## follow_up

The strikes and the trim are both new, and neither has been tested by a real case that fights back.

TWELVE STRUCK ROWS now carry escalation rules instead of questions. No minor has yet hit one. The first that does decides whether the strikes were right.

TWO ROWS carry a field-level trim. Every other row still serves its whole form wherever it applies.

## anything_else

