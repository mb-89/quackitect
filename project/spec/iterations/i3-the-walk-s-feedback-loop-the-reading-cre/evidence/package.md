---
form: package
by: agent
signed_off: 2026-08-13T14:55:30.578Z
authors: agent
files:
---

# Evidence form / package

## current_situation

The versioned artifact assembles by script. `engine/bin/package.ts` ran to exit 0 in 15 seconds and named its output.

The archive was opened and its contents read rather than trusted. The three things the state's statement demands are inside: README.md, the one-time installer RUNME.ps1, and the editor extension under project/deliverable/vscode with its extension.js, package.json and ATTACH.md.

RELEASES.md rides along, and the whole project tree under project/.

The expanded copy was deleted after the check.

## package

- dist/quackitect-4.0.0.zip

## works

yes — the packager ran clean and the archive opens with its three declared parts inside: the README, the RUNME.ps1 installer and the editor extension. What was NOT done is a real install on a fresh machine, which is tsp-first-run's demonstration and is carried by raid-debt-human-observed-demonstrations. So this is checked by inspecting the built artifact, not by installing it.

## emit_back

- guidance/refusals.md: SE-C-136 added, so a scoped test run with no question refuses ahead of time rather than at the boundary only.
- guidance/authoring/machines.md: the worked example taught `priority: 0.25` with a numeric range while every state note in the tree said a word. Now teaches rung words, and a test reads the page.
- machines/scale.md: `blocked` documented as a priority above the ladder, with why the two sides of the word sit at opposite ends of the range.
- engine/machines/compile.ts: a priority word resolving to 0 is read as blocked-above-the-ladder. Matching the resolved value instead of the string was the fix; the string match let three spellings through to 0.
- tests/scale.test.ts: the tier sweep is now a test rather than an inspection somebody has to remember, and it reads the authoring page as well as the canvases.
- tests/trace-coverage.test.ts: the live offer's lane-verb third is walked mechanically. The row said it could not be, and that was false for the verbs.
- meth-verification-discipline: NOT changed, but two gaps found against it. Its checklist field cannot hold the observation the card demands, and the verification state's fallback into fix-findings can never open. Both noted at must.
- The rigor matrix: twelve rows struck at minor this iteration, each minor_note rewritten to say what the size cuts and why.

## follow_up

THE RELEASE ENTRY IS NEXT, and the version is 4.0.0.

WHAT THE PACKAGE CANNOT CLAIM. It was inspected, not installed. A real first run on a bare machine is tsp-first-run's demonstration, carried by raid-debt-human-observed-demonstrations along with three other specs.

THE ENTRY DOCUMENT SHIPS INSIDE THIS ARCHIVE and still teaches 12 of 35 lane verbs. This delta fixed only the row it made wrong, which is se_test's new question. The rest is a standing gap and rides in the README a newcomer reads first.

## anything_else

