---
form: minor-column
by: agent
signed_off: 2026-08-13T12:15:15.272Z
authors: agent
files:
---

# Evidence form / minor-column

## current_situation

The first minor iteration ever walked found the column too heavy. Twenty work states and nine gates stood, and most of the work states could only answer "unchanged". The owner ruled it milestone by milestone.

## built

Twelve matrix rows struck at minor, each with its minor_note rewritten to carry the ruling and its escalation.

- M1-M2: define-actual, draw-context, map-stakeholders, gate-inputs. A new neighbour or a new stakeholder IS a major, so these could only ever answer "unchanged". gate-inputs folds into gate-requirements.
- M5: record-adrs, evaluate-architecture, gate-architecture. Only decompose-structure survives, because it ALLOCATES new functions into standing elements — and M7's trace-design needs that allocation.
- M6: rank-unknowns, run-spikes, fold-back, gate-prototype. Needing a spike is the tell that this is a major.
- M8: run-demos. The package state already ends with a person using the artifact.

TWO FORMS TRIMMED FIELD BY FIELD, using the mechanism from the previous chunk. frame-delta drops the gap claim and the why-now, which its own note already called inherited. draft-vision keeps only the goal system.

Nine gates became six. Twenty-eight rows apply at minor, plus start and end.

EVERY RULING IS IN THE ROW IT BELONGS TO, so the next project seeded from this repo gets it without anyone re-deciding.

Cases: tests/rigor-matrix.test.ts names all twelve struck and five that must survive; tests/sizes.test.ts re-authored to the new front-step rule.

## follow_up

Recorded as raid-iss-minor-lost-its-looking-places: twelve steps were also twelve cheap places to notice an iteration was really a major. The escalation rules replacing them are untested, and the first minor that has to escalate settles whether they work.

AND STRIKING M6 HAD A CONSEQUENCE NOBODY PREDICTED, found in this same iteration: the promotion scoping keys off M6's fold-back evidence, so the strike silently turned it off. Fixed here; the shape is worth the retro's attention, because a strike can disable a safeguard that lives somewhere else entirely.

## anything_else

