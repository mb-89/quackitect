---
form: gate-release
bless: blessed by agent
by: agent
signed_off: 2026-08-13T14:56:18.921Z
authors: agent
files:
---

# Evidence form / gate-release

## current_situation

The package stands: dist/quackitect-4.0.0.zip, assembled by script to exit 0, opened and read rather than trusted. README, the RUNME.ps1 installer and the editor extension are all inside.

The battery is green at 1151 of 1151, preflight green, biome exit 0.

Two gates already blessed today, each with a recorded override. Implementation at 14:50, validation at 14:53. This gate inherits both and adds nothing new to hide.

THIS IS NOT A TO-MARKET ITERATION, so the market block does not apply.

## market_block


## round_0_verify

- evidence vs claims: the package claim is the strongest in this record, because the artifact was opened and its three declared parts read by name. The battery claim was re-run independently by the tester at 1149 of 1149 before the last two fixes.
- types: clean throughout. Every patch was typechecked at the boundary, and one missing import was caught there before any test ran.
- lint: biome exit 0 on the packaged tree, no new suppression anywhere in the delta.
- tests: 1151 of 1151, up from 1144 at the iteration's start. Seven cases added, all guarding behaviour this delta introduced.

## round_1_validate

- exercised against the goal: the SHIPPED artifact was inspected, not installed. That is the honest boundary of this gate's evidence.
- missing: a real install on a bare machine. tsp-first-run's demonstration, carried by raid-debt-human-observed-demonstrations along with three other specs.
- wrong: nothing found wrong in the package itself. The archive's contents match what the state's statement demands.
- out of scope: the version stays 4.0.0, unchanged by this iteration. No version bump was in the delta and none was invented for the ship.
- prior art: NOT SCANNED at this gate either, and for the same reason as validation. The minor column drops state_of_the_art, and the package is the same shape it shipped in last time. What ships INSIDE it changed; how it ships did not.

## round_2_red_team

- STEELMAN, the case against shipping => The archive contains an entry document that teaches 12 of 35 lane verbs, and it is the first thing a newcomer opens. It contains a README that names verbs the product no longer has in its roadmap section. Nobody installed this package on a clean machine, so the one claim that matters to a new user - that one command gets them to the desk - rests on a script's exit code. Shipping a newcomer-facing artifact whose newcomer path is untested is precisely the gap the first-run spec exists to close, and it is being waved through as debt for the second gate running.
- The answer, partial and honest => The stale README is pre-existing and ships in every release; this delta fixed only the row it made wrong. The install path is unchanged by this delta, so a fresh install would exercise last release's code. Neither answer covers the steelman fully: "unchanged" is an argument for low risk, not for no evidence.
- KILL CRITERION: this is the wrong call if the package is broken in a way the inspection could not see => Looked for. The archive expands cleanly, its three declared parts are present by name, and the extension carries its package.json and entry script. What an inspection cannot see is whether RUNME.ps1 succeeds on a machine without the toolchain. That is exactly tsp-first-run, and it is exactly what the debt row carries.
- Nothing in this delta touched the packager or the installer => Verified: package.ts is unmodified this iteration, and RUNME.ps1 is generated by it. The risk of shipping is therefore the risk that already stood, not one this delta added.
- The same agent built, verified through a tester, and blessed all three gates today => Unanswered structurally. The tester caught two defects the builder missed and one of them would have let a blocked state run at every setting. Nothing prevents a future gate being pressed with no second reader at all.
- ASSUMPTION: that shipping without a first-run demonstration is safe because the install path did not change => Holds only while the install path does not change. The moment it does, this gate's reasoning expires and the demonstration becomes mandatory rather than owed.

## raid_additions

- [[raid-debt-human-observed-demonstrations]]

## verdict

pass with overrides — the package assembles by script, opens clean, and carries the README, the installer and the editor extension it promises. The tree inside it is green at 1151 of 1151 with biome clean. THE OVERRIDE, carried from both earlier gates: the artifact was INSPECTED, never installed, and no must story was demonstrated. THE DISSENT, recorded rather than smoothed: this ships a newcomer-facing archive whose newcomer path rests on a script's exit code, and whose entry document still teaches 12 of 35 lane verbs. It passes because nothing in this delta touched the packager or the installer, so the install path being shipped is the one that shipped last time. That argument expires the moment the install path changes.

## follow_up

SHIPPED AT 4.0.0, unchanged from the last release. The version was not bumped because this delta adds no capability - every change enforces a demand that already stood.

THE DEBT ROW IS THE LIVE FOLLOW-UP. raid-debt-human-observed-demonstrations carries four specs and, through them, six of seven must stories. Its trigger is the consolidation pass that collects everything needing a person.

THE ENTRY DOCUMENT SHIPS STALE. It teaches 12 of 35 lane verbs and names se_next and se_submit in its roadmap, neither of which was ever built. It is the first thing a newcomer opens and it is inside this archive.

FOUR NOTES STAND AT MUST for the retro: the fallback edges no state can ever open, the mid-walk sync, frontmatter written by the agent instead of selected from a schema, and the worktree jumping the owner has ruled the next iteration's work.

## anything_else

