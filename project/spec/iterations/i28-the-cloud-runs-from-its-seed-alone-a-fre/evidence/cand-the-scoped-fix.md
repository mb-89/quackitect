---
form: cand-the-scoped-fix
by: agent
signed_off: 2026-08-15T16:59:44.079Z
authors: agent
files:
---

# Evidence form / cand-the-scoped-fix

## current_situation

M4's first candidate elaboration. The morphological chart drew four lines and this is the first: the design this iteration arrived with, composed as a candidate so it competes rather than presides.

THE DRAWING AND THE NOTES WERE BOTH MISSING. build_chart is supposed to author the candidate drawing and the candidate notes, and it authored neither. That is the defect i11's bundle already records, hit live.

## built

[[cand-the-scoped-fix]], at project/spec/trace/candidate/cand-the-scoped-fix.md.

THREE SECTIONS FILLED, as the compose state owes.

- How it works.
- What it costs.
- What it leans on.

Nothing else was minted and no spike ran — the rough checks are M4's and the deep ones are M6's, on the winner alone.

WHAT THE COMPOSITION ACTUALLY SAYS. The reader moves and nothing else does.

- Seeding stops creating a folder.
- Entry creates one.
- Close commits-or-refuses, then removes.
- A sweep clears what earlier closes left.

THE SEAMS ARE WRITTEN OUT rather than assumed, because a composed candidate is only as good as its joints. Reader to claim ledger stays two separate reads. Entry to filesystem is one call at one moment, priced at 593 ms. Close to git gains the commit-or-refuse step, which is the only place this candidate can destroy work.

ITS BLIND SPOT IS NAMED IN ITS OWN NOTE. A crashed walk leaves two things behind.

- A folder no close will remove.
- An iteration held by a machine that no longer exists.

On an ephemeral host that is the expected ending.

## follow_up

- THREE CANDIDATES REMAIN and they run in parallel: the lifecycle is the claim, no folders at all, and the host is declared
- THE CANDIDATE NOTES ARE BEING MINTED BY HAND because build_chart authored neither the drawing nor the notes; that is i11's recorded defect and it cost two manual writes so far
- NO SPIKE RAN HERE, deliberately. Running the rough checks per candidate would spend the budget before anything is chosen.
- evaluate-set judges, and this state deliberately does not know the scores while writing the description
- nothing is parked from this state

## anything_else

### Why this candidate is drawn at all

IT IS THE DESIGN WE ARRIVED WITH, and drawing it as a line is the point. A chart whose incumbent is assumed rather than composed cannot compare anything: the other three would be measured against a moving target that nobody wrote down.

SO ITS WEAKNESSES ARE WRITTEN AS PLAINLY AS THE OTHERS' WILL BE.

- Two reads that can disagree.
- 1326 files per entry.
- A sweep that is not one-time in practice.
- No answer at all when a walk dies.

### The defect this state hit

run-candidates REFUSED TO ENTER because the candidate drawing still carried the pin's placeholder. i11's bundle records exactly this: build_chart writes the candidate notes but not the candidate drawing, so run-candidates can stand empty under a full chart.

IT IS WORSE THAN RECORDED. Neither the drawing NOR the notes were written, so both had to be authored by hand. The refusal itself was clean and carried its remedy, which is the mechanism working; what failed is the state that should have produced the artifact.
