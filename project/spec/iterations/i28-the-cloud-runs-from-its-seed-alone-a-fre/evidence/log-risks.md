---
form: log-risks
by: agent
signed_off: 2026-08-15T15:00:22.603Z
authors: agent
files:
---

# Evidence form / log-risks

## current_situation

i28 is bound, the kickoff is blessed at MAJOR, and the vision state inherited [[vp-the-engine]] without bending it.

THE REGISTER OPENS WITH FIVE ENTRIES, and four of them were minted at the kickoff gate rather than here. That is deliberate and the method sanctions it: any state may add an entry the moment it is noticed, and waiting for the right state is how an entry is lost.

WHERE THEY CAME FROM. Three were found by this iteration's own entry failing, which is the most direct evidence available that they are real. One is the debt the owner accepted for the cloud validation. One is the field's own widest-blast-radius finding from the second machine on 2026-08-14.

THE OWNER'S DESIGN LANDED BETWEEN THE VISION AND THIS STATE, and it is recorded in the record rather than restated here: git is the truth, the disk is a workspace, and a claimed iteration is drawn greyed with its holder rather than hidden.

## raid_opened

- [[raid-asm-git-answers-open-without-a-worktree]]
- [[raid-a-close-that-removes-the-folder-destroys-uncommitted-work]]
- [[raid-sweeping-the-folders-early-hides-every-seeded-iteration]]
- [[raid-the-read-proof-locks-weaker-models-out-of-the-system]]
- [[raid-debt-cloud-validation-needs-a-machine-this-one-cannot-make]]

## follow_up

- the kill-criterion assumption carries a written probe that has not run, and it runs in this walk before the reader changes
- the close-destroys-work risk is graded fatal and expected, so its mitigation is a design rule rather than a watch: the close commits or refuses, never removes blind
- the ordering risk exists because the owner asked for the fix and the sweep in one breath, and the sweep is the easiest of the four to do first and the only one that must be last
- the read-proof issue may be DELETED rather than fixed, if a document handed over through a tool call turns out not to need proving; three things must survive whatever replaces it and they are named on the entry
- [[raid-debt-claim-pool-surfaces]] is partly repaid by this iteration, because the greyed claimed iteration is the first product surface the claim ledger ever reaches, and its entry wants updating when that lands
- nothing here is parked, and no note was written from this state

## anything_else

ONE ENTRY WAS CONSIDERED AND NOT WRITTEN. The six failure points in this record's seed — the server dying on stdin EOF, the missing entrypoint, itAdopt not being a lane verb, the manual fetch refspec, the absent lane lifecycle, and the last gate parking an unattended run — are SCOPE rather than register entries. They are known defects this iteration exists to fix, and logging them here would duplicate the goal as risks.

THE TEST APPLIED: an entry earns its place when it says something the scope does not. The read-proof issue passed that test, because it is not on the fix list and it closes the door on a whole class of model.
