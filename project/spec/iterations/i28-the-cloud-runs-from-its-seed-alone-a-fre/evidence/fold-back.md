---
form: fold-back
by: agent
signed_off: 2026-08-15T19:10:04.587Z
authors: agent
files:
---

# Evidence form / fold-back

## current_situation

Three spikes ran and every one produced a finding, though only one ran the way it was drawn.

TWO OF THE THREE FELL. The caller is not released, and the declared runtime floor is not the floor.

ONE IS UNSETTLED because the host ran out rather than the timebox.

NOTHING IS PROMOTED. No spike wrote code, so the throwaway law costs nothing here.

## folded

- exp-does-a-backgrounded-lane-release-its-caller.folds_to: [[el-entrypoint]]'s start step. It cannot use a background flag and must use an explicit platform detach, proven by this same measurement before anything downstream is built. [[raid-asm-a-host-keeps-a-backgrounded-lane-alive]] splits in two: releasing the caller is a spawn question and is now answered, surviving the session is a host question and stays owed.
- exp-does-a-backgrounded-lane-release-its-caller.promote: none. The child was a bare sleeping process and the finding is the product.
- exp-what-runtime-the-engine-actually-needs.folds_to: [[el-entrypoint]]'s verify step compares against a PINNED version rather than a floor. The declaration at package.json line 8 is wrong and misleads the next reader exactly as it misled this design. [[raid-asm-the-installed-runtime-is-one-the-engine-runs-on]] is answered for the local half and stays owed for what a default install gives.
- exp-what-runtime-the-engine-actually-needs.promote: none. Three facts were read and nothing was written to throw away.
- exp-the-posix-branches-have-never-run.folds_to: nothing structural moves. It re-frames the first spike's finding rather than changing an element: the start step is platform-split by design, and only the Windows half has ever run. [[raid-lane-works-on-posix]] keeps its grade and gains the count.
- exp-the-posix-branches-have-never-run.promote: none. Nothing was built.

## follow_up

- gate-prototype is next and it inherits two fallen assumptions rather than a clean sheet
- TWO EDITS TO [[el-entrypoint]] ARE OWED before M7 builds it: an explicit platform detach in start, and a pinned version in verify
- ONE DECLARATION IS WRONG and should be corrected wherever it stands: package.json's engines floor
- THE FIRST CLOUD RUN IS A TEST, and the detach path is the first place to look
- ONE HALF OF EACH FALLEN ASSUMPTION STAYS OWED, and both need the machine this one cannot make
- nothing is parked from this state

## anything_else

### What the three spikes cost and bought

BUDGETED 95 MINUTES ACROSS THE THREE. Two were settled by measurement and one by reading the source.

NONE OF THEM NEEDED THE CLOUD HOST they were drawn against, and that is the finding behind the finding. Each assumption tangled a local question with a host question, and in every case the local half was answerable here.

THE HOST HALF WAS NEVER THE BLOCKER. It was the reason nobody looked.

### Why two fallen assumptions is a good outcome

BOTH WOULD HAVE FAILED ON THE FIRST CLOUD RUN, and both would have failed as something other than their cause. A start step that never returns reads as a hang. A runtime below the real floor reads as a syntax error deep in a spawned script.

THAT IS EXACTLY THE SYMPTOM [[req-one-command-starts-an-unattended-machine]] EXISTS TO REMOVE. Finding them here rather than there is the whole point of spiking before building.

### The throwaway law cost nothing

NO SPIKE WROTE CODE. One spawned a sleeping process, one read three declarations, one ran a search. There is nothing to be tempted by and nothing to promote.
