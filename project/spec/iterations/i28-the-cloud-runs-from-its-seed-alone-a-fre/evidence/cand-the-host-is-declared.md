---
form: cand-the-host-is-declared
by: agent
signed_off: 2026-08-15T17:03:24.876Z
authors: agent
files:
---

# Evidence form / cand-the-host-is-declared

## current_situation

M4's last candidate. All four lines of the chart are now composed, and this is the only one that questions the boundary rather than the mechanism.

ALL FOUR CANDIDATE NOTES WERE MINTED BY HAND. build_chart authored neither the drawing nor the notes, which is i11's recorded defect hit four times in one state.

## built

[[cand-the-host-is-declared]], at project/spec/trace/candidate/cand-the-host-is-declared.md.

IT MOVES THE BOUNDARY RATHER THAN THE MECHANISM. Every other line improves how the product stands itself up. This one asks whether standing itself up is our job, and hands four of the entrypoint's seven steps to somebody else's control plane. It is the only line that SHRINKS the entrypoint instead of improving it.

HOW IT WORKS: a container definition names the runtime and the product, so no verify step and no install step. The entrypoint reduces to fetch the iteration and start walking. The iteration list is collected while the previous surface is being read. Entry materialises the record's own folder rather than the whole product.

THREE SEAMS, and two are real problems. Image-to-product is a DRIFT seam: two artifacts must agree about the runtime and nothing keeps them agreeing. Prefetch-to-truth is a CACHE, added to a design whose central complaint was a cache pretending to be the truth, and its invalidation is undesigned.

WHAT IT COSTS is three things.

- It moves the problem rather than removing it.
- It changes the precondition rather than meeting it.
- It leaves two paths where this iteration wanted one.

A genuinely bare host with a shell and nothing else cannot use an image at all, and that is exactly the host nbr-cloud-host describes.

## follow_up

- ALL FOUR CANDIDATES ARE COMPOSED and the join hands the set to evaluate-set
- THIS CANDIDATE'S PREFETCH IS ITS OWN WEAKEST PART, because it adds a cache to a design whose central complaint was a cache; the two are separable and evaluate-set should notice that
- THE CHART SHOWS A COMBINATION NO CANDIDATE TAKES: the null option plus the lease. Folder lifetime and claim expiry are independent decisions, and a shortlist could take both.
- gate-candidates is where the owner meets this
- nothing is parked from this state

## anything_else

### What the four candidates actually disagree about

THEY AGREE ON ONE THING and it is the iteration's central change: git answers which iterations exist. Every line takes it, and no line was drawn that keeps the disk test, because keeping it would make every other row cosmetic.

THEY DISAGREE ABOUT THREE THINGS.

- WHETHER A FOLDER SHOULD EXIST AT ALL. A and D say yes with a lifecycle, B says only under a live claim, C says never.
- WHAT ENDS A CLAIM. Three say a person's judgment, B says a lease.
- WHOSE JOB IT IS TO STAND THE PRODUCT UP. Three say ours, D says the environment's.

### The combination the chart shows and no candidate takes

THE NULL OPTION PLUS THE LEASE. C removes the folder entirely and still leaves a claim held by a dead machine; B's lease answers that and is independent of whether folders exist.

THAT COMBINATION IS LEGAL, SIMPLE AND UNDRAWN. It is named here rather than added as a fifth line, because four is already a sample and evaluate-set can reach it from the chart.
