---
form: converge-pugh
by: agent
signed_off: 2026-08-15T18:01:34.433Z
authors: agent
files:
---

# Evidence form / converge-pugh

## current_situation

M5 converges. The front of three entered, the datum was the strongest of them, and one run settled it.

THE WINNER IS `cand-the-lifecycle-is-the-claim`. It took the datum seat and no rival scored above zero against it.

THE RUN IS STABLE and the engine reports no problems. Nothing here was typed; the arithmetic wrote it.

THE WINNER IS WEAKEST ON THE AXIS THAT MATTERS MOST, and that is not a complaint about the arithmetic. It is the shape of the set, flagged at the gate before this run happened.

## matrix_runs


## follow_up

- reverse-sensitivity is next, and it has one obvious thing to test: how much the winner depends on the bootstrap axis staying unanswered
- THE GRAFT IS THE REAL OUTPUT OF THIS STATE, not the winner alone. The winner is a spine; the bootstrap answer has to come from `cand-the-host-is-declared`.
- `cand-no-folders-at-all` SCORED ZERO AGAINST THE DATUM, which is the closest any rival came. Its one advantage is real and cheap, and it is worth carrying rather than discarding.
- ONE REQUIREMENT IS STILL UNSERVED BY THE WINNER as it stands: `req-one-command-starts-an-unattended-machine`, graded fatal. M6 cannot treat that as settled.
- THE ITERATION'S TARGET IS RUNNING IN THE CLOUD (owner, 2026-08-15). The winner is the claim and worktree half of that. The startup half is the graft.
- nothing is parked from this state

## anything_else

### What the run says, in plain numbers

THE DATUM WAS `cand-the-lifecycle-is-the-claim`, the strongest candidate on the front. Every rival was scored against it, axis by axis.

- `cand-no-folders-at-all` totalled 0. It beats the datum on bootstrap, ties on the worktree axis, loses on the holder axis.
- `cand-the-host-is-declared` totalled -1. It beats the datum on bootstrap and loses the other two.
- `cand-the-scoped-fix` totalled -2. It beats the datum nowhere.

NO RIVAL SCORED POSITIVE, so the datum keeps the seat and the run is stable. One run was enough.

### The thing this run does not settle

BOTH RIVALS THAT SCORED ABOVE THE FLOOR BEAT THE WINNER ON THE SAME AXIS, and it is the top-ranked one. `req-one-command-starts-an-unattended-machine` is graded fatal, and the winner scores 1 on it.

SO THE WINNER LOSES THE MOST IMPORTANT COLUMN AND STILL WINS THE MATRIX. That is legitimate Pugh arithmetic and it is also a warning. A total is a sum, and a sum can carry a candidate past a column that should have stopped it.

THE METHOD'S ANSWER IS THE GRAFT, not a re-run. Controlled convergence exists to take the winner's spine and fold in what the runners-up do better. Here that is one specific thing: the declared image from `cand-the-host-is-declared`, which is the only mechanism in the whole set that deletes the verify and install steps.

### Why this is not scored as a defect in the winner

THE WINNER WAS NEVER COMPOSED TO ANSWER STARTUP. It came from four options about claims and folders, and it answers those completely, including the case no other candidate touches: a machine that dies mid-iteration, where the lease lapses and the tree lapses with it.

ASKING IT TO ALSO ANSWER BOOTSTRAP WOULD BE ASKING IT TO BE THE WHOLE ITERATION. That is what the graft is for, and reverse-sensitivity is where the dependency gets measured rather than asserted.
