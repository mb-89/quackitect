---
form: probe-assumptions
amended: "2026-08-15T16:10:43.179Z by agent — The kill-criterion probe was run for real and holds; and three probes carried a false reason, since this state grants se_run."
by: agent
signed_off: 2026-08-15T15:59:51.908Z
authors: agent
files:
---

# Evidence form / probe-assumptions

## current_situation

M3's last work state. Sixteen assumptions stand and twelve carried a probe result already. This state answers the four this iteration opened.

TWO CHEAP REAL CHECKS RAN and both changed something. One shrinks this iteration's scope. The other turns a smell into a measured fact.

ONE CORRECTION TO MY OWN CLAIM at identify-assumptions: I said the peer-platform assumption was silently falsified. It was not. Its probe of 2026-08-13 already records the Linux run and grades it held narrowly.

## probes

| raid | probe | probed |
| --- | --- | --- |
| [[raid-a-crashed-walk-leaves-a-folder-that-means-nothing]] | scheduled. Killing a bound walk kills this session's own engine, so it needs a throwaway clone with its own engine. That is a spike, not a probe. | 2026-08-15 |
| [[raid-asm-a-host-keeps-a-backgrounded-lane-alive]] | scheduled. Our half is built: se-mcp.ts line 495 skips the stdin reader under --headless. The host half needs a cloud host, which this machine is not. | 2026-08-15 |
| [[raid-asm-an-omit-is-authored-honestly]] | unprobed. Two rows carry an omit and both were authored in the same hour as the mechanism, so they are not independent evidence. | 2026-08-13 |
| [[raid-asm-dial-carries-adjudication]] | holds - owner ruling 2026-08-09 sanctions the agent blessing its own gate at high autonomy. See the node's Probe section. | 2026-08-12 |
| [[raid-asm-every-condition-can-say-what-it-wants]] | partially probed, holds so far. Five condition types stand and each says what it wants. The computed pile is uncounted. | 2026-08-13 |
| [[raid-asm-git-answers-open-without-a-worktree]] | HOLDS, with a condition. Measured over 33 branches: existsSync 12.6 ms, batched cat-file 58.7 ms, one git show per iteration 1004 ms. Batching is required. | 2026-08-15 |
| [[raid-asm-grey-verb-distinct-from-se-help]] | scheduled. The check puts both verb signatures side by side, and se.help does not exist yet. It waits on i8 landing. | 2026-08-13 |
| [[raid-asm-help-query-vocabulary-overlaps]] | unprobed - no help-demand log exists yet, so there is no real usage to spot-check. Re-probe once query traffic accumulates. | not yet - needs real se_help usage |
| [[raid-asm-line-endings-do-not-move-under-us]] | unprobed. The check mutates the tree to answer a design question, so it belongs beside the storage design. | 2026-08-13 |
| [[raid-asm-owner-pushes-keep-remote-fresh]] | scheduled - ship locally unpushed, then have a second clone claim a dependent stub and read the refusal. Runs at M7. | not yet - scheduled at M7 |
| [[raid-asm-peer-runs-supported-platform]] | held narrowly, on a DIFFERENT platform than the answer named. The 2026-08-13 peer ran Linux, not Windows. Full detail on the node. | 2026-08-13 |
| [[raid-asm-refusals-recover-a-weak-model]] | scheduled, never probed. The check is one prepared session with a weak model, which the owner holds. | 2026-08-13 |
| [[raid-asm-remote-serializes-claims]] | holds locally, genuinely concurrent - two in-flight pushes, one lands, the loser learns the holder. Network still faked. | 2026-08-12 |
| [[raid-asm-session-identity-survives-a-reload]] | unprobed, deliberately. The check needs an se_reload, which would disturb a bound record to answer a non-blocking question. | 2026-08-13 |
| [[raid-asm-the-installed-runtime-is-one-the-engine-runs-on]] | partially probed. The floor IS declared: package.json line 8 says node >=22.6. Confirming a bare host's default needs a bare host. | 2026-08-15 |
| [[raid-lane-works-on-posix]] | holds on Linux - the first POSIX run, 2026-08-13, battery 1104/1117 with no path or worktree failures. Full detail on the node. | 2026-08-13 |

## follow_up

- THE KILL-CRITERION HOLDS AND IT CONSTRAINS THE BUILD. Batching is not an optimisation; a reader that spawns git per iteration costs 1004 ms over 33 branches and breaches the one-second rule before a single new iteration is seeded.
- THE READER TAKES THE RECORD FROM TRUNK, not from the branch tip. The batched read returned a status line for all 33 branches while the survey calls 27 open, because a shipped iteration's record on its own branch can still read as open.
- THE THREE REMAINING PROBES ARE SPIKES, not blocked calls. Killing a bound walk kills this session's engine; the other two need a host this machine is not.
- SCOPE SHRINKS: --headless already exists at se-mcp.ts line 495, so the entrypoint passing it is what remains.
- THE RUNTIME FLOOR IS 22.6 and nothing enforces it at start.
- PROBES BECOME DEDICATED NOTES in the implementation phase, on the owner's ruling of 2026-08-15.
- nothing is parked from this state.

## anything_else

### The probe that ran, and what it cost to get right

THREE ATTEMPTS. The first measured a baseline of FAILED lookups, because the shell's working directory is the bound worktree and `.worktrees/<id>` is invisible from inside it. The second broke the git format by unquoting it under PowerShell. The third measured cleanly.

THAT IS RECORDED RATHER THAN TIDIED because a baseline of failed lookups would have made the comparison come out in our own favour, and nothing in the numbers would have looked wrong.

### A correction that matters more than the probe

THE EARLIER VERSION OF THIS FORM said four probes were scheduled because no M3 state grants se_run. THAT WAS FALSE. M3_20C_probe-assumptions.md line 18 grants se_run, and this state has had it all along.

SO THE KILL-CRITERION WAS DEFERRED TO M6 FOR NO REASON, and it decides whether this iteration's whole design is right. One state's tool list was read and generalised to the milestone.

IT IS THE THIRD ASSERTION-WITHOUT-CHECKING IN ONE SESSION, and it is noted as note-db74bf2b7f0b against the standing work on claims citing their sources. The other two were an offline answer that contradicted a standing ruling, and a truncation cap inherited from another note's inference.

THE LINT WAS ALSO MISREPORTED as owed and refused. It is granted at build-steps, fix-findings and sweep-consistency, which are M7 and M8. A gate refusing it is correct, and normal sequencing was reported as a debt.
