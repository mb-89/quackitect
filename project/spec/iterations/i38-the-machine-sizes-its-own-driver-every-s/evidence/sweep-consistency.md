---
form: sweep-consistency
reopened: "2026-08-20T23:57:06.617Z — the machinery-ascends repair landed below this: a reopened claim on a state that runs a sub-machine is reachable from inside that sub, and its submit is legal there. dsp-walk-machine.md carries the design and tests/reopen-past-a-sub.test.ts guards it. No shipped behaviour this state answers for changed."
by: agent
signed_off: 2026-08-20T23:57:08.235Z
authors: agent
files:
---

# Evidence form / sweep-consistency

## current_situation

The demonstration is performed. This state asks which documents still teach the superseded way.

### What this iteration changed, from the evidence trail

- A MATRIX CELL CAN CARRY A DIFFICULTY, `<column>_complexity: C3/R1`, beside its applies value and its note.
- A STEP PUBLISHES ONE ON THE PULL, as `hand`, carrying a rung and the pair it came from.
- EVERY CALL RECORD CARRIES THREE MORE COORDINATES: `answered_by`, `state` and `part`, plus `relayed_by`, `named_driver`, `went_weaker`, `weaker_reason` and `unreasoned`.
- SIX ARGUMENTS NOW RIDE EVERY LANE TOOL, the way `update` does.
- THE LOG CAN BE GROUPED BY STATE, by model and by part, and says when a grouping key reached nothing.

### What each class turned up

- command and tool docs: SWEPT AND ONE FIXED. `project/guidance/walking.md` teaches what rides every lane call and taught only `update`. It now carries `as`, `relayed_by`, `answered_by` and the three that hold the safety rule, with the reason the default part is `walker` and not `guide`. The contract files are ASSEMBLED from this card, so fixing it fixes them.
- engine-served strings: SWEPT AND CLEAN BY CONSTRUCTION. The six new arguments carry their own descriptions in the tool schema, written when they were added, and each says what the value means and that it is a claim. Nothing else the engine serves describes the changed behaviour.
- method cards: SWEPT AND TWO FIXED. `project/guidance/method/lane.md` said the record carries WHO ACTED and named three actor values; it now names four coordinates and says which one the server can see for itself. `project/guidance/method/subagents.md` is the card about spawning and had no word for the two hands — it now carries the walker and the guide, that either may work the lane, and that a relayed answer names its author.
- matrix rows: SWEPT AND THE README FIXED. It teaches what a cell's frontmatter holds and listed the applies value and the note; it now lists `<column>_complexity` with both vocabularies. NO ROW WAS EDITED — none is rated, which is the standing debt.
- templates and skeletons: SWEPT, NOTHING TO FIX. No template teaches a call record's shape or a matrix cell's, and the test-spec and design-spec templates are untouched by this change.
- guidance chapters: SWEPT AND ONE FIXED, and it is the sharpest of the sweep. `project/guidance/method/retro.md` said PER-STEP COST IS NOT COMPUTABLE TODAY and explained that the state rides inside a narration record's arguments. That is what this iteration removed. The card now says it is computable, how to group by all three coordinates, that two of them are claims, and that records from before this build carry none of it.
- book chapters: SWEPT, NOTHING TO FIX. Searched for the changed behaviours across the book; nothing teaches the call record's fields or the matrix cell's shape.
- README and entry documents: SWEPT, NOTHING TO FIX. The entry documents carry no method jargon by law and describe none of this.
- panels and form help: SWEPT, NOTHING TO FIX. No form's help text describes a call record, and the sizing block reaches no surface — nothing reads the published field, which the gate records as an override rather than a gap.

## swept

- [x] command and tool docs
- [x] engine-served strings
- [x] method cards
- [x] matrix rows
- [x] templates and skeletons
- [x] guidance chapters
- [x] book chapters
- [x] README and entry documents
- [x] panels and form help

## follow_up

THE RETRO CARD'S CORRECTION IS THE ONE WORTH FOLLOWING. It carried a measured claim from 2026-08-17 that per-step cost could not be computed, and this iteration's own `uc-attribute-a-finished-walk` cites that same documented consequence as its motivation. The card is now the first place a reader learns the answer changed.

AND IT CARRIES ITS OWN CAVEAT RATHER THAN A CLEAN CLAIM: a window spanning this build answers for part of itself, because records written before it carry none of the three fields.

ONE THING NOT SWEPT AND SAID RATHER THAN MISSED. `project/guidance/voice.md` rules that prose never says "human vs agent" and that the actor stamp is a recorded metric with a fixed vocabulary. That rule is unchanged and still right — the vocabulary widened, and the rule is about prose rather than about the metric.

THE MATRIX README EDIT MOVES THE MATRIX CONTENT HASH, so every open record re-pins on the next look. That is the mechanism the rating pass will trip on a second time, and it is written into the debt entry's repayment.

## anything_else

