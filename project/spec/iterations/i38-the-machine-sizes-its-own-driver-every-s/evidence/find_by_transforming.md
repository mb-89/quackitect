---
form: find_by_transforming
amended: "2026-08-20T14:10:08.637Z by agent — three finders where five had run, contradicted by the same form's opening line"
by: agent
signed_off: 2026-08-20T12:14:02.798Z
authors: agent
files:
---

# Evidence form / find_by_transforming

## current_situation

Fifteen options stand from five finders. This state runs two fixed catalogues — SCAMPER's seven letters and SIT's five patterns — against the incumbent design, which is the four-function sizing block as the seed proposes it.

THE SUBJECT IS ONE CLUSTER AND ONE EXCEPTION. Eleven operators are held against cluster-the-sizing, because that is what the change proposes and a transformation has to have something to transform. Task Unification is held against cluster-the-account instead, because the component with a spare job in it is the call record.

THE METHOD CARD CHANGED WHAT THIS STATE OWES. meth-scamper used to budget one honest minute per operator, for a person; it now says run both catalogues whole and write the misses. Twelve rows follow, three of them saying nothing.

## applies

yes

## sweep

| operator | subject | what_it_produced |
| --- | --- | --- |
| Substitute | cluster-the-sizing | swap the unit the decision is made over — coarser than the seed's milestone, one driver for the whole record read off the change size gate-kickoff already blessed (opt-one-driver-named-per-record-at-kickoff) |
| Combine | cluster-the-sizing | merge the new declaration into the row that already exists — the cell's own field count and method already say how hard it is at that size, so nothing is declared and nothing can drift (opt-the-difficulty-is-computed-from-the-row-s-own-field-count) |
| Adapt | cluster-the-sizing | nothing — this operator is find_prior_art and find_by_analogy in one letter, both already run whole over this cluster, and repeating them here would recount their five and two options as new |
| Modify | cluster-the-sizing | nothing new — magnifying the granularity reaches opt-name-the-driver-per-state-not-per-milestone and minifying it reaches the null option, both already on the chart from the trimming finder |
| Put to other use | cluster-the-sizing | ask who else wants the difficulty as-is — a cost estimate, a schedule and a reviewer triaging fifty-three states all do, so publish the number and let driver selection be one consumer (opt-publish-the-difficulty-and-let-consumers-choose-their-own-hand) |
| Eliminate | cluster-the-sizing | nothing new — removing the mapping is opt-the-row-names-the-driver-and-there-is-no-mapping and removing the reduction is opt-name-the-driver-per-state-not-per-milestone, and meth-scamper says this overlap with trimming is deliberate |
| Reverse | cluster-the-sizing | invert which side of the handover decides — the block states the difficulty and the driver accepts or refuses, which is the bidirectionality the closest prior art has and ours does not (opt-the-driver-reads-the-difficulty-and-decides-whether-to-take-it) |
| Subtraction | cluster-the-sizing | remove the roster, which looked essential — the block names a rung and never a model, and raid-asm-one-model-list-serves-every-host-the-engine-supports stops being needed rather than being weakened (opt-the-block-names-a-rung-and-never-a-model) |
| Task Unification | cluster-the-account | give the call record a second job — it is already gaining two fields this iteration, and at three it holds the whole decision, so publication becomes a read rather than a send (opt-the-record-is-the-channel-and-there-is-no-separate-publication) |
| Multiplication | cluster-the-sizing | copy the walk and change the copy's hand — walk a sampled step twice on two rungs and compare, which is the only option on the chart that can answer that the ladder is not worth having (opt-walk-a-sampled-step-twice-on-two-rungs-and-compare) |
| Division | cluster-the-sizing | split the difficulty and rearrange what depends on each part — how hard the judgement is and how much must be read are independent, and the corpus holds both extremes (opt-difficulty-splits-into-judgement-and-reading) |
| Attribute Dependency | cluster-the-sizing | make difficulty vary with a quantity it currently ignores — a row's own reopen history, which is the closest thing the corpus has to an observation of difficulty rather than a claim about it (opt-a-row-s-difficulty-tracks-its-own-reopen-history) |

## options

- opt-one-driver-named-per-record-at-kickoff
- opt-the-difficulty-is-computed-from-the-row-s-own-field-count
- opt-publish-the-difficulty-and-let-consumers-choose-their-own-hand
- opt-the-driver-reads-the-difficulty-and-decides-whether-to-take-it
- opt-the-block-names-a-rung-and-never-a-model
- opt-the-record-is-the-channel-and-there-is-no-separate-publication
- opt-walk-a-sampled-step-twice-on-two-rungs-and-compare
- opt-difficulty-splits-into-judgement-and-reading
- opt-a-row-s-difficulty-tracks-its-own-reopen-history

## follow_up

NINE OF TWELVE PRODUCED SOMETHING AND THAT RATE DESERVES SUSPICION, not celebration. It is high because the incumbent is a four-function block nobody has built yet, so every operator has an unconstrained thing to push on and none of them meets resistance from an implementation. The FIVE finders that ran before this one were bounded by something outside the design in three cases and by something inside it in two — CORRECTED at gate-candidates, and the correction weakens the point rather than the count: prior art, contradiction and analogy are bounded outside, trimming and heuristics are not, and this form's own opening line says fifteen options stood from five finders — what exists elsewhere, what the record already contradicts, what another domain settled — and this one is bounded by nothing. Treat the nine as a wide net, not as nine good ideas.

THREE OF THEM ARE THE STRONGEST THINGS ON THE CHART, WHICH IS THE OTHER HALF OF THE SAME OBSERVATION. Deriving difficulty from the row's own field count is the only candidate that cannot drift, because there is no declaration to raise. Publishing the difficulty rather than a driver removes two unproven assumptions and makes the design's safety rule hold by construction. Walking a sampled step twice is the only candidate on the chart that can return the answer that this whole record was not worth making.

TWO PAIRS COMPOSE RATHER THAN COMPETE, and evaluate-set should be told so before it scores them as rivals. The field-count derivation is a formula for the reading axis and silent about judgement, so it and the split fit together. The record-as-channel and the both-drivers-on-the-record option put the same two facts on the same line, and taking one makes the other free.

THE THREE SILENT OPERATORS ARE NOT ALIKE, AND ONE OF THEM IS THE FINDING. Modify and Eliminate reached options the chart already held, from the trimming finder. Adapt is different: it is find_prior_art and find_by_analogy compressed into a letter, both ran whole over this cluster, and between them they produced seven options. Running Adapt again would have re-found some of those and reported them as new — which is the failure mode a twelve-row sweep invites, because the operators overlap and a sweep that never says nothing is a sweep that is double-counting.

## anything_else

