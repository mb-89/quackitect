---
form: draft-vision
by: agent
signed_off: 2026-08-12T20:56:56.923Z
authors: agent
files:
---

# Evidence form / draft-vision

## current_situation

i8 escalated to minor, which pulls in the full M1/M2 spine (draft-vision, gate-motivation, context/stakeholders/stories/use-cases, gate-inputs) ahead of write-requirements — not visible in the mid-walk busbar the escalation left behind, but reached correctly by leaving and re-entering the iteration machine fresh. This product has never authored a formal M1 vision packet as a trace node; project/spec/v3-plan.md stands in as the resident vision and is cited as the pointer.

## big_idea

INHERITED, pointer: project/spec/v3-plan.md is the resident vision for this product — a governed lane (the `se` MCP server) that replaces every native tool the cage removes, with typed refusals, logged calls and evidence-gated work. i8's delta: the lane's own tools and guidance become findable BY THE LANE ITSELF, closing the one gap where an agent still has to already know a tool's exact name.

## to_be_world

DELTA SCENE (axiom untouched): an agent mid-task does not know which lane verb covers "find the file that mentions X" or "why is this state grey". Today it either guesses a name and gets refused, or drops to se_run — se_run stood at 3249 of 28612 calls on 2026-08-07, the lane's second-busiest door (guidance/method/retro.md, cited in record.md). After i8: the agent calls se_help with a plain-words description, gets a ranked list of real tools and guidance pages, or an honest miss that a retro later reads as a ranked demand list instead of hand-mining the shell log.

## goal_system

Inherited from v3-plan.md's standing laws (typed rejections everywhere, honest truncation, every dispatch logged, prose never changes agent behavior — refusals do). i8 adds one local goal, already argued and RAID-logged at this gate's own kickoff (round_2_red_team, raid-risk-se-help-search-half-unproven): search value vs. build cost. The vision itself names the conflict — the search half is weaker here than in v2 because this harness already loads tool schemas on demand — and RULES it: build both halves, but if one must be cut, the demand log wins, because it is the half with evidence behind it (record.md vision).

## moore_pitch

- FOR an agent working through the se lane
- WHO does not already know the exact name of the tool or guidance page it needs
- THE se_help verb IS a keyword search over the lane's own tools and guidance
- THAT ranks matches in plain words and logs every miss as a ranked demand
- UNLIKE dropping to se_run or guessing a tool name and eating a refusal
- OUR TOOL turns every failed search into evidence the next retro can act on, mechanically, instead of losing it

## follow_up

gate-motivation next, then M2 (context, stakeholders, stories, use-cases, gate-inputs) before write-requirements can be resubmitted through the walk properly.

## anything_else

