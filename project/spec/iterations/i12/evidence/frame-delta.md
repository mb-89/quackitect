---
form: frame-delta
by: agent
signed_off: 2026-08-15T10:16:24.595Z
authors: agent
files:
---

# Evidence form / frame-delta

## current_situation

The delta authors NO new value prop. It extends a standing one.

vp-rigor-without-toil says the person's time should go to redlines and thumbs rather than to producing artifacts. The resident reading of toil is PAPERWORK.

This delta adds the half that reading missed. WAITING IS TOIL TOO. A machine that takes four seconds to open a state, or hands back an answer the host has to move to disk before it can be read, spends the person's time on watching rather than judging.

That is a need under the same prop, not a new proposition, so the prop is extended rather than replaced.

## value_props

- vp-rigor-without-toil

## business_case

SKIPPED, with the reason recorded. No acquirer exists. The product is self-hosting and its only user today is the owner, so there is no currency to state the effort in beyond the owner's own time.

What the effort buys IS the owner's time, and that is already the value prop this delta extends rather than a separate case.

## follow_up

- The extension landed on the standing artifact rather than in this form. vp-rigor-without-toil now carries the delta's success criterion with two metrics and their targets.
- THE PASS LINES ARE CHECKABLE AND ONE OF THEM IS ALREADY FAILING. Every surface request answering inside a second is measurable from the mirror_slow records, and this session recorded nineteen breaches in a single window.
- The second pass line, that no answer needs moving to disk before it can be read, is measurable from the pull's own bounded answers.
- Neither pass line has a test today. The record names the missing one by the owner's own request: a test that fails when a render passes a second, where the existing guard covers lane calls only.

## anything_else

ON WRITING A PASS LINE THAT ALREADY FAILS.

The state's guidance is blunt that a criterion nothing will check is not a criterion. It is worth saying plainly that the criterion added here is not aspirational.

Both metrics are measurable from data the system already records. The first was measured during this very walk: nineteen mirror_slow records in one window, with /widget/details over the second on all seven of its requests.

So the prop now carries a line the product visibly does not meet. That is the honest state, and it is the point of writing it down at the frame rather than at the end.
