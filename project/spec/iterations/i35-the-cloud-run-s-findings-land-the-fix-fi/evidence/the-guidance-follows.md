---
form: the-guidance-follows
by: agent
signed_off: 2026-08-17T12:09:39.744Z
authors: agent
files:
---

# Evidence form / the-guidance-follows

## current_situation

The act existed and the card still described five hand-performed steps, so an arriving agent would have followed the prose past the script.

## built

project/guidance/method/cloud-runner.md, Arrival A.

IT LEADS WITH THE ONE COMMAND and says the root settings already fired it at session start on most hosts, naming SE_NO_ARRIVE and SE_AUTONOMY.

THE FIVE ACTS STAY, and that is deliberate: a failed step has to be fixable by hand, and the reasons are the part worth keeping. They gained a step — the fetch — with the measurement behind it, and the act count follows.

THE FETCH SECTION SAYS WHICH HALF PEOPLE SKIP, with both observations: after the fetch alone the ref: search still failed, and only the local branch fixed it. It also carries what skipping it cost i15 — four refused calls, a wrongly-minted assumption, and a false claim through six evidence forms.

## follow_up

- The card still prescribes the caged-subagent hand-over, which did not work on this harness. That paragraph is now known to be wrong for at least one host and is not yet corrected.
- Its own read-probes changed when it was edited, which is correct and surprising.

## anything_else

EDITING A GUIDANCE FILE CHANGES ITS OWN READ-PROBES. The boot loop quotes four words from three places, so improving the document invalidated the answers cached from before the edit.

That is the mechanism working — the proof is of THIS text, not of a remembered one — and it is worth knowing before it looks like a bug.
