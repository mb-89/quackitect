---
form: sweep-consistency
by: agent
signed_off: 2026-08-17T12:39:06.023Z
authors: agent
files:
---

# Evidence form / sweep-consistency

## current_situation

This iteration changed one entry point, one hook, two engine modules, one guidance card, four test files and the repository's own settings.

The sweep asks where each of those is TAUGHT, and one surface had never heard of the arrival at all.

## swept

- [x] command and tool docs
- [x] engine-served strings
- [x] method cards
- [x] matrix rows
- [x] templates and skeletons
- [x] guidance chapters
- [x] book chapters
- [x] README and entry documents
- [x] panels and form help

## follow_up

- cloud-runner.md still prescribes the caged-subagent hand-over, which did not work on this harness because a subagent inherits the session's MCP registry. That paragraph is now known to be wrong for at least one host and is NOT corrected here — it needs the owner's ruling on whether the pattern survives at all.
- RELEASES.md is not touched by this sweep. It is a shipped-version log and the package state owns it.

## anything_else

WHAT EACH BOX ACTUALLY COVERED, because a checklist of nine ticks is worth nothing without it.

COMMAND AND TOOL DOCS: se-arrive.ts and se-hook-arrive.ts each carry a --help or an opt-out line, and the help conformance check binds them — it fired on this iteration when a literal fence read as a switch, so it is live rather than decorative.

ENGINE-SERVED STRINGS: the hook's own session-start text was written to teach the five pull instructions and the aim, since on a cloud box it is the first thing an agent reads.

METHOD CARDS: cloud-runner.md leads with the one command and keeps the five acts beneath it, with the fetch measurement in place.

MATRIX ROWS: none changed. The arrival is a new element, not a new step in the walk, so no row describes it.

TEMPLATES AND SKELETONS: none changed. The cage templates are placed by the arrival and unmodified by it.

GUIDANCE CHAPTERS: cloud-runner.md is the only one whose subject moved.

BOOK CHAPTERS: none. The book derives from the corpus, and the corpus gained its nodes this run.

README AND ENTRY DOCUMENTS: THE GAP THIS SWEEP FOUND. The README described exactly two ways to start — RUNME.ps1 and opening the folder in VS Code — and never mentioned a machine nobody is watching. A reader arriving cold would have concluded the cloud path did not exist. It now carries the one command, what it does, that it is idempotent, the opt-out, and a pointer at the card. Written without method jargon, because an entry document is what a stranger reads first.

PANELS AND FORM HELP: none changed. The dial is taken as a parameter and nothing about its surface moved.
