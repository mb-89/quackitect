---
form: frame-delta
by: agent
signed_off: 2026-08-16T05:41:28.970Z
authors: agent
files:
---

# Evidence form / frame-delta

## current_situation

i34 stands at frame-delta, on the branch the gate-motivation join was still owed.

HOW IT GOT HERE IS WORTH ONE LINE, because it is a live instance of a parked note. Completing log-risks activated nothing: gate-motivation is an AND-join and frame-delta had never been walked, so the only offered door was the join itself and the join refused. `se_aim {to: frame-delta, go: true}` drew the route back through draft-vision and walked it. note-5aabf156e5f7 predicted exactly this shape.

THE FRAME IS UNCHANGED. i34 removes a mechanism. It creates no audience, no outcome and no new prop.

## value_props

- none

## business_case

SKIPPED WITH A REASON: there is no acquirer. Quackitect is self-hosting and this iteration is spent entirely inside its own engine, so there is no currency but the maintainer's own time.

WHAT THE EFFORT BUYS, in that currency, measured rather than asserted:

- 290 MB of duplicated checkouts stop existing, against a 26.63 MiB compacted object store.
- 1,854 phantom dirty files stop appearing in every stub worktree, so a dirty tree becomes a signal again.
- The resolution seam stops existing rather than being fixed for a fourth time. Three attempts stand on the record, and one of them failed again while this iteration was being entered.

WHICH EXISTING PROPS IT SERVES, by pointer rather than by authoring: vp-the-engine, whose measure is acts from clone to a running walk, and vp-rigor-without-toil, whose subject is exactly the time an agent spends on ceremony rather than work.

WHAT IT COSTS THE SAME CURRENCY: vp-the-engine's own claim-based measure stops being meaningful, because nothing is claimed once the ledger goes. That is recorded in raid-dec-one-tree-beats-a-record-travelling-between-machines rather than hidden here.

## follow_up

- SCOPE-NON-GOALS IS THE NEXT OWED LEG of the gate-motivation join, and the aim answer names it: the fan at gate-motivation still lists scope-non-goals.
- NO VALUE-PROP NODE IS AUTHORED, so gate-motivation has no new artifact to follow. It reviews the inherited props by pointer.
- THE JOIN DEADLOCK IS EVIDENCE, not just an obstacle: it confirms note-5aabf156e5f7 live, and the recovery verb was se_aim rather than an escape. Worth knowing at the retro, because the parked note assumes the recovery is expensive and it was one call.

## anything_else

