---
form: draft-vision
by: agent
signed_off: 2026-08-16T05:37:37.313Z
authors: agent
files:
---

# Evidence form / draft-vision

## current_situation

i34 stands at draft-vision, the first state of the minor machine the kickoff bless compiled.

THE KICKOFF SETTLED AT minor, corrected from my own major proposal after the owner challenged it. The matrix decided it: minor drops 23 rows of choosing between candidate designs and keeps every row that guards a deletion.

WHAT THE MINOR MACHINE LOOKS LIKE HERE. This state asks one field instead of four. The big idea, the to-be world and the pitch are dropped mechanically, because a product whose identity changed is not a minor.

WHAT THE VISION IS. Unchanged. i34 removes a mechanism and argues for no new product.

## goal_system

INHERITED. The resident vision stands untouched: no big idea moves, no to-be world changes, no pitch is rewritten. i34 removes a mechanism; it does not argue for a different product.

ONE GOAL CONFLICT IS PULLED OPEN BY THIS DELTA, and it is real rather than decorative.

THE TWO GOALS.

- A RECORD TRAVELS BETWEEN MACHINES. It is served today by the claim ledger on the claims branch, by the seed's push of its it/<id> stub, and by itAdopt. The value prop it belongs to is vp-the-engine, whose measure is acts from clone to first claimed iteration.
- AN AGENT NEVER LOSES TIME TO WHICH-TREE QUESTIONS. It is the owner's stated reason for i34: "the agents spend too much time working around that."

WHY THEY COLLIDE. Travel is what put a record on its own branch, and the branch is what made a second tree exist. One tree is exactly what travel cannot have.

THE RULING, and it is the owner's from 2026-08-16: the second goal wins outright, and travel is switched OFF rather than redesigned. The claims branch goes, itAdopt goes, and the seed stops pushing a stub. A cloud machine will have no way to claim an iteration and no way to be refused.

WHY THAT IS NOT A LOSS BEING HIDDEN. Travel was never observed working end to end. i28's own validation gate passed with an override recording exactly that: the mechanism exists and no rented host has ever run it. So the goal being switched off is one that never delivered, and the goal being served is one that has cost measurable time in every session this week.

WHAT IT WOULD TAKE TO HAVE BOTH LATER, stated so nobody re-derives it: two clones, each a single tree, coordinating through a ledger that never touches a working tree. claims.ts already works that way — it operates on refs through a temporary index. So the conflict is between travel and MULTIPLE TREES ON ONE MACHINE, not between travel and one-tree-per-machine. A later iteration can restore travel without restoring worktrees.

NO NEW GOAL IS NEEDED, so no escalation is argued here.

## follow_up

- THE GOAL CONFLICT IS RULED AND RECORDED, so no later state re-litigates it: travel between machines is switched off, one tree wins, and the path back to both is named.
- NEXT: frame-delta, which is open. log-risks is blocked on reading.
- THE PROBE STILL OWED before any levelling: does anything OUTSIDE a record folder address that record by worktree path? It is cheap and it gates the move.
- THE SELECTION STATE the owner added to scope is build work, and it lands in M7 with its test first — which is what the owner asked for in the same breath as the size: write the tests for the new behaviour, throw the old out, write the new.

## anything_else

