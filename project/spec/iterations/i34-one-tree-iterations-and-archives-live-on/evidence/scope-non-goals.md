---
form: scope-non-goals
by: agent
signed_off: 2026-08-16T05:42:10.489Z
authors: agent
files:
---

# Evidence form / scope-non-goals

## current_situation

i34 stands at scope-non-goals, the last owed leg of the gate-motivation join.

THE SCOPE WAS SETTLED AT THE KICKOFF and is not reopened here. What this state adds is the vision-level line between what the effort takes on and what it deliberately leaves, so the gate has something to adjudicate rather than a list to re-read.

## scope

NINE STEPS, IN THIS ORDER, AND THE ORDER IS THE SAFETY.

1. RESCUE the 14 files that exist only inside worktrees — twelve record.md with uncommitted edits, two decisions.jsonl never committed. Nothing destructive runs before this.
2. LEVEL the 26 stub records onto trunk, after the self-contained assumption is probed.
3. STATUS BECOMES THE OPEN FLAG at six sites: itList, itFind, generateIterations, generateIterationArchive, expList, and the survey's now-redundant second filter.
4. THE RECORD READ COLLAPSES to one tree at two sites: readItRecord and readRecord, each three fallbacks today.
5. DELETE THE SEAM THAT PICKS TREES: storeFor's branch and Roots.bound, machineRootOf, fansOut and methodFilesIn, setMethodMirror and fanOutMethod, itAdopt, and claims.ts entire with its two pushSeed callers. recordOwnerOf and pathKind STAY.
6. CUT THE WORKTREE OUT of the seed and both closes, including the npm install per worktree and the git rm that retires a record directory.
7. BUILD THE SELECTION STATE so entering the iterations container binds nothing until a choice is made.
8. REMOVE THE ARTEFACTS: 27 worktrees, 33 it/* branches, every exp/*, and claims.
9. REWRITE the 66 obsolete test references across 20 files.

TESTS COME FIRST WITHIN EACH BUILD STEP, which is the owner's own instruction: write the tests for the new behaviour, throw the old out, write the new.

## non_goals

- REPLACING THE CLOUD CLAIM. The claims branch goes and nothing takes its job. A cloud machine will have no way to claim an iteration and no way to be refused one. Recorded as a consequence on raid-dec-one-tree-beats-a-record-travelling-between-machines, not smuggled in later.
- RECOVERING ANY DROPPED HISTORY. Seven closed iterations and every expedition go, i27 alone being 87 files including 79 signed evidence forms. The owner ruled this twice and the size was stated to them before the go.
- ADDING THE MISSING GIT LANE VERBS. Six are named — count-objects, rev-list, ls-files, a recursive size, worktree removal, a commit into another tree. i34 will need two of them and will use the shell escape with its reason logged rather than build them. Parked as note-0a16f21b695e.
- THE ENGINE CODE REVIEW for competing mechanisms and quick hacks. i34 removes one competing mechanism; it is not that sweep. Parked as note-fe9e091bfa4c.
- FIXING THE TRANSPORT-CRASH AND UNLOGGED-FAILURE HALVES of raid-iss-a-blocked-walk-can-kill-the-connection-instead-of-refusing. Only its third symptom is in scope, as the selection state. The other two are named and left standing on purpose, because they are engine-wide and this iteration is a removal.
- ANY CHANGE TO THE 17 PARKED NOTES. Each has a ready-when and none blocks this work.

## follow_up

- GATE-MOTIVATION IS NOW REACHABLE: both its owed legs, frame-delta and scope-non-goals, are walked.
- THE FIRST BUILD ACT IS THE RESCUE, and it is the one step that cannot be reordered. Everything else can be argued about; that one runs first or work dies.
- THE PROBE ON raid-asm-a-record-folder-is-addressed-only-from-inside-itself gates step 2 and costs three searches.

## anything_else

