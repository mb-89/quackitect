---
form: gate-requirements
bless: blessed by human
amended: "2026-08-19T15:12:54.193Z by agent — one of the five design choices this gate hands forward was struck by the owner on 2026-08-19"
by: agent
signed_off: 2026-08-19T13:51:33.975Z
authors: agent
files:
---

# Evidence form / gate-requirements

## current_situation

Design input is complete. Five states signed: write-requirements, derive-functions, identify-assumptions, probe-assumptions, and this gate's own reading.

EIGHT REQUIREMENTS AND THREE FUNCTIONS carry the delta. Five rows came from the milestone itself, and THREE MORE WERE WRITTEN AT THIS GATE, because adjudicating the quality sweep found three demands with nothing under them.

THE STRONGEST THING FOUND ALL MILESTONE WAS NOT WRITTEN HERE. req-product-is-a-folder has stood since i1 as a must graded crippling: every artifact a product owns lives inside that product's own root folder. Its own list names the notes, the inbox, the evidence files and the logs, and all four sit outside that folder today.

SO THIS ITERATION CLOSES A STANDING VIOLATION rather than adding a demand. That was found while deriving functions, not while arguing for the change, which is why it is worth more than the arguments made without it.

## round_0_verify

- evidence vs claims: Opened rather than trusted, and it changed two answers rather than confirming them. A standing must-row was found already demanding what this iteration argues for. A probe was corrected after a first script looked in three wrong places and reported no runtime pin at all — a negative result from a search that never reached the file is not a finding, and it would have been recorded as one had the glob not run afterwards.
- types: Nothing compiled, and nothing should have. This milestone changed corpus prose and two throwaway scripts in the workbench. No engine code moved.
- lint: Not run, and the reason is mechanical rather than a judgment. The lint verb is not among this gate's legal tools, and the prose it would check belongs to the states that own that text.
- tests: None run against the product, correctly — no product code changed. What DID run were two scripts that asked the real channel questions the register wanted answered, which is probing rather than testing, and it is recorded on the nodes themselves.

## round_1_validate

- exercised against the goal: Six of the seven blessed goals now carry design input. The seventh is an ordering rule over edits with nothing to state as a demand. Two of the six only gained it AT this gate, which is the finding rather than a detail.
- missing: THREE FOUND AND ALL THREE CLOSED HERE, each the same shape — a blessed goal or a swept characteristic with no requirement under it, which leaves the design milestone nothing to satisfy. Performance efficiency had no clock on the entry point. Maintainability had a goal about one corpus reader and no row. The lane's file-level exclusion had a goal and no row. Rows were written rather than reasons.
- wrong: ONE, and it was this milestone's own. The requirements state answered performance efficiency and maintainability as untouched by the delta. Both answers were wrong, and write-requirements was amended with its signature kept.
- out of scope: The owner's button-press bound, reported today. A control answering a press is a different demand from a product coming up, with a different reason — a press with no reaction gets pressed again. It stays a note against the person-facing boundary rather than being smuggled into this iteration's scope.
- prior art: THE COMPARISON MADE AT THE M2 GATE CARRIED INTO THESE ROWS AND CHANGED TWO OF THEM, which is the honest way to report it. The discoverability row cites what actually surfaces something to a person — four mechanisms a host already scans for — and the finding that a conventionally-named script surfaces nothing. The consent row cites three vendors who each kept the trust record outside the tree. NO NEW COMPARISON WAS MADE AT M3, and that is a finding rather than a blank: the requirement set was written against the standards the method cites, which is a citation and not a comparison. What was not asked is how other systems STATE a consent demand or a discoverability demand as a requirement rather than as a feature.

## goals_served

- COLLAPSE THE WRAPPER. Everything at the repository root moves into the folder that gets opened, until the root holds the project and the repository's own plumbing and nothing else. A level above the project that carries product files is a level that should not exist.: SERVED as design input, and by a stronger route than expected. req-product-is-a-folder already demanded the containment since i1, so what was missing was never the demand but the REFERENT. req-the-machine-state-sits-in-the-folder-that-is-open supplies it, and fn-run-a-governed-walk.resolve-a-path carries both.
- THE MACHINE-STATE FOLDER LEADS THAT MOVE, because it is the one whose home the rule settles first: it belongs to the thing being worked on rather than one level above it.: Nothing yet — the design milestone owns the ordering. No requirement's truth depends on which folder moves first, so there is nothing here to state.
- MOVE FIRST, CLEAN SECOND, AND STOP IN BETWEEN. Nothing is renamed and nothing is deleted while things are moving. Then the owner decides what should not have travelled at all.: Nothing yet — the build milestone owns it. It is an ordering rule over edits, and design input has no demand to make of it.
- OPENING THE EDITOR IS THE ONLY ENTRY POINT AFTER THE FIRST INSTALL, for a folder that IS a project. The launcher runs once. From then on, opening a project gets it everything it needs, and anything that cannot apply to a running window says so instead of going quiet.: SERVED, and it is where most of this milestone went. Four rows: req-the-editor-is-the-only-entry-point, req-one-script-installs re-earned, req-a-folder-is-driven-only-with-consent, and req-the-desk-is-usable-soon-after-the-folder-opens written at this gate. One new function carries them, fn-run-a-governed-walk.bring-the-product-up, with a state model whose last transition is what makes the launcher a one-time act.
- SPLIT THE LANE'S EXCLUSION BY FILE INSTEAD OF BY DIRECTORY, so the three files with a structured door stay hidden and everything else becomes readable.: SERVED, and only from this gate. req-only-a-file-with-its-own-door-is-withheld was written here, because the goal had a user-level story behind it and no requirement under it. It also demands the refusal NAME the verb that serves the file, since a blank refusal teaches the reader the folder is closed.
- GIVE THE ENGINE ONE CORPUS READER, so what the corpus IS has a single answer rather than one per caller.: SERVED, and only from this gate. req-what-the-corpus-is-has-one-answer states the outcome the goal wanted, written as agreement between callers rather than as one reader, because one reader is a mechanism. It rests on a probe rather than a worry: two functions of the same name in two files, disagreeing about what a malformed node does.
- PIN BRANCH INDEPENDENCE WITH A TEST. The folder resolves to one place while a record is bound, and that guarantee stops being trusted and starts being checked.: SERVED as input. The branch claim is one half of req-the-machine-state-sits-in-the-folder-that-is-open, with the test as its stated pass line. The assumption it probes stays open and graded crippling, and its probe result says plainly that the test is owed twice over — once as a probe and once as a goal.

## bound_breaches

- if-agent-harness-to-entrypoint: BREACHED AND STANDING, unchanged from the last gate. The last trustworthy figure is 2026-08-17: 1834 of 8424 calls over the one-second bound, worst answer 33,461 ms. NO NEW MEASUREMENT was taken and none should be claimed — the aggregate instrument is itself the broken part, since the call log query omits matching records and reports that it omitted none. WHAT THIS MILESTONE DID INSTEAD OF MEASURING was add a bound where none existed: req-the-desk-is-usable-soon-after-the-folder-opens gives the person-facing start a clock, which is the neighbour boundary that had never been given one. DISPOSITION unchanged: verification owns the re-measure, and it matters more this iteration because the one-corpus-reader goal changes what an answer is assembled FROM, which this interface names as where the cost lives.

## round_2_red_team

- STEELMAN, the strongest case against this requirement set: every row here deliberately names no mechanism, so not one of them can be checked until the design milestone chooses one, and a set that cannot be checked today is a set that can be quietly reinterpreted tomorrow => Accepted as the real cost, and it is the price of the rule rather than a mistake. The mitigation is that each row carries a MEASURE rather than only an outcome - five seconds, two in three within sixty seconds, byte-identical read-back, agreement including on failure. A measure survives reinterpretation in a way a description does not.
- KILL-CRITERION: this whole set is the wrong work if the collapse turns out not to be needed => Looked for it and found the opposite, which is the strongest single result of the milestone. req-product-is-a-folder has demanded the containment since i1, graded crippling, and four things its own list names sit outside the folder today. The kill-criterion would need that row to be wrong, and nothing here argues it is.
- THREE ROWS WERE WRITTEN AT THIS GATE BY THE HAND THAT THEN JUDGED THEM, which is the shape the method forbids: never judge something on text you wrote into it yourself in the same pass => CONCEDED, and named rather than smoothed over. The gate's own instruction is that a named gap gets a row before it blesses, so writing them is sanctioned; judging them in the same breath is not. THE DISPOSITION: those three are named here so they get an independent read, and the person blessing this gate is that read. They are req-the-desk-is-usable-soon-after-the-folder-opens, req-what-the-corpus-is-has-one-answer, and req-only-a-file-with-its-own-door-is-withheld.
- TWO NUMBERS IN THIS SET ARE CHOSEN RATHER THAN MEASURED, and a target reads exactly like an observation once it is a number in a document => Real, and handled inside the rows rather than by a register entry. Both say in their own words that they are a chosen bar with their reasoning, and both name what would move them. The five-second bar has never been measured against a person, and neither has the two-in-three.
- A PROBE FOUND THE CONSENT ROW UNSATISFIED BY WHAT STANDS => NARROWED BY AN OWNER RULING THE SAME DAY, and still worth acting on. The extension activates on the editor finishing startup, which is blind to which folder is open. That is FINE under the rule the owner gave: waking in every window is allowed, provided a folder carrying no machine state is identified and then left completely alone. WHAT IS NOW UNPROBED is whether the extension actually performs that check today and writes nothing when it fails - the manifest settles what wakes it, and says nothing about what it does next. The design milestone inherits one question rather than one redesign, which is a smaller and more accurate finding than the one first written here.
- THE SET COULD HAVE BEEN CALLED COMPLETE AT write-requirements AND WAS NOT => The three gaps were found only because the sweep was adjudicated one characteristic at a time. Answering it as a block would have passed, which is the argument for the owner's instruction that the nine be walked rather than summarised.

## raid_additions

- none

## verdict

pass — design input is complete and the three demands that had nothing under them now have rows rather than reasons. The sweep was adjudicated one characteristic at a time, which is what found them: performance efficiency and maintainability had both been answered as untouched, and the lane's file-level exclusion had a goal with no requirement. One thing is conceded rather than defended: those three rows were written at this gate by the same hand that is judging them, so they have had no independent read, and the bless on this gate is that read.

## follow_up

THE DESIGN MILESTONE INHERITS FOUR CHOICES, each with its space already narrowed rather than left open. It was five when this gate signed.

- How a folder is recognised as a project, where a marker check and a walk upward are both mainstream.
- Which activation event brings the product up, given that today's is blind to the folder and is the latest of those available.
- STRUCK. This read "where the consent record lives". No record is kept, on the owner's ruling of 2026-08-19, so design has nothing to place.
- Which mechanism presents the one thing to run, from four that a host already scans for.
- How callers come to agree about the corpus, where one reader is the obvious answer and not the only one.

THREE ROWS WANT AN INDEPENDENT READ before they are built on, and they are named in the red team. They were written at this gate and judged in the same pass.

TWO QUESTIONS ARE OWED TO A PERSON AND NEITHER IS EXPENSIVE. Whether the ruling that pinned session state meant depth as well as branch is one sentence, and that assumption is graded crippling with every goal sitting on it. Whether anybody can name three people who have never seen this product decides whether the discoverability row can be verified at all.

ONE CALLER IS ALREADY KNOWN FOR THE COLLAPSE'S COUNT. The engine manifest's serve script starts the lane with a root two levels up, which is exactly the wrapper this iteration removes. It fell out of an unrelated probe, which suggests the count will find more than the scope list names.

## anything_else

