---
form: cand-the-repository-is-the-state
by: agent
signed_off: 2026-08-19T15:48:40.540Z
authors: agent
files:
---

# Evidence form / cand-the-repository-is-the-state

## current_situation

THE THIRD AND LAST LINE IS COMPOSED, and composing it found a contradiction between two of its own picks.

THE MACHINE-STATE CELL SAYS THERE ARE NO PLAIN FILES, only commits and refs. The drawn-surface cell stops drawing things BECAUSE they are plain files a person can open. Both cannot be true of everything.

IT IS RESOLVED RATHER THAN RECORDED, and the resolution keeps both halves. The reading file has to survive, because it exists so a person with no lane can read what the walk owes them, and it is session state that version control was never going to hold. So the headline number moves from one file outside version control to two.

THE DRAWN-SURFACE CELL THEREFORE DELIVERS LESS ON THIS LINE than on the other two. It shows two files rather than the whole stored half. That is now written on the candidate.

THE OPTION NODE AND THE DRAWING WERE BOTH CORRECTED so the number agrees everywhere it appears.

## built

project/spec/trace/candidate/cand-the-repository-is-the-state.md — the three sections the item card assigns to this state are composed.

HOW IT WORKS leads with the contradiction and its resolution, because that is the most important thing composing produced. It then names three more seams, including the one where this line beats the loudness line directly: walking up to a repository works on a machine with no editor, and a host's pattern test does not.

WHAT IT COSTS names the largest open risk plainly. Nothing here measures what a ref write costs on the walk's hot path, and no probe was run on it. That is the number this line most needs and does not have.

WHAT IT LEANS ON carries five assumptions, and the unmeasured performance one is marked as this line's biggest unknown.

TWO OTHER FILES WERE CORRECTED so the number agrees everywhere. opt-there-is-no-machine-state-folder-the-repository-is-the-state now says two files and says what found the second. The candidate drawing's step statement moved with it.

NOTHING HERE IS SCORED. The node says this is the largest cost on the chart, which is a property of its own cells.

## follow_up

ALL THREE LINES ARE COMPOSED. The join hands them to evaluate-set.

ONE NUMBER IS OWED AND NOBODY HAS IT. What a ref or commit write costs on the walk's hot path. It decides whether the repository line is viable at all, and a rough check cannot settle it. If that line survives the cut, it is the first thing M6 should spike.

ONE HOLE IS IN A ROW RATHER THAN IN A LINE. The open row describes a host testing a pattern, which does not exist on a machine with no editor. Two of the three lines merge the unattended arrival into the same path, and only one of them has a way in that works there. The gate should read that as a missing cell, not as a weakness of either line.

ONE COST ESTIMATE MOVED DOWNWARD ON MEASURED EVIDENCE, and one headline number moved upward on a resolved contradiction. Both are recorded on the nodes rather than only here.

CUT-CRITERIA WAS WALKED THROUGH BEFORE THIS MACHINE RAN. Its evidence should be checked before evaluate-set reads it, because criteria fixed after the candidates are composed are not fixed at all.

## anything_else

