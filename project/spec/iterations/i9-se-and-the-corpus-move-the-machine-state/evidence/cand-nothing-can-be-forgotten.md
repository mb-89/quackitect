---
form: cand-nothing-can-be-forgotten
by: agent
signed_off: 2026-08-19T15:46:45.422Z
authors: agent
files:
---

# Evidence form / cand-nothing-can-be-forgotten

## current_situation

THE SECOND OF THREE LINES IS COMPOSED. It arrived with eight picks and one paragraph, and leaves with its architecture, its seams, its rough checks and five assumptions.

COMPOSING CHANGED ONE COST ESTIMATE, downward, on evidence. The include list is cheaper than its own option node argues, because probe P3 measured that both producing acts run our own filter function rather than a vendor's ignore file. Inverting a filter is a change in one place.

COMPOSING ALSO FOUND A SEAM THAT DRAWING THE LINE DID NOT. This line takes the host's pattern test as the way in and also merges the unattended arrival into that same path. A machine with no editor has no host to run the test, so the merged function needs a second way in that the open row does not describe.

NO SCORING HAPPENED HERE. The node names this as the most expensive line on the chart, which is a property of its own cells rather than a comparison.

## built

project/spec/trace/candidate/cand-nothing-can-be-forgotten.md — the three sections the item card assigns to this state are composed.

HOW IT WORKS traces an open end to end, then names three seams. The strongest is that this line CLOSES the seam the reference line leaves open: ownership is stated and acknowledged, so two host mechanisms disagreeing is a refusal rather than a lane rooted somewhere nobody chose.

WHAT IT COSTS orders the failure modes by how badly they fail rather than by likelihood, because every failure in this delta is silent and that ordering is the line's whole argument. The worst is the merged arrival losing its second guarantee, which leaves an agent believing it is caged when it is not.

WHAT IT LEANS ON gained one assumption from composing: that a machine with no editor has a way in at all. Four of the five were already known.

ONE ESTIMATE MOVED ON MEASURED EVIDENCE. The include list's cost dropped, and the node says which probe moved it and what that probe does not cover.

## follow_up

ONE LINE REMAINS.

ONE ASSUMPTION HERE WANTS A REAL SPIKE RATHER THAN A ROUGH CHECK, and M6 is where that belongs. Whether the arrival's two-sided guarantee survives being folded into a path built for a present person cannot be settled by reading. It is the only piece on this line whose failure is worse than what it replaces.

ONE GAP IS CARRIED RATHER THAN CLOSED. The open row describes a host testing a pattern, which does not exist on an unattended machine. This line needs a second way in and the chart has no cell for one. That is a hole in the ROW rather than in the line, and the gate should see it as such.

ONE COST ESTIMATE ON ANOTHER NODE IS NOW STALE IN OUR FAVOUR. opt-package-from-an-include-list-so-an-unlisted-file-cannot-ship says a packaging tool that only supports exclusion patterns forces a build step. Probe P3 measured that neither producing act is such a tool. The option node still carries the general warning, which is right, and this candidate carries the measured local answer.

## anything_else

