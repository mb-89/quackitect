---
steps:
  - id: the-declaration
    statement: "One module answers what the machine-state folder is called and which files inside it have a structured door. Two facts, no third, and nothing else decides where the folder sits."
    depends_on: []
    realization: software
  - id: the-generated-consumers
    statement: "Generate the five consumers from the declaration rather than writing them beside it: the path resolver's exclusion, the producing acts' exclusion, the search ignore, the table sweep's skip set and the editor's hide rule."
    depends_on: [the-declaration]
    realization: software
  - id: the-staleness-check
    statement: "A check that each generated consumer matches the declaration, and it fails where a person will see it. It guards a FATAL requirement now, because the root check runs a test generated from the same place."
    depends_on: [the-generated-consumers]
    realization: software
  - id: the-root-check
    statement: "The system decides its own root: run the host's own content test once per open folder and count carriers. Refuse on nought, bind on one, refuse and ask on two or more. Use a file-exists call and not a search."
    depends_on: [the-declaration]
    realization: software
  - id: the-exclusion-split
    statement: "The lane withholds a file because a structured verb serves it, never because of the folder it sits in, and the refusal names the verb that serves it."
    depends_on: [the-generated-consumers]
    realization: software
  - id: the-move
    statement: "Move the machine-state folder into the opened folder, over the twenty-six hard-coded sites a spike counted. Nothing is renamed and nothing is deleted while things are moving, and the eleven recorded queries are re-run afterwards."
    depends_on: [the-generated-consumers]
    realization: software
  - id: the-branch-pin
    statement: "The test that stops the branch-independence ruling being trusted: bind a record, write session state, switch branch, read it back, and switch back."
    depends_on: [the-move]
    realization: software
  - id: the-one-corpus-reader
    statement: "Delete the second corpus reader and route every caller through the one that remains, so a malformed node has one answer and the failing path is named in it."
    depends_on: []
    realization: software
  - id: the-install-preflight
    statement: "One read-only check runs every precondition before the install writes anything, names each missing tool with its version and where to get it, and changes nothing on disk. Then the entry point verifies itself after the install and reports ready."
    depends_on: []
    realization: software
---
