---
form: gate-release
bless: blessed by agent
by: agent
signed_off: 2026-08-12T15:18:23.533Z
authors: agent
files:
---

# Evidence form / gate-release

## current_situation

The 4.0.0 archive stands signed at the package state with its works check yes. This gate judges the ship. The two absent rows (the emitted book, the dependency rulings) wait outside the matrix per the owner's ruling of 2026-08-11.

## market_block


## round_0_verify

- evidence vs claims: the package state's claim stands signed - the zip on disk, the works check yes with its observations; gate-validation signed and blessed behind it with all seven props and seven must stories answered
- types: tsc runs blocking in the pre-commit and passed on trunk commit 3af19d31, the last engine change (the packager's exclude fix)
- lint: biome check --error-on-warnings passed blocking on the same commit
- tests: the packaged engine ran its own desk suite cold from the archive - 5 of 5; the battery stands green from the verification submit

## round_1_validate

- exercised against the goal: the shippable artifact exists and worked cold - extracted, installed in 3 seconds, booted to the desk from the archive alone
- missing: the fresh-machine first-timer run, owed since 3.0.0 and named on the package claim; the claim-pool surfaces, standing as raid-debt-claim-pool-surfaces
- wrong: nothing found at this gate
- out of scope: the emitted book (finalize-docs) and the dependency rulings (ship-review) - outside the matrix until their mechanisms exist
- prior art: not re-scanned here - gate-validation's live scan is one state old and stands

## round_2_red_team

- the archive ships something that should stay home => swept: no records, no session state, no scratch probes, no installed dependencies inside; the scratchpad leak was caught by this iteration's own check and fixed on trunk
- the package works here but not on a stranger's machine => the contained check used the archive alone; the true fresh-machine run is the named owed gap, unchanged from 3.0.0
- the version lies about compatibility => the major bump is earned - drawings authored with rung words refuse on 3.x engines, and the release entry says so plainly
- a Windows peer in a deep folder cannot seed => raid-issue-windows-longpaths stands open with two one-act fixes; it does not gate this ship because the default install path is shallow

## raid_additions

- none

## verdict

pass — the artifact stands, worked cold, and its named gaps are register-ridden with triggers.

## follow_up

shipped closes the record; a needs-retro note fires the retro, which drains the register debts on the owner's standing ruling.

## anything_else

