---
form: claims-branch
by: agent
signed_off: 2026-08-11T17:25:48.400Z
authors: agent
files:
---

# Evidence form / claims-branch

## current_situation

The claims-branch line composes the blessed architecture whole; its note carries the seams, the costs and what it leans on, written at minting and deepened here.

## built

The composition stands in cand-claims-branch, three sections per the item card. HOW IT WORKS - the seams: seeding pushes the stub branch as machinery; claiming writes claims/<iteration>.md (minted machine id, UTC time) on the claims branch and pushes in one act; a rejected push is a lost race handled by re-fetch and re-pick; offline the announce waits while the local claim binds this machine; release is a person's recorded force commit. WHAT IT COSTS - one extra branch to fetch and an ever-growing small-file history; the race mechanism measured locally on 2026-08-11 (push A landed, push B rejected non-fast-forward). WHAT IT LEANS ON - the remote serializing pushes (register assumption, local half measured, origin half at M7) and credentials on every machine (register dependency).

## follow_up

The two sibling composers run the same write-back; the cut and the scoring follow.

## anything_else

