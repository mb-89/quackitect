---
form: draft-vision
by: agent
signed_off: 2026-08-11T15:59:12.913Z
authors: agent
files:
---

# Evidence form / draft-vision

## current_situation

i2 stands pinned at major on the blessed kickoff; the resident vision packet from the product iteration stands untouched on the trace. This state judges whether the parallelism change bends it — it bends the goal system by one goal and touches nothing else.

## big_idea

INHERITED by pointer — the resident vision stands: an engineer drives an agent through a drawn state machine, and the engine enforces the order instead of reminding. THE DELTA, one breath: the engine now serves MANY HANDS — several machines, several agents, one product, one ledger — and any drawn machine gets the same teeth.

## to_be_world

The resident to-be world stands; this iteration adds one scene. An engineer's desk machine walks one iteration while their laptop walks another; a colleague's agent claims a third from the same git remote. Nobody collides: a claim is taken in one push, a taken iteration is visibly taken everywhere, and an abandoned one is quietly forced free. The record stays one record — whoever asks why a year later still gets the answer in one click, whichever machine did the work.

## goal_system

INHERITED, with one goal added and its conflict named openly. NEW GOAL: throughput scales sideways — more machines and agents mean more shipped per day, under the same rigor. NAMED CONFLICT 1: sideways scale strains the one-ledger promise — many writers, one truth; resolved by the claim lock (collision-free work assignment) and the standing one-trunk merge discipline. NAMED CONFLICT 2: the engine's generality (any drawn machine) against the SE machine's specificity — resolved by the value-prop split: vp-the-engine promises the platform, vp-systematic-engineering promises the flagship machine, and neither swallows the other.

## moore_pitch

FOR engineers driving agents WHO need more throughput than one machine gives, Quackitect is the process engine THAT lets many agents work one product in parallel — iterations claimed over git, first push wins, one refusable ledger. UNLIKE workflow engines (BPMN, Temporal, n8n) and agent fleets, which run flows or spawn swarms with no evidence discipline — and unlike what the AI crowd ships as an ontology, a schema with no teeth — OUR engine refuses: every hand, every machine, the same enforced order and one record that answers why.

## follow_up

The goal-system delta feeds M2: the value-prop work (mint vp-the-engine, scope vp-systematic-engineering, the ontology Unlike line, the rewiring) and the risk log pick up from here.

## anything_else

