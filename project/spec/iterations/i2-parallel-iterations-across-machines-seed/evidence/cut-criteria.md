---
form: cut-criteria
by: agent
signed_off: 2026-08-11T18:48:37.471Z
authors: agent
files:
---

# Evidence form / cut-criteria

## current_situation

Three candidates stand composed; the six-row judged pool arrives from derive-criteria with its chain settled and no compounding. The cut runs against the composed set before any score is written.

## cuts

1. [[req-unshipped-dependency-refused]] [cut: identical by construction — no claim-question pick touches dependency refusal; all three lines inherit the same engine behavior]
2. [[req-force-release-recorded]] [cutoff]
3. [[req-autonomy-is-categorical]] [cut: identical by construction — the autonomy vocabulary rides the engine, not the claim design]
4. [[req-nodes-scoped-to-iteration]] [cut: identical by construction — node scoping rides the shared graph picks on every line]
5. [[req-walk-branches-at-waypoint]] [cut: identical by construction — the walk's branching rides the shared picks on every line]
6. [[req-pin-writes-seeded-scaffolds]] [cut: identical by construction — pinning precedes any claim; no line touches it]

## follow_up

evaluate-set scores the three lines on the surviving axis beside the must gates; the cut list rides beside the criteria table for the gate to read.

## anything_else

## cut

- req-unshipped-dependency-refused: CUT — reads only cells the three lines share; no claim-question pick touches dependency refusal, so every candidate meets it identically by construction
- req-autonomy-is-categorical: CUT — the autonomy vocabulary rides the engine, not the claim design; every line inherits the same behavior
- req-nodes-scoped-to-iteration: CUT — node scoping rides the shared graph picks, identical on every line
- req-walk-branches-at-waypoint: CUT — the walk's branching rides the shared picks, identical on every line
- req-pin-writes-seeded-scaffolds: CUT — pinning precedes any claim; no line touches it
- req-force-release-recorded: KEPT — the release story differs by construction: a recorded force commit (claims-branch), a ref delete that erases the history unless mirrored (refs-cas), and no release at all (preassign)

## order

1. req-force-release-recorded — the only surviving axis; the rough sort has nothing to move and no move was made

## boundary

the line sits below rank 1: the one surviving axis is scored. A one-axis table is honest here because the five struck axes went by construction — the lines' cells are identical where those axes read — never by resemblance.
