---
form: gate-candidates
bless: blessed by agent
by: agent
signed_off: 2026-08-11T18:54:00.352Z
authors: agent
files:
---

# Evidence form / gate-candidates

## current_situation

The set stands evaluated: three composed lines, one surviving axis, second-hand scores, a computed front of one. This gate judges the reasons no check can make and blesses the front, never a winner.

## reasons_hold

THE CUTS, checked against the candidate records rather than taken on trust. The three lines pick identical options everywhere outside the three claim questions - seeds-ride-their-stub-branch, graph-with-cycles, derive-every-view-on-every-look, the-branch-is-the-record, no-installer-clone-the-template, the-stray-is-a-log-entry are the same cells on every line. The five cut axes (dependency refusal, autonomy vocabulary, node scoping, walk branching, pinning) read none of the claim questions, so "identical by construction" is literal: the cells those axes see are the same objects. The cuts hold. A future candidate differing in a shared cell would re-demand the axes, and the standing-artifact mechanics grey the state when one arrives.

THE MOVES: none were made. The computed order stood; nothing was jumped toward a favourite.

THE FRONT OF ONE, which is the real question here. The space did not collapse upstream: the pool held six judged axes and the cut struck five by construction, so the decision honestly turned on the one axis the lines differ on - the release story - plus the must gates. The alternatives were real. refs-cas was drawn to make claims-branch defend its extra branch, and it forced exactly that defence: the branch buys the visible ledger and the permanent release record that the ref-delete erases. preassign was drawn as the null line to be beaten, and a BLIND scorer beat it on the axis (no distinct force act) rather than the composer waving it off. The scores were second-hand: a spawned clean-context agent, per the 2026-08-10 ruling, and its refusal reasons are on the evaluate-set record.

RESERVATION, carried to M5: a one-axis table is thin, and the leader leans on an assumption whose origin half is unmeasured - the remote serializing pushes (raid-asm-remote-serializes-claims). reverse-sensitivity owes the tripwire: if the origin half falsifies, the claims-branch lock breaks and the flip is credible. The bless is of the FRONT, not a winner; convergence stays M5's.

## round_0_verify

- evidence vs claims: the chart, the cutoff, the compositions and the scores all stand on their records; the front is computed from the table, typed by nobody; the scores are second-hand per the 2026-08-10 ruling and landed verbatim
- types: no engine code changed in this state; nothing to typecheck beyond the nodes, which compile into the chart and table views
- lint: the register and candidate nodes pass the submit checks that ran here; zero TBD markers in the register
- tests: no code under test at this state; the race mechanism's local half is measured (probe 2026-08-11), the origin half rides M7

## round_1_validate

- exercised against the goal: the two-machines must story and uc-claim-an-iteration walk through the leader - seed visible on its stub branch, claim one pushed file first-push-wins, offline claim reconciles, force release recorded with who and why
- missing: nothing found - the null line and the purist line were both drawn as full candidates and both beaten on the record, not waved off
- wrong: nothing found - both eliminations match the candidates' own recorded costs, written before scoring
- out of scope: convergence and the winner (M5's); the origin-half serialization measurement (M7's)
- prior art: the leader matches the lease-branch pattern and library circulation, named on its score; refs-cas matches Gerrit's ref namespaces and inherits their known invisibility

## round_2_red_team

- a cut axis could hide a differentiator => re-read all five cuts against the lines: each reads only cells the three lines share identically, so no cut can move the ranking; held
- the leader's lock leans on unmeasured remote serialization => registered as raid-asm-remote-serializes-claims; a mis-serialized push degrades to a late loser on re-fetch, not a corrupt ledger; M5 owes the tripwire and M7 the measurement
- refs-cas resurrected by adding a mirror => the mirror forks the single ledger its own note records; visibility is bought back at exactly the cost the surviving axis measures; attack fails
- the blind scorer received composer prose inside the candidate notes => within the 2026-08-10 rule, which names the records as the scorer's input; the channel is real and noted for the retro

## raid_additions

- none new: the leader's flip risk is already registered as raid-asm-remote-serializes-claims (origin half unmeasured, M7 measures it); reverse-sensitivity owes its tripwire at M5

## verdict

pass — the front of one is honest: real alternatives drawn adversarially, cut by construction, scored blind, eliminated by arithmetic; the front (cand-claims-branch) is blessed as the set, and convergence stays M5's

## follow_up

converge-pugh runs with claims-branch as datum; reverse-sensitivity writes the serialization tripwire; the M7 build measures the origin half of the race.

## anything_else

