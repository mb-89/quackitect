---
form: reverse-sensitivity
by: agent
signed_off: 2026-08-11T18:54:54.927Z
authors: agent
files:
---

# Evidence form / reverse-sensitivity

## current_situation

The winner's seat is stress-tested before it is declared: the engine tables the swing cells, and every plausible losing world gets ruled credible or not, with credible ones minting tripwires.

## sensitivity

- refs-cas +2 on req-force-release-recorded (a maintained mirror restores the release record): not credible — the mirror is unbuilt machinery whose own note records that it forks the single ledger; the cost the axis measures returns through the mirror itself
- preassign +2 on req-force-release-recorded (a dedicated release verb over stub edits): not credible — a release verb rebuilds the claim mechanism the null line exists to trim; preassign with a force verb is no longer preassign
- claims-branch unseated by the world where the remote mis-serializes pushes: credible — only the LOCAL half of raid-asm-remote-serializes-claims is measured; the origin half rides M7. Tripwire: the M7 build measures the push race against the real origin before the claim lane ships. Fallback: preassign partitioning holds the fleet while the lock is redesigned — the null line is the degraded mode, not a redesign under fire.

## follow_up

declare-winner names cand-claims-branch on the standing arithmetic; record-adrs writes the deciding ADRs and the register carries the tripwire with its fallback.

## anything_else

