---
form: gate-kickoff
bless: blessed by agent
by: agent
signed_off: 2026-08-11T15:55:04.303Z
authors: agent
files:
---

# Evidence form / gate-kickoff

## current_situation

i2 stands seeded from the owner's parallelism rulings; the onboarding retro is stamped; three discussion rounds with the owner (2026-08-11, recorded as aq entries) settled the design and ended with the owner's go at major.

## retro_drained

- note-e225dfdd356e: drained done — the categorical-autonomy ruling rides this kickoff's brief as an in-scope rider; the inbox stands at zero

## goal

Parallel iterations across machines: seeds live on git, a claim marks who works what, dependency edges gate claiming — and the promise behind it is minted as vp-the-engine (any drawn machine gets teeth; many hands drive it concurrently).

## pulled_in

- the categorical autonomy rework: the word slider and the 0-to-1 numbers go everywhere, the tiers carry it (owner ruling 2026-08-11)
- the one-source version: package.json carries it once, the four bootstrap stamps go
- the refusal-guidance anchors and their enforcing test
- the se_git_sync duplicate-key sweep over the merged tree
- the refs.test.ts split by theme (153 s summed, the battery's heaviest file)
- the paint test pin (green is submitted, thumb is blessed, law-proven greens)

## left_out

- kind B, one engine driving many hands on one machine — parked, ready when the owner orders that round
- kind C, sub-agent tier rules — parked, ready when the autonomy guidance is next edited
- the SVG panel round (browsable archive, bless repaint, why-not-green, OR bar) — the owner opens it
- the reading-credit fix rides i3 unless the owner pulls it in
- the i5 stub drops as double-counted

## change_size

major — the iteration mints a value proposition (vp-the-engine) and retires the numeric autonomy scale product-wide; the implementation architecture itself is not upended, but the promise set and the autonomy model both change, and that is major by the column's own bar.

## round_0_verify

- evidence vs claims: the seed vision carries the owner's rulings near-verbatim, and the six design answers stand as recorded aq entries in the call log
- types: green — the battery's preflight typecheck passed at i1's close on this same tree (1088 of 1088, trunk 00e0e0be); nothing built since
- lint: biome green at the same battery, zero errors
- tests: the same battery stands; scoped runs ride every i2 build step and the battery is earned at the gates

## round_1_validate

- exercised against the goal: three live discussion rounds settled the push ruling, the claims home, the claim identity, the change size, the scope and the value-prop shape — the goal is the owner's own words
- missing: kinds B and C of the parallelism design, deferred by the owner's ruling and parked with ready-whens; the reading-credit toll rides i3 unless pulled in
- wrong: nothing known wrong; the i5 stub double-counts the hygiene riders and drops
- out of scope: two agents on one machine, sub-agent tiers, the SVG panel round
- prior art: first-push-wins on a shared ref is the standard git-native distributed-lock pattern (lease branches in CI fleets, git-based locks), compared against a coordinator service — we shed the coordinator because the remote already serializes pushes; named from practice, no single primary fetched

## round_2_red_team

- the claim push relaxes the never-push rule, work could leak => steelman: one sanctioned push is the thin end; answer: the engine pushes ONLY the seed stub and the claim file as machinery acts, v3 and the record branches stay the owner's, and the refusal keeps refusing every agent push
- a dead machine's stale claim wedges an iteration => the claim carries the machine id and a timestamp, and a quiet FORCE flag overrides an abandoned claim (owner ruling 2026-08-11) — against silence, not malice
- the lock itself could be a fiction => the design leans on the remote serializing pushes; that is now a register assumption with its probe (raid-asm-remote-serializes-claims), run against origin before the mechanism is called done — this is the kill-criterion, and it is testable in minutes

## raid_additions

- raid-asm-remote-serializes-claims

## verdict

pass — the scope is the owner's own rulings from three recorded rounds, the size is major by their word, and the one assumption the lock leans on stands on the register with a runnable probe. The bless pins the machine.

## follow_up

On the bless the machine pins at major and the walk enters M1: the value-prop minting and rewiring come first, the claim mechanism follows, the riders land where their milestones sit.

## anything_else

## kickoff_brief

ONE NOVEL IDEA: iterations become claimable across machines. Seeding pushes the stub to git as a machinery act; a claim is one small file on a dedicated claims branch; the push race is the lock (first wins, the loser re-fetches and picks another); the claimant is a minted 8-hex machine id stored outside git, never a hostname. A claim can be OVERRIDDEN (owner ruling 2026-08-11): a quiet force flag releases an abandoned claim — guarding against a machine that claims and goes silent, never against malice; deliberately low-key, not a everyday control. Dependency edges between iterations get recorded, drawn and claim-gated — i2 gates i3-i7, which run as parallel legs.

VALUE-PROP WORK at M1-M2 (owner rulings): mint vp-the-engine — any drawn machine gets teeth, and many hands drive it concurrently; the many-hands facet absorbs what would have been its own prop. Scope vp-systematic-engineering to the SE machine with one Unlike line. Put the ontology sentence in the Unlike where it stings. Rewire requirements and trace edges onto vp-the-engine where the mapping honestly improves; slight reformulation is sanctioned.

RIDERS (the owner's standing rule - mechanical work rides in): the categorical autonomy rework (the word slider and the 0-to-1 numbers go everywhere; the tiers operational/tactical/strategic carry it); the one-source version; the refusal-guidance anchors with their enforcing test; the sync duplicate-key sweep; the refs.test split; the paint test pin.

OUT OF SCOPE, parked with ready-whens: one engine driving many hands on one machine (kind B), sub-agent tier rules (kind C), the SVG panel round. The i5 stub drops as double-counted.
