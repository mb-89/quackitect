---
form: gate-inputs
bless: blessed by human
by: agent
signed_off: 2026-08-11T16:28:36.411Z
authors: agent
files:
---

# Evidence form / gate-inputs

## current_situation

M2 stands whole: the context with its two new neighbours, the role map re-checked, the story corpus at 25 with the two-machines must story, and the new use case carrying the claim pass with its race, offline and force extensions. The owner reserved their look at this gate.

## picture_judged

The input picture holds together against the M1 frame: the context's one moved seam (the remote as coordination surface) is exactly where the vision's new goal lives, the two new neighbours carry the interfaces the claim pass crosses, and no role moved - the engineer gains throughput, the agent gains a peer discipline, nobody newly loses.

## unspecified_capability

One capability is deliberately under-specified and named so: HOW the machinery lists claimable iterations with their dependency state (uc-claim-an-iteration step 1) - the listing surface is M4-M5 design territory, and specifying it here would freeze mechanism as requirement. Nothing else known unspecified: the offline, race and force branches all stand as extensions.

## passes_concrete

The must pass is concrete end to end: seed a batch at the desk, open the laptop, claim with one push, watch the race resolve by first-push-wins, ship two iterations and read both attributed in the archive. Real remote (github.com/mb-89/quackitect), real machine ids, and the demonstration counts closes rather than impressions.

## round_0_verify

- evidence vs claims: every M2 claim is a node on file - two neighbours, one story, one use case minted; the coverage laws ran at both story and use-case submits and passed
- types: unchanged code tree; M2 added trace markdown only
- lint: the new nodes follow their templates; biome untouched by markdown
- tests: the battery stands green from i1's close; no code changed since

## round_1_validate

- exercised against the goal: the inputs answer the owner's parallelism design directly - the claim pass is now a story a person can falsify and a use case requirements derive from
- missing: nothing known - the offline ruling and the force override both landed as extensions rather than staying chat
- wrong: nothing known wrong
- out of scope: kinds B and C stay excluded per the binding list at draw-context
- prior art: the story and use-case shapes are the corpus's own (Alley decks, Cockburn cases); the claim mechanics positioned at M1 against git-native locks

## round_2_red_team

- the use case could be a design in disguise, freezing mechanism as requirement => it names outcomes (visible claim, honored race, local claim offline) and leaves the mechanism to M4-M5; the one named mechanism, the push, is the M1-blessed architecture itself
- the must story could be undemonstrable on one desk => it needs two machines, and the owner's second machine is already planned - the demonstration doubles as the owed ramp-up run
- the rewire could have weakened the flagship prop => it kept seven stories and its own must set; only the live-drawing story moved, with its reason recorded

## raid_additions

- none — the five standing entries carry this review's concerns

## verdict

pass — the users are understood in the users' own words: the owner dictated the pass, the story tells it, the use case generalizes it, and both coverage laws hold mechanically. Presented for the owner's look as reserved.

## follow_up

On the owner's bless M3 opens: requirements derive from the claim use case's steps and extensions.

## anything_else

