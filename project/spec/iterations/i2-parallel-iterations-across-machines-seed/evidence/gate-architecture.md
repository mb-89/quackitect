---
form: gate-architecture
bless: blessed by human
by: agent
signed_off: 2026-08-11T19:08:30.456Z
authors: agent
files:
---

# Evidence form / gate-architecture

## current_situation

The baseline stands for review: one new element (the claim ledger), the owed interface answered, the inherited record-life elements, the ruled deck and the deciding ADR. This fill is the review's evidence; the bless stays the owner's hand at this gate.

## round_0_verify

- evidence vs claims: the matrix, the deck and the register derive from the nodes on every look; the ADR carries its grades, rejected options and consequences; the interface names both ends and the flow it carries
- types: no engine code changes in this baseline; the nodes compile into the matrix and deck views
- lint: the new nodes (element, interface, decision, requirement) follow their item shapes - the upstream submits refused until they did
- tests: nothing executable at this state; the claim mechanics carry one measured half (the local race probe, 2026-08-11) and one owed half (origin, M7)

## round_1_validate

- exercised against the goal: the two-machines story walks the structure end to end - seed pushed on its stub branch, claim through the ledger, record opened over the standing claim through the owed interface, release by recorded force
- missing: the allocation spread on hold-the-work is incomplete - the resume family lists only el-record-store while the serving lives in el-walk-engine; the owner's stale-data observation, filed as note-6f2990e0b16f, cleanup owed before M7 builds
- wrong: nothing found - no element implements nothing, no function stands unimplemented
- out of scope: the deck's delta-scoping (note-57d654fdc89d) and the adrs list template (note-0bb21ad5e8d3) - method edits for the retro
- prior art: the ledger matches the lease-branch lock pattern and library circulation, named on the scores; the structure adds no mechanism beyond them

## round_2_red_team

- the claim ledger is one element hiding two mechanisms, lock and outbox => a split adds an interface no flow crossing demands; record-then-announce is one mechanism seen at two moments; held as one element
- the boot row's chain lands on el-record-store, the wrong realizer => real - the concrete face of the stale allocation; the fix is a spread (el-walk-engine also implements hold-the-work), a content edit riding the cleanup pass, and it moves no decision made here
- the at-risk desync window could hide a corrupting write => it cannot corrupt: claims are add-only and the conflict surfaces at the eventual push; the worst case is a duplicated day, already graded corrosive on the register
- M7 could find the origin race behaves differently and invalidate the baseline => the return path is drawn: a new element found mid-build returns to this gate, never in silently, and the fallback (preassign) stands on the ADR

## raid_additions

- none new: the desync window and the serialization assumption already stand on the register with their triggers; the allocation cleanup is a note, not a register entry

## verdict

pass with overrides — the decomposition is sound and the trace closes on both paths, with two named blemishes overridden: the stale allocation spread on hold-the-work (cleanup owed before M7, note-6f2990e0b16f) and the deck's full re-deal (method note for the retro). The bless is deliberately not carried here: the owner adjudicates this gate and presses it themselves.

## follow_up

The owner's bless closes the gate. Then rank-unknowns picks the spike targets with the origin-half race leading, and the allocation cleanup runs before M7 builds inside the baseline.

## anything_else

