---
form: gate-prototype
bless: blessed by agent
reopened: "2026-08-11T20:05:49.094Z — diagnostic re-earn: the route holds this gate unmet while its file stands signed and blessed — the resubmit forces the check to name what it dislikes, or clears the…"
by: agent
signed_off: 2026-08-12T10:59:12.202Z
authors: agent
files:
---

# Evidence form / gate-prototype

## current_situation

One spike was seeded, ran green inside its timebox, and left its experiment node with the fold keys answered. The gate judges buildability on that evidence.

## buildable

yes — the winner's mechanism is measured end to end locally (9 of 9 checks): the lock, the lost-race recovery, the offline reconcile and the recorded force release all ran as code. The structure is one new element and one owed interface on an inherited baseline. The one live hinge — the forge's concurrent receive half — is named, registered, scheduled at M7, and carries preassign as its fallback. Nothing in the design waits on an unbuilt capability.

## round_0_verify

- evidence vs claims: the spike's 9 checks ran green in 2.9 s; the experiment node carries the measurement, what was faked and the pre-agreed fallback; fold-back's submit refuses unanswered fold keys by construction
- types: no engine change rides this gate; the spike is record content and stays out of the shipped tree
- lint: the experiment node passed the item checks — the form vocabulary refused until it fit the scale
- tests: the spike script IS the run, 9 of 9; the last engine suites ran 20 of 20 green on the walker fix

## round_1_validate

- exercised against the goal: the two-machines story's claim pass ran as code — claim, race, lost-race recovery, offline reconcile, force release, and the whole ledger read fresh
- missing: the forge's concurrent receive half — named on the node, registered, scheduled at M7; absent by schedule, never silently
- wrong: nothing found — every measured behavior matched the candidate's composed mechanism
- out of scope: the M7 build itself and the origin measurement; the spike code ships nothing
- prior art: the measured behavior matches the lease-branch pattern the winner was scored against; nothing new appeared under test

## round_2_red_team

- sequential pushes could mask a true concurrency hole => the proven property (non-fast-forward rejection on a taken ref) is order-independent; the concurrent half is exactly what M7's origin race owes, and the node's faked field says so in its own words
- rebase-and-retry could silently merge two claims for one iteration => add-only files cannot merge-conflict, and the race check proves the second writer REJECTS rather than merges; reconcile lands only claims for iterations still unclaimed
- the green could be read as the origin half proven => the register entry keeps probed scheduled-at-M7, and the node forbids that reading
- the code could ship instead of the mechanism => promote names the mechanism and calls the code throwaway; gate-implementation catches spike code shipping

## raid_additions

- none new: the origin-half measurement and the credentials dependency already stand with their triggers; the entry keeps the dated local measurement extended to the whole verb

## verdict

pass — the riskiest assumption's local half is measured through the whole verb, the design is buildable on evidence rather than belief, and the one owed half stands as a named debt with its M7 measurement and its fallback on the record

## follow_up

M7 opens: author-tests takes the fitness candidates (the boot bench, the one-second pull, kill-and-resume, the race test); the promoted mechanism specifies the build; the origin race runs before the claim lane ships.

## anything_else

