---
form: gate-candidates
bless: blessed by agent
by: agent
signed_off: 2026-08-19T12:36:42.918Z
authors: agent
files:
---

# Evidence form / gate-candidates

## current_situation

Two candidates were composed and both sit on the empty-score front. All seven i36 requirements are must-priority demands, so the decisive question is viability against those demands, not the empty score table.

## reasons_hold

No scored axis was cut because i36 contributed no should/could criteria. The front contains both candidates because there are zero score axes; the gate therefore reads viability directly from the candidate records.

Candidate B now retains MCP handshake identity, shape-level failure routing, interruption diagnosis and tolerant record reading. Its differentiated mechanism is a closed harness type with Unknown plus a heartbeat that corroborates, but never replaces, the Copilot blocking stop hook. That is a real implementation choice without dropping a must demand.

## round_0_verify

- evidence vs claims: pass; build_chart, both candidate notes, cut-criteria and evaluate-set stand and were read.
- types: pass; both candidate nodes have candidate type and completed body sections.
- lint: pass at artifact shape; each candidate resolves from build_chart and evaluates through the recorded front.
- tests: not yet owed; candidate composition is document evidence, and executable proof belongs after the architecture selection.

## round_1_validate

- exercised against the goal: pass; both candidates allocate harness identity, interruption diagnosis, shape-level failure routing, stop behavior and tolerant test-record reading.
- missing: none found at candidate allocation level.
- wrong: the previous Candidate B omitted two demands; its repaired record now retains them and differs by defensive representation and liveness corroboration.
- out of scope: live Copilot stop-hook implementation remains later work within i36; Candidate B makes that implementation path explicit.
- prior art: not compared at this gate; the primary-source comparison was completed at motivation, not candidate viability.

## goals_served

- Measure what every supported host actually provides.: Candidate A and Candidate B both allocate fn-arrive-on-a-machine.identify-the-harness.
- Close the five measured harness breaks in the prepared brief.: both candidates allocate the required response functions; Candidate B adds the Copilot heartbeat corroboration.
- Make the lane report which harness it is talking to.: both candidates identify the harness from the MCP handshake.
- Make future boots quicker by removing the test-metadata recovery step from the manual boot path.: both candidates allocate fn-run-a-governed-walk.tolerate-old-test-records.
- Make oversized pull results recoverable through the lane instead of host files.: this M4 candidate set preserves the standing serve-a-step allocation; neither candidate drops it.

## bound_breaches

- if-agent-harness-to-entrypoint: breached; the latest harness-side MCP interruption cancelled the lane response. The restart preserved the walk, which narrows the observed failure to the harness/client path, but it still lacks a named stopping layer. i36 owns diagnosis and a live Copilot stop-hook proof.

## round_2_red_team

- strongest opposing case => Candidate B may be a refinement of Candidate A rather than a genuinely independent architecture, because both retain all hard-demand functions.
- attack => the liveness mechanism changes materially: Candidate A relies on the blocking veto with evidence reconstruction, while Candidate B adds a typed Unknown boundary and host-visible heartbeat corroboration. The Copilot live proof can distinguish their failure behavior.
- kill criterion => Candidate B is invalid if heartbeat replaces the blocking hook, or if Unknown silently inherits a supported profile. Its candidate record forbids both: heartbeat complements the veto, and Unknown is explicit.
- result => not found in the repaired record.

## raid_additions

- [[raid-asm-an-engineer-can-tell-stop-hook-from-cancellation-today]]
- [[raid-debt-harness-fallback-and-bounds-need-implementation-proof]]

## verdict

pass — both candidates now allocate every must-backed i36 function. The empty score table correctly retains both on the front. Their remaining difference is a valid architecture question for M5: direct blocking-veto baseline versus a closed harness adapter with heartbeat corroboration.

## follow_up

Proceed to M5 convergence. The selected architecture must implement and live-prove a Copilot blocking stop hook with the same forward-progress contract as the Anthropic path; Candidate B's heartbeat remains supplementary evidence only.

## anything_else

