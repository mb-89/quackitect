---
form: sweep-consistency
by: agent
signed_off: 2026-08-19T18:27:07.601Z
authors: agent
files:
---

# Evidence form / sweep-consistency

## current_situation

The iteration changed eight engine modules, one cage file, two extension files and one guidance chapter. The sweep looked for any surface still teaching the way things worked before.

WHAT THE ITERATION CHANGED, as taught behaviour rather than as code.

- Following the lane's own spill cursor is now legal in every state and pays no narration toll. Before, two guards could refuse it.
- The answer bound is derived from the tightest measured host limit rather than chosen.
- The lane identifies the client harness and stamps it on every record.
- The cage list gained `bash` and `rg`, and names `web_search` and `WebSearch` as deliberate exceptions.
- Boot tolerates a test record written before the current shape.

ONE SURFACE WAS STALE and is fixed: `project/guidance/method/lane.md` taught the spill cursor without saying that following it is always permitted. Four sentences were added, and the prompt layer was re-projected, which rewrote AGENTS.md, CLAUDE.md, the VS Code instructions and the three skill copies.

NOTHING ELSE TAUGHT THE OLD WAY. The searches behind that claim are named per class below.

## swept

- [x] command and tool docs
- [x] engine-served strings
- [x] method cards
- [x] matrix rows
- [x] templates and skeletons
- [x] guidance chapters
- [x] book chapters
- [x] README and entry documents
- [x] panels and form help

## follow_up

THE RESIDUE, named rather than skipped.

THE HARNESS REGISTRY, THE LIFECYCLE LOG AND THE STOPPING-LAYER REPORT ARE TAUGHT NOWHERE. That is deliberate and it is not a stale surface. Two of the three have no agent-facing surface at all, which is exactly the debt minted at the gate — `raid-debt-two-must-requirements-compute-a-result-that-reaches-no-surface`. Teaching a report that nothing produces would be worse than the silence. WHEN THE DEBT IS REPAID, `guidance/method/lane.md` is the surface that has to change with it.

THE CAGE LIST IN `lane.md` IS A SUMMARY, NOT AN INVENTORY. It names seven native tools; the shipped `copilot-cage.json` now excludes nineteen. The summary is still true and the inventory is now checked mechanically by `tests/cage-inventory.test.ts`, which is the better place for the full list. Left as is, on purpose.

THE BOOK WAS NOT CHANGED, because nothing in it teaches any of the five behaviours above. That was checked by search, not assumed.

## anything_else

