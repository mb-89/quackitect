---
form: sweep-consistency
by: agent
signed_off: 2026-08-16T19:38:42.511Z
authors: agent
files:
---

# Evidence form / sweep-consistency

## current_situation

THE SWEEP HAS A MECHANICAL HALF NOW, and it ran here first.

engine/bin/sweep.ts is this row's own exit script. It reads every node under project/spec and reports four kinds: a node that will not parse, a value outside its key's vocabulary, a rule with no way forward, and a rule bound to a node the corpus does not hold. 1019 nodes in 327 ms, green.

THE JUDGMENT HALF IS STILL A PERSON'S. Whether a document teaches the current behaviour is not a set operation, and nothing here pretends otherwise. The boxes below are that judgment; the script is the arithmetic under it.

THE LIST WAS BUILT WHILE WALKING rather than at this state. Every item below was found by a check firing on this iteration's own work, and each was recorded where it was found and deferred here.

## swept

- [x] command and tool docs
- [x] engine-served strings
- [x] method cards
- [x] matrix rows
- [x] templates and skeletons
- [x] guidance chapters
- [owed] book chapters — raid-debt-human-observed-demonstrations
- [x] README and entry documents
- [x] panels and form help

## follow_up

THE VALIDATION GATE IS NEXT, then package and release.

THE SWEEP RUNS AT THREE MOMENTS NOW and none of them is a verb: this row's exit, the boot's prepare_idle, and a test decision whose diff is mostly documents. A person never has to remember it, and an agent cannot spam it.

WHAT THIS STATE COULD NOT DO MECHANICALLY, and it is the honest limit: the script reads FRONTMATTER. It knows a value is outside its list and a rule is bound to nothing. It cannot know that a paragraph teaches a behaviour that changed, which is why eight of the nine classes above are still a judgment.

EIGHT NOTES STAND FOR THE RETRO. Four are about the code and four about how I work.

NOTHING IS BLOCKED.

## anything_else

### What each checked class rests on

COMMAND AND TOOL DOCS. se_test's description was rewritten by the owner's ruling and now also names the sweep that rides its decision. Both seed verbs' descriptions were false since i34 — each claimed the seed mints a worktree, mints a branch and pushes it — and both now say what the seed does: a record folder on trunk and nothing else.

ENGINE-SERVED STRINGS, four corrections.

- Two remedies still named se_test's deleted `files` argument, which is a refusal handing back another refusal.
- Three comments named an autonomy SLIDER that no longer exists; the control went categorical and the prose did not. They say dial now.
- The fallen-input refusal now names the chain's ROOT and the verb that fits it.
- The write guard's refusals carry the file, the line, the value and the fix.

METHOD CARDS. Swept for se_test and for test-scope language: no card names either, so none taught the old contract. Nothing to change is a result rather than a blank.

MATRIX ROWS. M7_50_verification unchanged in substance. M8_20A_sweep-consistency gained its exit script and a section saying why there is no verb. Nine gate rows gained the write verbs their own raid_additions field has always demanded.

TEMPLATES AND SKELETONS. The seed forms gained a depends_on field marked `always`, and the mirror's collector now sends a marked field even when empty — because a shown box left blank is an answer, not a silence.

GUIDANCE CHAPTERS. refusals.md gained SE-C-138 and carries the retirement of SE-C-130 and SE-C-131 with the collision story that killed them. The prompt layer was reprojected from guidance, which the boot's preflight had flagged stale.

README AND ENTRY DOCUMENTS. Untouched by this delta, and checked rather than assumed: no chunk changed an entry document, and the entry-document law forbids method jargon there, which none of this iteration's vocabulary reaches.

PANELS AND FORM HELP. The seed form's new field carries its own help, and it says the empty list is a stated decision rather than leaving the reader to guess.

### Three corpus contradictions, each found by a check rather than by looking

el-record-store CONTRADICTED ITSELF IN ONE FILE. Its statement said "what exists is read from git branches, never from folders on disk" and its own closing section recorded i34 removing exactly that. One file, two answers, and the newer one was right. Head rewritten to match tail.

dsp-record-lifecycle taught one worktree per open record, in its statement and in a constraint. Both rewritten, with the assumption that replaced the guarantee named.

vp-the-engine's third example of a consequence was "a claim refuses collision" — the claim ledger i34 retired. It now names the write guard i6 built, so the example is current rather than merely different.

### Six standing breaks the sweep found on its first run

NONE WOULD HAVE BEEN CAUGHT AT THE WRITE, because every one predated the guard. That is exactly the class the sweep is for: the guard stops a new break at the door, the sweep finds the ones already inside.

- Five option nodes: found_by said "prior_art" or "transforming" where the template admits prior-art and transform. An underscore for a hyphen, and a participle for a verb.
- One register entry: status "held", which is not one of the eight. Its own text says "accepted on their word" twice, so it is accepted.

### The one class that is owed

BOOK CHAPTERS. The book derives its verification state from the live suite rather than from a pasted log, so nothing this delta changed lands in a chapter mechanically. Whether a chapter still READS correctly is a person opening it, which is the same shape as the four demonstrations raid-debt-human-observed-demonstrations already carries. It is checked against that entry rather than ticked unobserved.
