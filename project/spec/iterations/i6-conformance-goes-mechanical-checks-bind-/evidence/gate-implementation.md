---
form: gate-implementation
bless: blessed by human
by: agent
signed_off: 2026-08-16T19:31:53.760Z
authors: agent
files: null
---

# Evidence form / gate-implementation

## current_situation

FIFTEEN BUILD CHUNKS SIGNED, AND THE BATTERY IS GREEN: 1385 of 1385, with biome and preflight clean.

THE ITERATION'S THESIS WAS THAT A RULE WANTING ENFORCEMENT GETS A CHECK RATHER THAN A READER. Nine requirements were minted against it and each has its check.

THE THESIS WAS TESTED ON THIS WALK RATHER THAN ARGUED. Every mechanism built here fired on the work that built it — the write guard refused a fixture that had been malformed for months, the vocabulary check caught six standing breaks, the ripple named its own root, the dead-code sweep named a file written twenty minutes earlier, and the supply check found 29 gate/field pairs nobody had looked for.

THE FIRST FULL BATTERY SINCE CHUNK SIX FOUND 15 FAILURES. Every one was real and every one is fixed. That number is the honest cost of six chunks built while the test verb was deadlocked.

## quality_ok

- [x] Dependencies stay layered
- [x] Every new element carries one stated responsibility
- [x] The linter and the complexity ceiling are clean, with no new suppression
- [x] Every new behavior carries its check, and the battery is green at rest
- [x] Nothing speculative shipped
- [x] What changed is findable
- [x] Every quick-and-dirty taken stands as a visible raid debt entry

## debt_taken

- raid-debt-the-bound-surface-demo-leans-on-two-open-records

## risks_acceptable

acceptable — two risks moved and neither is new in kind.

THE GATES CAN NOW WRITE. Nine gate rows gained se_file_write and se_file_patch, because every one of them asks for the register entries its own review added and none could mint one. The supply check found it; the method's own ruling demanded it ("waiting for the state that owns the register is how an entry is lost"). A gate is already trusted to write its own evidence, so this widens what it may touch rather than what it may decide. If the owner wants it narrower, the honest fix is a verb that mints a register entry and nothing else — not taking the demand away, which would lose entries.

THE SWEEP NOW RUNS AT THE BOOT. A check that blocks every session start is a check that can stop every session. It is measured at 388 ms over 1015 nodes, it reports rather than guesses, and its findings are one-line frontmatter corrections. The risk is real and small; the alternative was a check nothing ran at all.

raid-iss-reopening-inside-a-sub-machine-has-no-short-way-back is added here and is NOT accepted as a risk — it is an issue with a trigger, and the owner has already asked the retro to re-analyse the pattern it belongs to.

## round_0_verify

- evidence vs claims: fifteen chunk forms read against what landed. TWO WERE FALSE AND WERE CORRECTED ON THE RECORD — chunk ten claimed a remedy that showed the empty list and shipped one that did not, and chunk twelve cited a requirement node that was never written. Both are amended and say so.
- types: clean. The lane typechecks after every write and the last several calls carried no typecheck_error.
- lint: clean. biome over 266 files, no fixes applied, no suppression added. One warning was fixed by DELETING the dead parameter rather than underscoring it.
- tests: 1385 of 1385 green, from the engine's own battery at verification. Nineteen new cases across six new files.

## round_1_validate

- exercised against the goal: yes, and by the goal's own subject. "Conformance goes mechanical: checks bind to the named elements and run at the write, not at a review." Every check built here fired on this iteration's own work, unprompted, and found things a review had not.
- missing: THE SWEEP HAD NO CALLER until the owner ruled where it runs. That was a genuine hole in req-a-check-too-slow-for-the-write-moves-to-the-sweep — the check existed, was tested, and nothing ran it. It now runs at three moments and none of them is a verb.
- wrong: THREE CASES I WROTE WERE WRONG, all from chunks built while the test verb was deadlocked so they never ran. One demanded a phrase no requirement asks for; two drove a surface they could not reach. Written confidently, signed, and false. That is the cost of signing a chunk with its run owed, and it is the strongest argument in this record for the deadlock fix.
- out of scope: the fallback fix and the sweep wiring were both outside every chunk's statement and both were the owner's explicit go. The reading-surface deletion was ruled and deferred by the owner rather than taken.
- prior art: NOT COMPARED, and here is why. The nearest neighbours are schema validators that refuse at write time — JSON Schema in CI, Rust's type system, database constraints. What this iteration adds over them is that the RULE lives on the node it governs rather than in the validator, so a new rule costs no engine change. I have not run any of those against this corpus, so I cannot say we do it better. An unmade comparison is a finding and this is one.

## round_2_red_team

- STEELMAN: this iteration built checks instead of doing work, and every check is a place the walk can now jam => Answered by measurement rather than by argument. The checks fired 40-odd times on this walk and were right every time: six standing corpus breaks, 29 supply gaps, one malformed fixture, one unclaimed file, two false claims of my own. Not one false positive survived investigation. A check that is right is not a jam.
- THE KILL-CRITERION: if a check built here had refused correct work and had to be weakened to pass, the thesis would be wrong => Looked for it. One candidate: the supply check found 29 pairs, and I could have narrowed the rule instead of fixing the gates. I fixed the gates, and the method's own ruling says that was right. The rule was not weakened.
- A SECOND CANDIDATE, and it is closer => the bound-rules fixture. My write guard refused it, and the fixture had been malformed for months. I fixed the fixture rather than the guard. If the guard had been wrong there, weakening it would have been the easy move and I cannot prove I would have resisted it.
- THE REAL WEAKNESS IS THE VERIFICATION ITSELF => Self-adjudicated. The method demands a fresh-eyes tester subagent and the harness forbids spawning one. Five checks were made by the same eyes that wrote the code. Four of the five are sweeps with a stated command and a stated result, which a second reader can re-run rather than re-judge — that is what protects them, and it is weaker than fresh eyes.
- WHAT THIS GATE CANNOT SEE => whether nine requirements was the right scope for a minor. The iteration grew: two engine fixes outside every chunk, a sweep wiring, and a deadlock repair. Each had the owner's word, and the total is larger than what was blessed at kickoff.

## raid_additions

- raid-iss-reopening-inside-a-sub-machine-has-no-short-way-back
- raid-debt-the-bound-surface-demo-leans-on-two-open-records

## verdict

pass — the build is complete, the battery is green at 1385 of 1385, and every check this iteration built has been exercised on real work rather than on a fixture alone.

WHAT THE PASS RESTS ON: nine requirements, each with a check that fires mechanically; fifteen chunks signed; two false claims of my own found and corrected on the record; 15 battery failures found and fixed in one pass.

WHAT IT DOES NOT COVER, said plainly rather than left to the reader: the verification is SELF-ADJUDICATED, because the method wants a fresh-eyes tester and the harness forbids one. Seven of twelve non-test specs are owed against three open register entries. The prior-art comparison was not made.

THE BLESS IS THE OWNER'S. This form is submitted, not blessed.

## follow_up

THE OWNER'S BLESS IS WHAT THIS GATE WAITS FOR. Nothing else here is blocked.

AFTER THE BLESS: fill-story-evidence, then the validation gate with sweep-consistency on its fan, then package and release.

SWEEP-CONSISTENCY CARRIES A LIST ALREADY, and it now runs the conformance sweep as its own exit script.

- Three stale comments naming an autonomy slider that no longer exists.
- dsp-record-lifecycle's statement and one constraint still describe one worktree per open record; i34 deleted worktrees.
- req-product-is-a-folder carries an empty "## Detail" twice.
- Three files still cite the dead i27 long id.

EIGHT NOTES STAND FOR THE RETRO, and four are about how I work rather than about the code.

- I ask for the battery and then report without waiting for the verdict (note-4ead924e4cab).
- I walk back and forward too often; the owner wants an amend that carries a fix, and the retro to re-analyse it (note-4be3cfe2a2fe).
- .se/reading.md is to be deleted with the mechanism that writes it (note-de5bc82a2d75), ruled and deferred by the owner.
- The fix-findings fallback was unreachable (note-ed0ce3eea951) — now fixed, and worth reading for how it was missed.

THE 26 LEFTOVER BRANCHES ARE STILL THE OWNER'S TO DELETE. Nothing in the engine reads them.

## anything_else

