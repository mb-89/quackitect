---
form: log-risks
amended: "2026-08-16T16:29:15.774Z by agent — raid-asm-a-bound-check-runs-inside-the-write-budget moved to probed at probe-assumptions; the set opened here is unchanged"
by: agent
signed_off: 2026-08-16T15:40:00.787Z
authors: agent
files:
---

# Evidence form / log-risks

## current_situation

THE REGISTER STANDS AND SIX ENTRIES ENTER IT. One was minted at the kickoff gate, five here.

WHAT WAS CHECKED: the DELTA's four ruled goal conflicts, not the whole standing goal system. Each produced exactly one entry. A fifth came from a defect this state hit while writing the first four.

ONE STANDING ENTRY WAS CHECKED AND DELIBERATELY NOT DUPLICATED. raid-dec-the-walk-never-reaches-a-state-it-cannot-leave covers a STATE demanding what it has no verb to supply. A CHECK is not a state — it fires inside a verb, against whatever file is under the hand, where no machine-shape check can see it. That gap got its own risk rather than a note on the standing decision.

## raid_opened

- raid-asm-a-bound-check-runs-inside-the-write-budget
- raid-risk-a-bound-check-refuses-the-write-that-fixes-it
- raid-dec-a-check-refuses-a-wrong-write-and-reports-a-wrong-corpus
- raid-dec-a-seed-states-its-dependency-or-refuses
- raid-risk-the-small-fixes-crowd-out-the-conformance-system
- raid-iss-a-write-can-leave-the-corpus-unparseable

## follow_up

FRAME-DELTA IS NEXT, and nothing blocks it.

WHAT THE REGISTER HANDS FORWARD.

- THE BUILD ORDER IS ON THE RECORD as the crowding risk's mitigation: the write-budget probe first, the conformance chunks before the fixes, and gate-implementation checking the order rather than the count.
- THE FIRST CHECK TO BUILD IS NAMED, and it is the frontmatter parse. It needs no binding and no corpus, so it answers the write-budget question from the cheap end. If a check reading only the incoming bytes cannot fit in a write, nothing can.
- THE REFUSE-VERSUS-REPORT SEAM decides the shape of every check written at M3 and M7. A check that picks neither is unfinished, and that is now a consequence on the decision rather than a preference.
- ONE ASSUMPTION IS UNPROBED and it is load-bearing. probe-assumptions at M3 will find it open by design, not by neglect, and its probe is the first build chunk's own first act.

## anything_else

TWO THINGS THIS STATE LEARNED BY DOING, both recorded rather than smoothed over.

### The corpus check works, and it caught me

The first submit was refused: SE-C-112, naming five failures across two decision nodes. Both were missing `## Rejected options` and `## Consequences`, and one had no traceable ref in source_refs.

That is the shape this iteration is trying to spread. The demand was mechanical, the refusal named every failing field at once rather than the first, and the remedy was executable. Writing the missing sections cost one call.

### The write path does not work, and it also caught me — four calls later

Writing the four conflict entries produced a node whose frontmatter did not parse. The write was accepted with a hash. The next pull threw, naming a line and a column in no particular file, and the walk stopped.

THE CONTRAST IS THE WHOLE ARGUMENT. Same corpus, same author, same session. The check at the submit cost one call to satisfy. The absent check at the write cost four to recover from, and the error it produced named neither the file nor the cause.

UNDER CONTRACT RULE 4 NEITHER IS A STRAY. A stray is elsewhere; a hole in the thing under your hands is the work. Both went to the register.
