---
form: frame-delta
by: agent
signed_off: 2026-08-16T15:41:05.995Z
authors: agent
files:
---

# Evidence form / frame-delta

## current_situation

THE FRAME IS INHERITED. i6 is a minor, so the gap claim and the why-now stand from the resident frame and the form drops them.

ZERO NEW VALUE PROPS. The delta extends two standing ones instead, and both extensions are written into the artifacts themselves rather than described here.

THE DELTA IN ONE LINE: rules that are read and broken become checks that refuse at the write.

## value_props

- vp-rigor-without-toil
- vp-the-engine

## business_case

THE CASE IS A MEASURED ONE, taken inside this iteration rather than argued.

### What the delta adds to vp-rigor-without-toil

THE NEED: a broken rule costs one refusal, not a hunt. The break is named where it is made, while the author is still there and the fix is one move.

THE PASS LINES, both mechanical.

- Lane calls spent recovering from a break a write-time check could have refused. Target: zero.
- Corpus-shape rules enforced by a check rather than by prose, as a share of all of them. Target: rising, retro over retro.

THE EVIDENCE IS FROM THIS WALK, two states ago. One unquoted colon in a frontmatter value was accepted at the write and returned a hash. The next pull threw, naming a line and a column in no particular file. Four calls to find and fix.

THE SAME CORPUS REFUSED CORRECTLY ONE STATE LATER. The submit-time check named five failures across two nodes at once, with an executable remedy. One call to satisfy.

SAME AUTHOR, SAME SESSION, SAME CORPUS. Four calls against one. That ratio is the whole business case and it was not modelled.

### What the delta adds to vp-the-engine

THE NEED: a write refuses a break. That is the FOURTH consequence the drawing attaches, beside a state refusing tools and a gate refusing passage.

THE PASS LINES.

- A write that leaves the corpus unreadable by the engine's own reader. Target: none.
- Engine code changed to add a new bound check. Target: none.

THE SECOND ONE MIRRORS THE PROP'S OWN FIRST CRITERION on purpose. A drawn machine compiles with zero engine code; a bound check should bind the same way, to an el- element or a dsp- design spec. A check that needs engine code to exist is a check that will not be added.

### Why no new prop

A NEW PROP WOULD BE THE WRONG SHAPE. This delta does not promise the engineer something new. It makes two standing promises true in a place they were not.

The record's own thesis says the same thing from the other side: a rule that can be read and still broken was never a promise kept, only a promise stated.

## follow_up

SCOPE-NON-GOALS IS NEXT, and gate-motivation stands open behind it.

ONE THING IS DEFERRED FROM THIS STATE, and it is not this state's to fix.

vp-the-engine's Unlike section still reads "a claim refuses collision". i34 retired the claim system whole, and the file says so twenty lines above in its own note. Editing it here would be an unasked refactor under contract rule 2, so it goes to sweep-consistency, which is the state that owns exactly this.

NOTHING IS BLOCKED.

## anything_else

