---
form: gate-candidates
authors: agent
files:
---

# Evidence form / gate-candidates

## current_situation


## reasons_hold

YES. Every one of the 96 cut axes carries the reason "identical by construction" or equivalent, and it is checkable directly against the candidate records: all five composed candidates state, in their own How it works section, that "the unchanged baseline... stays exactly as it is" outside cluster-the-query and cluster-the-disposition. An axis about the archive, the tour, the bootstrap, or any other untouched subsystem is textually declared identical across all five, not guessed to be so.

No row was moved out of rank order — the mechanical breaks_how_badly sort stood unedited, so there is no favorite-aimed edit to scrutinise.

The front is size 2, not 1, and it is not artificially narrow: cand-fast-path-plus-blocking beats cand-explicit-and-safe on speed/scale-related axes (req-trace-source-never-mixes, req-call-answers-in-one-second) while losing on correctness-related ones (req-a-wrong-act-never-passes-silently, req-trace-view-derived-from-files, req-query-is-deterministic) — a genuine crossing trade, confirmed by the engine's own arithmetic matching my hand check before submission.

## round_0_verify

Mechanical checks: all five candidates were scored on all seven surviving axes (35/35 cells filled, none blank). The front (2 candidates) and eliminations (3, each with a lost_on list) were computed by the engine's own arithmetic from the score table, not typed. Utopia/nadir/flat were likewise engine-computed. Scores landed verbatim from the spawned subagent with no post-hoc edits — my one disagreement (cand-narrow-grammar-plus-explicit's req-call-answers-in-one-second score) is recorded in evaluate-set's reading field beside the score, never written over it, per the state's own rule.

## round_1_validate

Does the gate's real question hold up, not just the checklist. Cut reasons: verified above as checkable, not guessed. No moves: verified as none made. Front honesty: verified as a genuine trade, not a collapsed space — the two front candidates differ by real, load-bearing design choices (cache-with-drift-risk vs. explicit-review-with-no-cache), and the scoring subagent independently found the crossing pattern without being told which candidate anyone favoured.

## round_2_red_team

Attacking the weakest point: cut-criteria's own reasoning was written by the same agent who composed the candidates being cut against, which is the exact conflict-of-interest the spawned-scorer rule exists to prevent — but the CUT step is blind to importance and checkable against the candidates' own explicit "unchanged baseline" text, not a judgment call about which candidate wins, so the conflict that matters (composer scoring composer's own preferred candidate) does not apply here the way it would at scoring. The genuine red-team finding: req-broken-trace-is-a-defect scored 0 across every candidate, meaning a real requirement axis got no design attention from any of the five — not a false pass, but a real, carried gap, already flagged in evaluate-set's reading and repeated in raid_additions below.

## raid_additions

- raid-risk-i15-broken-trace-defect-unaddressed-by-any-candidate: none of the five composed candidates for cluster-the-query/cluster-the-disposition describe how a broken trace link inside the new query or disposition machinery is caught and failed at gate review (req-broken-trace-is-a-defect scored 0/5 unanimously at evaluate-set). Carry into M5 elaboration of whichever candidate(s) survive, or M6 as a spike question.
- raid-note-i15-narrow-grammar-timing-score-may-be-asymmetric: cand-narrow-grammar-plus-explicit's req-call-answers-in-one-second score (0) may reflect uneven write-up depth rather than a real architectural gap, since it uses the same probed regex mechanism cand-continue-v1s-shape cites timing for. Recorded in evaluate-set's reading; worth a second look before M5 treats the elimination as settled.

## verdict

PASS. The front (cand-explicit-and-safe, cand-fast-path-plus-blocking) is blessed as the surviving set — never a winner, per set-based discipline. Two RAID entries added above carry the one real gap (req-broken-trace-is-a-defect) and one flagged uncertainty (the narrow-grammar timing score) into M5 rather than letting either pass silently.

## follow_up


## anything_else

