---
form: probe-assumptions
by: agent
signed_off: 2026-08-18T09:40:52.610Z
authors: agent
files:
---

# Evidence form / probe-assumptions

## current_situation

This state's input is the RAID folder, not the form above it. 55 assumptions stand. The minor column scopes the walk to the delta's own four plus any standing one whose trigger has FIRED, and three standing triggers fired in this session — which is unusual and is the reason this state is worth more here than it usually is.

THE SESSION ITSELF IS THE PROBE for those three. This is a cloud run on Linux, which is the host family cloud-runner.md names as "the branch that has never run", and three entries were written against exactly that gap.

AND THE FULL BATTERY RAN HERE, which is the first state since M0 that grants se_test. 1450 tests, 135 suites, 0 failures; biome clean over 286 files; the sweep green over 1348 nodes. That closes the override both previous gates carried.

## probes

- raid-asm-the-corpus-sweep-already-covers-a-minted-option: HOLDS. Run, not reasoned. A throwaway root was built carrying one minted option under project/spec/trace/option/, with a git identity and a HOME belonging to nobody real and the option's body carrying both. prose-inspect came back RED with two findings, naming the file and the line for the username and for the home directory, exit 1. The free coverage the design counts on is real. It does NOT settle the inherited blind spot: a leaked name that is also common project vocabulary is muted, in a minted option as anywhere else.
- raid-asm-a-host-keeps-a-backgrounded-lane-alive: HOLDS, and the POSIX branch is no longer unexercised. Its trigger names a host whose process supervision differs, and this is one — a Linux container. se-arrive spawned the lane detached, returned, and the lane answered every call for the whole session across dozens of separate shell invocations. Nothing reaped it. i35 measured the same on Windows; this is the other family, and cloud-runner.md's warning that the POSIX path was written and unexercised can come down.
- raid-asm-the-arrival-runs-before-the-agent-reads-anything: HOLDS on this host, and it was UNPROBED before today by its own admission. i35 could observe the hook running but nothing established ORDERING against the first turn. Here the ordering evidence is direct: the hook's step lines were already sitting above the agent's first turn, in the transcript, before anything was read. The agent could not have raced it. STILL NOT ESTABLISHED: the resume case, which the trigger also names. This was a startup.
- raid-asm-the-cage-holds-so-every-write-passes-the-lane: HOLDS ON ENFORCEMENT, AND ITS NAMED GAP WAS WALKED THROUGH FOR REAL. The enforcement half re-probed green — se_help refused SE-C-110 with the state's exact tool list, se_web_search refused SE-C-106, every form check refused SE-C-112 naming its field. But three engine files were edited with native tools this session, because the lane would not start and boot/prepare_idle grants no tools while its own guidance promises repair tools. THE CAGE DID NOT FAIL; IT WAS NEVER BINDING, since a settings file placed after a session starts does not bind that session. What the probe changes is the SIZE of the uncaged window: it is not only the arrival, it is the whole of any session whose boot cannot complete. Recorded on the entry, and the cause is raid-iss-boot-grants-no-tools-while-promising-repair.
- raid-asm-a-verbatim-overlap-check-catches-the-paste-that-matters: UNPROBED, and the reason is that the thing it is about does not exist yet. The check is not built and the pool has no content, so there is nothing to measure a distribution over. The probe is written on the entry — measure the longest verbatim run between each minted option and its source note — and it is owed to the migration rather than to this record. ITS FATAL GRADING IS THE ARGUMENT FOR RUNNING IT EARLY rather than at fifty mints.
- raid-asm-the-drain-is-the-only-door-into-the-pool: UNPROBED, same reason and a sharper one. It is an inspection of every writer of the option kind, and today there are none. What CAN be said is that its analogue holds: engine/inbox.ts is the only writer of the note store today, which is why the belief is credible. That is a reading, and a reading is not a probe.
- raid-asm-a-node-file-per-option-holds-at-the-sizes-this-pool-reaches: UNPROBED, and it cannot be probed from this machine at all. The 205 parked options are machine-local on the owner's laptop; this clone holds three notes. The probe is the migration's own before-and-after timing, and it answers raid-asm-a-migrated-pool-does-not-drown-the-corpus with the same measurement — deliberately one run, not two.
- raid-asm-the-pool-is-a-node-kind-under-project-spec: PARTLY PROBED, and honestly only partly. Its own probe asks for three of this session's findings expressed as pool nodes and checked against the reader, the sweep and the views. ONE THIRD OF THAT IS NOW DONE: the sweep walks such a node and finds what is in it, proven by the run above. The corpus reader and the reference views were NOT exercised against an option node, because the kind does not exist yet. Status stays open.

## follow_up

- cloud-runner.md still says the POSIX branch has never run and points at exp-the-posix-branches-have-never-run. That is now false and the card should say so; it is a guidance edit rather than a build step
- three of the four unprobed assumptions are owed to the migration, which is a non-goal of this record. They travel with it rather than being lost, because the register is addressable
- the fourth, drain-is-the-only-door, is owed at M7 the moment a writer exists: read every writer of the option kind, which is what its requirement names inspection for
- the resume case of the arrival assumption is still open, and this session cannot produce one
- the full battery's verdict is on record now, so the next se_test has a baseline to scope against — which the engine said it lacked

## anything_else

## fallout

NONE. No assumption turned false, so nothing rests on something known false and no entry changed kind.

SAID PLAINLY BECAUSE THE ABSENCE IS A RESULT: four of the eight probed HOLD with evidence, four are unprobed with a reason, and not one was falsified. The nearest thing to a falsification is the cage entry, and it is not one — the enforcement it asserts held wherever it applied, and what widened is the gap the entry had already named itself.
