---
form: scope-non-goals
by: agent
signed_off: 2026-08-18T09:23:47.301Z
authors: agent
files:
---

# Evidence form / scope-non-goals

## current_situation

The frame delta is signed with one new value prop. This state says how much of it this iteration takes.

THE KICKOFF ALREADY CUT ONCE, and this is not a second cut of the same thing. The kickoff cut the RECORD — the criteria machinery and the 205-note migration left the iteration entirely. This state scopes what remains: how much of the pool proposition one iteration takes on.

THE PROPOSITION HAS FOUR SUCCESS CRITERIA and this iteration serves three of them fully and one partly, which is what the two fields below say.

## scope

- THE MINT. Draining a note to backlog writes an option node on trunk, carrying its statement, its re-entry condition and its source. This is the whole mechanism and everything else is around it
- THE REWRITE AS A REQUIRED ACT. The mint takes an authored statement. There is no path from a raw note's own text to trunk, and no convenience that skips it
- THE RAW NOTE STAYS AND IS MARKED DRAINED. It is not moved, not copied, not deleted. Two objects, deliberately, and the option is the truth from the mint onward
- THE POOL IS READ. se_survey and the front desk read open options from the corpus rather than from .se, so a fresh clone sees the pool. WITHOUT THIS THE ITERATION HAS BUILT A FOLDER, and it is the half most likely to be dropped under pressure
- AN AGENT MAY FILE. An unattended walk can mint an option mid-walk, performing the same rewrite a person performs. This is the cloud case the owner added to the record on 2026-08-17
- WHAT CANNOT BE STATED CLEANLY IS FILED AS AN OPEN QUESTION, never guessed at and never skipped

## non_goals

- THE MIGRATION OF THE 205 PARKED OPTIONS. Impossible from this machine — this clone holds three notes — and it is the mechanism's first USE rather than part of building it. It runs where the notes are, and its first act is a report of what did not fit
- RETIRING project/spec/version-planning.md. It dissolves into the pool, so it follows the migration and cannot precede it
- THE CRITERIA MACHINERY, all nine engine capabilities. The seed calls it "the other half of this iteration's subject" and two of the nine reach into i15 and i23. It is an iteration
- A FIFTH DISPOSITION THAT MERGES DUPLICATES. Named as the first thing worth stealing from Linear, and filed as a risk. Designing a disposition before the first one has been used is designing against an imagined shape
- A WAKE CONDITION A MACHINE CAN EVALUATE. Linear's snooze returns by itself; our `where` is a sentence a person re-reads. Worth having, and it changes what a re-entry condition IS, which is a bigger question than this iteration
- A NAMED OWNER FOR THE QUEUE. Linear rotates triage responsibility deliberately. Ours is drained by whoever runs the retro. A real gap, and it is a process change rather than a build
- ANY PANEL OR UI FOR THE POOL. i23 owns surfaces
- ROUTING A WORK TOKEN TO A PERSON. The owner's phrase covers filing AND routing; this iteration is filing. Routing is what raid-debt-ten-checks-wait-on-a-person-or-a-second-machine waits on
- A DIFFERENT STATUS FOR AN AGENT-FILED OPTION. Deliberately left open at M1 and carried to M3, where it is a requirements question. Answering it here would invent a field before anything needs it

## follow_up

- the reader half — the desk and the survey serving the pool — is named in scope precisely because it is the droppable one; M8 checks it rather than the build claiming it
- three non-goals came from the prior-art scan rather than from the seed: the merge, the machine-evaluable wake, and the owned queue. Each is a risk or a reference note, so none is lost by being excluded
- the binding system-level exclusion list is owed at draw-context (M2); this list is vision-level

## anything_else

