---
form: the-drain-mints
by: agent
signed_off: 2026-08-18T10:01:35.434Z
authors: agent
files:
---

# Evidence form / the-drain-mints

## current_situation

The module and its refusal stand. This chunk is the first half of the spine: the act that actually crosses the boundary.

## built

engine/inbox.ts `drainNote` takes a statement and a project root; engine/tools.ts carries the argument on se_note_drain; engine/session.ts carries it on the mirror's own path.

THE ORDER IS THE GUARANTEE, and it is the one thing in this chunk that would be easy to get subtly wrong. The mint runs FIRST and the note is marked drained SECOND. A refused mint therefore leaves the note pending, undrained and still in the count — because a rejected crossing that consumed the note anyway would lose the finding in exactly the situation the refusal exists to protect.

BOTH DOORS GET THE SAME DEMANDS. The agent's se_note_drain and the person's own mirror path both pass the statement through, so there is no lighter path for either. That is req-the-crossing-is-the-same-act-for-a-person-and-an-agent, and it is why its verify method is inspection rather than test.

backlogNotes SURVIVES BUT ITS MEANING NARROWED, and its comment now says so: it is the LOCAL half of a crossing, kept so the two ends can be found from each other. It is not the pool, and after the next chunk nothing reads it as one.

## follow_up

- the offer chunk is what makes this visible from another clone; until it lands, the mint writes something nothing reads

## anything_else

