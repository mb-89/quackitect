---
form: gate-kickoff
bless: blessed by agent
by: agent
signed_off: 2026-08-18T09:08:05.831Z
authors: agent
files:
---

# Evidence form / gate-kickoff

## current_situation

i17 is entered on an unattended cloud box, the onboarding retro is signed, and the inbox stands at zero.

THE RECORD IS TWO ITERATIONS IN ONE FOLDER, and that is the first thing this gate has to settle. Its vision carries the options pool AND the criteria machinery, and the vision itself calls the second one "the other half of this iteration's subject". The criteria half names nine engine capabilities owed before any of it runs, two of which reach outside this record into i15 and i23. Two halves of that size are not one iteration.

AND ONE PIECE OF THE SEEDED SCOPE CANNOT BE DONE FROM THIS MACHINE AT ALL. The vision says MIGRATE THE 205 drained notes. This clone's `.se/notes.jsonl` holds THREE lines, all written in this session. The 205 are machine-local on the owner's laptop, which is the exact fact this iteration exists to fix, and it means the migration cannot even be attempted from here. Counted, not assumed.

SO THE PROPOSAL IS THE MECHANISM, WITHOUT THE MIGRATION AND WITHOUT THE CRITERIA MACHINERY. Everything the mechanism needs is on this clone: `engine/inbox.ts` holds the four dispositions and already REQUIRES a `where` on backlog, the corpus reader and writer are here, and trunk is here.

THIS SESSION IS ITSELF THE CASE. Three findings were captured as notes at boot. Had the box been released before the retro drained them, all three would have gone with it, exactly as the i15 note did on 2026-08-16. The record's own owner ruling of 2026-08-17 names that as what this pool is for.

THE CHANGE SIZE REASONING IN FULL, because the field takes one line.

WHY NOT MAJOR. Major's tell is a new function that fits no existing cluster, which is the architecture moving. Nothing here moves it. The drain exists, its four dispositions exist, the backlog disposition already demands a re-entry condition, and the corpus already stores typed nodes with frontmatter under `project/spec/`. This adds a WRITE at a point that already exists, and a node kind beside the kinds already there.

WHY NOT PATCH. New requirements are written rather than merely satisfied. "A raw note never enters version control" and "an option nobody can state cleanly was never an option" are demands the system does not carry today, and patch means no new requirements.

THE PREDICTION, stated so it can be wrong in public: the requirements that move are the note pipeline's, and the architecture holds because the mint is a corpus write like every other corpus write. IF THE POOL TURNS OUT TO NEED ITS OWN STORE rather than a node kind under `project/spec/`, that is the architecture moving and the walk escalates visibly.

STRIKES: none proposed beyond what the minor column already carries. Under minor, M4, M5 and M6 do not apply at all, gate-inputs folds into the requirements gate, and the engine compiles the rest.

## retro_drained

- note-9760aab9c1a1 THE LANE DIED ON NaN BEFORE ITS FIRST CALL: carried — fixed in engine/session.ts setAutonomy and engine/bin/se-mcp.ts, pinned by three cases in tests/scale.test.ts
- note-747e0ee09da0 PROSE-INSPECT WAS PERMANENTLY RED AND BOOT COULD NOT FINISH: carried — fixed in engine/bin/prose-inspect.ts, pinned by five cases in tests/identity-collision.test.ts; the boot deadlock it also named is NOT closed and is carried into this gate's raid additions
- note-f32988a14c99 THE READING PROBE REFUSED A VERBATIM ANSWER: carried — fixed in engine/readproof.ts, pinned by three cases in tests/read-probes.test.ts whose fixture reproduces the live probe exactly
- the inbox: drained to zero at i17/onboard-retro on 2026-08-18, as 3 carried and nothing else
- the backlog: empty on this clone, and empty for the same reason the 205 are missing — .se is machine-local
- the debts: all 10 swept. One had its trigger FIRE — raid-debt-cloud-validation-needs-a-machine-this-one-cannot-make, whose trigger is the first cloud run — and it is rescheduled with a dated 2026-08-18 look recording what the second cloud run showed. One carried NO look date at all and now does. The other eight were re-affirmed unchanged from the 2026-08-17 sweep, one day old
- the field-feedback question: ASKED AND UNANSWERED, recorded as owed rather than ticked, because nobody is at this box
- the memory drain: nothing was surfaced in context, so nothing was drained. Said rather than ticked, which is what retro.md step 6 demands

## goals

- the pool travels: draining a note to backlog mints a committed, rewritten item on trunk
- the rewrite is the privacy boundary: a raw note never enters version control, and what cannot be stated cleanly is refused rather than guessed
- an unattended walk can file into the pool, so a finding survives the box being released
- engine improvements

## pulled_in

- goal one, the pool travels: se_note_drain with disposition backlog MINTS a node under project/spec/, carrying the option, its ready-when and nothing else; the raw line in .se/notes.jsonl stays local and is marked drained
- goal one, the reader side: se_survey and the desk read the pool from the corpus rather than from .se, so a fresh clone sees what it may pull from
- goal two, the rewrite: the mint takes a REWRITTEN statement, not the capture text, and the drain refuses a backlog disposition that hands back the raw text unchanged
- goal two, the hard line: raw notes never enter version control, enforced rather than documented
- goal three, the cloud case: a walk with no person at the box can put a finding into the pool, which is the owner's work token under its settled name (owner ruling 2026-08-17: "Yes, the work token is i17's options pool")
- goal four, engine improvements: the three boot-blocking defects fixed and pinned this session — the autonomy word, the identity check's false reds, the reading probe's verbatim refusal
- goal four, the finding this gate adds: boot/prepare_idle grants no tools while its own guidance promises repair tools, so a red exit script at boot cannot be repaired through the lane

## left_out

- MIGRATING THE 205 DRAINED NOTES: impossible from this box. This clone's .se/notes.jsonl holds three lines. The 205 are on the owner's machine, and the migration runs where they are, after the mechanism exists. It is the mechanism's first use rather than part of building it
- THE CRITERIA MACHINERY, all nine engine capabilities: the record's own vision calls it "the other half of this iteration's subject", and two of the nine reach outside this record into i15's table surface and i23's UI. It is an iteration, not a milestone, and it should be seeded as one
- RETIRING project/spec/version-planning.md: it dissolves into the pool, so it follows the migration and cannot precede it
- THE POOL'S OWN UI: reading the pool from a panel is i23's surface work
- THE WORK-TOKEN ROUTING TO A PERSON: the owner's phrase covers filing AND routing. Filing is this iteration. Routing a token to a human is what raid-debt-ten-checks-wait-on-a-person-or-a-second-machine waits on, and it is not built here
- THE BOOT REPAIR DOOR: named in raid additions below, and it is engine-machine work that changes a state's legal tools rather than pool work

## change_size

minor — the drain, its four dispositions and the backlog's required re-entry condition all exist in engine/inbox.ts today, and the corpus already stores typed nodes under project/spec/, so this adds a write at a point that already exists rather than moving the architecture; it is not a patch because two new requirements are written, that a raw note never enters version control and that an unstatable option is refused; the prediction is that the requirements that move are the note pipeline's, and if the pool turns out to need its own store rather than a node kind the walk escalates visibly

## round_0_verify

- evidence vs claims: CHECKED, not assumed. The retro's survey figures were re-counted here — 0 expeditions, 24 iteration stubs, 0 pending notes, 0 backlog. And the seed's own MIGRATE THE 205 was checked against this clone: .se/notes.jsonl holds 3 lines, so that half of the scope is impossible from this machine rather than merely hard. That single count is what shapes left_out
- types: GREEN by construction and not by a run. The three changed engine files are executed unflagged by the runtime on every call, and the lane, the boot scripts and three test files all ran against them after the change. No separate tsc pass was made, and se_test — which is what would have made one — is not legal at this gate
- lint: RUN AND GREEN. biome over 285 files, clean. It was red once, on the record's own opening commit, and the pre-commit hook caught it before anything landed
- tests: THREE FILES RUN AND GREEN — read-probes 9 cases, identity-collision 5 cases, scale 13 cases, 0 failures. TWO THINGS ARE WRONG WITH THAT LINE AND NEITHER IS DRESSED UP. They ran through the native runner rather than se_test, which was legal at onboard-retro and was not used, so the lane has no record of the verdicts. And the FULL BATTERY IS NOT RUN, owed at the first state that grants se_test. Nothing outside those three files changed, so the exposure is bounded — which is not the same as having run it
- the boot exit scripts: RUN THROUGH THE LANE AND GREEN — preflight, smoketest, sweep over 1304 nodes, prose-inspect and record-inspect, all exit 0. Two of them were red an hour earlier and are the reason this walk could begin at all

## round_1_validate

- exercised against the goal: the scope answers the goal's own sentence, a committed rewritten item that travels, and drops only what cannot be done here or is a second iteration by the seed's own words
- missing: NOTHING IN THIS SCOPE PROVES THE POOL IS READ. A minted node nobody consults is a folder, not a pool. The reader side is in pulled_in and it is the half most likely to be dropped under pressure, so it is named here as the thing to check at M8. Also missing, and admitted: the vision's MIGRATE THE 205 and the whole criteria machinery, both moved to left_out with reasons
- wrong: nothing found wrong in the seed itself. The seed is OVERSIZED, which is a scope error rather than a wrong idea, and the fix is this gate's cut rather than a correction to the record
- out of scope: the three engine fixes, carried anyway. They are not pool work. They ride under the standing engine-improvements goal because without them there is no walk at all — the lane died before its first call and boot could not finish
- prior art: NOT SCANNED, and the reason is mechanical. gate-kickoff grants neither se_web_search nor se_web_fetch, meth-gate-review demands a LIVE scan at the gate, and both writing a name from memory and routing around the lane are explicitly forbidden. So the honest answer is the gap, and it is FILED rather than only noted — raid-iss-the-kickoff-gate-demands-a-live-scan-and-grants-no-web-verb. WHAT THE GAP COSTS: the pool's shape is fixed against Anderson's Kanban Method as cited in the seed, which is a book, not a product with users. The systems people actually reach for — trackers with a triage stage, and the note tools that sync a private capture into a shared store — are exactly what a live scan surfaces, and none has been looked at. THE SCAN IS OWED at M1's draft-vision, the first state that can make it

## bound_breaches

- if-agent-harness-to-entrypoint: FOUR calls over the one-second bound in this window, all se_pull, at 7187, 2478, 1866 and 1778 ms. Every one carried a document or walked the machine, which is the shape i33's milestone four already owns; this gate adds no new cause and claims no fix. Nothing else in 65 calls passed 700 ms
- the count itself: A FLOOR, NOT A RATE. This clone's call log starts inside this session, so four breaches over 65 calls says what happened here and nothing about the standing rate. Naming that is the point of the line

## round_2_red_team

- STEELMAN, the strongest case against this gate: splitting the record is the agent choosing its own scope, and the seed is the owner's; an agent that keeps only what fits its afternoon has quietly taken the person's decision => answered two ways. The column is a PROPOSAL and the bless is the choice, which is the mechanism's own answer. And the cut is argued from a counted fact rather than a preference: the 205 notes are not on this clone, three are. That is not a judgement call
- the split could be an excuse to ship the easy half => the mechanism is not the easy half; it is the only part that unblocks the other two, since neither the migration nor the retirement of version-planning.md can precede a thing to migrate INTO
- KILL CRITERION: this is the wrong call if the mechanism cannot be built without the 205 to define its shape => LOOKED FOR, and the answer is no. The mint's shape is decided by what a corpus node must carry and by the privacy line, neither of which needs 205 examples. It WOULD be the wrong call if the 205 held shapes the mint cannot express, which is why the migration's first act must be a report on what did not fit rather than a silent conversion
- 205 new nodes against 1304 existing ones is a sixth of the corpus arriving at once, and every sweep, conformance pass and reference view walks it => REAL and UNMEASURED. Filed as raid-asm-a-migrated-pool-does-not-drown-the-corpus rather than waved off. It is not a reason to refuse this gate, because the migration is already a separate act with its own measurement
- an unattended agent filing into the pool can now write to trunk from a box nobody watches => that is the point and it is also the risk. The mitigation is structural: a mint is a REWRITE, so nothing lands that the agent did not state deliberately, and the raw dump stays local by construction
- the weakest part of this gate is its own prior art, and the red team says so rather than making the reviewer find it => a shape settled against one book, with the live scan owed to a later state, is the shape most likely to be re-cut at M1. Accepted as an override rather than hidden in the verdict

## raid_additions

- raid-iss-boot-grants-no-tools-while-promising-repair
- raid-iss-an-exit-script-may-not-read-unpinned-host-state
- raid-iss-the-kickoff-gate-demands-a-live-scan-and-grants-no-web-verb
- raid-asm-the-pool-is-a-node-kind-under-project-spec
- raid-asm-a-migrated-pool-does-not-drown-the-corpus

## verdict

pass with overrides — the scope is sound and buildable from here, and three things it could not do are named rather than hidden: the prior-art scan the gate grants no verb for, the full battery no state here grants se_test for, and a scope cut made against the owner's own seed.

THREE OVERRIDES, each carrying a register entry as meth-gate-review requires.

1. THE PRIOR-ART SCAN IS NOT MADE. The gate demands a live scan and grants no web verb. Filed as raid-iss-the-kickoff-gate-demands-a-live-scan-and-grants-no-web-verb; the scan itself is owed at M1's draft-vision, the first state that can make it.

2. THE FULL BATTERY IS NOT RUN, and the three files that were run went through the native runner rather than se_test, so the lane holds no record of the verdicts. Owed at the first state that grants se_test. The two boot-repair breaches behind it are filed as raid-iss-boot-grants-no-tools-while-promising-repair.

3. THE SCOPE IS CUT AGAINST A SEED THE OWNER WROTE. The criteria machinery and the 205 migration are both dropped — one is a second iteration by the seed's own words, the other is impossible from this machine, counted rather than argued. The bless is the decision and this is a proposal, and on an unattended box the dial hands that bless to the agent. IF THE OWNER READS ONE THING IN THIS FORM, IT SHOULD BE THIS OVERRIDE. The assumption it rests on is filed as raid-asm-the-pool-is-a-node-kind-under-project-spec.

WHY PASS RATHER THAN FAIL. Nothing named above is fixed by re-doing M0. The scan needs a state with a web verb, the battery needs a state with se_test, and the scope cut needs the owner — not another round of the same form.

## follow_up

- M1 opens with the prior-art scan this gate could not make, and draft-vision records it as its own first act
- the first state granting se_test runs the full battery, and its verdict says which three files were already green
- the mint's shape is probed before it is built: express three of this session's own findings as pool nodes and check the corpus reader, the sweep and the reference views take them unchanged — raid-asm-the-pool-is-a-node-kind-under-project-spec
- the migration of the 205 is prepared here and RUN on the owner's machine, and its first act is a report of what did not fit rather than a silent conversion
- the criteria machinery is seeded as its own iteration, carrying the nine engine capabilities and the two that reach into i15 and i23
- the three register issues this gate filed are engine-machine work and belong to whichever iteration takes the machine next; none is pool work

## anything_else

