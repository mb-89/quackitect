---
form: decompose-structure
by: agent
signed_off: 2026-08-18T09:44:04.383Z
authors: agent
files:
---

# Evidence form / decompose-structure

## current_situation

Two new functions want allocating. Both land in existing elements, no element is created, no interface is owed, and no group moves — which is what the minor column allows and what the escalate rule watches.

THE ESCALATE TEST WAS PUT FIRST, because wanting a new element or a new interface IS the architecture moving. Neither wanted one, and the check that says so is the flow crossing: flow-standing-option is produced in `the-holding-pen` and consumed in `the-record-life`, and BOTH of those crossings already have interfaces, because notes already cross from the pen to the record store and recommendations already cross from the desk to the person.

ONE FUNCTION IS ALLOCATED TWICE, DELIBERATELY, and the spread is the information. mint-an-option lands on el-holding-pen AND on el-record-store, because the act and the landing are genuinely two things: the pen owns the demands on the crossing, and the store owns where the option comes to rest. Allocating it only to the pen would have hidden that the pool is stored by the same element that holds records — which is exactly the assumption the whole minor column rests on.

## elements

- el-holding-pen — unchanged as an element, extended by one function. It already owns a stray from capture to disposition; the mint is the last act of that life
- el-record-store — unchanged as an element, extended by one function. It already reads what exists from folders on trunk, which is what a minted option becomes
- el-front-desk — unchanged as an element, extended by one function. It already turns intent into a vehicle; offering the whole pool is what makes the list it routes from complete
- NO NEW ELEMENT, and the argument is above rather than merely asserted. No new interface either: both crossings this delta creates already have their pair

## allocation

- fn-run-a-governed-walk.mint-an-option -> el-holding-pen, which owns the ACT and its demands: authored rather than pasted, saying what the option is and when it comes back, and no lighter path for an agent
- fn-run-a-governed-walk.mint-an-option -> el-record-store, which owns where the option LANDS. Two elements for one function, and the spread says the pool is stored by the same element that holds records
- fn-run-a-governed-walk.offer-what-may-be-taken-up -> el-front-desk, which already reads the live machinery and recommends from it; this makes the reading complete rather than one machine's worth
- EVERY NEW FUNCTION IS ALLOCATED AT LEAST ONCE, and nothing was added that implements nothing
- THE TRACE REACHES EVERY NEW REQUIREMENT TRANSITIVELY, by the function chain, and none needed naming directly on an element. That was checked rather than assumed: all eight rows refine a use case, are served by one of the two functions, and both functions are implemented. Zero unreached

## follow_up

- the double allocation of mint-an-option is the structural form of raid-asm-the-pool-is-a-node-kind-under-project-spec. If that assumption falsifies, el-record-store stops implementing it and a new element appears — and that IS the architecture moving, visibly
- no interface node was minted, and the reason is that both crossings already have their pair. If M7 finds a crossing that does not, that is the escalate signal rather than a small addition
- el-record-store's own prose was not rewritten, only its allocation extended; the pool's landing is described in el-holding-pen where the act lives, so the two do not both describe one thing

## anything_else

