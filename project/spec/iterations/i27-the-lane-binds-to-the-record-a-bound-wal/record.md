---
id: i27-the-lane-binds-to-the-record-a-bound-wal
status: shipped
closed: 2026-08-14T19:54:00.895Z
started: 2026-08-13T18:17:34.105Z
opened: 2026-08-13T15:10:43.192Z
goal: "The lane binds to the record: a bound walk never has to leave its worktree, and the write it makes lands where the walk stands."
vision: "ENABLER 1 OF 4, and the first record of the run order (owner, 2026-08-13). Nothing waits on it, and three records wait on it.\n\nA record's worktree is the whole workspace while that record is bound. Every write the work needs — engine, tests, matrix rows, guidance, spec — lands in the worktree and travels to trunk by the merge that already exists. Stepping out to trunk stops being a step anybody takes, because there is nothing left on the other side to reach.\n\nWHY, MEASURED. The step-out ran about eight times in one session on 2026-08-13, three of them inside a single verification. Each one re-walks the machine from its start on the way back. It also caused the day's worst incident: the trace edits landed at trunk, a mid-walk sync pulled 26 commits in to carry two files, and four signed claims fell and had to be re-earned.\n\nIt cost the other machine too. The cloud field report records se_help implemented twice, differently, on two branches, and three merge conflicts at the close.\n\nTHE SHAPE IS DECIDED (owner ruling 2026-08-13). The iteration builds it rather than re-deciding it. Both halves land, and they are one idea seen twice: a record's tree is the only tree the walk can address.\n\n- THE GENERAL RULE, for every product but this one: while a record is bound, the lane's ROOT IS the worktree. project/... resolves there and trunk is unaddressable from inside, so the se_run hole closes without enumerating tools rather than by adding another check. ONE QUESTION IT MUST ANSWER: the running engine lives in trunk's deliverable/, so either a reload can serve from the bound worktree or an engine change stays invisible until the record lands. That question does not arise for a product that never edits the engine, which is every product except this one.\n- THIS PRODUCT IS THE EXCEPTION: Quackitect is self-hosting, so it gets NO worktree and its records walk on trunk. The engine refuses to make one for it. WHY THE EXCEPTION IS SAFE AND HAS EXACTLY ONE MEMBER: a product never works on Quackitect. Only Quackitect works on itself, and a vehicle on itself. So self-hosting is a property a product declares about ITSELF, and no other product can drift into it. That also ends the two-tree drift here, where deliverable/ and spec/ resolved differently and nothing said so. THE STATE MODEL MUST MOVE WITH IT, because this decision breaks it. Today an iteration counts as OPEN if its worktree DIRECTORY exists (iterations.ts:77), and the archive is everything not open (iterations.ts:1192). That separates seeded from finished on this machine only because seeding happens to create a worktree here. It already fails on any peer — a fresh clone showed all 19 pushed records as archived, none ever started — and it will fail HERE the moment this product stops making worktrees, because then every record is not-open and the archive swallows the one being walked. THE OWNER'S RULING, three parts. STATUS COMES FROM THE RECORD, which already carries status: seeded, never from the filesystem. THE ARCHIVE LOOKS EVERYTHING UP THROUGH GIT, for expeditions exactly as for iterations — that is the whole point of retiring a record to its branch. AND A FINISHED RECORD'S WORKTREE IS DELETED FROM DISK: done work keeps no folder. Twenty-four worktrees stand on this machine right now, which is what not doing it looks like.\n\nWHAT MUST SURVIVE EITHER WAY. SE-C-134 exists because of a real accident: on 2026-08-07 a method write from inside a record overwrote trunk's tool list and deleted two lane verbs. Whatever replaces the refusal has to stop that, not merely stop refusing.\n\nTHE LEAK IS PART OF THE JOB. SE-C-134 guards five write tools and not se_run, which is how the same feature got built twice. A fix that leaves se_run addressing trunk has not finished.\n\nALSO IN SCOPE, four things that cost calls every session. THE RIDE-ALONG: every lane result carries what the engine's background work has done — transitions once each, and a rough progress number for anything still running, which se_test already computes and throws away — plus wait:true to attach when there is nothing else to do, and a deadline so a job that will never finish says so. THE COMMIT UNIT IS THE STATE, not the milestone; three cloud sessions ended with 46, 29 and 28 files uncommitted. THE WALK BACK IS TOO SLOW: a re-entry or a reload reboots the machine at start and re-walks every state to get back, recomputing the route and re-evaluating conditions for states already signed and standing. Twelve reloads today. NOT the reading — i3 already made the credit survive a reload and key on content, pinned by feedback-loop.test.ts lines 63 and 79. The cost is the walking, not the proving. A NARRATION REFUSAL MUST NOT DESTROY THE CALL it rode on; the engine already has the right shape where it answers update_refused beside a completed call. VERIFICATION MUST BE ABLE TO FAIL. A claim box can be checked, owed or unchecked, and there is no way to say observed-and-RED — so a red finding and an unstarted one are identical to the engine, the submit refuses at the form lint before any outcome exists, and the fallback into fix-findings can never open. The outcome word for failure appears three times in the engine and no caller passes it. Add the red observation, complete the state as failed when one stands or the battery is red, and the loop the drawing already promises works: fix-findings collects everything, fixes in one pass, and its recovery edge returns for one confirm run. THE LANE IS AN INTERFACE AND NOBODY SPECIFIED IT (owner ruling 2026-08-13). Seventeen interface nodes define every seam between our own elements — method-compiler to walk-engine, record-store to mirror — and not one defines the seam the user actually touches: the agent to the engine, over MCP, carrying 36 verbs. Mint it, with nbr-agent-harness as its source, and make the rule that a new lane verb is defined there. THAT RETIRES THE REQUIREMENT-PER-VERB QUESTION rather than answering it: req-reachable-capability-is-traced demands a requirement naming each capability, its own test spec tsp-derivation-analysis demands only a requirement covering the use case, and 34 of 35 verbs hang on which wording wins. Neither is the right bar. A command surface is an interface, so it is specified as one, and the requirement is corrected to say so. A STANDING CLAIM IS RE-CHECKED WHEN ITS SUBJECT CHANGES, never merely because an iteration reached M7 (owner ruling 2026-08-13). Today M7's claims checklist is applies: full at every size, so an iteration about a keyword search inherited eight whole-product claims including one about the editor panel. This is computable rather than judged: a spec names what it verifies, and a delta names what it changed. Where they do not intersect, the standing verdict carries. WHAT IT IS NOT. Not the frontmatter schema, not the fallback edges, not the headless cloud bootstrap — that is i28, and the rest are backlog rows. This one is the binding, and it is done when a whole iteration runs without the walk ever leaving its record.\n\nISOLATION IS NOT THE GOAL (owner ruling 2026-08-13, correcting the packet). THE GOAL IS THAT WORKTREES WORK. No confusion about where a read or a write lands, and no stepping out of one to get work done. Making trunk unreachable from inside a record is ONE CANDIDATE MECHANISM for that, and it may well be the one we take - it is not the objective and must not be written as though it were.\n\nWHAT THAT CORRECTS. The M1 evidence states the scope as the lane's root IS the worktree, with trunk unaddressable. That is a solution wearing the goal's clothes, and it was written into a gate that is now blessed. The seeded goal line is the honest one and it stands unchanged: a bound walk never has to leave its worktree, and the write it makes lands where the walk stands.\n\nTHE ARCHITECTURE MILESTONE IS THEREFORE NOT PRE-DECIDED, whatever the M1 prose suggests. At least three shapes answer the goal and none is chosen: move the root so trunk is unreachable; judge each path against the record so a wrong write refuses; or fan method out to every tree so nobody needs to leave in the first place. THE THIRD ANSWERS PROBLEM (b) DIRECTLY AND ISOLATES NOTHING.\n\nONE RELATED CORRECTION, from the same ruling. There is no problem with one record reaching into another - that is normal work, not a leak to be sealed. What is wrong is being CONFUSED about which tree a call reached, which is what one path resolving two ways produces.\n\nTWO ITEMS PULLED FORWARD FROM i11 (owner ruling 2026-08-13, after the kickoff blessed). Scope growth past a signed gate, recorded here because se_amend is not legal at draft-vision and the gate's own scope field could not be edited from where the walk stands.\n\n- THE PULL MUST NEVER OVERFLOW. Three overflows landed in M0 alone, at 281KB and 277KB, and M0 is this record's smallest milestone. Every submit landed, so the cost so far is one extra read to confirm it signed. WHAT MAKES IT URGENT IS THE NEXT FAILURE: when a submit is REFUSED, the reason sits inside the payload nobody can read, and no cheap question answers it. This record is sized major, so it walks more and larger forms than any other.\n- THE FILL ANSWER RETURNS THE WHOLE FORM TWICE in one response. Same code path, plausibly half the payload, and the cheapest correction in the set.\n\nTHE OWNER'S DESIGN, AND IT IS BOTH HALVES (2026-08-13, corrected the same day). Fix the SOURCE and keep a PAGINATION MECHANISM. Not one instead of the other. Split guidance, forms and templates into chunks small enough that any one of them can be pulled whole. Where a file must split, give it sub-indexing — dot notation, the same shape as the module-qualified ids i10 brings. The agent then asks for detail where it needs detail, rather than being handed everything in case.\n\nWHY BOTH, AND WHY NEITHER ALONE. Pagination alone still assembles the whole thing and then cuts it, so the cost stays and only the delivery changes. Splitting alone leaves no answer for the case a chunk is still too big, or a caller legitimately wants many chunks at once - and there will always be one. The mechanism is the floor that guarantees no answer can ever overflow whatever the sources look like; the split is what makes that floor almost never bind. Small sources are cheap at every layer: the reading credit keys per chunk, so a changed paragraph re-owes a paragraph rather than a document; a form carries the ids of what it needs rather than the prose; and the same split serves the mirror, which today cannot render a large evidence form either.\n\nWHAT IT TOUCHES THAT IS NOT OBVIOUS. The reading proof is per-document today, so splitting multiplies the documents and the proofs with them. That wants the credit keyed on content — which i3 already built and pinned. So the prerequisite is already standing.\n\nFROM THE POOL, 2026-08-13. Four more, all of them the same binding seen from other sides.\n\n- THE BOUND RETRO CANNOT DO THE RETRO'S OWN JOB (note-430f3af449b8). The retro exists in two homes - as a door from idle, unbound, where it can write method, and as a state inside every iteration, bound, where it cannot. Same state, same guidance, same demands, and in one home it is mechanically barred from the step that is its whole point. NOTHING WARNS YOU: the guidance says to aim improvements at durable homes, the patch tool is listed as legal, and the refusal arrives only at the write. The preferred fix matches the emit-back method: the bound retro COLLECTS, its promotions evidence becomes the list, and a later unbound act lands it. ALSO: the note verb is not in that state's legal tools, yet the method says to emit notes.\n- THE COST OF THE GUARD, MEASURED, AND ITS REPLACEMENT (owner, note-76b47e277859). One stale sentence needed deleting; the round trip was five calls, four of which did no work. The owner is leaning towards removing the rule. THE RULE IS NOT WRONG ABOUT THE DANGER - a method write from a bound worktree fans out over trunk, which happened twice on 2026-08-07 and once deleted two lane verbs. THE REPLACEMENT WAS ALREADY RULED at the 2026-08-06 retro: a method change lands in trunk and every open worktree at once, with the reconcile pass as the reload-time backstop. DO NOT RETIRE THE GUARD ALONE - build the fan-out first and retire it in the same act, because removing the guard without the mechanism removes the cost and the protection together.\n- THE RECORD'S FILES ARE INVISIBLE TO THE OWNER (note-81c6cc77171e, note-b086cd36f9a0). The worktree lives at the repo root while the owner opens the project folder, so a record's minted nodes sit outside the workspace, search cannot find them, and the form's reflinks do not open them. The owner could not open a record's own findings file: the deliverable reached the record and not the reader. Either the worktrees move under the opened folder, or the mirror's links resolve into the worktree so a click opens the file wherever it lives. A SECOND QUESTION RIDES IT: does the lane's state folder move INTO the opened project folder, local-only and ignored by version control, so the handover, the reading file and the findings pointers open in the editor.\n- THE RETRO CANNOT DRAIN THE ASSISTANT MEMORY (note-e4e1f3049d0e), and the fix is one line of configuration only the owner can write. Contract rule 7 says the memory is a scratchpad and every retro drains it. The memory lives outside the project root, the file read refuses paths outside it, and neither door out fits - a committed ref reaches this repo's past, and a declared named root is the owner's act. Reaching for the shell would be working around a refusal. ONE THING STILL WANTS DECIDING: draining means REMOVING what moved into the repo, and a read-only root cannot delete."
inputs:
depends_on:
---

# i27-the-lane-binds-to-the-record-a-bound-wal

## Goal

The lane binds to the record: a bound walk never has to leave its worktree, and the write it makes lands where the walk stands.

## Rough vision

ENABLER 1 OF 4, and the first record of the run order (owner, 2026-08-13). Nothing waits on it, and three records wait on it.

A record's worktree is the whole workspace while that record is bound. Every write the work needs — engine, tests, matrix rows, guidance, spec — lands in the worktree and travels to trunk by the merge that already exists. Stepping out to trunk stops being a step anybody takes, because there is nothing left on the other side to reach.

WHY, MEASURED. The step-out ran about eight times in one session on 2026-08-13, three of them inside a single verification. Each one re-walks the machine from its start on the way back. It also caused the day's worst incident: the trace edits landed at trunk, a mid-walk sync pulled 26 commits in to carry two files, and four signed claims fell and had to be re-earned.

It cost the other machine too. The cloud field report records se_help implemented twice, differently, on two branches, and three merge conflicts at the close.

THE SHAPE IS DECIDED (owner ruling 2026-08-13). The iteration builds it rather than re-deciding it. Both halves land, and they are one idea seen twice: a record's tree is the only tree the walk can address.

- THE GENERAL RULE, for every product but this one: while a record is bound, the lane's ROOT IS the worktree. project/... resolves there and trunk is unaddressable from inside, so the se_run hole closes without enumerating tools rather than by adding another check. ONE QUESTION IT MUST ANSWER: the running engine lives in trunk's deliverable/, so either a reload can serve from the bound worktree or an engine change stays invisible until the record lands. That question does not arise for a product that never edits the engine, which is every product except this one.
- THIS PRODUCT IS THE EXCEPTION: Quackitect is self-hosting, so it gets NO worktree and its records walk on trunk. The engine refuses to make one for it. WHY THE EXCEPTION IS SAFE AND HAS EXACTLY ONE MEMBER: a product never works on Quackitect. Only Quackitect works on itself, and a vehicle on itself. So self-hosting is a property a product declares about ITSELF, and no other product can drift into it. That also ends the two-tree drift here, where deliverable/ and spec/ resolved differently and nothing said so. THE STATE MODEL MUST MOVE WITH IT, because this decision breaks it. Today an iteration counts as OPEN if its worktree DIRECTORY exists (iterations.ts:77), and the archive is everything not open (iterations.ts:1192). That separates seeded from finished on this machine only because seeding happens to create a worktree here. It already fails on any peer — a fresh clone showed all 19 pushed records as archived, none ever started — and it will fail HERE the moment this product stops making worktrees, because then every record is not-open and the archive swallows the one being walked. THE OWNER'S RULING, three parts. STATUS COMES FROM THE RECORD, which already carries status: seeded, never from the filesystem. THE ARCHIVE LOOKS EVERYTHING UP THROUGH GIT, for expeditions exactly as for iterations — that is the whole point of retiring a record to its branch. AND A FINISHED RECORD'S WORKTREE IS DELETED FROM DISK: done work keeps no folder. Twenty-four worktrees stand on this machine right now, which is what not doing it looks like.

WHAT MUST SURVIVE EITHER WAY. SE-C-134 exists because of a real accident: on 2026-08-07 a method write from inside a record overwrote trunk's tool list and deleted two lane verbs. Whatever replaces the refusal has to stop that, not merely stop refusing.

THE LEAK IS PART OF THE JOB. SE-C-134 guards five write tools and not se_run, which is how the same feature got built twice. A fix that leaves se_run addressing trunk has not finished.

ALSO IN SCOPE, four things that cost calls every session. THE RIDE-ALONG: every lane result carries what the engine's background work has done — transitions once each, and a rough progress number for anything still running, which se_test already computes and throws away — plus wait:true to attach when there is nothing else to do, and a deadline so a job that will never finish says so. THE COMMIT UNIT IS THE STATE, not the milestone; three cloud sessions ended with 46, 29 and 28 files uncommitted. THE WALK BACK IS TOO SLOW: a re-entry or a reload reboots the machine at start and re-walks every state to get back, recomputing the route and re-evaluating conditions for states already signed and standing. Twelve reloads today. NOT the reading — i3 already made the credit survive a reload and key on content, pinned by feedback-loop.test.ts lines 63 and 79. The cost is the walking, not the proving. A NARRATION REFUSAL MUST NOT DESTROY THE CALL it rode on; the engine already has the right shape where it answers update_refused beside a completed call. VERIFICATION MUST BE ABLE TO FAIL. A claim box can be checked, owed or unchecked, and there is no way to say observed-and-RED — so a red finding and an unstarted one are identical to the engine, the submit refuses at the form lint before any outcome exists, and the fallback into fix-findings can never open. The outcome word for failure appears three times in the engine and no caller passes it. Add the red observation, complete the state as failed when one stands or the battery is red, and the loop the drawing already promises works: fix-findings collects everything, fixes in one pass, and its recovery edge returns for one confirm run. THE LANE IS AN INTERFACE AND NOBODY SPECIFIED IT (owner ruling 2026-08-13). Seventeen interface nodes define every seam between our own elements — method-compiler to walk-engine, record-store to mirror — and not one defines the seam the user actually touches: the agent to the engine, over MCP, carrying 36 verbs. Mint it, with nbr-agent-harness as its source, and make the rule that a new lane verb is defined there. THAT RETIRES THE REQUIREMENT-PER-VERB QUESTION rather than answering it: req-reachable-capability-is-traced demands a requirement naming each capability, its own test spec tsp-derivation-analysis demands only a requirement covering the use case, and 34 of 35 verbs hang on which wording wins. Neither is the right bar. A command surface is an interface, so it is specified as one, and the requirement is corrected to say so. A STANDING CLAIM IS RE-CHECKED WHEN ITS SUBJECT CHANGES, never merely because an iteration reached M7 (owner ruling 2026-08-13). Today M7's claims checklist is applies: full at every size, so an iteration about a keyword search inherited eight whole-product claims including one about the editor panel. This is computable rather than judged: a spec names what it verifies, and a delta names what it changed. Where they do not intersect, the standing verdict carries. WHAT IT IS NOT. Not the frontmatter schema, not the fallback edges, not the headless cloud bootstrap — that is i28, and the rest are backlog rows. This one is the binding, and it is done when a whole iteration runs without the walk ever leaving its record.

ISOLATION IS NOT THE GOAL (owner ruling 2026-08-13, correcting the packet). THE GOAL IS THAT WORKTREES WORK. No confusion about where a read or a write lands, and no stepping out of one to get work done. Making trunk unreachable from inside a record is ONE CANDIDATE MECHANISM for that, and it may well be the one we take - it is not the objective and must not be written as though it were.

WHAT THAT CORRECTS. The M1 evidence states the scope as the lane's root IS the worktree, with trunk unaddressable. That is a solution wearing the goal's clothes, and it was written into a gate that is now blessed. The seeded goal line is the honest one and it stands unchanged: a bound walk never has to leave its worktree, and the write it makes lands where the walk stands.

THE ARCHITECTURE MILESTONE IS THEREFORE NOT PRE-DECIDED, whatever the M1 prose suggests. At least three shapes answer the goal and none is chosen: move the root so trunk is unreachable; judge each path against the record so a wrong write refuses; or fan method out to every tree so nobody needs to leave in the first place. THE THIRD ANSWERS PROBLEM (b) DIRECTLY AND ISOLATES NOTHING.

ONE RELATED CORRECTION, from the same ruling. There is no problem with one record reaching into another - that is normal work, not a leak to be sealed. What is wrong is being CONFUSED about which tree a call reached, which is what one path resolving two ways produces.

TWO ITEMS PULLED FORWARD FROM i11 (owner ruling 2026-08-13, after the kickoff blessed). Scope growth past a signed gate, recorded here because se_amend is not legal at draft-vision and the gate's own scope field could not be edited from where the walk stands.

- THE PULL MUST NEVER OVERFLOW. Three overflows landed in M0 alone, at 281KB and 277KB, and M0 is this record's smallest milestone. Every submit landed, so the cost so far is one extra read to confirm it signed. WHAT MAKES IT URGENT IS THE NEXT FAILURE: when a submit is REFUSED, the reason sits inside the payload nobody can read, and no cheap question answers it. This record is sized major, so it walks more and larger forms than any other.
- THE FILL ANSWER RETURNS THE WHOLE FORM TWICE in one response. Same code path, plausibly half the payload, and the cheapest correction in the set.

THE OWNER'S DESIGN, AND IT IS BOTH HALVES (2026-08-13, corrected the same day). Fix the SOURCE and keep a PAGINATION MECHANISM. Not one instead of the other. Split guidance, forms and templates into chunks small enough that any one of them can be pulled whole. Where a file must split, give it sub-indexing - dot notation, the same shape as the module-qualified ids i10 brings. The agent then asks for detail where it needs detail, rather than being handed everything in case.

WHY BOTH, AND WHY NEITHER ALONE. Pagination alone still assembles the whole thing and then cuts it, so the cost stays and only the delivery changes. Splitting alone leaves no answer for the case a chunk is still too big, or a caller legitimately wants many chunks at once - and there will always be one. The mechanism is the floor that guarantees no answer can ever overflow whatever the sources look like; the split is what makes that floor almost never bind. Small sources are cheap at every layer: the reading credit keys per chunk, so a changed paragraph re-owes a paragraph rather than a document; a form carries the ids of what it needs rather than the prose; and the same split serves the mirror, which today cannot render a large evidence form either.

WHAT IT TOUCHES THAT IS NOT OBVIOUS. The reading proof is per-document today, so splitting multiplies the documents and the proofs with them. That wants the credit keyed on content - which i3 already built and pinned. So the prerequisite is already standing.

FROM THE POOL, 2026-08-13. Four more, all of them the same binding seen from other sides.

- THE BOUND RETRO CANNOT DO THE RETRO'S OWN JOB (note-430f3af449b8). The retro exists in two homes - as a door from idle, unbound, where it can write method, and as a state inside every iteration, bound, where it cannot. Same state, same guidance, same demands, and in one home it is mechanically barred from the step that is its whole point. NOTHING WARNS YOU: the guidance says to aim improvements at durable homes, the patch tool is listed as legal, and the refusal arrives only at the write. The preferred fix matches the emit-back method: the bound retro COLLECTS, its promotions evidence becomes the list, and a later unbound act lands it. ALSO: the note verb is not in that state's legal tools, yet the method says to emit notes.
- THE COST OF THE GUARD, MEASURED, AND ITS REPLACEMENT (owner, note-76b47e277859). One stale sentence needed deleting; the round trip was five calls, four of which did no work. The owner is leaning towards removing the rule. THE RULE IS NOT WRONG ABOUT THE DANGER - a method write from a bound worktree fans out over trunk, which happened twice on 2026-08-07 and once deleted two lane verbs. THE REPLACEMENT WAS ALREADY RULED at the 2026-08-06 retro: a method change lands in trunk and every open worktree at once, with the reconcile pass as the reload-time backstop. DO NOT RETIRE THE GUARD ALONE - build the fan-out first and retire it in the same act, because removing the guard without the mechanism removes the cost and the protection together.
- THE RECORD'S FILES ARE INVISIBLE TO THE OWNER (note-81c6cc77171e, note-b086cd36f9a0). The worktree lives at the repo root while the owner opens the project folder, so a record's minted nodes sit outside the workspace, search cannot find them, and the form's reflinks do not open them. The owner could not open a record's own findings file: the deliverable reached the record and not the reader. Either the worktrees move under the opened folder, or the mirror's links resolve into the worktree so a click opens the file wherever it lives. A SECOND QUESTION RIDES IT: does the lane's state folder move INTO the opened project folder, local-only and ignored by version control, so the handover, the reading file and the findings pointers open in the editor.
- THE RETRO CANNOT DRAIN THE ASSISTANT MEMORY (note-e4e1f3049d0e), and the fix is one line of configuration only the owner can write. Contract rule 7 says the memory is a scratchpad and every retro drains it. The memory lives outside the project root, the file read refuses paths outside it, and neither door out fits - a committed ref reaches this repo's past, and a declared named root is the owner's act. Reaching for the shell would be working around a refusal. ONE THING STILL WANTS DECIDING: draining means REMOVING what moved into the repo, and a read-only root cannot delete.
