---
form: find_without
by: agent
signed_off: 2026-08-14T07:03:14.722Z
authors: agent
files:
---

# Evidence form / find_without

## current_situation

EVERY CLUSTER IS ASKED, including the ones that obviously stay. A cluster that survived the question is better justified than one nobody asked about.

NONE OF THE FIVE CAN GO WHOLE. That is the honest answer and it is not the interesting one.

THE INTERESTING TRIM IS ONE LEVEL DOWN, on the function this record just minted. Asking whether resolve-a-path can go at all produces three of meth-trimming's four outcomes, and every one of them is a real option nobody would have proposed.

- THE USER takes over: every path names its tree and nothing is inferred.
- THE ENVIRONMENT takes over: one process per record, rooted by the operating system.
- NOBODY takes over, because there is only one tree: opt-the-branch-is-the-record, rejected at i1 and back on the chart as the null option.

THE NULL OPTION IS THE ONE THIS FINDER EXISTS FOR. It says the function this iteration minted should not exist, and it goes on the chart precisely because nobody proposes that.

## applies

yes

## trims

| cluster | who_takes_over | what_breaks |
| --- | --- | --- |
| cluster-the-walk | nobody | The product stops being the thing it claims to be. Serving one governed step at a time is what it IS, so this is the fatal row and asking it confirms the partition rather than moving it. |
| cluster-the-record-life | cluster-the-walk | The walk could open and close its own records. What breaks is that landing, closing and archiving lose their separate refusals, and req-archive-releases-worktrees has nowhere left to live. |
| cluster-the-account | the environment | A person can read the repository with an editor and git. What breaks is that nothing DERIVES: the trace, the tour and the panel become hand-maintained and rot the day they are written. |
| cluster-the-holding-pen | cluster-the-account | The account could hold strays as ordinary written records. What breaks is the rule that neither member may open a record - which is the only thing holding those two together, so the cluster's own rationale predicted this answer. |
| cluster-the-bootstrap | the environment | A person can copy a template folder by hand. What breaks is req-scaffold-from-template's own line: a hand-rolled product tree drifts from the schema and the engine misreads or refuses it later. |

## options

- project/spec/trace/option/opt-the-caller-names-the-tree.md
- project/spec/trace/option/opt-one-process-per-record-rooted-by-the-os.md
- project/spec/trace/option/opt-the-branch-is-the-record.md

## follow_up

opt-the-branch-is-the-record IS BACK ON THE CHART AND IT WAS REJECTED AT i1. raid-dec-thin-tree lists it among the rejected options with one line of reason: no isolation seam at all.

THAT REASON MAY NO LONGER BIND. It was written when isolation was the goal. This record's own correction of 2026-08-13 rules that ISOLATION IS NOT THE GOAL - the goal is that worktrees work, with no confusion about where a read or a write lands, and isolation is one candidate mechanism rather than the objective.

With one tree there is no confusion, because there is nothing to confuse. The architecture milestone owes it a proper look rather than an inherited rejection.

THE HOLDING PEN'S ROW CONFIRMS ITS OWN CLUSTER NOTE. cluster-the-holding-pen calls itself the weakest cluster in the partition and says its two members share no flow, only a rule. The trim asked independently and reached the same place. Not a finding to act on - a check that the partition is honest about itself.

## anything_else

WHY THE CLUSTER-LEVEL PASS LOOKS DULL AND IS NOT WASTE.

Five rows, five survivals. Read alone that is a pass that changed nothing.

What it actually produced: the-holding-pen's weakness confirmed from a second direction, the-record-life named as the only home for an unmet requirement, and the-account's failure mode stated as ROT rather than absence.

meth-trimming's own failure mode is trimming what is cheap instead of what is expensive, and the two expensive rows here are the-walk and the-record-life. Both were asked first and both were asked seriously.

THE FUNCTION-LEVEL TRIM IS WHERE THE VALUE WAS, and the table's grain is the cluster, so it is recorded here rather than lost. Three of the four sanctioned outcomes fired on one function, which is the strongest yield of any finder run on this record so far.
