---
form: refusal-remedies
by: agent
signed_off: 2026-08-14T17:54:26.388Z
authors: agent
files:
---

# Evidence form / refusal-remedies

## current_situation

The last unsigned feeder into `build-steps/end`. Its code was built earlier in this record; the form was never stamped.

The statement promises three things, and each one closes a way the engine used to block somebody without telling them how to get unblocked.

## built

A NOT-STANDING REFUSAL NAMES THE VERB THAT FIXES IT.

`fallenRemedy` in `engine/session.ts`. A claim loses its green two ways and they want DIFFERENT verbs, so the engine decides which rather than making the reader guess.

- ITS CONTENT STILL PASSES. Something upstream moved and the ripple dropped it. The remedy is `se_amend`, which leaves the tree standing — and the bless with it.
- ITS CONTENT NO LONGER PASSES. The work is genuinely wrong. The remedy is `se_reopen`, and it quotes the problem that makes it wrong.

WHY IT MATTERS THAT THE ENGINE DECIDES. `se_reopen` on the first case is what cost a bless on 2026-08-14: the resubmit dropped the person's adjudication and six milestones fell with it, each needing a hand fix.

A REOPEN SAYS WHAT IT WILL DROP, BEFORE DROPPING IT.

`engine/session.ts`, the reopen verb. A reopen keeps the SIGNATURE and always said so. It does not keep the BLESS, and nothing said so.

So a reopen of a person-blessed claim now REFUSES without `confirm: true`, and the refusal carries three facts: the bless it would destroy, quoted; how many states fall with it, counted by `wouldFall`; and the fact that `se_amend` would leave the tree standing if the content still passes.

A SIGNATURE AND A BLESS ARE NOT THE SAME LOSS. A signature records who wrote it. A bless records who ADJUDICATED it, and at a low dial only a person can. That one is confirmed rather than assumed.

A RESUBMIT CARRIES THE BLESS.

The same guard is what makes this true. The path that silently erased an adjudication now cannot be taken by accident, and the path that preserves it is named in the refusal rather than left to be discovered.

Proof, and the first is better than a test: `fallenRemedy` FIRED LIVE during this session. `se_why` on `iterations/i27/evaluate-architecture` answered with the `se_reopen` branch and quoted the defect — "allocation: owed crossings without an interface — el-resolution-seam → el-account". Declaring the missing interface cleared it, and NO REOPEN WAS NEEDED. The remedy told the truth about which verb the situation wanted, which is the whole claim.

`note-77786c9a36ea` holds the incident this chunk answers.

## follow_up

This was the last unsigned feeder. `build-steps/end` still owes its own form, and then the join closes.

Thirteen chunks, thirteen signed forms.

No notes parked from this chunk.

## anything_else

One thing worth saying about how this chunk was verified, because it is unusual.

I DID NOT WRITE A TEST FOR THE FIRST PROMISE. It was exercised for real, mid-walk, on a fallen input this session actually produced — and the remedy it named turned out to be the wrong one to follow, in the useful sense: the right move was to fix the defect the remedy QUOTED, after which nothing needed reopening at all.

That is a stronger reading than a fixture would have given. A test would have asserted that the remedy says `se_reopen`. The live run showed that the remedy's quoted reason was accurate enough to fix the cause instead, which is what a person actually does with it.

The other two promises are read from the code rather than exercised, and I am saying so rather than implying otherwise. Reproducing them needs a person-blessed claim to reopen, and destroying one to prove the guard would be the same harm the guard exists to prevent.
