---
form: gate-candidates
bless: blessed by human
by: agent
signed_off: 2026-08-14T09:27:29.900Z
reopened: "2026-08-14T07:51:40.526Z — The gate blessed a front computed over an unfair comparison, and its red team found one real defect and stopped looking before reaching this one."
authors: agent
files:
---

# Evidence form / gate-candidates

## current_situation

THE FRONT IS ONE: cand-judged-path. It dominates all three rivals.

TOTALS: judged-path 36, speaking-root 34, fixed-root 32, os-rooted 24. Twelve axes.

cand-os-rooted FELL, and that is the change. It stood on the front through three passes on exactly one cell - the only 4 on the board, on parallel records. A blind rescore moved that 4 to cand-judged-path, and with it gone cand-os-rooted is beaten on every axis.

WHY THE RESCORE HAPPENED. The owner reworked the register and two surviving rows were restated from a mechanism to an outcome after the anchors were written. A spawned scorer with none of this record's reasoning read the four candidate records and the two restated rows, and returned eight cells.

IT OVERRULED THE WALKING AGENT ON FOUR OF THE EIGHT. The walking agent had predicted cand-speaking-root down to 2 on parallel records and cand-os-rooted holding its 4. Both predictions were wrong, and the corrections went against the walking agent's own reasoning.

THE DEMAND GATE RAN FOR THE FIRST TIME. meth-derive-criteria says a `must` gates every candidate pass or fail here, and in four passes of this gate nothing had ever applied one. Eight demands, four candidates. Only cand-judged-path passes every demand that can be adjudicated from a candidate record.

THIS GATE'S OWN FIELDS WERE CORRUPTED AND REBUILT. A fill meant for evaluate-set landed here at 09:14 because the walk had moved on, overwriting three fields with the wrong state's prose. Recorded rather than smoothed.

## reasons_hold

A FRONT OF ONE, WHICH THIS GATE'S GUIDANCE ASKS ABOUT BY NAME. Honest, or did the space collapse upstream? Both cases, then the ruling.

THE CASE THAT IT COLLAPSED, and it is the stronger-sounding one. cand-judged-path now equals the UTOPIA POINT on every single axis. Utopia is 4 on parallel records, 2 on archive and 3 elsewhere, and the winner holds every one. A Pareto front exists to keep a trade visible, and there is no trade left to see. That is what a collapsed space looks like from inside.

THE CASE THAT IT DID NOT, and it rests on where the collapse happened. The space held four complete lines drawn across seven chart rows and 768 combinations. All four were scored on the same twelve axes. Three fell to DOMINATION rather than to a sum - beaten or equalled everywhere, strictly beaten somewhere. Nothing was cut to reach this. The collapse is in the scoring, not in the drawing.

THE RULING: HONEST, AND RECORDED AS THE THING TO ATTACK AT M5. A front of one is not evidence of a bad process here, because the process kept four candidates alive and let a blind scorer overturn the walking agent's guesses. It IS evidence that twelve axes may be too coarse to see what the winner costs, and that is an override below.

THE THREE ELIMINATIONS, EACH CHECKED AGAINST ITS CANDIDATE RECORD.

cand-speaking-root loses on two cells. On req-engine-folder-is-sealed its confinement covers writes into the folder while resolving the engine's own resources from it rests on raid-asm-engine-serves-from-the-bound-tree, whose own probe field says it cannot be run from this repository. On req-parallel-iterations-own-worktrees the blind scorer held it at 3 rather than 4 because the confinement handle is a variable rather than a kernel one, and the record itself logs a bypass within days of the last guard landing. Both are properties of the shape, not gaps in the writing.

cand-fixed-root loses on three. Two are one cause - the fan-out leaves a copy in every tree and its own text says nothing signals which copy answered. The third is new: the blind scorer read its parallel-records answer as an ambiguity refusal whose cases nobody has enumerated, and its own core option says the shape needs a dispatch judgment beside it which the candidate does not pick.

cand-os-rooted IS THE ELIMINATION THAT NEEDS THE MOST SCRUTINY, because it survived three passes and fell in the fourth on one cell moving. The scorer's reason is checkable against the record: the mirror, the claim ledger and the note inbox stay machine-wide and single, and the candidate says in as many words that neither answer is designed. The restated row demands that zero record be put in the way of any other. Undesigned shared state is exactly where one record is still put in the way of another.

THE CUTS HOLD, NINETY-ONE OF THEM, unchanged across four passes.

ONE ROW WAS STRUCK IN THIS PASS: req-entry-binds-worktree, superseded by owner ruling 2026-08-14. Its column held 3, 3, 3, 2 and its removal crossed no pair. It cannot have caused the front to collapse.

NO ROW WAS MOVED OUT OF RANK ORDER in any pass.

## round_0_verify

- evidence vs claims: PASS, with the standing gap named. Every measurement was re-read at source: exp-trunk-read-cost for the three read shapes, and the 2026-08-14 probe for process startup with the engine module load named as excluded. The two rescored columns were filled by a SPAWNED blind scorer that read the four candidate records and both restated requirement files in full and had none of this record's reasoning. It overruled the walking agent on four of the eight cells it touched. THE GAP: the other ten columns still carry scores from the earlier pass, where the scorer read five of the axis definitions in full and judged the rest from ids and candidate text.
- types: NOT RUN. No code was written in this milestone, so a type check has nothing to read.
- lint: RAN AND CAUGHT ONE. The requirement lint refused req-a-wrong-act-never-passes-silently for the weasel word "would" in its statement. Rewritten and accepted. The form's own line checks fired at cut-criteria and build_chart in earlier passes.
- tests: NOT RUN, and correctly. SE-C-130 refuses an unchanged tree, and nothing in the deliverable changed.

## round_1_validate

- exercised against the goal: PASS, and for the first time the DEMAND GATE ran. meth-derive-criteria says a `must` gates every candidate pass or fail here, and four passes of this gate had never applied one. Eight demands were checked against all four candidate records; the table is in anything_else. THE RESULT: cand-judged-path is the only candidate passing every demand that a candidate record can be judged on. cand-os-rooted fails req-a-write-lands-where-it-is-meant, because nothing in it refuses an escaping relative path. Three of the four fail req-shared-change-reaches-without-unlanded-work-reaching, all for the same reason the row's own Detail names - they copy shared method into every tree, so propagation passes while the copies drift.
- missing: TWO SHAPES NOW. First, unchanged across four passes: nothing addresses a walk ALREADY IN FLIGHT when the resolution rule changes. Second, found by the blind scorer rather than by the composer: ARCHIVE IS ABSENT FROM ALL FOUR RECORDS. Not one mentions archive, close, or removing a working copy, and req-archive-releases-worktrees now demands zero copies left on disk.
- wrong: ONE PROCESS DEFECT, in this gate rather than in the result. A fill meant for evaluate-set landed on this gate at 09:14 and overwrote three of its fields with the wrong state's prose, because a bare submit had walked the position forward two states and nothing in the answer said so before the fill was accepted. The fields are rebuilt here. Nothing was found wrong in the comparison itself.
- out of scope: NOTHING PULLED IN. Tree-qualified identifiers stays with i10; a resolver answering file ownership stays with i7 and i18.
- prior art: COMPARISON MADE IN BOTH DIRECTIONS, and it moved this pass. CAP-STD AND openat2 RESOLVE_BENEATH still do confinement better than anything here - kernel-enforced, surviving the process, one system call - and the blind scorer used exactly that comparison to hold cand-speaking-root at 3 rather than 4, citing the candidate's own admission that its handle is a variable. GIT WORKTREE WITH SPARSE-CHECKOUT plus MICROSOFT SCALAR is the comparison cand-judged-path was judged PAR with, and that is the only 4 on the board. THE OS PROCESS WORKING DIRECTORY is still real prior art and cand-os-rooted still uses it rather than reimplementing it - the scorer's reason for dropping it to 3 is not the mechanism but the machine-wide shared state left around it. MONOREPO TOOLING remains the comparison we beat: workspace-tools resolves to the git root rather than the workspace root and the caller finds out later. THE HONEST READING, revised: we trail the kernels on prevention, we now match git's own per-record checkout shape on isolation rather than the process shape, and we lead the tooling on saying what we did.

## round_2_red_team

- STEELMAN, and it is sharper this pass than any before => The demand gate had never run in four passes of this state. It ran on the pass where it would confirm the winner. Three of its eight demands were minted this morning by the same agent, at the owner's instruction, in the same session that produced the winner. One of those three eliminates three of four candidates by itself. From outside, that is a rule invented and then applied to reach an answer.
- THE FIRST ANSWER, which has to be evidence rather than protest => req-shared-change-reaches-without-unlanded-work-reaching names its own failure modes in its Detail, and it names them GENERICALLY: "Copy the method into every tree and propagation passes while the copies drift. Seal every tree and isolation passes while a method fix never arrives." Three candidates copy. One does not. That is a mechanical reading of the text, not a judgment anybody tuned.
- THE SECOND ANSWER, from the owner's side => That row exists because the owner asked, in their own words, how several records working in parallel avoid poisoning each other while the changes still apply. The demand is theirs. If it eliminates three candidates, the elimination is theirs too.
- THE THIRD ANSWER, and it is the one that would have caught fitting => If the gate were being fitted, the fit would favour cand-speaking-root, the shape this whole record was created to argue for. It is eliminated. Four passes in a row have moved AGAINST the record's own thesis.
- A COUNTER I ACCEPT RATHER THAN ANSWER => cand-judged-path passes the propagation half of that demand VACUOUSLY. It has no copy to update, because the thin tree reads trunk live. That is a real answer and it is also the cheapest possible one, and it is bought at 2.04 ms per shared read against 0.5 ms on plain disk. The cost is scored, at 3 on req-call-answers-in-one-second, and the under-load case is unmeasured.
- THE KILL-CRITERION => This gate is wrong if cand-judged-path's dominance rests on machinery that does not exist. Its decisive cells - the sealed engine, the trace source, the overlay, the archive, the only 4 on the board, and the one demand that eliminated three rivals - are ALL earned by the thin tree.
- LOOKED FOR IT, AND FOUND IT, WORSE THAN LAST PASS => raid-dec-thin-tree reads status: decided, and the probe of 2026-08-14 found project/ inside the bound worktree still holding deliverable, guidance and spec. The thin tree is DECIDED AND UNBUILT. Last pass three cells rested on it. Now six do, plus the demand gate's decisive row. THE WINNER IS ALMOST ENTIRELY CONTINGENT ON MACHINERY NOBODY HAS BUILT.
- WHAT THAT DOES TO THE VERDICT => It does not overturn the front, because the candidate's own text prices the thin tree as BUILD rather than as inheritance, and both scorers read it that way. It does mean M5 opens on a winner whose every advantage is a promise, and the architecture milestone's first act must be to price the thin tree rather than to assume it.
- A SECOND ATTACK, ON THE COLLAPSE ITSELF => The winner equals the utopia point on every axis. Either it genuinely leads everywhere, or twelve axes cannot see what it costs. The second reading has support: archive came back 2, 2, 2, 1 because NO candidate addresses archive, so that axis is scoring an absence rather than a difference, and req-trace-view-derived-from-files is flat at 3 across all four. Two of twelve axes are carrying no real signal.
- A THIRD ATTACK, ON THE ELIMINATIONS' FRAGILITY => cand-speaking-root's loss on req-engine-folder-is-sealed still rests on one unprobeable assumption; resolve it favourably and that cell moves to 3. But it now ALSO loses on parallel records, so the domination survives that reversal. The elimination is firmer than it was last pass, and the reason is a blind scorer rather than an argument made here.
- WHAT I COULD NOT SETTLE, HANDED UP BY THE BLIND SCORER => Whether an archived record drops out of the claim file. Whether one in-process seam can be made unbypassable, which BOTH leaders rest on. Whether the thin tree can be built at all. Whether machine-wide shared state serialises parallel records. And what answers for a product with no claim entry - this product's own case - when all four candidates lean on the claim file.

## raid_additions

- none

## verdict

pass with overrides — the front of one is honest and every elimination is domination, but the winner's advantages all rest on one decision that reads decided and is unbuilt, and two of the twelve axes are carrying no signal.

WHAT PASSES. The 91 cuts hold across four passes. The one row struck this pass was superseded by owner ruling and crossed no pair. Three eliminations, all domination rather than margin, all checkable against the losing candidates' own recorded text. The two rescored columns came from a spawned blind scorer that overruled the walking agent on half the cells it touched, in both directions. The demand gate ran for the first time and its decisive row is the owner's own words. The prior-art round runs both ways and places us honestly.

THE FIRST OVERRIDE, AND IT IS BIGGER THAN LAST PASS. cand-judged-path's decisive cells are the sealed engine, the trace source, the overlay, the archive, the only prior-art par on the board, and the demand that eliminated three rivals. Every one of them is earned by the thin tree. raid-dec-thin-tree reads decided and the probe of 2026-08-14 found it unbuilt. The winner's advantages are, almost without exception, promises. M5 must price the thin tree as build before it converges on anything.

THE SECOND OVERRIDE. The winner equals the UTOPIA POINT on every axis, which is the one shape a Pareto front is supposed to make impossible. Two axes explain part of it and neither explanation is comfortable: req-archive-releases-worktrees came back 2, 2, 2, 1 because no candidate addresses archive at all, and req-trace-view-derived-from-files is flat at 3. Two of twelve axes carry no real signal, and the criterion set may be too coarse to see what the winner costs.

THE THIRD OVERRIDE, ON WHAT THE DEMANDS THEMSELVES SAY. Two of the eight `must` demands still name the mechanism in their statement - req-a-method-change-reaches-every-tree says "every open worktree" and req-entry-levels-the-record-tree says "its tree". Gating candidates on a demand that names a mechanism prejudges the solution, and it is the exact class the owner's audit was opened to find. cand-judged-path passes both of them vacuously, having no copy to level. That is a pass on the letter and the letter is wrong.

THE FOURTH, ON PROCESS. This gate has now been earned four times in one day and its own fields were overwritten by a misdirected fill in the middle of the fourth. The walk moved two states forward on a bare submit and nothing said so before the next fill was accepted. That is the same class of failure this whole iteration exists to end, occurring inside the iteration studying it.

THE THUMB IS NOT MINE. The bless is the owner's, by their instruction on 2026-08-14. No earlier bless carries - it was given to a packet now replaced three times.

## follow_up

PRICE THE THIN TREE FIRST, BEFORE M5 CONVERGES ON ANYTHING. raid-dec-thin-tree reads decided and is unbuilt, and six of the winner's cells plus the decisive demand rest on it. Either the status is corrected or the build is carried explicitly as the architecture milestone's first cost.

ARCHIVE IS A HOLE IN ALL FOUR CANDIDATE RECORDS. Not one mentions archive, close, or removing a working copy. Whatever wins inherits an undesigned half of req-archive-releases-worktrees.

VERSION CONTROL IS THE SAME HOLE. No candidate record addresses req-version-control-resolves-like-every-call. cand-judged-path's write predicate covering "every verb including the shell" is the only text that could be read to cover it, and reading it that way is inference rather than what the record says.

THE SURFACES ARE THE THIRD HOLE. No candidate addresses req-a-surface-resolves-to-what-it-shows, and cand-os-rooted actively breaks it. Three demands the four records cannot be judged on at all is a finding about the records, not about the demands.

TWO DEMANDS STILL NAME THEIR MECHANISM and want restating as outcomes. req-a-method-change-reaches-every-tree says "every open worktree". req-entry-levels-the-record-tree says "its tree". Both are `must`, so both gate, and both prejudge the solution.

A SIBLING `must` IS STILL OWED for req-a-wrong-act-never-passes-silently. It is a `should`, so a candidate that fails silently is marked down rather than refused.

THE FALL-PROPAGATION DEFECT FIRED AGAIN. Reopening cut-criteria left evaluate-set and this gate walkable, and a bare submit then walked straight through both. Notes note-aed2d996bf92, note-8fc86955ef7d and note-c602df6006d2 all describe it and it is i6's.

## anything_else

THE DEMAND GATE, RUN FOR THE FIRST TIME. meth-derive-criteria line 67: a `must` is a demand, never a criterion, and it gates every candidate pass or fail here. Eight demands stand in this record's cone.

| demand | speaking-root | fixed-root | judged-path | os-rooted |
| --- | --- | --- | --- | --- |
| req-a-read-comes-from-where-it-is-meant | pass | pass | pass | pass |
| req-a-write-lands-where-it-is-meant | pass | pass | pass | FAIL |
| req-a-resolution-is-proven-by-read-back | not answered | not answered | pass | not answered |
| req-a-method-change-reaches-every-tree | pass | pass | pass (vacuous) | pass |
| req-entry-levels-the-record-tree | pass | pass | pass (vacuous) | pass |
| req-shared-change-reaches-without-unlanded-work-reaching | FAIL | FAIL | pass | FAIL |
| req-version-control-resolves-like-every-call | not answered | not answered | not answered | not answered |
| req-a-surface-resolves-to-what-it-shows | not answered | not answered | not answered | FAIL |

THE TWO FAILING DEMANDS, WITH THEIR EVIDENCE.

cand-os-rooted FAILS req-a-write-lands-where-it-is-meant. Its own text says nothing refuses an escaping relative path, so the write completes and reports success with the tree merely named. The demand says the write lands in the tree the caller meant.

THREE OF FOUR FAIL req-shared-change-reaches-without-unlanded-work-reaching, all for one cause the row's own Detail names in advance: they copy shared method into every tree, so propagation passes while the copies drift. cand-speaking-root levels at entry, so a change made mid-walk in one record does not reach another's already-open tree. cand-fixed-root and cand-os-rooted fan out, and both records say nothing signals which copy answered. cand-judged-path keeps exactly one copy in trunk and reads it at the moment it is needed, so there is nothing to propagate and nothing to drift.

THREE DEMANDS CANNOT BE ADJUDICATED FROM ANY CANDIDATE RECORD, and that is a finding rather than a blank. The read-back proof binds how the build is TESTED, not how it is designed. Version control and the person-facing surfaces are simply absent from all four records. All three bind M7 and none of them discriminated here.

TWO PASSES ARE VACUOUS AND MARKED AS SUCH. cand-judged-path meets both worktree-levelling demands by having nothing to level. That is a genuine answer to the outcome and a hollow one to the letter, and it is why both demands are named above as wanting restatement.

THE FIVE PASSES OF THIS MILESTONE, because the sequence is the finding.

FIRST, twelve axes with cand-judged-path drawn incomplete: it came third of four. Front of four.
SECOND, drawn complete, the measurement read for direction: a dead tie at the top. Front of three.
THIRD, thirteen axes with the owner's failure-mode criterion: it dominates two rivals. Front of two.
FOURTH, twelve axes over the reworked register, two columns rescored blind: it dominates all three. Front of ONE.

EVERY MOVE CAME FROM AN INPUT CORRECTION, never from a change of opinion. A candidate given the cell the chart had denied it. A measurement read for its direction rather than its existence. A criterion added because the register was blind to failure mode. Two rows restated from mechanism to outcome and rescored by somebody who had not seen the earlier answers.

THE FIRST PASS HAD THE WINNER LAST BUT ONE. That is the honest measure of what a malformed chart cost, and it is why note-3b6264ca20c9 is a must.

WHY raid_additions IS `none`, unchanged and still correct. A gate's tools are read-only and a write refuses under SE-C-110 - a gate that could mint into the register could manufacture its own evidence. The four overrides above are owed as register entries by the next state that can write one.

## scores

| candidate | axis | score | anchor | prior_art |
| --- | --- | --- | --- | --- |
| cand-speaking-root | req-a-wrong-act-never-passes-silently | 3 | solid baseline - confinement, a named-form refusal for the rare reach, and the envelope field; the unbypassable-seam assumption is recorded with the i8 bypass standing against it | |
| cand-speaking-root | req-trace-source-never-mixes | 3 | solid baseline - unqualified paths reach the bound tree only, and every answer names the tree it used | |
| cand-speaking-root | req-engine-folder-is-sealed | 2 | partial - confinement covers writes into the folder, but resolve-from-own-folder rests on raid-asm, which its own lean calls unprobeable from here | |
| cand-speaking-root | req-open-notes-stay-visible | 3 | solid baseline - one engine and one inbox, untouched by the change | |
| cand-speaking-root | req-panel-shows-the-machine | 3 | solid baseline - one engine and one mirror, untouched | |
| cand-speaking-root | req-overlay-resolution | 3 | solid baseline - reconcile at entry and reload serves the overlay card at next load; the walked-for-hours gap is recorded | |
| cand-speaking-root | req-trace-view-derived-from-files | 3 | solid baseline - the claim file answers where a record lives from the repository rather than local disk | |
| cand-speaking-root | req-walk-survives-host-swap | 3 | solid baseline - one resolver inside the engine, nothing host-specific added | |
| cand-speaking-root | req-fresh-machine-runs | 3 | solid baseline - no new runtime component; smallest build on the chart | |
| cand-speaking-root | req-call-answers-in-one-second | 3 | solid baseline - 0.46 ms disk reads and a tens-of-bytes envelope field; the entry levelling pass over 27 trees is unmeasured | |
| cand-speaking-root | req-archive-releases-worktrees | 3 | solid baseline - the record's tree is removed at archive and its branch keeps the record retrievable, so nothing of it survives on disk | |
| cand-speaking-root | req-parallel-iterations-own-worktrees | 2 | partial - unlanded work stays in each record's own tree so nothing overwrites, but the engine's root is bound to one record at a time, so a second open record waits for the rebind; the row's own breaks_if_removed names exactly that queuing | |
| cand-fixed-root | req-a-wrong-act-never-passes-silently | 3 | solid baseline - the tree is a required argument, so an ambiguous path is unrepresentable and refused by the existing schema check; the only-a-check lean is recorded | |
| cand-fixed-root | req-trace-source-never-mixes | 2 | partial - the tree is stated per call, but the fan-out leaves every tree with a copy and its own lean says nothing signals which copy answered | |
| cand-fixed-root | req-engine-folder-is-sealed | 3 | solid baseline - the root never moves, so the engine folder sits at one path for the whole session | |
| cand-fixed-root | req-open-notes-stay-visible | 3 | solid baseline - one engine and one inbox, at a fixed path all session | |
| cand-fixed-root | req-panel-shows-the-machine | 3 | solid baseline - one engine and one mirror, untouched | |
| cand-fixed-root | req-overlay-resolution | 2 | partial - a tree behind trunk serves the engine's card instead of the builder's, and its own lean says nothing signals that it did | |
| cand-fixed-root | req-trace-view-derived-from-files | 3 | solid baseline - the claim file makes the tree argument checkable from the repository rather than from disk | |
| cand-fixed-root | req-walk-survives-host-swap | 3 | solid baseline - nothing depends on what the host bound; a call means one thing all session | |
| cand-fixed-root | req-fresh-machine-runs | 3 | solid baseline - no new runtime component; the widest-migration cost is build, not install | |
| cand-fixed-root | req-call-answers-in-one-second | 3 | solid baseline - 0.46 ms disk reads; the fan-out is N writes and stays far inside the bound | |
| cand-fixed-root | req-archive-releases-worktrees | 3 | solid baseline - the fanned-out copy of method lives inside the removed tree, so the archive leaves nothing of the record on disk | |
| cand-fixed-root | req-parallel-iterations-own-worktrees | 3 | solid baseline - the tree is a call argument, so two open records are addressed at once with no ambiguity and neither waits on the other | |
| cand-judged-path | req-a-wrong-act-never-passes-silently | 3 | solid baseline - four layers, and the thin tree makes method-overwrite a filesystem fact rather than a rule; the predicate refuses, the read-back proves the landing, reads name their tree, and the two-rules-drift hole is recorded | |
| cand-judged-path | req-trace-source-never-mixes | 3 | solid baseline - the thin tree holds the record alone, so trunk and record content cannot be confused; reads stay permissive and carry their source | |
| cand-judged-path | req-engine-folder-is-sealed | 3 | solid baseline - the write predicate covers every verb including the shell, and no shared file sits in the tree to overwrite | |
| cand-judged-path | req-open-notes-stay-visible | 3 | solid baseline - one engine and one inbox, and the root never moves | |
| cand-judged-path | req-panel-shows-the-machine | 3 | solid baseline - the panel still draws from node files; a whole-corpus draw now pays 2.04 ms per shared read | |
| cand-judged-path | req-overlay-resolution | 3 | solid baseline - shared method has exactly one copy, read from trunk at the moment it is needed, so no overlay card can go stale | |
| cand-judged-path | req-trace-view-derived-from-files | 3 | solid baseline - the claim file answers a record's tree on a peer machine with no local worktree | |
| cand-judged-path | req-walk-survives-host-swap | 3 | solid baseline - one predicate inside the engine, and the batch reader needs only git, already required | |
| cand-judged-path | req-fresh-machine-runs | 3 | solid baseline - nothing new to install; the thin worktree is unbuilt machinery inside the published entry | |
| cand-judged-path | req-call-answers-in-one-second | 3 | solid baseline - 2.04 ms per shared read plus one read per write, inside the bound; the under-load hole is recorded | |
| cand-judged-path | req-archive-releases-worktrees | 3 | solid baseline - the thin tree holds the record alone, so removing it removes the only working copy and the branch answers for the record afterwards | |
| cand-judged-path | req-parallel-iterations-own-worktrees | 3 | solid baseline - the root never moves and each tree holds only its own record, so two open records address at once and neither can write into the other | |
| cand-os-rooted | req-a-wrong-act-never-passes-silently | 2 | partial - the OS prevents the default and closes the shell hole by inheritance, but nothing refuses an escaping relative path; it completes and reports success with the tree merely named | |
| cand-os-rooted | req-trace-source-never-mixes | 3 | solid baseline - per-process rooting separates the trees and every answer names the one it used | |
| cand-os-rooted | req-engine-folder-is-sealed | 2 | partial - the fan-out writes method into every tree, and its own text says the self-hosting exception is multiplied rather than answered | |
| cand-os-rooted | req-open-notes-stay-visible | 1 | gesture - the note inbox is named as broken by several processes, with two options sketched and neither chosen | |
| cand-os-rooted | req-panel-shows-the-machine | 2 | partial - the mirror is named as the natural owner, but the panel must now draw several processes and nothing says how | |
| cand-os-rooted | req-overlay-resolution | 2 | partial - the fan-out plus one loaded copy per process, and which process sees an overlay edit when is undesigned | |
| cand-os-rooted | req-trace-view-derived-from-files | 3 | solid baseline - views still derive from node files and the claim file carries the tree | |
| cand-os-rooted | req-walk-survives-host-swap | 2 | partial - process supervision and a local channel for shared state are new host-facing machinery | |
| cand-os-rooted | req-fresh-machine-runs | 2 | partial - a fresh machine must bring up N processes and a shared-state owner, neither of them designed | |
| cand-os-rooted | req-call-answers-in-one-second | 1 | gesture - the lean says nobody has measured it; the probe then measured 67 ms cold per process with engine load excluded, and ~970 ms of startup floor for 27 records against a 1-second bound | |
| cand-os-rooted | req-archive-releases-worktrees | 2 | partial - a live process holds the record's tree open, so archive must reap it first, and its own text leaves that ordering against the strays commit unaddressed | |
| cand-os-rooted | req-parallel-iterations-own-worktrees | 4 | prior-art par - one process per record with its own root serves both at once, and the isolation is the platform's own rather than a reimplementation | the OS process working directory, inherited by child processes |

## front

- [[cand-judged-path]]
- [[cand-os-rooted]]

## reading

TWO ELIMINATIONS, AND I ACCEPT BOTH. They are domination rather than margin, and neither changed when the axis set did.

cand-speaking-root is beaten by cand-judged-path on every axis and strictly on two now. On req-engine-folder-is-sealed its confinement covers writes into the folder, while resolving the engine's own resources from that folder rests on raid-asm-engine-serves-from-the-bound-tree, which its own record says cannot be probed from this repository. On req-parallel-iterations-own-worktrees the restated row asks that no record be put in the way of another, and a root bound to one record at a time puts the second in a queue.

cand-fixed-root is beaten everywhere and strictly on two, both the same cause: the fan-out leaves a copy in every tree and its own text records that nothing signals which copy answered. That is the failure this iteration exists to end, appearing inside a candidate for ending it.

THE RESTATED ROW COST THE CANDIDATE THIS RECORD WAS ARGUING FOR, which is worth saying plainly. cand-speaking-root is the shape the record set out to build. Restating a row as an outcome took a point off it and off nothing else.

IT CHANGED NO OUTCOME EITHER. cand-speaking-root was already eliminated by domination on the engine-folder cell alone, so the move costs it nothing it still had.

NO AXIS IS FLAT ACROSS ALL FOUR, so nothing says a criterion is missing.

THE FRONT IS TWO AND THEY ARE NOT CLOSE ON TOTALS - 36 against 26. cand-os-rooted survives on ONE CELL, and it is the only prior-art par on the board. That is exactly what a Pareto front is for: a sum would have dropped it ten points ago, and the one thing it does better than anything else would have gone with it.

THE FRONT SITS ONE STEP FROM UTOPIA ALMOST EVERYWHERE. Utopia across the front is 4 on parallel records and 3 elsewhere. Only cand-os-rooted reached the 4.

NO WINNER IS PICKED HERE. That is M5's.
