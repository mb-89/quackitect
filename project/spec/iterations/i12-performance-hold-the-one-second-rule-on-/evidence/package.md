---
form: package
by: agent
signed_off: 2026-08-15T13:13:59.359Z
authors: agent
files:
---

# Evidence form / package

## current_situation

THE PACKAGE BUILDS BY SCRIPT AND THE SCRIPT IS THE ONLY WAY IT IS BUILT. node project/deliverable/engine/bin/package.ts, exit 0, 14724 ms, and it prints the path it wrote.

Version 4.1.0. The version is read from one source rather than typed here.

## package

- dist/quackitect-4.1.0.zip

## works

yes — and the honest scope of that word is worth stating. The archive assembles, carries its README, its one-time installer and the editor extension, and the script that built it exits clean. What was NOT done is a fresh-machine install from this exact archive; that is tsp-first-run, and it is owed against raid-issue-must-demos-owed like every other demonstration this record could not watch. The claim here is that the package builds and is well-formed, not that a newcomer installed it today.

## emit_back

- meth-verification-discipline: a spec already owed against an OPEN debt entry should arrive pre-filled as owed, not blank. Every record re-adjudicates the same five demonstrations, and raid-iss-whole-product-claims-reverified-by-every-record names this and nothing acts on it.
- The gate-implementation row: a FAILED gate has nowhere to go. Its only successor is fill-story-evidence, so an owner's fail cannot move the walk and the agent must route around it by hand.
- SE-C-123's refusal text: it blames the drawing for an AND-join when the drawing may be correct. It sent a walk hunting a defect that did not exist; a fallback edge simply does not fire from a state that passed.
- se_file_replace: a landing inside a fenced block or a quoted measurement should refuse by default and be named to proceed. A rename changes identifiers, never quotations.
- The lane's captured run output: redact the machine root at the source. A fatal privacy requirement whose only guard is a reader recurred one record after it was fixed, because quoting evidence verbatim is otherwise correct.
- meth-consistency-sweep: nine surface classes and no way to record that a class was walked and found nothing. A checked box cannot distinguish swept-and-clean from swept-and-fixed.

## follow_up

- GATE-RELEASE IS NEXT, and it is the last state of this record.
- THE NEXT ITERATION IS SEEDED AT THE RETRO, on the owner's word: get the calls under a second, and where that is genuinely impossible give the person quick feedback instead. note-5e2d3cae20e0 holds the measurement, the cause and the first step.
- THE DAG IS THE INSTRUMENT for the repetition, and downstreamCone already exists in the engine for the invalidation half.
- THE FIRST MOVE IS FOUR LINES: split the machine phase into its four parts, and let the shape of the answer decide whether the DAG or a targeted fix is right.

## anything_else

ON WHAT THIS RECORD ACTUALLY DELIVERED, since the goal it was named for is not met.

THE INSTRUMENT WORKS AND RECORDS WHERE THE LANE CAN READ. That is the whole first half of MEASURE FIRST, FIX SECOND, and before this record a bound walk wrote 1301 timing rows per run into a directory nothing opens.

THE RANKING IS NOW TAKEN FROM LIVE DATA. 2311 calls over a second, 422 over ten, and a phase breakdown naming the four seconds. Every earlier attempt to rank this work used figures from memory.

AND THE ANSWER SURPRISED THE PLAN. The cost is not the compile, not the SVG, and not the corpus. It is one derivation repeated per state per render, which no candidate fix in the record's own vision would have touched.

THAT IS THE ARGUMENT FOR THE SEQUENCE. A record that had gone straight to FIX SECOND would have cached the comparison walk, preloaded the forms and moved the render to Cytoscape — three real pieces of work, none of which would have moved the number.
