---
form: decompose-structure
by: agent
signed_off: 2026-08-15T10:56:37.737Z
authors: agent
files:
---

# Evidence form / decompose-structure

## current_situation

The structure does not move. This is the one edit legal at this size: new requirements land on existing functions, and those functions are already allocated to existing elements.

No element was added. No interface was added. Neither was wanted, which matters, because wanting either is the tell that the change is a major wearing a minor's clothes.

The matrix computes clean: nothing unimplemented, nothing idle, nothing undemanded, no missing interface on any crossing.

## elements

- el-account
- el-bootstrap
- el-claim-ledger
- el-front-desk
- el-holding-pen
- el-method-compiler
- el-mirror
- el-record-store
- el-test-runner
- el-walk-engine

## allocation

THE MATRIX IS COMPUTED AND IT IS CLEAN. What follows is the spread this delta argues, which the computation cannot judge.

THE FIVE NEW REQUIREMENTS REACH THE STRUCTURE TRANSITIVELY, on the ordinary path: requirement, then function, then element. None needed a direct `satisfies` on an element, which is the path reserved for a structural quality the shape answers.

- req-scoped-run-records-its-timings reaches el-test-runner, through answer-with-tests.
- req-surface-answers-in-one-second reaches el-mirror, through show-where-it-stands.
- req-survey-counts-only-open-records reaches el-mirror, through the same function.
- req-answer-pages-never-overflows reaches el-walk-engine, through serve-a-step.
- req-container-offers-its-records reaches el-walk-engine, through serve-a-step.

THE SPREAD IS THREE ELEMENTS FOR FIVE DEMANDS, and that is information rather than an accident. This delta is one demand, answer inside a bound, arriving at three different boundaries: a test run, a person's look, and an instruction handed to a driver. Each boundary is measured differently, which is why the requirements split and why the elements do.

ONE ELEMENT CARRIES TWO OF THE FIVE. el-mirror answers both the render and the survey, and both failures show as the same symptom to a person: a look that costs more than it should, or reports more than stands.

## follow_up

- The structure standing unchanged is the evidence that minor was the right column. The kill-criterion named at the kickoff was a drawing change, and this is the second check that found none.
- flow-test-timings, added at derive-functions, produces no boundary crossing in the computed matrix. Its consumers are a later run of the same element and the retro's reading, so no interface is owed. That is recorded rather than assumed, because an output nobody consumes is a finding and this one has to be argued rather than counted.
- Nothing here escalates. Wanting a new element or a new interface would, and neither was wanted.

## anything_else

ON A MILESTONE WHOSE CORRECT OUTPUT IS NO CHANGE.

A decomposition state that adds nothing looks like a state that was skipped. It is worth saying what was actually done, because the difference is invisible in the artifact.

The computed matrix was read rather than trusted. Ten elements, seventeen boundary crossings, every one already carrying its interface. Four lists were checked and all four are empty: unimplemented, idle, undemanded, problems.

The one judgment this state owed was where flow-test-timings lands. It is a new output, and a new output is exactly the thing that creates an owed interface. The computation says no crossing, and the reason is that its consumers sit inside the element that produces it.

HAD THAT GONE THE OTHER WAY, this record would have escalated. An owed interface between el-test-runner and el-account is the architecture moving, and the state's guidance says so plainly. It did not go the other way, and the check is recorded so nobody has to redo it to find that out.
