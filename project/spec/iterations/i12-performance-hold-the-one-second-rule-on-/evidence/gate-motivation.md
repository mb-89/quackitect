---
form: gate-motivation
bless: blessed by agent
by: agent
signed_off: 2026-08-15T10:25:07.275Z
authors: agent
files:
---

# Evidence form / gate-motivation

## current_situation

M1 is walked. The vision is inherited with its goal conflicts ruled, the register is open with five entries, the delta extends one standing value prop, and scope and non-goals are signed.

THE MILESTONE TURNED UP TWO THINGS NOBODY WAS LOOKING FOR, and both change how this record proceeds.

First, a scoped run records no timings. The record's whole ranking of what to speed up comes from battery numbers carrying twenty-way contention, and nothing could separate work from queueing.

Second, entering a record levels its tree's method and engine with trunk and leaves the spec corpus at the seed. Two trace tests failed inside this record that pass on trunk. A sync of sixty-nine commits cleared them.

The second one nearly cost the iteration its central measurement. A baseline taken before that sync would have been compared against a battery figure from a different tree.

## vision_scope_stated

COMPLETE, and complete at the size the column compiled.

The minor column drops the big idea, the to-be world and the pitch mechanically. A minor cannot move the product's identity, so those three are inherited from the resident vision rather than answered briefly.

What the column DOES ask is answered in full.

- The goal system, with three conflicts named and each one ruled, in draft-vision.
- The delta's new need with its pass lines, in frame-delta, landed on vp-rigor-without-toil rather than in the form.
- Scope and non-goals, in scope-non-goals, with six exclusions each carrying where it went.
- The register, open with five entries, in log-risks.

The gap claim and the why-now are not asked at this size and are not missing. They stand from the resident frame.

## problem_agreed

THE DELTA IS REAL, and it was measured during this walk rather than argued.

Nineteen mirror_slow records landed in a single window. /widget/details broke the one-second rule on all seven of its requests, between 2720 ms and 3468 ms. /widget/machine answered in 3966 ms. The root page answered in 4026 ms.

Those are not edge cases. They are the surfaces a person looks at while deciding, and every one of them is over the standing requirement by a factor of three.

THE GOAL IS WORTH HAVING, and the argument is the owner's own. Every later iteration runs the battery many times, so a saving there is collected on every run rather than once.

THE ARGUMENT AGAINST ITSELF, because a gate that only argues for is worth nothing. The battery already runs in 77 seconds of wall clock, which is not painful. If the only prize were the battery, this would be a small win dressed up.

The prize is not only the battery. It is that a person watching a surface think for four seconds is being taxed on every decision, and that tax is invisible because nobody records it as cost.

## prior_art_positioned

AGAINST WHAT EXISTS, and the honest comparison is unflattering in one place.

Node's own test runner already emits per-case durations in the TAP stream. The proof is in this walk: a failing case came back carrying `duration_ms: 454.6437` from a SCOPED run, which is the very run that records nothing.

So the data was never missing. The engine parses that TAP for pass and fail counts and drops the durations on the floor. Other runners in daily use, Jest and Vitest and go test among them, surface per-test timings in every mode rather than only in a full run.

That is prior art we are behind, and the gap is not a design tradeoff. It is a field nobody read.

WHAT OURS SHEDS, and this part is genuinely ahead. A run here is a durable job carrying the QUESTION it answers, and its verdict is bound to the tree it judged, so an identical tree is answered from the record instead of re-run. None of those three runners tie a verdict to a tree hash or refuse a run that would prove nothing.

WHAT FAILED BEFORE, from this project's own history rather than from outside. v1 faced the same battery-length problem and rejected thinning the test set, choosing a bounded worker pool inside the guarded write path. That rejection is why this record's non-goals forbid the same shortcut.

## success_measurable

EVERY NEED CARRIES ITS PASS LINE, and they live on the artifact rather than in this form.

vp-rigor-without-toil now carries the delta's criterion with two metrics and their targets.

- The share of surface requests answered inside a second. Target: all of them.
- An answer a host must move to disk before the agent can read it. Target: none.

BOTH ARE MEASURABLE TODAY FROM DATA THE SYSTEM ALREADY WRITES. The first reads out of the mirror_slow records. The second reads out of the pull's own bounded answers.

BOTH ARE CURRENTLY FAILING, and that is stated rather than softened. The first was breached nineteen times in one window during this walk. The second was breached by this walk's own kickoff answer at 259015 bytes.

NEITHER HAS A TEST. The record names the missing one by the owner's own request: a test that fails when a render passes a second. The existing guard covers lane calls only, which is how both surfaces got past it.

## risks_logged

THE REGISTER IS OPEN WITH SIX ENTRIES, each carrying an owner and a trigger.

- raid-dec-speed-never-buys-from-the-guard, a decision, carrying all three ruled goal conflicts with their rejected options.
- raid-asm-battery-timings-measure-work, an assumption, on whether a battery case's duration measures its own work.
- raid-asm-wall-clock-is-a-baseline, an assumption, on whether the total repeats between runs of one tree.
- raid-risk-git-fake-drifts-from-git, a risk, taken from the record's own design of the fix.
- raid-asm-method-write-reaches-every-tree, an assumption, probed for free by the merge at close.
- raid-iss-record-entry-levels-method-not-spec, an issue, opened by this milestone and already costing six calls.

EVERY OWNER IS A ROLE. Four sit with the driving agent, one with the adjudicator, and none names a person.

EVERY TRIGGER IS AN EVENT RATHER THAN A DATE, so the register stays live instead of becoming a graveyard.

## round_0_verify

- evidence vs claims: green. Every number in this gate was opened rather than trusted. The slow surfaces come from this session's own mirror_slow records, the battery figures from .se/test-last-run.json, the missing reporter from engine/tools.ts line 953, and the levelling boundary from the file lists of commits 9654e1e1 and bd2853fa.
- types: green. npx tsc --noEmit exited 0 in 2370 ms, run AFTER the sixty-nine-commit merge rather than before it.
- lint: green. se_lint reports 0 findings on every node this milestone wrote. Two findings on my own entries were caught and fixed before this gate, and 29 standing findings on OTHER people's register entries were left alone and noted for the overhaul.
- tests: green. trace-coverage, trace and traceschema ran 56 of 56 after the sync. The same three files ran 54 of 56 before it, and that red is now a recorded issue rather than an open question.

## round_1_validate

- exercised against the goal: yes, and the milestone moved the goal's ORDER rather than the goal. The measurement instrument is repaired before anything it would justify, which is the record's own measure-first instruction followed rather than overridden.
- missing: the field-feedback answer, owed since the onboarding retro because the owner left mid-session having authorised the walk to continue. Nothing else the milestone needs is absent.
- wrong: one thing, and it was corrected inside the milestone. The first diagnosis of the trace red called this worktree three days behind trunk, which was too coarse. The levelling had already brought method and engine current, and only the spec corpus was stale.
- out of scope: six items, each with its home named in scope-non-goals. Screen work goes to i23, the ticket desk wants its own walk, preloading waits on measurement, two i27 promotions are not performance work, and i8 must not be entered from this machine.
- prior art: Node's own runner already emits per-case durations on a scoped run, and the engine drops them. Jest, Vitest and go test all surface per-test timings in every mode. We are behind there, and it is a field nobody read rather than a tradeoff. Ours ties a verdict to the tree it judged and refuses a run that would prove nothing, which none of those three do.

## round_2_red_team

- STEELMAN: the whole measurement detour is procedural throat-clearing on a battery that already finishes in 77 seconds, and a record that spends its first milestone repairing a reporter is avoiding the work => The steelman is right about the battery and wrong about the stakes. The detour already paid for itself once: the trace red would have made any pre-sync baseline a comparison between two different trees, and that error would have survived into the record's conclusions.
- The two reds inside the record were caused by the nodes this walk wrote, and the sync was a cover story => Refuted on the mechanism. Both failing assertions ask whether a verb is NAMED in the trace, and this walk only ADDED trace files. Adding text cannot make a name disappear. Trunk's trace named those verbs forty times across seventeen files and the record's named them twice.
- Syncing sixty-nine commits mid-record is a large uncontrolled change to a tree the record is about to measure => True, and taken deliberately with the alternative named. The alternative was measuring against a tree matching neither trunk nor the seed. The merge is one revertable commit and the three trace files went 54 of 56 to 56 of 56 across it.
- KILL-CRITERION for this gate: the delta is not real if the slow surfaces were a one-off, for instance a cold cache on the session's first minute => Looked for and not found. The nineteen mirror_slow records span twelve minutes rather than one, and /widget/details broke the second on all SEVEN of its requests rather than on the first. A cold cache does not repeat seven times.
- The value prop was extended by the same walk that now cites it as evidence the goal matters => Named rather than hidden. The prop's new criterion is not evidence FOR the delta; the mirror_slow records are. The criterion is what makes the delta checkable afterwards, and it was written to a standing artifact so a later record can argue with it.

## raid_additions

- raid-iss-record-entry-levels-method-not-spec

## verdict

pass — the delta is measured rather than asserted, with nineteen breaches of the one-second rule recorded in this session's own log. The vision packet is complete at the compiled size, the register carries six entries with roles and triggers, and the pass lines live on a standing artifact where a later record can argue with them. All four round-0 checks are green on the merged tree. ONE DISSENT IS LOGGED: this gate was blessed by the agent rather than the owner, who is away and authorised the walk to continue, and the field-feedback answer stays owed.

## follow_up

- The build work begins with the scoped-run timings fix, and it is smaller than the kickoff assumed. Node's TAP stream already carries duration_ms per case, so the engine may only need to read what it is already being handed.
- Then the baseline: claims.test.ts measured alone on the POST-SYNC tree, compared against the battery's 75218 ms. Any pre-sync measurement is void.
- raid-iss-record-entry-levels-method-not-spec wants one of its two remedies, and the smaller one carries the hazard i27 was built to stop.
- note-70c755925b31 parks the register's 29 standing lint findings for the overhaul rather than this record.
- Owed to the owner: the field-feedback answer.

## anything_else

ON THE ONE THING THIS MILESTONE GOT WRONG AND CORRECTED.

The first reading of the trace red blamed the worktree for being three days behind trunk. That was wrong in a way worth recording, because the correction is the actual finding.

The levelling had already brought this tree's method and engine current with trunk. Two commits did it, carrying 78 and 56 files. Read their file lists and neither touches anything under project/spec.

So the tree was not uniformly stale. It was stale in exactly one layer, and that layer is the one the newest tests check.

A uniformly old tree would have been green, because old tests would have checked an old corpus. The mixture is what broke, and a mixture is harder to notice than an old tree.
