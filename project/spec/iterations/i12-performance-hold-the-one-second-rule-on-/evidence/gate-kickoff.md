---
form: gate-kickoff
bless: blessed by agent
by: agent
signed_off: 2026-08-15T10:11:02.486Z
authors: agent
files:
---

# Evidence form / gate-kickoff

## current_situation

M0's onboarding retro is signed. The inbox stands at zero.

i12 is the fourth of the owner's four enablers and the last one needing nothing from them. The run order set on 2026-08-13 is i27, i28, i11, i12, then i9.

The owner's argument for i12, in the plan's own words: it halves the battery's wall clock, which every later iteration pays on every run.

THE RETRO CHANGED WHAT THIS ITERATION MUST DO FIRST.

The record's standing instruction is MEASURE FIRST, FIX SECOND. That instruction cannot be followed today. A scoped run records no timings, so a file can only be measured inside the battery, where about twenty files run at once and every duration carries the others' contention.

The ranking that decides what to fix is therefore built on numbers nobody can separate into real cost and queueing. The measurement instrument is the first thing this iteration repairs.

## retro_drained

- inbox: zero. Both notes captured during this walk were drained in the onboarding retro, and nothing else pended.
- note-86bbee3a08ab: carried into this iteration's scope. A bare pull walks into the first started record, and from inside it no sibling record is reachable.
- note-69f1c756aeb2: carried into this iteration's scope. se_survey counts a shipped record among the open ones.

## goal

Performance: hold the one-second rule on the surfaces that break it, paginate the pull instead of overflowing it, and kill the cubic comparison walk.

## pulled_in

- The scoped run records no timings. Found this walk, proved at engine/tools.ts line 953, and it is a prerequisite for the record's own measure-first instruction.
- The battery's tallest pole. One claims.test.ts case cost 75218 ms of a 76985 ms wall, and note-c25579614b03 carries the designed fake of its single git seam.
- Opening the state machine past a second. Measured this session at 3966 ms on /widget/machine.
- Opening an evidence form past a second. Measured this session between 2720 and 3468 ms on /widget/details, over seven of seven requests.
- The pull paginates rather than overflowing. Half already stands: this walk's own kickoff answer was bounded and paged at 259015 bytes.
- The survey's window governs every list it prints.
- The cubic comparison walk, cached against a hash of the judgment list, from note-e06d7b099f89.
- The container routing trap, from note-86bbee3a08ab and the owner's ruling of 2026-08-15.
- The survey counting a shipped record as open, from note-69f1c756aeb2.
- Aiming at a record by its long id, which refuses and leaves the walk inside the wrong record, from note-b37c766b720c.
- The bless path taking longer than a second, from note-5cebd22ef8f1 and note-ff8f4378deab.
- The drift test failing only under battery load, from note-f3cd9c12d210.

## left_out

- The async round's ticket desk and its workers. The record itself says that round wants a walk that starts on it, so it is a record of its own rather than a cut from this one.
- The surface half of the one-second rule, meaning the toast and the progress bar. That is screen work and belongs with i23, which the owner runs at the screen.
- Preloading beyond what measurement justifies. The record sanctions preloading and warns in the same breath that preloading the wrong thing is work moved rather than saved.
- The rigor matrix must-check and the panel choice control. Both carried forward from i27's emit_back and both recorded in this record's promotions, but neither is performance work.
- i8. A cloud agent holds no claim on it and note-d6bdf43d9dde says plainly not to enter it from this machine.

## change_size

minor — engine internals and test machinery only, behind interfaces that already exist. Patch fails because the goal carries three demands and twelve scope items. Major fails because no part of the baseline moves. The container fix is engine code behind an unchanged drawing. Strikes: none.

## round_0_verify

- evidence vs claims: green. Every claim in this brief was opened rather than trusted. The run order reads at version-planning.md line 916, i27's shipped status at its own record, i11's owed ruling in its record text, the scoped run's missing reporter at engine/tools.ts line 953, the battery figures at .se/test-last-run.json, and the slow surfaces in this session's own mirror_slow records.
- types: green. npx tsc --noEmit against project/deliverable/tsconfig.json exited 0 in 2599 ms.
- lint: green. se_lint on the one file this milestone changed, meth-consistency-sweep.md, reported 0 findings.
- tests: green. catalogs.test.ts ran 10 of 10, answering whether rewriting that card's surfaces item broke the catalogue reader that serves it.

## round_1_validate

- exercised against the goal: yes. The goal is unchanged from the seed, so nothing was quietly narrowed to fit what was easy. What changed is the ORDER, and it changed to follow the record's own measure-first instruction rather than to override it.
- missing: the field-feedback answer. The onboarding retro asked the owner's standing question and they had already left, so it is owed on their return. Nothing else the brief needs is absent.
- wrong: one thing was wrong and is corrected here. The record ranks its work by battery numbers, and those numbers cannot be read as cost until a scoped run records timings, so the ranking is provisional and is now marked as such.
- out of scope: five things, each with its home named in left_out. The async ticket desk wants its own walk, the toast and progress bar belong to i23, preloading waits on measurement, two i27 promotions are not performance work, and i8 must not be entered from this machine.
- prior art: Shape Up sizes by APPETITE and story points size relatively against a team's history. Both beat us on one axis, because both bound or calibrate DURATION and our columns bound neither, which is exactly why no record here fits a two-hour window. What ours sheds is that both leave rigor to habit while our blessed column compiles the state machine, so a small change cannot walk a large ceremony and a large one cannot skip it.

## round_2_red_team

- Twelve pulled-in items is a bundle, and bundles are how one record quietly becomes two => Answered by order rather than by a cut. The measurement fix runs first and re-ranks the rest, so items that turn out to be contention fall out on evidence instead of on appetite.
- The container change alters how the walk moves between records, which is behaviour every future record depends on, so this reads as baseline and wants the major column => Checked and refused. iterations.canvas holds two nodes and one edge, and its own comment says the states are GENERATED AT ENTRY from the open records. The record chain is computed in the engine, so the fix sits behind an unchanged drawing.
- The kill-criterion for the minor column: if fixing the container needs a drawing edit, the graph moved and minor is wrong => Looked for and not found, on the evidence in the line above. It is recorded as an assumption because the whole column rests on it.
- The measurement detour may buy nothing: if claims.test.ts really costs 75 seconds alone, the battery ranking was sound all along => Accepted knowingly. That is a cheap way to be wrong and an expensive way to be right by accident, so the detour stands. It is minted as raid-asm-battery-timings-measure-work with its probe.
- The promotion may not have landed where it was claimed, because the write was made inside a bound worktree => Named rather than assumed. Minted as raid-asm-method-write-reaches-every-tree, with the merge at close as its probe.

## raid_additions

- raid-asm-battery-timings-measure-work
- raid-asm-method-write-reaches-every-tree

## verdict

pass — the retro is drained to zero, the goal is unchanged from the seed, the scope names what enters and what stays out with a reason for each, and the minor column carries its argument and its kill-criterion. The bless records the OWNER'S choice of column, given on 2026-08-15 as "Iteration twelve. Do it as a minor", rather than the agent's. ONE DISSENT IS LOGGED RATHER THAN SMOOTHED: twelve pulled-in items is a bundle, and it passes because the first item re-ranks the rest on measurement, not because twelve is comfortable.

## follow_up

- Attach the timings reporter to the scoped run in engine/tools.ts, with a test that fails when a scoped run leaves no timing record.
- Measure claims.test.ts alone and compare its slowest case against the battery's 75218 ms. That comparison sets the order of everything after it.
- Fix the container so a bare pull at a container's start offers a choice instead of entering the first started record.
- Give a bound record a way back to its container's start, so reaching a sibling does not cost an escape.
- Stop se_survey counting a shipped record among the open ones.
- Re-drain note-5cebd22ef8f1, note-ff8f4378deab and note-f3cd9c12d210 as this record reaches the work they wait on.
- Owed to the owner on their return: the field-feedback answer the onboarding retro asked for.

## anything_else

ON WHY THE MEASUREMENT FIX COMES BEFORE THE PERFORMANCE FIX, since the order is the only thing this gate changed about the record.

The record ranks its work by numbers from the battery. Those are the only numbers that exist, because a scoped run throws its durations away.

Under the battery about twenty files run at once. A case's recorded duration is its own work plus whatever it waited for, and nothing separates the two.

The distortion is not small. Summed case time on the last run is 1534695 ms against a wall of 76985 ms, roughly twenty to one. A case credited with 75 seconds could be doing a few seconds of work and queueing for the rest.

Fixing the instrument costs one reporter argument. Fixing the wrong test costs the iteration.

ON THE PRIOR-ART LINE, which one line could not hold.

Shape Up, from Basecamp, fixes time and lets scope vary inside it. Story points calibrate relatively against a team's own history. Both bound or improve estimates of DURATION.

Ours does neither, and today showed the cost. The owner asked for something doable in two hours and no open record could answer, because every record here is sized to about a day while the column sets rigor rather than length.

The gain is real all the same. Both of those size effort and then leave rigor to habit, where the blessed column here COMPILES the machine that follows.

The column names are borrowed from semantic versioning, where they classify API compatibility. Ours classify process rigor, so the word is shared and the meaning is not.
