---
form: speaking-root
by: agent
signed_off: 2026-08-14T07:17:22.110Z
authors: agent
files:
---

# Evidence form / speaking-root

## current_situation

cand-speaking-root is composed. Its three sections are written into the candidate note itself, and nothing new was minted.

THE CHOSEN OPTIONS WERE ALREADY ON THE CHART. This state's job is the SEAMS between them, which is where a combination stops being a list and becomes an architecture.

THE SEAM THAT DECIDED THE SHAPE is the retro's. Total confinement bars the one state whose job is method, so the reach became a property of the STATE rather than of the walk - and that column sits beside legal_tools, which already refuses when it is exceeded.

NOTHING HERE IS SCORED. The costs are rough and proportional; judging is evaluate-set's.

## built

project/spec/trace/candidate/cand-speaking-root.md - three sections appended: How it works, What it costs, What it leans on.

No node was minted. The candidate note the chart line created is the only artifact, per the owner ruling of 2026-08-08 that a composed record beside the candidate is a second copy of one thing.

## follow_up

THE THIRD ASSUMPTION IS THE ONE TO CARRY. This candidate leans on the per-state reach column being enough for every state that writes method. note-430f3af449b8 names the bound retro; nobody has walked the state list to see whether it is alone.

That is a cheap check and it is not this state's job. It belongs at the architecture milestone, before this candidate is weighed against the others, because if several states need the reach the column stops being a small addition.

THE FIRST ASSUMPTION IS ALREADY IN THE REGISTER and stays there. raid-asm-engine-serves-from-the-bound-tree cannot be probed from this repository, and this candidate rests on it more heavily than the other three do.

## anything_else

WHAT THIS COMPOSITION CHANGED ABOUT THE CANDIDATE.

Drawn on the chart it was six picks. Written out, one of the picks turned out to need a mechanism that was not on the chart at all: confinement plus a per-state exemption is not the same thing as opt-confine-the-root-to-the-bound-tree alone, and opt-grant-the-method-reach-per-state is the option that supplies it.

That option exists - find_contradiction minted it - and this line does not visit it, because the chart's rows put it under how shared method reaches a tree while its real job here is qualifying the first row.

RECORDED RATHER THAN REDRAWN. Moving it would change the chart after the fact, and the honest place to raise it is the architecture milestone.
