---
form: fixed-root
by: agent
signed_off: 2026-08-14T07:18:36.880Z
authors: agent
files: null
---

# Evidence form / fixed-root

## current_situation

cand-fixed-root is composed. Three sections written into the candidate note, nothing minted.

THE SEAM THAT DECIDED THE SHAPE is the refusal. Under a fixed root there is no structural barrier at all, so every other candidate's belt-and-braces becomes one check standing alone.

That is the honest reading of this line, and it is what makes it interesting rather than weak: it is the only candidate whose safety is entirely visible in one place.

## built

project/spec/trace/candidate/cand-fixed-root.md - three sections appended: How it works, What it costs, What it leans on.

No node minted.

## follow_up

THE SECOND ASSUMPTION DECIDES THIS LINE. It leans on ambiguity being decidable, and nobody has enumerated the cases. raid-risk-a-write-lands-in-the-wrong-tree-silently names four path kinds and says the bug lives at the seams between them, which is the same enumeration this candidate needs and does not have.

If the cases cannot be enumerated the refusal cannot fire reliably, and then this candidate has no guard at all. That is cheap to check and it belongs before the weighing.

opt-resolution-varies-with-the-path-kind IS PROBABLY ITS MISSING PIECE. It is on the chart, this line does not visit it, and it supplies exactly the enumeration the refusal needs.

## anything_else

THE STALE-METHOD FAILURE IS THIS CANDIDATE'S ALONE, and writing it out is what surfaced it.

Under a fixed root the fan-out is what makes shared method reachable. A tree that has not caught up answers from an older copy, and nothing in this line says which copy answered - because the whole point of the fixed root is that a path means one thing regardless of what is bound.

So the property that makes this candidate clear is the same property that hides a stale read. cand-speaking-root does not have that problem, because its envelope field names the tree on every answer.

A genuine trade rather than a defect, and the kind of thing this state exists to find.
