---
form: os-rooted
by: agent
signed_off: 2026-08-14T07:20:16.513Z
authors: agent
files: null
---

# Evidence form / os-rooted

## current_situation

cand-os-rooted is composed. Three sections written into the candidate note, nothing minted.

THE SEAM THAT DECIDED THE SHAPE is the shared state. This is the only candidate that cannot be described without answering a question nobody has asked: the mirror, the claim ledger, the note inbox and the call log are machine-wide and single, and several engine processes cannot each own them.

THE RESOLUTION ITSELF IS FREE. It is the only line on the chart whose core mechanism was written and exercised by somebody else, and the only one that adds no engine code to the thing this record is actually about.

## built

project/spec/trace/candidate/cand-os-rooted.md - three sections appended: How it works, What it costs, What it leans on.

No node minted.

## follow_up

THE SHARED-STATE QUESTION IS OWED BEFORE THIS LINE CAN BE WEIGHED. Two shapes are visible - one owner process serving the rest over a local channel, or the state moving to disk with a lock - and neither is designed. Without an answer the cost column is a guess, and a guess is what the evaluation would score.

It is also the largest thing on this chart that is NOT about path resolution, which is worth saying plainly: this candidate answers the record's question cheaply and opens a different one.

NOBODY HAS MEASURED WHAT N ENGINE PROCESSES COST. Twenty-seven worktrees stood on this machine on 2026-08-13, and req-call-answers-in-one-second applies to every process. That is a probe M6 could run cheaply, and it should be named at the architecture milestone rather than discovered later.

## anything_else

THE FOUR ARE NOW COMPOSED AND THEY DISAGREE HONESTLY.

Writing them out moved three of them, and none of the moves came from argument.

- cand-speaking-root needed a mechanism its own line does not visit, because total confinement bars the retro.
- cand-fixed-root turned out to have no structural guard at all, which is a real property rather than a flaw.
- cand-judged-path looked cheapest and is not, because a decision it leans on is unbuilt.
- cand-os-rooted costs nothing where the record is looking and opens a question somewhere else.

EACH OF THOSE CAME FROM WRITING THE SEAMS. That is the argument for this state existing separately from the chart, and it is worth recording once because the state's own value is easy to mistake for paperwork.
