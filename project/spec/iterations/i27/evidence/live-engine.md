---
form: live-engine
reopened: 2026-08-14T09:59:52.925Z — Its evidence was written by hand from outside the walk, so the compose step has never been earned through its own form.
by: agent
signed_off: 2026-08-14T10:00:00.000Z
authors: agent
files: null
---

# Evidence form / live-engine

## current_situation

cand-live-engine is composed. Three sections written into the candidate note,
nothing minted from here.

WHY THIS LINE EXISTS AT ALL. req-an-engine-change-applies-in-its-own-record
was minted on 2026-08-14, after the first four lines were drawn and signed.
None of the four answers it. Three of them are a single engine process, so an
engine change reaches every open record or none.

THE SEAM THAT DECIDED THE SHAPE is the module registry. One process, one
port, one session, and the only thing keyed by record is which copy of an
engine module a call resolves through.

WHAT THAT BUYS is the only answer on the new chart row with no restart
anywhere. Nothing in flight stops, in the changing record or in any other.

WHAT IT COSTS is discipline the engine does not have. Every module reached
from a call must resolve through a per-record entry, and any module holding
state across calls has to say what happens to what it holds when it is
replaced. This engine holds sessions, open handles and caches.

## built

project/spec/trace/candidate/cand-live-engine.md - minted, then three
sections appended: How it works, What it costs, What it leans on.

FOUR OPTIONS MINTED for the chart row this line answers, because the row did
not exist when the chart was built:

- project/spec/trace/option/opt-reload-the-whole-engine.md
- project/spec/trace/option/opt-restart-only-this-records-engine.md
- project/spec/trace/option/opt-swap-the-changed-module-in-place.md
- project/spec/trace/option/opt-the-engine-runs-from-the-record-it-serves.md

THE FOUR EARLIER LINES were each given their answer on the new row, taken
from what their own records already said rather than from the best answer
available. Three said reload. cand-os-rooted already runs a process per
record and takes the per-record restart.

## follow_up

THIS LINE IS INCOHERENT AS DRAWN AND A BLIND SCORER FOUND IT, not the
composer. It takes the thin-tree pick, where a record's worktree holds only
that record's own folder and everything shared is read from trunk. Under that
pick the record holds NO ENGINE TO CHANGE. Its sibling cand-judged-path says
exactly that in its own text.

SO ONE DECISION IS OWED BEFORE THIS LINE MEANS ANYTHING: does a record's
worktree carry the machine that record is allowed to change. Answer no and
this line cannot work. Answer yes and the thin tree grows a fourth thing a
record owns.

THAT DECISION IS A CHART ROW NOBODY DREW, and it sits under three of the five
lines rather than under this one alone.

WHAT THE SWAP COSTS IN WALL-CLOCK IS UNMEASURED. The module graph loads
inside the latency budget of the next call in that record, and
req-call-answers-in-one-second applies to it. That is a probe M6 could run
cheaply.

## anything_else

HOW THIS RECORD WAS COMPOSED, WHICH IS A DEFECT RATHER THAN A METHOD.

This compose step was added to the candidate drawing on 2026-08-14, after
build_chart and run-candidates had both signed. Neither submachine can be
re-entered: a reopen refuses with "no form on disk" because a submachine
carries no form, and reopening one of its substates leaves the parent's view
of it met.

SO THE CHART ROW, THE FOUR OPTIONS, THE CANDIDATE AND THIS EVIDENCE were all
written from cut-criteria and evaluate-set by the walking agent, outside the
compose state that should have held them.

THE COMPOSER THEREFORE KNEW THE WHOLE COMPARISON while writing this line,
which is exactly what the method forbids. It shows: the line came out
dominated on every scored axis and internally contradictory.

THE SCORING IS STILL BLIND. A spawned scorer with a clean context read this
candidate, the four beside it and all twelve axis files, and returned twelve
cells. It is the scorer that found the contradiction above.

RECORDED HERE RATHER THAN SMOOTHED, so a reader can discount this line's
composition without having to discover why.
