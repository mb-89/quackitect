---
form: evaluate-set
by: agent
signed_off: 2026-08-14T12:40:14.179Z
reopened: "2026-08-14T12:38:46.076Z — The scores were taken over records that have since been completed and one that changed shape, and a fresh blind scoring of all six now stands."
authors: agent
files:
---

# Evidence form / evaluate-set

## current_situation

SCORED FROM SCRATCH BY A BLIND AGENT THAT WAS GIVEN NO EARLIER SCORES. Six candidates, twelve axes, seventy-two cells.

TOTALS: core-satellite 27, live-engine 20, judged-path 18, os-rooted 14, speaking-root 13, fixed-root 12.

THE ORDER REVERSED AND THE CANDIDATES DID NOT CHANGE. cand-judged-path led at 36 in the previous pass and sits third at 18 here. What changed is that this scorer gave 0 FOR SILENCE where the last one gave 3 for "solid baseline" on axes the records never mention.

THERE IS A GREAT DEAL OF SILENCE. No candidate mentions vendoring, the note inbox, or install anywhere. Three axes are therefore scored almost entirely on adjacent properties or on zero, and the scorer says so rather than inventing a reading.

WHY THE OLD SCORES COULD NOT CARRY. Every record gained a section answering demands it had been silent on, and two changed shape when contradictions in them were resolved. A cell taken over an incomplete record measures the incompleteness.

cand-core-satellite LEADS ON SCORES AND HAS ZERO DEMAND FAILURES, and those are two independent judgments by two agents that never saw each other's work.

ITS ONE 4 IS THE ONLY ONE ON THE BOARD: unlanded work lives in the satellite that owns it with no filesystem presence, so another record cannot reach it even by an absolute path. The scorer named PostgreSQL MVCC, where an uncommitted transaction's changes live in its own backend and are invisible to every other backend until commit.

AND IT IS NOT A CLEAN WIN. That parity rides a memory refinement the record defers pending a profile, and the durability half PostgreSQL gets from a write-ahead log has no counterpart here.

## scores

| candidate | axis | score | anchor | prior_art |
| --- | --- | --- | --- | --- |
| cand-speaking-root | req-a-wrong-act-never-passes-silently | 3 | claims the top mode - the bound tree held as a capability so a path naming another tree cannot be expressed - and records the lean that kills it, the i8 bypass of 2026-08-12 against a guard covering five verbs | |
| cand-speaking-root | req-trace-source-never-mixes | 3 | one engine and one root, so a surface sees exactly the walk's tree and mixing is unavailable; the record itself calls this the weakest guarantee because a view of a non-bound record cannot be served at all | |
| cand-speaking-root | req-engine-folder-is-sealed | 0 | vendoring, a host repository and writing into an engine folder are absent from the record | |
| cand-speaking-root | req-open-notes-stay-visible | 0 | the note inbox is never mentioned | |
| cand-speaking-root | req-panel-shows-the-machine | 1 | surfaces get one paragraph about sharing the walk's binding, and displaying the tree is admitted to be new work | |
| cand-speaking-root | req-overlay-resolution | 0 | method is reconciled into trees at entry and reload - copying rather than a layered identity chain, and no overlay concept appears | |
| cand-speaking-root | req-trace-view-derived-from-files | 1 | a surface sees what the walk sees is the whole statement; where a view's truth lives is never named | |
| cand-speaking-root | req-walk-survives-host-swap | 1 | the claim file lets a peer that cloned the repository answer without local disk; hosts, position recovery and repair steps are absent | |
| cand-speaking-root | req-fresh-machine-runs | 0 | install is never mentioned | |
| cand-speaking-root | req-call-answers-in-one-second | 1 | the only timing-shaped sentence is that the envelope field costs tens of bytes; no serving measurement and no background handle | |
| cand-speaking-root | req-archive-releases-worktrees | 0 | the worktree levelling it cites happens at entry; archiving and release are absent | |
| cand-speaking-root | req-parallel-iterations-own-worktrees | 3 | the capability stops one record's write reaching another's tree, and the counter-fact is recorded plainly: an engine reload reaches every other open record whether they wanted it or not | |
| cand-fixed-root | req-a-wrong-act-never-passes-silently | 3 | makes the tree a schema argument so a missing one refuses as SE-C-046, and admits the refusal now carries the whole load with the undecidable cases unenumerated | |
| cand-fixed-root | req-trace-source-never-mixes | 3 | every call names its tree, so a cross-record view is expressible and correct rather than a bug; the fan-out's nothing-signals-which-copy-answered is the recorded counterweight | |
| cand-fixed-root | req-engine-folder-is-sealed | 0 | not addressed; the fan-out writes method into trees but no engine folder or vendoring appears | |
| cand-fixed-root | req-open-notes-stay-visible | 0 | the note inbox is never mentioned | |
| cand-fixed-root | req-panel-shows-the-machine | 1 | the panel appears once, as an example of a view naming its own tree; setup and appearance are untouched | |
| cand-fixed-root | req-overlay-resolution | 0 | copies in every tree instead of a resolution chain; no identity scheme and no layer order | |
| cand-fixed-root | req-trace-view-derived-from-files | 1 | a surface names the tree it is showing gestures at a file source and stops there | |
| cand-fixed-root | req-walk-survives-host-swap | 1 | a call means one thing for the whole session makes a later reader independent of what was bound - useful and partial; position, hosts and repair steps are absent | |
| cand-fixed-root | req-fresh-machine-runs | 0 | install is never mentioned | |
| cand-fixed-root | req-call-answers-in-one-second | 1 | the fan-out is priced in writes over 27 worktrees and never in time; no serving measurement anywhere | |
| cand-fixed-root | req-archive-releases-worktrees | 0 | the 27 worktrees are cited as fan-out cost, never as something a close releases | |
| cand-fixed-root | req-parallel-iterations-own-worktrees | 2 | the fan-out writes method into every open tree, and isolation rests solely on a check the record admits nobody has enumerated | |
| cand-judged-path | req-a-wrong-act-never-passes-silently | 3 | one predicate at one dispatch pass plus a read-back that proves where bytes went rather than by its own verdict; the two-rules-drift risk is named as the line's weakest point | |
| cand-judged-path | req-trace-source-never-mixes | 3 | reads resolve where the caller names and the answer carries the tree it came from; thin, because it never asks whether trunk-read shared content composes into a record's view | |
| cand-judged-path | req-engine-folder-is-sealed | 1 | shared method is not in the tree at all is a sealing-shaped property aimed at another problem; vendoring is absent | |
| cand-judged-path | req-open-notes-stay-visible | 0 | the note inbox is never mentioned | |
| cand-judged-path | req-panel-shows-the-machine | 1 | the panel is listed among surfaces going through the predicate, and naming the record on screen is admitted new work | |
| cand-judged-path | req-overlay-resolution | 0 | one copy of shared method in trunk, no overlay layer and no identity resolution | |
| cand-judged-path | req-trace-view-derived-from-files | 2 | trace views are named as resolving through the same dispatch pass as an agent's call, so they read the same files; zero truth stored only in the view is never addressed | |
| cand-judged-path | req-walk-survives-host-swap | 1 | one clause about the predicate working on a peer machine with no local worktree; hosts, position and repair steps absent | |
| cand-judged-path | req-fresh-machine-runs | 0 | install is never mentioned | |
| cand-judged-path | req-call-answers-in-one-second | 3 | the number its design depends on is measured and matches exp-trunk-read-cost - 2.0 ms batch reader against 47 to 54 ms spawned - and the naive shape is ruled out on it; the background handle and the many-throttled-cores shape are untouched | |
| cand-judged-path | req-archive-releases-worktrees | 1 | the thin tree shrinks what a worktree holds and the claim file addresses records without one; nothing releases anything at close | |
| cand-judged-path | req-parallel-iterations-own-worktrees | 3 | writes resolve into the bound record and nowhere else, and the thin tree leaves no other record's file within reach; cross-record reads are argued as normal work with the ruling cited | |
| cand-os-rooted | req-a-wrong-act-never-passes-silently | 2 | admits the forbidden mode in its own words - the OS serves a path climbing out of the record perfectly happily and nothing refuses it - and names the fix as a rule it has not written; the admission is what keeps it off 1 | |
| cand-os-rooted | req-trace-source-never-mixes | 1 | calls its own surface story broken and undesigned, offering only that every answer names the tree it came from | |
| cand-os-rooted | req-engine-folder-is-sealed | 0 | not addressed | |
| cand-os-rooted | req-open-notes-stay-visible | 1 | names the inbox as machine-wide and single and says several processes cannot each own it; two options are floated and neither is designed | |
| cand-os-rooted | req-panel-shows-the-machine | 1 | states the panel must now draw several processes with nothing saying how - the axis is addressed as a self-inflicted break | |
| cand-os-rooted | req-overlay-resolution | 0 | fan-out to every tree; no layering or identity resolution | |
| cand-os-rooted | req-trace-view-derived-from-files | 1 | answers name the store they came from; the view's backing is never discussed and the surface is admitted undesigned | |
| cand-os-rooted | req-walk-survives-host-swap | 2 | states the files-only resume as construction - the pull recomputes position from disk and the reading is re-owed - and prices a restart at 67 ms cold; host differences and per-host arming are absent | |
| cand-os-rooted | req-fresh-machine-runs | 0 | new supervision machinery is named as cost; no install answer | |
| cand-os-rooted | req-call-answers-in-one-second | 3 | names the one-second rule against its own shape and records that 27 engine processes are unmeasured; the figure it does give excludes the engine module load, which is the dominant term | |
| cand-os-rooted | req-archive-releases-worktrees | 1 | reaping a per-record engine is named as machinery to build; worktrees and retrievability at close are absent | |
| cand-os-rooted | req-parallel-iterations-own-worktrees | 2 | separate processes leave every other record running the version it started with, but the line admits a path can climb out of a record into another's tree with nothing refusing it | |
| cand-live-engine | req-a-wrong-act-never-passes-silently | 3 | inherits the predicate and read-back and says so, then records the silent class its own keying creates - one module reached outside it serves the wrong version and reports success - plus a partial swap that does not fail cleanly | |
| cand-live-engine | req-trace-source-never-mixes | 2 | resolves per record, then reads the record first and trunk second, so trunk content composes into the record's view by design; the record never tests that composition against this demand | |
| cand-live-engine | req-engine-folder-is-sealed | 1 | the delta keeps the base untouched and the change beside it, which is sealing's shape by accident; vendoring and the write-zero-files rule are absent | |
| cand-live-engine | req-open-notes-stay-visible | 2 | one session holds the mirror, claim ledger, note inbox and call log, so no split owner can lose a note; nothing about the count and the feed agreeing at every read | |
| cand-live-engine | req-panel-shows-the-machine | 1 | a surface resolves through the lane and sees the record's delta; no setup or appearance answer | |
| cand-live-engine | req-overlay-resolution | 3 | the only real layering on the chart - identity resolved record-first then trunk, edits live at next load with no rebuild, staleness against the overridden trunk file recorded as a cost | |
| cand-live-engine | req-trace-view-derived-from-files | 1 | the surface sees the record's delta; where a view's truth is stored is never stated | |
| cand-live-engine | req-walk-survives-host-swap | 0 | no restart anywhere is the pitch; position, files-only resume and a different host are all absent | |
| cand-live-engine | req-fresh-machine-runs | 0 | install is never mentioned | |
| cand-live-engine | req-call-answers-in-one-second | 3 | the only record citing an experiment by id and ruling out the 47 to 54 ms shape; N in-memory engine copies recorded as unmeasured, and one process is one core, which the held throttled-cores assumption argues against unanswered | |
| cand-live-engine | req-archive-releases-worktrees | 1 | argues disk cost is the delta rather than the engine so stale trees stay cheap; nothing releases them, and what frees a closed record's module entry is unstated | |
| cand-live-engine | req-parallel-iterations-own-worktrees | 3 | a changed engine file invalidates only its own record's entry and nothing else notices, over an inherited write predicate; the escapable keying is the recorded hole | |
| cand-core-satellite | req-a-wrong-act-never-passes-silently | 3 | every path meets the seam, is compared to the record's root and refused if outside, with a process boundary under the rule; the section self-stamps as written after the comparison ran, and the refinement names a partial flush reporting success as the forbidden failure mode exactly | |
| cand-core-satellite | req-trace-source-never-mixes | 3 | trunk and a record have different owners, so a view routes to exactly one and the answer names the record; cites the two notes where a worktree sat outside the folder the owner had open | |
| cand-core-satellite | req-engine-folder-is-sealed | 1 | the only record naming the hazard at all - a flush lands in the tree that carries the engine copy, which nothing yet says it may not - with no sealing rule designed | |
| cand-core-satellite | req-open-notes-stay-visible | 3 | the core owns the note inbox by design rather than as a leftover, and the lean is recorded: nothing says the inbox can live behind the mirror and no probe has run | |
| cand-core-satellite | req-panel-shows-the-machine | 2 | a named serving path - the core owns the mirror, routes a surface's question to the owning satellite, answer names the record; the setup-and-appearance half is absent and unrecorded | |
| cand-core-satellite | req-overlay-resolution | 0 | a satellite loads a whole engine copy from its record's tree; no layering and no identity chain | |
| cand-core-satellite | req-trace-view-derived-from-files | 3 | the only record to name this requirement by id and state precisely how its own refinement breaks it - the node files on disk are stale - leaving the base design meeting it | |
| cand-core-satellite | req-walk-survives-host-swap | 2 | the walk resumes from the repository by construction, so a satellite is replaceable under a live walk without the agent stepping out; hosts absent, and the memory refinement's crash window recorded as never measured | |
| cand-core-satellite | req-fresh-machine-runs | 2 | argues no RAM disk, no kernel driver and no administrator prompt, so the named install rows survive; the supervisor and protocol it must ship are never priced against setup | |
| cand-core-satellite | req-call-answers-in-one-second | 3 | cites the throttled-cores assumption by id, makes the N-cores argument, then records the falsifier that assumption itself names - if the slow calls are IO-bound, more processes buy nothing - and admits its 67 and 36 ms figures exclude the term the line needs | |
| cand-core-satellite | req-archive-releases-worktrees | 1 | records that each of 27 trees would carry an engine copy, making a stale tree costlier, and names reaping as machinery; nothing releases a worktree at close | |
| cand-core-satellite | req-parallel-iterations-own-worktrees | 4 | unlanded work lives in the satellite that owns it with no filesystem presence, so another record cannot reach it even by absolute path; parity rides the memory refinement the record defers pending a profile, and the durability half has a write-back design nobody has drawn | PostgreSQL MVCC, where an uncommitted transaction's changes live in its own backend and are invisible to every other backend until commit |

## front

- [[cand-core-satellite]]

## reading

ONE ELIMINATION I DO NOT ACCEPT, AND FIVE I DO.

cand-core-satellite IS NOT ELIMINATED - it leads. What I do not accept is the earlier pass's conclusion, and it is worth saying why the same six records produced a different order.

THE LAST SCORER GAVE 3 FOR SILENCE. It read "solid baseline" into axes the records never discuss, so a candidate that said nothing about vendoring, the note inbox or install scored the same there as one that had thought about them. This scorer gave 0, and the spread collapsed for four lines and held for two.

THAT IS THE HONEST SCORING AND IT IS ALSO THE UNCOMFORTABLE ONE. Three axes now sit at or near zero across the whole board, which says less about the candidates than about what M4 asked them to describe.

WHAT IS ACTUALLY FLAT, and the decision does not turn on any of it.

- req-engine-folder-is-sealed: 0, 0, 1, 0, 1, 1. No candidate mentions vendoring. The axis is scored on adjacent properties by silence rather than by failure.
- req-fresh-machine-runs: 0, 0, 0, 0, 0, 2. Install is mentioned by exactly one record, and only to say what it does NOT need.
- req-open-notes-stay-visible: 0, 0, 0, 1, 2, 3. Only the per-process lines discuss the inbox, because only they break it.

SO THREE OF TWELVE AXES ARE MEASURING WHETHER A RECORD HAPPENED TO MENTION SOMETHING. That is a finding about the chart's coverage rather than about any design, and it belongs at the gate.

THE FRONT IS ONE AND IT IS EARNED DIFFERENTLY THIS TIME. cand-core-satellite is not the utopia point: it takes 0 on the overlay axis and 1 on two others. It leads because it is the only line that ANSWERS most axes rather than being silent on them.

AND ITS ONE 4 IS QUALIFIED BY THE SCORER ITSELF. The PostgreSQL MVCC parity rides the memory refinement, which the record defers pending a profile, and the durability half PostgreSQL gets from a write-ahead log has no counterpart here.

NO WINNER IS PICKED HERE. That is M5's, and for the first time in this milestone the scored leader and the demand survivor are the same line.

## follow_up

THE DEMAND TABLE AND THE SCORES NOW AGREE, and they were produced by two agents that never saw each other's work. cand-core-satellite has zero demand failures and the highest score. That agreement is the strongest evidence this milestone has produced, and it is also the moment to be most careful, because a result that confirms itself twice invites less scrutiny rather than more.

THREE AXES ARE MEASURING SILENCE and the gate should rule on whether they still belong. Vendoring, install and the note inbox are mentioned by almost no record, so they discriminate on whether an author happened to write a paragraph.

FOUR CONTRADICTIONS THE AUDIT FOUND ARE RESOLVED rather than recorded, per the owner's rule that a defect found while building is solved before anything is judged.

- cand-core-satellite carried a whole engine per record while picking the thin tree. It now carries a DELTA, like cand-live-engine, which keeps both picks true.
- Its escape-refusal read as contradicting its core-as-door. Routing is not path resolution, and that is now written.
- A citation in cand-fixed-root attributed a sentence to that record that lives in the requirement. Corrected, and the correction is stamped.
- cand-os-rooted's method answer needs a trunk write its own record says it lacks. It is out on two demands regardless, and the contradiction stands recorded on it.

THE OVERLAY AXIS IS THE LEADER'S WORST CELL, at 0, and cand-live-engine's best, at 3. The delta correction may move it and the cell was not re-scored after the change. That is the first thing M5 should look at.

THREE MEASUREMENTS ARE STILL OWED: a satellite start with the engine module load INCLUDED, a profile of a slow call, and the background handle req-call-answers-in-one-second allows and no candidate offers.

## anything_else

WHAT THE THREE SCORING PASSES ACTUALLY MEASURED, because the same six designs produced three different orders.

PASS ONE ranked judged-path 36 and core-satellite 31. PASS THREE ranks core-satellite 27 and judged-path 18.

NOTHING ABOUT THE DESIGNS EXPLAINS THAT. Two records gained answers and two had contradictions resolved, which moves cells at the margin. The order inverted because the scorers disagreed about what SILENCE is worth.

A GENEROUS SCORER REWARDS A SHORT RECORD. If saying nothing about install scores 3, then the line that never thought about install beats the line that thought about it and found a problem. That is backwards, and it is what pass one did.

THE SCORER THAT GAVE 0 FOR SILENCE ALSO WROTE THE MOST USEFUL SECTION IN THIS STATE: a list of answers that read stronger than they are. Its examples include a line claiming an answer in the conditional and retracting it one paragraph later, a line borrowing a rival's design as its own answer, and two lines offering adoptability as adoption.

THAT SECTION IS WORTH MORE THAN THE TOTALS. Totals rank; it explains why a record can look complete and be thin.

AND IT CAUGHT ME TWICE. A citation I wrote attributing a sentence to the wrong record, and a candidate I completed while leaving a contradiction between two of its own sections. Both are the same class as the borrowed verdict, made after the rule against it was written down.
