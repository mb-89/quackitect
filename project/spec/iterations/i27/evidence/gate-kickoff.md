---
form: gate-kickoff
bless: blessed by agent
by: agent
signed_off: 2026-08-13T18:31:02.543Z
authors: agent
files:
---

# Evidence form / gate-kickoff

## current_situation

M0 is walked. The onboard retro is signed at 18:20:03, the note pool stands at one pending row, and i27 is bound and claimed as the first record of the run order the owner set today.

WHAT THE GATE IS SIZING. A record's worktree becomes the whole workspace while that record is bound, so stepping out to trunk stops being a step anybody takes. The owner ruled the SHAPE on 2026-08-13 and said the iteration builds it rather than re-deciding it, which is unusual and changes what this gate is for: there is no candidate round to price, only a build.

FOUR THINGS THE SEED LISTS ARE ALREADY DONE, landed earlier today on trunk and merged into this tree. The mirror levels and commits at entry. The claim closes when a record ships. The reading credit survives a reload and keys on content. The dependency graph is filled in and rendered, and the container's own offer proves it holds by listing exactly the seventeen enterable records.

WHAT IS LEFT IS THE BINDING ITSELF, and it is the largest single change to the lane since the lane existed: the root a path resolves against stops being fixed.

## retro_drained

- note-86df58115eb7 — THE PULL OVERFLOWED AT 281KB ON i27's FIRST FORM: PENDING, and it cannot be otherwise here — the note was minted after the onboard retro signed, and `carried` is refused outside a retro, so nothing in this milestone can disposition it; its content is destined for i11 and the next retro carries it

## goal

Bind the lane to the record: while a record is bound, its worktree is the only tree the walk can address, and this self-hosting product is the one exception that walks on trunk.

## pulled_in

- THE GENERAL RULE, for every product but this one: while a record is bound the lane's ROOT IS the worktree, so project paths resolve there and trunk is unaddressable from inside — origin: the owner's ruling of 2026-08-13
- THE SELF-HOSTING EXCEPTION: Quackitect gets no worktree and its records walk on trunk, because a product never works on Quackitect and only Quackitect works on itself — origin: the same ruling
- THE se_run LEAK: the method-write guard watches five write tools and not the shell, which is how one feature was built twice on two branches — origin: the cloud field report, 2026-08-12
- THE STATE MODEL: status comes from the record rather than from whether a worktree directory exists, the archive looks records up through git, and a finished record's worktree is deleted — origin: the owner's three-part ruling, forced by the exception above
- VERIFICATION MUST BE ABLE TO FAIL: add the observed-red, complete the state as failed, and the fallback into fix-findings becomes reachable — origin: the owner's ruling of 2026-08-13, and it overrides the seed's own stale line
- THE LANE IS AN INTERFACE AND NOBODY SPECIFIED IT: mint the interface node for the agent-to-engine seam, and make the rule that a new lane verb is defined there — origin: the owner's ruling of 2026-08-13
- A STANDING CLAIM IS RE-CHECKED WHEN ITS SUBJECT CHANGES, computed from the intersection of what a spec verifies and what a delta touched, never blanket at a milestone — origin: the owner's ruling of 2026-08-13
- THE RIDE-ALONG, THE COMMIT UNIT AND THE WALK-BACK COST: three measured per-session wastes the seed names with their numbers — origin: this record's own vision
- A NARRATION REFUSAL MUST NOT DESTROY THE CALL IT RODE ON — origin: this record's own vision, and the engine already has the right shape elsewhere

## left_out

- THE FRONTMATTER SCHEMA and everything typed-field — i29, which is chartered for exactly that
- THE HEADLESS CLOUD BOOTSTRAP — i28, which waits on this record and runs next in the order
- THE PULL'S OVERSIZED PAYLOAD — i11, confirmed by the owner during this walk after it fired live on this record's first form
- THE WALL-CLOCK FLAKE in the parallel battery — i12, carried at the retro
- THE FROZEN-CORPUS SWEEP that makes a bound retro read a stale register — i22, which owns the freeze
- THE ISSUE-KIND WIDENING of the retro's debt sweep — i10, with the exact one-line change named
- THE PRIORITY THE CONTAINER CANNOT DRAW: the owner ruled that order is not dependency and that nothing changes today, so the run order lives in the plan as text

## change_size

major — the architecture gains an interface node, a requirement is corrected rather than added, and the state model changes: what makes a record open stops being a directory on one machine's disk.

WHY NOT MINOR. Those three move together and a minor column prices none of them. The agent-to-engine seam becomes a specified interface, where seventeen interface nodes today define seams between our own elements and the one the user actually touches has none. The capability requirement and its own test spec set different bars, and 34 of 35 verbs hang on which wording wins.

WHY NOT PRODUCT. Nothing here is a new product or a new vehicle. It is one seam in an existing engine, however deep it cuts.

WHAT MAJOR BUYS THAT IS NEEDED. The architecture milestone, because a minted interface must be placed against the elements it separates. The full verification round, because this changes the path every write resolves through, and a wrong resolution is silent — it lands somewhere plausible.

WHAT IT COSTS THAT IS NOT, said honestly. The candidate and comparison machinery prices a decision already made: the owner ruled the shape and said the iteration builds rather than re-decides. Those states get walked over a settled question. THE STRIKE IS NOT TAKEN, because de-escalation is refused once pinned and the architecture half genuinely applies. Paying ceremony on the candidates is the cheaper mistake than under-pricing a change to the root every path resolves against.

## round_0_verify

- evidence vs claims: PASS. The onboard retro is signed at 18:20:03 with all nine sections filled and none boilerplate; opened and read rather than taken from the bless. Its call-log figures are the log's own, not remembered.
- types: PASS. tsc over the whole deliverable exits 0, run after the four engine edits that landed today.
- lint: PASS. biome reports 7 infos and no errors, all of them pre-existing template-literal suggestions in files this record has not touched.
- tests: PASS WITH A NAMED RED. The battery ran 1164 cases and returned 1163. The one failure is a wall-clock assertion that passed 15 of 15 when run scoped, so it measured the machine's load rather than the code. That is recorded and carried to i12 rather than waved past.

## round_1_validate

- exercised against the goal: PARTLY, and honestly so. The goal is that a bound walk cannot address trunk. Nothing of the root swap is built yet, so it is unexercised by construction at a kickoff. What IS exercised is the ground it stands on: 27 trees levelled, the claim ledger closing, the dependency graph proving itself through the container's offer.
- missing: ONE THING, and the seed leaves it open deliberately. The running engine lives in trunk's deliverable, so either a reload can serve from the bound worktree, or an engine change stays invisible until the record lands. It is now registered as an assumption with a probe rather than carried as prose.
- wrong: THE SEED CONTRADICTS ITSELF ON THE FALLBACK EDGES. Its scope says verification must be able to fail and the fallback into fix-findings must open; its exclusion list says the fallback edges are not in scope. The owner's ruling of 2026-08-13 settles it in favour of the scope, and the exclusion line is the stale half. Recorded here rather than left for a later reader to trip over.
- out of scope: NOTHING FOUND that the left-out list does not already name. Six items were placed at other records during the drain that produced this seed, each with the record that owns it.
- prior art: NOT COMPARED, and here is why. The nearest thing people actually use is git worktrees themselves, plus per-workspace tooling roots — a monorepo tool resolving paths against a package root rather than the repository root is the same shape. Neither was run. A comparison needs both sides and our side does not exist yet, so writing one would be fabrication. THE COMPARISON IS OWED AT THE ARCHITECTURE GATE, when there is something of ours to hold against them.

## round_2_red_team

- STEELMAN: the exception swallows the only case that matters => Its best advocate says this. Quackitect is the one product being developed today, so the general rule ships untested and the exception carries all the traffic. A rule that no live product exercises is a rule nobody knows is broken, and the first vehicle to bind will find out. THAT ARGUMENT IS STRONG AND IT STANDS. The mitigation is the probe on the assumption, run against a product that does not edit the engine, before the general rule is trusted.
- STEELMAN: the leak is the whole feature and closing it is not in the plan => The advocate says the guard already exists and was already bypassed, so the interesting work is not binding the root but stopping the shell from reaching around it. The seed agrees in one line and does not say how. THE ANSWER IS THE BINDING ITSELF: with the root moved, trunk is not addressable by any tool including the shell, so the leak closes by construction rather than by another check. That is the strongest argument for this shape over a bigger guard, and it is worth stating out loud rather than assuming.
- KILL-CRITERION => If a reload taken while bound cannot serve the engine from the bound tree, then this product cannot develop its own engine inside its own record, and the exception stops being a tidy carve-out and becomes the normal case. LOOKED FOR IT: not observable today, because nothing has moved the root yet. It is registered with its probe and its trigger is the first reload after the root moves.
- ATTACK: the state model change is bigger than the binding => Status moving off the filesystem touches the archive, the container, the panel and every peer. It has already failed once measurably — a fresh clone showed all 19 pushed records as archived, none ever started. It rides here because the binding breaks the old model, not because it belongs to the same idea. ACCEPTED AS SCOPE, and it is the main reason the size is major rather than minor.
- ATTACK: four of the seed's items landed before the gate priced them => The mirror levelling, the claim closing, the reading credit and the dependency graph were built today outside this record. That is work done before its own kickoff, which is exactly the pattern the register already carries as scope growing past a signed state. NAMED HERE so the build states do not re-plan them, and the requirements state owes them rows.

## raid_additions

- project/spec/trace/raid/raid-asm-engine-serves-from-the-bound-tree.md

## verdict

pass — one coherent idea seen from several sides, sized right, with its open question registered as a probed assumption rather than left as prose.

THE SCOPE IS COHERENT. A record's tree is the only tree the walk can address. The state model, the shell leak and the self-hosting exception all fall OUT of that one change rather than being bundled with it.

THREE THINGS THIS GATE FIXES RATHER THAN WAVES PAST.

The seed contradicts itself on the fallback edges — its scope demands verification be able to fail, its exclusion list says the fallback edges are out. Resolved in favour of the scope, on the owner's ruling of 2026-08-13.

The one open question the seed leaves is now an assumption with a probe and a trigger, not a sentence in a vision nobody re-reads.

Four items landed before this gate priced them, so they are named: the build states inherit them as done, and the requirements state knows it owes them rows.

WHAT KEEPS THIS FROM A CLEAN CONSCIENCE, said rather than hidden. The prior-art comparison is not made, and at a kickoff it cannot be, because our side does not exist yet — it is recorded as owed at the architecture gate rather than left blank. And the candidate machinery will be walked over a question already ruled, which is ceremony paid knowingly.

## follow_up

THE MACHINE GROWS TO MAJOR ON THIS BLESS, and the walk continues into M1 with the size pinned.

WHAT THE NEXT STATES INHERIT AS ALREADY DONE, so nothing re-plans them: the entry-time tree levelling, the claim ledger's terminal state, the reading credit keyed on content, and the dependency graph. The requirements state owes rows for the first two, because they are behaviour this iteration now carries and nothing in the register describes them.

WHAT THE ARCHITECTURE MILESTONE OWES: the interface node for the agent-to-engine seam, placed against the elements it separates, and the prior-art comparison this gate could not make.

WHAT THE ASSUMPTION OWES: its probe, run against a product that does not edit the engine, before the general rule is trusted anywhere.

ONE NOTE STANDS PENDING and cannot be drained inside this milestone. It goes to i11 and the next retro carries it.

## anything_else

A MECHANICAL GAP FOUND WHILE FILLING THIS FORM, and it is small enough to state and cheap enough to fix somewhere.

A note minted AFTER the onboard retro signs and BEFORE its gate cannot be dispositioned by anything in the milestone. The retro is the only state where `carried` and `backlog` are legal, and it has already closed. So the gate's own retro_drained field lists a note it has no legal way to answer, and the only honest fill is to say it stands pending.

That is not a defect in the ruling — carried and backlog genuinely want the whole picture, which is the retro's job. It is a gap in the WINDOW: the field asks a question the state cannot answer.

Two shapes would close it, neither chosen here. The gate could accept a pending note with a reason, as this fill does by hand. Or the retro's drain window could extend to its own gate, since they are one milestone.

Not carried anywhere as a note, because doing so would need the verb this very gap blocks. It is recorded here, which is the one place that survives.
