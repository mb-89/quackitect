---
form: find_by_transforming
by: agent
signed_off: 2026-08-14T07:07:36.789Z
authors: agent
files: null
---

# Evidence form / find_by_transforming

## current_situation

THE SUBJECT IS THE INCUMBENT - the resolution behaviour that stands today. Twelve operators run against it, seven from SCAMPER and five from SIT, and the whole grid is below.

WHY ONE SUBJECT AND NOT FIFTY-FIVE. The picker offers every cluster and every standing option, which is 660 cells. meth-scamper's own situation line says this method is weak on green fields and strong on redesign, and the thing being redesigned here is one behaviour. Running it against options that do not exist yet would transform guesses.

FOUR OPERATORS PRODUCED SOMETHING NEW, and all four came from SIT rather than SCAMPER. That is worth noticing: SCAMPER's letters kept landing on mechanisms the earlier finders had already reached, while SIT's patterns cut the incumbent in ways nothing else had.

TWO OPERATORS REACHED SOMEWHERE ON PURPOSE. Eliminate reaches the null option that find_without already produced, and meth-scamper rules that reaching it twice is not waste.

## applies

yes

## sweep

| operator | subject | what_it_produced |
| --- | --- | --- |
| Substitute | the incumbent | Key resolution on the RECORD ID carried in the call rather than on whatever root the process holds. Confirms opt-mark-the-tree-at-bind-while-intent-is-still-known from a second direction. |
| Combine | the incumbent | Merge resolution with the tool-legality check so one pass answers both which tool may run here and which tree it reaches. Confirms opt-one-resolution-seam-not-a-rule-per-tool. |
| Adapt | the incumbent | nothing - the neighbouring-domain question is find_analogy's whole job and it ran separately. Recording it here would be the same search counted twice. |
| Modify | the incumbent | Magnify the tree into every identifier, not only paths - node ids, refs and log lines all tree-qualified. A real transformation, and it is i10's module-qualified id work rather than this record's. Named, not minted. |
| Put to other use | the incumbent | Once a resolver exists it also answers which record owns a given file, which is what the trace source split and the blast radius both need. Belongs to i7 and i18. Named, not minted. |
| Eliminate | the incumbent | The null option, reached for the second time: delete the decision and make the caller name the tree. Deliberate overlap with find_without, and opt-the-caller-names-the-tree already carries it. |
| Reverse | the incumbent | Invert the direction: the engine hands out a HANDLE to a tree and the caller opens from it, rather than the caller naming a path and the engine finding the tree. That is cap-std's Dir, reached here without the literature. Confirms opt-confine-the-root-to-the-bound-tree. |
| Subtraction | the incumbent | Remove the root concept entirely and make every path absolute from the machine. KILLED: it breaks req-walk-survives-host-swap, because an absolute path is machine-specific and a repository moved between hosts strands every call. Recorded rather than dropped. |
| Task Unification | the incumbent | The claim file already says which machine holds a record; give it the second job of saying which tree holds it. New option. |
| Multiplication | the incumbent | Copy the resolver and change the copy: one rule for reads, a stricter one for writes. New option, and it dissolves the question the architecture gate was going to have to settle. |
| Division | the incumbent | Split the path string into two arguments, the tree and the path within it, so nothing has to be parsed to know which tree is meant. New option. |
| Attribute Dependency | the incumbent | Make the rule vary with the PATH KIND - method, record content, session state, repository root. New option, and the four kinds are already named in the risk register. |

## options

- project/spec/trace/option/opt-separate-rules-for-reads-and-writes.md
- project/spec/trace/option/opt-resolution-varies-with-the-path-kind.md
- project/spec/trace/option/opt-the-tree-is-an-argument-not-part-of-the-path.md
- project/spec/trace/option/opt-the-claim-file-registers-the-tree.md

## follow_up

opt-separate-rules-for-reads-and-writes MAY CLOSE AN OPEN ISSUE RATHER THAN FEEDING IT. raid-iss-cheaper-alternative-never-compared names the deciding question as whether a path judgment covers reads, and treats it as the argument for moving the root. Multiplication says the two were never one question. The architecture milestone should test that before it weighs the expensive answer.

TWO IDEAS BELONG TO OTHER RECORDS AND ARE NAMED RATHER THAN MINTED. Modify produced tree-qualified identifiers, which is i10's one-pass-over-one-key work. Put to other use produced a resolver that also answers file ownership, which i7's trace source split and i18's blast radius both need. Minting options here for work another record owns would put them on a chart that cannot choose them.

ONE TRANSFORMATION WAS KILLED AND KEPT. Subtraction's absolute-path idea breaks host portability. It is recorded with its kill reason so nobody proposes it again, which is what meth-trimming means by a dead end being evidence nobody pays for twice.

## anything_else

SIT OUT-PRODUCED SCAMPER FOUR TO NOTHING HERE, and the reason looks structural rather than accidental.

SCAMPER's letters ask what else the thing could BE - substitute it, adapt it, put it to another use. Every one of those landed on a mechanism prior art or analogy had already found, because those finders ask the same question with better sources.

SIT's patterns ask what happens when the thing is CUT - split it, copy it, remove a piece, make two attributes move together. Nothing else on this record asks that, and all four new options came from it.

Not a finding to act on today. Worth one line if the same pattern shows up on the next few records, because it would mean the two catalogues are not equally worth running whole.
