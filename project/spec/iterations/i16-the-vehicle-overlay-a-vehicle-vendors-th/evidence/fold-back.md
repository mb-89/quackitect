---
form: fold-back
by: agent
signed_off: 2026-08-18T16:09:46.028Z
authors: agent
files:
---

# Evidence form / fold-back

## current_situation

TWO SPIKES RAN AND BOTH ARE SIGNED. Neither reopens anything, and the reason is worth stating rather than leaving as a blank.

### What the evidence did

THE TRIPWIRE DOES NOT FIRE. exp-a-structural-rename-across-a-vehicle took the rename this iteration actually performed — a file moved, an identity changed inside it, citers touched — and expressed it as a what-not-where program without trouble. The winner's falsifier is not met.

AND IT MEASURED SOMETHING NOBODY HAD. A plain git merge handles the realistic case completely: a 20-line node, a 2-line upstream edit and a vehicle-added section merge at MERGE_EXIT=0 into one correct file. Git fails only where the node is short enough that an upstream edit drops similarity below its 50 per cent rename threshold, which a 3-line node with two lines changed demonstrates at MERGE_EXIT=1 with both files left in the tree.

THE CORPUS SITS ON THE SAFE SIDE of that threshold. Trace nodes run 40 to 130 lines.

### Why nothing is reopened

THE EVIDENCE BOUNDED AN ADVANTAGE. It did not invalidate a score. cand-the-program-route still beats cand-everything-declared 4 to 3 on req-overlay-survives-update, and the anchors behind both cells are unchanged: the winner still matches the best comparable tool, and the runner-up still errors out on fuzz.

RE-WEIGHTING THE AXIS NOW WOULD BE THE POISONING cut-criteria's ORDERING EXISTS TO PREVENT. Weights are fixed before the candidates are known, on purpose. A spike run after the scoring is the wrong moment to move them, and moving them with the totals visible is exactly the edit that can be aimed at a favourite.

SO THE BOUND IS RECORDED ON THE TRIPWIRE and the seat stands.

### One spike produced no experiment node, deliberately

THE PATH-JAIL SPIKE BUILT NOTHING. It read the resolver and read the tests, so `built` is none and its finding lives in its own signed evidence rather than in an experiment node.

ITS ANSWER IS THE MORE USEFUL OF THE TWO. Driving a foreign project is one branch in engine/paths.ts, not a redesign: the declaration mechanism exists, the containment guard exists, and read-only is a single `if` at line 93.

### The table

FOURTEEN EXPERIMENTS STAND IN THE CORPUS and thirteen were folded by earlier iterations. Two of those carry WITHDRAWN promotions, both because i34 deleted the mechanism they were promoting into. This iteration adds one row.

## folded

| experiment | folds_to | promote |
| --- | --- | --- |
| [[exp-a-structural-rename-across-a-vehicle]] | Settles raid-tripwire-i16-a-structural-migration-cannot-be-written: it does NOT fire. A structural migration can be expressed as a program, so the winner's falsifier is not met and no score moves. What the evidence ADDS is a bound nobody had: the program route's advantage over a plain git merge exists only where a node is short enough that an upstream edit drops similarity below git's rename threshold, and the corpus sits on the safe side of that line. The bound is recorded on the tripwire rather than applied to the scores, because re-weighting an axis with the totals already visible is the poisoning cut-criteria's ordering exists to prevent. | none — the two probe repositories are throwaway by the throwaway law, and the finding is the product. The one thing worth carrying is the bound itself, which belongs on the tripwire rather than in build code. |
| [[exp-channel-cost]] | raid-asm-machine-wide-state-serves-over-a-local-channel is probed for the call log at 144 microseconds a crossing | none — the build attacks the 124.7 microsecond direct-append floor first, by keeping a handle open |
| [[exp-claim-verb-race]] | raid-asm-remote-serializes-claims carries the dated local-half measurement extended to the whole verb; nothing upstream moves — the winner and its ADR stand as decided | none — WITHDRAWN 2026-08-16 by i11. It promoted the claim verb's mechanism (record then announce, rebase-and-retry on rejection, release as a second commit) into chunk core-process. i34 deleted the claim system whole, so there is no verb left to promote it into. The 9-of-9 measurement stands and is not retracted; only the promotion is. |
| [[exp-does-a-backgrounded-lane-release-its-caller]] | el-entrypoint's start step releases its caller on both platforms, so nothing downstream depends on a platform split for that reason. The detach stays for a DIFFERENT reason - a POSIX process group so a closing session does not take the lane down - and that reason is untested here. The assumption splits in two: releasing the caller is a spawn question and is answered here, surviving the session is a host question and stays owed. | none |
| [[exp-i15-query-latency-at-real-corpus-scale]] | raid-risk-i15-query-latency-unmeasured-at-real-scale — status moves open to mitigated, 31 ms measured against the 1000 ms bound is a 32x margin, comfortably absorbing the throttled-hardware discount raid-asm-the-target-machine-is-many-throttled-cores names | none — the real filters.and/or evaluator and YAML frontmatter parser are still owed at build time; this script is throwaway per the spike-tracer law |
| [[exp-inflight-death]] | raid-ar-crash-lands-safe is re-grounded, because the channel detects a dead satellite in under 100 ms | none — the deadline is one number for specify-build, and it must sit above the 94 ms a crash takes to reach the caller |
| [[exp-kill-and-resume]] | raid-ar-resume-needs-no-person carries the dated probe — the hinge holds today, and the hard-kill variant stays deferred with the POSIX until | none — the files-only property holds by construction, and the build must not erode it |
| [[exp-latency-ledger]] | raid-ar-call-answers-in-one-second re-grounded on the dated ledger and the stale 274 retired — the demand stands and is missed today | none — the fix is the async round's ticket desk, chartered in the backlog |
| [[exp-one-seam]] | raid-risk-a-write-lands-in-the-wrong-tree-silently moves from risk to issue on two dated instances | none — the work is routing the modules that read the filesystem for themselves through the resolver, and lint.ts is the worked example |
| [[exp-satellite-start]] | raid-ar-call-answers-in-one-second is re-grounded at 306.9 ms with the module load included | none — the constraint enters specify-build as a property of the START act, and loading less eagerly is the lever it has |
| [[exp-the-posix-branches-have-never-run]] | Nothing structural moves. It re-frames the first spike rather than changing an element - the start step is platform-split by design, and only the Windows half has ever run. raid-lane-works-on-posix keeps its grade and gains the count. | none |
| [[exp-trunk-read-cost]] | raid-dec-thin-tree carries the dated measurement — the bet holds in the batch-reader shape only, and no requirement moves | none — WITHDRAWN 2026-08-16 by i11. It promoted a long-lived batch reader as the trunk-read shape, because a spawn per read cost 47 to 54 ms a file. i34 removed the reason: a closed record's folder stays on disk, so nothing is read out of git and there is no trunk read to shape. The 2.0 ms measurement stands for whoever needs a batch reader again. |
| [[exp-watchdog]] | raid-ar-crash-lands-safe narrows further, because a heartbeat covers one hang shape of two | none — the deadline and the beat interval are both numbers for specify-build |
| [[exp-what-runtime-the-engine-actually-needs]] | el-entrypoint's verify step compares against a PINNED version rather than a floor, and package.json line 8 is corrected. The assumption is answered for the local half and stays owed for what a default install gives a bare host. | none |

## follow_up

IMMEDIATELY: gate-prototype, which asks whether the riskiest assumptions are validated by evidence.

### What the gate inherits

TWO ANSWERS AND ONE NEW GAP.

- THE SECOND WRITE TARGET IS ONE BRANCH. `.se/roots.json` already declares outside folders, resolveDeclaredRoot already contains them, and read-only is one `if`. A declared root gains a writable flag and resolveInRoot routes a writable root-ref into the resolver that already guards it.
- A STRUCTURAL MIGRATION CAN BE A PROGRAM, and git already answers the case the corpus has.
- AND ONE GUARD DOES NOT EXIST: a declared write target must never be the engine's own tree. Nothing checks it, because nothing has ever been declarable as a write target. gate-architecture named this as override six and the spike confirms it is absent rather than unlocated.

### Two test cases come before any of it

WHOEVER CHANGES THE RESOLVER WRITES THESE FIRST, because they are what stops the change being a hole.

- resolveInRoot REFUSES a read-only root-ref, with the clause it claims.
- resolveDeclaredRoot REFUSES a path climbing out of its declared base.

BOTH DESCRIBE BEHAVIOUR THAT ALREADY EXISTS, so both should pass on the unchanged engine. A red there means the guard was never real.

tests/roots.test.ts HAS FOUR CASES TODAY and covers neither. It covers routing and wiring.

### What the spikes did not settle

- THE SILENT-FALLBACK HALF of req-overlay-survives-update. Only the restructuring half was probed. The other measure is about resolution rather than merging.
- THE COLLISION uc-vendor-and-overlay EXTENSION 3a LEGALISES. Neither run tested a vehicle's in-place edit meeting an update's instruction on the same line.
- WHETHER A DRIVEN PROJECT NEEDS A WRITABLE ROOT AT ALL, or whether the producer writes once and the seam only reads afterwards. If nothing writes after production, the permission is needed for one act rather than for the walk.

### Parked, with what makes each ready

- THE OWNER'S STEER on what the update mechanism is worth, recorded as note-beac84587cd9. It says the axis this iteration turned on matters less than it was scored. Acting on it means reopening converge-pugh and everything below, and it waits on the owner's word rather than on evidence.
- THE RETIRED WORKTREE CONCEPT, note-7432b8a852f6. 261 references in the trace corpus and 123 in the engine, thirteen of them in design input. It reads as its own iteration.
- THE SCOPED RUN handed off as job test-msyun0bj-2, whose verdict records itself.

## anything_else

