---
form: gate-requirements
bless: blessed by agent
by: agent
signed_off: 2026-08-18T15:05:51.887Z
reopened: "2026-08-18T15:03:00.432Z — derive-functions re-signed with two added functions, three added flows and one rename, so this gate blessed a function structure that has since changed"
authors: agent
files:
---

# Evidence form / gate-requirements

## current_situation

The walk stands at gate-requirements with M3 rebuilt for the affordance: eleven rows, five assumptions and two issues.

DESIGN INPUT ENDS HERE. Everything after is solution space.

WHAT CHANGED SINCE THE LAST SIGNATURE. Nothing in the register. Eleven rows stood at 11:14 and eleven rows stand now, with the same wording, the same verify methods and the same sources.

WHAT MOVED IS BELOW THE REGISTER. derive-functions was re-signed with two functions added, three flows added and one renamed, so the cone is now six functions rather than four. Taking an update and reporting what a vehicle changed each had use-case steps behind them and no function in front of them. And `bring-forth-a-copy` is now `bring-forth-a-vehicle`, on the owner's correction: it is not a copy, it is a vehicle, and this product is the engine.

THAT CHANGE CANNOT MOVE THIS GATE'S CLAIM. A function is how a requirement gets served, never what it demands. Adding two did not add, remove or reword a row.

THIS GATE CARRIES NO FIELDS OF ITS OWN, deliberately. Six stood here and every one was already settled by a mechanical check. A gate that re-asks a mechanical check teaches people to skim.

## round_0_verify

- evidence vs claims: every load-bearing claim in M3 is a run or a read from this session. The four probe runs are in the call log with commands and output. engine/paths.ts lines 1-12 for the path jail. engine/tools.ts for the 34 lane verbs. vscode/src/extension.ts lines 1923-1942 for the live surface. product/engine-go/i18_red3.go at ref main, whole, for v1's chain — which the owner pointed at and which turned out to answer the hardest open question.
- and one claim was checked rather than assumed at this pass: that the extension already offers something like the two new acts. It does not. Four commands stand and none produces anything.
- types: NOT RUN and it would answer nothing. No TypeScript changed in M3. The only engine edit this session was engine/calllog.ts, inside the retro.
- lint: RUN, green, inside the battery at 08:42 — 285 files in 430 ms with no fixes applied. Nothing since has touched code, only corpus.
- tests: NOT RUN AT THIS GATE and not legal here. The battery at 08:42 answered 1439 with one failure, which was a stale test asserting the behaviour the retro's window fix removes. It is repaired and the confirm run is owed at verification. WHAT IS NEW SINCE: roughly sixty corpus nodes written or patched across the day, none of which the battery covers — the sweep does, and it is the engine's at verification.

## round_1_validate

- exercised against the goal: The goal of M3 is a register a design can be built from. Exercised by asking whether every use-case step and extension has somewhere for a requirement to attach. FOUR NEW EXTENSIONS ARRIVED WITH THE AFFORDANCE and two needed rows; the other two were already covered by an existing row's fourth facet, which is a fold rather than a gap.
- missing: STEP 7 STILL HAS NO ROW, deliberately and unchanged. Deciding a collision cannot get a requirement before M4 picks a mechanism.
- also missing: the outside prior-art scan, four gates old now. THE EVIDENCE IS LOPSIDED rather than thin: two mechanisms have runs and three families have never been opened.
- wrong: NOTHING IN M3's OWN OUTPUT THIS PASS, which is the first time today that sentence can be written. The two errors caught earlier — a pointer swept onto the wrong function, and an assumption a search could settle — were both from the morning and both already corrected.
- out of scope: nine items at scope-non-goals, plus two more excluded at the boundary specifically for the affordance.
- prior art: TWO MECHANISMS COMPARED WITH RUNS, and one more READ AT ITS PRIMARY because the owner named it. v1's i18_red3.go is the vehicle-drives-stub chain end to end, and it answers what this iteration called its hardest open question: a driven project finds its method through a recorded pointer to the copy that made it. WHAT IT DOES BETTER THAN ANYTHING WE WOULD WRITE: it ran, and it guards a hazard somebody hit in the field. WHAT IT USES THAT WE DO NOT HAVE: a machine-local data home for that pointer, which this product's path jail refuses and which raid-asm-the-pointer-survives-what-the-builder-does-to-the-tree now shows survives neither a move nor a clone.

## goals_served

- A DESCENDANT IS A COMPLETE INDEPENDENT COPY. It comes up on a machine with nothing of the parent's beside it, and everything in it is its owner's to change in place, including the parts the parent wrote. (vp-vendoring, amended 2026-08-18. Its requirement is OWED at write-requirements — req-engine-folder-is-sealed said the opposite and is removed there on the owner's ruling.): SERVED, and now bounded as well as demanded. req-one-command-produces-a-complete-copy carries the five facets, and req-an-act-writes-only-the-tree-it-produced bounds the act that makes it — which matters more now the act is one press away from somebody who read nothing.
- NOTHING A DESCENDANT DOES CAN REACH ITS PARENT. No write, no link, no mount, no install step that writes to the source. The rule names the DIRECTION OF WRITES rather than any mechanism. (raid-dec-an-import-is-read-only-and-a-vendored-copy-is-yours, minted from v2's law with its witness: a symlink and a routine cleanup deleted a repository on 2026-07-25.): SERVED HARDEST, and it gained a second surface. The path jail already enforces the run-time half at ONE resolver. The probe found the platform hole. And the new act row closes the one moment the system legitimately writes somewhere new — inside the produced tree and nowhere else, refusing before creating anything rather than leaving half a tree.
- THE OVERLAY WINS BY IDENTITY. Where a descendant carries a card for an identity the engine also ships, the descendant's card is served at every point that identity resolves, and an un-overridden resource is inherited. (req-overlay-resolution): SERVED, and v1's test is the proof shape — its stub asserts the vehicle's override BEATS the vendored copy. AND THE OVERLAY NOW HAS TO TRAVEL: req-where-each-artifact-lands-when-driving's second facet says the copy's overriding content applies to work in the driven tree, not only in the copy.
- IT WORKS WITH NO OVERLAY AT ALL. With none present, the product comes up on the engine's shipped method and zero builder-authored configuration files. (req-setup-serves-shipped-method): SERVED. The ramp-up story's sixth slide is this pass observed — the desk greets them, and they have written nothing of their own.
- AN UPDATE REACHES A DESCENDANT WITHOUT TAKING ITS CHANGES AWAY. What no longer resolves is REPORTED rather than silently defaulted. HOW is the open design question of this iteration and it is not answered here. (req-overlay-survives-update, req-overlay-drift-reported, raid-risk-ownership-and-receiving-pull-against-each-other): SERVED, and the probe answered HOW for the file half this morning. UNTOUCHED BY THE AFFORDANCE, which is worth saying: scope grew twice and this goal's evidence did not change once.
- ONE COMMAND MAKES A DESCENDANT. The export produces a complete named copy with an empty overlay ready to write into, and no second install of anything. (req-second-product-reuses-install): SERVED, AND THIS IS THE GOAL THE AFFORDANCE CHANGES MOST. One command is now one command OR one act at the surface. AND THE SIXTH GOAL — a copy working on itself while driving something else — gained the row that makes it possible at all: req-the-system-runs-in-a-tree-that-is-not-its-own, whose mechanism v1 already shipped and whose weakest link is now a named assumption graded expected.

## bound_breaches

- if-agent-harness-to-entrypoint: BREACHED THROUGHOUT and the shape is unchanged across five gate signatures today. Submits scale with the size of the evidence rather than with the walk, and this record has now submitted more than forty large forms because M1, M2 and M3 were each written two or three times. THE SECOND KIND, added this afternoon and not this interface: four `se_run` probes at 1.0 to 2.4 seconds, which is git doing real work in throwaway repositories and is not governed by the one-second rule. THE DISPOSITION IS UNCHANGED AND IS A DISPOSITION RATHER THAN AN ACKNOWLEDGEMENT: neither is a defect to fix, the first is the cost of writing evidence, and the alternative is writing less. WHICH MILESTONE OWNS THE MEASUREMENT: none of this iteration's — note-f2bc2e1e2fb8 asks for the pull's phase breakdown and note-83b3d8a1d65a records that every figure from se_pull's own duration is a floor.

## round_2_red_team

- STEELMAN: this gate should FAIL, because design input has now closed twice in one afternoon and closing twice means it never closed => THE ATTACK IS FAIR AND IT MISREADS WHAT MOVED. Both closings were over the same goals; what changed between them was the owner adding a capability the record had excluded. A gate that refused to re-sign after a scope addition would be refusing to accept new information, which is worse than signing twice.
- KILL-CRITERION: this is the wrong call if any of the eleven rows would have to be rewritten once a mechanism is chosen => Walked row by row. TEN SURVIVE ANY MECHANISM. THE ELEVENTH IS req-overlay-drift-reported, unchanged from the last pass: its wording depends on there being a notion of the version a copy came from, and a mechanism with no provenance would break it. ONE ROW, ONE CLAUSE, and the mechanism it excludes is the one everything else already argues against.
- THE NEW ROW DESCRIBES v1's IMPLEMENTATION CLOSELY ENOUGH TO BE A DESIGN => The sharpest attack on this pass. req-the-system-runs-in-a-tree-that-is-not-its-own says the system finds its method through a RECORDED POINTER, and v1 records one in a file. WHAT ANSWERS IT: the row does not say where the pointer lives, and `bring-forth-a-project` names four candidate answers — one of which questions whether a pointer is needed at all. WHAT SURVIVES: the word POINTER is closer to a mechanism than the rest of this register gets, and if M4 picks the name-it-each-time candidate, the row's wording needs one edit.
- AN ASSUMPTION GRADED `expected` IS A RISK WEARING AN ASSUMPTION'S CLOTHES => Checked against the method's own rule. It is not established, and it is not controlled — nobody here decides whether a builder clones their own project. The probe is writable and it is three runs, which is the test the card sets for whether something is an assumption at all. AND ONE OF ITS FOUR CANDIDATE ANSWERS IS ALREADY DISPROVED BY INSPECTION, which is more than most entries can say on the day they are written.
- SIXTY CORPUS NODES WRITTEN TODAY AND THE BATTERY HAS NOT RUN SINCE 08:42 => True and unavoidable: se_test is not legal at any state this walk has stood on since. The sweep is the check that covers corpus writes and it is the engine's at verification. WHAT THIS GATE CAN SAY is that the last full run was green but for one case, that case was mine, and the repair is in the tree unconfirmed.
- THE RATE OF OWNER CORRECTIONS HAS NOT SLOWED => Four today, and the last two arrived after this gate first signed. THE HONEST POSITION is that the record cannot predict when they stop, and the useful one is that each has cost less than the last because the goals stopped moving after the first.

## raid_additions

- none
- Eleven entries stand from this iteration and this gate adds none. Five assumptions, two issues, three decisions and one risk, with three older decisions carrying supersession reasons.
- The gate's job per meth-raid is to ask whether what stands is believable and whether anything obvious is missing. Both were asked.
- WHAT CHANGED IN THE REGISTER RATHER THAN BEING ADDED: one assumption became an ISSUE with a reproduction, two were stamped probed with their runs written into their nodes, and one new assumption arrived from the function work rather than from the sweep that was meant to find it.
- AND ONE THING IS RAISED AND NOT MINTED, unchanged: the evidence for candidate mechanisms is lopsided. Two have runs and three families have never been opened. enumerate-space corrects it and a register entry would only restate an override four gates already carry.

## verdict

pass with overrides — design input closes over a register that did not move since the last signature, re-earned because the function cone beneath it did.

WHAT THE PASS RESTS ON. Eleven requirements, each with a verify method, a removal test and a source. Six functions in the cone, three of them new across this iteration, because producing a project, producing a vehicle and taking an update each yield a different thing. Five assumptions, two of them probed green by running something, one probed FALSE with a reproduction, and one graded expected with a candidate already eliminated by inspection.

WHY THIS SIGNATURE IS A RE-EARN RATHER THAN A SECOND CLOSING. derive-functions was re-signed after this gate, so this gate had answered older ground. The ground it answered is the REGISTER, and the register is byte-identical. What changed is the layer below: two functions, three flows and one rename. Flow closure passed on the new set, which is the mechanical check that the additions land on both ends.

WHAT THIS PASS ADDED THAT MATTERS MOST, unchanged from the last signature. The load-bearing third of what the owner asked for is a row: the system comes up in a tree that carries none of its method, finding it through a recorded pointer to the vehicle that made the project. THAT IS THE ONE THING IN THIS ITERATION WITH NO MECHANISM TODAY, and v1 already shipped one.

AND ONE BOUND ARRIVED WITH THE AFFORDANCE that did not exist when the acts were commands. An act that writes a whole folder at a path somebody named must write inside it and nowhere else, and must refuse before creating anything rather than leaving half a tree. A button removes every accidental barrier in front of a dangerous act, and this row replaces them.

OVERRIDE ONE — THREE PRIOR-ART FAMILIES ARE UNREAD, and the evidence is lopsided. THE DISSENT: two mechanisms have runs and three families have nothing, which is not a comparison. WHY IT PASSES: enumerate-space fixes it, and no requirement names a mechanism — though the word POINTER is the closest this register comes, and the red team says so.

OVERRIDE TWO — ONE USE-CASE STEP STILL HAS NO REQUIREMENT. Deciding a collision. THE DISSENT: a step with no row is a step nothing must do. WHY IT PASSES: a row naming a collision mechanism would freeze design as obligation, which this method forbids in as many words.

WHAT THE PASS DOES NOT CLAIM. That the design is known — where the overriding content sits and where the pointer lives are both open, and both are M4's. That the register is finished — 52 standing assumptions were counted rather than re-run. And that scope has stopped growing: it grew twice today.

ONE THING IS STILL OWED FROM THIS MORNING. The confirm run on the retro's window fix, at verification.

## follow_up

IMMEDIATELY, on the bless: M4 opens. partition-functions and derive-criteria are both offered.

derive-criteria HAS A CHEAP CHECK WAITING. Both new rows are `must`, so neither joins the criterion pool. The pool should still be five and the standing order should still hold — worth confirming rather than assuming, because a new requirement turning that state grey is exactly what a standing artifact is designed to do.

WHAT M4 INHERITS, and it is narrower and better founded than at any earlier gate.

- FIVE MUST ROWS GATE EVERY CANDIDATE: producing a complete copy, nothing reaching the source, where each artifact lands, coming up in a foreign tree, and the act writing only its own tree.
- TWO MECHANISMS ARE RUN RATHER THAN READ, and a third — v1's whole chain — is read at its primary because the owner named it.
- TWO CONSTRAINTS NO CANDIDATE MAY WEAKEN: the path jail having one write target, and the isolation check being platform-shaped rather than one check.
- AND ONE CANDIDATE IS ALREADY ELIMINATED before any scoring: a machine-local pointer survives neither a move nor a clone, by construction.

THE OVERRIDE WITH A DEADLINE IS FOUR GATES OLD. The outside scan at enumerate-space, with a question narrowed three times now: does anything off the shelf resolve method artifacts by IDENTITY, does anything report what a copy CHANGED rather than what diverged, and does anything let a tool come up in a tree that carries none of its own configuration.

AND WHAT specify-build OWES IS UNCHANGED AND CHEAP. `RUNME.ps1 --export` throws the relationship away by making a fresh repository with one commit at lines 57-155. Keeping the history IS the channel.

## anything_else

