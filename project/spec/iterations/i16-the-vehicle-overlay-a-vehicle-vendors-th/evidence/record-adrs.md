---
form: record-adrs
reopened: "2026-08-18T15:40:15.371Z — declare-winner was re-signed after the fitness flags landed, so this claim answered ground that has since moved"
by: agent
signed_off: 2026-08-18T15:40:18.654Z
authors: agent
files:
---

# Evidence form / record-adrs

## current_situation

SIX DECIDING DECISIONS RECORDED, each a register entry beside the risks rather than a separate document. Every one grades crippling or worse, which is what makes it an ADR rather than an ordinary decision.

THE WINNER'S NINE PICKS ARE NOT NINE ADRs. Three of them serve corrosive rows and are recorded in the candidate node with their costs. These six are the ones whose loss ends the product or stops a use case.

AND EACH CARRIES ITS LOSERS AS HISTORY. The rejected options are written with what they were good for, not just why they lost, because two of them are still the right answer somewhere else and one of them ships today.

## adrs

- raid-dec-an-update-arrives-as-a-program
- raid-dec-an-override-matches-by-declared-identity
- raid-dec-an-override-merges-key-by-key
- raid-dec-a-driven-tree-names-which-copy-drives-it
- raid-dec-a-producing-act-is-bounded-by-the-tree-it-produces
- raid-dec-a-copy-is-a-clone-that-keeps-its-history

## follow_up

### What each decision leaves for the build

THE PROGRAM ROUTE owes a migration runner with an ordered version model, and the diff left unstaged for a person to read. Its tripwire is live: raid-tripwire-i16-a-structural-migration-cannot-be-written, triggered by the first migration that must understand structure rather than match text.

IDENTITY MATCHING owes the report that makes an unmatched override loud. That report is what buys step 8 of uc-vendor-and-overlay, and without it this decision reintroduces the silence it was chosen to prevent.

KEY-BY-KEY MERGING owes a fallback for prose. Frontmatter merges; a method card's sections do not, and most of this product's method is prose. The honest resolution may be both mechanisms with a rule saying which applies.

THE RECORDED IDENTITY owes a decision nobody has made: what a copy's identity IS. A copy today is a folder with a name and two people could produce copies with the same name.

THE TRAVELLING BOUND owes the jail's second containment, which is M6's, and the check that a declared target can never be the source, which is owed WITH that change rather than after it.

THE CLONE owes the export's two hard-won guards, both found by failure: three required arguments with no fallback, and the repository marker excluded as a FILE as well as a directory.

### Three things no decision covers, and they are named rather than implied

NOTHING REFUSES AN UNDECLARED CHANGE. Debian's source format fails the build when the tree holds changes not recorded as patches. No candidate has a counterpart, the chart has no row for it, and it is a decision taken WITHOUT alternatives rather than one that passed as covered.

NOTHING ASKS WHERE ARTIFACTS LAND WHILE DRIVING. A must requires every candidate to answer it and all four are silent, so it gated nobody out and left every line unfinished in the same place.

AND NOTHING SAYS WHETHER THE RESOLUTION ORDER IS STATED. The iteration's whole subject is the overlay and no criterion asks it. note-aa4d271a7717 carries the gap; the hole is at write-requirements, which is M3's.

### What the winner should still take from the design that lost

REASONS. The winner's inventory is recomputed from the merge each time, so it can never be false and can never say why. The runner-up attaches a declared reason to every difference. Nothing in the field does both, and evaluate-architecture should test whether a derived inventory carrying declared reasons is expressible at all.

## anything_else

