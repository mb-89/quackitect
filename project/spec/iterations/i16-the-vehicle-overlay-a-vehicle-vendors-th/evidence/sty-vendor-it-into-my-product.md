---
form: sty-vendor-it-into-my-product
by: agent
signed_off: 2026-08-18T20:27:00.096Z
authors: agent
files:
---

# Evidence form / sty-vendor-it-into-my-product

## current_situation

NOT PERFORMED, and this is the slide the whole iteration rests on.

THE PASS LINE IS A MACHINE HOLDING NOTHING OF THE ENGINE. Both headline requirements say so in as many words, and both say why a test cannot stand in: a test running inside this repository's own tree cannot establish it.

TWO REASONS IT WOULD FAIL TODAY, neither speculative:

- THE COPY NEEDS A NETWORK INSTALL. `node_modules` does not travel, so a receiving machine must fetch dependencies before the copy runs at all.
- THE PROMPT LAYER TRAVELS NOW, but only since this evening. The Copilot half was being dropped until the tester caught it, which is exactly the class of fault this slide exists to catch and which no test found for hours.

## built

NO REPORT, BECAUSE NO PERFORMANCE. raid-debt-i16-ships-with-its-demonstrations-unperformed carries it, and raid-iss-the-vehicle-demonstration-has-never-been-performed is the standing issue beneath it.

WHAT IS MECHANICALLY TRUE ABOUT THE PRODUCED TREE:

- the engine, the guidance, the machines and a front door of its own all travel
- the name is written once, in the brand fact
- the root `.claude/settings.json` travels, so the arrival hook can fire on a fresh clone
- `project/.github/instructions/protocol.instructions.md` travels, so the Copilot host has its prompt layer
- nothing generated travels: `dist`, `scratchpad`, `.obsidian`, `.vscode`, the repository's own workflows

WHAT NONE OF THAT ESTABLISHES is that the thing RUNS somewhere else. Every assertion above is about which bytes are in a folder.

## follow_up

READY WHEN somebody carries a produced vehicle to a second machine. It is the last step of one sitting and the only one that needs two computers.

THE SIXTH SLIDE IS THE FREE TEST OF THE NAME: the desk in the new window greeting under the vehicle's own name rather than the engine's. That is req-the-product-name-is-one-fact observed rather than asserted — and note-8aae512f9e01 says the name is still spelled in nine places below the root, including the editor icon, so this observation would probably catch something.

## anything_else

IF ONLY ONE DEMONSTRATION IS EVER PERFORMED, IT SHOULD BE THIS ONE.

The other three are about behaviour inside a machine that already works. This one is the only one that asks whether the thing works anywhere else, which is the entire proposition of a vehicle.

AND IT IS THE GATE'S KILL-CRITERION, written there before this slide was reached: "this is the wrong call if a produced vehicle cannot actually be used on another machine."
