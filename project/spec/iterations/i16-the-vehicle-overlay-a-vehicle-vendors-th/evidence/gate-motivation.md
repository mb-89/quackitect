---
form: gate-motivation
bless: blessed by agent
by: agent
signed_off: 2026-08-18T10:55:54.124Z
reopened: "2026-08-18T10:54:37.504Z — Scope gained a seventh item: the extension affordance the owner asked for, which this record had excluded by name. The gate reviewed six."
authors: agent
files:
---

# Evidence form / gate-motivation

## current_situation

The walk stands at gate-motivation, re-signed a third time after SCOPE gained a seventh item.

WHAT WAS ADDED: the extension affordance. A create-vehicle action, a start-a-project action, and — the load-bearing third — the plugin running in a project a vehicle drives. The owner's framing is that this is the expected behaviour rather than a follow-on, and this record had excluded it by name.

AND THEY NAMED PRIOR ART THAT PROVED EXACT. The extension is v3-only, but spawning a vehicle and running a subproject was already in an earlier version. Confirmed: neither ancestor carries an extension, and v1 carries the whole chain as a passing test at product/engine-go/i18_red3.go.

WHAT THAT SETTLES, and it is the hardest open question of the iteration: a driven project finds its method through a RECORDED POINTER to the vehicle that made it, and `--base <dir>` sets the root. Captured as note-b966f8fd311e.

PAST THIS GATE THE VISION IS AXIOMATIC. What moved is scope and evidence; the goal system has not changed since it was rebuilt on the owner's model this morning.

## vision_scope_stated

YES, AND ONE PART OF IT WAS AMENDED RATHER THAN INHERITED, which is what this gate exists to adjudicate.

THE RESIDENT VISION is vp-the-engine: draw YOUR process as a state machine and the engine gives it teeth. draft-vision points at it and argues the delta in one paragraph rather than re-deriving the axiom. Unchanged.

WHAT WAS AMENDED IS vp-vendoring. Its outcome line carried a MECHANISM the owner withdrew. It now reads: a vehicle is a complete independent copy of the engine that its owner may change entirely, lays its own guidance over what it carries, and can still take improvements from where it came from — while nothing it does can reach that source. Its four success criteria were rebuilt with it.

THE GOAL SYSTEM IS SIX GOALS with five conflicts named and ruled. UNCHANGED since this morning's rebuild, and that is the point worth making: scope has grown twice today and the goals have not moved once. The additions serve goals that already existed.

THE SCOPE IS NOW SEVEN ITEMS IN AND NINE OUT. The seventh is the affordance, and it maps to no new goal because it is where goals 1 and 6 become reachable.

AND THE PACKET IS COMPLETE ONLY AFTER TWO CORRECTIONS THIS GATE FORCED OR FOLLOWED. The first: a goal with no scope item behind it — driving a foreign project — which this gate is supposed to catch and did not, until the record's own rough vision was read for another reason. The second: an item the agent EXCLUDED that the owner wanted, which no check could have found because the exclusion was signed evidence agreeing with itself.

## problem_agreed

YES, AND IT IS MEASURED RATHER THAN ASSERTED.

THE PROBLEM: the only way to give somebody this product is a fork. WITNESS, read this session: RUNME.ps1 lines 57-155 copy the whole tree, rename the brand and commit once into a fresh repository with no history. The receiver gets our guidance and our engine as one undifferentiated tree, and no relationship to where it came from.

THE MECHANISM BEHIND IT, also read: method resolves from two literal lists and nothing else. PROMPT_SOURCES at engine/promptlayer.ts lines 22-34 is four hardcoded paths; METHOD_PREFIXES at engine/paths.ts lines 172-177 is three hardcoded prefixes. There is one layer and it is ours.

AND THE PROBLEM IS SHARPER THAN IT LOOKED THIS MORNING, because the alternative is not exotic. Vendoring a system and still taking its updates is ordinary. WITNESS, primary, fetched this session: git-subtree's own manual in git's tree, which documents ownership, merging that keeps local changes, human conflict resolution and an explicit path back. WHAT THAT MAKES THE PROBLEM: not that the arrangement is hard, but that our copy step throws the relationship away at the moment of copying.

THE PROBLEM IS AGREED BY SOMEBODY OTHER THAN THIS WALK. vp-vendoring was minted at i1 and graded MUST, with its reason on the node: quackitect goes open source while company-specific guidance must stay inside the company, and without vendoring those two facts cannot both hold.

THE OWNER RE-AGREED IT TODAY, twice. "Get to a state where we can work, and start with the vehicle and the foreign project tomorrow." And: "We are just doing what everybody already does."

WHAT IS NOT AGREED, and this gate should not pretend otherwise: nobody outside this house has ever asked for it. The audience is stk-vehicle-owner and no such person has been observed. The problem is real for US and inferred for them.

## prior_art_positioned

PARTLY, AND IT IMPROVED SHARPLY SINCE THIS GATE LAST SIGNED. One family is now read at its primary and it disproved the previous version of the gap claim.

WHAT WAS READ AT ITS PRIMARY, and it decided the shape of the iteration: git-subtree's manual page in git's own tree, `contrib/subtree/git-subtree.adoc`, fetched this session.

WHAT IT DOES BETTER THAN OURS, said first as the sycophancy guard demands — and it is nearly everything this iteration wanted.

- OWNERSHIP: "A subtree is just a subdirectory that can be committed to, branched, and merged along with your project in any way you want."
- RECEIVING WITHOUT LOSING LOCAL CHANGES: `merge` "doesn't remove your own local changes".
- COLLISIONS: "If your merge introduces a conflict, you can resolve it in the usual ways."
- A REVIEWED PATH BACK: `split` then `push`, two explicit commands, nothing automatic.
- AND IT IS INVISIBLE TO THE RECEIVER: subtrees "do not force end-users of your repository to do anything special".

WHAT IT SHEDS, and this is what remains ours. It merges FILES, so an override is a permanent edit to somebody else's file rather than a card that wins by identity. It vendors a PART into a WHOLE, and a descendant here IS the whole. And it cannot report drift, because it has no concept of an identity — when upstream renames a card an overlay points at, the merge succeeds and the override silently stops applying.

OUR OWN PRIOR ART WAS ALSO READ, at ref main: v1's product/engine-go/resolver.go, 196 lines. `overlayLayers()` returns three layers most-specific first. `Resolve()` takes the first hit in ten lines. `ResolveGuides()` walks backwards so the vehicle overwrites. `selftestSplit()` asserts three properties. WHAT IT DOES BETTER: it exists, it is thirty lines, and its selftest tests the seam rather than the code. WHAT IT DOES NOT SETTLE: its most-specific layer is a per-workspace data home, and this product has none.

WHAT WAS STILL NOT COMPARED PROPERLY, and this remains the override. Four outside families are named in frame-delta from familiarity with primaries unopened — scaffolding, package dependencies, base-plus-overlay configuration such as Kustomize, and theme override by path. No documentation was opened for any of them.

AND THE SCAN NOW HAS A SHARPER QUESTION than it had this morning, which is worth more than the breadth it lacks. It is no longer "has anybody solved vendoring" — they have. It is whether any of them resolves by IDENTITY rather than by path, and whether any reports that the base renamed something an overlay still points at.

WHY IT IS AN OVERRIDE AND NOT A FAIL. The comparison that decides the DESIGN happens at enumerate-space/find_prior_art, which the major column grants. What this gate judges is whether the motivation is worth having, and no load-bearing claim in it now rests on an unread source.

## success_measurable

YES, AND THE CRITERIA WERE REBUILT AT THIS ITERATION rather than inherited, because the old ones measured the wrong thing.

WHAT THE OLD ONES MEASURED: writes under the engine folder during a vehicle run, target zero. That counted writes INSIDE the copy, which the owner has ruled entirely legal. It measured the seal rather than the isolation.

vp-vendoring's FOUR CRITERIA AS THEY NOW STAND, read this session on the amended node:

- Dependencies of a descendant on the quackitect working copy. Metric: the count. TARGET: NONE.
- Writes, links, mounts or install steps that resolve OUTSIDE the descendant's own tree. Metric: the count. TARGET: ZERO. This is the isolation law made countable, and it points outward rather than inward.
- A descendant's own changes lost to an update. Metric: the count. TARGET: none it did not decide on. The qualifier is load-bearing — a change the owner chose to drop in a conflict resolution is not a loss.
- Identities resolving two ways on one load. Metric: the count. TARGET: ZERO.

A FIFTH MEASURE THIS ITERATION ADDS, from the check that set the column: an interface carrying flow-overlay. Today ZERO — the flow carries `crosses: in` and appears in exactly two files in the corpus. TARGET: one.

NONE OF THE FIVE HAS EVER BEEN MEASURED, because what they measure does not exist. Today they are unmeasurable rather than failing, which is a different and more honest statement.

AND ONE MEASURE IS DELIBERATELY NOT NUMERIC. Whether a builder can take an engine update and keep their own changes. It is a demonstration rather than a count, and tsp-overlay-seam already names tests/overlay.test.ts as its home with three of four claims defined ahead of their cases.

## risks_logged

YES — FIVE ENTRIES STAND, all nodes rather than table rows, all in project/spec/trace/raid/. THREE MORE WERE KILLED BY THE MODEL CORRECTION and carry their reasons rather than being deleted.

### The five that stand

- raid-dec-an-import-is-read-only-and-a-vendored-copy-is-yours — DECISION, graded FATAL, the only entry here at that grade. It is v2's own law of 2026-07-25, recovered at ref v2 and minted with its witness: an npm `file:` dependency implemented as a symlink, and a routine `git worktree remove --force` that followed it and deleted another repository's working tree and .git.
- raid-risk-ownership-and-receiving-pull-against-each-other — RISK, crippling, plausible. REWRITTEN TWICE TODAY and it now says close to the opposite of its first version. The hazard is INVENTING a mechanism when standard ones exist, not the absence of one. A reader who trusts its title rather than its body will get it backwards.
- raid-risk-the-overlay-location-is-unchosen — RISK, crippling. Three candidates and no criteria yet. This is the entry the major column exists to settle.
- raid-dec-serve-the-overlay-and-report-the-drift — DECISION, and better founded than when it was written. The vendoring mechanism handles text; nothing off the shelf can know an identity moved. The quiet fallback stays the one unacceptable outcome.
- raid-iss-a-must-value-prop-is-served-only-by-coulds — ISSUE, present tense, the owner's to rule.

### The three killed, with their reasons on the nodes

- raid-dec-the-seal-outranks-the-overlay — SUPERSEDED. It protected the copy from its owner when the thing needing protection was the source from the copy.
- raid-dec-probe-the-engine-folder-rather-than-move-it — SUPERSEDED. It answered a question no descendant has, because a descendant contains everything.
- raid-risk-a-sealed-engine-cannot-be-patched-by-the-builder-who-hits-the-bug — DISCHARGED, status decided. Nothing is sealed, so the blocked builder fixes their own copy that afternoon.

THE STANDING REGISTER WAS RE-READ RATHER THAN RE-DERIVED. All ten debt rows were swept at this iteration's own onboard-retro this morning and each carries a dated look. One changed state: raid-debt-cloud-validation-needs-a-machine-this-one-cannot-make became repayable for the first time because i35 shipped.

WHAT IS NOT LOGGED YET, and it is deliberate. No risk about the BUILD, because the design that produces those risks does not exist. reverse-sensitivity at M5 adds a tripwire per credible flip.

## round_0_verify

- evidence vs claims: every load-bearing claim in this milestone cites a document opened this session. RUNME.ps1 lines 28-33 and 57-155. engine/promptlayer.ts lines 22-34. engine/paths.ts lines 172-177. product/engine-go/resolver.go at ref main, whole. product/spec/ledger/se/law-imports-are-read-only.md at ref v2, whole. git-subtree's manual at git master, whole, fetched. vp-vendoring before and after its amendment. flow-overlay, and the two-file search that found no interface carrying it. The severity and likelihood vocabularies, read from machines/items/raid.md rather than guessed.
- and the discipline has a reason: FIVE claims have been retracted today, and each failed by reasoning about something checkable. This milestone was rebuilt after that lesson rather than before it. The specific tell worth naming is a claim quantified over everything with no citation after it — "every existing alternative" was exactly that, and one primary document killed it.
- types: NOT RUN, and it would answer nothing. No TypeScript changed in this milestone. The engine edit this session was engine/calllog.ts, inside the retro.
- lint: RUN, green, inside the battery at 08:42. biome checked 285 files in 430 ms with no fixes applied.
- tests: RUN AND RED, THEN REPAIRED, AND THE CONFIRM RUN IS STILL OWED. The battery at 08:42 answered 1439 tests, 1438 pass, 1 fail. The failure was mine: tests/retro.test.ts asserted that a `done` drain marks a retro, which is the behaviour the window fix removes, while its sibling in tests/mcp.test.ts asserted the opposite. The stale case is corrected and carries the reason. se_test is not legal at this gate, so the confirm run stands owed to verification.

## round_1_validate

- exercised against the goal: The goal is that somebody can build software with this system, keep their own guidance, and still receive what we fix later. Exercised against today's product and it FAILS on the third clause: the export is a fork with no relationship to what it came from. AND NOW ALSO ON A FOURTH: nothing in the surface offers either capability, which is what the new scope item answers.
- missing: THE AUDIENCE, unchanged. stk-vehicle-owner has never been observed, asked or watched. Every claim about what a builder wants is inferred from our own position.
- also missing: three of five prior-art families are still unread, and the scan is owed at enumerate-space. The evidence is now LOPSIDED rather than thin — one mechanism has two runs.
- wrong: THE BIGGEST ERROR OF THE AFTERNOON WAS AN EXCLUSION, and no check could have caught it. This record's scope said in my own words that the iteration does not build the desk affordance around beginning a descendant. The owner asked for exactly that and called it the expected behaviour. THE CORPUS AGREED WITH THE WRONG ANSWER, because the exclusion was signed evidence.
- out of scope: nine items, each in scope-non-goals with its destination, and TWO exclusions were removed rather than added today.
- prior art: THE STRONGEST ANSWER THIS FIELD HAS HAD, and it came from the owner rather than from a scan. v1's product/engine-go/i18_red3.go is the whole chain as a PASSING TEST: a vehicle scaffolded, its own method committed into its overlay, a stub created, the stub resolving the vehicle's method with the override beating the vendored copy, and the machine-global pointer never captured. WHAT IT DOES BETTER THAN ANYTHING WE WOULD WRITE: it exists, it ran, and it guards a hazard somebody hit in the field — a bare engine method directory inside a vehicle that stole the pointer live. WHAT IT USES THAT WE DO NOT HAVE: a per-workspace data home for the pointer, and a settable base. Both are named in the register.

## goals_served

- A DESCENDANT IS A COMPLETE INDEPENDENT COPY. It comes up on a machine with nothing of the parent's beside it, and everything in it is its owner's to change in place, including the parts the parent wrote. (vp-vendoring, amended 2026-08-18. Its requirement is OWED at write-requirements — req-engine-folder-is-sealed said the opposite and is removed there on the owner's ruling.): SERVED, AND THE REQUIREMENT NOW EXISTS. req-one-command-produces-a-complete-copy was minted at M3 after the coverage sweep found step 1 uncovered. req-engine-folder-is-sealed is DELETED after a sweep of 64 references, 12 of them live edges. AND IT IS DEMONSTRATED: a colleague cloned from an internal repository with no reference to the source and got a working system. Scope item one carries it, and item three now makes it reachable from the surface.
- NOTHING A DESCENDANT DOES CAN REACH ITS PARENT. No write, no link, no mount, no install step that writes to the source. The rule names the DIRECTION OF WRITES rather than any mechanism. (raid-dec-an-import-is-read-only-and-a-vendored-copy-is-yours, minted from v2's law with its witness: a symlink and a routine cleanup deleted a repository on 2026-07-25.): SERVED HARDEST. The path jail already enforces the run-time half at ONE resolver. The probe then found the hole: on Windows a junction destroys a neighbour through the incident's own command at exit code 0, while a directory symlink cannot be created without elevation. A symlink-shaped check would have passed that tree. Scope item four carries it and the requirement carries the platform split.
- THE OVERLAY WINS BY IDENTITY. Where a descendant carries a card for an identity the engine also ships, the descendant's card is served at every point that identity resolves, and an un-overridden resource is inherited. (req-overlay-resolution): SERVED, AND v1's TEST IS THE PROOF SHAPE. Its stub asserts the vehicle's override BEATS the vendored engine copy — precedence rather than mere existence, in its own words. That is the assertion this iteration's own suite owes. Scope item five carries it.
- IT WORKS WITH NO OVERLAY AT ALL. With none present, the product comes up on the engine's shipped method and zero builder-authored configuration files. (req-setup-serves-shipped-method): SERVED, and its sealed wording was corrected at M3. It refines uc-vendor-and-overlay extension 2a and nothing about it moved in substance.
- AN UPDATE REACHES A DESCENDANT WITHOUT TAKING ITS CHANGES AWAY. What no longer resolves is REPORTED rather than silently defaulted. HOW is the open design question of this iteration and it is not answered here. (req-overlay-survives-update, req-overlay-drift-reported, raid-risk-ownership-and-receiving-pull-against-each-other): SERVED, AND THE GOAL'S LAST SENTENCE IS NOW WRONG RATHER THAN MERELY OVERSTATED. The probe answered HOW: clone and pull, zero files silently lost, one honest conflict. What stays open is which mechanism and where the overlay sits, both of which are M4's. Scope item six carries it.
- ONE COMMAND MAKES A DESCENDANT. The export produces a complete named copy with an empty overlay ready to write into, and no second install of anything. (req-second-product-reuses-install): SERVED, AND THIS IS THE LINE THE NEW SCOPE ITEM CHANGES MOST. "One command" is now one command OR one action in the extension, and the owner asked for the second. The actionable finding stands: `RUNME.ps1 --export` makes a fresh repository with one commit at lines 57-155, and that single choice is the only reason its copies cannot take an update. AND THE SIXTH GOAL — a copy working on itself while driving something else — is carried by req-where-each-artifact-lands-when-driving AND by scope item two, whose mechanism v1 already shipped: the driven project records the vehicle as its engine home.

## bound_breaches

- if-agent-harness-to-entrypoint: BREACHED TWENTY-ONE TIMES in this record, which is the whole window because this gate has never signed on the corrected model. Its bound is 1 second. THE DISPOSITION, which this field demands rather than an acknowledgement: the breaches scale with the SIZE OF THE FORM being submitted rather than with the walk. A nine-kilobyte evidence form takes roughly nine seconds; a bare submit takes about one. That is the pull assembling, checking and signing a claim rather than idling, and this milestone submitted eleven large forms because every one of them was rewritten twice. WHICH MILESTONE OWNS IT: none of this iteration's. The pull's own cost is note-f2bc2e1e2fb8, which asks for a phase breakdown, and note-83b3d8a1d65a, which measures the reading itself. Both are parked at this morning's retro with ready-whens. WHY NO FIX IS PROPOSED HERE: a bound breach caused by writing more evidence is not a defect to fix, it is a cost to know about, and the honest response is to record it rather than to write less evidence. AND THE NUMBERS ARE LOWER BOUNDS: note-83b3d8a1d65a records the transport measuring 69,141 ms for a call the pull reported as 3,384 ms in the previous window, so every reading taken from se_pull's own duration is a floor. The interface's own node says the same at lines 42-44.

## round_2_red_team

- STEELMAN: this gate should FAIL, because scope grew twice in one afternoon and a gate that keeps re-signing a growing scope is not adjudicating anything => THE STRONGEST ATTACK AVAILABLE, and it is the one to sit with. Two additions, both from the owner, both after this gate had passed. WHAT ANSWERS IT: neither addition changed a goal. The goal system has been fixed since this morning's rebuild, and both items map onto goals that already existed — which is the test for whether scope grew or the vision did. WHAT SURVIVES: a gate cannot catch an omission the corpus agrees with, and both additions were exactly that.
- KILL-CRITERION: this is the wrong call if the affordance is a different iteration wearing this one's name => Looked for it properly. The test is whether item three could be built without items one and two, and it cannot: a create-vehicle button with no vehicle mechanism is a button that does nothing. THE REVERSE ALSO HOLDS — items one and two without item three ship a capability reachable only from a script nobody runs. THE CRITERION DID NOT FIRE, and the shape that would make it fire is scope creep into the rest of tsp-product-scaffold, which non_goals names explicitly.
- THE AGENT EXCLUDED SOMETHING THE OWNER WANTED, AND WROTE A PARAGRAPH DEFENDING THE EXCLUSION => True, and it is the finding of the afternoon. The defence was that an affordance over a capability that does not work yet is decoration. That reasoning is not silly and it is still wrong, because the surface is how anybody finds out whether the capability works.
- FOUR REOPENINGS IN ONE DAY AND EVERY ONE CAME FROM THE OWNER => Counted rather than felt: the sealed model, vendoring being ordinary, the foreign project, and the affordance. NONE was catchable by reading the corpus, because in each case the corpus agreed with the wrong answer. THE INSTRUMENT THAT FOUND ALL FOUR was the owner reading what the agent wrote, which argues for showing work early rather than for checking harder.
- SO WHY BELIEVE THE PACKET IS NOW COMPLETE => Do not. The honest position is that it is complete against everything asked so far, and the rate of additions has not obviously stopped. What the record can offer instead is that each addition has cost less than the last, because the goals stopped moving after the first one.
- THE DEADLINE IS STILL TOMORROW AND SCOPE JUST GREW AGAIN => Real, and the trade is now visible rather than argued. What grew is the surface; what shrank is the build. The channel turned out to be `git clone` and a pull, and the driven-project mechanism turned out to be v1's engine-home pointer with a settable base. Two of the three hardest things are ports rather than designs.

## raid_additions

- none
- Five entries stand and three carry supersession reasons, and this gate adds none. All were opened at log-risks or rewritten there this session.
- The gate's job per meth-raid is to ask whether what stands is believable and whether anything obvious is missing, not to count. Both questions were asked.
- ONE ASSUMPTION IS OWED AND IS NAMED HERE SO identify-assumptions CANNOT MISS IT: that a standard vendoring mechanism can carry a WHOLE product descendant rather than a subdirectory of somebody else's project. git-subtree's own framing is a subproject inside an application, and that is not this shape. It is not minted here because note-6c9321309b45 records what happens when a gate authors the nodes it then judges.
- A SECOND IS OWED at the same state and was named at the kickoff: v1's most-specific overlay layer is a per-workspace data home, and this product has none.
- What is deliberately absent is any risk about the BUILD, because the design that would produce them does not exist. reverse-sensitivity at M5 adds a tripwire per credible flip.

## verdict

pass with overrides — the motivation is worth having, the amendment to vp-vendoring is sound, and two things are passed on the record.

WHAT THIS RE-SIGNATURE CARRIES that the last one did not. Scope has seven items rather than six. The seventh is the affordance the owner asked for and this record had excluded by name, and it is where both capabilities become reachable.

AND THE EVIDENCE UNDER THE PACKET IS STRONGER THAN AT EITHER EARLIER SIGNATURE. Two probes ran, so taking an update onto a modified copy is measured rather than assumed. One assumption turned false with a reproduction. And v1's own end-to-end chain turned out to answer the hardest open question, which the owner pointed at rather than any scan finding.

WHAT THE PASS RESTS ON, unchanged. The problem is measured: the export produces a fork, and method resolves from two hardcoded lists. The constraint behind it is on vp-vendoring and graded MUST since i1. The goal system has not moved since this morning.

OVERRIDE ONE — THREE PRIOR-ART FAMILIES ARE UNREAD, and the evidence is lopsided. THE DISSENT: one mechanism has two runs and three families have nothing, which is not a comparison. WHY IT PASSES: enumerate-space fixes it, and no requirement names a mechanism.

OVERRIDE TWO — THE AUDIENCE IS INFERRED. stk-vehicle-owner has never been observed. THE DISSENT: the shape of the solution is our own position projected outward. WHY IT PASSES: the constraint making this a MUST is about this product rather than about them.

WHAT THE PASS DOES NOT CLAIM. That scope has stopped growing — it grew twice today and the rate has not obviously stopped. That the design is known — where the overriding content sits is still three candidates. And that a gate can catch what the corpus agrees with: four reopenings today all came from the owner, and this gate found none of them.

## follow_up

IMMEDIATELY, on the bless: M2 reopens, and three states need ADDITIONS rather than re-signing.

- write-stories owes a story for the affordance, and it is the RAMP-UP of this iteration: somebody presses create-vehicle, answers what the export asks, and lands in a new window in their own vehicle. Every other story in this record starts where that one ends.
- generalize-use-cases owes an extension on each use case, offering the act in the surface rather than only at a command line.
- write-requirements owes a row that is not a button: THE SYSTEM RUNS IN A TREE THAT IS NOT ITS OWN, finding its method through a recorded pointer to the vehicle that made it. That is v1's engine-home mechanism, and it is the third and load-bearing part of the owner's ask.

AND derive-criteria WILL NEED REDOING, because the pool grows with those rows. A new requirement turning the state grey is the standing artifact working as designed.

WHAT enumerate-space NOW INHERITS. note-b966f8fd311e carries v1's chain as a candidate that has already been RUN, with its own guarded hazard: a bare engine method directory inside a vehicle, which stole the pointer live before somebody wrote the test.

THE OVERRIDE WITH A DEADLINE IS UNCHANGED and now four gates old. The outside scan at enumerate-space, with the narrow question: does anything off the shelf resolve method artifacts by IDENTITY, and does anything report what a copy CHANGED rather than what diverged from a snapshot.

## anything_else

WHAT CHANGED BETWEEN THIS GATE'S TWO SIGNATURES, since a reader comparing them deserves the short version.

THE FIRST SIGNATURE PASSED A VISION WHOSE FIRST GOAL WAS A MECHANISM. "Vendored and sealed" ranked above every need, at a milestone whose entire job is to say why. Three review rounds passed over it without one asking who wanted it sealed. The answer was nobody: it was inherited from a standing requirement the owner has since called old and ruled out.

THE SECOND CORRECTION HIT THIS GATE'S OWN EVIDENCE rather than the packet below it. The gap claim said no alternative gives both ownership and a channel. It sounded like analysis and was an assumption wearing its clothes. One fetch of one primary document killed it.

WHAT BOTH FAILURES SHARE, and it is worth more than either finding: an inherited claim read as a given, and a given is never red-teamed. The first was a requirement in our own corpus. The second was a sentence in my own draft.

WHAT WOULD CATCH THE NEXT ONE. Not more caution — a tell. A goal that can be tested by counting writes is a mechanism. A claim quantified over every existing thing, with no citation after it, is a guess. Both are cheap to look for and neither round currently looks.

AND THE HAPPY HALF, recorded because a gate should say when its evidence moved it in a good direction. Both corrections made the iteration SMALLER. The seal's removal deleted a constraint that was never wanted. The vendoring correction took the channel off the shelf. What is left to build is the overlay and the drift report, which is the part that is actually ours.
