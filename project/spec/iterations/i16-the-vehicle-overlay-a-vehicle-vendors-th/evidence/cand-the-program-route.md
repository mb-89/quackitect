---
form: cand-the-program-route
by: agent
signed_off: 2026-08-18T14:33:27.346Z
reopened: "2026-08-18T14:25:20.491Z — the candidate swapped its pick on how a driven tree finds its copy, so what this state composed is no longer what stands"
amended: "2026-08-18T13:52:04.747Z by agent — the probe conflicted rather than failing to merge, and the program arm had no failure mode available"
authors: agent
files:
---

# Evidence form / cand-the-program-route

## current_situation

THE THIRD OF FOUR, and the one the whole survey points at.

ROUGHLY A HUNDRED PRODUCTS WERE READ and every one makes the copy choose: hold a reference you cannot edit and receive updates, or hold a copy you own and never receive another. Codemods are the single exception, and this line is built on them.

AND THIS ITERATION'S ONLY PROBE TESTED EXACTLY THIS CELL. A copy that merely reordered a file took a merge CONFLICT on an upstream change to a line it never touched, needing a person to settle it. The same change shipped as a program applied with no conflict at all.

CORRECTED AT THE CANDIDATES GATE, 2026-08-18, where this read "could not merge". The run printed `upstream change landed: True` and `copy's own edit kept: True` beside `merge exit: 1`, so the cost is one human resolution per restructured file rather than a change that cannot arrive. And the program arm was a text substitution checked for its own effect, with no repository and no failure mode available, so it demonstrates the mechanism rather than measuring it.

IT IS ALSO THE LARGEST BUILD OF THE FOUR, and the only one whose central claim rests on something the probe deliberately faked.

## built

cand-the-program-route IS RECOMPOSED. One pick changed and three sections were rewritten, at project/spec/trace/candidate/cand-the-program-route.md. Nothing was minted.

### The pick

ROW 4, HOW A TREE CARRYING NO METHOD FINDS THE COPY THAT DRIVES IT. From opt-the-executable-finds-its-own-home to opt-the-tree-names-what-not-where.

WHY, and it is not a preference. The old pick records nothing, and req-the-system-runs-in-a-tree-that-is-not-its-own is priority MUST with a facet binding resolution to a recorded pointer. A demand check found three of four candidates failing it. A must gates whatever a candidate scored, and this one had dominated every rival on the table.

THE OWNER RULED THE DEMAND STANDS and is not to be softened, then asked the question inverted: what would have to CHANGE for each failing candidate to pass. This is that answer, executed on their go.

### Why the repair is one pick and nothing else

THREE OF THIS LINE'S OTHER PICKS ALREADY PAY THE IDENTITY PIN'S PRICE, and each was checked against the node rather than assumed.

- Its producing act is bounded by the tree it produces, so the record has a writer.
- Its resolution already matches by stable identity, so the namespace exists.
- Its updates already go in order one version span at a time, so a version is already in the model.

THE OTHER TWO FAILING CANDIDATES REPAIR BADLY, which is why only this one was repaired. Both keep production outside the lane, so the record would have no writer and a second hand-written guard set would be needed. And repairing the incumbent deletes what it is for: its claim is zero build and changing nothing, and it exists on the chart as the zero the others are priced against.

### The three sections rewritten

HOW IT WORKS now separates two questions the old text ran together. Running inside its own copy, the system still walks up from its executable. Driving a tree that carries no method, the tree records which copy drives it and at what version.

AND IT NOW STATES THE REFUSAL, which the demand's fourth facet asks for and which no candidate had. Three states rather than two: no record, refuse and name what was sought; a record resolving to nothing on a machine that has never seen the named copy, refuse naming the identity; a record that resolves, come up. The wording is fixed by three existing sources rather than invented.

WHAT IT COSTS gained the identity resolution: a first run on a machine that has never seen the named copy needs a lookup and somewhere to hold the result. And it inherits one unmade decision, which now blocks two candidates rather than one — what a copy's identity actually IS has never been decided, and two people could produce copies with the same name.

### What was deliberately not copied

THE PREDECESSOR'S OWN MECHANISM. v1 keeps this pointer in a machine-local data home keyed by a hash of the absolute path. Move the vehicle and it resolves to a layer-less path, is silently skipped, and a machine-global pointer answers instead — a wrong answer rather than an absent one. Recording an identity rather than a location has no local path to go stale.

### What it cost on the table

NOTHING. A spawned scorer with a clean context rescored all ten cells against the rewritten node. Every cell held, and three anchors changed materially: the silent-discovery hole on rank 1 is closed, the bind-to-the-wrong-tree weakness on setup is closed, and the cold-start lookup is a new cost no cell measures.

AND IT IS STILL A DIFFERENT DESIGN. It differs from cand-everything-declared on five of nine rows, four of them the payment axis: upstream hand-writing a migration per breaking change against the copy's owner declaring every edit before making it.

## follow_up

IMMEDIATELY: the last compose state, then evaluate-set.

ONE PROBE IS OWED AT M6 IF THIS LINE WINS, and its question is already written. Whether a migration that cannot be expressed as a text substitution still survives a restructured file. That is the one belief holding this candidate up, and it is cheap to test.

AND ONE PIECE OF PRIOR ART DESERVES A SECOND LOOK BEFORE SCORING. A vendor has shipped migrations that hand the judgment to an installed coding agent where a change cannot be expressed deterministically. One vendor, one release, no independent account — but it is the nearest thing to this product's own architecture that anybody has built, and this candidate is where it would matter.

## anything_else

