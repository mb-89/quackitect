---
form: chunk-runme-drops-the-export
by: agent
signed_off: 2026-08-18T19:05:27.301Z
authors: agent
files: null
---

# Evidence form / chunk-runme-drops-the-export

## current_situation

THE EXPORT IS GONE FROM EVERY PLACE THAT OFFERED IT. 1464 of 1465 pass; the one failure is the corpus-wide churn alarm.

### It was in five places, not one

THE CHUNK NAMED THE LAUNCHER AND THE FRONT DOOR. A sweep before touching anything found three more, and two of those matter more than the ones that were named.

- `project/deliverable/brand/README.entry.md` is the TEMPLATE every copy's README is rendered from. Leaving it would have shipped the old instruction to every vehicle ever produced, in the document its receiver reads first.
- `project/deliverable/engine/bin/se-mcp.ts` holds the ONE help text both the launcher and the engine print. A flag documented and not implemented is worse than one that was never there.

### What replaced it rather than what was deleted

EACH PLACE NOW SAYS HOW THE ACT IS REACHED. A deletion that leaves a gap where an instruction was is a reader stranded at the exact moment they were trying to do something.

AND THE HELP KEEPS A LINE FOR THE FLAG THAT IS GONE. Somebody who knew `--export` will look for it, and finding nothing reads as a broken install rather than a moved affordance.

### The guards are not lost with the script

BOTH TRAVELLED BEFORE THE DELETION, into engine/produce.ts, and both are recorded on el-vehicle-producer so this chunk could not take them:

- all three arguments required, with no fallback to this product's own name
- `.git` excluded as a FILE as well as a directory

THE SECOND ONE IS BELT AND BRACES NOW rather than load-bearing, because a copy makes its own repository from scratch. It stays because the failure it prevents is silent and the check is one line.

## built

### RUNME.ps1

THE 85-LINE EXPORT BLOCK IS GONE, 555 lines down to 470. In its place a comment saying where the act went, why the deletion is part of the design rather than tidying, and which two guards travelled with it.

### README.md

THE SECTION NOW POINTS AT THE BUTTON, and a second section describes starting a project the system drives — which the front door never mentioned at all.

### project/deliverable/brand/README.entry.md

THE SAME TWO SECTIONS, in the template, so every copy produced from here tells its receiver the same thing this one says.

### project/deliverable/engine/bin/se-mcp.ts

The `--export` help block is replaced by a `(no --export)` entry naming both buttons and saying why the flag went.

### project/deliverable/engine/bin/package.ts

A comment saying its exclusions mirror "RUNME's --export" now points at the producing act, which is the thing that still exists.

### What was deliberately left alone

EVERY TRACE NODE MENTIONING THE EXPORT STAYS. They say why a requirement exists, in the past tense, and they are accurate about the past. A requirement's reasoning is not invalidated by the thing it reasoned about being replaced — rewriting them would erase why the row was written.

## follow_up

IMMEDIATELY: the join at the end of the build machine, then the rest of the milestone chain.

### The state of the six chunks

ALL SIX ARE SIGNED. The guards were proved real, the second write target exists, the bound travels with the act, both producers work, both buttons are wired, and the export is gone.

### What the next states will meet

TRACE-DESIGN, then verification, then gate-implementation — which is the owner's to bless.

AND VERIFICATION FIRES THE FULL BATTERY, which is where the churn alarm stops the walk.

### The one thing that blocks, with its ready-when

THE FORMATTER-CHURN ALARM: 872 of 1693 notes would be reformatted, against a limit of 50 percent. It is corpus-wide, spanning i10 through i16, the method cards, the form templates and the guidance.

IT IS NOT THIS ITERATION'S AUTHORSHIP. A parse error in three specs was hiding it, and fixing those let the count be taken at all.

THE FIX IS ONE `se_format` CALL AND IT IS PROVEN SAFE: the test directly above the failing one reprints the whole vault and asserts no note's data and no note's body changes. It passes.

READY WHEN the owner says whether an 872-file reprint may land inside this iteration. It touches method and guidance files, which is why it was not done unasked.

### Still not built, and named rather than hidden

RESOLVING AN IDENTITY TO A TREE. A driven record is readable and comparable but not resolvable, because nothing holds which copies this machine has seen.

## anything_else

### The sweep is the part worth keeping from this chunk

THE CHUNK'S OWN STATEMENT NAMED ONE FILE. Five held the thing it was removing, and the two most damaging were the two nobody had listed: a template that ships to every copy, and the single help text two programs print.

SO THE WORK WAS NOT THE DELETION. It was finding out where the thing actually lived before deleting anything, which took longer than the edit and is the only reason the edit is complete.

THAT IS THE SAME SHAPE AS THE WORKTREE RESIDUE and the ungraded register entries, both caught earlier this iteration. note-a5b270dd8a0f already carries it: a major change needs a state that asks which standing things it makes obsolete.
