---
form: gate-implementation
bless: blessed by human
by: agent
signed_off: 2026-08-16T13:44:03.987Z
authors: agent
files: null
---

# Evidence form / gate-implementation

## current_situation

TWELVE CHUNKS BUILT, TWO REOPENED AND RE-EARNED, THE BATTERY GREEN AT 1319 OF 1319.

THE BATTERY RAN MECHANICALLY FOR THE FIRST TIME. Verification's own exit script fired it and the walk moved on when it came back green. Before today `filled_by: engine` was a sentence in a row and the agent ran the suite from wherever it stood.

THE RECORD'S OWN DEFECTS WERE THE INTERESTING PART. Three of i11's chunks were wrong in ways only the live walk could find, and each was caught by the machine rather than by a reading.

## quality_ok

- [x] Dependencies stay layered
- [x] Every new element carries one stated responsibility
- [x] The linter and the complexity ceiling are clean, with no new suppression
- [x] Every new behavior carries its check, and the battery is green at rest
- [x] Nothing speculative shipped
- [x] What changed is findable
- [x] Every quick-and-dirty taken stands as a visible raid debt entry

## debt_taken

- none

## risks_acceptable

acceptable — no new risk was minted and none regraded; the one worth naming is opt-in and cumulative

THE TWO REGISTER ENTRIES THIS RECORD ADDED are an issue and a decision, not risks: raid-iss-the-seeds-live-defects-outlive-i11 and raid-dec-stop-at-is-a-four-notch-dial-the-hook-enforces.

THE ONE WORTH NAMING IS THE STOP-AT DIAL'S TOP NOTCH. `blockers only` means nothing brings the person back until the walk cannot continue, which is the same shape as ideation on the autonomy dial. machines/stopat.md says so in its own words rather than leaving it to be discovered.

ACCEPTABLE BECAUSE IT IS OPT-IN AND CUMULATIVE. A session starts at `agent judgement`, and reaching the top takes two deliberate presses.

## round_0_verify

- evidence vs claims: every chunk's evidence names what it built and where. Two claims did not survive their own evidence and were reopened rather than argued with — test-verb twice.
- types: the on-change typecheck ran on every .ts edit. Two errors surfaced and both were fixed at the source (a missing AmendOp import, a renamed callback parameter).
- lint: clean. Two complexity refusals were met by extracting a method rather than by suppressing — entryWeight out of expandNode, agentCopy out of formForAgent.
- tests: 1319 of 1319, run by the engine at verification. The first run was 1318 of 1319 and the red was this record's own.

## round_1_validate

- exercised against the goal: the goal is about twenty named defects. Ten struck as already fixed, four fixed here, sixteen recorded live with what is known and what is UNPROBED said as unprobed.
- missing: the seed's own two do-first items were both live and both are now done — the stale security row and the route that reads a shut container door as open. The audit found the second still reproducing three days after the seed described it.
- wrong: three of this record's own chunks. test-verb made the full battery unrunnable everywhere, and separately broke a verdict-log case. The stop-at notches file was authored with wrapped lines and would not parse at all, while every panel test stayed green because they use a hand-written bank.
- out of scope: the numeric autonomy scale's removal is i14's and stayed there. The blast radius' harder half stayed in i18. Sixteen seed defects were recorded rather than fixed, which is the plan's own rule for what is off this surface.
- prior art: NOT COMPARED THIS ROUND, and that is a finding rather than a blank. The bucket's shape came from NASA NPR 7123.1 — a review completes on the agreed disposition of every finding, not on every finding being fixed — but that comparison was made at the retro that seeded this work, not here. Nothing in this record was measured against a system people actually use. The honest reason is that eleven of twelve chunks are repairs to our own mechanisms, where there is no external counterpart to compare against; the twelfth, the stop-at dial, has obvious counterparts in CI approval gates and debugger breakpoints and was NOT compared to either.

## round_2_red_team

- STEELMAN: this record should have been three records, not one => The strongest case against it. Twelve chunks over one surface is a lot to bless at once, and three of them were wrong. A smaller record would have caught test-verb's trap before ten more chunks stood on it. WHAT SAVES IT: the chunks are genuinely independent — nine depend on nothing — and the traps were found by walking, which a smaller record would have walked later rather than sooner.
- The refusals shipped without the paths they demand => THIS HAPPENED TWICE AND IS THE RECORD'S REAL DEFECT. test-verb allowed the battery only at verification, which cannot call it. Verification's exit script then guarded its own fallback, so the repair state was unreachable exactly when the battery was red. Both are fixed; the pattern is that a refusal was written from the caller's side without walking the callee's.
- The audit struck ten defects as fixed on my own reading => A verdict on unfinished work is the thing rule 5 forbids. Four were struck with a live probe or a test; six were struck by reading the code. The six are the weaker half and they are named as such in the evidence.
- KILL-CRITERION: this is the wrong call if the engine-fired battery makes verification slower than the agent running it by hand => Measured: 50 seconds, once, at the one state that owes it. The habit it replaces ran five batteries in a day. Looked for and not found.
- SECOND KILL-CRITERION: this is the wrong call if the stop-at dial's `state end` notch makes an ordinary session unusable => It would, and that is why it is not the default. A seventeen-state re-entry costs seventeen presses. stopat.md records this as the notch doing its job rather than as a defect.

## raid_additions

- project/spec/trace/raid/raid-iss-the-seeds-live-defects-outlive-i11.md
- project/spec/trace/raid/raid-dec-stop-at-is-a-four-notch-dial-the-hook-enforces.md
- project/spec/trace/raid/raid-dec-the-engine-runs-the-red-and-owns-its-own-promotions.md

## verdict

pass — the bundle did what it said, the battery is green at rest, and every defect it introduced was caught and re-earned rather than patched around

TWELVE CHUNKS, the seed's two do-first items both closed, and sixteen live defects recorded with their evidence rather than left as a feeling.

WHAT I WOULD NOT HIDE FROM A READER: three of this record's own chunks were wrong, and two of those made the walk unwalkable until they were fixed. Each was found by the machine — a state with nothing legal to run, a fallback that could not be reached, a file that would not parse — and each was reopened and re-earned.

THE VERDICT IS MINE TO PROPOSE AND THE BLESS IS THE OWNER'S. gate-implementation is theirs by the contract's own list, and this session's dial does not change that.

## follow_up

THE BLESS IS OWED AND IT IS THE OWNER'S. Everything else in the record is done to this point.

THREE THINGS THE OWNER SHOULD WEIGH BEFORE BLESSING.

- Nine of twelve verification claims stand owed against two open register entries. Three were observed here — the three i11's delta put at risk. raid-iss-whole-product-claims-reverified-by-every-record is asking whether a record should carry the whole product's standing claims at all, and that is their decision rather than mine.
- The numeric autonomy scale is still in 108 places across 18 files, and this was the third round of removing it from whichever surface somebody was looking at. note-b8b5d725a61d enumerates them and names what would finish it: a lint, not a fourth sweep.
- `filled_by: engine` still means nothing in code. Verification's battery is fired by an exit script, not by that field. Better than before and not yet honest.

AFTER THE BLESS: fill-story-evidence, gate-validation, package, gate-release.

## anything_else

