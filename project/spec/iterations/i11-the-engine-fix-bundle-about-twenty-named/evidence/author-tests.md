---
form: author-tests
by: agent
signed_off: 2026-08-16T11:36:49.354Z
authors: agent
files: null
---

# Evidence form / author-tests

## current_situation

THREE SPECS AND TWO TEST FILES, written before the build so each demand is watched failing.

TWO TEST-METHOD SPECS cover the four new requirements, plus one older row the owner ruled into scope. ONE DEMONSTRATION-METHOD SPEC carries the must story, which is the link i34 shipped broken and had caught two gates late.

THE TEST FILES EXIST AND FAIL ON ASSERTIONS, never on imports. That shape is deliberate: a test that fails at load takes the whole battery with it and produces a red nobody can read.

## checks

- tsp-the-bucket
- tsp-lane-cost
- tsp-carry-a-finding

## follow_up

SPECIFY-BUILD TAKES THE PAIRING RULES, both already on the register.

- se_test blocking and the full-battery refusal land in ONE build step. raid-dec-blocking-and-the-battery-refusal-ship-together carries the ruling and its rejected options.
- The bucket's permission and its guard land together. The permission alone is strictly weaker than the unchecked box it replaces.

OBSERVE-RED WATCHES ALL SIX CASES FAIL, and one of them will fail for a reason worth reading: the close-side case tests a requirement written in i1 that has never had an implementation.

THE PROSE-CITATION CASE IS THE ONE TO WATCH at the build. It is designed so that a deletion warning built on the trace graph alone still fails it, because the graph sees roughly a fifth of what a deletion breaks.

## anything_else

### Why one older requirement is verified by a new spec

req-close-refuses-loose-ends WAS MINTED IN i1 AND HAS NEVER HAD A MECHANISM. It is a must graded fatal, its Detail says the engine shall refuse the close while any finding stands without a recorded ruling, and searching the engine for a loose-end computation returns nothing.

IT ARRIVED IN THIS ITERATION'S SCOPE BY ACCIDENT, through a probe. raid-asm-an-entry-status-says-whether-it-is-open set out to compare the close's reader against the owed-item guard's, and found there was nothing to compare against. The owner ruled the same day: the close site gets built here.

SO tsp-the-bucket VERIFIES A ROW OLDER THAN THE ITERATION. That is not a scope leak — the bucket's far end IS that row, and building the near end without it would ship a mechanism for carrying defects with nothing to stop them shipping.

### The mapping was checked, not assumed

NO EXISTING BATTERY FILE ALREADY CARRIED ANY OF THESE. The two files are new, and the specs name them rather than claiming a case in an existing suite. A mapping nobody checked is fabricated coverage, and i34 shipped one of those — a must story whose only demonstration had been deleted with the claim system.
