---
form: trace-design
by: agent
signed_off: 2026-08-16T13:21:37.424Z
authors: agent
files:
---

# Evidence form / trace-design

## current_situation

THE DESIGN TRACE IS THE SAME SHAPE i11 FOUND IT, with one file added.

THE BUNDLE MOVED NO ARCHITECTURE. Twelve chunks, and every one allocated to a function that already existed — which is why specify-build never asked for a new element and this state has no new realization to record. The one place it was tested is on the record: allocating the deletion warning would have owed two new interfaces, and minting an interface IS the architecture moving, so the work went to `serve-a-step` instead.

ONE NEW CODE FILE, `engine/bin/red-observed.ts`, claimed by `dsp-quality-toolchain` beside the other scripts that judge whether the tree stands.

WHAT THE SWEEP CANNOT SEE, said plainly rather than left to the reader: `machines/stopat.md` is read by the engine and is not a code file, so no spec claims it and the file sweep does not cover it. It went unreadable today with every panel test green, because those tests use a hand-written bank. A test now loads the shipped file. That is a test closing the hole, not the trace closing it.

## design_trace

THE THREE CHECKS ARE LAW AT THIS SUBMIT, so what follows is what I did before letting them run, not a claim about their verdict.

### What the delta added

ONE NEW CODE FILE: `project/deliverable/engine/bin/red-observed.ts`, the condition script behind observe-red.

IT IS CLAIMED BY `dsp-quality-toolchain`, beside the other scripts that run or judge the suite — `preflight.ts`, `selftest.ts`, `smoketest.ts`, `grades-complete.ts`. That spec's subject is the toolchain that decides whether the tree stands, and firing a record's new checks and demanding a failure is exactly that.

THE PRECEDENT IS `grades-complete.ts`, a condition script claimed by the same spec. Nothing new is being invented about where a script lives.

### What the delta did NOT add

EVERY OTHER CHANGE LANDED IN A FILE THAT WAS ALREADY CLAIMED. Twelve chunks, and eleven of them edited existing files:

- `session.ts`, `tools.ts`, `machine.ts`, `files.ts`, `forms.ts`, `decisions.ts`, `discipline.ts`, `scale.ts`, `params.ts`, `render.ts`, `mirror.ts`, `errors.ts`, `worktree.ts`, `stateform.ts`

SO THE UNCLAIMED LIST SHOULD BE UNCHANGED BY THIS ITERATION. That is the claim this state's third check tests, and it is the one I would expect to be wrong if I have missed a file.

### Two new files that are NOT code

`project/deliverable/machines/stopat.md` and the new register and requirement nodes. The sweep's grain is the deliverable's code files; a machine's data file is read by `scale.ts`, which is claimed.

WORTH SAYING PLAINLY: `stopat.md` is read by the engine and it is not covered by the file sweep. It went unreadable today — wrapped notch lines — and every panel test stayed green because they use a hand-written bank. `scale.test.ts` now loads the shipped file, which is the check that would have caught it, but that is a TEST closing the hole rather than the trace closing it.

### No element or interface was minted

THE BUNDLE ADDED NO ARCHITECTURE. Every chunk allocated to a function that already existed, which is why specify-build never asked for a new element. `req-a-deletion-names-what-points-at-the-node` records the one place this was checked and the answer was to use `serve-a-step` rather than mint two interfaces — minting one IS the architecture moving, and this was a minor.

SO THE FIRST CHECK — every element and interface realized — is unchanged by the delta, and passes or fails on what stood before it.

## follow_up

NOTHING BLOCKS. Verification is next.

ONE ITEM FOR THE NEXT ENGINE ITERATION, and it is the owner's standing complaint rather than a new finding: the old numeric autonomy scale is still in 108 places across 18 files, and this was the third round of removing it from a surface somebody happened to be looking at. note-b8b5d725a61d enumerates them, separates the autonomy scale from the note-and-requirement scale that shares the word, and names what would actually finish it — a lint that refuses a numeric autonomy value on any served answer, rather than a fourth sweep.

THE SAME NOTE RECORDS A DEBT ONE DAY OLD: `machines/stopat.md` carries four new numbers because the rungs format demands a value column.

## anything_else

