---
form: decompose-structure
by: agent
signed_off: 2026-08-16T11:32:31.212Z
authors: agent
files: null
---

# Evidence form / decompose-structure

## current_situation

NO NEW ELEMENT AND NO NEW INTERFACE. Fifteen elements stand and fifteen stand after, which is the escalate test passing for the second time — the first was at derive-functions, where no new function was needed either.

ONE ALLOCATION EDIT, which is the one edit a minor allows. el-walk-engine now implements fn-run-a-governed-walk.work-the-register alongside el-mirror.

THE MATRIX COMPUTES CLEAN: no unimplemented function, no idle element, no missing interface on any boundary-crossing cell.

## elements

- el-account
- el-bootstrap
- el-core
- el-engine-delta
- el-entrypoint
- el-front-desk
- el-holding-pen
- el-method-compiler
- el-mirror
- el-record-store
- el-resolution-seam
- el-satellite
- el-satellite-supervisor
- el-test-runner
- el-walk-engine

## allocation

THE MATRIX IS COMPUTED. What is argued here is where this delta's work lands and why no element had to be added.

### Where each scoped item lands

- THE PULL PAYLOAD, THE ROUTER, `reentry: restart`, se_why's root-naming, and the bucket's permission all land on el-walk-engine. Its own statement already covers them: it computes the position, hands out instructions and readings, and judges every claim against its form. Every one of these is that sentence working better.
- THE CLOSE-SIDE GUARD lands on el-record-store, which implements close-a-record. The owner ruled it into scope after a probe found no implementation behind req-close-refuses-loose-ends. It is new CODE inside an existing element, not a new element.
- se_test BLOCKING AND THE BATTERY REFUSAL land on el-test-runner, which implements answer-with-tests and already carries both paths — scopedGate for a scoped run, batteryGate and testGate for the battery.
- THE TWO MIRROR BUTTONS land on el-mirror, which already implements show-where-it-stands. A freeze and a bless are controls on a surface that already carries the autonomy dial.
- se_amend'S PATCHING and the file tools' argument names land on el-walk-engine, at the dispatch seam its boundary paragraph already names.

### The one allocation that was wrong, and is fixed here

req-a-deletion-names-what-points-at-the-node WAS ALLOCATED TO A FUNCTION WHOSE ONLY IMPLEMENTER NEVER SEES THE ACT. work-the-register was el-mirror's alone — the table, the rows, the edit — because until now the register was only ever a VIEW.

THE DELETE IS A LANE CALL, and the lane is served by el-walk-engine. A warning owed at the moment of deletion cannot be built by an element that renders a page.

SO THE FUNCTION IS NOW IMPLEMENTED TWICE, SPLIT BY MOMENT RATHER THAN BY DATA. Both faces read the same graph the coverage laws already build. The mirror answers what the register says; the engine answers what this act breaks, while the act can still be reconsidered.

NO INTERFACE IS MINTED FOR IT. el-walk-engine already crosses to el-account and el-mirror, and the warning rides those existing cells.

### Why nothing escalates

WANTING A NEW ELEMENT OR A NEW INTERFACE IS THE ARCHITECTURE MOVING, and neither was wanted. The bucket looked most likely to force one, because it adds a path rather than removing a cost — and it did not, because a disposition is a claim about a claim, and judging claims is what el-walk-engine is for.

## follow_up

THE ONE UNSIZED PIECE IS THE CLOSE-SIDE READER, and it now has an element rather than a shape. el-record-store owns close-a-record; what it does not yet own is a computation of what counts as a loose end.

LOOK ONCE MORE BEFORE WRITING IT. Two searches at probe-assumptions returned nothing, and that is evidence rather than proof. A third look belongs in the build step, not another state.

THE THIRD ASSUMPTION UNBLOCKS THE MOMENT THAT READER EXISTS: which of the register's eight status values count as open. `accepted` and `deferred` are where a carried finding drifts, and the answer has to be the same on both sides of the bucket.

AUTHOR-TESTS IS NEXT and owes a demonstration-method spec to sty-carry-a-finding-without-stopping, because it is a must story.

## anything_else

