---
form: log-risks
by: agent
signed_off: 2026-08-16T11:07:05.872Z
authors: agent
files:
---

# Evidence form / log-risks

## current_situation

The register stands and this delta adds four entries, each with an owner and a revisit trigger.

THE GOAL CONFLICT NAMED AT draft-vision IS THE FIRST ENTRY, as the method asks: pace pulls against thoroughness, and the bucket is where that pull is sharpest.

NO EXISTING ENTRY MOVED. i34 re-pointed one risk and put it back the same day; nothing this delta does regrades anything already standing.

## raid_opened

- raid-risk-an-owed-item-without-a-guard-ships-a-known-defect
- raid-dec-blocking-and-the-battery-refusal-ship-together
- raid-asm-the-bundles-defect-list-still-stands
- raid-risk-a-trimmed-payload-costs-a-second-call

## follow_up

THE FOUR TRIGGERS ARE WHAT BRING THESE BACK, and each names a state rather than a date.

- The owed-item guard returns at write-requirements, as a DEMAND rather than an intention.
- The blocking pair returns at specify-build, where the two items are scheduled into one pass.
- The stale-list assumption is probed at frame-delta, item by item, and a struck item is struck WITH its evidence.
- The payload trim's measure returns at verification, where calls per state is the number that answers, not bytes per call.

NOTHING HERE IS DEFERRED TO A LATER ITERATION. All four belong to this one, which is what makes them risks rather than backlog.

## anything_else

## raid_entries

- raid-risk-an-owed-item-without-a-guard-ships-a-known-defect
- raid-dec-blocking-and-the-battery-refusal-ship-together
- raid-asm-the-bundles-defect-list-still-stands
- raid-risk-a-trimmed-payload-costs-a-second-call

## top_risks

- THE BUCKET WITHOUT ITS GUARD is the fatal one, and it is the goal conflict made concrete. NASA's precedent turns on one word: a disposition is AGREED, not asserted. An owed item that names no open raid entry with an owner is a defect shipped quietly, and it contradicts contract rule 4 head-on. Both halves of the guard already exist here and i34 used neither — every checklist box was ticked `[x]` and `[owed]` was never written once. So the failure is not a bypassed guard; it is a guard nothing routes work into.
- BLOCKING WITHOUT THE REFUSAL reverses this iteration's own goal. The polling is currently an accidental deterrent: a battery costs a handoff and ten calls of watching, which is part of why five were run rather than fifty. Blocking removes that cost and leaves the agent's judgment, which the measurement says was wrong five times in one day. Ruled as a decision rather than left as a hazard: the two ship in the same pass.
- THE LIST MAY ALREADY BE FIXED, and one sample of twenty says half. i11's own "worth doing first" item was rewritten by i8 before i11 was ever walked — and two halves survived anyway, one of them the structural fault the item named, reintroduced by the sentence that recorded the fix. Filed as an assumption because it is cheap to settle and nobody has settled it.
- A TRIMMED PAYLOAD MAY COST MORE THAN IT SAVES, and the obvious metric will not catch it. Bytes per pull drops whether the trim was right or wrong. The measure that answers is CALLS PER STATE, and that is the trigger on the entry.
