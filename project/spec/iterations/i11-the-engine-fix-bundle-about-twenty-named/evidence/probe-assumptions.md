---
form: probe-assumptions
by: agent
signed_off: 2026-08-16T11:26:52.233Z
authors: agent
files:
---

# Evidence form / probe-assumptions

## current_situation

THREE ASSUMPTIONS PROBED AGAINST THE REAL CHANNEL, and every one moved. One is false in part, one holds better than expected, and one turned up something worse than the assumption it was written for.

EVERY PROBE READ CODE OR COUNTED EVIDENCE. None is an argument that it must hold, which is how the assumption got made in the first place.

STANDING ASSUMPTIONS WHOSE TRIGGER HAS NOT FIRED are not re-probed. Twenty-eight stand; three are this delta's and they are the three below.

## probes

- raid-asm-an-entry-status-says-whether-it-is-open.probed: unprobed — the close-side reader it would be compared against does not exist in the engine
- raid-asm-the-bundles-defect-list-still-stands.probed: half — ten of twenty-four checked, and items are partly done rather than done or not
- raid-asm-the-engine-can-tell-who-asked-for-a-run.probed: holds — se_test already branches on scope and the battery path already carries two gates
- raid-asm-the-launched-agent-can-authenticate-itself.probed: unprobed — deferred, and the cloud host its probe needs does not exist
- raid-asm-the-trace-graph-holds-every-reference.probed: false in part — the graph sees the frontmatter edges and not the twenty prose citations

## follow_up

raid-risk-an-owed-item-without-a-guard-ships-a-known-defect NEEDS ITS BODY CORRECTED. It argues that both halves of the guard already exist and were merely never used. The probe says the close-side half has no implementation, which makes the risk larger rather than smaller. Correcting it is the first act of the next state that may touch the register.

DECOMPOSE-STRUCTURE INHERITS TWO CONSTRAINTS FROM THESE PROBES.

- The deletion warning reads the trace graph AND a text sweep over the id.
- The bucket's close-side guard is built here, not assumed.

A THIRD ASSUMPTION IS STILL OWED. Once the close-side reader exists, which of the register's eight status values count as open for it is a live question, and `accepted` and `deferred` are exactly where a carried finding drifts.

## anything_else

## fallout

- req-a-deletion-names-what-points-at-the-node NOW RESTS ON A HALF-FALSE ASSUMPTION and its implementation is constrained by it. A warning built on the trace graph alone would report a clean list and be believed, which is worse than no warning. The row stands; decompose-structure must pick two sources, not one.
- req-a-harmless-finding-names-an-open-entry AND req-close-refuses-loose-ends ARE NOW A PAIR NOBODY PLANNED. The bucket was scoped assuming the close already guarded the other end — the kickoff said so, the gate said so, and raid-risk-an-owed-item-without-a-guard-ships-a-known-defect says "both halves already exist in this system". THAT SENTENCE IS WRONG. The requirement exists; the mechanism appears not to. This iteration therefore builds BOTH ends of the bucket or neither, and the risk entry's own reasoning has to be corrected rather than carried.
- THE SCOPE GREW AND SHRANK IN THE SAME STATE, which is worth saying plainly. The battery refusal got cheaper — one condition in an existing gate. The bucket got more expensive — its close-side guard has to be built rather than reused. Neither was known before the probes ran.
