---
form: narration-grace
by: agent
signed_off: 2026-08-16T12:46:56.969Z
authors: agent
files: null
---

# Evidence form / narration-grace

## current_situation

THE TWO MOST-HIT NARRATION REFUSALS ARE BOTH OFF THE HOT PATH NOW.

BY THIS SESSION'S OWN LOG, the boot banner reports SE-C-120 three times and SE-C-133 twice in the last window, against work that was moving in every case.

WHAT IS DELIBERATELY UNCHANGED. The toll itself — an update owed every so many calls — is the person's dial and was never the complaint. What was wrong was the SHAPE of the correction, not the cadence.

## built

TWO CORRECTIONS, both taking a refusal off a path that was already computed.

### The stall warned and bit one call later

BOTH THRESHOLDS WERE THE SAME NUMBER. `NUDGE_AFTER = 5` drove the warning AND the refusal, so the fifth update was nudged and the sixth was refused. A rule that warns and then bites immediately is not a warning; it is a two-stage refusal.

WHY THAT IS WRONG HERE. The counter measures updates since anything CLOSED. Reading four files to find a root cause closes nothing and is not a stall — it is the work. This iteration's own walk hit the refusal repeatedly during chunks that were moving.

NOW TWO CONSTANTS, and the gap between them is the grace.

- `NUDGE_AFTER = 5` — the warning rides the result and costs nothing.
- `REFUSE_AFTER = 12` — the teeth.

TWELVE UPDATES WITH NOTHING CLOSED IS A GENUINELY STALLED CHECKLIST, so the teeth stay. The remedy is still one call away and a resolving op is still never refused.

### A chained fork brief refused while an update's was corrected

AN UPDATE'S CHAIN HAS BEEN APPLIED AS A PLAN SINCE 2026-08-02. That was the lane's most-hit refusal — 174 of one window's 505 failures — and the refusal already computed the split before throwing it away.

A FORK'S CHAIN STILL REFUSED, with the same split computed and the same round trip charged for a comma.

IT NOW CORRECTS, AND STAYS A FORK. That distinction is the whole design: a fork opens a BLOCKING detour and a plan does not, so rewriting the op would change what the call means. Only the shape moves — the parts become the detour's items, and its first part names it, which is what "fix A, then B, then C" says.

THE CORRECTION IS ANNOUNCED, like every other one. `corrected` rides the result.

### What still refuses

A RESOLUTION'S CHAINED BRIEF. Which part resolved the node is not the engine's to guess, and that is unchanged.

### Green

8 of 8 in `narration.test.ts` — run `test-msvstmzv-8`. Two cases are new: the fork correcting while keeping its op, and the pair that must not move with it — an update's chain still becoming a plan, a resolution's still refusing.

`project/guidance/refusals.md` carries both rules ahead of the refusal, sections SE-C-120 and SE-C-133.

## follow_up

NOTHING BLOCKS.

NEXT IS `mirror-buttons`: the stop-at dial. Its spec half already stands — `machines/stopat.md` holds the four notches and `machines/panels/controls.md` carries the row. What is owed is the code: the stop hook reading the notch instead of applying one fixed rule, and the engine holding a transition under `state end`.

## anything_else

