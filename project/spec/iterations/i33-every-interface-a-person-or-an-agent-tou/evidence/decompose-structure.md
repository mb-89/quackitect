---
form: decompose-structure
by: agent
signed_off: 2026-08-17T16:56:27.677Z
reopened: "2026-08-17T16:55:53.141Z — gate-requirements re-signed and re-blessed above it with an eighth requirement in the register. Every requirement must reach the structure by one path or the other, so a new row is a real check here rather than a formality."
amended: "2026-08-17T14:39:37.878Z by agent — gate-requirements re-signed with a real breach reading; the structure stands"
authors: agent
files:
---

# Evidence form / decompose-structure

## current_situation

ONE EDIT IS LEGAL AT THIS SIZE: new functions allocate into existing elements. THIS DELTA MINTED NO NEW FUNCTION, so there is nothing new to allocate.

RE-READ FIVE TIMES SINCE, and no element or interface is owed by any of it. A latency reading names no new element. A goals list names no new element. An owner ruling about which act re-greys a chain names no new element either.

THE FIRST FIVE ROWS REACH THE STRUCTURE TRANSITIVELY, by the path the guidance calls the usual one: requirement, served by fn-run-a-governed-walk.show-where-it-stands, implemented by el-mirror. Nothing is written twice and no requirement needs naming directly in an element's satisfies.

THE THREE LATER ROWS REACH IT THE SAME WAY, through a second element. req-one-operation-reads-its-input-once is served by fn-run-a-governed-walk.serve-a-step. req-a-breached-bound-is-put-in-front-of-a-reviewer and req-an-amend-leaves-the-tree-standing are both served by fn-run-a-governed-walk.judge-a-claim. el-walk-engine implements both functions, which was checked against the element's own implements list rather than assumed.

SO NO NEW ELEMENT AND NO NEW INTERFACE, at any pass. The escalate tell at this state is wanting one, and it did not fire even with the register grown from five rows to eight.

THAT THE FIRST FIVE ALL LANDED ON el-mirror IS THE SAME FINDING AS EVERY LAYER ABOVE. An iteration whose goal is that calls answer fast allocated entirely to the element that DRAWS things. The three added since all land on el-walk-engine, which is the element that ANSWERS, and that split is the correction visible in the structure rather than only in the prose.

ELEMENTS, GROUPS AND INTERFACES STAND UNTOUCHED.

## elements

- el-mirror
- el-walk-engine

## allocation

UNCHANGED, and that is the whole answer rather than a shortfall.

WHY NOTHING MOVED. Allocation binds FUNCTIONS to elements. This delta extended three existing functions with eight new requirements between them. It minted no function, so no allocation edge was created or removed.

WHO IMPLEMENTS WHAT, checked at this state rather than assumed. A search across project/spec/trace/element for fn-run-a-governed-walk.show-where-it-stands returns exactly one element, el-mirror. serve-a-step and judge-a-claim are both implemented by el-walk-engine.

WHY ONE IMPLEMENTER FOR THE SURFACE FUNCTION IS CORRECT AND NOT A HOLE, and this was probed rather than argued. The VS Code host also SHOWS the control bar, so a reader might expect a second element to implement the same function. It does not render it. extension.ts line 373 fetches the mirror's /widget/controls and takes its MARKUP, and the comment above that line states the rule: the bar arrives as markup, never as data to re-draw, because deriving a second picture is how struck sliders once stayed on screen for a whole expedition.

SO THE BEHAVIOUR THE FIVE ROWS DEMAND LIVES IN ONE PLACE, which is why one element implements it. A host that displays somebody else's rendering is not a second implementer.

THE INTERFACES OWED: none new. An interface is owed where a flow's producer and consumer land in DIFFERENT elements. No new flow was minted at derive-functions — the delta changes what flow-surface carries rather than the set of flows — so no new crossing exists and no cell is owed.

THE TRACE IS COMPLETE ON THE TRANSITIVE PATH ALONE. All eight requirements reach an element through the function chain, five to el-mirror and three to el-walk-engine. Zero unreached, and none needed a direct satisfies line.

## follow_up

M4's remaining states inherit a structure that did not move, which is what a minor should look like at this point.

WHAT TO WATCH NEXT, and it is the wording strain recorded at derive-functions rather than a new concern. req-a-refused-act-says-why-and-what-next binds an AGENT as well as a person. el-mirror serves the person's surface, and the agent's typed refusals come from the lane. If that row's agent half wants a second implementer, it surfaces here as an element wanting a function it does not implement — and it did not surface today, because the row's Detail carries both halves as one demand on one behaviour.

WHAT WOULD HAVE ESCALATED AND DID NOT: wanting a new element or a new interface. Neither was wanted. The blessed minor column still holds, and the tripwire for the opposite outcome remains raid-asm-the-boundaries-are-few-enough-to-model-one-node-each, which fires at milestone one's count.

## anything_else

