---
form: breaches-reach-the-reviewer
by: agent
signed_off: 2026-08-17T14:06:16.462Z
authors: agent
files: null
---

# Evidence form / breaches-reach-the-reviewer

## current_situation

The chunk the kickoff's own red team named as this iteration's pass-fail: a milestone three that ends with no state reading the instrument means i33 repeated i12.

IT IS BUILT AND GREEN, and its reach is one boundary of thirteen — stated here rather than discovered later.

## built

READING THE INSTRUMENT IS NOW SOMEBODY'S JOB, mechanically.

WHAT LANDED:

- engine/machine.ts: `bound_breaches` joined STANDARD_ROUNDS. Required, per-item over `$breaches`, on every gate. Both compilers get it from one place, as with goals_served.
- engine/stateform.ts: `breachItems`, and `$breaches` in the source resolver. The window is SINCE THIS GATE LAST SIGNED, read off the gate's own signature rather than from a clock — so a breach fixed and re-broken between two reviews appears at both.
- tests/rounds.test.ts knows the round list.

THE DESCRIPTION DEMANDS A DISPOSITION, not an acknowledgement. Say the fix, the reason it is acceptable, or which milestone owns it. An acknowledgement with no disposition is exactly the reading this field replaces.

A CLEAN WINDOW IS STILL A ROW. An empty per-item field refuses as unanswered, and an absent field would make a clean window and an unasked question look identical — the same defect as a control that declines in silence. So the reader confirms they looked, and the field can still say no, which is what separates it from a field that only ever passes.

WHAT IT REACHES TODAY, AND WHAT IT DOES NOT. Every lane call crosses if-agent-harness-to-entrypoint, so the log answers for that boundary directly. The other twelve need their crossings attributed in the log before they can be counted. That limit is written into breachItems' own comment, because a silent zero on twelve boundaries would be a measured zero dressed as a clean bill — which is the failure mode this whole iteration is about.

VERIFIED: 1399 tests, 0 failures, sweep green over 1219 nodes.

## follow_up

TWO THINGS FOLLOW, and the first is immediate.

EVERY GATE IN THIS ITERATION NOW OWES bound_breaches. Adding a required field to the standard rounds greys every signed gate, which is the ripple doing its job on my own change. They are re-earned rather than waved through.

ATTRIBUTING A CALL TO ITS CROSSING is what widens this from one boundary to thirteen. It belongs to fix-what-the-numbers-name, because until the numbers name a crossing there is nothing to fix per crossing.

## anything_else

