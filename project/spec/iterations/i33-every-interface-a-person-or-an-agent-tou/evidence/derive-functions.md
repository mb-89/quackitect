---
form: derive-functions
by: agent
signed_off: 2026-08-17T16:52:02.868Z
reopened: "2026-08-17T16:50:45.668Z — write-requirements re-signed above it and gained an eighth row, req-an-amend-leaves-the-tree-standing. A new requirement must map to a function or the structure has a hole, so this is a real check rather than a formality."
amended: "2026-08-17T14:38:24.498Z by agent — the register re-signed above it; the three functions stand"
authors: agent
files:
---

# Evidence form / derive-functions

## current_situation

The function structure EXTENDS and is not re-derived. All five original requirements landed on ONE existing function, fn-run-a-governed-walk.show-where-it-stands, whose statement is to put the whole standing position in front of a person without them asking anybody.

RE-READ FOUR TIMES SINCE, and the escalate tell has not fired at any pass. No new function was minted at any of them.

THAT THE FIRST FIVE ALL LANDED ON A SURFACE FUNCTION IS THE FINDING. show-where-it-stands is about SHOWING. An iteration whose goal is that calls come back fast produced five requirements that all mapped to how a surface displays things, and every coverage check passed.

THREE MORE ROWS LANDED ON TWO OTHER FUNCTIONS as the walk found what was missing.

req-one-operation-reads-its-input-once goes to fn-run-a-governed-walk.serve-a-step, which already satisfies req-call-answers-in-one-second and whose own rationale says the one-second answer is a property of THAT function because nothing else is on the driver's critical path. The same argument carries the new row: a bound on the answer means nothing while the cost multiplies by how many times the work asks for its input.

req-a-breached-bound-is-put-in-front-of-a-reviewer and req-an-amend-leaves-the-tree-standing both go to fn-run-a-governed-walk.judge-a-claim. The second is the closer fit of the two: whether a claim still stands after something above it moved is the exact question that function answers, and an amendment and a reopen are the two acts that answer it differently. It sits beside req-a-ripple-names-its-root, which is the same subject from the other end.

THE COVERAGE CHECK REFUSED THE FIRST SUBMIT, and it was right. This field said the eighth row lands on judge-a-claim while no function node in the corpus named it. Prose is not the edge — the node is — and the engine would not take the sentence for the fact. That is the check this iteration exists to defend working on this iteration's own evidence.

NO NEW FUNCTION WAS MINTED, and that is the kickoff's prediction holding rather than a convenience. The escalate tell at this state is a new function fitting no existing cluster, which is the architecture moving. Nothing here moved it, including the eighth row — judging a claim is what the walk already does, and the ruling changes which answer is right rather than adding a capability.

THE FIT WAS TESTED RATHER THAN ASSUMED. show-where-it-stands already satisfies req-controls-draw-from-their-spec and req-a-surface-resolves-to-what-it-shows and req-surface-answers-in-one-second. A refusal's reason, a running operation's signal and a control's repeat-semantics are all part of the standing position, and the third one has a sibling row already sitting there.

## functions

- fn-run-a-governed-walk.show-where-it-stands
- fn-run-a-governed-walk.serve-a-step
- fn-run-a-governed-walk.judge-a-claim

## flows

- flow-surface

## neutrality

EVERY ROW WAS ASKED THE QUESTION: could two honestly different designs both do this?

ALL THREE REDO ROWS PASS IT.

req-one-operation-reads-its-input-once says the corpus is read once per operation and never how — a parameter handed down, a memo keyed to the operation and a single reader object all satisfy it.

req-a-breached-bound-is-put-in-front-of-a-reviewer says the breaches are presented at a gate and never in what shape — a form field, a rendered panel and a refusal listing them all satisfy it.

req-an-amend-leaves-the-tree-standing says a correction leaves the claims below standing and a reopen drops them, and never how that is decided. Comparing signature times, writing a marker on each affected file, and walking the graph from the changed node all satisfy it. The engine happens to compare times, and the row would be equally true of the other two.

YES ON ALL FIVE ORIGINALS, and none needed changing. No row names a database, a queue, a file format, a vendor, a component or a file to edit. No row uses an implementation verb — nothing here says cache, index or poll.

THE ONE THAT CAME CLOSEST TO NAMING A SOLUTION was the slowness signal. An earlier phrasing said what the signal must not CONTAIN in mechanism terms, a completion percentage. It survives only because the row states an OUTCOME instead — the showing shall not leave a person less willing to wait than silence leaves them — and the percentage appears in the Detail as the known way to fail it rather than as the demand.

THE SUBTLE TEST, applied deliberately because no word list catches it: would each function still be called this in a design we rejected? show-where-it-stands survives every alternative surface — a terminal, a phone, a printed report. It names what the system does and not how any host draws it.

ONE HONEST GAP, recorded rather than smoothed over. req-a-refused-act-says-why-and-what-next binds an AGENT as well as a person, and its Detail carries both halves in a table. show-where-it-stands is worded for a person. The agent half may want allocating to a second function, and that is M4's allocation question rather than this state's. It is not a coverage hole — the row is served — but it is a wording strain worth naming before M4 rather than after.

## follow_up

M3'S REMAINING STATES are identify-assumptions and probe-assumptions, and the sweep starts from what these rows lean on rather than from memory.

WHAT THE FLOWS DID NOT CHANGE, and why that is the honest answer. No new flow was minted. The delta changes what flow-surface must CARRY — a refusal's reason, a control's repeat-semantics, a running signal — and not the set of flows crossing the structure. Minting a flow-progress-signal with one producer and no separate consumer would put a one-ended flow into a closure the exit script checks both ways, which is a hole rather than a detail.

WHAT TO CARRY INTO M4: the wording strain above. If the agent-facing half of the refusal row wants its own function, that is an allocation question and it will show up as a second element wanting the same requirement.

## anything_else

