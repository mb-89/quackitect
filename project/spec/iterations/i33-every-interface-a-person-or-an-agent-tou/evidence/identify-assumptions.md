---
form: identify-assumptions
by: agent
signed_off: 2026-08-17T16:53:18.507Z
reopened: 2026-08-17T16:52:15.471Z — derive-functions re-signed above it, and write-requirements gained an eighth row. The sweep walks the requirements, so a new row means the sweep has a source it has not been asked about.
amended: 2026-08-17T14:38:36.979Z by agent — the function state re-signed above it; the sweep opens nothing new
authors: agent
files: null
---

# Evidence form / identify-assumptions

## current_situation

The sweep walked the requirements written at the previous state, per source, and every source got an answer including the ones that got none.

SWEPT FIVE TIMES SINCE, and a measured number opens nothing here. A latency reading is a FACT, not a condition somebody leans on, so it belongs in a gate's reading rather than in this register.

THE SIXTH ROW OPENS NOTHING, which is a result rather than a shrug. req-one-operation-reads-its-input-once leans on whether a collected input can be threaded to every part that reads it. That is a property of OUR OWN ENGINE, and this method's own test separates the two: not controlled is what makes a thing an assumption rather than a decision. We control it, so the threading question is a decision to be made at design, not a condition to be probed.

THE EIGHTH ROW OPENED A CANDIDATE AND THE PROBE KILLED IT, in that order, which is how this state is supposed to work. req-an-amend-leaves-the-tree-standing rests on comparing one signature against another. The engine compares them as strings, and the test suite already records that a claim stamped by a much older engine can carry PROSE there, which does not order against a date. That is a real condition, it is about a corpus we did not fully write, and it has a probe one search long. Run: zero signatures in the whole spec are anything but an ISO timestamp. CHECKED RATHER THAN BELIEVED, so no row is minted.

TWO ASSUMPTIONS WERE OPENED. Both carry a probe that can be run, which is the test for whether a thing is an assumption at all — a worry with no writable probe belongs in the body of a risk instead.

STANDING ASSUMPTIONS WERE NOT RE-IDENTIFIED, only added to. Four already stood from this iteration's earlier states, and probe-assumptions checks all of them rather than only these two.

## assumptions

- raid-asm-every-host-hands-in-every-value-the-panel-can-draw
- raid-asm-the-boundaries-are-few-enough-to-model-one-node-each

## sweep

- environment: ONE OPENED. raid-asm-the-boundaries-are-few-enough-to-model-one-node-each. Both new pass lines take a share over the set of modelled interfaces, and nobody has counted the element-to-neighbour pairs that carry traffic. Four neighbours against an unknown number of elements is a product rather than a list.
- the eighth row, across all six sources: NONE, AND ONE CANDIDATE WAS PROBED AWAY RATHER THAN WAVED. req-an-amend-leaves-the-tree-standing rests on signatures ordering against one another, and the suite records that an older engine could stamp PROSE where a date belongs. Probed at this state with one search over project/spec: zero signatures are anything but an ISO timestamp. A condition that can be checked in one call is checked, and a register that fills with what a search would have settled becomes a list nobody reads
- the seventh row, across all six sources: NONE, and one source is worth the sentence. req-a-breached-bound-is-put-in-front-of-a-reviewer leans on the call log holding enough history to cover the window since the last review. That is not written as an assumption because it has ALREADY BITTEN and is recorded in present tense: raid-iss-the-log-serves-a-cut-response-while-the-guidance-promises-otherwise. Writing it here as something merely believed would hide that it happened
- the sixth row, across all six sources: NONE. What req-one-operation-reads-its-input-once leans on is the shape of our own engine, which is a decision rather than an assumption. No source outside the product bears on whether an operation reads its corpus once
- toolchain: NONE, and the reason is that no row here needs anything installed. The requirements are satisfied by rendering and by timing, both of which the product already does with what it has. No version, no binary and no package appears in any statement or any Detail
- host: ONE OPENED, and it is the source where this delta leans hardest on something nobody here controls. raid-asm-every-host-hands-in-every-value-the-panel-can-draw. A host that draws its own bar and omits a value makes the control render OFF, which is indistinguishable from a refused click, and the engine's own comment records two earlier victims
- platform: NONE, and it was checked rather than waved. No row here is operating-system specific: a reason on a control, a fresh rendered value, a warning before a destructive repeat and a running signal are all platform-neutral. The POSIX-versus-Windows question that runs through this product belongs to i35's container work, not here
- neighbours: NONE NEW, and the reason is that the one real neighbour claim already bit and is therefore an issue rather than an assumption. raid-iss-the-log-serves-a-cut-response-while-the-guidance-promises-otherwise records it in present tense. Writing it here as something merely believed would hide that it already happened, which this method warns against by name
- people: NONE NEW, and two standing risks already carry what would go here. raid-risk-an-honest-slow-interface-becomes-noise-nobody-reads and raid-risk-an-accurate-progress-signal-can-drive-abandonment are both about what a person does with a signal. Neither has a probe that can be written without watching people, which is exactly why each is a risk with the owner rather than an assumption with a probe

## follow_up

probe-assumptions checks ALL standing assumptions next, not only these two. Six stand for this iteration.

THREE HAVE CHEAP PROBES that can run in this iteration:

- The host one is a single read of the VS Code extension's control rendering, and it is the same read that settles the owner's stop-at report.
- The boundary-count one is a walk of the element nodes against four neighbours, and it is milestone one's opening act anyway.
- The phase-split one is four lines, and it decides the shape of milestones three and four.

TWO ARE THE OWNER'S and cannot be probed by an agent: both signal risks need people watched, not code read.

ONE IS ALREADY AN ISSUE rather than an assumption, and probing it means fixing it or withdrawing the promise it breaks.

THE HOST ONE IS THE ONE TO RUN FIRST. If it is falsified it becomes an issue keeping its id, and the fix stops being one host's bug: a panel that reads an absent value as OFF cannot tell missing from off, and that is the same shape three times over.

## anything_else

