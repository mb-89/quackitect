---
form: probe-assumptions
by: agent
signed_off: 2026-08-17T16:54:34.249Z
reopened: 2026-08-17T16:53:29.107Z — identify-assumptions re-signed above it, and one candidate was probed away there rather than minted. This state probes every standing assumption, so it records that result alongside the rest.
amended: 2026-08-17T17:15:41.449Z by agent — This form recorded a probe as HOLDING that had been falsified, and said in as many words that nothing was falsified. Both are corrections of false statements rather than changes to the question the state answers, so the fallout is listed here and each affected form is corrected in its own right.
authors: agent
files: null
---

# Evidence form / probe-assumptions

## current_situation

Every standing assumption is answered, and the input was the RAID folder rather than the state above it — which is what makes this its own state.

THE REDO ADDED NOTHING TO PROBE across five passes. A real breach reading is a measurement, and no standing entry's trigger fires on one. No trigger fired from the goals change either: the goals are what the work is measured against, and no assumption in the register rests on how they are written down.

THE SEVENTH ROW leans on the call log holding its history. That is already an ISSUE in present tense rather than an assumption, so it is not probed here — it is fixed at milestone four.

THE SIXTH ROW was swept across all six sources and opened no assumption, because what it leans on is the shape of our own engine and that is a decision rather than a condition.

THE EIGHTH ROW OPENED A CANDIDATE AND THE PROBE KILLED IT AT THE STATE ABOVE. req-an-amend-leaves-the-tree-standing rests on signatures ordering against one another, and the suite records that an older engine could stamp prose where a date belongs. One search over project/spec: zero signatures are anything but an ISO timestamp. Checked rather than believed, so no row was minted and none is probed here.

ONE WAS PROBED AND FALSIFIED, AND THIS FORM RECORDED IT AS HOLDING FOR MOST OF A DAY. That is the correction, and it is the worst thing in this record. raid-asm-every-host-hands-in-every-value-the-panel-can-draw was probed against ONE host, signed as holding, and credited with eliminating both proposed causes of the stop-at fault. A fresh-eyes tester re-checked both with line numbers and both are dead: a second host does draw its own values, and the bar's refresh is not unconditional. The node had already been converted to kind issue while this form, and its own machine-readable probe field, went on serving the dead answer.

THE SHAPE OF THAT FAILURE IS THE ONE CONTRACT RULE 5 NAMES: the defect was named accurately, in the right place, with the right severity, and then the walk continued past it as though naming were fixing. It survived a re-signing of this very form.

WHAT IT COSTS BEYOND ITSELF: the stop-at search must restart rather than continue from these eliminations, and anything downstream that cited them is resting on nothing.

ONE WAS ANSWERED FROM THE LOG rather than by a fresh run, because the measurement its probe asks for was taken yesterday and the log keeps it.

ONE FORM LINE CONTRADICTED ITS OWN NODE and was corrected here. The green-derivation row read SCHEDULED for M6 in this form while its node carried a full split and a verdict of holds. The node was right, and a form that disagrees with the evidence it points at is worse than a blank.

THE FORM'S CHECK READS THE NODES, NOT THIS FIELD, and finding that out cost two refusals. Each entry now carries `probe:` and `probed:` in its own frontmatter, which is where the answer belongs — a probe result on a form would freeze at sign-off and could never be re-read by a later iteration.

## probes

- raid-asm-every-host-hands-in-every-value-the-panel-can-draw.probe: RAN AND FALSIFIED. A SECOND HOST DRAWS ITS OWN VALUES: engine/render.ts lines 3782 to 3786 build panelValues from rungs, autonomy and ints alone, three of nine. AND THE BAR'S REFRESH IS NOT UNCONDITIONAL: extension.ts line 415 returns early on a null poll, before fetchBar at line 420, and api() returns null on a 2500 ms timeout.
- raid-asm-every-host-hands-in-every-value-the-panel-can-draw.probed: 2026-08-17, status ISSUE. The entry keeps its id and changed kind. THE TWO STOP-AT ELIMINATIONS IT WAS CREDITED WITH NEVER HAPPENED, so that search restarts rather than continuing from where this probe left it.
- raid-asm-the-boundaries-are-few-enough-to-model-one-node-each.probe: count the element-to-neighbour pairs that carry traffic, against the four neighbour nodes; under about thirty the assumption holds.
- raid-asm-the-boundaries-are-few-enough-to-model-one-node-each.probed: 2026-08-17, UNPROBED with its reason — the probe IS milestone one's opening act, and running it here would do the work twice.
- raid-asm-the-conformance-checks-stay-affordable-as-the-corpus-grows.probed: 2026-08-17, UNPROBED AT SCALE and NOT falsified. Its trigger is two seconds or three thousand nodes; the log records the sweep at 351 ms over 1025 nodes on 2026-08-16, so the trigger has not fired. The number at three thousand nodes needs a grown corpus nobody has built.
- raid-asm-the-slow-phase-is-the-green-derivation-repeated.probe: split the machine phase into its four parts and read it against two numbers, drawingSets and stateDetails over 90 percent and the SVG under 50 ms. RAN, and both pass lines are met by a wide margin — 99.5 and 99.7 percent against a line of 90, and an SVG at 0.56 ms against a line of 50.
- raid-asm-the-slow-phase-is-the-green-derivation-repeated.probed: 2026-08-17, status PROBED AND HOLDS. This line said SCHEDULED for M6 until now and the node said probed, which is a form disagreeing with its own evidence; the node was right. A SECOND READING was added today from the other end: one green computation makes 245 reads through the file door over a 200-node corpus, which is one sweep rather than fifteen. The two agree — the derivation reads its input once, and the repetition is the render asking the green question once per state. So the memo on the downstreamCone edge is the fix and a change inside the derivation buys nothing.

## follow_up

THE STOP-AT FAULT IS A NARROW SEARCH NOW RATHER THAN AN OPEN ONE, which is this state's most useful product.

ELIMINATED, WITH LINE NUMBERS:

- A host omitting a value. The VS Code host supplies no values at all; it takes the mirror's rendered markup.
- A permanently stale bar. fetchBar is called unconditionally from pollWalk at line 420, so the markup refreshes every poll and a stale draw cannot survive a session.

WHAT IS LEFT: the POST, or the value behind it. Either the click never reaches /stop-at, or session.stopAtValue does not hold what the press set. Both settle from the LOG rather than the source — a mirror_stop_at record either exists for this morning's press or it does not, and that is the next probe.

ONE WAS FALSIFIED, SO FALLOUT IS OWED AND IT IS LISTED. This line previously read that nothing was falsified, which was untrue when it was written.

WHAT RESTS ON THE DEAD ANSWER, each to be corrected rather than merely noted:

- The two stop-at eliminations. Neither happened, so that search restarts at the host rendering rather than at the POST.
- gate-requirements and gate-implementation both say the two beliefs were killed with line numbers at this state. They were not; this state asserted them.
- req-a-surface-shows-the-state-an-act-produced is unverifiable from the engine alone while a host can draw three values of nine, which is what the entry's own impact line already said.

TWO ENTRIES STAY THE OWNER'S and no reading will move them: both signal risks need people watched rather than code read.

AND ONE FINDING ABOUT THE MACHINE ITSELF, worth a retro rather than a fix here: this state's check reads the assumption NODES while its form field asks the same question, so a correct answer in the form still refuses until the node carries it. The field and the check disagree about where the answer lives.

## anything_else

