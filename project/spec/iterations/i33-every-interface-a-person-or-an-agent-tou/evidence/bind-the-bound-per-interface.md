---
form: bind-the-bound-per-interface
by: agent
signed_off: 2026-08-17T14:00:31.224Z
authors: agent
files: null
---

# Evidence form / bind-the-bound-per-interface

## current_situation

The thirteen modelled boundaries each carry a bound, and the demand behind it is HALF built. The field and the law exist; nothing runs the law yet, and that is stated rather than claimed away.

## built

EACH MODELLED INTERFACE CARRIES ITS OWN BOUND, argued rather than defaulted.

WHAT LANDED:

- machines/items/interface.md: `bound` joined the skeleton, so it is a field of the type. Its default is `inherited` — honest for an in-process crossing, which has no clock of its own and is paid for by the outside call that reached it. That default is what keeps the forty standing internal interfaces conforming, per the ruling that widening a template must not break the corpus overnight.
- the same file gained the doctrine: three answers are legal, and a not-one-second bound is not an exemption but a move of the demand to the honest half.
- engine/trace.ts: outsideBoundaryProblems. An interface with `nbr-` at either end may NOT inherit.
- engine/elematrix.ts: the element matrix now knows a neighbour is a legal end. It is element-to-element by construction, so it reported all thirteen boundaries as naming an end no element carries — the law being narrow rather than the nodes being wrong. Both ends outside is still a defect.
- dsp-the-outside-boundaries-and-their-bounds realizes all thirteen, because specify-build's law refuses an interface no design spec realizes.

THE SPREAD ACROSS THE THIRTEEN: five say ONE SECOND, six say NOT one second with a reason, one says NONE. A flat rule would be unkeepable at six of them.

THE LAW DOES NOT BITE YET, AND THIS IS THE FINDING RATHER THAN A CAVEAT. conformance() runs at form submit over the nodes a form field references, and no evidence field enumerates interfaces. PROVEN, NOT SUSPECTED: a deliberately non-conforming node was written, the engine reloaded, and the full battery run — 1399 pass, 0 fail, sweep green, with the bad node in the corpus. The probe was then deleted. Saying it bites would have been fabrication, and the only reason I know it does not is that I wrote a bad node and looked.

ONE MORE THING THIS CHUNK COST, and it is the cleanest evidence in the iteration. Fetching each claim's signature in a second pass over the same files put recordDone at 1117 ms over 200 nodes against a 1000 ms budget. THIS ITERATION'S OWN ONE-SECOND RULE CAUGHT THIS ITERATION'S OWN CHANGE, and the fix was this iteration's own new requirement: take the time out of the read that already happens, in standingClaims. Green at 1399.

## follow_up

ONE THING BLOCKS THE OTHER HALF, and it needs measuring before it is decided.

THE SWEEP SHOULD RUN CONFORMANCE over every node, so a shape law binds whether or not a form happens to mention the node. Captured as note-29960c805dc0.

WHAT IS UNMEASURED: how many standing nodes that would name. Two attempts to count it failed on loadTrace's argument, so the blast radius is genuinely unknown — zero or hundreds. Turning on a check that names hundreds of standing findings is a scope call rather than a build step, so it is presented rather than taken.

UNTIL THEN the thirteen bounds are authored and correct, and nothing stops a fourteenth being written without one.

## anything_else

