---
form: gate-validation
bless: blessed by agent
by: agent
signed_off: 2026-08-17T18:31:45.844Z
authors: agent
files:
---

# Evidence form / gate-validation

## current_situation

THE ACCEPTANCE GATE, and the bless here IS the sign-off. There is no second artifact.

WHAT STANDS BELOW IT: twenty-three states green, the implementation gate blessed by the owner, the battery green at 1402 tests with 0 failures, the consistency sweep signed with four of nine surface classes carrying a repair, and the demo drawing authored — the first one any iteration has written rather than left as the seeder's placeholder.

THE HARD ANSWER IS musts_demonstrated, and it is zero of three. Every must story needs a person, and this walk cannot produce one. That is stated below rather than argued around.

## meets_need

- vp-rigor-without-toil: SERVED, AND THE SERVICE IS HALF BUILT. The one prop this delta touched. It gained a criterion — a slow interface tells the person — with two zero-target pass lines, and it gained the DENOMINATOR that made i12's standing criterion computable in principle: thirteen outside boundaries now exist as nodes where none did, nine timed directly. WHAT IT DID NOT GAIN is a reading over that denominator. Twelve of the thirteen have no calls attributed to them, so the share the criterion asks for cannot be taken yet
- vp-autonomy-range: UNTOUCHED. This delta wrote no autonomy behaviour. It read the dial to decide which gates were the owner's and changed nothing about how the dial works. Untouched is fine here because the delta's subject is how fast an interface answers, which is orthogonal to how much an agent may decide
- vp-qualities: UNTOUCHED AS A PROP, and this is the closest call of the six. The delta adds two quality requirements and a quality scenario, so it FEEDS this prop's subject. It does not extend the prop itself: no criterion was added or changed on it, and claiming service because the work was quality-shaped would be the fabricated coverage this field warns about
- vp-systematic-engineering: UNTOUCHED. The method gained a goals list and two gate rounds, which is machinery this prop is about — but again no criterion moved. The honest line is that the delta made the method ask one more question, and whether that serves the promise is a judgment nobody has made against this prop's own pass lines
- vp-the-engine: UNTOUCHED. Engine code changed under goal five, which is engine improvement rather than service to this prop's promise. No criterion on it was tested or extended
- vp-the-ledger: UNTOUCHED. The record gained evidence and register entries, which is the ledger doing its job rather than the ledger being improved. Nothing here changes what the ledger promises
- vp-vendoring: UNTOUCHED, and nothing in this delta comes near it. No bootstrap path, no second product, no packaging

## musts_demonstrated

- sty-the-control-that-says-why-it-declined: NOT DEMONSTRATED, and this delta authored it. No report exists because no run happened. It needs a person at this machine to press a control that declines and watch what the surface says, about an hour. Step one of machines/demos.md, and it sits in raid-debt-ten-checks-wait-on-a-person-or-a-second-machine
- sty-the-slow-call-that-says-it-is-working: NOT DEMONSTRATED, and it cannot be agent-run even in principle. Its pass line is about what a person PERCEIVES while waiting, and no instrument reads that. It needs people watched side by side. Step two of the drawing
- sty-the-call-that-comes-back-inside-a-second: NOT DEMONSTRATED, and it is the closest to runnable. A clock and a list answer its pass line with no watcher. What blocks it is the list — twelve of thirteen boundaries have no calls attributed to them, so a run today would read one crossing and report it as the whole. Step three of the drawing. THE COUNT FOR THIS DELTA IS ZERO OF THREE, and the verdict says so rather than leaving it to be added up
- sty-a-check-binds-without-engine-code: UNTOUCHED BY THIS DELTA, and its demonstration status is unchanged. It stands where the standing register entry for human-observed demonstrations left it
- sty-answer-what-does-this-touch: UNTOUCHED, status unchanged, same standing entry
- sty-ask-the-lane-what-it-can-do: UNTOUCHED, status unchanged, same standing entry
- sty-carry-a-finding-without-stopping: UNTOUCHED, status unchanged, and it is named in the debt entry as needing a second machine
- sty-dispose-a-candidate-coupling: UNTOUCHED, status unchanged, same standing entry
- sty-hand-over-and-walk-away: UNTOUCHED, status unchanged, same standing entry
- sty-ramp-up: UNTOUCHED, status unchanged, and it needs real newcomers rather than a run
- sty-review-a-gate: UNTOUCHED AS A STORY, though this walk exercised gate review eleven times over. That is USE rather than demonstration, and calling it one would be exactly the substitution this field exists to refuse
- sty-start-a-new-product: UNTOUCHED, status unchanged, same standing entry
- sty-the-agent-proves-it-read: UNTOUCHED AS A STORY, and the same caveat applies — the reading proof fired during this walk, which is use and not a demonstration under its own procedure
- sty-the-write-refuses-the-break: UNTOUCHED AS A STORY, and the same again — four refusals landed during this walk, which is the behaviour working rather than its demonstration being run
- sty-walk-it-by-hand: UNTOUCHED, status unchanged, and it needs a person driving
- sty-work-on-two-machines: UNTOUCHED, status unchanged, and it needs a second machine that does not exist here

## market_tier

not a market iteration — this is an internal minor against a product with one user, so the expensive real-world tier does not apply and is not claimed. Naming it here rather than leaving the field blank, because a blank reads as an oversight and this is a ruling.

## round_0_verify

- evidence vs claims: HELD, and the mechanical checks did the holding rather than my judgment. Four refusals landed during this walk and each named a real defect: a function node that did not carry the edge a form claimed, two runners no design spec claimed, an amend that would have changed a question rather than a wording, and a checklist that wanted its items bare. A fresh-eyes tester then broke six more things after the implementation gate had already stamped
- types: RUN AND GREEN inside the battery, over engine source that changed today
- lint: biome clean over 274 files, no suppression added. One complexity ceiling was hit and refactored out rather than silenced
- tests: 1402 tests, 134 suites, 0 failures, plus preflight and a corpus sweep over 1226 nodes. Sixteen of those cases are this iteration's, and the newest asserts the half of the amend rule that nothing covered until the tester named it

## round_1_validate

- exercised against the goal: PARTLY, and the split has not moved since the implementation gate. The modelling half is built and measured; the fixing half moved twice and was never re-read in aggregate
- missing: the demonstrations, all three, and the aggregate latency reading. Both are named in the register with what each needs
- wrong: four beliefs were falsified during this walk, three of them mine and one of them the engine's own behaviour. That a host hands in every panel value — false, and it stood recorded as holding for most of a day. That the control bar refreshes unconditionally — false. That an amendment should re-grey the chain — overturned by the owner, twice. And that a door-access count catches a per-state corpus load — false, because the corpus memoizes above the door
- out of scope: the demonstrations. They are not deferred because they are hard; they are deferred because an agent cannot perform them, which is a different reason and the register records it as such
- prior art: NOT SCANNED AT THIS GATE, and that is a gap rather than a blank. The two gates below scanned latency budgets and requirement-set quality. What nobody compared is how other systems decide a piece of work is ACCEPTED when its acceptance tests cannot be run — which is exactly this gate's problem, and the comparison would have been useful here

## goals_served

- model the outside boundaries: BUILT AND UNCHANGED SINCE THE LAST GATE. Thirteen interface nodes, realized by dsp-the-outside-boundaries-and-their-bounds
- bind the one-second demand to them: BUILT AND MEASURED. Nine timed directly; if-record-store-to-origin-remote's bound was corrected by its own measurement
- instrument every interface: BUILT AS A DEMAND. req-a-breached-bound-is-put-in-front-of-a-reviewer, and the bound_breaches round on every gate in this column including this one
- fix what the numbers name: PARTLY, and it is still the thinnest. Two fixes, no aggregate re-reading
- engine improvements: BUILT, AND ONE MORE LANDED AFTER THE LAST GATE. The corpus now meters its own asks, so the green guard asserts one ask per operation rather than counting door reads that a memo above the door hides. That fix exists because a tester falsified the guard's own claim about itself

## bound_breaches

- if-agent-harness-to-entrypoint: BREACHED AGAIN DURING THIS WALK, and twice in a way worth recording rather than counting. Two pulls TIMED OUT entirely on the hop into the implementation gate, because that hop's exit script fires the full battery — about a minute of work behind a single call that the harness gives up on. THE DISPOSITION: it is the honest kind rather than a defect. A battery is a minute by design and the state that fires it says so. What is NOT honest is that the call gives no sign it is working, which is this iteration's own second half failing on this iteration's own walk. It is the sharpest live example the record has of the demand it was written for, and it belongs to whoever takes the streaming work. THE STANDING FIGURE of at least 181 calls over bound is unchanged and remains a floor: se_log_query drops matching records without saying so

## round_2_red_team

- STEELMANNED OPPOSING CASE: this gate should FAIL. Its own guidance says the bless IS the acceptance, and zero of three must stories have been demonstrated. A gate that accepts on argument when its own acceptance evidence is missing has redefined acceptance to mean something cheaper, and every future iteration inherits that reading => THE STRONGEST ANSWER IS NOT THAT THE ARGUMENT IS WRONG. It is that a fail sends work back, and the work it would send back cannot be done by anything that walks this machine. A fail here would be a blocked walk dressed as a judgment. The right shape is a pass whose override is loud enough that a reader cannot miss it, plus a register entry with an owner and a trigger — which is what stands
- SO THE OVERRIDE IS DOING WORK A FAIL SHOULD DO, and it will be ignored the way overrides are => conceded, and the mitigation is that the demonstrations are ALSO a drawing now. machines/demos.md is the first one any iteration has authored, one step per must story, and run-demos runs it. A drawing is harder to walk past than a paragraph
- THE TESTER FOUND SIX SEVERE THINGS AFTER THE PREVIOUS GATE STAMPED, so what did this gate's own rounds miss => a fair question and the honest answer is: unknown, because no tester ran against THIS gate. The one that ran was briefed on the implementation gate's subject. That is named rather than papered over
- THE ITERATION CORRECTED ITS OWN ENGINE MID-WALK, twice, on rulings given while it was walking => true, and both landed outside the chunk plan. specify-build names it. Whether a mid-walk ruling should be allowed to land without re-planning is the retro's question and not this gate's
- KILL CRITERION for this gate, named and looked for => this acceptance is wrong if the demonstrations, once run, would FAIL. Looked for by reading what each story asserts against what shipped: the control story's demand is built and its test is green, the slow-call story's signal is built and its non-intrusive half is asserted, and the speed story's clock reads what it reads. NOT FOUND, and the honest qualifier is that finding it this way is exactly the argument-instead-of-evidence this gate is accepting on

## raid_additions

- none

## verdict

pass with overrides — STAMPED BY THE AGENT, UNBLESSED, and this bless is the acceptance itself rather than a formality on top of it. THE OVERRIDE, and it is the whole of the dissent: ZERO OF THREE MUST STORIES ARE DEMONSTRATED. This gate is accepting on argument where its own guidance asks for the report of a real run. WHY NOT A FAIL: a fail returns work, and the returned work cannot be performed by anything that walks this machine — two of the three need a person at this screen and one needs people who are not us. A fail would be a blocked walk wearing a judgment's clothes. WHAT MAKES THE PASS DEFENSIBLE RATHER THAN CONVENIENT: the demonstrations are a drawing now rather than a paragraph, machines/demos.md, one step per must story, and they carry an owner and a trigger in raid-debt-ten-checks-wait-on-a-person-or-a-second-machine. WHAT SHOULD PRESS BACK: a reviewer may hold that acceptance without acceptance evidence is not acceptance, and that reading is correct on this gate's own words. The thumb is the owner's and this is the decision it is for.

## follow_up

WHAT SHIPS, and what does not.

SHIPPING: thirteen modelled boundaries with argued bounds, eight requirements, three stories, six test-specs, a goals list that every gate measures against, two new gate rounds, an amend rule that no longer re-greys the tree, a green guard that counts the thing it names, and two inspection runners in the boot.

NOT SHIPPING, and named so nobody discovers it later: any demonstration of any must story, and any aggregate proof that a call is faster than it was this morning.

WHAT THE NEXT ITERATION INHERITS — the same four the implementation gate named, unchanged: the attribution gap on twelve boundaries, the log query that omits matching records, the one-act chain refresh that does not exist, and two boundaries an agent cannot time.

AND ONE THIS GATE ADDS: the pull that times out on the hop into a gate. It fires the battery behind a single call, gives no sign it is working, and the harness gives up. It happened twice in this walk. It is the clearest live instance of this iteration's own demand that the record contains, and the streaming work owns it.

## anything_else

