---
form: verification
amended: "2026-08-17T17:19:52.110Z by agent — This form's headline finding described the pre-fix world and was carried forward unchanged through a re-signing, so it contradicted three forms signed before it. The finding is kept as evidence and dated; the current state is stated beside it. Found by a fresh-eyes tester."
by: agent
signed_off: 2026-08-17T17:06:15.161Z
reopened: "2026-08-17T17:05:45.856Z — trace-design re-signed above it after the dead-code sweep caught two unclaimed runners, and the whole chain above that re-signed today carrying the kickoff's five goals. The battery this state fires must run against the tree as it now stands rather than as it stood at 12:42."
authors: agent
files:
---

# Evidence form / verification

## current_situation

THE WHOLE CHAIN ABOVE THIS STATE WAS RE-EARNED TODAY, twenty states, one submit each. The kickoff's one prose goal became a list of five — a changed question, which is a reopen — so every claim below it had answered an older question and had to answer this one.

IT WAS NOT A REWRITE. Each state came back as a recheck with its text and signature intact. Bodies moved only where something in them had gone false.

SIX THINGS HAD GONE FALSE, and they are the argument for the pass rather than against it.

- gate-requirements and gate-motivation both claimed tests were not re-run "because nothing since has touched engine code". Engine code had changed. Both now carry a run: 1401 tests, 0 failures.
- write-stories listed three stories and said TWO in two other fields, and the third had no reason for its demonstration.
- probe-assumptions said an assumption was SCHEDULED for M6 while its own node carried a full split and a verdict of holds.
- Goal five had no substance under it at either gate.
- Three states carried stacked RECHECKED paragraphs, four deep.
- Two register entries went stale the same day they were written, both about runners that had since been built.

THE MACHINE REFUSED ME THREE TIMES ON THE WAY, which is worth more than the six.

- derive-functions refused because the form said a requirement landed on judge-a-claim while no function node said so. Prose is not the edge.
- trace-design refused with a dead-code finding: bin/prose-inspect.ts and bin/record-inspect.ts, both written this iteration, claimed by no design spec.
- identify-assumptions had a candidate assumption probed away rather than minted, because the probe was one search long.

A SECOND TESTER ROUND IS IN FLIGHT at this submit, with fresh eyes, briefed to attack the amend rule, the rewritten green guard, the goals_served ids and cross-form consistency. Its findings land in fix-findings, which is the path this state's fallback exists for.

WHAT THE BATTERY CANNOT COVER IS UNCHANGED FROM THE LAST ROUND: the cases prove renderPanel given values. They prove nothing about which values a host hands in, nothing about the CSS, nothing about timing, and nothing about the served concatenation.

## claims

- [owed] tsp-a-slow-signal-keeps-the-wait — raid-debt-ten-checks-wait-on-a-person-or-a-second-machine
- [owed] tsp-autonomy-tiers — raid-iss-the-autonomy-number-still-rides-every-answer
- [owed] tsp-bound-surface — raid-iss-whole-product-claims-reverified-by-every-record
- [owed] tsp-coupling-disposition — raid-iss-whole-product-claims-reverified-by-every-record
- [owed] tsp-derivation-analysis — raid-issue-the-corpus-wide-inspections-have-no-runner
- [owed] tsp-desk-and-gates — raid-debt-ten-checks-wait-on-a-person-or-a-second-machine
- [owed] tsp-first-run — raid-debt-ten-checks-wait-on-a-person-or-a-second-machine
- [owed] tsp-panel-walkthrough — raid-debt-ten-checks-wait-on-a-person-or-a-second-machine
- [owed] tsp-prose-inspection — raid-issue-the-corpus-wide-inspections-have-no-runner
- [owed] tsp-read-back-inspection — raid-issue-the-corpus-wide-inspections-have-no-runner
- [owed] tsp-record-inspection — raid-issue-the-corpus-wide-inspections-have-no-runner
- [owed] tsp-tour-run — raid-debt-ten-checks-wait-on-a-person-or-a-second-machine
- [owed] tsp-two-machines — raid-debt-ten-checks-wait-on-a-person-or-a-second-machine
- [owed] tsp-unattended-start — raid-debt-ten-checks-wait-on-a-person-or-a-second-machine

## follow_up

ELEVEN FINDINGS, COLLECTED WHOLE BEFORE ANYTHING IS FIXED. Three are severe and two of those falsify something signed.

### Severe

- F1. A SECOND HOST DRAWS THE SAME PANEL AND GETS THREE VALUES OF NINE. engine/render.ts builds its own panelValues with rungs, autonomy and ints only — no stopat, stop_at, emergency, toggles, texts, choices or running. So on the mirror's own bar an armed engine draws a plain rung and the running signal never appears, which is the exact failure set this delta was written from. It FALSIFIES raid-asm-every-host-hands-in-every-value-the-panel-can-draw, which I probed on one host and signed as holding. Already converted to kind issue, id kept, with the repair written.
- F2. THE CSS IS IN THE SOURCE AND NOT IN THE SHIPPED BUNDLE. vscode/extension.js carries zero hits for .running or .rung.unknown while holding eight other .rung rules, and package.json names extension.js as main. engine/render.ts's own stylesheet lacks both too. Two signed evidence forms claim the DOM leg landed; both are true of the source and false of what runs.
- F3. NOTHING MEASURES THE BOUND AND THE MECHANISM HAS NO BOUND. Neither case reads a clock. runningJob compares nothing and reports a 5 ms job like a 5 minute one. Worse, the job registry holds only shell children, so the slow lane calls the requirement's own scenario cites can never be reported by it at all.

### The rest

- F4. The running signal renders TWICE on the sidebar, because mirror.ts concatenates two renderPanel calls and params.ts appends renderRunning to every panel.
- F5. The payload seam guard ALREADY EXISTS at tests/params.test.ts and `running` is simply absent from its key list. My stated reason for skipping it was wrong: the fix is one array element, not a booted server.
- F6. The absent-is-not-zero case asserts only difference, not legibility. Blank the message and it still passes, because the class alone makes the renders differ.
- F7. The stop-at floor notch promises a release it cannot perform — its title says click to release while the bank's own comment says that notch cannot be released.
- F8. fetchBar is NOT unconditional. api returns null on a 2500 ms timeout and pollWalk returns early, so the bar stops refreshing exactly when the engine is slow. The word carried an elimination in a signed probe and is wrong.
- F9. The non-intrusive case asserts contiguity, not non-intrusiveness. A full-screen modal appended after the panel passes it.
- F10. Five silent-break points in params.ts, including a dead fallback branch and an unasserted esc on an arbitrary shell string.
- F11. Two signed forms miscount a case, and observe-red's tick on the demonstration means deduced-red rather than watched-red.

### The bigger finding, and it is about the machine

THE BUILD PLAN WAS NEVER CHECKED AGAINST THE SCOPE, AND THAT IS WHAT THIS FINDING WAS FOR. This iteration's signed scope names four milestones — model the boundaries, bind the demand, instrument per interface, fix what the numbers name. When this was first written its four build chunks implemented NONE of them.

THE COUNT BELOW IS THE PRE-FIX WORLD AND IS KEPT AS THE FINDING'S EVIDENCE. It is no longer the current state: eleven chunks are stamped, all four milestones have chunks, and two fixes landed. A later re-signing of this form carried the old sentences forward unchanged, which a fresh-eyes tester caught — a finding that outlives its own repair reads as a live defect.

EVERY OTHER COVERAGE HERE IS CHECKED BOTH WAYS AND REFUSES: requirement to use-case, function to requirement, element to function, spec to element. Milestone to chunk is checked by nobody, so a scope of four and a build of none passed every gate.

THE SPEED HALF WAS UNBUILT WHEN THIS WAS WRITTEN. What is true now is narrower and worse-sounding said plainly: two fixes landed against the lane door, and NO AGGREGATE RE-READING WAS TAKEN after them, so nothing demonstrates the system is faster. The instrument that would show it drops matching records without saying so. gate-implementation carries that as its override rather than as a clean pass.

## anything_else

FOURTEEN OWED AND NONE CHECKED, and that is the honest count rather than a poor showing.

EVERY ONE OF THE FOURTEEN NEEDS SOMETHING THIS WALK CANNOT PRODUCE. Four need a person watching a live session. Four need a corpus-wide inspection nothing runs. One needs a second host that does not exist. One needs the launch path i34 switched off. One needs people watched side by side. The rest are whole-product claims no single record re-verifies.

NONE OF THE FOURTEEN IS THIS DELTA'S. This delta is verified by TEST, and the findings above are what the fresh eyes returned on it.

### A finding about the checklist itself

tsp-bound-surface IS STILL ENUMERATED HERE and it was DROPPED this morning on the owner's ruling. It carries `status: dropped`, its debt is `decided`, and its own body says no walk owes it.

THE ENGINE STILL DEMANDS A BOX FOR IT. So a spec that was deliberately killed keeps costing every future verification a line, and the only honest line available is an owed one pointing at a generic whole-product entry — which reads as work outstanding when the truth is that the work was cancelled.

WHAT IT ASKS FOR: the checklist should read `status` and drop a dropped spec, the same way the survey drops a shipped record.

### What the tester changed about my own account

IT CONTRADICTED ME IN BOTH DIRECTIONS, which is the mark of a real one.

AGAINST ME: a second host exists and I signed a probe that never opened it; and the CSS I recorded as landed is in the source and not in the shipped bundle.

IN MY FAVOUR: the payload seam guard already exists and wanted one array element rather than the booted server I claimed it needed; and my word "unconditionally" about the bar's refresh was wrong in a way that made an elimination less safe than I had written it.

A VERIFICATION THAT ONLY AGREED WITH THE BUILDER WOULD NOT HAVE BEEN ONE.
