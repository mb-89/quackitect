---
form: observe-red
by: agent
signed_off: 2026-08-17T16:59:04.733Z
reopened: "2026-08-17T16:58:33.189Z — specify-build re-signed above it, and author-tests added a sixth spec. The red check reads every test-spec minted in this record, so it must run against the set as it now stands."
amended: "2026-08-17T17:20:19.024Z by agent — This form said the build begins with four chunks, hours after eight were stamped, and it was re-signed carrying that sentence. The four are kept as the original plan because their ordering is the finding, and the current count is stated beside them."
authors: agent
files:
---

# Evidence form / observe-red

## current_situation

RE-READ FIVE TIMES, and the answer does not change for the specs that already stood.

SIX SPECS NOW STAND FOR THIS DELTA — five test-method and one demonstration. Three of the six are named here plainly rather than swept in, because each departs from test-first in a different way and this is the state that exists to catch exactly that.

tsp-one-operation-reads-its-input-once IS GREEN, NOT RED. Its build landed first — the duplicate green pass in render.ts was removed while fixing the ripple, and the case was written afterwards to hold the property. A green-from-birth check is what this state exists to catch, so it is named instead of hidden.

tsp-a-breached-bound-reaches-a-reviewer HAS NO CASE YET, by the ordering the scope fixed. Its row says every instrumented interface, and the interfaces are not nodes until model-the-boundaries lands. A case written now would assert against a hand-typed list. The claim is defined in the spec ahead of its case, the way tsp-claims-and-drift does for the panel naming a fallen condition.

tsp-an-amend-leaves-the-tree-standing IS ALSO GREEN FROM BIRTH, and its reason is the plainest of the three. The owner ruled mid-walk that an amendment must not re-grey the chain below it, overturning behaviour built earlier in this same iteration. The fix and its test were written together, after the ruling. Test-first was not followed and could not have been: nothing knew the rule until it was given.

WHAT THAT COSTS, said rather than glossed. A test written beside its fix has never been seen to fail for the right reason. This one is stronger than most of that class because its FIXTURE is the finding — it signs a whole chain, where every amend test before it signed one state and could not tell "leaves the tree standing" from "has no tree". But it is still a green-from-birth check, and the honest place for that fact is here.

THE CHECKLIST HOLDS EVERY NON-TEST SPEC IN THE CORPUS, fourteen of them, and each is claimed deliberately rather than swept. ONE had its red observed. THIRTEEN cover standing behaviour this delta does not remove, so red is impossible for them and that is accepted.

## red_observed

- [x] tsp-a-slow-signal-keeps-the-wait
- [x] tsp-autonomy-tiers
- [x] tsp-bound-surface
- [x] tsp-coupling-disposition
- [x] tsp-derivation-analysis
- [x] tsp-desk-and-gates
- [x] tsp-first-run
- [x] tsp-panel-walkthrough
- [x] tsp-prose-inspection
- [x] tsp-read-back-inspection
- [x] tsp-record-inspection
- [x] tsp-tour-run
- [x] tsp-two-machines
- [x] tsp-unattended-start

## follow_up

THE BUILD BEGINS — AND BY THE TIME THIS FORM WAS LAST SIGNED IT HAD LONG BEGUN. Eleven chunks are stamped, not four. The four below are the ORIGINAL plan, kept because the order they set is the finding: the riskiest thing runs first and can invalidate what comes after. The seven added on the redo carry the milestones and are enumerated at specify-build. A fresh-eyes tester caught this sentence still reading as though nothing had been built.

- CHUNK ONE, the phase split. A measurement rather than a change to behaviour, settling raid-asm-the-slow-phase-is-the-green-derivation-repeated. If the cost is concentrated in one call rather than spread across states, the DAG is the wrong instrument and milestones three and four are aimed wrongly — which is why it runs before anything rests on it.
- CHUNKS TWO AND THREE fan out. Each touches params.ts in a different function and neither depends on the other.
- CHUNK FOUR waits on chunk three, because both change what the values contract carries and running them together would put two edits on one boundary.

WHAT WOULD MAKE THIS STATE'S RED A LIE: a case green from birth, a file that does not exist, or a run with no TAP summary. The engine checks all three at this submit rather than trusting the checklist, which is the habit this state was rebuilt to remove.

## anything_else

THE CLAIM BEHIND EACH CHECK, one line per spec, because the checklist itself carries only the tick.

RED OBSERVED, one spec:

- tsp-a-slow-signal-keeps-the-wait — its procedure cannot run today for the reason its own text names. Step 2 asks for two builds of the same surface, one showing the running signal, and nothing carries a running signal to the panel because that is chunk four, unbuilt. The procedure fails at its second step.

RED IMPOSSIBLE AND ACCEPTED, thirteen specs, each covering behaviour that stands today:

- tsp-bound-surface — and the reason arrived today. It was DROPPED on the owner's ruling of 2026-08-17, carries status dropped, and its debt is settled as decided. There is no procedure left to fail.
- tsp-autonomy-tiers — the autonomy rungs as they stand. This delta shares renderRung with that bank and only ADDS to what a locked rung says. Nothing this spec asserts is removed.
- tsp-panel-walkthrough — the panel as it stands, and the delta adds to the control bar without taking anything away. It also needs a person watching.
- tsp-coupling-disposition — standing behaviour in the disposer, untouched by a delta that edits the control bar.
- tsp-derivation-analysis — a corpus-wide inspection over standing nodes. This delta adds nodes and removes none.
- tsp-desk-and-gates — standing behaviour at the desk and the gates, neither touched.
- tsp-first-run — a newcomer's first session, needing real newcomers rather than a run.
- tsp-prose-inspection — an inspection over standing prose. This delta writes prose the inspection will judge later; it does not make standing prose fail.
- tsp-read-back-inspection — standing behaviour of the read-back, untouched.
- tsp-record-inspection — standing behaviour over records, untouched.
- tsp-tour-run — the tour as it stands, needing a person, untouched.
- tsp-two-machines — it needs a second host that does not exist, which its own body says. Unchanged by this delta and not this delta's red.
- tsp-unattended-start — its subject is the launch path i34 switched off, so the procedure cannot be run at all. Recorded before this delta and untouched by it.
