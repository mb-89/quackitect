---
id: e32-fix-the-engine-fix-bundle-the-day-s-bucket-a
kind: fix
status: open
opened: 2026-08-02T17:54:58.546Z
goal: "THE ENGINE-FIX BUNDLE — the day's bucket after the retro of 2026-08-02, seeded on the owner's word. Small, live-found defects, bundled deliberately; the iterations kickoff runs tomorrow in a fresh session. AT ENTRY, sweep the parked notes whose ready-when names this bundle, and re-drain each as its fix lands.\n\nJOB 1 — ENCODING, FIRST (owner priority, 2026-08-02: line-end and BOM trouble still happens too often; implement the mechanical fix and continue). The range verb counts lines with the reader's own splitter (note-35991f95ed54). Then sweep the other encoding seams — BOM tolerance, CRLF auto-correct coverage — and make each correct-and-announce rather than refuse.\n\nJOB 2 — THE UPDATE LANE APPLIES ITS OWN SPLIT. 174 of the window's 505 failures are SE-C-120 alone, and the refusal already computes the corrected plan before throwing it away (note-9a612bcaa728, decisions.ts:228). Apply the split, announce it, refuse only the ambiguous.\n\nJOB 3 — TEST PAIN (owner field feedback: still not happy with duration). The reaper for test runs that outlive their session (note-330609f32d3e). The se_test job handle for runs that outlive the call (note-cc2565c04afe). The booted template root that cuts the per-case boot walk (note-2b63b3266a46). The engine-to-test map follow-up (note-f89785d6364a).\n\nJOB 4 — SMALL LANE FIXES. The land gate stops counting files the landing branch already ignores (note-75113557e1b5). A decision node from an earlier visit becomes resolvable, or the leave gate counts only the current visit (note-92d1054f70ad). The survey windows or drops its backlog payload (note-f27d4b29d8ac). se_web_fetch pages large results (note-3ef0c0deff64). The mirror answers an unknown POST with 404 (note-f68465ddd2a1).\n\nJOB 5 — THE VS CODE SHELL, folded in on the owner's word at the retro. The launcher gets a real activation event and refuses a second agent (note-add5e8c8e08b, note-f9c432c4ecf3). The hard-coded second control bar goes, so one renderer draws the controls (note-e5b3243b5510, note-4c07f556ec41). The hot-swap copy: RUNME.ps1:351 still copies the extension into the user profile; a folder load or junction replaces it (note-68d8f764347e). The layout items needing the owner beside the screen stay parked.\n\nOUT OF SCOPE: the iterations kickoff (tomorrow, fresh session). The render rounds (they need the owner's choices). The desk-teeth proposal (waits for the owner's green light)."
---

# e32-fix-the-engine-fix-bundle-the-day-s-bucket-a

Free prose — the human head of the record. Machine-facing fields stay in the frontmatter.
