---
id: e31-fix-the-last-expedition-before-the-iteration
kind: fix
status: open
opened: 2026-08-02T13:45:40.014Z
goal: "THE LAST EXPEDITION BEFORE THE ITERATIONS LANE. It is the day's bucket and it says so — four jobs, bundled deliberately, because the iterations lane is what comes next and this clears the runway.\n\nJOB 1 — LAND workspace/_incoming/lane-verbs.patch.\nOne commit from the cowork session of 2026-08-02, 6 files, +1356/-72, grounded in a harvest of 17,359 lane calls where 2,589 were se_run and 46% of those were shell standing in for a missing lane verb. It grows se_file_patch (append, prepend, regex replace, line-range replace, CRLF auto-correct), grows se_file_search (context lines, include globs, count_only), adds engine/discipline.ts (a warn-then-refuse ladder when a shell command does a lane tool's job), makes se_test scoped with structured results, and adds se_run {job, wait_ms}.\nVerified on Linux only: 30/30 new tests green, zero new tsc errors. Windows, the battery, and preflight are unverified — this expedition verifies them.\nSeams to check first: tools.ts was diffed against the 2026-08-02 morning state, so the se_run, se_test, se_file_patch and se_file_search blocks may fuzz. If SE-C-129, SE-C-130 or SE-C-131 are already claimed, renumber across errors.ts, discipline.ts and tests/discipline.test.ts.\nRESTART THE SE SERVER after landing — the running server holds the old tool surface.\n\nJOB 2 — KILL THE WORKSPACE FOLDER AND THE COPY DANCE.\nOWNER RULING 2026-08-02, answering the three problems recorded in note-9304878a3906.\nThe opened folder becomes project/. The current workspace/ files move in, with the host dot-config at product's top level because the hosts look for it there (.claude, .copilot, .vscode, .mcp.json, AGENTS.md), and the rest in a subfolder (_cage, the UI sketch, _incoming).\nRUNME stays, and stays named RUNME for convention's sake. It becomes an INSTALL script the owner runs ONCE: it installs the extension and opens VS Code, and after that they never touch it again — unless a new dependency arrives that the extension cannot install itself. Everything RUNME does per launch today (mirroring the extension into ~/.vscode/extensions, two npm installs, re-copying the cage configs) either moves into the extension or dies. The extension already places the cage files, already runs npm install and already spawns the engine on activation, so this is mostly deletion from RUNME.\nThe copy of the extension into ~/.vscode/extensions is the reason it cannot hot-swap (note-68d8f764347e). Kill the copy if a folder load or a junction serves.\nThe restart rule gets written down where a person reads it, and better, made mechanical: a surface running older code than the disk should say so.\nOWNER'S ANSWERED QUESTION, carried: the shape generalises to other projects, because the cage is folder-scoped. The one thing that does not is the extension reaching the engine at <root>/project/deliverable/engine/bin/se-mcp.ts. That belongs to the parked project-folder-shape note (note-916094043ca4) and is NOT in scope here.\n\nJOB 3 — SWEEP THE INBOX.\n77 pending notes and 44 parked items stand open. Drain the mechanically dead ones with a reason each. Implement the small settled ones while in here, and drain each as it lands. Anything needing carried or backlog judgment waits for a retro, because the engine holds those for the retro alone.\n\nJOB 4 — FOLD IN THE RULINGS AND CLEAR _incoming.\nSix owner rulings ride in workspace/_incoming/HANDOVER.md and belong in guidance: correct what is mechanical and announce it, one table three outputs, ship at warn and block on evidence, the battery is earned, the engine carries the bookkeeping, and QA as a peer role rather than a subagent.\nCONFIG, per the owner 2026-08-02: adopt alwaysThinkingEnabled and MAX_THINKING_TOKENS from claude-settings.proposed.json. Do NOT pin the model to opus, and do NOT run the A/B comparison. Build the voice.md projection — the extension generates the output style from project/guidance/voice.md so voice stays single-source and the style cannot drift.\nMove i1-prep to where iteration i1 can find it, then delete _incoming.\n\nOUT OF SCOPE, ruled 2026-08-02: iteration i1 stays open with its worktree and its ambiguity. The owner decides its fate at the iterations kickoff, not here."
---

# e31-fix-the-last-expedition-before-the-iteration

Free prose — the human head of the record. Machine-facing fields stay in the frontmatter.
