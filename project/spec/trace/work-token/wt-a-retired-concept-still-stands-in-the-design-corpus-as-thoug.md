---
id: wt-a-retired-concept-still-stands-in-the-design-corpus-as-thoug
type: "[[work-token]]"
statement: "A retired concept still stands in the design corpus as though it were current. Several demands, three use cases, two stories and one proposition describe a per-record working tree. One flow node and the interfaces citing it keep the same idea inside the signed structure. Read each against the decision that settled on one tree per machine, then rewrite or retire it. The running code is nearly clean already: four mentions remain under the deliverable, and the module once blamed for fifteen of them survives only inside two packaged releases."
ready_when: "ready when a record opens for a design-input sweep"
source: "note-7432b8a852f6"
---

## Why it stands

A retired concept still stands in the design corpus as though it were current. Several demands, three use cases, two stories and one proposition describe a per-record working tree. One flow node and the interfaces citing it keep the same idea inside the signed structure. Read each against the decision that settled on one tree per machine, then rewrite or retire it. The running code is nearly clean already: four mentions remain under the deliverable, and the module once blamed for fifteen of them survives only inside two packaged releases.

## When it comes back

ready when a record opens for a design-input sweep
