---
minted_in: i2-parallel-iterations-across-machines-seed
id: raid-risk-engine-prop-blurs
type: "[[raid]]"
kind: risk
statement: The engine's generality and the SE machine's specificity could blur - vp-the-engine swallowing the flagship, or the flagship implying it owns the engine.
owner: the driving agent
trigger: a story serves neither prop cleanly, or a requirement maps to both props with no argument
status: open
impact: A blurred prop split makes the M8 coverage law undiscriminating - stories point at a blob, and the Unlike lines go mushy against distinct competitors.
breaks_how_badly: abrasive
how_likely: possible
---

The named conflict from i2's goal system. The resolution is the split
itself: vp-the-engine promises the platform (any machine, many hands),
vp-systematic-engineering promises the flagship machine, one scoping
line in each Unlike. The trigger brings this back the first time the
rewiring produces a both-ways mapping.
