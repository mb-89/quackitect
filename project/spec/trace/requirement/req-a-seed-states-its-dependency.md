---
minted_in: i6-conformance-goes-mechanical-checks-bind-
id: req-a-seed-states-its-dependency
type: "[[requirement]]"
statement: If a seed call omits depends_on, then the engine shall refuse it and name the remedy, and an empty list shall be legal as the explicit statement that nothing is waited on.
kind: functional
verify_method: test
breaks_if_removed: The container is a DAG and this key is its only input. An unset key is a missing EDGE rather than a missing note, so work is ordered wrongly and two agents can be handed files that fight.
breaks_how_badly: fatal
refines:
  - uc-open-an-iteration
source_refs:
  - raid-dec-a-seed-states-its-dependency-or-refuses
  - project/spec/iterations/i6-conformance-goes-mechanical-checks-bind-/record.md
  - req-a-records-dependency-is-declared
priority: must
---

## Detail

TWO ANSWERS LOOK IDENTICAL ON DISK TODAY, and only one of them is a
decision.

- I FORGOT.
- I DECIDED NONE.

The empty list is what makes the second expressible. Without it the key
cannot distinguish a silence from a statement, and that is the whole
defect rather than a nicety.

## Why a refusal, on evidence

THE GUIDANCE ALREADY EXISTED AND DID NOT HOLD. The rule stands in the
seed tool's own argument description, in the argument list, unmissable.

MEASURED 2026-08-13: twenty-seven iterations seeded, the key set on
seven. Three stated a wait in their own vision prose and carried no edge
for it — the UI sitting after the panel round, the comment system after
the machine format, and the cloud iteration after the lane binding.

A RULE BROKEN THAT WAY WANTS A REFUSAL, NOT ANOTHER SENTENCE. This row
is this iteration's thesis applied to its own first case.

## What the refusal carries

THE REMEDY IS THE CALL TO MAKE INSTEAD, with the key present and an
empty list in it. A refusal that only says the key is missing leaves the
caller to work out that empty is legal, and most will assume it is not.

## One line of guidance rides along

A PERSON SEEDS TOO, and a person does not read a tool schema. One line
beside the refusal says the dependency is the container's DAG and the
only thing stopping two agents racing the same files.

## What this row does not do

IT DOES NOT BACKFILL. Twenty existing records carry an unset key and
they stay that way. Backfilling would guess, and a plausible wrong edge
is worse than a visible missing one — an absence is readable as an
absence, an invented edge is not.

## Behaviour

NO MODEL WANTED. One argument check at one boundary.
