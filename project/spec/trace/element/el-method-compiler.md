---
minted_in: i1
id: el-method-compiler
type: "[[element]]"
statement: Holds the shipped method and compiles it — matrix rows to machines, templates to forms — and sweeps the system back into step when drift is found.
kind: existing
realization: make
group: the-walk
implements:
  - fn-run-a-governed-walk.hold-the-method
  - fn-run-a-governed-walk.catch-the-system-up
source_refs:
  - cand-thin-worktree
  - raid-dec-thin-tree
  - raid-dec-idempotent-scaffold
---

The method's home: guidance, machines, matrix rows and templates, read from
trunk at the moment they are needed ([[raid-dec-thin-tree]]). The compiler
turns the blessed column into the record's pinned machine. The overhaul
sweep reports divergence, never merges it.

Boundary: the interfaces the element matrix mints for its flows.

Realization: the standing compiler and the overlay machinery.
