# Vale holds the rules a pattern can hold. The style folder is the one place
# they are written, and every caller reads it: the write door in level zero,
# the command line, and the editor.
StylesPath = spec/config/styles
MinAlertLevel = suggestion

[*.{md,markdown,txt}]
BasedOnStyles = VoiceQuackitect
