// The endings a pointer leaves off, and the order a resolver tries them in.
// [[spec/design_output/index#a-note-and-its-links]]
package pointer

// Endings holds the path as written first, then a note, then a process file. [[spec/design_output/index#a-note-and-its-links]]
var Endings = []string{"", ".md", ".yaml", ".yml"}
