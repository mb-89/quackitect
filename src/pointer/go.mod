// The endings a pointer leaves off, one list the index and the language server
// both import, so a link resolves the same way in either.
// It takes no dependency, so it builds in seconds with no cgo and no network.
module quackitect/pointer

go 1.24
