# stakeholders — one note per stakeholder role

Shape: `items/stakeholder.md`. Roles, never persons. One source feeds two views:
the ch0 reader matrix and the ch3 concerns table. Conflicts between rows are
connection notes of kind conflicts-with. Mint: `quack mint stakeholder`.
