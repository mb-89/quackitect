# rules — the binding design rules

Shape: `items/rule.md` (statement, scope, the establishing decision). Internally
chosen governance - distinct from externally imposed constraints. The ch4 rules
view renders the set. Mint: `quack mint rule --of adr-<slug>`.
