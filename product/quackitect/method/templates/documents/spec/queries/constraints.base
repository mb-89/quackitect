# Constraints - externally imposed requirements, each linking its normative reference.
filters:
  and:
    - 'type == "requirement"'
    - 'kind == "constraint"'
views:
  - type: table
    name: Constraints
    order: [file.name, statement, source]
