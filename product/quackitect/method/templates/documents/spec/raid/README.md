# raid — risks, assumptions, issues, dependencies

Shape: `items/raid.md` (kind, probability and impact 0..1, mitigation, owning
role, status). Assumptions render twice: ch3 context and the ch6 register - one
source, two views. Mint: `quack mint raid`.
