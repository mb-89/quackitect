# project_types/

A nestable tree of project types. **default/** always applies (its guides are the
baseline). Specific types nest underneath (e.g. default/tool/, default/tool/framework/)
and ADD their own guides/ (and later checklist/), cascading down the path.

For now only `default` exists — every project is the default type and gets the default guidance.
