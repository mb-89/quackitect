{
  "iteration": "i4-questions-and-hygiene",
  "state": "pressure_test",
  "at": "2026-07-24T11:05:00.000Z",
  "note": "Inline fill - same skip class as 07; the exhibit stands.",
  "payload": {
    "prfaq": "PRESS RELEASE (working backwards): 'The machine grows up: quackitect v2 closes the seam between what its drawings promise and what its engine enforces. The board reads like a tool made for a person - JSON unfolds, parallel work shows as parallel, nothing pending hides in frontmatter nobody can reach. Every law that was prose is now a refusal, every gate carries an absolute quality score set by research against prior art, and the estimates on the board come from measurements, not moods.' HOSTILE FAQ: Q1 Twenty-nine steps - is this a junk drawer? A: No - one seam, six pains, every step traces to a drained note or a register gap; the junk-drawer version would have no pass lines. Q2 Why complete laws instead of shipping the book (i5)? A: the book renders the LEDGER; rendering an untrusted ledger (pending stamps, schema-free mints) publishes the mess - hygiene first is sequencing, not avoidance. Q3 The scorer: an agent grading its own homework? A: the scorer is a SEPARATE research agent against external prior art, and the owner calibrates its first round (R3) - the builder never scores itself. Q4 What if parallel loop wiring breaks mid-iteration again? A: R2 - the i3 instance lane (locks, atomic writes, git history) was built for exactly this; worst case restores one chunk. WEAKEST ANSWER: Q1's junk-drawer charge is real if the M6 plan balloons - folded into R1 with the renumber-split trigger armed.",
    "findings_folded": "Q1 sharpened R1 (the split trigger is the honest answer to scope weight); Q2's sequencing argument enters the gate brief as the why-now core; Q3 confirmed R3's calibration round. Nothing else moved upstream."
  }
}
