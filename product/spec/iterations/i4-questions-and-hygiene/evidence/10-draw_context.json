{
  "iteration": "i4-questions-and-hygiene",
  "state": "draw_context",
  "at": "2026-07-24T11:01:49.608Z",
  "payload": {
    "boundary": "INHERIT [[context]] (se.context), one amendment owed and named rather than silently absorbed: the boundary GAINS the sub-agent seam this iteration formalizes - the guidance registry serves chunk packets to builders, and the research-agent scorer becomes a named neighbour (spawned per evaluate, reads prior art, writes one score row, holds no state). Amendment lands on the node during M3 when the scorer requirement is written; everything else - inside set, neighbours, interfaces - stands as recorded.",
    "intended_use": "INHERIT [[context]] section Intended use, unchanged: one owner, one project, agent sessions driven by drawn machines, a record complete enough to reconstruct everything. i4 narrows nothing and widens nothing - it makes the recorded intent true in more places.",
    "excluded_use": "INHERIT [[context]] section Excluded use, unchanged and binding: no multi-project instances, no multi-owner adjudication, no unpinned formats, no CI/CD, no secrets, no replacing git, no general agent framework. The worktree-per-iteration question the owner raised today deliberately does NOT amend this list - it is a capability question for the plan (A/C pending), not a boundary change; one project root per server stands either way."
  }
}
