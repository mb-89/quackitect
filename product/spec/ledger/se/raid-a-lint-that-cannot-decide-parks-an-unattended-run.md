---
id: se.raid-a-lint-that-cannot-decide-parks-an-unattended-run
kind: raid
statement: "A lint that neither passes nor fails - one that asks a question the machine cannot answer - would stop an unattended iteration dead, at any hour, waiting for a human who was explicitly not expected to be there. That inverts the point of the whole delegation: se.law-responsibility-never-delegates rules that the involvement slider must reach ZERO and still work, so a mechanical check that can park the run is a regression dressed as rigor."
provenance:
  iteration: i12-tool-surface
  ai_involvement: agent-drafted
raid_kind: risk
raid_owner: agent
trigger: "Any lint design that admits a third outcome. RULED NOW: a lint either DECIDES or DEFERS TO JUDGEMENT - it never parks. Deferring means the check stays in the evidence form as prose for the agent to argue, exactly as today; that is the honest floor and it costs nothing. Watch for it at M4 when the mechanical/judgement split is drawn, and treat 'this lint needs more information' as a signal that the check belongs on the judgement side, not that the run should wait."
---


