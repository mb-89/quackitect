---
id: se.law-workaround-then-record-the-gap
kind: decision
statement: "A WORKAROUND IS LEGAL WHEN THE LANE CANNOT DO THE JOB - AND IT MUST LEAVE A RECORD. Owner ruling, 2026-07-25. Hand-rolling around the tool surface is not a sin in itself; pretending it did not happen is. Whenever work is done outside the lane because the lane lacks an affordance, the agent MUST leave (a) a note for the retro's drain and (b) a RAID entry naming the missing affordance, so the next retro fixes the tooling instead of the agent quietly absorbing the cost forever. The failure this prevents is silent adaptation: an agent that routes around a gap without recording it makes the gap invisible and permanent, and the tool surface stops learning from its own misses. Corollary: se_help is the first move when an affordance seems missing, because the logged miss IS the demand signal - the workaround comes after, not instead."
provenance:
  iteration: i8d-phone-brief
  ai_involvement: agent-drafted
  adjudicated_by: owner
  channel: chat
breaks_if_removed: Tooling gaps become permanent. The agent absorbs the cost of every missing affordance silently, the retro never sees the demand, and the tool surface stops improving - while the record shows work that appears to have ridden the lane when it did not.
applies_to: every hand-rolled script, ad-hoc command or out-of-lane edit, in any iteration
---


