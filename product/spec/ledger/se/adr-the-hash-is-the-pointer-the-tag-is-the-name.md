---
id: se.adr-the-hash-is-the-pointer-the-tag-is-the-name
kind: decision
statement: "THE EVIDENCE COMMIT HASH IS THE POINTER; THE TAG IS A CONVENIENCE NAME. ADDRESSES R22, R23, R24, R25 and R5 (record search needs unambiguous refs). The close records the hash in the grant, creates an ANNOTATED tag - push.followTags carries only annotated tags, verified 2026-07-25, and ours were lightweight, so the config every VS Code user would set pushed commits and silently dropped every iter/* tag - and DELETES the iteration branch after a successful merge. Deleting the branch is not housekeeping: git emitted 'warning: refname iter/i8d-phone-brief is ambiguous' during an i12 probe, because a closed iteration carried both a branch and a tag of that name, so ref-scoped record search would have been built on refs git itself calls ambiguous. Reachability never depends on either name: the merge's second parent holds the evidence commit, so it survives any tag deletion. REJECTED: lightweight tag only (probed defective on both counts); hash only (loses the readable name a person can type, which the function structure separates deliberately as F23)."
provenance:
  iteration: i12-tool-surface
  ai_involvement: agent-drafted
---


