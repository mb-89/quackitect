---
id: se.raid-brief-exports-evidence-content
kind: raid
statement: "The hand-off brief carries the offer's evidence fields VERBATIM to a third-party relay and store. Today that is prose and run references - already ledger content - so the exposure is acceptable. It stops being acceptable the moment an evidence field carries something that must not leave the machine: a credential, a filesystem path, or an excerpt from a no-remote source. The encryption protects against the HOST, not against whoever holds the ntfy topic, and the relay retains for about twelve hours. Nothing currently filters brief content by tier."
provenance:
  iteration: i8d-phone-brief
  ai_involvement: agent-drafted
raid_kind: risk
raid_owner: owner
trigger: "The first no-remote or credential-bearing content to reach an evidence field - which kb's tiering makes a matter of when, not if. Fallback: filter brief content by tier before publishing, or drop to the actions-only rung for that gate. Recorded now so the filter is owed work rather than a surprise."
---


