---
id: se.adr-a-lint-decides-or-defers
kind: decision
statement: "A LINT YIELDS PASS, FAIL, OR DEFER-TO-JUDGEMENT - and nothing else. ADDRESSES R16, R17, R18, R19, se.law-responsibility-never-delegates, and the two lint risks logged at i12. It never blocks awaiting input, and it never judges intent. Deferring means the check stays as prose in the evidence form exactly as today, which is the honest floor and costs nothing. Two failures are designed out at once: a lint that PARKS would stop an unattended run at 3am for a human deliberately absent, inverting the delegation the project exists for; and a lint that judges INTENT would refuse correct work, making the rational response to route around it - replacing 'the tools are too weak to use' with 'the checks are too dumb to satisfy', which is the same failure carrying more authority. Corollary, ruled now so it is not negotiated under pressure: a lint that fires wrongly is a defect to fix or remove, never a cost to absorb. REJECTED: a third 'needs review' outcome (it is a park wearing a friendly name); lints that score quality or judge whether prose is good (unfalsifiable, and they would make the machine an unaccountable critic)."
provenance:
  iteration: i12-tool-surface
  ai_involvement: agent-drafted
---


