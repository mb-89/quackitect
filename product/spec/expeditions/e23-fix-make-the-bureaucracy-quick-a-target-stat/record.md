---
id: e23-fix-make-the-bureaucracy-quick-a-target-stat
kind: fix
status: open
opened: 2026-07-29T14:20:02.457Z
goal: "Make the bureaucracy quick. A target state with a route, and a desk that prefills.\n\nTHE ROUTE, both layers, and it is SCHEDULING ONLY. It removes no guard, no condition and no autonomy rule (owner, 2026-07-29). It collapses model round trips, nothing else.\n\n- THE BLUE LINE first. A route to a named target computes the path and reports every hop: its priority, its unmet conditions, the documents it will demand. It moves nothing. The mirror draws it. Clicking a state you are not standing in is how you set the target.\n- THE SWEEP second. One call carries every read hash the route demands, and the engine advances hop by hop. It STOPS at the first hop above the slider, or with a condition it cannot satisfy, and says which hop and why. Every hop still fills its state, still logs its own transition, still writes its own feed line. A route that sails past a killer state is a hole through contract rule 3, so each hop is weighed separately.\n- THE DEFAULT TARGET IS THE FRONT DESK (owner ruling). Starting the engine sets it, so boot walks there rather than stopping at idle.\n\nThe graph already carries what a route needs - every state has its edges, and a submachine state expands to its own start-to-end run. Prior art is real: orchestration engines run Dijkstra over the state graph and step through it. The everyday frame is make - name a target, compute what is needed, run it, halt on the first failure.\n\nTHE DESK PREFILLS. Needs almost no new mechanism: nothing guards a seed, because the prefill guard lives on the LEAVE form. The desk composes the goal from the conversation, and for an iteration the vision, the inputs and the kickoff brief too.\n\nINPUT IS NOT EVIDENCE (owner ruling). Prefill hard at the ENTRY of work, where a wrong value goes visibly wrong at once with the owner present. The guard at the EXIT is untouched, because a report CLAIMS work was checked.\n\nA PREFILL IS NOT A LAW (owner ruling). The secretary fills the form to the best of their knowledge and sends you on your way. Whoever receives it still checks whether the prefilling was right. A prefilled value carries its provenance and is judged by the state that receives it, never obeyed. This sharpens the prefill rule in ux.md.\n\nMECHANICAL WORK PULLED IN, on the owner's word to take anything needing no input.\n\n- THE DECISION-NODE MAP, from note-792c32b5425e item 5. Update responses echo the open node ids with their briefs, so an id is never guessed. Hit four times in e22, each time recovered by deliberately naming a bogus node to make the refusal print the map.\n- THE PROSE SWEEP, note-7e573cb1b669. Thirteen lint findings stand in guidance documents e22 did not write. Judge each - the lint checks form, never meaning, and a finding in a sentence that reads well is noise, which is the linter law's answer and moves the threshold instead.\n- STATE GUIDANCE FIELDS have never been linted. They are prose an agent reads on every visit, so they are the prose that matters most. The sweep needs the scalar pulled out of the frontmatter first."
---

# e23-fix-make-the-bureaucracy-quick-a-target-stat

Free prose — the human head of the record. Machine-facing fields stay in the frontmatter.
