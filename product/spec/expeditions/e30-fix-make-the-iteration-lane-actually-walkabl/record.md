---
id: e30-fix-make-the-iteration-lane-actually-walkabl
kind: fix
status: closed
closed: 2026-08-02T12:00:27.781Z
ruling: applied
report_override: "the owner, in this session's chat on 2026-08-02: \"you can sign the report, close the expedition, and ... end the session and write a handover\""
opened: 2026-08-01T16:33:18.821Z
goal: "MAKE THE ITERATION LANE ACTUALLY WALKABLE, so the product iteration can be opened.\n\nWHY AN EXPEDITION AND NOT AN ITERATION. The owner's word, 2026-08-01: \"We don't have proper iteration support yet. The first iteration that we open is the one that will describe the whole product. And I need it to halfway run until then. So let's do an Expedition.\" No iteration has ever been walked — there is no it/* branch and no product/spec/iterations/ directory.\n\nTHE SHUTDOWN ROW — a new control, the owner's explicit ask. Add a shutdown row to the controls panel with TWO buttons that may BOTH be active at once.\n\n- BLOCK AUTO-SLEEP: keep the computer awake so it does not sleep by itself.\n- SHUTDOWN AT IDLE: keep the computer awake while anything is happening; then shut the COMPUTER down once nothing is.\n\nNeither pressed means the machine does nothing about power at all.\n\nIdle means all of: the walk stands at idle, no new log line for about five minutes, and no subagent running. THE MACHINE DOES THIS WITHOUT THE AGENT. It is a timer the machine owns, not something the language model decides or triggers.\n\nWHAT IS ALREADY RULED, carried in so it survives a compaction.\n\nTHE FLOOR LAW (decision 3): enforce it in the compiler, AS A REFUSAL. Four rows carry floor: true and the flag is parsed at rigor-matrix.ts and read by nothing, so a cell edit could strike the release gate and every test would stay green. A lint beside the refusal, so it is caught while still in the file.\n\nTHE KILLER FLAG (decision 7 of the questions): DELETE it from the documented semantics. It reaches the author inside an HTML comment and the checker strips HTML comments before looking, so it has never done anything. required already defaults true.\n\nTHE RE-WALK (decision 4): escalation is NOT a special case. Leaving an iteration and returning, escalating it, and reworking it after guidance changed are the same act — walk it top to bottom and check whether any evidence no longer fits what is expected. Build ONE mechanism with several triggers. reopenStates in machine.ts is the mechanism and the demand diff in iterations.ts is the input; nothing joins them.\n\nEVIDENCE REACHES THE AGENT (decisions 6 and 8, merged on the owner's word). The machine hands back an evidence form when leaving a state demands one, and a decision form when the road splits. The agent returns the filled form ON THE NEXT PULL. There is no separate submit verb. Pulling again without it returns the same form. The machine accepts or rejects, and a rejection names what to correct.\n\nTYPED FIELDS with per-field guidance. DERIVED evidence never reaches the agent at all: the machine computes it, and only speaks if it fails.\n\nWHAT THE PRODUCT SIZE IS FOR (decision 5). Not a different structure. The FRONT is much heavier. A product iteration defines the vision, the stakeholders and the early phases for the WHOLE PRODUCT, and lays the architectural groundwork. Later iterations, even a major one, inherit those and define design input only for their own scope. Stakeholders especially are defined once and rarely revisited.\n\nPARKED, NOT DROPPED. Machine-picture layout is parked for today; when it returns, lift v2's ranker and render it better. The specification column is parked until a consumer exists.\n\nHOW TO WORK HERE, the owner's instruction: do everything that can be done, and bring questions ONE AT A TIME, at the point of need, with concrete detail. Abstract menus of options were explicitly rejected as too vague to answer."
---

# e30-fix-make-the-iteration-lane-actually-walkabl

Free prose — the human head of the record. Machine-facing fields stay in the frontmatter.
