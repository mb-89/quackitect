---
id: e28-fix-the-day-s-bucket-for-2026-08-01-five-kno
kind: fix
status: open
opened: 2026-08-01T07:47:43.602Z
goal: "THE DAY'S BUCKET for 2026-08-01. Five known machinery defects, every one already ruled, bundled so the owner can leave the walk running. This goal names the bundle rather than one fix, per the sizing law — amend it if the bundle grows.\n\n1. se_file_move must understand SOURCE, not only prose (owner ruling 2026-07-31, note-15e1a87d2bc2). A declared format whitelist with .ts first, then .ps1 and .json. After a move, search the tree for the old path and REPORT every hit the tool could not rewrite, with file and line, as work the caller still owes. Silence on a broken reference is the real defect: today `rewritten: []` reads as \"nothing needed changing\", and it once dangled nine references across four files while reporting success. This goes FIRST because it is a tool defect that makes every later move unsafe.\n\n2. Preflight must parse the WEBVIEW scripts inside vscode/extension.js (note-1ea247c3d2c1). `node --check` sees a webview's script as text, so a syntax error there ships green and the pane is silently dead. Extract each script block, blank the ${...} interpolations, new Function() each one. Proven in-session, costs no spawn.\n\n3. The palette remainder (note-24123ac2c8f8, with note-df2f3945e0ac). OWNER RULING 2026-08-01: the TypeScript colour values — the log-kind map and RESERVED_COLOURS — are made configuration by PARSING palette.css at runtime, not by adding a second data file. One palette file stays the single source of truth and an edit shows without a code change, per the 2026-07-30 colour law. Also name the route blue and its ring, the JSON value colours, the menu hover surface and the lost-link banner, and sweep the machine view's group boxes and legend onto --se-* vars. ux.md already rules that a colour written at its use site is a defect.\n\n4. The render pull (note-11ae87270527) — the largest MEASURED cost in the system. OWNER RULING 2026-08-01: take ONE guidance snapshot per render and THREAD A PARAMETER through render.ts. It stays live, because the next render re-derives it; what stops is re-deriving it a hundred-plus times inside one synchronous render to draw twelve states. Baseline is measured: a render was 1043ms, is 892ms after the easy half, and the suite moved 459.4s to 447.2s. Measure again after, since the point is the number.\n\n5. The voice-lint prose sweep, ALL 26 findings (note-42caca46253d). OWNER RULING 2026-08-01, taken against the recommendation to leave the 13 state-guidance findings for an attended session: sweep all of them. The risk was named and accepted — rewriting a state's guidance changes what every agent reads and does, and idle and front_desk are the two most-walked states. The note's own escape hatch is honoured: the lint checks FORM and never meaning, so where a finding is noise the THRESHOLD moves instead of the prose, and those parameters are data in machines/lint/voice-lint.md.\n\nWHAT IS DELIBERATELY NOT IN HERE: hand verification. Three notes stand on VS Code work nobody has watched run (note-424aa024a797, note-578fab270599, note-4d775ebe6df6). Those need a person driving the shell and wait for the owner's return whatever the slider says."
---

# e28-fix-the-day-s-bucket-for-2026-08-01-five-kno

Free prose — the human head of the record. Machine-facing fields stay in the frontmatter.
