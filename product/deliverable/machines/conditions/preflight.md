---
condition: preflight
---

# preflight — the engine's checks are green

Arguments: none. The ENGINE evaluates this condition; nobody can claim it.

What it checks today: every machine canvas compiles, every referenced
document exists, ripgrep and git answer, the call log writes. How to
satisfy it: trigger a run (the mirror's run button, or a tick attempt) and
fix whatever failure the refusal names — the checks re-run on every attempt.
