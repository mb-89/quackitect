---
kind: matrix-cell
row: sweep-consistency
column: patch
applies: full
---

FLOOR - never struck. A fix that changes behavior a document teaches
leaves that document lying until the sweep runs. The sweep is scoped to
what the patch touched, and it always runs.
