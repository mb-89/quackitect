---
state: prepare_idle
state_kind: work
legal_tools: se_boot
guidance: Preflight. Today this step is nearly empty — confirm nothing feels broken and call se_boot again. It grows into real checks (compile what needs compiling, look for errors, look for missing pieces) so idle always starts clean.
---

# Prepare idle

The preflight check before idle: anything that must be compiled, verified,
or repaired so the session can continue cleanly happens here. Deliberately
a stub for now — the owner fills it as real preflight needs appear.
