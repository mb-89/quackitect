{
  "name": "quackitect",
  "version": "2.0.0-bootstrap",
  "private": true,
  "description": "SE — a process layer over a git repository. v2: TypeScript, MCP-only lane, guidance-first, update-toll, modules by default.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "guards": "node --test tests/guards.test.ts",
    "verify": "npm run typecheck && npm test"
  },
  "dependencies": {
    "kb": "file:../../../benjamin"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.8.3"
  }
}
