{
  "_readme": [
    "THE VS CODE MCP CONFIG. VS Code agent mode reads .vscode/mcp.json and",
    "ignores every other host's file, so the se lane needs its own copy.",
    "RUNME places it at workspace/.vscode/mcp.json, the same way it places",
    "Claude's .mcp.json and Copilot CLI's .copilot/mcp-config.json.",
    "",
    "THE SHAPE IS NOT THE SAME as the other two. VS Code wants `servers`,",
    "not `mcpServers`, and it wants an explicit `type`. Copying one of the",
    "sibling files here would load nothing and say nothing about why.",
    "",
    "PATHS ARE RELATIVE TO THE WORKSPACE FOLDER. Open VS Code ON workspace/,",
    "not on the repo root, or the server starts with the wrong root and the",
    "walk cannot find product/.",
    "",
    "THE CAGE DOES NOT COME WITH IT. Claude is caged by a settings file and",
    "the Copilot CLI by --excluded-tools; VS Code agent mode has no",
    "equivalent that a workspace can enforce. Its native tools stay visible.",
    "So on this host contract rule 1 is ADVISORY - the agent is ASKED to",
    "stay in the lane rather than prevented from leaving it.",
    "",
    "VERIFY BEFORE TRUSTING IT, the same empirical way copilot-cage.json was",
    "verified: open a session here and ask it to list the exact names of",
    "every tool it can see. Anything that is not an se_ tool is a hole."
  ],
  "servers": {
    "se": {
      "type": "stdio",
      "command": "node",
      "args": [
        "../product/deliverable/engine/bin/se-mcp.ts",
        "--root",
        ".."
      ]
    }
  }
}
