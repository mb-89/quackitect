---
kind: [[handover]]
status: done
urgency: now
---

# Where it stands

[[spec/tickets/guidance-rides-the-step]] closes `done`, and every child closes with it. `./RUNME.sh check` exits 0 on this tip, with every test green and the rest standing at warning.

The branch carries one change outside its tickets: `spec/processes/question.yaml`. The owner's rule says a cloud box hands a person step out and waits for nobody. The cloud guidance names `branch unblock` for that, and the verb takes a successor whose first step reads `by: person`.

| what the verb wants | what the tree holds before this branch |
|---|---|
| a successor opening at `by: person` | every process opens at a step admitting an agent |
| so the unblock runs | so the unblock refuses each successor, and the wall stands |

The new process opens that road, and this branch is the first to walk it.

| what this branch does | where it stands |
|---|---|
| closes the group and its three children | [[spec/tickets/guidance-rides-the-step]] |
| hands the person step out of the group | [[spec/tickets/a-person-reads-the-split]] |
| opens the road the cloud rule names | `spec/processes/question.yaml` |
| names what the walk turns up | three tickets in the pool, below |

# What waits

| what | who | where |
|---|---|---|
| answer the question the split ticket carries | a person | [[spec/tickets/a-person-reads-the-split]] |
| keep the unblock's question in its own shape | an agent | [[spec/tickets/the-unblock-keeps-its-shape]] |
| name `branch unblock` where the pull answers `wait` | an agent | [[spec/tickets/the-pull-names-the-unblock]] |
| give the question process its test and its chapter | an agent | [[spec/tickets/the-question-process-gets-tests]] |

Each of those four names no group, so each stands in the pool on trunk once this branch merges.

This box writes no script, so `.se/scripts` stands empty.

# The retro

[[spec/tickets/guidance-rides-the-step]] carries this box's window under its retro chapter. The short of it: box after box walks this group and leaves the wall standing. The rule each one needs stands in the cloud guidance the whole time, and one missing process file is the whole difference.
