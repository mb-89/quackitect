---
kind: [[handover]]
status: todo
urgency: now
---

# Where it stands

The group [[spec/tickets/the-warnings-feed-a-refactorer]] stands part way. Its
design leaves move, and its implement leaves wait.

Two children leave the group as questions this box hands out. Both decisions
belong to a box, so both now carry an answer:

| question | the answer it carries |
|---|---|
| [[spec/tickets/the-rule-shares-one-slug]] | the Go rule carries its own slug, with the reason and a contract test |
| [[spec/tickets/apply-lane-carries-a-hand]] | the lane stamps as it reads, so the callers change nothing |

Each answer names what a wrong one costs. Neither costs more than a commit.

`./RUNME.sh check` answers 0 on this commit. The check asks the server, so run
`./RUNME.sh serve` before it.

| child | step |
|---|---|
| [[spec/tickets/the-runtime-files-stand-apart]] | implement/tests-red, its design stands |
| [[spec/tickets/a-rule-carries-its-side]] | design/draft |
| [[spec/tickets/the-hook-spawns-a-refactorer]] | design/draft |
| [[spec/tickets/the-panel-draws-every-file]] | design/draft |
| [[spec/tickets/a-pointer-names-its-heading]] | closed, and a question carries it |
| [[spec/tickets/a-write-meets-its-hash]] | closed, and a question carries it |

# What waits

| what | who does it |
|---|---|
| carry out the two answers, at the `do` step of each question | the next hand |
| write the tests for the runtime split, then make the change | the next hand |
| draft the approach on the three children at design/draft | the next hand |

A cloud box reaches a ticket outside a group through no pull, because the pull
takes a branch for it. So the two questions want a hand on a desk, or a group of
their own.

# What the two answers carry

The slug answer takes the copy in Go:

- four copies stand in JavaScript, and a Go module imports none of them
- voice rule 7 takes a copy a technical reason forces, beside its reason
- a contract test holds the five to one answer, so a drift fails the check

The apply lane answer takes the stamp at the read:

- the lane reads each file at the moment it writes, so its read is the freshest
- stamping there tells the truth about what it reads
- a wrong answer refuses one batch edit, and the hand reads again

# What surprises me

The write door takes an answer written under a step reading `by: person`. The
pull refuses that step to an agent, and the door refuses nothing. So the block
sits in one place alone, which is smaller than it looks.

A question ticket carries no group, so a group branch hands it out nowhere. The
two questions here sit outside every route a cloud box walks.
