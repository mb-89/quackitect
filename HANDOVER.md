---
kind: [[handover]]
status: todo
urgency: now
---

# Where it stands

An agent merges. A cloud box alone stops short, because the harness opens no
pull request there.

`./RUNME.sh check` answers 0.

The design said this correctly all along.
`spec/design_output/work#a-box-writes-its-branch` scopes the trunk guard to a
cloud box. The code missed that scope and refused every box, and the guidance
around it spread the wrong rule into every brief the tree writes.

| the piece | what it says now |
|---|---|
| the trunk guard | it reads the cloud flag, and a desk box passes |
| the refusal | it names the harness, and no person |
| the brief contract | step 6 names `work merge`, and a cloud box stops at step 4 |
| `work done` | it prints the merge verb to run next |
| the round trip | a cloud session stops, and every other box carries on |
| the review rule | the reviewer merges once every fix lands |

# What waits

Nothing on this branch. Merge it and close it.

# What stays

The copilot road keeps its rule. `lib/copilot-dispatch.js` and
`lib/copilot-runtime.js` speak to a box that truly may never merge, and
`spec/rationales/cloud.md` records why. All three stand as they were.
