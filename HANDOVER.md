---
kind: [[handover]]
status: done
---

# Where it stands

The branch closes its group and every child under it. `./RUNME.sh check`
answers 0 here, and `./RUNME.sh branch done` sets the status this file carries.

| what | where |
|---|---|
| the group | [[spec/tickets/the-bridge-keeps-transport]] |
| the retro over this window | the same ticket, under its retro chapter |
| every child, each one done | the group's own list of them |

The ask names a fault a line, and a child answers each one. The engine's own
work moves out of the bridge into `src/engine`, so a reader finds each piece in
the folder owning its topic.

# What waits

Some private notes decide as `became`, and each one names what its successor
carries. Nothing stands minted for them yet, and the private box goes with this
box, so the table below is what survives.

| what the successor carries | where the note stands |
|---|---|
| the pull takes a held leaf out of its answer | `parallel-hands-take-one-leaf` |
| every line-per-row reader takes the tolerant one | `torn-log-line` |
| a draft names what it reads beside each claim | `drafts-assert-unchecked-facts` |
| the cage block stands proven off a live cloud box | `cage-block-wants-a-prompt` |
| the rule takes a span a slow box meets | `two-checks-time-vale-out` |

The last row bites now and then. `./RUNME.sh check` answers 1 on one line
naming a timeout, and every rule then stands unread. A run of `./RUNME.sh lint`
alone answers 0, so a red of that shape reads as a busy box.

`.se/scripts` holds nothing, so this box leaves no script behind.
