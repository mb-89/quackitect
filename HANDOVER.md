---
kind: [[handover]]
status: done
urgency: soon
---

# Where it stands

The to-do flag stands, and every piece the brief names answers. A hand parks
work by tagging a note, and the push door refuses a delta carrying one.

| the piece | where | what answers it |
|---|---|---|
| the field | `spec/schemas/ticket.schema.yaml` | `todo`, a boolean, and a note lacking it reads false |
| the reader | `.claude/skills/level0/lib/todo.js` | the tag, the tagged files in a delta, the refusal, the order |
| the mint flag | `src/scripts/ticket.js` | `ticket note <name> <line> --todo` |
| the tag verb | the same file | `ticket todo <name>`, and `--off` takes it back |
| the push door | `src/scripts/prepush.js` | it reads the delta each ref carries |
| the Bash door | `.claude/skills/level0/hooks/level0.js` | it reads the commits no remote holds |
| the uncommitted check | `src/scripts/work.js` | `dirty` looks past a tagged file |
| the take | the same file | it saves each tagged file, resets, and writes it back |
| the close | the same file | a group closing loses its tag |
| the tests | `test/level0/` | `todo`, `prepush`, `hooks`, `ticket-verb` and `work` |

The push door runs green end to end. A tagged tracked ticket commits clean,
and the push comes back refused, naming the file.

# What waits

| the thing | who takes it |
|---|---|
| the pull calls `taggedFirst` | the branch that builds the pull |
| the chapter reaches trunk | this branch's merge |
| a stale schema costs a session | whoever tires of it |

## The pull order waits

The branch that builds the pull wires `taggedFirst` in one line. It puts every
tagged note ahead of every free ticket, and the tests hold it. No pull verb
stands in the tree yet, so nothing calls it.

## The chapter rides here

Trunk lacks the chapter, and this branch brings it in.

| the fact | where it stands |
|---|---|
| the brief cites the chapter The to-do flag | `spec/design_input/the-agent-pulls-tickets.md` |
| trunk carries no such chapter | `main` |
| one commit adds it | `claude/relaxed-knuth-f0uk4d`, which nothing merges |
| this branch cherry-picks that commit | so every code link resolves |

# What surprises me

| what | what it costs |
|---|---|
| the git door trims its answer | the first porcelain row loses its leading space |
| the write door caches the schemas | a fresh field reads as a stranger all session |
| the commit door reads the command | a heredoc message meets the paragraph rules |
| the brief cites a chapter trunk lacks | a read of history before a line of code |

## A porcelain row lies

The trim is the one dead end here.

`git.run` trims its answer, so ` M a.md` arrives as `M a.md`. A slice at column
three then cuts the path short, and the take refuses a tree carrying one tagged
file. `changedIn` now matches the status code, and it leaves the columns alone.
It stands exported, so a test holds every shape a porcelain row takes.

## A field costs a session

| the fact | what it means |
|---|---|
| the schema takes `todo` | `./RUNME.sh check` agrees |
| the write door refuses it here | the door reads the schemas once at the start |
| a branch adding a field writes no note carrying it | a fresh session clears it |
| the verb writes through the disk door | it meets no such wall |

## Where the scripts stand

`.se/scripts` holds nothing, because none of the three probes earns a home.
They run out of the scratchpad. One reads the tag off a fake disk, one runs the
take off fake doors, and one reads the schema over a minted ticket.
