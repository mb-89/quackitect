{
  "$comment": "GENERATED from util/cage/mcp.json. Edit the source, not this file.",
  "mcpServers": {
    "quackitect": {
      "command": "{{method}}/.bin/se-mcp",
      "args": ["--method", "{{method}}", "--work", "{{work}}"]
    }
  }
}
