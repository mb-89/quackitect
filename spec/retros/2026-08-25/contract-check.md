---
id: retro-2026-08-25-contract-check
statement: The contract walked rule by rule against this window's recorded trail, with two breaches named.
---

# Contract check — the window opening 2026-08-24T14:23:57Z

Walked against the call log, the decision graphs of i60 and i61, and the notes.
Private thinking is not in the store, so this reads what the lane recorded.

TWO RULES WERE BROKEN. Both are named below with what would give them teeth.

## Rule by rule

| rule | verdict | what the trail shows |
| --- | --- | --- |
| 1 — the lane is the only door | held | every call in the window is a lane call. One shell command did a lane job and declared its reason, which is the sanctioned exit |
| 2 — walk the state in your hand | held | no state was worked from outside itself |
| 3 — autonomy is the person's dial | held | nothing stopped above the dial |
| 4 — strays are notes | held | 34 notes captured, and the inbox reached zero in this retro |
| 5 — finish it before you judge it | BROKEN | see below |
| 6 — confirm before you compose | BROKEN | see below |
| 7 — disagree and commit | held | reservations were recorded and the work continued |
| 8 — the repo is the memory | held | the memory folder was read and is empty, the notes are drained, the field reports are mined and the old ones removed |
| 9 — never open a record unasked | held | nothing was seeded; the plan is presented and waits |
| 10 — never look at the screen unasked | held | no capture was taken |
| 11 — subagents and research are yours | not exercised | no helper was spawned in a window of 2,124 calls, and nothing asked for one |
| 12 — walk, do not ruminate | held | no settled decision was re-derived |
| 13 — recite the rules at the desk | held | recited once, at the first visit |

## Rule 5 — a round judged work it had not finished

i61's goal named three things to shorten. Between the commit that started it and
the commit that shipped it, none of the three was touched.

Every gate passed anyway, because the release gate checks the aims list written
at the opening checkpoint, and that list had been replaced sixteen minutes in
with four unrelated aims.

THE SHAPE IS EXACTLY WHAT RULE 5 WARNS ABOUT: the work is judged against
something other than what it was for, and the judgment reads as rigour.

HOW IT GETS TEETH. The opening checkpoint compares its aims against the
record's own stated purpose, its sketch and the sources it was seeded from, and
refuses a list that does not answer them. A second hand reviews that
checkpoint. Both stand as
`wt-a-second-hand-reviews-the-checkpoint-that-opens-a-piece-of-w`.

THIS IS NOT THE FIRST TIME. `raid-debt-i15-gate-implementation-reached-with-five-goals-unbuilt`
records the same shape in an earlier round. Closing that entry did not prevent
the recurrence, because the entry was about one round and the cause is in the
mechanism.

## Rule 6 — the walking agent acted before it explained

On 2026-08-25 the owner asked, in order: look at what is upstream, explain
whether it makes sense to keep, and merge it if it does.

The agent merged first and explained afterwards. The owner said so directly.

WHY IT MATTERS AND WHY IT IS NOT MECHANICAL. Nothing in the lane can tell an
explanation from an action, and no refusal could have caught this. The
instruction named a sequence and the sequence was not followed.

WHAT REDUCES IT. Where a person's request names an order, restate the order
before starting, so the first step is visibly the first step. That is a habit,
recorded here rather than dressed up as a check.

## Rule 11 — the unexercised one

No helper was spawned across 2,124 calls, and no state asked for one. The
walker ceiling stood at zero throughout i60, which is why ten steps that exist
to ask for helpers produced blank fields.

That is not a breach. It is worth recording because the rule says helpers are
the agent's to reach for, and a window this size reached for none.
