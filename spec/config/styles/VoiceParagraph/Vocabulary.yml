# GENERATED. Edit the source named below, not this file. It is written again
# every time the tree is projected, so an edit here is lost. Source:
# spec/schemas/paragraph.schema.yaml
extends: script
message: "A word stands outside the words this tree writes. Write a word from spec/vocabulary/words.yml, or add it there with `from: session` and a meaning."
link: spec/funnel/a-paragraph-has-a-schema.md
level: error
scope: raw
script: |
  text := import("text")

  matches := []

  blanked := func(said, pattern) {
    found := text.re_find(pattern, said, -1)
    if is_undefined(found) { return said }
    parts := []
    at := 0
    for one in found {
      m := one[0]
      parts = append(parts, said[at:m.begin])
      parts = append(parts, text.repeat(" ", m.end - m.begin))
      at = m.end
    }
    parts = append(parts, said[at:])
    return text.join(parts, "")
  }

  plain := func(said) {
    out := said
    out = blanked(out, `(?s)^---\n.*?\n---`)
    out = blanked(out, "(?s)```.*?```")
    out = blanked(out, `(?s)~~~.*?~~~`)
    out = blanked(out, `(?s)<!--.*?-->`)
    out = blanked(out, `(?m)^(?:\t| {4,}).*$`)
    out = blanked(out, "(?s)`[^`]*`")
    out = blanked(out, `https?://[^\s)]+`)
    out = blanked(out, `\[\[[^\]]*\]\]`)
    out = blanked(out, `\[[^\]]*\]\([^)]*\)`)
    out = blanked(out, `[A-Za-z0-9_.-]+/[A-Za-z0-9_./-]+`)
    return out
  }

  list := `
  a abbreviation ability about above absence absences absent absorb accept
  acceptance access accident accidental accidentally account accurate
  accurately acknowledgement across act action actionable actionables
  activation active activities activity actor acts actual actually adapt
  adapter add address adjust adjustable adjustment administrator admits
  adopts advances advice advises after afternoon afterwards again against
  age agent ago agree ahead aimed aiming aims align alike alive all allow
  allowance allowed almost alone along already also alternative although
  always amber ambiguous among amount an analog anchor anchored anchors
  and animate animates animation another answer any anybody anyone
  anything anyway anywhere apart appear append applicable application
  applications applier apply appreciate approach approval approved
  approximate approximately arbitrates archaeology archive are area argue
  argument arguments arm around array arrival arrive arrives arrow
  artefact artifact artwork as aside ask asked asking asks assemble
  assembly assert asserting assertion asserts asset assigned assigning
  assignment assigns assumption asymmetry asynchronous at atomic attach
  attempt attention attribute audience audit authenticate author authored
  authority authorizes authors auto automatic automatically autonomy
  auxiliary available away back backend background backlog backslash
  backslashes backspace backup backward bad badly bake balance band bands
  bar bare based bash basic batch battery be bearing beat beats became
  because become before began beginning behaves behavior behaviour behind
  belief believes belong belonged belonging belongs below bend beneath
  beside best better between beyond bill bin binary bind binding binds
  biome bit bite bites blacklist blameless blank blanks blast bleed blind
  block blockage blocked blocks blow blunt board body bold bond boolean
  boring borrowed bot both bottom bought bound boundary box boy bracket
  bracketed braille branch breach break breakdown breathe brew brief
  briefly briefs bright brightly bring brings broaden broader broken
  brother browser bubble bucket budget buffer bug build builds built bulk
  bullet bundler buries burn burst business busy but button buttons buys
  by bypass byte bytes cache caches cage calculate calibrate calibration
  call callback caller callers calling calls calm came camel can canary
  cancel candidate cannot canvas cap capacity capital capitals card
  careful carefully carries carrot carry carrying case cases catch
  category cause ceiling cell ceremony cgo chain challenge chance change
  chapter character characters charge chat cheap cheapest check checker
  checkers checklist checklists checkout checkpoint child children choice
  choose chose chunks circular citation cite claim claimed claims class
  classification claude clean cleanup clear clearance clearly click
  clicker clicks client climbs clips clock clockwise clogged clone clones
  close closer closes cloud coarse code coerces coil cold collapse
  collapsed collapsible collect collides colon color colour column
  combines come comfortable command commands commas comment comments
  commit commits common communicate compact compaction compacts compare
  compatibility compatible compile compiled compiler compiles complete
  completed completion complex component compose composes comprehension
  compress compression computed computes concentration concept condition
  confidence confident confidential confidently config configuration
  configure configuring confirm conflict confuse connect connection
  connotation constant constantly consumer consumes consumption contacting
  contain container contamination content contents context continuation
  continue continuous continuously contour contract contraction
  contractions contrast contributes control conventions conversation cool
  coordinates copilot copy corner corporate correct correction corrective
  correctly cost costs could count counter counterclockwise course cover
  covers craft crash create credentials criteria criterion crossing
  current curve customer customizations cut cycle damage damaged dangerous
  dark dash data database date day dead deadline deaf debt debug debugger
  decide decided decides deciding decision declaration declared declares
  declines decrease dedicated deep deeper def default defect defective
  define defined definition delegated delete deletes deleting deletion
  deletions delivery delta demand demanded demands denial deny departs
  departure depend dependency dependent depending depends deploy depth
  derives deriving describe describes description design desk destination
  detail details detect detection deterioration deterministic device
  diagnosis diagnostics diagonally diagram dialog did die dies diff differ
  difference different differently differs difficult digit digital
  digitally digits dimension dimly direction directly directory dirty
  disagree disagrees disappear disassemble discard disconnect discovery
  discussion disengage disk dispatch dispatcher display distance distract
  diversion divide division do doc document documentation dodge does
  domain done door doors doorway dot dots double doubt down download
  downstream draft dragging drags drain dramatic draw drawing drawn draws
  dressed drift drifts drink drive driver drives driving drop drove drowns
  dry duck dump duplicate durable during dying each earlier early earn
  earned earns easily easy ecosystem edge edit edition editor education
  effect effective effort egress eight either electric electrical
  electrically electronic electronically element eleven ellipsis else
  elsewhere em email emergency emitter emoji emphasis empty enabler
  enablers enables enclosing encoding end endings enforceable enforced
  enforcement engage engine engineering enough enter entire entrance entry
  enum enumerable enumerate env envelope envelopes environment ephemeral
  equal equally equipment equivalent erase error escalating escalation
  escape escapes estimate evade event ever every everybody everything
  everywhere evidence exact exactly examine example except exception
  exceptions exciting excludes executable exempt exemption exemptions
  exercise exhaust exist existence existing exists exit expand expect
  expects experience expired explain explanation explicit explorer
  explosion explosive expose expression extend extension external
  externally extinguish extra eyes facial facility fact fail failing fails
  failure faithfully fake fakes fall false families family far fast faster
  fault faults feature fed feed feedback feeding feeds feel feeling female
  fence fenced fences fetch fetches few fewer field fields fifteen fifth
  fifty fight file files filesystem fill filter filtered find findings
  fine finish finished finishes finishing finite fire fires firing first
  fits five fix fixer fixes fixture fixtures flag flame flammable flange
  flash flat flatten flattened flattening fleet flexible flick flight
  floor flow fluent fluid flush fly fold folder follow foot for forbidden
  forces forever forget form format formatter forward found four fraction
  fragment frame frameworks free freely freeze frequent frequently fresh
  from front frontmatter frozen full fully function funnel funny further
  future gain gains gaming gap gas gate gather gathering gathers gear
  general generalises generate generated generator generic gesture get git
  give given glance glob globally globs glossy gnu go god goes going gone
  good governed governs gradually grammar grant granted grants graph
  graphical green grep grew grey grid groove ground group growing grows
  guarantee guarantees guard guardrail guards guess guessing guidance
  gutter hacking had half halfway halves hand handback handed handing
  handle handles handover handovers hands hang happen happy hard hardest
  harmony harness has hash hastrustdialogaccepted have he head header
  headless health hear hearing heartbeat heat heavy height held help
  helper helpers her here heredoc hers herself hide hides high highlight
  him himself hints his history hit hold holder holding holds hole home
  honest honours hook hooks hope horizontal horizontally host hot hour
  hover how however hundred hundreds hunk hydraulic hydraulically hyphen
  hyphenated i icon id idea ideation idempotent identification identifier
  identify identity idle ids if ignore illuminate imagine immediately
  implement implementation implements implicitly implies import important
  imports impose imposes impossible improvement improvements in inarow
  inbox incident include incomplete incorrect incorrectly increase
  increment indent independently index indication indicator information
  inherits initial initially inject injection injury inline inner input
  inserted insertion inserts inside inspect inspection install
  installation installer installers instance instead institutional
  instruction instrument insulation intact integer integration intelligent
  intensity intent interact interchange interchangeable interesting
  interface intermittent intermittently internal internally international
  interpolated interpret interpreter interrupted interruption interval
  into invalid invariant invariants inverting investigation invoke
  irregular irregularly is isolate isolation issue issues it item
  iteration its itself javascript job join joined joining joins joint
  journal judge judged judgement judgment junction just kata kebab keep
  kept key keyed keys keyword kill kind kinds kit knob knobs know
  knowledge known label labelled labels lack lacks ladder laminated
  lamination land landing lands lane language lapses large largest last
  late later lateral laterally latest launches layer layout lays lead
  leading leaf leak leakage leans learn lease least leave leaves ledger
  left legal length less let letter letters level lexer library licence
  licensed life lifecycle lift light lighting lightly like likely limit
  line linear lines link lint linter linting lints linux liquid list
  listen listener listing lists literal live lives lnav load loads local
  location lock log lone long longer longest longitudinal longitudinally
  look lookahead lookup loop loopback loose loosely loosen loses losing
  loss lost lot loud loudly low lower lowercase ls lubricate machine
  machinery macos made magic magnetic magnetically mail main maintainer
  maintenance make male malfunction man managed manager managing mandatory
  manifest manual manually many map maps mark markdown marker marketplace
  markup mass match matcher matches matching material matrix matte matter
  matters mature maximum may me mean meaning meant meanwhile measure
  measurement measurements mechanical mechanically mechanism median
  mediation medical meet meeting meets member memory mend mends mention
  mentions menu merge merged merges merging merits message met method mid
  middle middleware migrated migration millisecond milliseconds mine
  minimum mint minted minter minting mints minus minute mirrors misdirect
  misfiring mismatch misread misreads misses missing mistake mistakes
  mixture mobile mock modal modals mode model moderate moderately modifier
  modifiers module modules moist moment momentarily monitor month more
  most motivation movable move movement ms msvc much multiply must mutate
  mutations my myself name narrows native natural near nearest necessary
  need needing needs negative neighbours neither nest nested nests net
  network neutral never new newer newest newline next nine ninety no
  nobody node noise non nonce none nor normal nose not notch note nothing
  notices notification nouns now nowhere null number numeric obey object
  objection of off offending offers offset offsets often ok old older
  oldest omit on once one only onto op opaque open opening operate
  operation operator opposite ops option optional or orchestration order
  ordinary organisation organize origin original orphan other otherwise
  our ours ourselves out outcome outcomes outdoors outer outermost outgrow
  outline outlives output outranks outright outside over overlap override
  overwriting overwritten overwrote owe owed owes own owner owns package
  page paid paint pair pane panel paragraph parallel parameter paraphrases
  park parked parks parse parser part participle particle particular pass
  passable passed passes passing passive password past paste patch path
  paths patience pattern pay paying payload pays people per percent
  perfect performance period permanent permanently permission permit
  permits permitted perpendicular person personnel phase phases phone
  phrase pick picks picture piece piled pin pink pinned pins pipe place
  placeholder placement places plain plainer plan planning platform play
  please plugin plugins plumbing plus pneumatic pneumatically point
  pointer poisonous policy polish poll polling pools port position
  possible possibly posts powershell pre precaution precision preference
  prefers prefill preparation prepare prepending prepends present
  preserves press presses pressurization pressurize prevent preview price
  primary principle print prints priority privacy private probable
  probably probe problem procedure proceed proceeds process produce
  production program progress progressive project projected projecting
  projection projector projects promise promotion prompt pronouns proof
  property proportion proposal proposed prose protection protective
  protocol prove proven provenance proves proving proxy publication
  publish publishes pull pulse punctuation pure purpose push put
  quackitect qualifies quality query question queue quickly quiet quit
  quits quiz quote race radial radially radioactive radius raises ran
  random randomly range rank ranked ranking ranks rare rate ratio
  rationale raw re reach reaches reaching reaction reacts read readback
  reader readings ready real really rear reason reasonable rebases rebuild
  rebuilds receipt receive recognisable recommend record recover
  recoverable recovery recurse recursion recycle red reddens redesign
  redesigns redirections redraw reduces ref refactor refer reference
  refills reflection reflows refresher refuel refusal refuse refused
  refuses refusing regex regexp regions register registering registers
  registrations registry regressions regular regularly reject rejection
  related relation relative relaunch release reload remain remaining
  remedy remember remote removal remove rename render rendered renderer
  renders renumbering reopen reopens reordered repair repeat repeated
  repeating repeats replace replacement replay replays reply repo report
  reporter repository request require required requirement requires rerun
  research reserve reserved resets resident resistant resolve resolver
  resolves resolving response responsibility responsible rest restarting
  restarts restore restored restriction restrictions result resumes retain
  retire retires retract retraction retries retro retry retunes return
  returns reuse reuses review reviewer revisit reward rewrite rewrites
  ride rides right rigid ripgrep risk road roads role roles room root
  rotates rotation rots rough round rounded rounds roundup route routes
  routine routing row rule rulers rules ruleset ruling run runaway rung
  runme runner running runtime s sad safe safely safety said same samples
  sampling sandbox satisfactorily satisfactory save say scalar scalars
  scale scales scalpel scan schedule schema scope scopes score scores
  scratchpad screen screenshot script scripture scroll scrolls se search
  second secondary seconds secret section sections see seen segment select
  selection semicolon send sense sensible sensitive sent sentence separate
  separately separation separator sequence serialize serializes serious
  server serves service serviceable servicing serving session sessions
  sessionstart set settable setting settle settled settles setup seven
  seventy several shadow shake shape share sharp she shebang sheet shell
  shifted shifts shiny ships shock short shortcut shorter should shout
  show shrinks shut shuts sibling siblings side sidebar sign signal signer
  silence silent silently simple simulate since single sipoc sister sit
  site sits situation six sixteen sixth sixty size skill skills skim skims
  skip skipping skips slash slice slides slop slot slow slowly small
  smaller smallest smoke smooth smoothly so socket soft solid solution
  solves some somebody something sometimes somewhere sooner sort sorts
  sound source space span spare spawn speak speakers special specially
  specific specification specified speech speed spell spelling spells
  spend spending spends spent spill split splits spoken spot spray stable
  stack staffing staged stake stale stalls stamp stamps stand standard
  standing stands star start startup state states status stay stdin stdio
  steel step sticks still stood stop storage store stores story straight
  stranger stray streams stress strict string strip stripe strong stronger
  structural structurally structure style sub subagent subassembly
  subcommand subject submission submit submits submitted subsequent
  subsequently subset substitution subsystems subtract success successful
  successor successors such sudden suddenly suggest suite suites sum
  summary supplier supply supported sure surface survey surveyed survived
  survives surviving swap sweep swept switch switches symbol symbolic
  symmetrical symmetrically symptom sync synced synchronized system table
  tag tagger tail take taken takes taking talk talking tap target task
  taught taxonomy teach teaches teaching team tell temp template
  temporarily temporary ten tend tense tenth terminal terms terse tertiary
  test testable text than thanks that the their theirs them theme
  themselves then there therefore these they thick thickness thin thing
  things think thinning third this those thought thoughts thousand
  thousandth three threshold through throughout throw throwaway throws
  thus tick ticket tickets ticks tie tiers ties tight tighten tighter
  tightly tilt time timeline timeout timestamp timestamped timestamps tip
  title tmp to toast today todo together toggle token tokens tolerance
  tomorrow tone too took tool tooth top torn total touch toward trace
  tracked tracks trails train training transaction transcript transition
  transitions translate translation translator transmit transparent travel
  treat tree trigger trims trip tripwires trivial true truncates trunk
  trust trusted trusts truth try tune turn twelfth twelve twenty twice two
  type typical typo un unable unattended unavailable unbound unbuilt
  uncertainty unchanged unclaimed unclean uncommitted uncovers under
  underscore understand understood undisclosed undo undoes unexpected
  unfinished unfold uninstall unique unit universal unknown unless unlock
  unmerged unnamed unproven unreachable unread unreadable unrunnable
  unsatisfactorily unsatisfactory unserviceable unset unstructured
  unticked until untracked untried untrue unusual unusually unwanted
  unwind unwraps up update upgrade upload uppercase upward urgency urgent
  url us use user usual usually v vale valid validate validation validator
  validators value variable variables varies vehicle verb verbatim verbs
  verdict verification verify vernacular version vertical vertically very
  video view viewer violations visible visual visually vocabulary voice
  volume vote vs vscode wait wakes walk wall wanders want warm warning
  warns was watch watcher way we weak wear weather web webview week weigh
  weight well went were wet what whatever when whenever where wherever
  whether which whichever while whitelist whitespace who whoever whole
  whom whose why wide widens wider widget widgets width wildcard will wind
  window windows winget winning wins wire wires wiring wish with within
  without woman word words work workday worker workflow working works
  workspace worktree worry worse worsens worst worth would wrapper wraps
  write writer writers writeups written wrong wrongly wrote x yaml year
  yellow yes yet yields you young your yours yourself yourselves zero zip
  `

  inside := {}
  for w in text.re_split(`\s+`, list, -1) {
    if len(w) > 0 { inside[w] = 1 }
  }

  roads := `
  acceptable=permitted alternate=alternative avoid=prevent fit=install
  insert=put perform=do portion=part rotate=turn secure=attach shall=must
  using=use utilise=use utilize=use
  `

  swaps := {}
  for one in text.re_split(`\s+`, roads, -1) {
    pair := text.split(one, "=")
    if len(pair) == 2 { swaps[pair[0]] = pair[1] }
  }

  known := func(w) {
    if inside[w] != undefined { return true }
    n := len(w)
    if n > 3 && text.has_suffix(w, "ies") {
      if inside[w[:n-3] + "y"] != undefined { return true }
    }
    if n > 2 && text.has_suffix(w, "es") {
      if inside[w[:n-2]] != undefined { return true }
      if inside[w[:n-1]] != undefined { return true }
    }
    if n > 1 && text.has_suffix(w, "s") {
      if inside[w[:n-1]] != undefined { return true }
    }
    if n > 3 && text.has_suffix(w, "ied") {
      if inside[w[:n-3] + "y"] != undefined { return true }
    }
    if n > 2 && text.has_suffix(w, "ed") {
      if inside[w[:n-2]] != undefined { return true }
      if inside[w[:n-1]] != undefined { return true }
      if inside[w[:n-3]] != undefined { return true }
    }
    if n > 3 && text.has_suffix(w, "ing") {
      if inside[w[:n-3]] != undefined { return true }
      if inside[w[:n-3] + "e"] != undefined { return true }
      if inside[w[:n-4]] != undefined { return true }
    }
    return false
  }

  said := plain(scope)
  said = blanked(said, `(?m)^#{1,6} +`)
  said = blanked(said, `(?m)^[ \t]*(?:[-*+]|[0-9]+[.)]) +`)
  said = blanked(said, `(?m)^[ \t]*> ?`)
  said = blanked(said, `\|`)
  said = blanked(said, `[*_]`)
  said = blanked(said, "TL;DR")
  said = blanked(said, "isnt")

  opens := func(at) {
    i := at - 1
    for i >= 0 {
      c := said[i:i+1]
      if c == " " || c == "\t" || c == "\n" { i-- ; continue }
      if c == "." || c == "!" || c == "?" || c == ":" || c == ";" { return true }
      return false
    }
    return true
  }

  found := text.re_find(`[A-Za-z][A-Za-z0-9'’-]*`, said, -1)
  if is_undefined(found) { found = [] }

  for one in found {
    m := one[0]
    w := m.text
    if text.re_match(`[0-9_]`, w) { continue }
    head := w[0:1]
    if head != text.to_lower(head) && !opens(m.begin) { continue }
    if len(w) == 1 && head != text.to_lower(head) { continue }

    low := text.to_lower(w)
    low = text.trim_suffix(low, "'s")
    low = text.trim_suffix(low, "’s")
    if text.contains(low, "'") || text.contains(low, "’") { continue }

    bad := ""
    for part in text.split(low, "-") {
      p := text.trim_space(part)
      if len(p) == 0 { continue }
      if known(p) { continue }
      bad = p
      break
    }
    if bad == "" { continue }

    road := swaps[bad]
    say := bad + " stands outside the words this tree writes. "
    if road != undefined {
      say += "Write " + road + " instead."
    } else {
      say += "Write a word from spec/vocabulary/words.yml, or add " + bad +
        " to it with `from: session` and a meaning."
    }
    matches = append(matches, {begin: m.begin, end: m.end, message: say})
  }
