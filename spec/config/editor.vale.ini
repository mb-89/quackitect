# The Vale extension's config, which turns on no style, so the extension draws
# nothing. The problems panel reads the battery's list through se-lsp, and that
# list carries the tense reader raw Vale lacks.
# [[spec/design_output/lsp#the-panel-reads-the-battery]]
StylesPath = styles
MinAlertLevel = suggestion
