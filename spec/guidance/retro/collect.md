---
kind: [[guidance]]
scope: ["whoever runs the collect step of a retro"]
rationale: [[spec/rationales/collecting]]
---

# Actionables

1. Run `./RUNME.sh retro collect <retro>` first, while no other hand holds a ticket. *
2. Read the input folder and nothing else, because an input you go looking for is a fault in the verb. *
3. Read the count it prints for each source, because a short answer reads like a whole one. *
4. Expect `.se` to hold `.runtime` and `.retro` alone, and fix the writer of anything else standing there. *
5. Write a file a session throws away under `.se/.runtime`, and history straight under `.se`. *
6. Name every refused line of the manifest, and move that file by hand before the next step.
