---
kind: [[ticket]]
state: open
steps:
  - name: feedback
    does: takes the owner's field feedback one point at a time, and writes each confirmed point as a private note
    by: person
    needs: ["ticket note"]
    reads: [[spec/guidance/retro/feedback]]
    evidence:
      - name: notes
        form: list
        says: every private note this step writes, one a line
  - name: collect
    does: moves everything the private folder holds past its dot folders into the retro's input folder, and copies the transcripts, the memory and the scratchpads beside it
    by: anyone
    to: owner
    input: feedback
    needs: ["retro"]
    reads: [[spec/guidance/retro/collect]]
    evidence:
      - name: run
        form: command
        expects: 0
        says: retro collect, which leaves the private folder holding .runtime and .retro alone
  - name: effect
    does: counts the last retro's class patterns over this input, and says per class whether its failure still occurs and at what rate
    by: anyone
    input: collect
    needs: ["retro"]
    reads: [[spec/guidance/retro/effect]]
    evidence:
      - name: effect
        form: command
        expects: 0
        says: retro effect, which answers each earlier class with its rate before and now
  - name: audit
    does: auditors walk the checklist over what the window lands, and write a findings column each
    by: anyone
    input: effect
    needs: ["retro", "retro audit"]
    reads: [[spec/guidance/retro/audit]]
    checklist:
      - the code the window lands follows [[spec/guidance/code/code]]
      - every change lands with a test that fails before it and passes after, as [[spec/guidance/code/testing]] asks
      - every note the window writes holds the shape [[spec/guidance/guidance]] asks
      - the work follows [[spec/guidance/working]], a turn at a time
      - a change reaches every place holding the same thing, each writer, each copy and each name
      - the running system carries the change, rebuilt, restarted or reloaded
      - every claim to the owner rests on a check against what the owner sees
      - every mechanical step runs through a verb, and no hand repeats it
      - every piece of work lands in the place owning its topic
      - "no sentence the window writes says what the structure beside it holds: a count, a member, a link, a connection"
      - every experiment stands decided, which `retro audit` answers
      - every refusal teaches the hand, and no hand retries it blind
      - the battery's report reads beside the last retro's, and each part that grows has a finding, as [[spec/guidance/retro/effect]] asks
      - the chat's answers score under `answer.ceiling` in `spec/config/level0.json`, which `./RUNME.sh voice measure --transcripts` over the window's transcripts answers
    evidence:
      - name: audits
        form: list
        says: every audit findings file this step writes, one a line
      - name: trials
        form: command
        expects: 0
        says: retro audit, which answers 0 once every experiment stands decided
  - name: chapter
    does: cuts the window into chapters of about six hours, and the engine hands every chapter its lines
    by: anyone
    input: audit
    needs: ["retro"]
    reads: [[spec/guidance/retro/chapter]]
    evidence:
      - name: chapters
        form: command
        expects: 0
        says: retro chapters, which refuses a gap or an overlap between two chapters
  - name: read
    does: one reader a chapter answers the five starfish questions and the five improvements, and the engine draws the matrix
    by: anyone
    input: chapter
    needs: ["retro"]
    reads: [[[spec/guidance/retro/read]], [[spec/guidance/retro/signals]]]
    evidence:
      - name: matrix
        form: command
        expects: 0
        says: retro matrix, which refuses a chapter standing without its findings
  - name: classify
    does: collapses the findings into class fixes with a rate each, gives every finding a disposition, and names every promotion
    by: anyone
    input: read
    needs: ["retro"]
    reads: [[spec/guidance/retro/classify]]
    evidence:
      - name: classes
        form: command
        expects: 0
        says: retro classes, which counts each rate and refuses a finding with no disposition
  - name: check
    does: reads every class against the tree as it stands, closes the ones the tree answers already, and mints a ticket a class standing open
    by: anyone
    to: owner
    input: classify
    needs: ["retro"]
    reads: [[spec/guidance/retro/check]]
    evidence:
      - name: minted
        form: command
        expects: 0
        says: retro mint, which refuses a class carrying no status and mints the rest
step: check
process: [[spec/processes/retro]]
process_hash: a65d754dda816a7e
record:
  - step: feedback
    hand: box d6f05e3a585030 · claude-code · the owner says so
    hash_before: 65c3028de1b3bd13e05d5fb861617a8dcd0dc8e1
    hash_after: 65c3028de1b3bd13e05d5fb861617a8dcd0dc8e1
  - step: feedback
    hand: box d6f05e3a585030 · claude-code · the owner says so
    hash_before: 21143f2fe7e05b17cd836ea2ec40f5606d4c3869
    hash_after: 21143f2fe7e05b17cd836ea2ec40f5606d4c3869
    returns: 1
    why: the hand takes it back
  - step: feedback
    hand: box d6f05e3a585030 · claude-code · the owner says so
    hash_before: 4ef8f080a794f614ca5b63e9ec3600551d242c9b
    hash_after: 4ef8f080a794f614ca5b63e9ec3600551d242c9b
  - step: collect
    hand: box d6f05e3a585030 · claude-code
    hash_before: ae477d8d2bed0d2f91eafaae477a40c7a4912d6e
    hash_after: ae477d8d2bed0d2f91eafaae477a40c7a4912d6e
    answered:
      - name: run
        exit: 0
        said: .se/.retro/retro-65c3028/input holds a whole run already, and this one changes nothing.
  - step: effect
    hand: box d6f05e3a585030 · claude-code
    hash_before: 3ad2b85165a7942c34e876377f204ce9d0572df2
    hash_after: 3ad2b85165a7942c34e876377f204ce9d0572df2
    answered:
      - name: effect
        exit: 0
        said: battery  21854 to 26939 ms  4 new, 1 grown, 4 gone
  - step: audit
    hand: box d6f05e3a585030 · claude-code
    hash_before: fddbda2b8a8a69cbe03b9c754afbaceb601557df
    hash_after: fddbda2b8a8a69cbe03b9c754afbaceb601557df
    answered:
      - name: trials
        exit: 0
        said: Every experiment stands decided, so the retro closes.
  - step: chapter
    hand: box d6f05e3a585030 · claude-code
    hash_before: 447537239da57e1c0f2ac24a408848ca1d0b0116
    hash_after: 447537239da57e1c0f2ac24a408848ca1d0b0116
    answered:
      - name: chapters
        exit: 0
        said: "c4  2026-09-25T14:00:00.000Z to 2026-09-26T12:00:00.000Z  15371 line(s)  the editor: the inset, the work buttons, the ta"
  - step: read
    hand: box d6f05e3a585030 · claude-code
    hash_before: 4ded3e490514e29be7c0dfa902ae91a85d78829c
    hash_after: 4ded3e490514e29be7c0dfa902ae91a85d78829c
    answered:
      - name: matrix
        exit: 0
        said: The report of retro-65c3028 draws 7 column(s), bottom line first, in its retro folder.
  - step: audit
    hand: box d6f05e3a585030 · claude-code
    hash_before: 35c58acbb5ea768bf44f24ded0767cb473227bf8
    hash_after: 35c58acbb5ea768bf44f24ded0767cb473227bf8
    returns: 1
    why: the hand takes it back
  - step: audit
    hand: box d6f05e3a585030 · claude-code
    hash_before: 05159f3b85f8fe0e80b66566cf313f785fd1d72e
    hash_after: 05159f3b85f8fe0e80b66566cf313f785fd1d72e
    answered:
      - name: trials
        exit: 0
        said: Every experiment stands decided, so the retro closes.
  - step: chapter
    hand: box d6f05e3a585030 · claude-code
    hash_before: 3b9b33792171d5384b8c7c3029e9d124f41cfd4c
    hash_after: 3b9b33792171d5384b8c7c3029e9d124f41cfd4c
    answered:
      - name: chapters
        exit: 0
        said: "c4  2026-09-25T14:00:00.000Z to 2026-09-26T12:00:00.000Z  15371 line(s)  the editor: the inset, the work buttons, the ta"
  - step: read
    hand: box d6f05e3a585030 · claude-code
    hash_before: d73042d235dcdc2661e1ae90eb3432e0bcb02d70
    hash_after: d73042d235dcdc2661e1ae90eb3432e0bcb02d70
    answered:
      - name: matrix
        exit: 0
        said: The report of retro-65c3028 draws 7 column(s), bottom line first, in its retro folder.
  - step: classify
    hand: box d6f05e3a585030 · claude-code
    hash_before: 9b946892a2d6fb888df0eb3ff6d4248aef2ba041
    hash_after: 9b946892a2d6fb888df0eb3ff6d4248aef2ba041
    answered:
      - name: classes
        exit: 0
        said: 24 active hour(s), and every finding, note and memory carries a disposition.
---

# Ask

<!-- why, as text: what calls for it, as notes standing open, an iteration ending, or the owner asking -->

The owner asks for a retro, and private notes stand open.

# feedback

<!-- takes the owner's field feedback one point at a time, and writes each confirmed point as a private note -->

## notes

<!-- every private note this step writes, one a line -->

<!-- the form is list -->

- `the-window-size-stays-unknown`
- `the-sidebar-counts-the-tab`
- `the-refusal-names-the-todo`
- `a-verb-lands-cloud-branches`
- `the-bench-note-drops-writeat`

# collect

<!-- moves everything the private folder holds past its dot folders into the retro's input folder, and copies the transcripts, the memory and the scratchpads beside it -->

## run

<!-- retro collect, which leaves the private folder holding .runtime and .retro alone -->

<!-- the form is command -->

    ./RUNME.sh retro collect retro-65c3028

# effect

<!-- counts the last retro's class patterns over this input, and says per class whether its failure still occurs and at what rate -->

## effect

<!-- retro effect, which answers each earlier class with its rate before and now -->

<!-- the form is command -->

    ./RUNME.sh retro effect retro-65c3028

# audit

<!-- auditors walk the checklist over what the window lands, and write a findings column each -->

## audits

<!-- every audit findings file this step writes, one a line -->

<!-- the form is list -->

- findings/audit-code.md
- findings/audit-notes.md
- findings/audit-conduct.md

## trials

<!-- retro audit, which answers 0 once every experiment stands decided -->

<!-- the form is command -->

    ./RUNME.sh retro audit retro-65c3028

## checked

<!-- one line per item of the checklist, on how you take it into account -->

<!-- the form is checklist -->

- the code: broken, comments carry a sentence beside their pointer, and a header counts its members
- the tests: broken, two commits land code before any red run
- the notes: broken, a screenshot and a page land in governed folders, and the check misses both
- the turn: broken, the stop claims the owner asks to talk where nobody asks
- one place: broken, a helper spells the Go server's port wait again, and a rename leaves its old file
- the running system: broken, the sidebar badge redraws on config alone, and the extension fails its load once
- the claims: broken, the icon count closes on a fake verb, with no read of the sidebar
- the verbs: broken, raw commits and pushes on main, and splits and red runs by raw git
- the owner's place: broken, a funnel note holds rulings, and step commits sweep in other files
- the counts: broken, headings and ticket evidence count the structure beside them, and this item prints as an object
- the experiments: held, the audit verb answers every one decided
- the refusals: broken, the write door takes any open ticket, and a judge refusal names no line
- the battery: broken, four cases grow or land slow, each with a smaller fixture named
- the answers: broken, the measure reads well past the ceiling, and it counts progress lines as answers

# chapter

<!-- cuts the window into chapters of about six hours, and the engine hands every chapter its lines -->

## chapters

<!-- retro chapters, which refuses a gap or an overlap between two chapters -->

<!-- the form is command -->

    ./RUNME.sh retro chapters retro-65c3028

# read

<!-- one reader a chapter answers the five starfish questions and the five improvements, and the engine draws the matrix -->

## matrix

<!-- retro matrix, which refuses a chapter standing without its findings -->

<!-- the form is command -->

    ./RUNME.sh retro matrix retro-65c3028

# classify

<!-- collapses the findings into class fixes with a rate each, gives every finding a disposition, and names every promotion -->

## classes

<!-- retro classes, which counts each rate and refuses a finding with no disposition -->

<!-- the form is command -->

    ./RUNME.sh retro classes retro-65c3028

# check

<!-- reads every class against the tree as it stands, closes the ones the tree answers already, and mints a ticket a class standing open -->

## minted

<!-- retro mint, which refuses a class carrying no status and mints the rest -->

<!-- the form is command -->

    ./RUNME.sh retro mint retro-65c3028

# Discussion

<!-- what anybody adds, at any time, on this ticket -->

The effect step's reading, off `effect.json`:

| the class | its rate | what classify does |
|---|---|---|
| `claim-from-recall` | grows | carries it, and names its fix as failing |
| `prose-verb-reads-short` | grows | carries it, and names its fix as failing |
| `binding-blind`, `owner-hears-late`, `check-on-a-stand-in` | falls a little | carries each, since the failure still occurs |
| `restated-fact-passes` | nearly gone | retires it, and keeps its fix |
| every other class | falls | keeps its fix |
| the battery | grows | a finding for classify, off `battery.json` |

The check step's reading: every class stands open, and the mint hands each its ticket in a cloud group:

- [[spec/tickets/the-gates-read-the-state]]
- [[spec/tickets/the-servers-and-views-hold]]
- [[spec/tickets/the-verbs-land-whole]]
- [[spec/tickets/the-owners-word-reaches-work]]
- [[spec/tickets/each-thing-stands-in-place]]

The owner takes each proposal below at the report's read:

| class | what the tree says | the proposal |
|---|---|---|
| `stop-claim-misreads-state` | `spec/design_output/stop.md` holds the helper claim under every binding, and a todo waiting on its own helper loops | the ticket leaves out the clause failing the claim while a todo waits |
| `todo-stands-outside-the-queue` | `spec/design_output/pull.md` puts every todo before every ticket | the ticket leaves out the clause placing a todo behind the ticket in hand |
| `notes-land-in-the-wrong-place` | a page under `spec/design_input` stands as the owner's own | each page past its folder's kind draws a warning, and the owner moves it |
| `restated-fact-passes` | `.vale.ini` takes `CountedList` off tickets as a ruling | the ticket reads a heading over a list or a table alone |
| `retro-steps-stop-short` | `spec/guidance/retro/collect.md` drains every entry past the dot folders | `collect --again` keeps `.se/scripts`, where working rule 8 sends a script |
| `one-line-faults-wait` | the note `a-hand-holds-two-tickets` asks a change to the pull's design | the note moves to the process work |
| `one-line-faults-wait` | `retro mint` mints on the standard route alone | one ticket carries every small fault |
