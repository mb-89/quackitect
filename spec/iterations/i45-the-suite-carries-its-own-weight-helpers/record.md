---
id: i45-the-suite-carries-its-own-weight-helpers
status: seeded
opened: 2026-08-20T16:49:57.394Z
goal: "The suite carries its own weight: helpers live in one home, refusal-only cases share a boot, the dead guard pin retargets to frontmatter, and testlint refuses the next local copy."
vision: "Item list in spec/overhauls/2026-08-20/plan.md, seed 7. The measured baseline: tests duplicate 4.1 percent of their significant lines, against 1.2 in the engine.\n\nDONE LOOKS LIKE: gitInit, refusal() and the seed-an-iteration scaffold exist once in helpers.ts; about ten boot walks and roughly ninety seconds of battery wall clock recovered by sharing servers across refusal-only cases; fallback-outcome reads the row's YAML instead of regex-matching a prose quote of a removed guard; testlint's three new cases green; the orphan voice-matrix fixture is gone per owner decision 2; helpers.readDocs deleted."
inputs:
  - "spec/overhauls/2026-08-20/plan.md"
depends_on: []
---

# i45-the-suite-carries-its-own-weight-helpers

## Goal

The suite carries its own weight: helpers live in one home, refusal-only cases share a boot, the dead guard pin retargets to frontmatter, and testlint refuses the next local copy.

## Rough vision

Item list in spec/overhauls/2026-08-20/plan.md, seed 7. The measured baseline: tests duplicate 4.1 percent of their significant lines, against 1.2 in the engine.

DONE LOOKS LIKE: gitInit, refusal() and the seed-an-iteration scaffold exist once in helpers.ts; about ten boot walks and roughly ninety seconds of battery wall clock recovered by sharing servers across refusal-only cases; fallback-outcome reads the row's YAML instead of regex-matching a prose quote of a removed guard; testlint's three new cases green; the orphan voice-matrix fixture is gone per owner decision 2; helpers.readDocs deleted.

## Inputs

- spec/overhauls/2026-08-20/plan.md
