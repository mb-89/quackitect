---
form: a-usable-reason
amended: 2026-08-26T13:28:38.452Z by agent — a Cyrillic character slipped into one word of the follow-up
by: agent
signed_off: 2026-08-26T13:28:23.443Z
authors: agent
files: null
---

# Evidence form / a-usable-reason

## current_situation

UNSETTLED, on a population of one.

The tree holds exactly one departure list, at `deliverable/machines/widget-exemptions.md`. It was read whole.

### The one departure, and its reason is good

Line 51 names `deliverable/engine/bin/mermaid-check.ts`. Its reason says the file is a diagnostic page a maintainer opens to see whether a document's diagrams parse, that it renders nothing about the walk, and that the panel never reaches it.

A reviewer can act on that. It names what the file is, why the rule does not fit it, and the specific test the rule turns on. Nothing about it is boilerplate.

### One case is not evidence

The assumption is about what a POPULATION of authors writes under a refusal. A single entry by a careful author says nothing about the population.

The falsifier the assumption names is a column of near-identical sentences. A column of one cannot be near-identical to anything.

### What the file records instead, and it is worth more

The list used to hold 21 entries. The rule changed on 2026-08-23, from asking whether the editor registry NAMES a module to asking whether the VS Code panel REACHES it. That left three, two of which were folded into the surface.

Sharpening the predicate collapsed the list by twenty entries. That is a measured fact about departure lists in this tree, and it is not what the spike went looking for.

It cuts against the worry the assumption carries. On this one precedent, a long list of thin reasons signals that the PREDICATE is wrong rather than that the authors are lazy.

## built

- spec/trace/experiment/exp-can-a-reader-act-on-the-departures-the-tree-holds.md

## follow_up

- The assumption stays open. Ready when the tree holds more than one departure, which the build itself will produce.
- The predicate finding belongs in the design. A departure list that grows long is first evidence about the rule, and only then evidence about the authors. `el-door-rule` should be shaped so a long list is readable as a signal rather than as a cost of doing business.
- Nothing here needs a person. The population grows as a side effect of building, so no separate work is owed.

## anything_else

The falsifier and the fallback were both written before the run. The fallback was that boilerplate reasons drop the reason column and leave a plain allow-list.

Neither fired, because the run could not reach either verdict. Saying so is the result.
