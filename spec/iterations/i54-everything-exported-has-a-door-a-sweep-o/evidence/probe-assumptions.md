---
form: probe-assumptions
by: agent
signed_off: 2026-08-26T11:51:49.438Z
authors: agent
files: null
---

# Evidence form / probe-assumptions

## current_situation

Eight probe cells stood empty. Six belong to assumptions opened at the previous state; two are older entries owed a date.

All eight are now answered against the real channel, and every answer says which channel was actually used.

Three ran here and settled. Two could not run in this container and say why. Two are scheduled on their own node's instruction. One came back FALSE and has changed kind.

## probes

| raid | probe | probed |
| --- | --- | --- |
| [[raid-asm-every-export-in-this-tree-is-declared-statically]] | holds. Searched deliverable/engine for the five computed-export shapes — a star re-export, an assignment onto module.exports, an assignment into an exports index, an Object.assign onto exports, and an awaited default export. Zero hits across the whole tree. One hit would have falsified it. | 2026-08-26 |
| [[raid-asm-the-toolchain-here-enumerates-exports-with-no-new-dependency]] | holds, by the same route the widget guard already uses. That guard finds its emitters with a regular expression and no parser at all, at deliverable/engine/widgets.ts line 136. A regular expression over the same tree answered the export question in one call, so no parser was needed to run the check either. It only reads static shapes, which the sibling assumption establishes. | 2026-08-26 |
| [[raid-asm-every-write-that-adds-a-departure-passes-through-the-lane]] | unprobed, and the reason is the container rather than the question. The probe reads the call log for writes to the exemption list. This box was cloned fresh, so its log holds one session, and that session never touched the file. The zero is an absence of history, not an absence of shell writes. | 2026-08-26 |
| [[raid-asm-an-exemption-key-reads-the-same-on-every-platform]] | unprobed. The check needs a Windows machine and this is a Linux container. What the read DID establish is that the question is real — deliverable/engine/widgets.ts line 136 writes the forward slash into the matching pattern itself, so the key is platform-shaped by construction. | 2026-08-26 |
| [[raid-asm-each-neighbour-holds-one-conversation-with-this-system]] | false. Walked all seven neighbours and wrote what this system says to each and what it says back. Two pairs collapsed and two neighbours hold no conversation at all. Four doors result, one primary and three secondary. The entry is now an issue and carries the table. | 2026-08-26 |
| [[raid-asm-an-author-refused-at-write-time-states-a-usable-reason]] | holds on a sample of one, and the sample is the whole population. The exemption list carries one declared departure. Its reason names the diagram check, the maintainer's act, and why the panel never reaches it — all three specific to that file. One line cannot settle how authors behave under deadline. | 2026-08-26 |
| [[raid-asm-a-demanded-reason-is-a-considered-reason]] | holds at this size, on the sample the node itself names. The widget registry holds ONE entry and its reason is specific rather than general, so the count is one specific and zero boilerplate. The node's own caveat binds — one registry written as an experiment is weak evidence about one written under deadline. | 2026-08-26 |
| [[raid-asm-the-seven-heaviest-modules-speak-for-the-other-fifty-three]] | scheduled, on the node's own instruction. Its Probe section names who runs it — the walker of the state that sets the door's scope, before it sets it — and that is M4, not this state. The ten-file reading pass stands as written and is carried into the follow-up. | 2026-08-26 |

## follow_up

The falsified entry is the one that routes work. raid-asm-each-neighbour-holds-one-conversation-with-this-system is now an issue, and its table is the door set M4 partitions on.

Nothing fell with it. The door-naming requirement and the root function both named it, and both are strengthened rather than broken — they say the unit is a conversation, and the walk is what showed a conversation is not the same object as a neighbour.

The ten-file reading pass over the remaining engine-core writes stays owed. Its node names the state that runs it, which is the one that sets the door's scope.

Two probes need a machine this is not. The platform one needs Windows. The lane-write one needs a call log older than this container.

## anything_else

One probe result is worth reading before the gate.

The seven neighbours produce FOUR doors. Two of them share one conversation — the toolchain and the driven project are both addressed with a path and both answer with bytes, and the root is an argument rather than a conversation. Two hold no conversation at all. Obsidian changes the tree without speaking, and the cloud host is a condition rather than a party.

Four is the top of the range Cockburn's paper favours, and the walk was not aimed at it. It asked what each neighbour says and the count fell out.

The primary and secondary split came free with the same walk. One door is driven from outside and three are driven by us.

Two probes came back weak on sample rather than weak on method, and both are the reason-quality pair. The exemption list holds one entry. One line is honest evidence about that line and nothing more.
