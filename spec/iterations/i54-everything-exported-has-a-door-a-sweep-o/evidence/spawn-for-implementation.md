---
form: spawn-for-implementation
judgment: passed at 2026-08-26T13:43:24.087Z with deliverable/engine/bin/hands-spawned.ts@8be27987d4e0
by: agent
signed_off: 2026-08-26T13:43:15.183Z
authors: agent
files: null
---

# Evidence form / spawn-for-implementation

## current_situation

The prototype gate is blessed and M7 begins. This state asks which hand walks the build.

### No walker is spawned, and that is a standing ruling

The owner questioned whether delegation was paying. `meth-spawn-hands` had already measured that it usually does not, and the ruling was recorded at `evidence/pressure-test.md`. A hand is spawned only where a gate needs separation.

The guide walks this record directly, and that includes the build.

### What this costs, said plainly

A hand that writes the code and then verifies it is reviewing its own reasoning. That is exactly what this form's motivation warns against, and walking the build myself means accepting it here.

The cost is paid back at verification rather than avoided. That state spawns fresh eyes, which the contract names as the case where a hand is worth spawning.

### What the build enters with

Three constraints came out of M6 and they bind the code rather than the plan.

- `el-door-rule` states its coverage limit, because 38 of 178 modules reach a shell and a shell carries no path a guard can judge.
- `el-door-sweep` reuses a pass that already ran. As a lookup over parsed frontmatter a rule costs about 1 ms; as its own walk, about 100 ms.
- The door's scope comes from the read-modify-write pile, 37 sites of 151.

## hands

- [ ] Walker (deliverable/machines/methods/meth-spawn-hands.md) — not spawned. The guide walks the build directly, on the standing ruling that a hand is spawned only where a gate needs separation.

## follow_up

- The build starts now, on the three constraints M6 produced. Nothing here waits on a person.
- Verification spawns the fresh eyes. That is where the cost of walking the build myself is paid back, and the contract names it as the case where spawning is worth it.
- Two questions from the prototype gate stay with the owner and do not block the build: whether to build a predicate that dependency-cruiser already ships, and whether the outward door stays in scope on a count of 17 modules alone.

## anything_else

This form's motivation is the argument against what it records, and it is worth naming rather than smoothing over.

It says the roster is the whole reason verification is worth running. Leaving the roster empty means verification's separation has to come from the hand spawned there instead of from the build phase.

That is a weaker arrangement than two hands, and it is the one the measurement supports.
