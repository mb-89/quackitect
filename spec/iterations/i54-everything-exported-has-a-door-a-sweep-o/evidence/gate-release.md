---
form: gate-release
bless: blessed by agent
by: agent
signed_off: 2026-08-26T16:03:54.509Z
authors: agent
files:
---

# Evidence form / gate-release

## current_situation

dist/quackitect-8.1.0.zip stands, unpacks into a clean tree, and carries every fix this record made. The whole suite is green, preflight included, for the first time this session.

## market_block


## round_0_verify

- evidence vs claims: the archive was opened rather than trusted. Each of the four fixes was looked for inside the unpacked tree and each is present, along with the departure list and both refusal sections.
- types: clean. The typechecker caught two missing imports during the containment work and named both before any run.
- lint: clean, no suppression. Safe fixes ran and were named; two imports left unused by the refactor were reported and removed.
- tests: green. The confirm run covering the containment predicate, the condition checker, the departure remedy and the prompt-layer quote reported outcome green and exit 0 across 184 files.

## round_1_validate

- exercised against the goal: yes, and the package was checked by using it rather than by assembling it. The entry script is present and the root holds the eight things a stranger opens first.
- missing: the packaged engine was never STARTED. No lane call was served from the unpacked tree, because that needs a second machine and this is the one packaging.
- wrong: nothing standing. One report claim was corrected before this gate; nothing new was found here.
- out of scope: unchanged from the validation gate. Around eighty undeclared reaches and 17 help findings, both registered.
- prior art: no new comparison was made at this gate, and saying so is the honest answer. The six-system comparison stands from the implementation gate and was corrected once mid-record after reading dependency-cruiser's own rules reference. Repeating it here as if it were fresh would be the shape this field exists to refuse.

## goals_served

- Replace the hand-written entry-point list with a sweep over every exported entry point, so the guard stops depending on somebody maintaining a list: served, and the sweep ships inside the archive.
- Name everything exported that no surface can reach, and answer each one with either a door or a deletion: NOT SERVED on the goal's own words, unchanged from the validation gate. What ships enumerates runnable files; the goal promised exports. Four are named and none is answered.
- Give the two already-found pieces their door, so a capability the tests prove is a capability somebody can use: not served. drivenBy ships in the archive still reachable from no surface.
- Establish whether one door per capability actually pays here, by judging the measured sites rather than by counting them again: served. The judgment and its correction both ship in the departure list.
- Build the disk door and its declared exception list, generalising the shape that already governs widget markup: served and shipped, with the departure guard's remedy now applicable in both the on-disk and refused-write cases.
- Give reaching outward a central door that earns its keep, with guidance for a search and a place results are kept: not served, deliberately, reason recorded at the prototype gate.
- Record a reason for every door proposed and dismissed, so a dismissal is evidence rather than silence: served, and the archive carries both the reasons and the record that argues them.

## bound_breaches

- if-agent-harness-to-entrypoint: breached again, unchanged in cause and unchanged in disposition. Packaging spawns a process and waits, which the one-second bound counts as a lane call. Registered as raid-iss-the-one-second-bound-counts-calls-that-spawn-external-work, and the record that re-draws the bound owns it.

## round_2_red_team

- STEELMAN: shipping an artifact nobody started is shipping a guess. The check was a grep over an unpacked folder, not a running system => It HOLDS as a limit and it is stated in round 1 rather than hidden. What answers it partly is that the engine in the archive is byte-identical to the one that just ran 1905 cases green on this machine. What does not answer it is the install path, which no check here touched.
- The record ships with its headline goal unmet on its own words => Carried from the validation gate as the first override, and it is the honest reason this is a pass-with-overrides rather than a pass. The archive is not wrong; the goal statement is.
- Two fixes landed AFTER the implementation gate signed, so that gate's test count is stale => True. The gate says 1902 and the tree now stands at 1905 plus four cases from the containment work. The verification round here carries the current figure, and a reader comparing the two gates sees the growth rather than a contradiction.
- KILL-CRITERION for shipping at all: this is the wrong call if the archive cannot be used by somebody who did not build it => Looked for it, and found the honest edge. The tree unpacks and reads correctly. Whether it INSTALLS is untested here and always has been on an unattended box.

## raid_additions

- spec/trace/raid/raid-iss-an-absolute-canvas-reference-is-resolved-with-no-containment-check.md

## verdict

pass with overrides — the artifact stands and the dissents are the validation gate's, carried rather than re-argued.

FIRST: the kickoff goal is unmet on its own words. The sweep enumerates runnable files; the goal promised exports. The next record that touches this owes the restatement or the widening.

SECOND: the packaged engine was never started. The check was an unpack and a look, and that is stated rather than dressed up.

THIRD: the rule warns rather than refuses across roughly eighty undeclared reaches.

FOURTH: the reason-quality claim rests on two samples and one of the two was wrong.

WHAT EARNS THE SHIP is that the tree is green including preflight for the first time this session, that every fix was found inside the archive rather than assumed into it, and that three defects this record would otherwise have shipped were found by performing its demonstrations and by asking a reviewer to attack its own reports.

## follow_up

The record ships here. What it hands on:

- FIVE ITEMS IN emit_back, each naming its target file and the change, for the next record's onboard-retro to land or explicitly drop.
- THREE REGISTER ENTRIES opened at M8: the absolute canvas reference with no containment check, the write verbs announcing no door refusal, and the reason-quality assumption with its falsifying sweep.
- THE FIELD REPORT, owed to the person now, as a downloadable file and printed in full.
- FOUR NOTES for the retro, covering the reload pin and its fix, the surface's unrendered hop list, and the prompt-layer projection that never converged.

## anything_else

