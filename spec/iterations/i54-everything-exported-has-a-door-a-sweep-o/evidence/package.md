---
form: package
by: agent
signed_off: 2026-08-26T16:03:14.720Z
authors: agent
files: null
---

# Evidence form / package

## current_situation

The whole tree is green, preflight included, and the validation gate is blessed with four overrides. The archive assembles by script at version 8.1.0.

## package

- dist/quackitect-8.1.0.zip

## works

yes — unpacked into a fresh folder and checked by looking for this record's own work inside it rather than by assuming it travelled. All four fixes are present: the exported containment predicate in paths.ts, bases.ts asking it, the condition checker asking the runner's question in session.ts, and the prompt-layer quote in promptlayer.ts. The departure list and both refusal sections are there. The entry script RUNME.ps1 is present and the root holds the eight things a stranger opens first.

WHAT WAS NOT CHECKED, said plainly: the packaged engine was not started and no lane call was served from the unpacked tree. That needs a second machine, and this box is the one being packaged from.

## emit_back

- guidance/refusals.md, under ANYTHING THAT BLOCKS OWES A REMEDY: a remedy must be aimed at the state the refusal LEAVES BEHIND, not at the state that provoked it. A guard that stops a write leaves the offending line on no disk, so a patch anchored to that line matches nothing. Found by performing a demonstration; the refusal contract's promise of recovery in one turn failed in the commonest case it has.
- guidance/craft/software.md, under Guards that teach: a guard that fires on every run teaches the reader to ignore it. The prompt-layer projection warned LOST HAND EDIT on every single placement because it wrote one quote character the formatter then rewrote. The warning was correct in form and worthless in practice.
- guidance/craft/software.md, under A stored copy never beats a derived one: two readers of one truth must read the same STORE. The condition checker asked memory while the judgment runner asked disk, and after a reload they disagreed forever. This is the fifth defect of that family and the first where neither copy was wrong - only the choice of where to look.
- guidance/craft/software.md, on containment: a prefix test without a separator is not containment. Five sites held this question and disagreed; one guarded a write with a bare prefix, so a sibling directory sharing a name prefix passed as inside.
- deliverable/machines/methods/meth-validation-container.md: a demonstration report must say what it did NOT show. Three reports here imported a unit case's result into the demonstration's own voice, and a reviewer caught it. The pass line now in tsp-the-door-regime-is-met-as-a-person-meets-it says a demonstration confirming everything the builder believed has not been performed hostilely enough to count.

## follow_up

The release gate is next and it is the last judgment. The field report is owed to the person the moment it signs, as a downloadable file and printed in full.

The emit_back list above is the catching end of the next record's onboard-retro. Five items, each naming its target file and the change.

## anything_else

