---
form: run-candidates
by: agent
signed_off: 2026-08-26T12:22:20.192Z
authors: agent
files: null
---

# Evidence form / run-candidates

## current_situation

The chart is drawn and three lines cross it. Each candidate carries all four sections, because the check at build-chart demands the composition sections and not only the drawing ones.

So the drafts already stand and this state replaces them rather than filling blanks.

The three differ in kind rather than in degree. One builds the two-caller guard the requirements assume. One buys the sweep and drops the write-time half. One removes the rule entirely by making the violation unwritable.

## follow_up

COMPOSING AND JUDGING NEVER SHARE A STATE, and the drafts were written under that rule. None of the three carries a comparison, a ranking or a better-than, and the scoring state reads criteria fixed before any candidate existed.

TWO OF THE THREE ARE KILL CRITERIA WEARING A CANDIDATE'S CLOTHES, and that is deliberate. Buy-the-sweep is the gate's own first kill criterion drawn as a line, because a kill criterion nobody draws is a kill criterion nobody faces.

ONE NUMBER RESIZED EVERY LINE. A probe found the path resolver reaching 15 of 81 disk-touching modules, so no candidate now starts from a partially-adopted door. Each of the three pays for its own adoption in full.

WHAT THE COMPOSITION STILL OWES is the seam question the transform finder raised and no finder answered. Carve by purpose says four conversations; express a rule once says one expression. One rule engine reading four declarations satisfies both and appears in no candidate.

## anything_else

THREE LINES IS THE MINIMUM THE CHART WOULD ACCEPT, and it is worth saying that the minimum was not the target.

The row holds twenty options and most of them are not alternatives to each other. Some answer where the rule lives, some what a departure must carry, some how the standing violations are handled. A candidate takes one cell per row, so a row bundling several questions cannot express a combination of answers.

THAT IS A LIMIT OF THE ROW RATHER THAN OF THE DESIGN SPACE, and the honest response was to draw three lines that differ at the level the row can express — build, buy, or remove the problem — and to carry the finer answers inside each candidate's own sections.

A SHARPER ROW SPLIT WOULD HAVE SERVED THIS BETTER. Splitting the door regime into where-the-rule-lives, what-a-departure-carries and how-the-standing-violations-are-handled would have made the chart express combinations the way it is meant to. That is a finding about this record's partition rather than about the method, and it is recorded here rather than acted on.
