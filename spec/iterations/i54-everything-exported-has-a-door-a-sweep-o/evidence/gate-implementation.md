---
form: gate-implementation
bless: blessed by agent
by: agent
signed_off: 2026-08-26T15:14:17.972Z
authors: agent
files: null
---

# Evidence form / gate-implementation

## current_situation

The battery is green at rest: 1902 cases, 158 suites, zero failures. The sweep is green carrying its warnings, and the claims table stands observed by fresh eyes across two rounds.

### What shipped

One rule module holds each conversation's rule. Two write-time refusals read it and hold no copy of any predicate. A sweep asks the same rule about the whole tree. A departure list carries every module allowed past, with the reason it is allowed.

That reason-or-refuse is the one thing the six-system prior-art scan found nobody else doing.

### What did not ship, deliberately

The outward door. The prototype gate logged it as a dissent: that goal was served by a count of 17 modules and no probe, and seeding chunks for it would have built on the same kind of premise this record corrected four times.

### What the regime cannot yet prove about itself

The rule table takes any number of conversations and was proved against one. Two behaviours fixed this round — the per-door escape and the plural list resolution — cannot be exercised until a second door exists. That is carried as debt, not glossed.

## quality_ok

- [x] Dependencies stay layered
- [x] Every new element carries one stated responsibility
- [x] The linter and the complexity ceiling are clean, with no new suppression
- [owed] Every new behavior carries its check, and the battery is green at rest — raid-debt-the-door-regime-is-built-for-four-doors-and-proved-with-one
- [x] Nothing speculative shipped
- [x] What changed is findable
- [x] Every quick-and-dirty taken stands as a visible raid debt entry

## debt_taken

- spec/trace/raid/raid-debt-the-door-regime-is-built-for-four-doors-and-proved-with-one.md

## risks_acceptable

acceptable — the two risks this build added are bounded and both are registered. `raid-debt-the-door-regime-is-built-for-four-doors-and-proved-with-one` says the regime is built for four conversations and proved with one, and its trigger fires before a second door is registered rather than after. `raid-asm-the-drift-ceiling-s-per-state-term-is-chosen-not-measured` says a guard's bound was chosen rather than measured, and names the cheap experiment that would settle it. Neither can ship a wrong answer to a user; both can let a later regression through, which is why each carries a trigger rather than a date.

## round_0_verify

- evidence vs claims: every claim in the verification form cites a file and a line, and fresh eyes re-read seven of seven inspection items against the source with a quote behind each verdict. The one claim I could not make myself — that the regime's static attributes hold — is the tester's, twice.
- types: clean. The lane typechecks after every write to source and reported nothing across roughly twenty patches.
- lint: clean, with no suppression added. The lane's safe fixes ran on six files and named each one.
- tests: 1902 cases, 158 suites, zero failures, run as the state's own exit script rather than by me. Seven new door cases and five replaced ones are inside that count.

## round_1_validate

- exercised against the goal: yes, and against the record's own code. The rule the record built was applied to the record and refused two things it had shipped. The write-time guard caught the new door bypassing the existing widget door, and the departure guard's own list survives the malformed-bullet refusal added this round.
- missing: the outward door, deliberately, with the reason recorded at the prototype gate. And a behavioural test of the chokepoint — what stands is a source-level assertion that all four write verbs call the one guard, which catches a call site deleted and not a guard that stops refusing.
- wrong: nothing standing. Four regressions this round were found by fresh eyes and fixed, and the confirm run proves the fix rather than my reading of it.
- out of scope: 17 help-rule findings across 11 files, and around eighty undeclared reaches. Both registered with every item named, both left alone. Widening a record into eleven files on the strength of a guard it built the same afternoon is the scope creep the method exists to stop.
- prior art: dependency-cruiser, ArchUnit, ESLint, Rust, Go and Bazel were compared, and the comparison was CORRECTED mid-record after reading dependency-cruiser's own rules reference. It DOES express who may reach the filesystem — `from` as well as `to`, `not-to-core` as its own documented example, `fs` under the core dependency type. What survives is narrower and it is stated narrowly: its baseline is generated rather than authored, its `comment` field is documented as unused in rule logic, and it ships `severity: "ignore"`. Every one of the six ships a way to switch the whole thing off. This one ships per-module departures that must each state a reason, and no off-switch at all.

## goals_served

- Replace the hand-written entry-point list with a sweep over every exported entry point, so the guard stops depending on somebody maintaining a list: served and shipped. `deliverable/tests/help.test.ts` calls `entryPoints(repoRoot)` where it read a list of six, and the widening found 17 real pre-existing findings in 11 files on its first run.
- Name everything exported that no surface can reach, and answer each one with either a door or a deletion: served, and the naming is honest rather than complete. The sweep reports four entry points nothing invokes, and the check was sharpened twice this record after each version was found to be wrong — once for counting prose as invocation, once for letting a file certify itself with its own usage comment.
- Give the two already-found pieces their door, so a capability the tests prove is a capability somebody can use: not served by this milestone, and it is the ratchet gap. The sweep now NAMES every undeclared reach, which is what makes moving them possible; moving them is a record of its own and is registered as such.
- Establish whether one door per capability actually pays here, by judging the measured sites rather than by counting them again: served, and the answer was a judgment on sites rather than a count. `exp-does-one-rule-fit-all-four-conversations` holds with an eight-module margin, and `run.ts` was judged the clean counter-example at 0 of 10 sites a door would improve, which is why it is a declared departure rather than a stray.
- Build the disk door and its declared exception list, generalising the shape that already governs widget markup: served and shipped. It improves on its own worked example in one stated way — the widget list IGNORES a reasonless bullet and this one REFUSES it, along with a bullet it cannot parse at all.
- Give reaching outward a central door that earns its keep, with guidance for a search and a place results are kept: nothing yet — M8 or a later record owns it. The decision not to seed it is recorded at the prototype gate as a dissent, with its reason: the goal was served by a count of 17 modules and no probe, and this record corrected four premises of exactly that kind.
- Record a reason for every door proposed and dismissed, so a dismissal is evidence rather than silence: served, and the reasons outlived the dismissals. Four candidate shapes were scored and the losers' better parts were grafted, and the record carries a measured reason for each door not built rather than an absence.

## bound_breaches

- if-agent-harness-to-entrypoint: breached, and the breach is the bound's rather than the code's. It measures one second against every lane call including the verbs whose whole job is to spawn a process and wait — the battery, the sweep, a subagent registration. That is registered as `raid-iss-the-one-second-bound-counts-calls-that-spawn-external-work` and nothing this record did moved it either way. The disposition is that the bound is re-drawn to exclude spawning verbs, and the record that draws it owns that.

## round_2_red_team

- STEELMAN: the widget guard already did this for one conversation, so generalising it bought a second customer for a shape that had one, and one customer is not a pattern => That is the strongest case against and it nearly holds. What answers it is measured rather than argued: `exp-does-one-rule-fit-all-four-conversations` put the rule against all four candidate conversations and it survives with an eight-module margin. What does NOT answer it is anything about elegance, and the record deliberately makes no such claim.
- KILL-CRITERION: this is the wrong call if the second door needs a shape the first cannot express => Looked for it and found it, which is why this gate carries debt rather than a clean pass. Two walkers hardcode where the first door's files live, so a second door covering another folder would count zero and report UNCHECKED forever. The fix is a `roots` field on the door type. It is named, it is triggered before the second door is registered, and it is the single most likely thing to prove this generalisation premature.
- The guard is enforced on one write verb of four, so the rule is stated and not held => Was true and is fixed. All four verbs call one function now, and a case asserts each write module calls it. That case is a reading made mechanical, not a behaviour test, and the comment above it says so rather than implying otherwise.
- The reason requirement collects noise, so the list fills with words that say nothing => Measured rather than assumed: across 113 reasons a refusing verb has actually collected here, 104 are considered. The nine that are not sit in one record where the honest answer genuinely was the same nine times. Registered as `raid-asm-an-author-refused-at-write-time-states-a-usable-reason` with the sweep that would falsify it.
- The sweep reports 80 undeclared reaches, so the rule is a warning nobody acts on => The count is ratcheted and may only fall, and the departure list refuses a reasonless line rather than ignoring it. That is the difference between a warning and a floor. What is NOT answered is whether anybody moves the eighty, and that is registered as the ratchet gap rather than claimed.
- The record proved its own rule by reading rather than by running => True for two behaviours, and it is why this gate carries debt. Everything a single door can exercise is exercised: 1902 cases green, including one that spawns the sweep against an empty tree and demands a non-zero exit.

## raid_additions

- spec/trace/raid/raid-debt-the-door-regime-is-built-for-four-doors-and-proved-with-one.md
- spec/trace/raid/raid-asm-the-drift-ceiling-s-per-state-term-is-chosen-not-measured.md
- spec/trace/raid/raid-iss-eleven-entry-points-parse-a-switch-their-help-never-mentions.md
- spec/trace/raid/raid-iss-the-predicate-half-of-the-door-rule-already-exists-in-a-tool-nobody-here-runs.md

## verdict

pass with overrides — the mechanics are proved and the judgment carries four dissents, each logged rather than waved. FIRST, two behaviours fixed this round cannot be exercised until a second door exists, carried as debt with a trigger that fires before that door is registered. SECOND, the outward door named in the kickoff goals is not built, deliberately, with the reason recorded at the prototype gate. THIRD, the prompt layer is stale because this record edits guidance by design, and the verb that clears it is granted only at M8 — so this gate ships a known red that the next milestone owns. FOURTH, around eighty undeclared reaches and 17 help findings stand registered and untouched, which is the ratchet doing its job rather than the record failing. What earns the pass is that the rule was applied to the record and refused two things the record had shipped, and that fresh eyes rather than the builder observed all seven static attributes green.

## follow_up

### Owned by M8, in order

- Re-project the prompt layer. `sweep-consistency` grants the verb and the row's own comment says why.
- Correct the drift failure message, which still blames a per-state sweep that its sibling assertion rules out.
- Rewrite `deliverable/engine/bin/sweep.ts:1`, which cites the wrong design spec.

### Owned by the record that adds a second door

- A `roots` field on the door type, so a walker stops assuming where a door's files live.
- The two behavioural tests that need two doors to exist.

### Standing, registered, deliberately untouched

- Around eighty modules reach the disk conversation with no departure declared. The sweep is the figure and the ratchet holds it.
- 17 findings across 11 entry points break the help rule, every one named.
- Four entry points are invoked by nothing.

### For the retro, and every one is about this machine

- `note-a71bef4acbff`: the leaving judgment reports a verdict and keeps none of the output that explains it.
- `note-bad426c8c8f1` and `note-83a96c800f20`: preflight names a remedy granted in three states only.
- `note-86838fa04ab0`: the work account prints a call the verification state refuses.
- `note-e76110c5cd0e`: a cloud box cannot reach its subagent's partial output.
- `note-2a8bcca7c093`: a failure message blaming a cause its own suite rules out.

## anything_else

