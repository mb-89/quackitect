---
form: why-the-code-departs
by: agent
signed_off: 2026-08-26T15:43:52.494Z
authors: agent
files: null
---

# Evidence form / why-the-code-departs

## current_situation

The departure list stands with two entries. Whether a stranger can read it and tell a decision from a mistake is the one question no test answers.

## built

The demonstration was performed by a REVIEWER hand, spawned fresh with no share of the build's context, given the story and the step and nothing about what to conclude. Its report is spec/iterations/i54-everything-exported-has-a-door-a-sweep-o/reports/rpt-sty-read-why-the-code-departs-from-its-own-design.md

BOTH REASONS LANDED AS ARGUMENTS. The door's own departure is self-referential and says so, and the reader confirmed it in the file in about thirty seconds.

ONE REASON WAS OFF BY ONE, and the reader caught it. run.ts claimed all ten of its sites are jailed under .se/jobs. Eight are; two write .se/estimates.jsonl one level above, through a different helper. Verified at run.ts lines 1481 and 1482, and the departure now says so.

THE PAGE HID ITS OWN SCALE, and that was the larger finding. It closed with 'Two departures are declared below', which reads as two modules talking to the disk. Most of the engine does. The refusal to print a count is right, because a hand-written figure goes stale; what was missing was the pointer to the sweep, and the page now carries it without printing a number of its own.

## follow_up

Two limits a reader of the list alone still cannot learn, both registered rather than fixed here. The rule warns rather than refuses while the undeclared count is high, and the list does not say so. A bullet naming a path with no .ts extension is silently unmatched by a hand edit made outside the lane, and the How to add one section does not warn about it.

## anything_else

