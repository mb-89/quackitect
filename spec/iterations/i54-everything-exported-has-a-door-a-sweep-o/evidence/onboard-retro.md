---
form: onboard-retro
by: agent
signed_off: 2026-08-26T10:49:42.383Z
authors: agent
files: null
---

# Evidence form / onboard-retro

## current_situation

Record i54 is open and standing at its onboarding retro. Its seeded goal is one door for everything exported: a sweep over every entry point replaces a hand-written list of two.

The owner widened the record on 2026-08-26, the day it was entered. The widened principle is one door per capability, applied broadly. Three doors were named: disk, the declared exceptions to the disk rule, and the internet.

Nothing else is open. The survey reports no expeditions and 41 seeded iterations, none of them entered.

The pool held 128 work tokens when this state opened. This retro minted three more.

The notes inbox held two notes on entry and a third was captured inside this state. All three were drained here, and the inbox now reads zero.

This is a fresh cloud container with no person beside it. The call log starts empty in such a container, so the window this retro judges is this session alone.

## field_feedback

Nothing yet, and the question was not put to anybody.

This is an unattended run. The retro card names the field-feedback question as its one sanctioned stop, and a stop needs somebody on the other side of it.

The card is explicit that no amount of draining, mining or sweeping stands in for this answer. It is the owner's own report from outside the machine.

So the question is owed rather than answered. The next attended session should ask it before it does anything else, and it covers the whole span since the last time the owner was asked, not only this run.

## notes_drained

- note-829e1fafe23a — The M0 walker cannot be registered legally: the spawn check reads a ceiling the kickoff gate has not written yet: backlog, minted as wt-the-guard-that-limits-how-many-walking-hands-a-record-may-ru, ready when the spawn state's exit script or the milestone ordering is next opened. Checked before judging by reading deliverable/engine/bin/hands-spawned.ts lines 49 to 71, which confirms the ceiling is read from the kickoff gate's own evidence file and that unreadable returns zero.
- note-80f1a0fe7412 — Boot on this cloud box reported the mirror lane failed on port 7333: backlog, minted as wt-on-a-cloud-box-the-visual-surface-does-not-come-up-so-nobody, ready when a cloud run needs watching on a screen or the arrival script's port handling is next opened. Not fixable from here: the state is read-only and the process holding the port lives outside the project.
- note-1b23dc5e6a03 — The retro's memory drain is unreachable on a cloud box: backlog, minted as wt-one-step-of-the-looking-back-procedure-cannot-close-on-a-clo, ready when the retro card's memory step is next edited. Captured inside this state after the first two were drained, and drained in the same pass so nothing stays pending.

## call_log_mined

- Window: 91 records opening 2026-08-26T10:27:45Z, which is this session's first call. The container starts with an empty call log, so this is a fresh-clone window and not a quiet period. The count is what tells the two apart.
- The boundary really does move under you. After this retro's first drain to backlog, the same since:last_retro filter returned 1 record where it had returned 91. Step 1 of the retro card warns about exactly this, and taking the numbers before draining is what saved them.
- Tool counts over the window: se_update 31, se_pull 17, se_probe_cap 8, se_file_read 7, se_run 4, se_file_search 3, se_note 3, se_file_list 3, se_survey 2, and one each of se_reload, se_file_glob, se_answer, se_surface, se_note_drain, se_aim, se_file_patch and se_web_search.
- Narration outweighs walking: 31 update ops against 17 pulls, close to two narrations per pull. The stated band is one update per one to five minutes, so the log is being filled rather than kept.
- Refusals: 10 of 91 records, 11 percent.
- SE-C-120 fired four times, every one on a brief chaining three comma-joined parts. Twice the identical brief was re-sent unchanged after being refused, so one mistake cost two calls.
- SE-C-102 fired twice on a path beginning iterations/ where the project root wants spec/iterations/. That is the dropped-prefix failure the lane card names, caught here by a not-found rather than by a silent write into the wrong folder.
- SE-C-110 fired twice on reaching for a tool the standing state forbids: se_file_patch at iterations/i54/start, and se_web_search at iterations/i54/spawn-the-hands.
- SE-C-121 fired once on an update naming no node while a checklist stood open.
- SE-C-101 fired once on se_file_search being passed glob where the argument is include.
- SE-C-112 blocked the exit of spawn-the-hands and is not in the ten, because a blocking condition is an instruction rather than a refusal record. It is the finding this retro carries furthest.
- No lane job was done in the shell. The one se_run command inside this window is a Node program counting direct disk-write sites across the engine, which is a program rather than a lane job and is the healthy shape the card asks for.
- The demand log was not read. se_help is not among this state's legal tools, so the ranked missing-verb list could not be pulled here.

## waste_leads

- The M0 walker was spawned, registered, blocked at the ceiling check, and stood down again without walking anything under its own registration. The entire registration was wasted work caused by an ordering defect, and standing the hand down cost the panel its only progress signal for the rest of the milestone.
- The same chained brief was re-sent unchanged after SE-C-120 refused it, twice. The refusal already carried the rewritten plan in its text, so the second call bought nothing.
- Two SE-C-102 refusals were spent rediscovering the spec/ prefix, which the lane card states outright.
- The survey answer is 110 KB and its first page alone is 47 KB. Almost all of that is the 128-token backlog, and this state's form asks about none of it. A retro that only needs counts pays for the whole pool.

## promotions

- None found local to this record. i54 has walked one state, and nothing in its own machine, forms or item templates has been changed to promote.
- The three most recently shipped records emitted nothing to promote either. i60 and i61 both wrote none under emit_back, and i62 wrote a list of what its packaging check did not cover rather than any template change.
- One candidate exists but sits above this record rather than inside it. The walker ceiling is read from the kickoff gate's evidence, which the opening milestone has not written yet, so the defect belongs to the rigor matrix row and the exit script rather than to i54's local copy. It is minted as a work token instead of promoted here, because promoting a local change is not what it needs.

## process_stale

Not stale on the evidence available here, and the comparison was narrow enough to say so plainly.

What it was compared against: guidance/method/retro.md, walked step by step against what this state's form actually asks for.

The onboard retro is a reduced retro, and the reduction looks deliberate rather than decayed. It drops the boundary marking, the backlog migration, the debt sweep, the milestone table and the contract check. It keeps field feedback, the drain, the log mining, promotions and this question.

One gap is real rather than stylistic. The card's memory drain cannot be walked on this host at all, and the card's own escape hatch of declaring a root yourself assumes a write the harness may forbid. That is now a work token.

No outside practice was compared. A state-of-the-art check against how other systems handle a session retrospective was not run, because this state's legal tools carry no web search. Saying so is the honest answer rather than implying a comparison that did not happen.

## follow_up

Next is the kickoff, and it proposes its change size against the owner's widened scope rather than the seeded one.

Three work tokens were minted here and none of them is for this record to spend:
- The walker ceiling deadlock at the opening milestone, ready when the spawn state's exit script or the milestone ordering is next opened.
- The visual surface not coming up on a cloud box, ready when a cloud run needs watching or the arrival script's port handling is next opened.
- The retro's memory drain being unreachable where the harness forbids declaring a root, ready when that step of the card is next edited.

One question is owed to the owner and it blocks nothing here. The field-feedback question stands unanswered and belongs to the next attended session.

One transcribed word also stands open. The owner's dictation carried a further example of the one-door principle whose name did not survive transcription. It is recorded in the record's own file and nothing is built on the literal word.

## anything_else

The memory drain could not be walked, and this is the account rather than a tick.

The folder was never reached. .se/roots.json does not exist on this clone, so a glob of @sessions refused with SE-C-127 naming no declared root. The refusal's remedy said to declare one through the lane, which is what the retro card also says.

That write was then refused by the host rather than by the lane. Claude Code's auto-mode permission classifier blocked se_file_write to .se/roots.json outright.

So no memory folder was named and no file was counted. Zero files were read because nothing was reachable, not because nothing was there. The card asks for exactly this distinction to be stated.

A fresh cloud container also carries no persisted assistant memory of its own. The only standing project instruction in context is the repository's contract file, which already lives in the repo and needs no drain.

One more thing is worth recording about this window. The guide was working in this same state at the same time as the walker, running a measurement over the engine's disk-write sites. That is visible in the call log and in the decision graph, and it is the arrangement the spawn state describes rather than a conflict.
