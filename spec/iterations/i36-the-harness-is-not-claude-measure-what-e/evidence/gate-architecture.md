---
form: gate-architecture
bless: blessed by agent
by: agent
signed_off: 2026-08-19T13:46:08.768Z
authors: agent
files: null
---

# Evidence form / gate-architecture

## current_situation

Candidate A’s direct blocking-stop architecture is allocated to arrival, walk engine and mirror. The element matrix has zero owed crossings after adding the harness-profile and test-record interfaces and repairing the stale mirror-to-walk guard boundary.

## round_0_verify

- evidence vs claims: pass; element, interface, function and requirement nodes resolve from the decomposition and scenario paths.
- types: pass; each new i36 interface is typed and names source, destination, carry and form.
- lint: pass at artifact shape; the interface matrix reported zero owed crossings after repair.
- tests: focused lifetime and HTTP MCP regression tests passed; the full battery remains red on the separately-held spill-answer baseline debt.

## round_1_validate

- exercised against the goal: pass; the architecture now identifies the harness, bounds its HTTP transport, preserves state through interruptions and routes failure shapes.
- missing: the live Copilot stop-event proof remains owed before weaker-model routing may rely on it.
- wrong: no computed score winner existed; the owner-directed Candidate A decision is recorded in the ADR rather than misrepresented as Pugh arithmetic.
- out of scope: repairing the entire red battery is verification work held by its work token.
- prior art: addressed at motivation; current MCP lifecycle and Copilot behavior informed the design.

## goals_served

- Measure what every supported host actually provides.: el-arrival and if-arrival-to-walk-engine carry the harness profile.
- Close the five measured harness breaks in the prepared brief.: el-walk-engine and el-mirror carry bounded responses, interruption evidence and stop behavior.
- Make the lane report which harness it is talking to.: fn-arrive-on-a-machine.identify-the-harness is implemented by el-arrival.
- Make future boots quicker by removing the test-metadata recovery step from the manual boot path.: if-test-runner-to-walk-engine carries the historical test verdict.
- Make oversized pull results recoverable through the lane instead of host files.: el-mirror and el-walk-engine serve bounded pages and continuation cursors.

## bound_breaches

- if-agent-harness-to-entrypoint: breached; Copilot saw ECONNRESET on a reused localhost connection while the server remained alive. The explicit keep-alive policy and EPIPE/lifetime fixes are implemented. A live Copilot stop-event proof remains the M6 acceptance measure.

## round_2_red_team

- strongest opposing case => the new connection policy hides an event-loop starvation defect rather than fixing it.
- attack => the ATAM deck keeps req-surface-answers-in-one-second at risk until long scans leave the request path; the keep-alive policy prevents idle socket reuse resets only.
- kill criterion => a real Copilot stop request reaches an active walk and it ends anyway. This has not yet been tested, so the gate passes only with that override explicitly carried.

## raid_additions

- [[raid-dec-copilot-uses-the-blocking-stop-contract]]
- [[raid-debt-harness-fallback-and-bounds-need-implementation-proof]]

## verdict

pass with overrides — the architecture and its interfaces stand. M6 must prove the Copilot stop contract and eliminate request-path starvation before implementation verification.

## follow_up

Rank the live Copilot stop-hook proof, reused-connection stability and long-pull event-loop starvation as M6 unknowns. Build the focused spike for the blocking stop contract first.

## anything_else

