---
form: frame-delta
by: agent
signed_off: 2026-08-19T09:25:06.447Z
reopened: "2026-08-19T09:23:52.815Z — Four technical-detail value props were removed. I36 now extends existing value props through stories and use cases instead of authoring new propositions."
authors: agent
files:
---

# Evidence form / frame-delta

## current_situation

The actual state and RAID register stand.

I36 authors no new value prop.

The delta extends two existing marketing-level propositions:

- `vp-qualities`
- `vp-rigor-without-toil`

Four stories and three use cases carry the technical behavior below them.

## gap_claim

The scanned field shows a gap between configuration portability and runtime reliability.

Alternatives can supply configuration hooks or model instructions.

They shed the lane-level evidence needed to adapt boot to the current host.

They also shed the feedback loop that turns failed tool calls into durable work.

The prepared i36 report names this as three harness-coupling tiers:

- config slots
- host features
- agent compliance

The current gap is in the third tier.

Prompting the agent to comply is not enough when the host offloads results, truncates tool descriptions or repeats a route remedy.

## why_now

The gap is closable now because the failures are measured.

The prepared report gives byte counts and host behavior.

The fresh 2026-08-19 scan adds primary MCP cancellation, progress, Agent Skills and VS Code hook evidence.

Today's session reproduced:

- stale test metadata blocking boot
- oversized result offload
- missing search-provider configuration
- absent native search registration
- cancellation with no stopping-layer diagnosis

The lane logs every call, so repeated failure shapes are countable.

## value_props

- project/spec/trace/value-prop/vp-qualities.md
- project/spec/trace/value-prop/vp-rigor-without-toil.md

## business_case

The effort buys fewer stalled agent sessions and less repeated owner intervention.

The currency is agent time, owner attention and trust in unattended runs.

## follow_up

Use the inherited value props to retain scope and non-goals.

Keep harness behavior, failure recovery and research fallback below the value-prop layer as stories, use cases, requirements, quality rows and implementation debt.

## anything_else

No value prop is authored by i36.

Fresh primary evidence remains in `project/spec/references/ref-agent-harness-portability-2026-08-19.md`.
