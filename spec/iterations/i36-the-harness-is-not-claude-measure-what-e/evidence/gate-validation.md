---
form: gate-validation
bless: blessed by agent
by: agent
signed_off: 2026-08-19T18:28:55.778Z
authors: agent
files: null
---

# Evidence form / gate-validation

## current_situation

The build is done, the battery is green, the sweep is signed, and one gap is carried as owner-accepted debt.

WHAT SHIPS. Harness measurement with provenance, client identification on every record, an answer bound tied to the tightest measured host limit, a spill cursor that is now readable from any state, a boot that tolerates an old test record, and two guard checks that fail the battery when the cage list or the served corpus drifts.

WHAT DOES NOT. Two modules compute an answer that reaches no surface. That is `raid-debt-two-must-requirements-compute-a-result-that-reaches-no-surface`, minted at the implementation gate, and the owner took the override knowingly rather than reopening blessed work.

THE NUMBERS. 1456 tests in 140 suites, no failures. `tsc --noEmit` exit 0. `biome check --error-on-warnings` exit 0 over 312 files. No lint suppressions anywhere in the engine tree.

## meets_need

- vp-autonomy-range: UNTOUCHED, and that is fine. Nothing here changes what the dial governs or how far a walk may run unattended.
- vp-qualities: SERVED IN PART. Reliability gained a measured answer bound and a lifecycle log; the stopping-layer half of it is computed and unreported, carried as debt.
- vp-rigor-without-toil: SERVED. Two guard checks now fail the battery on drift that a person used to have to notice: the cage inventory and the served-payload sizes.
- vp-systematic-engineering: UNTOUCHED as a promise, and exercised as a practice. The iteration used the machine rather than changing it.
- vp-the-engine: SERVED. The engine now identifies its client, records its own lifecycle, and derives its answer bound from a measured fact instead of a chosen number.
- vp-the-ledger: SERVED. Every call record carries the harness it came from, so a refusal rate or a slow call can no longer be pooled across hosts.
- vp-vendoring: UNTOUCHED, and that is fine. No vendored dependency moved.

## musts_demonstrated

- sty-a-check-binds-without-engine-code: NOT TOUCHED by i36. Its standing demonstration is unchanged and its evidence side was filled at fill-story-evidence, which is signed.
- sty-a-finding-outlives-the-box-that-found-it: NOT TOUCHED. Standing demonstration unchanged.
- sty-answer-what-does-this-touch: NOT TOUCHED. Standing demonstration unchanged.
- sty-ask-the-lane-what-it-can-do: NOT TOUCHED. Standing demonstration unchanged.
- sty-carry-a-finding-without-stopping: DEMONSTRATED THIS SESSION. Fifteen notes were captured mid-walk without leaving the state in hand.
- sty-dispose-a-candidate-coupling: NOT TOUCHED. Standing demonstration unchanged.
- sty-hand-over-and-walk-away: NOT TOUCHED. Standing demonstration unchanged.
- sty-ramp-up: NOT TOUCHED. Standing demonstration unchanged.
- sty-review-a-gate: DEMONSTRATED THIS SESSION, including the unhappy path. The implementation gate was filled, blessed, then amended to `pass with overrides` when a red-team finding landed after the bless, with the dissent logged and the signature kept.
- sty-run-deep-research-across-harnesses: DEMONSTRATED. `ref-agent-harness-portability-2026-08-19.md` is the report of its real run, and its unreachable sources are named rather than papered over.
- sty-see-what-the-other-machine-may-pull-from: NOT TOUCHED. Standing demonstration unchanged.
- sty-send-an-agent-to-a-cloud-box: NOT TOUCHED. Standing demonstration unchanged.
- sty-start-a-new-product: NOT TOUCHED. Standing demonstration unchanged.
- sty-the-agent-proves-it-read: DEMONSTRATED THIS SESSION. Every reading hop was proved by its last words before the next document was served.
- sty-the-call-that-comes-back-inside-a-second: NOT DEMONSTRATED, and the honest reason is in bound_breaches. Four crossings ran long, every one of them a verb asked to spawn a process.
- sty-the-control-that-says-why-it-declined: NOT TOUCHED. Standing demonstration unchanged.
- sty-the-slow-call-that-says-it-is-working: NOT TOUCHED. Standing demonstration unchanged.
- sty-the-write-refuses-the-break: NOT TOUCHED. Standing demonstration unchanged.
- sty-turn-a-failed-call-into-improvement-work: NOT DEMONSTRATED. This is the story the debt is about. `failure-shapes.ts` computes the work statement and nothing mints it, so no failed call became work by machine this iteration. It happened by hand instead.
- sty-walk-it-by-hand: NOT TOUCHED. Standing demonstration unchanged.
- sty-work-on-two-machines: NOT TOUCHED. Standing demonstration unchanged.

## market_tier

NOT APPLICABLE. This is not a market iteration. The work is internal engine and harness measurement, with no real-world tier owed.

## round_0_verify

- evidence vs claims: CHECKED, and the check is what found the gap. Following the exported symbols of the eight new modules showed four with no importer outside their own test file. Two of those four are guards, correctly test-hosted. Two are not, and the implementation gate was amended rather than left standing.
- types: GREEN. `npx tsc -p . --noEmit --pretty false` exits 0 with no output. It is not in the battery.
- lint: GREEN. `npx biome check --error-on-warnings .` exits 0 over 312 files. Also not in the battery.
- tests: GREEN. 1456 tests, 140 suites, no failures, run twice through verification.

## round_1_validate

- exercised against the goal: YES for the measurement work, which was used by this session to read its own oversized answers. NO for the two unwired modules, which have never run outside their own tests.
- missing: THE TWO SURFACES. A stopping-layer diagnosis nobody can ask for, and a recurring failure shape nothing mints. Both are named in the debt entry with a repayment plan.
- wrong: THE TESTS ARE IN THE WRONG FILES for both. Each test-spec declares LEVEL: integration and names existing files; the cases were written in new component files instead. Nothing in the machine compares a test-spec's `files:` list against where the cases actually live.
- out of scope: THE WEB VERBS. `se_web_search` without a Brave key, the native-search handoff, and `se_web_fetch` reporting its post-redirect URL are all in the earlier debt entry's repayment plan and none was touched.
- prior art: UNCHANGED FROM THE IMPLEMENTATION GATE, where it was argued in full against terminfo and browser feature detection, with the citation status of each stated honestly. No new comparison is owed by a validation round that added no new mechanism.

## goals_served

- Measure what every supported host actually provides.: SERVED. `ref-agent-harness-portability-2026-08-19.md` is the scan; `engine/harness.ts` holds three entries, each limit naming its provenance.
- Close the five measured harness breaks in the prepared brief.: SERVED IN PART. Six chunks landed and are signed. The stopping-layer break is computed but unreported, which is the debt.
- Make the lane report which harness it is talking to.: SERVED. `clientInfo` is read at initialize and every record is stamped; an unidentified client is left unstamped rather than guessed.
- Make future boots quicker by removing the test-metadata recovery step from the manual boot path.: SERVED. A record carrying neither question nor scope is treated as stale, not as a finding.
- Make oversized pull results recoverable through the lane instead of host files.: SERVED, and used to fill this form. The spill dir is per server, and the toll and the state gate both exempt a read under `.se/answers/`.

## bound_breaches

- if-agent-harness-to-entrypoint: BREACHED, and unchanged since the implementation gate. Four crossings there, all of them verbs asked to spawn a process: 2421 ms for the typechecker, 1903 ms for the linter, 3903 ms for both, and at least 111765 ms for the pull that fired the battery. DISPOSITION: no fix, question written down as raid-iss-the-one-second-bound-counts-calls-that-spawn-external-work. Nobody has decided whether the bound governs spawning verbs, so every gate answers it again by hand.

## round_2_red_team

- THE STRONGEST CASE AGAINST SHIPPING => Two `must` requirements are not met, and shipping anyway teaches the corpus that `must` is negotiable. That is a real cost and it is not being denied. What makes it survivable is that the gap is written down as debt with a repayment plan, the gate says `pass with overrides` rather than `pass`, and the dissent is logged in the verdict. A gap that is named and dated behaves differently from one that is hidden.
- THE KILL CRITERION FOR THIS DECISION => It would be wrong if the debt were never repaid and a later reader trusted a diagnosis nothing produces. The guard against that is the debt entry's trigger, which fires at the next iteration that opens the lane's reporting surfaces, and a retro note naming the two mechanical checks that would have caught it.
- WHY THE GAP WAS NOT CAUGHT EARLIER => `trace-design` checks that design specs claim their files and that the files exist. `verification` checks that tests are green. Neither follows a call graph. A module that only its own test imports is indistinguishable from a wired one, and this is the first time that has mattered.
- THE FIX IS CHEAPER THAN THE MISS => Two checks would close the blind spot: an exported symbol with no importer outside its own test, and a test-spec whose `files:` list does not match the files holding its cases. Both are static and both are cheap. That is the retro's to weigh, and the note is filed.
- IS THE FLAKE FIX STILL SOUND => Yes, and it has now survived two full battery runs. The mirror binds 127.0.0.1 by stated design, and twelve tests were dialling a name that also resolves to ::1. Nothing about the server changed.

## raid_additions

- raid-debt-two-must-requirements-compute-a-result-that-reaches-no-surface
- raid-iss-the-one-second-bound-counts-calls-that-spawn-external-work

## verdict

pass with overrides — THE NEED IS MET FOR WHAT THIS ITERATION WAS FOR. Every host limit the lane depends on is now measured rather than assumed, each number carries where it came from, an unmeasured host is answered with `undefined` instead of a guess, and the lane says which harness it is talking to on every record. The oversized-answer path that made this session possible works, and two drift guards now fail the battery instead of waiting for a person to notice. THE OVERRIDE, and its dissent. Two `must` requirements are not met: `req-interrupted-call-names-the-stopping-layer` and `req-repeated-failure-shape-becomes-durable-work` each compute an answer that reaches no surface. Judged alone, those two fail. They are shipped as `raid-debt-two-must-requirements-compute-a-result-that-reaches-no-surface`, which carries the repayment plan and the contradiction that must be settled first — the test-spec demands a report naming the host, and the design says the host is the one layer nothing observes. THE OVERRIDE IS THE OWNER'S, taken knowingly on 2026-08-19 with the gap fully described in front of them. THE BLESS IS THE SIGN-OFF, and it is given on that instruction.

## follow_up

WHAT COMES NEXT, in order.

THE RETRO IS OWED AND IT IS IMMEDIATE. Fifteen notes stand in the inbox. The one that matters most is `note-6deda1a1974f`: the machine cannot see an unwired module, and this iteration shipped two. It names the two cheap static checks that would close the blind spot. The owner has also asked the retro to answer why weaker models cannot drive this machine.

THE DEBT IS THE FIRST WORK AFTER IT. `raid-debt-two-must-requirements-compute-a-result-that-reaches-no-surface` carries four repayment items and one contradiction that has to be settled at the requirement before any code moves. READY WHEN the next iteration opens the lane's reporting surfaces.

THE OLDER DEBT IS STILL OPEN. `raid-debt-harness-fallback-and-bounds-need-implementation-proof` keeps four untouched items around `se_web_search` and `se_web_fetch`. READY WHEN a milestone owns the research verbs.

TWO REGISTER ENTRIES WANT A DECISION RATHER THAN CODE. `raid-iss-the-one-second-bound-counts-calls-that-spawn-external-work` asks which verbs the bound governs. `raid-asm-documented-harness-limits-stay-stable` is unprobed on purpose, and the dangerous direction is a vendor limit FALLING below 6000 bytes, not rising. READY WHEN the model is next opened, and at the next quarterly scan.

ONE THING THE BATTERY SHOULD SWALLOW. Verification signs on tests alone, so a walk can reach shipped with the typechecker and the linter never having run. Both live only in the pre-commit hook today. The verification row is where that widens.

## anything_else

