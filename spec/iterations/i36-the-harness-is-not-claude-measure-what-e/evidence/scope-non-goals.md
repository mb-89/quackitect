---
form: scope-non-goals
by: agent
signed_off: 2026-08-19T09:25:40.891Z
reopened: 2026-08-19T09:25:25.735Z — Frame-delta now inherits existing marketing-level value props; confirm the already bounded scope still holds.
authors: agent
files: null
---

# Evidence form / scope-non-goals

## current_situation

Iteration 36 has a refreshed vision, actual state, RAID register and frame.

The prepared report names five arithmetic breaks.

Fresh research adds primary evidence and named source gaps.

Live signals now include boot metadata coupling, result offload, route-remedy loops, search-provider gaps and undiagnosed cancellation.

## scope

This iteration takes on the five prepared harness breaks:

1. Boot's fourth document does not survive Copilot CLI's output threshold.

2. The stop hook can be overridden after eight blocks.

3. Five documented built-in tools are outside the cage.

4. `se_pull`'s own description is truncated by Claude Code.

5. `AGENTS.md` is larger than Codex reads.

It also takes on the live failures found while starting the iteration:

- make boot test-metadata recovery mechanical
- make oversized lane results refetchable through the lane
- distinguish stop-hook decisions from host cancellation
- prevent a route remedy from repeating the same refusal
- identify the active harness in lane output
- mine failed tool calls as improvement signals during the iteration
- measure guidance and tool payloads against each supported host's limits

The work may update engine code, hooks, cage files, prompt placement, tool descriptions, boot documents, tests and harness measurements.

## non_goals

- Do not remove boot checks to make boot faster.
- Do not treat prompt-only compliance as a portability mechanism.
- Do not redesign the product vision or stakeholder model.
- Do not resolve the HTML mirror decision owned by iteration 23.
- Do not resolve expedition archive visibility from the backlog token.
- Do not replace every host integration with one lowest-common-denominator interface.
- Do not fix unrelated historical lint debt found by broad sweeps.
- Do not assume a cancelled tool call proves the stop hook worked.
- Do not count a local retry as a durable fix.

## follow_up

Pressure-test the refreshed design input, then stop at the blessed motivation gate.

Implementation debt owns:

- finish and reload-test web provider fallback
- reload-test the 6 KB answer bound and character paging
- finish MCP lifecycle diagnostics
- include `se_why` blocker detail in pull refusals
- verify Codex, Cursor and full Claude primary sources
- classify remaining failed-call shapes into implementation chunks

## anything_else

The scope is intentionally major.

It keeps the prepared five breaks together because they share one harness measurement and delivery surface.
