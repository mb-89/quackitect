---
form: gate-motivation
bless: blessed by agent
by: agent
signed_off: 2026-08-19T09:44:52.030Z
reopened: 2026-08-19T09:44:07.115Z — Quality behavior moved from freely authored stories and use cases into requirements under fixed ISO 25010 quality use cases.
authors: agent
files: null
---

# Evidence form / gate-motivation

## current_situation

The motivation packet now follows the fixed quality hierarchy.

`vp-qualities` has one story: `sty-what-a-quality-is`.

Nine ISO/IEC 25010 quality characteristics are its use cases.

Six i36 quality requirements carry harness compatibility, security, reliability and performance behavior below those use cases.

## vision_scope_stated

Complete.

The packet states the inherited vision, harness delta, current failures, risks, scope and hostile PR-FAQ.

Marketing-level value props remain high-level.

Quality behavior is represented only through the umbrella quality story, ISO quality use cases and measured requirements.

## problem_agreed

The delta is real and worth addressing.

The prepared report measured five breaks against documented host limits.

This session reproduced payload offload and boot metadata coupling.

It also exposed an undiagnosed cancellation that may have involved the MCP server.

The cost is paid in boot time, owner attention and unreliable unattended work.

## prior_art_positioned

Fresh primary evidence confirms:

- MCP cancellation is optional and transport-specific; it does not prove server process restart.
- MCP progress is optional and cannot prove liveness alone.
- Agent Skills standardizes `SKILL.md` as a portable instruction artifact.
- VS Code supports repository hooks, diagnostics and cross-format hook parsing, while ignoring Claude matcher values.

Claude Code and the Copilot family provide stronger native lifecycle and hook integration than a plain MCP server.

Quackitect's proposed difference is one measured lane contract across harnesses.

Fresh Codex, Cursor and full Claude pages were unreachable through this session's fetch path.

Those vendor details remain unverified instead of being carried forward as current facts.

Sources: `ref-agent-harness-portability-2026-08-19` plus the prepared 2026-08-18 corpus.

## success_measurable

Six quality requirements carry the technical pass lines:

- `req-supported-harness-serves-one-lane-contract`
- `req-native-project-tools-stay-outside-the-cage`
- `req-interrupted-call-names-the-stopping-layer`
- `req-oversized-results-remain-recoverable-through-the-lane`
- `req-boot-needs-no-manual-test-metadata-repair`
- `req-stop-hook-yields-only-at-a-machine-stop`

Each has a verify method, measure and six-part quality scenario under an ISO quality use case.

## risks_logged

The register carries the five motivation risks and issues.

It also carries `raid-debt-harness-fallback-and-bounds-need-implementation-proof`.

That debt owns the unfinished provider fallback, answer-bound compatibility, lifecycle diagnostics, projection placement and fresh source gaps.

## round_0_verify

- evidence vs claims: pass; motivation claims point to standing high-level value props, the fixed quality hierarchy, six quality requirements and the dated research addendum.
- types: pass; only `sty-what-a-quality-is` refines `vp-qualities`, nine ISO quality use cases refine that story, and each i36 quality row refines an ISO quality use case.
- lint: pass at artifact shape; removed quality story and invented use-case identifiers have zero surviving trace references.
- tests: runtime implementation remains red and explicit debt; this gate claims only the design input and quality requirements.

## round_1_validate

- exercised against the goal: pass; each harness concern now has a measured quality row under compatibility, security, reliability or performance efficiency.
- missing: implementation tests and current unreachable vendor sources remain debt.
- wrong: technical quality behavior was modelled as free stories and use cases; those nodes are deleted.
- out of scope: UI mirror and expedition archive work remain elsewhere.
- prior art: ISO/IEC 25010 supplies the fixed quality taxonomy; project-specific choices begin at requirements.

## goals_served

- Measure what every supported host actually provides.: `req-supported-harness-serves-one-lane-contract` and the dated research addendum carry the compatibility measure.
- Close the five measured harness breaks in the prepared brief.: cage, stop, payload, boot metadata and host-profile requirements carry the quality demands.
- Make the lane report which harness it is talking to.: `req-supported-harness-serves-one-lane-contract` requires it under compatibility.
- Make future boots quicker by removing the test-metadata recovery step from the manual boot path.: `req-boot-needs-no-manual-test-metadata-repair` carries the reliability measure.
- Make oversized pull results recoverable through the lane instead of host files.: `req-oversized-results-remain-recoverable-through-the-lane` carries the performance-efficiency measure.

## bound_breaches

- if-agent-harness-to-entrypoint: breached; boot required a full test run, host-file fallback and manual research recovery. Iteration 36 owns the fixes. Their implementation proof is deferred to `raid-debt-harness-fallback-and-bounds-need-implementation-proof`.

## round_2_red_team

- Strong opposing case: requirements were authored before the normal M3 state => answer: the owner required the hierarchy repair now; later M3 work must review and may refine them, but they are correctly typed and measured.
- Strong opposing case: some harness demands span several qualities => answer: each row carries one concern and one characteristic; separate verification remains separate rows.
- Kill criterion: any story besides `sty-what-a-quality-is` refines `vp-qualities` => answer: not found; the count is exactly one.
- Kill criterion: an i36 quality row refines a freely invented quality use case => answer: not found; all six refine ISO quality-area use cases.

## raid_additions

- project/spec/trace/raid/raid-boot-test-metadata-coupling.md
- project/spec/trace/raid/raid-host-payload-offload-breaks-read-proof.md
- project/spec/trace/raid/raid-failed-tool-calls-stay-local.md
- project/spec/trace/raid/raid-route-remedy-can-repeat-refusal.md
- project/spec/trace/raid/raid-mcp-stop-is-not-diagnosable.md
- project/spec/trace/raid/raid-debt-harness-fallback-and-bounds-need-implementation-proof.md

## verdict

pass with overrides — The motivation and quality model are now correctly layered. The override is procedural: six quality requirements were authored during M2 repair and must be reviewed in normal M3 requirement work. Runtime experiments remain implementation debt.

## follow_up

Re-sign M2 evidence with only the non-quality stories and use cases.

At `gate-inputs`, judge user journeys separately from the six ISO quality requirements.

Normal M3 work must review the early-authored requirements before adding more.

## anything_else

The project-owned deep-research skill is available immediately in this VS Code host and is projected from one canonical source.
