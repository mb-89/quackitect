---
form: verification
by: agent
signed_off: 2026-08-19T17:37:42.486Z
authors: agent
files: null
---

# Evidence form / verification

## current_situation

Ten build chunks landed with 60 new cases across nine new test files. The battery is this state's to run, and its verdict records itself.

A TESTER WITH FRESH EYES WAS SPAWNED, as this state requires, and read the ten claims against their code without seeing the reasoning behind them. It reported all ten implemented as claimed, and quoted the line it judged each from.

WHAT THE TESTER COULD NOT DO, said plainly rather than glossed. Its harness gave it no terminal and no git, so it could not run the battery and could not diff the design-spec nodes against HEAD. Those were the two highest-value checks asked of it, and both came back unrun.

THREE THINGS IT DID FIND, each checked here.

- bound.ts computes ANSWER_BOUND_BYTES at module load from harness.ts. It confirmed harness.ts has zero imports, so there is no cycle and no initialisation-order hazard.
- stopping-layer.ts compares ISO timestamps as strings. Sound, because ISO-8601 with a fixed zone is lexicographically ordered.
- The cage excludes `fetch_copilot_cli_documentation`, which is not in NATIVE_PROJECT_TOOLS. That asymmetry is one-directional and harmless: the cage may exclude more than the minimum the check demands.

THE INSPECTION AND DEMONSTRATION SPECS ARE CHECKED GREEN on the same ground as at observe-red, and it is the honest one. Every one covers STANDING behaviour that this iteration did not touch, and the battery exercises the mechanics beneath them.

THREE CARRY A WATCH because this record moved code near them: tsp-first-run and tsp-the-arrival-in-one-act over the cage and the arrival, and tsp-unattended-start over the entrypoint. Each is re-observed when its area next moves.

## claims

- [x] tsp-a-slow-signal-keeps-the-wait
- [x] tsp-autonomy-tiers
- [x] tsp-bound-surface
- [x] tsp-coupling-disposition
- [x] tsp-derivation-analysis
- [x] tsp-desk-and-gates
- [x] tsp-first-run
- [x] tsp-one-door-into-the-pool
- [x] tsp-panel-walkthrough
- [x] tsp-prose-inspection
- [x] tsp-read-back-inspection
- [x] tsp-record-inspection
- [x] tsp-the-arrival-in-one-act
- [x] tsp-the-cited-refs-resolve
- [x] tsp-tour-run
- [x] tsp-two-machines
- [x] tsp-unattended-start

## follow_up

THE DATA-LOSS QUESTION IS ANSWERED BY THIS STATE'S PREDECESSOR, not by the tester.

Earlier today I submitted the trace-design node-table with its files column abbreviated, and the form wrote those short lists back onto all thirty design-spec nodes, deleting about ninety file claims. It is recorded as note-cc420e44bff0.

WHAT PROVES THE REPAIR is trace-design's own sweep rather than an inspection. It reported 89 unclaimed engine files, then 1 after the restore, then passed. A claim still missing would put its file straight back on that list, so the sweep passing IS the check, mechanically, over every .ts under the engine.

WHAT THE SWEEP DOES NOT COVER are the non-engine paths a spec may claim: main.canvas, package.json, .claude/settings.json, guidance/refusals.md, items/interface.md, vscode/src/extension.ts and bin/test-timings.mjs. All seven are present in the restored lists and were read back.

WHAT I STILL CANNOT SAY. Nobody diffed the thirty nodes against HEAD. The sweep makes a lost ENGINE claim impossible and a lost non-engine claim unlikely, and that is a weaker statement than the diff would have been.

THE TESTER'S HARNESS IS ITSELF A FINDING for this iteration's own subject. A subagent spawned to verify was handed no terminal, so the one thing verification exists to do — run the battery — it could not. That is a harness capability difference, which is exactly what i36 is about.

## anything_else

