---
form: draft-vision
by: agent
signed_off: 2026-08-19T09:06:11.403Z
reopened: 2026-08-19T08:46:19.331Z — The owner requires a fresh thorough online scan; inherited 2026-08-18 research is not sufficient for current harness claims.
authors: agent
files: null
---

# Evidence form / draft-vision

## current_situation

Iteration 36 remains a major self-hosting change.

The resident product vision still stands.

Fresh research now adds current MCP cancellation, progress, Agent Skills and VS Code hook evidence.

The lane research fallback, answer bound and deep-research skill have been implemented and are under focused test.

## big_idea

Quackitect should know the harness it is running in and adapt the lane before the walk becomes fragile.

A failed tool call becomes a measured improvement signal, not a mystery the next agent repeats.

## to_be_world

A driving agent starts a session and the machine identifies the host in front of it.

Boot serves only the text that host can reliably carry.

If a tool call fails, the lane records the shape, gives a working remedy and feeds that shape into the iteration's improvement work.

The owner sees a faster boot and fewer repeated recovery steps.

## goal_system

1. Boot must be reliable before it is complete.

2. Failed tool calls must become product feedback.

3. Host differences must be measured by the lane, not guessed by the agent.

4. The lane should keep the rules in one durable place and make tool descriptions small enough for host caps.

Conflict: boot completeness can fight boot speed.

Ruling: reliability wins, then speed improves by removing repeated recovery work, never by dropping checks.

## moore_pitch

FOR agents and owners running Quackitect across VS Code, Copilot CLI, Claude Code and cloud hosts WHO need the same walk to survive different harness limits, THE harness-aware lane IS a self-hosting runtime layer THAT detects host constraints and turns failures into mechanical fixes.

UNLIKE prompt-only compliance, OUR PRODUCT measures the harness through the lane and changes boot behavior from recorded evidence.

## follow_up

Re-check the downstream motivation packet against `ref-agent-harness-portability-2026-08-19`.

Carry these concrete signals forward:

- latest test metadata blocked boot
- oversized results forced host-file recovery
- signed kickoff routing repeated an illegal transition
- a cancelled pull did not identify the stopping layer
- web discovery lacked a configured provider and exposed no native search tool
- every failed call remains an improvement input

## anything_else

The vision inherits `project/product.md`.

Fresh evidence is in `project/spec/references/ref-agent-harness-portability-2026-08-19.md`.

The canonical workflow is `project/guidance/skills/deep-research/SKILL.md`.
