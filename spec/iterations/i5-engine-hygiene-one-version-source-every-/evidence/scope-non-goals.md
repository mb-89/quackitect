---
form: scope-non-goals
by: agent
signed_off: 2026-08-19T11:01:21.158Z
authors: agent
files: null
---

# Evidence form / scope-non-goals

## current_situation

The audit is signed at frame-delta. Eight items stand, three are struck, two were left out at the kickoff.

This state cuts the eight down to what one day can actually carry, and says plainly what it leaves.

## scope

SIX ITEMS. Each one is small, mechanical, and lands in the engine or its tests.

- THE VERSION FLAG. `se-mcp.ts --version` prints the version and exits without starting a server. This is the owner's package proof made runnable, and it is the one item with three named pieces behind it.
- THE ACTOR IS STAMPED, NOT GUESSED. The log record carries who made the call, and the feed reads that instead of the tool's name prefix. The guess stays only as the fallback for records written before the stamp existed.
- PREFLIGHT ASKS THE READER WHERE IT LOOKED. The palette and brand checks stop carrying their own copy of the path, so the check and the reader cannot go stale separately.
- AN EMPTY LIVE SOURCE SAYS SO. A field whose source resolves to zero items says that in the field, naming the source, instead of rendering as a field with no offer.
- THE PAINT RULES ARE PINNED. One test file asserts the three rules the note names, whatever the existing scattered cases already cover.
- THE HEAVY TEST FILE IS MEASURED, THEN SPLIT OR STRUCK. The measurement decides. If another file sets the critical path, the item is struck with that number and the split is not built.

WHAT MAKES THESE SIX ONE DAY. All six touch code that already exists, none adds a capability, and each has a named file and a named line to start from.

## non_goals

- THE CHECKPOINT COMMIT AT EVERY SIGNING. Real, confirmed open, and NOT taken. It changes the walk's own git behaviour, which is the one mechanism every other item depends on to land. A defect bundle is the wrong place to move it. It goes back to the pool with its ready-when: the next record that touches the record lifecycle.
- THE WRITE RATCHET. The note counts 36 direct write sites and the tree shows 76 occurrences of the call including imports. The two numbers are not comparable, and reconciling them is a measurement job that would eat the day this record is meant to spend building. Back to the pool, ready when somebody separates calls from imports.
- THE LOG GARBAGE COLLECTOR. calllog.ts already rotates by rename at a size limit. A harvesting collector is a different thing, nobody has asked for it since 2026-07-27, and no evidence says the log is a problem today.
- THE LEAVE CHECK REPORTING IN THE SUBMIT'S WORDS. Not opened at frame-delta and not taken here. It is a real owner ruling and it needs its own reading of the condition machinery, which is more than a bundle item.
- THE WHOLE-BATTERY GUARD'S OPEN QUESTIONS. The se_run hole and the poll cadence are rulings the owner owes. Left out at the kickoff, unchanged.
- ANY WIDENING OF THE TRACE OR THE CORPUS. This record touches the engine, its tests, the guidance where a refusal changes, and nothing else.
- THE FRESH-CLONE INBOX SIGNAL. Recorded as an assumption at the kickoff. The fix touches the survey, which is outside this goal.

## follow_up

gate-motivation is next and it judges whether six items and four strikes are still an iteration.

TWO ITEMS WENT BACK TO THE POOL rather than being dropped: the checkpoint commit and the write ratchet. Both carry a ready-when above.

THE MEASUREMENT THAT GATES ONE ITEM is still missing. The split item is written so that measuring is a legal outcome, which is what stops it becoming a build with no evidence behind it.

## anything_else

WHY SIX AND NOT EIGHT. The two dropped items are the only two that cannot be finished by reading one file and changing a few lines.

The checkpoint commit touches the walk's git behaviour, and this record needs the walk's git behaviour to stay still while it works.

The write ratchet is a counting exercise before it is a fix, and its own note admits the count is the hard part.

WHAT WOULD MAKE THIS SCOPE WRONG. If the six turn out to be larger than they look once decompose-structure opens them. The stale-list risk covers the opposite failure; this one has no entry, because the honest response is the same either way — escalate visibly at gate-implementation rather than quietly.
