---
form: generalize-use-cases
by: agent
signed_off: 2026-08-19T11:06:23.520Z
authors: agent
files:
---

# Evidence form / generalize-use-cases

## current_situation

One use case is new and none is rewritten. Forty-seven stood before it.

It generalises the one story this delta wrote. The other five scope items still have neither, which is recorded rather than filled.

## use_cases

- uc-prove-an-install

## follow_up

M3 derives the requirements from this case's steps and extensions.

FIVE ITEMS ARRIVE AT M3 WITH NO USE CASE ABOVE THEM. Their requirements are cross-cutting quality rows rather than derivations from a pass, and the write-requirements state is told that here so it does not go looking.

The extension 2a — an unreadable manifest answering `unknown` — is already the code's behaviour in engine/version.ts. It is written into the case so the requirement derived from it can cite the line rather than invent a rule.

## anything_else

WHY A NEW CASE RATHER THAN AN EXTENSION OF uc-install-quackitect. The method says to extend where a story fits an existing case, so the choice was made deliberately and not by default.

uc-install-quackitect ENDS AT THE FRONT DESK. Its guarantee is that the panel is drawn, the engine answers and the desk is waiting. Every step of it starts something.

THE NEW CASE'S WHOLE POINT IS THAT NOTHING STARTS. Folding it in as an extension would put a path that opens no port inside a case whose guarantee is a running lane, and the guarantee line would then be false for one of its own branches.

THE GOALS ARE DIFFERENT TOO. One is "reach the desk". The other is "know which build I have". A second goal is a second use case, by the method's own first rule.

WHAT THIS CASE DELIBERATELY DOES NOT CLAIM, written as extension 5a so a later reader cannot mistake it: the check proves the code loads and which build it is. It never proves a running lane stays up.
