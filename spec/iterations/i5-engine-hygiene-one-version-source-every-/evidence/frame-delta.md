---
form: frame-delta
by: agent
signed_off: 2026-08-19T11:00:42.125Z
authors: agent
files:
---

# Evidence form / frame-delta

## current_situation

The audit the kickoff routed here is done. Ten of the thirteen source items were opened against the tree, on top of the three opened at the gate.

THE COUNT: 3 of 13 are already landed or retired. 8 stand as written. 2 were left out at the kickoff and stay out.

The frame itself does not move. This delta authors no new value proposition — every item extends work the standing props already cover, and the register's five entries carry what is genuinely new.

## value_props

- none

## business_case

SKIPPED, with the reason recorded. There is no acquirer for this delta.

Every item is internal quality: a version flag on the entrypoint, an actor stamp in the log, a preflight check that asks the reader, a checkpoint commit, four test pins, and a measurement.

WHAT IT BUYS, in the maintainer's currency rather than in money: a packaged build that can be asked what it is, a feed whose actor column is a fact rather than a guess, and four rules that stop being enforced by memory.

THE ONE FIGURE WORTH QUOTING: the actor guess was wrong for 52 records in the window i34's retro measured, and it is wrong by construction for every server-side tool nobody remembers to add.

## follow_up

SCOPE-NON-GOALS TAKES THE STRIKES. The three settled items are struck there with the evidence read here, and the eight that stand become the record's real scope.

DECOMPOSE-STRUCTURE TAKES THE ONE MEASUREMENT. The test split cannot be designed before the battery's critical-path file is named, and that number does not exist on this container.

THE STALE-LIST RISK IS ANSWERED but not closed. 3 of 13 struck is under half, so the record still holds a day of work. The entry stays open until the build confirms the eight are as small as they look.

## anything_else

THE AUDIT, ITEM BY ITEM, so no later state repeats it.

LANDED ALREADY — struck at scope-non-goals.

- ONE VERSION SOURCE. engine/version.ts reads ../package.json through import.meta.url and answers "unknown" rather than throwing. No live engine file carries a 3.0.0-bootstrap stamp; the 14 hits are one test comment and the historical record.
- EVERY REFUSAL ANCHORED. tests/refusals.test.ts already asserts both directions — every registry clause has its section, and every section names a registry clause — and a third case checks the Rejection payload's pointer. errors.ts computes it in clauseGuidance.

RETIRED — struck at scope-non-goals.

- THE DUPLICATE-KEY SWEEP ON se_git_sync. The verb is gone. i34 removed se_git_land and se_git_sync with the claim ledger and the worktrees, and its own evidence says both were dead in public before that.

STANDS AS WRITTEN — the record's scope.

- THE VERSION FLAG. engine/bin/se-mcp.ts has --help and no --version. i16 found it by asking for it and starting a server instead.
- THE FEED'S GUESSED ACTOR. render.ts:554 reads `tool.startsWith("mirror_") ? "human" : "agent"`. The handler knows who called; the renderer guesses from a string.
- THE PALETTE'S SILENT FALLBACK. render.ts:117 reads the palette live and falls back silently by design. preflight.ts:180 checks its OWN copy of the path rather than asking the reader where it looked, so the two go stale together exactly as the note says.
- SIGNING WITHOUT COMMITTING. Commits fire at seed, started, pin, shipped and expedition open and close. Nothing commits at a signature.
- AN EMPTY LIVE SOURCE IS STILL SILENT. stateform.ts:494 throws on a `$name` NOBODY RESOLVES, which is the typo case. A source that resolves correctly to zero items still renders as a field with no offer.
- THE PAINT RULES. Cases exist in drift.test.ts, reopen.test.ts and claimops.test.ts. Whether they pin all three rules the note names is the author-tests state's question, not this one's.
- THE TEST SPLIT. refs.test.ts stands undivided, and its value is disputed by i16's own measurement.
- THE WRITE RATCHET. 76 occurrences of writeFileSync across engine/, imports included, against the note's count of 36 SITES. The two numbers are not comparable without separating calls from imports, and that separation is decompose-structure's.

LEFT OUT AT THE KICKOFF, unchanged here.

- The whole-battery guard's open design questions, which need the owner.
- The log garbage collector. calllog.ts rotates by rename at a size limit; a harvesting collector is a different thing and nobody has asked for it since 2026-07-27.
