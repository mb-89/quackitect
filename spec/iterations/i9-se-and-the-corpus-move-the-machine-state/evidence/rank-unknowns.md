---
form: rank-unknowns
by: agent
signed_off: 2026-08-19T18:47:05.252Z
authors: agent
files: null
---

# Evidence form / rank-unknowns

## current_situation

THE REGISTER HOLDS 168 OPEN ENTRIES. Sixteen of them carry FATAL damage; the rest sit at crippling, corrosive or below. The exposure ranking is the register view's, and this form does not restate it.

THREE UNKNOWNS ARE SEEDED. Each clears all three filters, and the two that dropped out are named below rather than left as silence.

THE HOT CORNER LOST TWO OF ITS OWN, both to the already-probed filter. That is the filter working rather than a gap.

- `raid-asm-the-cage-holds-so-every-write-passes-the-lane` is fatal, and it was re-probed on 2026-08-18. What remains of it is a named issue about boot granting no tools, which is work rather than an unknown.
- `raid-iss-two-entrypoints-place-the-cage-and-nothing-compares-them` is crippling and expected, and nothing about it is unknown. Its fix is written down: one shared module and a test that both entry points place identical bytes. This iteration's own merge decision folds those two entry points into one, so the goal already exercises it.

TWO MORE WERE CONSIDERED AND CUT, each on a stated filter.

- `raid-asm-the-isolation-rule-means-the-same-on-every-platform` is fatal and expected, and it is NOT SPIKEABLE HERE. It needs a second platform, and `raid-debt-cloud-validation-needs-a-machine-this-one-cannot-make` says in as many words that this machine cannot make one.
- `raid-risk-geometry-stops-excluding-session-state-and-nothing-replaces-it` is corrosive and expected, and the iteration's own goal exercises it. Splitting the exclusion by file is the work that answers it, so a probe would measure the thing the build is about to change.

## seeded

- project/spec/trace/raid/raid-iss-most-of-the-criteria-cut-was-judged-from-a-rows-name.md
- project/spec/trace/raid/raid-asm-the-hosts-pattern-test-and-its-handover-name-the-same-folder.md
- project/spec/trace/raid/raid-risk-a-path-that-resolves-the-machine-state-folder-is-missed.md

## follow_up

EACH SPIKE HAS ONE QUESTION AND A TIMEBOX, so the drawing can carry both.

### Was the criteria cut sound?

`raid-iss-most-of-the-criteria-cut-was-judged-from-a-rows-name`. Crippling and expected, never probed.

NINETY-NINE AXES WERE STRUCK AS NON-DIFFERENTIATING. Twenty were checked against their written statements. Of ten the gate re-read afterwards, two were wrong. SEVENTY-NINE WERE NEVER READ AT ALL.

THE ARCHITECTURE GATE FOUND INDEPENDENT EVIDENCE FOR THE SAME DISEASE. Ten of the twelve surviving axes are ties between the two leading designs. Twelve axes were kept to differentiate, and they differentiate on two.

THE PROBE IS THE ENTRY'S OWN AND IT IS MECHANICAL. Read all ninety-nine statements rather than their names, and hold each against the three candidate configurations. The entry says it is one script and one pass, because the statement is a single frontmatter key.

TIMEBOX: one hour. It returns a count of wrongly struck rows and their ids.

### Can the host state which folder it handed over?

`raid-asm-the-hosts-pattern-test-and-its-handover-name-the-same-folder`. Crippling and plausible, and its own text records it as Unprobed.

THE SEAT RESTS ON TWO CELLS AND THIS IS ONE OF THEM. The chosen design converts the worst silent failure on the chart into a refusal, by having the host STATE the tree it hands over and acknowledging that exact tree back. If the host cannot state it, the cell has nothing to acknowledge.

THE PROBE READS THE HOST'S OWN API AND THEN TRIES IT. Open a workspace holding several folders. Make the activation pattern match one and see which the handover offers.

TIMEBOX: one hour. It returns whether the two can disagree, and what the product sees when they do.

### How many places build a path from the machine-state folder?

`raid-risk-a-path-that-resolves-the-machine-state-folder-is-missed`. Corrosive and plausible, never probed.

THE RECORD'S OWN NUMBER IS WRONG AND THE BUILD PLAN STARTS FROM IT. The record calls the resolver three lines. A search already found the folder path threaded as an argument through at least four other modules.

THE FAILURE IS THE QUIET HALF. A missed caller finds nothing at the new location, and nothing is a legal answer here: a machine that has never run has no call log and no notes.

THE PROBE IS A COUNT BEFORE THE MOVE. Enumerate every module that builds a file path from this folder and list them. The entry says listing it is the first act of the move rather than a check afterwards.

TIMEBOX: thirty minutes. It returns the list, and the number replaces the wrong one in the record.

## anything_else

