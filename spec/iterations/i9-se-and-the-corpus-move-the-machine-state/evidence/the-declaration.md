---
form: the-declaration
by: agent
signed_off: 2026-08-20T07:46:57.043Z
authors: agent
files:
---

# Evidence form / the-declaration

## current_situation

THE SPINE'S FIRST CHUNK, and everything else in this record crosses it. The consumers, the exclusion split, the root check and the move all read the answer this module now owns.

WHAT STOOD BEFORE IT. Twenty-six sites across seventeen engine files each named the folder for themselves, which is the spike's own count re-taken against the merged tree at this chunk.

NOTHING HAS BEEN REPLACED YET. The declaration stands beside all twenty-six, and that is deliberate: this chunk makes the one answer exist, and the next generates the consumers from it.

## built

ONE MODULE, TWO FACTS. `deliverable/engine/statedecl.ts` holds what the machine-state folder is called and which files inside it have a door of their own. Nothing else in it, and no third fact.

WHAT THE FACTS ARE.

- The folder is `.se`.
- Three files have a door: the call log through se_log_query, the notes through se_note, and the reading through se_pull.

THE THREE ARE THE VISION'S OWN NUMBER. draft-vision settled it in as many words: the door rule is about THREE FILES, and hiding the whole directory was a proxy that over-reached onto everything else in the folder.

WHAT DERIVES RATHER THAN REPEATS. `machineStateDir(opened)` takes the OPENED folder and never a repository root, which is the whole architectural move stated as a signature. `declaredDoors()` hands the declaration out as a value, so the five consumers can be generated from it instead of written beside it. `doorFor(name)` matches a WHOLE name, because a prefix match would withhold a file whose name merely extends a door's.

WHAT IS MEASURED, NOT CLAIMED. Twenty-six sites across seventeen engine files name the folder today. That is the spike's own figure, re-counted at this chunk against the merged tree, and it is what the next chunks reduce.

WHAT THIS CHUNK DELIBERATELY DID NOT DO. It changed no consumer. The declaration stands beside twenty-six hard-codes and has replaced none of them, which is the next chunk's work and is why the staleness check after it is not tidy work.

THE RED CASES MOVED AND SAY SO. The consent cases now get past the folder's address and fail on the root check, naming it: "the system does not yet decide its own root". That is the chunk after next, and the failure message is the plan.

## follow_up

### The next chunk, and why it is not tidy work

FIVE CONSUMERS ARE GENERATED NEXT, not edited. The path resolver's exclusion, the producing acts' exclusion, the search ignore, the table sweep's skip set and the editor's hide rule each hold their own copy today.

THE STALENESS CHECK AFTER THEM GUARDS A FATAL ROW. The root check runs a test generated from this same declaration, so a consumer that drifts from it is no longer a filing problem.

### Two names the later chunks owe

THE RED CASES ALREADY NAME THEM. `chooseRoot` is owed by the root check, and the consent cases fail on it today saying so in their own words. `productOwnedNames` is owed by the move, and the resolution case that walks the level above fails on it.

BOTH ARE DELIBERATELY NOT HERE. This module holds two facts, and neither of those is one of them.

## anything_else

