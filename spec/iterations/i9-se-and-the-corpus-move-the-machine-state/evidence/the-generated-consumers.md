---
form: the-generated-consumers
by: agent
signed_off: 2026-08-20T07:50:08.788Z
authors: agent
files:
---

# Evidence form / the-generated-consumers

## current_situation

THE DECLARATION EXISTED AND NOTHING READ IT. That was the point of the chunk before this one, and it left twenty-six sites still naming the folder for themselves.

SIX CONSUMERS NOW DERIVE FROM IT, one more than the plan named. The extra is the vault watcher, which held the same list as the table sweep and would have drifted from it the first time either changed.

## built

FIVE CONSUMERS GENERATED, AND A SIXTH FOUND WHILE DOING IT.

- THE PATH RESOLVER. `paths.ts` had three copies: the excluded-directory set, the declared-roots path, and `seDir` itself. All three now come from the declaration, and `seDir` is one line calling `machineStateDir`.
- THE PRODUCING ACTS. `produce.ts`'s exclusion list takes the name rather than spelling it.
- THE SEARCH IGNORE. `search.ts` built its ripgrep glob from a literal.
- THE TABLE SWEEP. `tables.ts`'s skip set.
- THE VAULT WATCHER. `vault.ts` held four: its own exclude list plus three paths built by hand. It is the sixth consumer and it was not in the plan, which is the plan being wrong rather than the work growing.
- THE EDITOR. `vscodesrcextension.ts` cannot import the engine at run time, because it is a separate build. It holds ONE constant marked as generated, where it held two literals.

THE EDITOR'S COPY IS THE ONE THAT NEEDS THE CHECK. A copy nothing compares is exactly the drift the declaration exists to stop. A copy something compares is fine, and comparing it is the next chunk.

WHAT DID NOT MOVE, said plainly. The remaining hard-codes sit in scripts and hooks rather than in the five consumers this chunk names: the arrival, the stop hook, the web-search hook, the record inspector, the selftest, the outward search, the lifecycle, the stopping layer, the help store and the run lane. They belong to the move, which is the chunk that walks all twenty-six.

THE ENGINE STAYED GREEN THROUGH IT. The only failures in the run are this record's own reds, and every one names a chunk that has not been built yet rather than something this chunk broke.

## follow_up

### The next chunk is not tidy work

THE STALENESS CHECK GUARDS A COPY THAT CANNOT IMPORT. The editor's constant is the case that makes the check load-bearing rather than decorative, and it is the one a reader would otherwise trust because it looks like every other literal in that file.

### The plan named five consumers and there were six

THE VAULT WATCHER WAS MISSING FROM IT. It holds the same exclude list as the table sweep, and the two would have drifted the first time either was edited. Recorded here rather than quietly folded in, because the chunk plan is evidence and a plan that was wrong should say so.

### What the move still owes

TEN MORE FILES NAME THE FOLDER, in scripts and hooks rather than in consumers. The move walks all of them, and the spike's eleven recorded queries are the check that none was missed.

## anything_else

