---
form: run-spikes
by: agent
signed_off: 2026-08-19T19:02:45.654Z
authors: agent
files:
---

# Evidence form / run-spikes

## current_situation

THREE SPIKES RAN IN PARALLEL AND EVERY ONE FOUND SOMETHING. None came back "holds".

EACH WAS SEEDED FROM THE REGISTER at rank-unknowns, and each ran in a clean context with the lane rule passed to it. The results are written back onto the entries they settle, with the probe date.

### Was the criteria cut sound? FIVE MORE WRONG.

Ninety-nine of ninety-nine statements read, not sampled. Timebox one hour.

FIVE MORE ROWS WERE STRUCK WRONGLY, on top of the two the gate already found. All five share one blind spot: nobody checked the bootstrap cluster, where the third design deletes the installer AND the scaffold.

ONE OF THE FIVE SITS ABOVE THE LINE. `req-scaffold-from-template` at rank 14 becomes a thirteenth axis. The other four are out by position rather than on merit.

A SECOND DEFECT CAME WITH IT, and it is worse. Three rows carry the reason "vehicles, untouched" and none of the three is about a vehicle. A reason was copied down a block of adjacent rows. Judging from a name at least looks at the row.

FOUR MORE ARE UNSURE and each names what would settle it.

### Can the host state which folder it handed over? NO, AND THERE IS NO HANDOVER.

Read from the editor's own source and its entire published type surface. Timebox one hour.

THE ASSUMPTION IS FALSE, and not in the way it predicted. It assumed two host features each name a folder and might disagree. Only one names a folder, and it names it to itself.

THE EDITOR FINDS THE MATCHING FOLDER AND DISCARDS IT. The identity is in scope on the line that activates, and is not passed on. With a wildcard pattern the folder is never computed at all: one search runs across every folder and returns a boolean.

THE EXTENSION RECEIVES THE WHOLE WORKSPACE and no activation reason. Every published type and every proposed one was checked; the activation event appears nowhere.

TWO SILENT FAILURES WERE FOUND ON THE WAY. A seven-second timeout kills wildcard activation, leaving only a log line. Adding the first folder restarts every extension rather than firing the change event.

### How many places build a path from the machine-state folder? FORTY-SEVEN.

Counted by search across the tree, with every query recorded so the count can be repeated. Timebox thirty minutes.

THE RECORD SAID ONE PLACE, ABOUT THREE LINES. It is wrong by roughly a factor of fifteen, and this entry's own guess of four modules was an undercount too.

THE SPLIT IS THE FINDING. Four resolvers, three of them scripts that re-implement the one real resolver. Seventeen consumers, which ask and are therefore safe. Twenty-six hard-codes that never ask anything, which is the majority and the dangerous kind.

FOUR MORE HARD-CODES SIT OUTSIDE THE ENGINE and one of them is the ignore file. Miss that and machine state gets committed.

THREE TREES COULD NOT BE SEARCHED because version control ignores them and the search honours that. One of the three is the machine-state folder itself, which holds a generated client script naming its own path.

## follow_up

EACH RESULT LANDED ON THE ENTRY IT SETTLES, with its probe date, rather than only here.

### What the host finding forces, and it is a design change

THE WINNING DESIGN'S ROOT CELL IS HALF IMPOSSIBLE. `opt-ownership-transfers-by-explicit-handover` said the editor, the launcher or a hook states the root. The editor cannot, and the editor is the host this product runs in.

THE CELL AND ITS DECISION ARE BOTH REWRITTEN. The guarantee is unchanged: exactly one tree, or a refusal. The mechanism is now one-sided. The system runs the same content test the host ran, once per open folder, and counts carriers.

- Nought carriers. Refuse. Something claimed one exists.
- One. Bind to it, and echo it where a person reads it.
- Two or more. Refuse, and ask the person.

IT NEVER LEARNS WHICH FOLDER FIRED THE EVENT. It makes that question irrelevant instead, because the only case where the trigger's identity matters is the ambiguous one and that case refuses.

THE SHAPE IS WEAKER THAN WHAT WAS SCORED. A receiver checking its own input is not a two-sided transfer, and the score that put this line ahead on the only FATAL axis was awarded for the two-sided act. THAT IS BEING RESCORED by a second hand with a clean context, holding both designs on that axis at once. The composer does not get to decide it.

A REMAINING SESSION IS STILL WORTH SPENDING, now aimed at two narrower questions the source could not answer: the seven-second timeout under a slow search, and the search path under a remote authority.

### What the criteria finding forces

FIVE ROWS ARE RESTORED IN `cut-criteria`, with the ranks recorded and the one above the line named. A second hand is scoring them across all three designs, by axis rather than by candidate.

THE SEAT CANNOT MOVE ON THEM. All five separate the third design only, and both leaders keep the installer and the scaffold.

THE LARGER PROBLEM GETS WORSE, and this is what the retro should carry. The twelve surviving axes separated the two leading designs on two. A thirteenth they tie on makes it two of thirteen. Either the criteria do not discriminate, or the cut is removing the rows that would.

### What the path count forces

THE MOVE STARTS FROM FORTY-SEVEN, not three. The twenty-six hard-codes are the work, because they never ask a resolver and no rename reaches them.

THE TEST FILES ARE THE SAFETY NET rather than the risk. Twenty-four of them build the path themselves and fail loudly.

THE PROMPT LAYER IS THE QUIET RISK. Three documents that reach the agent every turn name the folder directly. A stale path there misdirects the agent rather than breaking a program, and nothing fails.

THREE BLIND SPOTS ARE NAMED rather than left as a clean number: the build output, the machine-state folder itself, and the workbench. Two environment variables also carry the folder by value into every condition script, so no search for the literal can find those call sites.

## anything_else

