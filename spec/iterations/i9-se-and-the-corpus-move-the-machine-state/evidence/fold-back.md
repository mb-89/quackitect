---
form: fold-back
by: agent
signed_off: 2026-08-19T19:10:12.980Z
authors: agent
files: null
---

# Evidence form / fold-back

## current_situation

THREE EXPERIMENTS FOLD BACK THIS ITERATION and all three carry a verdict of falls. The other fourteen rows in the table are earlier iterations' and are shown because the view reads every experiment node.

THE ANSWERS LIVE ON THE NODES. This form edits their two keys and restates nothing.

### What moved upstream

THE CRITERIA CUT LOST FIVE STRIKES. One of the five sits above the line and is now a thirteenth axis, scored across all three designs by one hand. The other four are out by position rather than on merit.

THE WINNING DESIGN'S ROOT CELL WAS REWRITTEN. Its option node, its decision, its own document and one element all changed. The guarantee held and the mechanism went from two-sided to one-sided.

THE MOVE'S SIZE WENT FROM THREE TO FORTY-SEVEN. Nothing upstream changed shape; the number the build plans against did.

### What did NOT reopen, and why that is a result rather than an omission

NO GATE WAS REOPENED. Every change landed as an amendment to a claim that still stands, and each says what changed and why.

THE ARCHITECTURE GATE SURVIVES THE HOST SPIKE. The spike changed a mechanism inside an element, not the set of elements or the crossings between them. No function moved, no interface was added, and nothing became owed.

THE SCORE THAT DECIDES THE SEAT SURVIVES IT TOO, and that was measured rather than assumed. A second hand held both designs on the FATAL axis after the rewrite. Neither number moved, because the requirement grades by failure mode and both shapes sit at the same rank.

THE THIRTEENTH AXIS TIES THE TWO LEADERS at nought each. The third design scores two there and it is already eliminated by six.

## folded

| experiment | folds_to | promote |
| --- | --- | --- |
| [[exp-a-structural-rename-across-a-vehicle]] | Settles raid-tripwire-i16-a-structural-migration-cannot-be-written: it does NOT fire. A structural migration can be expressed as a program, so the winner's falsifier is not met and no score moves. What the evidence ADDS is a bound nobody had: the program route's advantage over a plain git merge exists only where a node is short enough that an upstream edit drops similarity below git's rename threshold, and the corpus sits on the safe side of that line. The bound is recorded on the tripwire rather than applied to the scores, because re-weighting an axis with the totals already visible is the poisoning cut-criteria's ordering exists to prevent. | none — the two probe repositories are throwaway by the throwaway law, and the finding is the product. The one thing worth carrying is the bound itself, which belongs on the tripwire rather than in build code. |
| [[exp-can-the-host-state-which-folder-it-handed-over]] | opt-ownership-transfers-by-explicit-handover loses the editor from its list of things that can state a root, and raid-dec-ownership-is-stated-by-the-host-and-acknowledged-back is rewritten one-sided. The guarantee is unchanged and the mechanism becomes a self-check that refuses on any carrier count but one. | none — the check is four lines against the host's own file API and belongs in the build rather than in a script kept from here. |
| [[exp-channel-cost]] | raid-asm-machine-wide-state-serves-over-a-local-channel is probed for the call log at 144 microseconds a crossing | none — the build attacks the 124.7 microsecond direct-append floor first, by keeping a handle open |
| [[exp-claim-verb-race]] | raid-asm-remote-serializes-claims carries the dated local-half measurement extended to the whole verb; nothing upstream moves — the winner and its ADR stand as decided | none — WITHDRAWN 2026-08-16 by i11. It promoted the claim verb's mechanism (record then announce, rebase-and-retry on rejection, release as a second commit) into chunk core-process. i34 deleted the claim system whole, so there is no verb left to promote it into. The 9-of-9 measurement stands and is not retracted; only the promotion is. |
| [[exp-does-a-backgrounded-lane-release-its-caller]] | el-entrypoint's start step releases its caller on both platforms, so nothing downstream depends on a platform split for that reason. The detach stays for a DIFFERENT reason - a POSIX process group so a closing session does not take the lane down - and that reason is untested here. The assumption splits in two: releasing the caller is a spawn question and is answered here, surviving the session is a host question and stays owed. | none |
| [[exp-how-many-places-build-a-path-from-the-machine-state-folder]] | the move is sized from forty-seven rather than three, and the twenty-six hard-codes are the work because no rename reaches them. Every query is recorded on the register entry so the count can be repeated after the move. | the queries themselves. Re-running them after the move is the check that nothing was missed, and it costs one call. |
| [[exp-i15-query-latency-at-real-corpus-scale]] | raid-risk-i15-query-latency-unmeasured-at-real-scale — status moves open to mitigated, 31 ms measured against the 1000 ms bound is a 32x margin, comfortably absorbing the throttled-hardware discount raid-asm-the-target-machine-is-many-throttled-cores names | none — the real filters.and/or evaluator and YAML frontmatter parser are still owed at build time; this script is throwaway per the spike-tracer law |
| [[exp-inflight-death]] | raid-ar-crash-lands-safe is re-grounded, because the channel detects a dead satellite in under 100 ms | none — the deadline is one number for specify-build, and it must sit above the 94 ms a crash takes to reach the caller |
| [[exp-kill-and-resume]] | raid-ar-resume-needs-no-person carries the dated probe — the hinge holds today, and the hard-kill variant stays deferred with the POSIX until | none — the files-only property holds by construction, and the build must not erode it |
| [[exp-latency-ledger]] | raid-ar-call-answers-in-one-second re-grounded on the dated ledger and the stale 274 retired — the demand stands and is missed today | none — the fix is the async round's ticket desk, chartered in the backlog |
| [[exp-one-seam]] | raid-risk-a-write-lands-in-the-wrong-tree-silently moves from risk to issue on two dated instances | none — the work is routing the modules that read the filesystem for themselves through the resolver, and lint.ts is the worked example |
| [[exp-satellite-start]] | raid-ar-call-answers-in-one-second is re-grounded at 306.9 ms with the module load included | none — the constraint enters specify-build as a property of the START act, and loading less eagerly is the lever it has |
| [[exp-the-posix-branches-have-never-run]] | Nothing structural moves. It re-frames the first spike rather than changing an element - the start step is platform-split by design, and only the Windows half has ever run. raid-lane-works-on-posix keeps its grade and gains the count. | none |
| [[exp-trunk-read-cost]] | raid-dec-thin-tree carries the dated measurement — the bet holds in the batch-reader shape only, and no requirement moves | none — WITHDRAWN 2026-08-16 by i11. It promoted a long-lived batch reader as the trunk-read shape, because a spawn per read cost 47 to 54 ms a file. i34 removed the reason: a closed record's folder stays on disk, so nothing is read out of git and there is no trunk read to shape. The 2.0 ms measurement stands for whoever needs a batch reader again. |
| [[exp-was-the-criteria-cut-sound]] | cut-criteria loses five strikes, with each rank recorded and the one above the line named. req-scaffold-from-template becomes a thirteenth axis and is scored by a second hand. The other four are out by position rather than on merit. | none — the finding is the product. What is worth carrying is the guard, not the script - a cut whose reason does not quote the row's own statement is unverified by construction and can be marked so with no judgment at all. |
| [[exp-watchdog]] | raid-ar-crash-lands-safe narrows further, because a heartbeat covers one hang shape of two | none — the deadline and the beat interval are both numbers for specify-build |
| [[exp-what-runtime-the-engine-actually-needs]] | el-entrypoint's verify step compares against a PINNED version rather than a floor, and package.json line 8 is corrected. The assumption is answered for the local half and stays owed for what a default install gives a bare host. | none |

## follow_up

### What the build now owes that it did not before

THE ROOT CHECK IS THE SYSTEM'S OWN, and it is four lines against the host's file API. Count the folders carrying the marker. Refuse on nought, bind on one, refuse and ask on two or more.

USE A FILE-EXISTS CALL RATHER THAN A SEARCH. A search applies the person's own exclusion settings unless told otherwise, which makes it a different test that can disagree with the host's.

THE STALENESS CHECK IS NO LONGER TIDY WORK. It was owed for the exclusion. It is now the guard under a FATAL requirement, because the root check and the host's activation test are both generated from one declaration and a drift between them is silent.

THE MOVE IS SIZED FROM FORTY-SEVEN. Twenty-six of those never ask a resolver, so no rename reaches them.

RE-RUN THE ELEVEN RECORDED QUERIES AFTER THE MOVE. That is the check that nothing was missed and it costs one call. Three trees must be searched by hand, because version control ignores them and no sweep will look.

### One session is still owed and is now narrower

THE EDITOR WAS NEVER OPENED. The source answers whether the information exists, more strongly than a session could. Two questions still need a running editor: a seven-second timeout that kills wildcard activation with only a log line, and the search path forced under a remote authority.

### What the retro inherits

THE SEEDING CARRIED NO FALLBACK. All three spikes were seeded with a question and a timebox and nothing about what to do if the answer came back badly. Each experiment node records that against itself. A fallback decided after the result is not a fallback.

THE CRITERIA DO NOT DISCRIMINATE, OR THE CUT REMOVES THE ROWS THAT WOULD. Thirteen axes now separate the two leading designs on two. Nobody has asked whether the twelve that survived were the right twelve, and that is a different probe from the one that ran.

A CHEAP GUARD WOULD HAVE CAUGHT THE WORST OF IT WITH NO JUDGMENT AT ALL. A cut whose reason does not quote the row's own statement is unverified by construction. Three rows were struck with a reason that was true of neither the row nor its neighbours, copied down a block.

### Owner request parked with its condition

A PACKAGING COMMIT WAS ASKED FOR MID-WALK and could not be fetched here, because git is not among this state's legal tools. It is captured as a note with the condition: fetch it at the first state that grants git, and before anything touches the packaging path.

## anything_else

