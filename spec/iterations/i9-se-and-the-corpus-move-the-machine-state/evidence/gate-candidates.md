---
form: gate-candidates
bless: blessed by agent
amended: 2026-08-19T16:42:07.997Z by agent — the owner overruled the missing-install-row reasoning, the row was added and judged, and one of three dissents is now resolved
by: agent
signed_off: 2026-08-19T16:20:13.950Z
authors: agent
files: null
---

# Evidence form / gate-candidates

## current_situation

THE FRONT IS THREE AND NOTHING IS ELIMINATED. Every candidate is best somewhere and worst somewhere, which is what a real front looks like.

THE GATE FOUND TWO WRONG CUTS AND CORRECTED THEM BEFORE RULING. One of them landed above the line, so a twelfth axis was scored by a fresh second hand and domination was recomputed. The front did not move.

SEVEN OF THIS GATE'S EIGHT ACCEPTANCE ITEMS ARE COMPUTED and were true before the walk arrived. The eighth is the only judgment here, and it is the one that failed.

A LIVE PRIOR-ART SCAN RAN AT THIS GATE rather than a citation being reached for. It found a working precedent for what this iteration is inventing, a defect in the iteration's own vision, prior art arguing AGAINST one candidate's strongest cell, and a stronger comparison FOR it than the one it was scored against.

THE INSTALL ROW WAS ADDED AFTER THIS GATE FIRST STAMPED, on the owner's ruling, and it changed the chart more than any other correction. Their words: nobody has answers because nobody was asked, so asking might produce a differentiator. It did. The axes with no candidate at solid baseline went from four to one.

A SECOND LIVE PRIOR-ART SCAN RAN FOR THAT ROW and disproved a claim three of our own nodes rested on. All three are corrected.

THE BLESS IS NOT THE AGENT'S HERE. The owner said they would look at the gates, so this form is stamped and left unblessed.

## reasons_hold

THIS GATE ASKS ONE THING AND THE ANSWER IS NO, PARTLY. Two of the cut reasons did not hold, and they were found here rather than believed.

### The cuts

NINETY-NINE AXES WERE STRUCK AS NON-DIFFERENTIATING. Twenty were checked against their written statements before the cut. Seventy-nine were judged from the row's name and the delta's reach.

THE GATE RE-READ TEN, chosen adversarially rather than at random: the rows a candidate that moves records into branches would touch. TWO FAILED.

- req-setup-stops-before-partial, struck as "inherited identically". Its statement stops the setup script when a tool it NEEDS is missing, and one candidate adds version control as a required tool. It sits at rank 26, above the line, so it became a twelfth axis and was scored: 0, 2, 2.
- req-close-leaves-trunk-clean, struck as "closing a record, untouched". Its statement demands zero uncommitted changes from the record on the trunk, and one candidate exists to hold engine writes to four named mutations. It sits at rank 49, below the line, so it is now out by position rather than on its merits.

BOTH ARE RESTORED. cut-criteria was amended before this verdict was written, not after.

THE ERROR DID NOT AIM AT A FAVOURITE, which is the thing this gate exists to catch. Both wrongly cut rows would have scored AGAINST the same candidate, not for it. That is evidence the mistake was carelessness rather than steering, and carelessness is what raid-iss-most-of-the-criteria-cut-was-judged-from-a-rows-name is filed about.

SEVENTY-NINE CUTS REMAIN UNREAD. The number still wrong is unknown rather than zero, and the honest thing is to say the sample rate rather than to imply a clean sweep.

### The moves

TWO ROWS WERE MOVED OUT OF RANK ORDER AND BOTH REASONS HOLD.

- The desk row moved 70 places up. Its rationale cites an authored comparison written at derive-criteria before any candidate existed: a person who cannot start the product has no surface to find slow. The comparison is on the record and predates the options, so the move restores a blind judgment rather than jumping one.
- The corpus row moved above determinism. Its rationale is that a deterministic query over a corpus two readers disagree about is deterministically wrong. That is an argument from the rows themselves and not from any candidate's picks.

NEITHER MOVE HELPED A PARTICULAR LINE. The desk row's move put an axis where one candidate scores 4 and two score 2 into the vital few, which sharpens a difference rather than smoothing one. The corpus row's move did the same in the opposite direction.

### The front

IT IS THREE, NOT ONE, so the honesty question the gate reserves for a front of one does not arise.

IT IS ALSO ROBUST. Domination was recomputed THREE times: once when a second independent scorer produced a different table, once when the twelfth axis was added, and once when the install row moved nine cells. None of the three eliminates anything, and all three candidates still hold a strictly best score somewhere.

THE THIRD RECOMPUTATION IS THE ONE THAT COULD HAVE BROKEN IT. Nine of thirty-six cells changed, including one line gaining three points across two axes. The front held anyway, which is stronger evidence than the first two together.

## round_0_verify

- evidence vs claims: SIX ITEMS ARE COMPUTED AND ALL PASS. viable_set is three against a floor of two. complete_allocation holds, every candidate carrying one pick per row across all eight rows. criteria_traced holds, every axis a standing should-row that resolves. band_recorded holds, one cutoff on req-query-is-deterministic. front_recorded holds, returned by the arithmetic with both corners and typed by nobody. feasibility_checked passes at run-candidates, each candidate carrying make-reuse-buy, failure modes ordered by how badly they fail, and what is testable, two of them with measured numbers rather than estimates. cuts_reasoned PASSES MECHANICALLY AND FAILS ON INSPECTION, which is the one real result here: every cut carried a reason and two of the reasons were false. The second-hand rule also passes, and doubled by accident, with two clean-context subagents producing tables independently and the composer typing no score.
- types: not run, and no type could have moved. This milestone changed no product code. What changed is spec nodes and evidence, and the boot checks that read those ran green at the last reload.
- lint: not run. The lint verb is not among this state's legal tools, and the prose it would check belongs to the states that own that text.
- tests: none run, correctly. No code changed, so a run could only reassure, and the method forbids running to reassure. The measured numbers this gate relies on came from probes at find_by_probing, which are recorded with what they faked.

## round_1_validate

- exercised against the goal: THE DESIGN SPACE WAS EXERCISED AGAINST SIX OF SEVEN BLESSED GOALS and one is untouched by anything on the chart. The collapse, the machine-state folder's home, the entry point, the lane's exclusion split and the one corpus reader all have rows with competing cells, and every candidate answers each. Move-first-clean-second is a sequencing instruction rather than a design axis and needed no cell. Branch independence was exercised by a probe rather than by the chart, and the probe settled it: one machine-state folder per checkout before and after the collapse, so the test the vision asks for is a regression pin rather than a fix.
- missing: THE INSTALL ACT WAS MISSING AND IS NOW ON THE CHART, on the owner's ruling that asking might differentiate. It did. Three option nodes answering it already stood in the corpus from i1 and i28 with no chart row, so the cells existed and the row did not. Each candidate took a different one and a fresh second hand re-scored the three install axes: nine cells moved, the axes below solid baseline went from four to ONE, and a flat axis stopped being flat. raid-iss-the-chart-has-no-cell-for-standing-the-product-up is closed with what happened. WHAT IS STILL MISSING is one axis where nothing reaches baseline, a surface answering in one second, and one cell nobody visits, opt-one-binary-published-through-every-channel, which arrived from a scan after every line had picked. Also missing: the number that decides whether one whole line is viable, which is what a ref or commit write costs on the walk's hot path. Nobody has it and the candidate that rests on it says so.
- wrong: TWO CUT REASONS, both corrected here before the verdict was written. req-setup-stops-before-partial was struck as inherited identically and is not, because one candidate adds a required tool. req-close-leaves-trunk-clean was struck as untouched and is not, because one candidate exists to hold engine writes to four named mutations. Also wrong and corrected upstream: the iteration's own vision justifies its gitignore plan with an analogy that does the opposite of what it claims.
- out of scope: MIGRATING AN EXISTING CHECKOUT stays excluded on the owner's ruling that a hand migration is acceptable, and that exclusion is argued rather than assumed. The nested-worktree gap in the lane's read exclusion is filed as a note and left alone, because this iteration changes what the machine-state entry MEANS rather than what else belongs in that set. The pareto nadir dropping any zero-valued axis is an engine defect found here and filed, not fixed.
- prior art, second scan for the install row: IT DISPROVED A CLAIM THREE OF OUR OWN NODES RESTED ON, which is what a scan is for. They said no comparable tool stops before a partial install and names what is missing. `mason.nvim`'s health check does exactly that in two hundred lines: every check run, each tool named with its version, required split from optional, each version constraint stated in words, the remediation command carried in its own source, and nothing changed. `flutter doctor` is the same shape and Replicated sells the commercial form where a strict preflight blocks the install. All three nodes are corrected. WHAT THE ORIGINAL SWEEP PROBABLY MEANT SURVIVES: no INSTALLER does it. Homebrew's own script aborts on architecture, operating system and permissions before touching anything, then creates and chowns its whole tree, and only THEN aborts on missing git. The good implementations are health checks rather than installers, which is a real finding about where to look. TWO MORE THINGS THE SCAN FOUND. Nobody picks one distribution channel: Claude Code itself publishes one binary through a piped installer, Homebrew, WinGet, signed apt, dnf and apk repositories, and npm, plus a read-only doctor. And the editor's extension host IS a Node process, so a TypeScript extension needs no runtime installed at all and the install problem exists only for a separate-process or native backend, which is a constraint on this whole row that nobody has checked against our own shape. SOURCES: https://raw.githubusercontent.com/mason-org/mason.nvim/main/lua/mason/health.lua and https://github.com/Homebrew/install/blob/master/install.sh and https://code.visualstudio.com/api/advanced-topics/extension-host and https://code.claude.com/docs/en/setup and https://docs.replicated.com/vendor/preflight-support-bundle-about
- prior art: SCANNED LIVE AT THIS GATE, three searches, not reached for from memory. WHAT THEY DO BETTER, FIRST. Cursor already ships the shape this iteration is inventing: `.cursor/` with the directory ignored except a committed `rules/` subdirectory, which is exactly the folder-tracked-contents-ignored arrangement, working, in a product with users, while we derive it from first principles. It also exposes a defect in our own vision, which justifies the plan by citing our gitignore line for the editor's folder, an arrangement that ignores one file and tracks the rest, the opposite of what is wanted. THE WHOLE NEIGHBOURHOOD USES PER-TOOL DENY FILES, with .cursorignore, .aiderignore, .geminiignore, .clineignore, .rooignore and the cross-tool .agentignore all taking gitignore syntax, and that is prior art AGAINST cand-nothing-can-be-forgotten's include list, because a deny file is a convention users already know and can edit while an include list is ours alone. THE VENDOR ALSO CORRECTS OUR FRAMING OF THE OPEN, because VS Code's documentation says onStartupFinished is like the wildcard event but will not slow down VS Code startup, while our probe called it the latest available event as though the vendor had chosen badly; both are true and they measure different clocks, the vendor protecting the editor's startup and our row measuring when our own desk is usable. WHERE OURS HOLDS, npm's `files` field is an allowlist that TAKES PRECEDENCE over .npmignore, and OWASP's NPM Security Cheat Sheet recommends it for exactly the reason our option node argues, that a denylist is error-prone and a forgotten entry publishes something private, which is a stronger named comparison than the one the second hand had and it does not change the score. SOURCES: https://code.visualstudio.com/api/references/activation-events and https://www.vibereference.com/devops-and-tools/ai-coding-tool-configs-git-guide and https://s-celles.github.io/ai-config/docs/agentignore/ and https://cheatsheetseries.owasp.org/cheatsheets/NPM_Security_Cheat_Sheet.html and https://docs.npmjs.com/cli/v11/configuring-npm/package-json/

## goals_served

- COLLAPSE THE WRAPPER. Everything at the repository root moves into the folder that gets opened, until the root holds the project and the repository's own plumbing and nothing else. A level above the project that carries product files is a level that should not exist.: SERVED AS A DESIGN SPACE, not yet as a move. The chart's root row carries three cells and every candidate answers it. req-product-is-a-folder from i1 already demands this and is violated today, so the milestone closes a standing violation rather than opening a new want.
- THE MACHINE-STATE FOLDER LEADS THAT MOVE, because it is the one whose home the rule settles first: it belongs to the thing being worked on rather than one level above it.: SERVED. It is the subject of a whole chart row, with three competing cells including removing the folder entirely. req-the-machine-state-sits-in-the-folder-that-is-open carries the demand.
- MOVE FIRST, CLEAN SECOND, AND STOP IN BETWEEN. Nothing is renamed and nothing is deleted while things are moving. Then the owner decides what should not have travelled at all.: NOTHING YET, AND NOTHING HERE OWES IT. This is a sequencing instruction for the build rather than a design axis, so it earns no cell and no candidate differs on it. M7 owns it.
- OPENING THE EDITOR IS THE ONLY ENTRY POINT AFTER THE FIRST INSTALL, for a folder that IS a project. The launcher runs once. From then on, opening a project gets it everything it needs, and anything that cannot apply to a running window says so instead of going quiet.: SERVED, and it is the richest part of the chart. Two rows cover it, what happens when a folder is opened and how the product's root is decided, with three cells each. req-the-editor-is-the-only-entry-point and req-the-desk-is-usable-soon-after-the-folder-opens carry the demands, and a new function was minted for the act.
- SPLIT THE LANE'S EXCLUSION BY FILE INSTEAD OF BY DIRECTORY, so the three files with a structured door stay hidden and everything else becomes readable.: SERVED. req-only-a-file-with-its-own-door-is-withheld carries it, and the chart's withholding row carries three cells. Two probes priced it: the producing acts already exclude by name at any depth, so the live risk narrows to version control rather than packaging.
- GIVE THE ENGINE ONE CORPUS READER, so what the corpus IS has a single answer rather than one per caller.: SERVED, AND SHARPENED BY A PROBE. req-what-the-corpus-is-has-one-answer carries it and the chart's agreement row carries three cells. Probe P4 found the exact disagreement by line: one reader drops a malformed node, a second keeps it blank, and a boot check asserts the second behaviour of both.
- PIN BRANCH INDEPENDENCE WITH A TEST. The folder resolves to one place while a record is bound, and that guarantee stops being trusted and starts being checked.: SERVED BY A PROBE RATHER THAN BY THE CHART, and the probe changed what the test is for. One machine-state folder per checkout before and after the collapse, measured on a controlled tree with the engine's own resolver. So the test is a regression pin rather than a fix for a hazard the collapse creates. M7 still owes writing it.

## bound_breaches

- if-agent-harness-to-entrypoint: NONE MEASURED, and none could have been. This milestone ran no product path; it read the corpus and wrote spec nodes. The one timing figure produced here is a probe rather than an interface measurement: a full corpus read at 359 ms over 1097 nodes, against a five-second open bound and a one-second call bound, both comfortable. The owner's separate report of a button press taking over a second stands as a note against a person-facing control, which is a different interface with a different bound, and it is not this gate's to dispose of.

## round_2_red_team

- STEELMAN, THE STRONGEST CASE AGAINST BLESSING THIS FRONT: the front is three because the criteria are too blunt to separate the lines, not because the lines are genuinely incomparable, and a gate that blesses an undiscriminating front has blessed nothing => REJECTED ON THE NUMBERS. A blunt criteria set shows a narrow box on every axis. This box is wide on three: what a produced copy carries runs 3 to 5, surviving a host change runs 2 to 4, and the open clock runs 2 to 4. Each line owns one of those outright. The set separates them where it matters and ties where the rows genuinely do not bear on the choice.
- THE CUT IS UNSOUND AND MOST OF IT IS UNREAD. Seventy-nine of ninety-nine cut reasons were judged from a row's name. Two of the ten sampled were wrong. Blessing a front chosen against a criteria set of unknown correctness is blessing an unfinished comparison => ANSWERED BY PROOF RATHER THAN BY REASSURANCE. Every one of the three candidates holds a STRICTLY BEST score on at least one axis: the repository line at 4 on host-swap against two 2s, the reference line at 4 on the desk row against two 2s, the loudness line at 5 on what a copy carries against a 4 and a 3. A candidate that is strictly best somewhere cannot be dominated, and ADDING an axis can never create domination. So no missing axis, however many are still wrongly cut, can shrink this front.
- THE MIRROR OF THAT ARGUMENT IS THE ONE THAT COULD KILL IT. Removing a wrongly INCLUDED axis can create domination, and the twelve were kept by the same judgment that got two cuts wrong => LOOKED FOR AND NOT FOUND. The three axes that carry the strictly-best scores are the three whose differentiation is least arguable. Host-swap: one line needs no host feature and two depend on two host features each. The desk row: one line moves activation off the after-everything-else event and two do not. What a copy carries: one line inverts the default and two keep a deny list. Removing any of those is not defensible, so the front holds.
- THE COMPOSER WROTE THE CANDIDATES, THE CRITERIA AND THE CUT, and only the scoring was handed to a second hand. The blind-ordering guard protects the SORT, and the failure landed in the CUT => CONCEDED, AND IT IS THE STRUCTURAL FINDING OF THIS GATE. The method spawns a clean context for scoring because the composer would score favourably. The same argument applies to cutting and no guard exists there. Filed as raid-iss-most-of-the-criteria-cut-was-judged-from-a-rows-name, and the retro should decide whether the cut wants a second hand too.
- THE CHART HAD NO ROW FOR STANDING THE PRODUCT UP, and three of the four below-baseline axes were that act. Blessing a front over a chart with a shaped hole in it is judging unfinished work => RAISED HERE, AND CLOSED HERE. This gate first argued that adding a row nobody has cells for would change nothing. THE OWNER OVERRULED THAT AND WAS RIGHT: nobody had answers because nobody was asked. Asked, three cells were already standing in the corpus from earlier iterations, each line took a different one, and nine re-scored cells took the below-baseline count from four to one. The finding was correct and this gate's own reason for deferring it was not.
- THE SAME MISTAKE COULD BE ANYWHERE ELSE ON THE CHART. If one unasked row was hiding three axes' worth of differentiation, the argument that any other unasked question is inert has just been shown unreliable => PARTLY ANSWERED, HONESTLY. The other rows were reached by finders; this one was not, which is why it was empty. But the general lesson holds and it is the second-order finding of this gate: an axis nothing scores well on is evidence of a missing question, not of a hard problem. That reading now has a worked example behind it.
- THE KILL-CRITERION, NAMED AND TESTED: blessing this front is the wrong call if any candidate would be eliminated once the criteria set is correct => IT CANNOT BE. The proof is two lines above. Every candidate is strictly best on an axis whose inclusion is beyond argument, so the front is stable under any correction to the cut. The gate is blessing a set that arithmetic says cannot shrink, which is exactly what set-based design asks a gate to protect.

## raid_additions

- project/spec/trace/raid/raid-iss-the-chart-has-no-cell-for-standing-the-product-up.md
- project/spec/trace/raid/raid-iss-most-of-the-criteria-cut-was-judged-from-a-rows-name.md
- project/spec/trace/raid/raid-asm-the-hosts-pattern-test-and-its-handover-name-the-same-folder.md

## verdict

pass with overrides — the front of three is stable under every correction this review could make to the criteria set, and that is provable rather than argued: each candidate is strictly best on an axis whose inclusion is beyond doubt, and adding axes never creates domination. THREE DISSENTS ARE LOGGED RATHER THAN WAIVED. First, the criteria cut is unsound: seventy-nine of ninety-nine reasons were judged from a row's name, and two of the ten this gate sampled were wrong. Both were restored before this verdict was written and one of them changed the axis count from eleven to twelve. Second, and NOW RESOLVED RATHER THAN CARRIED: the chart had no row for standing the product up. The owner overruled this gate's reasoning that filling it would change nothing, the row was added, three cells already in the corpus were placed, each line took a different one, and a fresh second hand re-scored nine cells. The axes with no candidate at solid baseline fell from four to one, and the front held through the recomputation. The dissent stands only as a record of the gate's own wrong argument. Third, the composer wrote the candidates, the criteria and the cut, and only the scoring was handed to a clean context, so the guard the method built for scoring does not cover the place the error actually landed. Each dissent has a register entry, and none of the three changes what this gate is asked to bless. THE FRONT IS BLESSED AND NO WINNER IS PICKED. All three lines stay alive into M5.

## follow_up

THE BLESS IS THE OWNER'S AND IS DELIBERATELY NOT TAKEN. This form is stamped and left unblessed, on their standing instruction to look at the gates.

THAT DECISION WAS TAKEN AND THE WORK IS DONE. The owner ruled to add the install row and judge again. It is added, judged, and the register entry is closed with what happened.

ONE ROW STILL HAS NOTHING AT BASELINE: a surface answering in one second, where the best any line reaches is 2. Nobody asked whether that is a missing question too, and this gate's own worked example says that is now the first thing to suspect.

ONE CELL ON THE CHART IS VISITED BY NOBODY. opt-one-binary-published-through-every-channel arrived from a scan after every line had picked, and re-picking a line to suit a late cell would be composing toward a score. It is the cell a fourth line would be built on.

WHAT M5 INHERITS: three composed lines, twelve axes in a settled order, a score table with a second table recorded beside it, and both corners drawn.

WHAT M6 INHERITS ALREADY: two spikes worth naming now. What a ref or commit write costs on the walk's hot path, which decides whether the repository line is viable at all. And whether the arrival's two-sided guarantee survives being folded into a path built for a present person, which two of the three lines depend on.

THREE REGISTER ENTRIES WERE ADDED HERE and each carries its own trigger, so none of them needs remembering.

THREE NOTES WERE FILED DURING THIS MILESTONE AND NONE IS THIS GATE'S TO RULE ON: the chart field deleting candidate nodes from closed iterations, the lane's read exclusion not hiding a nested worktree, and the pareto nadir dropping any axis whose worst score is zero.

## anything_else

