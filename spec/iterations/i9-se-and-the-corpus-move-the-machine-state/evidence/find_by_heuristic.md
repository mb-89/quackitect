---
form: find_by_heuristic
by: agent
signed_off: 2026-08-19T15:19:07.861Z
authors: agent
files: null
---

# Evidence form / find_by_heuristic

## current_situation

Eight rules against eight clusters, sixty-four cells, and the misses are written down.

FOUR RULES BIT AND MINTED SOMETHING. Four bit and minted nothing, each for a reason on its row. Nothing was skipped.

THREE OF THE FOUR OPTIONS LAND ON THE BOOTSTRAP CLUSTER, which is where this delta's weight sits. The fourth lands on the walk.

THE SWEEP ALSO FOUND A DEFECT IN THE PARTITION, not an option. The bootstrap cluster gained a sixth member on 2026-08-19 and its node still argued for five. The node is repaired and the seam the sixth member opened is now written in it.

TWO RULES DISAGREE ABOUT THE SAME PAIR OF CLUSTERS, which is the most useful thing here. Grouping what changes together says merge the arrival into the bootstrap. Small interfaces says keep them apart. Both arguments are recorded on the option itself, so whoever decides pays a named price either way.

## applies

yes

## sweep

| heuristic | cluster | what_it_suggests |
| --- | --- | --- |
| Group what changes together; separate what changes apart. | cluster-the-account | BITES. What the system writes down and what it draws for a person now change apart, because after the collapse the written half is plain files in the folder somebody has open. This confirms the trim already minted as opt-the-editor-shows-what-the-folder-already-says rather than adding a partition of its own. |
| Group what changes together; separate what changes apart. | cluster-the-arrival | BITES, and it points at the bootstrap. Both end at a lane that answers, and this delta made that literal by giving one an output the other consumes. The trimming finder reached the same place by a different road. |
| Group what changes together; separate what changes apart. | cluster-the-bootstrap | BITES HARDEST, AND IT MINTED AN OPTION. Five members change when the shape of a tree changes. The sixth changes when the entry point changes, and this iteration proved it by moving that one and none of the others. |
| Group what changes together; separate what changes apart. | cluster-the-disposition | nothing. Two functions in sequence, and both change when a coupling judgment changes. |
| Group what changes together; separate what changes apart. | cluster-the-holding-pen | nothing. Three functions bound by one policy, and this delta touches nothing about what a note may carry. |
| Group what changes together; separate what changes apart. | cluster-the-query | nothing. One function cannot be separated from itself. |
| Group what changes together; separate what changes apart. | cluster-the-record-life | nothing. The collapse changes where records live and not when they change. |
| Group what changes together; separate what changes apart. | cluster-the-walk | SUGGESTED AND LOST. resolve-a-path is the only member the collapse touches, so the rule proposes separating it. Rejected on the matrix: the walk carries three flows and the only cycle in the whole DSM, and resolve-a-path feeds nearly everything. |
| Make the common case cheap; make the rare case possible. | cluster-the-account | BITES, AND THE ANSWER ALREADY STANDS. raid-asm-the-slow-phase-is-the-green-derivation-repeated records that the surface re-derives every state per render when at most one moved. Minting an option here would re-decide something already probed. |
| Make the common case cheap; make the rare case possible. | cluster-the-arrival | nothing. It runs once per machine, so there is no common case to make cheap. |
| Make the common case cheap; make the rare case possible. | cluster-the-bootstrap | BITES, AND IT MINTED AN OPTION. Installing happens once and opening happens every day. This delta already split those into two functions, and the rule says pay for the open with one existence check. |
| Make the common case cheap; make the rare case possible. | cluster-the-disposition | nothing. It runs when somebody asks it to, and never on a hot path. |
| Make the common case cheap; make the rare case possible. | cluster-the-holding-pen | nothing. Capturing a stray is already one call. |
| Make the common case cheap; make the rare case possible. | cluster-the-query | BITES WEAKLY, and the option already exists. One reader answering from a stamped cache is opt-one-reader-answering-from-a-stamped-cache, minted by another finder. This confirms it. |
| Make the common case cheap; make the rare case possible. | cluster-the-record-life | nothing. Opening and closing a record are both rare by nature. |
| Make the common case cheap; make the rare case possible. | cluster-the-walk | CONFIRMS WHAT IS BUILT. serve-a-step already walks the happy path to the next branching point in one call, which is this rule applied before the rule was written down here. |
| One source of truth; everything else derives. | cluster-the-account | BITES, and the trim option carries it. After the collapse the folder is the truth and the drawn surface derives from it. |
| One source of truth; everything else derives. | cluster-the-arrival | nothing. No member holds a second copy of anything. |
| One source of truth; everything else derives. | cluster-the-bootstrap | BITES. Packaging, the launcher and the editor each keep their own answer about the machine-state folder. The option is filed against the walk, because path resolution is where the one answer belongs. |
| One source of truth; everything else derives. | cluster-the-disposition | nothing. |
| One source of truth; everything else derives. | cluster-the-holding-pen | nothing. A stray gets exactly one home by construction, which is this rule already enforced. |
| One source of truth; everything else derives. | cluster-the-query | BITES, AND IT IS A BLESSED GOAL OF THIS ITERATION. req-what-the-corpus-is-has-one-answer is this rule with the corpus as its subject. The row already stands, so nothing new is minted. |
| One source of truth; everything else derives. | cluster-the-record-life | nothing. The archive is already the single copy of what a closed record said. |
| One source of truth; everything else derives. | cluster-the-walk | BITES, AND IT MINTED AN OPTION. resolve-a-path is where the answer about the machine-state folder belongs, and three other mechanisms currently keep their own copy. |
| Push decisions to the last responsible moment. | cluster-the-account | nothing. |
| Push decisions to the last responsible moment. | cluster-the-arrival | SUGGESTED AND LOST. It says defer judging the runtime and supplying dependencies until something needs them. Rejected on the cluster's own reason: nobody is present to fix a failure later, so early IS the responsible moment here. |
| Push decisions to the last responsible moment. | cluster-the-bootstrap | BITES, AND IT IS ALREADY DECIDED. Standing a product up used to decide at install time whether the walk stops at a desk. The consent row moved that decision to the folder, judged on every open. An option here would re-open a standing requirement. |
| Push decisions to the last responsible moment. | cluster-the-disposition | nothing. Ranking then recording is already the latest either can happen. |
| Push decisions to the last responsible moment. | cluster-the-holding-pen | CONFIRMS. Holding a stray until it has a home is this rule, and diverge-before-deciding is it again on options. |
| Push decisions to the last responsible moment. | cluster-the-query | nothing. |
| Push decisions to the last responsible moment. | cluster-the-record-life | CONFIRMS. offer-what-may-be-taken-up puts standing options in front of whoever decides rather than choosing for them. |
| Push decisions to the last responsible moment. | cluster-the-walk | nothing. A step is served when it is reached, which is already the last moment. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-account | nothing. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-arrival | BITES AND IS OUT OF REACH. place-the-cage writes a deny list, and the arrival's worst failure is an agent believing it is caged when it is not. Making that unrepresentable is a question about the host, which no design in this iteration can settle. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-bootstrap | BITES, AND IT MINTED AN OPTION. The packaging exclusion is a check today. Building from an include list makes an unnamed file unable to ship at all. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-disposition | nothing. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-holding-pen | CONFIRMS. A minted statement sharing a six-word run with its raw note is refused when it is written, not audited afterwards. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-query | nothing. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-record-life | BITES WEAKLY AND IS NOT THIS DELTA'S. Landing work whole or not at all is enforced by a check today. Nothing in this iteration touches it. |
| Make the illegal unrepresentable, not merely checked. | cluster-the-walk | BITES AND IS OUT OF REACH. guard-a-write checks what a write carries. Making a bad write unrepresentable is a type-system answer, and a far bigger question than this iteration. |
| Small interfaces between big parts beat the reverse. | cluster-the-account | nothing new. Its surface is already one drawn view over a store. |
| Small interfaces between big parts beat the reverse. | cluster-the-arrival | BITES AGAINST A STANDING OPTION. Seven functions meet six over two flows, which is already the shape this rule wants. Merging them destroys the boundary. Recorded on opt-the-arrival-is-the-bootstrap-with-nobody-present rather than minted separately. |
| Small interfaces between big parts beat the reverse. | cluster-the-bootstrap | THE SAME CELL FROM THE OTHER SIDE. One cross-cluster edge leaves this cluster, from bring-forth-a-project to resolve-a-path, and it is still the only one after the rebuild. That is a small interface worth keeping. |
| Small interfaces between big parts beat the reverse. | cluster-the-disposition | nothing. Two functions is not a big part. |
| Small interfaces between big parts beat the reverse. | cluster-the-holding-pen | nothing. |
| Small interfaces between big parts beat the reverse. | cluster-the-query | CONFIRMS THE SINGLETON. One function on its own external interface is this rule's ideal rather than an embarrassment, and the trimming finder reached the same verdict by a different road. |
| Small interfaces between big parts beat the reverse. | cluster-the-record-life | nothing. |
| Small interfaces between big parts beat the reverse. | cluster-the-walk | SUGGESTED AND LOST. Six functions, three flows and the matrix's only cycle is a big interface inside a big part. Narrowing it is real work, and nothing in this delta touches it. |
| If it must be remembered, it must be recorded. | cluster-the-account | CONFIRMS. keep-the-record is this rule written as a function. |
| If it must be remembered, it must be recorded. | cluster-the-arrival | CONFIRMS. account-for-the-arrival reports what each step did, which is this rule for a run nobody watched. |
| If it must be remembered, it must be recorded. | cluster-the-bootstrap | BITES AND IS ALREADY SATISFIED. What must be remembered per machine is that the install happened, because once counts machines and not projects. The record is the installed runtime and extension themselves rather than a file we wrote. Named here so nobody mints one. |
| If it must be remembered, it must be recorded. | cluster-the-disposition | CONFIRMS. record-a-coupling-disposition exists for exactly this reason. |
| If it must be remembered, it must be recorded. | cluster-the-holding-pen | CONFIRMS. A stray is remembered by being written down, and that is the whole cluster. |
| If it must be remembered, it must be recorded. | cluster-the-query | nothing. It reads and remembers nothing. |
| If it must be remembered, it must be recorded. | cluster-the-record-life | CONFIRMS. keep-the-archive holds every closed record exactly as it stood. |
| If it must be remembered, it must be recorded. | cluster-the-walk | CONFIRMS. hold-the-method compiles the authored method into the machine and keeps the two in agreement, which is this rule about the machine itself. |
| The default should be the safe thing. | cluster-the-account | nothing. Showing something is not a dangerous default. |
| The default should be the safe thing. | cluster-the-arrival | CONFIRMS. A failed step is reported without ending the session, so the default is to say what broke rather than to die quietly. |
| The default should be the safe thing. | cluster-the-bootstrap | CONFIRMS, AND IT IS THIS ITERATION'S OWN RULING. A folder with no machine state is told so and left untouched. Nothing is seeded and no byte is written. |
| The default should be the safe thing. | cluster-the-disposition | nothing. |
| The default should be the safe thing. | cluster-the-holding-pen | nothing. |
| The default should be the safe thing. | cluster-the-query | BITES, AND IT NAMES A GAP THE DESIGN MUST CLOSE. What the reader does with a malformed node is a default, and the probe found two functions of the same name disagreeing about it. req-what-the-corpus-is-has-one-answer demands that every caller agree, and deliberately does not say which answer is the safe one. That choice belongs to design and this row is its input. |
| The default should be the safe thing. | cluster-the-record-life | CONFIRMS. land-the-work is whole or not at all. |
| The default should be the safe thing. | cluster-the-walk | CONFIRMS. guard-a-write refuses by default and lands only what passed. |

## options

- project/spec/trace/option/opt-split-the-bootstrap-by-whether-the-tree-is-the-one-you-stand-in.md
- project/spec/trace/option/opt-the-open-pays-for-one-check-and-defers-the-rest.md
- project/spec/trace/option/opt-one-declaration-of-what-the-machine-state-folder-is.md
- project/spec/trace/option/opt-package-from-an-include-list-so-an-unlisted-file-cannot-ship.md

## follow_up

THE SWEEP FOUND A DEFECT AS WELL AS OPTIONS, and it is repaired rather than reported.

cluster-the-bootstrap GAINED A SIXTH MEMBER at partition-functions on 2026-08-19 and its node was not brought with it. The node said five, named five, and argued that all five share both halves of the binding. It now names six, and carries a section saying plainly that the sixth shares the hazard and not the unit.

ONE CELL IS AN INPUT TO A DESIGN DECISION RATHER THAN AN OPTION. What the corpus reader does with a malformed node is a default nobody has chosen. The requirement demands that every caller agree and stops there, on purpose, because naming the answer would freeze a mechanism. Design has to pick it, and the probe that found two functions disagreeing is the evidence it should pick from.

TWO RULES DISAGREE ABOUT THE ARRIVAL AND THE BOOTSTRAP. Grouping says merge them. Small interfaces says the two-flow boundary between them is already right. Both arguments are now written on the merge option itself, so the decision is made with the price named rather than discovered afterwards.

TWO BITES WERE RECORDED AND LEFT ALONE BECAUSE THEY ARE BIGGER THAN THIS ITERATION. Making a bad write unrepresentable instead of checked, and making a broken cage unrepresentable instead of announced. Neither is reachable from a delta about where a folder sits, and both are worth somebody's attention later.

## anything_else

