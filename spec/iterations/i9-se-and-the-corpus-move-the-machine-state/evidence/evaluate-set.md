---
form: evaluate-set
amended: 2026-08-19T19:08:15.577Z by agent — a spike restored a thirteenth axis above the line, and a second spike changed the mechanism behind the fatal-axis score
by: agent
signed_off: 2026-08-19T16:08:59.552Z
authors: agent
files: null
---

# Evidence form / evaluate-set

## current_situation

THREE COMPOSED CANDIDATES MEET TWELVE AXES. Thirty-six cells.

THE COUNT READ ELEVEN AND THIRTY-THREE UNTIL 2026-08-19. The install row was added after this form signed, on the owner's ruling that nobody had answers because nobody had been asked. The amendment note recorded the nine moved cells; this sentence did not follow it.

THE AXES WERE FIXED BEFORE ANY CANDIDATE WAS READ. cut-criteria struck 99 of 115 rows as non-differentiating, each with its own reason, and drew the line at eleven on a damage boundary. Five differentiating rows fell below it by position.

THE SECOND HAND IS SPAWNED RATHER THAN HOPED FOR. A subagent with a clean context fills the score table. It receives the three candidate records, the eleven axes, the anchors and the prior-art comparison from the M2 gate. It receives nothing of the composer's reasoning, and it is told not to rank, not to total, and not to name a winner.

ITS FIRST BRIEF CARRIED THE WRONG SCALE and was corrected before it finished. The anchors it now works to are the engine's own: 0 absent through 5 beyond prior art, with 4 and 5 requiring a NAMED external comparison. A scale invented by the walking agent would have been the same breach the spawn rule exists to prevent, arriving through the back door.

NO SCORE IS TYPED HERE BY THE HAND THAT COMPOSED. The returned table lands verbatim, and any disagreement is recorded beside a score rather than over it.

## scores

| candidate | axis | score | anchor | prior_art |
| --- | --- | --- | --- | --- |
| cand-the-folder-answers-for-itself | req-a-wrong-act-never-passes-silently | 2 | Partial. The loud paths hold and the packaging test is scriptable, but a silent mode stands and the line records it: "The two host mechanisms disagree about which folder. Silent and the worst of the three, because the lane comes up rooted somewhere nobody chose", with the assumption left "Unprobed" | none |
| cand-the-folder-answers-for-itself | req-resume-needs-no-person | 3 | Solid baseline. The computed half stays drawn ("where the walk stands, which states are green, what is walkable this minute") and the hole is recorded: "A PERSON HAS AN EDITOR OPEN. The drawn-surface cell hands the stored half to it, which is no answer at all on an unattended machine" | none |
| cand-the-folder-answers-for-itself | req-walk-survives-host-swap | 2 | Partial. Both the way in and the root come from one host's features, recorded as "THE HOST IMPLEMENTS A CONTENT TEST AND RUNS IT EARLY. Both halves are the host's to change, and neither is ours"; the picked root option adds that a terminal "needs a second answer for that case or it has no answer at all", and the line supplies none | none |
| cand-the-folder-answers-for-itself | req-fresh-machine-runs | 3 | Solid baseline. "ONE COMMAND AND NO WIZARD. Nothing stands between a bare machine and a running product, and the command installs whatever it needs itself." Zero steps outside the entry is met by construction, and the holes are recorded: the command must be runnable on the target machine and the runtime it installs must be one the engine runs on, both carried as assumptions. The requirement's verify-and-report-ready half is covered by neither the design nor its option. | none |
| cand-the-folder-answers-for-itself | req-fresh-product-starts-empty | 4 | Prior-art par. The producing exclusion is measured, not asserted: "`travels` withholds the folder by name at any depth and `package.ts` shares that list", plus the asserting test "Package a tree and assert the folder is absent" | aider, which per gate-inputs "adds its own state files to the ignore list automatically rather than leaving it to the user" — same deny shape, and this line adds the absence test on top |
| cand-the-folder-answers-for-itself | req-setup-floor-editor-shell | 3 | Solid baseline. The same sentence carries the floor: "the command installs whatever it needs itself." The one dependency it cannot install for itself is named as an assumption rather than a fact. Requirement met, hole recorded, and no mechanism shown for how a machine holding only an editor and a shell runs that first command. | none |
| cand-the-folder-answers-for-itself | req-call-answers-in-one-second | 3 | Solid baseline. Measured under the bound — "A full corpus read is 359 ms over 1097 nodes, and the stamped repeat is 11 to 13 ms" — with the cost recorded by the pick: "A cache is a second thing that can be wrong" | none |
| cand-the-folder-answers-for-itself | req-the-desk-is-usable-soon-after-the-folder-opens | 2 | SETTLED 2026-08-19 at gate-architecture, by two hands scoring BOTH lines on this axis, one led and one blind. Both returned 2 and 2. The blind hand found a real difference in commitment here and still scored them equal, which is the stronger result: the two lines diverge on the root handover, on the reader, and on cluster shape, and NEITHER prices its own choice against the five-second bound. What holds both at partial is the requirement's second clause, which the requirement itself calls the half that matters most: anything not ready by then shall say so rather than appear absent. Neither document carries a mechanism, a cost, a failure mode or a test for it, and neither records the omission. The 4 also broke the rubric on its face, because a score above 3 needs a named external comparison and neither document names one on this axis. THE SUPERSEDED ANCHOR FOLLOWS. Prior-art par. It moves activation off the after-everything-else event, names the residual honestly ("The corpus is not what threatens the bound; the host's activation timing is"), and its cluster split "separates two clocks", giving the open path its own five-second bound | VS Code's workspaceContains activation event, which per gate-inputs starts an extension on a matching file — this line adopts the vendor's own earliest content-based mechanism rather than a substitute |
| cand-the-folder-answers-for-itself | req-surface-answers-in-one-second | 2 | Partial. Trimming the stored half leaves "Less surface to build and keep in step", but the line declines the clock claim — "THE CACHE IS NOT WHAT MEETS THE CLOCK ... not to make the open fast" — and never measures a surface request | none |
| cand-the-folder-answers-for-itself | req-what-the-corpus-is-has-one-answer | 3 | Solid baseline. Every question goes through one reader and the failure case comes with it ("One reader means one answer when a node is malformed too"), with the hole recorded: "The mechanism exists; what is missing is that the readers all use it" | none |
| cand-the-folder-answers-for-itself | req-query-is-deterministic | 3 | Solid baseline. "a stamp keyed on the corpus decides when that answer is still the answer", so an unchanged corpus is served the same answer; the risk is recorded as "a stale answer is worse than a slow one" | none |
| cand-nothing-can-be-forgotten | req-a-wrong-act-never-passes-silently | 3 | RESCORED 2026-08-19 AFTER A SPIKE CHANGED THE MECHANISM, AND THE SCORE DID NOT MOVE. The spike found the editor cannot state which folder it handed over, so the two-sided act this cell was composed on became a one-sided self-check that refuses on any carrier count but one. The requirement grades by failure MODE rather than by mechanism, and both shapes sit at the same rank; the two-sided version's own failure row already read "The acknowledgement rejects the handed-over tree. Loud, and nothing starts." ONE CLAUSE MOVED IN THE REWRITE'S FAVOUR, because the environment reads "any host, bound or unbound" and the two-sided act had a declared gap for a host that only launches a process with a working directory. WHAT HELD IT AT SOLID BASELINE rather than lower is that both its breaches have homes. WHAT THE RESCORE FOUND THAT NOBODY HAD WRITTEN DOWN is that the self-check is sound only while the system's test and the host's test stay the same test, and this design's four-item failure list does not name that drift. THE ORIGINAL ANCHOR FOLLOWS. Solid baseline. Two silent modes are converted to loud ones — "The acknowledgement rejects the handed-over tree. Loud, and nothing starts" and "A file nobody named is not in the package" — and the residual is recorded first in its own ordered list: "The merged arrival loses its second guarantee. SILENT" | none |
| cand-nothing-can-be-forgotten | req-resume-needs-no-person | 3 | Solid baseline. Same computed-versus-stored split, with the pick's cost recorded: "The reading loop and the notes both have a structured verb serving them precisely because a raw read loses ordering and paging" | none |
| cand-nothing-can-be-forgotten | req-walk-survives-host-swap | 2 | Partial. The root becomes host-agnostic ("The editor, the launcher or a hook STATES the root it is handing over"), but the way in does not, and the line records it: "THERE IS A WAY IN ON A MACHINE WITH NO EDITOR. The open row does not describe one, and the merged path needs it" | none |
| cand-nothing-can-be-forgotten | req-fresh-machine-runs | 2 | Partial. "A declared image arrives with the runtime and the product already in it, so there is no install to get half-right" works only where the option's own precondition already holds, and that option says the container runtime the bare shell targets may not exist, so it changes the precondition rather than meeting it. Obtaining a container runtime is a step outside the published entry. The hole is recorded on the design. | none |
| cand-nothing-can-be-forgotten | req-fresh-product-starts-empty | 5 | Beyond prior art. The default is inverted rather than maintained: "The package is assembled from a list of what belongs in it. A file nobody named is not in the package, whatever it is and wherever it sits" — nobody has to remember a new file, and the residual is recorded ("A future producing act does not consult the include list. Silent") | aider, which per gate-inputs adds its own state files to the ignore list automatically — still a deny list that must name each new file; an include list makes the unnamed file unshippable instead |
| cand-nothing-can-be-forgotten | req-setup-floor-editor-shell | 3 | SETTLED 2026-08-19 at gate-architecture, by two hands scoring BOTH lines on this axis. The graft replaced this line's declared image with the same one-command install the reference line takes, so the container runtime that put this cell at 1 is gone. One hand was told a separate reading had found the mechanisms identical. The other was told nothing and scored blind. Both returned 3 and 3. Solid baseline: the requirement is met in commitment, and the one residual assumption about the installed runtime is recorded in the register. Not prior-art par, because neither document offers a named installer as a benchmark for our own dependency-installation behaviour. TWO READINGS ARE SUPERSEDED AND KEPT BELOW. The graft's hand read 2. The original read 1, and its anchor follows. Gesture, and the design states the failure in its own words: "THE INSTALL CELL FAILS THE SETUP FLOOR OUTRIGHT... An image needs a container runtime, which is more than that, so this cell CHANGES the precondition rather than meeting it." The option adds that it serves the wrong person, since telling somebody to run a container to get an editor extension is worse than the script they have. Addressed at length, so not absent; no path exists at the floor, so nothing checkable meets it. | none |
| cand-nothing-can-be-forgotten | req-call-answers-in-one-second | 3 | Solid baseline. "A full read is 359 ms and a stamped repeat is 11 to 13 ms" sits under the bound, and the new dependency is recorded: "a form that wanted eleven nodes depend on all 1097 ... the dependency is real" | none |
| cand-nothing-can-be-forgotten | req-the-desk-is-usable-soon-after-the-folder-opens | 2 | Partial. It takes the same activation lever but pushes the arrival's expensive work into the open path — "One bring-up path runs, the same one that serves a machine with nobody at it" — and prices only the corpus against the bound, never the seven merged functions | none |
| cand-nothing-can-be-forgotten | req-surface-answers-in-one-second | 2 | Partial. It removes a per-form directory re-read ("The second reader re-reads its folder on every form; the canonical one is stamped and served from a pass-scoped map") but makes every form depend on 1097 nodes, and no surface request is timed | none |
| cand-nothing-can-be-forgotten | req-what-the-corpus-is-has-one-answer | 3 | Solid baseline, and the strongest cell on this line. "The second one is deleted, so a malformed node has one answer by construction"; capped at 3 only because no external system was named for comparison | none |
| cand-nothing-can-be-forgotten | req-query-is-deterministic | 3 | Solid baseline. One stamped reader answers every request — it "inherits the cache for free ... served from a pass-scoped map" — so an unchanged corpus yields the same rows | none |
| cand-the-repository-is-the-state | req-a-wrong-act-never-passes-silently | 2 | Partial. Absence is loud ("a loud refusal that names what it looked for"), but the picked root option records that walking up "lands the system in whatever repository happens to be above the caller", and the line adds no guard; its exclusion pick records "A NEW ACT ARRIVES WITH NO ENTRY" | none |
| cand-the-repository-is-the-state | req-resume-needs-no-person | 3 | Solid baseline. The panel keeps its job here — "everything else the surface must draw because it lives in refs a person cannot read by opening them" — with the cost recorded: "A person without a lane loses most of the readable walk. Quiet" | none |
| cand-the-repository-is-the-state | req-walk-survives-host-swap | 4 | Prior-art par. "walking up needs no editor and no host feature" and "Walking up works everywhere", with the position in branches and refs; the price is recorded as "Version control becomes a hard dependency" | git, npm, Cargo, Black, Ruff and uv, which per gate-inputs all walk up to a marker, and npm's own documentation gives the reason — the command still works from a subdirectory |
| cand-the-repository-is-the-state | req-fresh-machine-runs | 2 | Partial. "THERE IS NO INSTALLER. The product is a repository template, so standing one up is a clone and one dependency install, and both are things the person already has." Two steps, no published entry that installs and verifies, and no report-ready. The hole is recorded. Version control is a hard dependency here for unrelated reasons, so git is a third thing the fresh machine must already carry. | none |
| cand-the-repository-is-the-state | req-fresh-product-starts-empty | 3 | Solid baseline. The inherited surface shrinks to "Two files, declared per act", the reason is recorded ("a producing act copies the WORKING tree and neither file is committed"), and the pick records that a new act inherits no entry | none |
| cand-the-repository-is-the-state | req-setup-floor-editor-shell | 2 | Partial. Nothing installs further dependencies, because the installer is deleted. The design names the assumption directly: the language's own package manager must be present, which the floor of an editor and a shell does not promise. Its machine-state cell adds git to the same list. Clone plus install is concrete and works on an ordinary developer machine, which is the demo path, but that machine sits above the floor by two tools. | none |
| cand-the-repository-is-the-state | req-call-answers-in-one-second | 2 | Partial, with the hole recorded loudly and the measure missing. "THE WALK'S HOT PATH BECOMES VERSION CONTROL. Nothing here measures that, and no probe was run on it ... what a ref write costs on this machine is unknown, and it is the number this line most needs" | none |
| cand-the-repository-is-the-state | req-the-desk-is-usable-soon-after-the-folder-opens | 2 | Partial. The open is one check ("That is the whole check the open pays. Nothing else runs until something asks for it"), but the line also takes the merged arrival, whose own node says the arrival's "whole shape is doing the expensive things early ... This option pulls the other way", and that collision is left unresolved | none |
| cand-the-repository-is-the-state | req-surface-answers-in-one-second | 1 | Gesture. The trimming "DELIVERS LESS HERE THAN ELSEWHERE", so the surface draws more, over refs whose cost is unmeasured; no checkable claim is made about a surface request | none |
| cand-the-repository-is-the-state | req-what-the-corpus-is-has-one-answer | 2 | Partial. It "writes down what a malformed node IS and what every reader must do about it" and "LEAVES BOTH READERS STANDING", while the pick records that without an enforcing check "it is weaker than simply having one reader" — and no check is named | none |
| cand-the-repository-is-the-state | req-query-is-deterministic | 3 | Solid baseline. One named outcome is fixed in advance for the ambiguous case, and the corpus does not move ("compatible with the corpus living where it already lives, which this line does not move"); the missing enforcement is recorded | none |
| cand-the-folder-answers-for-itself | req-setup-stops-before-partial | 3 | Solid baseline, SETTLED 2026-08-19 by one hand scoring BOTH lines side by side on this axis. The graft left the two lines holding the same install cell, `opt-one-command-install`, so a two-point gap between them was an artefact of two scorers rather than a difference in mechanism. Both documents name the same model and the same five properties: every check run, each tool named with its version, required split from optional, the remediation carried in the source, and nothing changed. Both record the same debt. The settling hand found nothing in either document justifying a gap and held both at solid baseline. The hole is recorded, which clears partial. Building to a standard is not matching it, which is why neither reaches prior-art par. TWO READINGS ARE SUPERSEDED AND KEPT BELOW, because a score's history is part of it. THE SECOND READ, gesture, rested on the anchor being found to quote a sentence the candidate no longer contains. The requirement asks for a script that stops before changing anything and names each missing tool with where to get it. This line delivers nothing checkable against either clause, and says so itself: "THE STOP-BEFORE-PARTIAL CHECK IS STILL WORK TO MAKE". Its own testable list names an open with no machine state, a packaged tree, and a generated-consumer match. No setup preflight is among them. Naming mason.nvim raises the ceiling rather than the score, because the scale does not move for intent. THE SUPERSEDED ANCHOR IS KEPT BELOW, because a score's history is part of it: "THE STOP-BEFORE-PARTIAL CHECK IS OURS ALONE TO BUILD... this line commits to it without a model to copy." The commitment is costed as work to make and the absence of prior art is recorded, but no mechanism is described: nothing says what the preflight checks, that it stops before changing anything, or that it names each missing tool and where to get it. | none |
| cand-nothing-can-be-forgotten | req-setup-stops-before-partial | 3 | Solid baseline. "An image either built or it did not, so the half-installed machine stops being a state that can exist rather than a state something has to detect." The primary clause, stop before changing anything, is satisfied by construction and is checkable, and the drift hole is recorded. The second clause, name each missing tool and where to get it, is unaddressed: a machine with no container runtime gets no named remedy. | none |
| cand-nothing-can-be-forgotten | req-scaffold-from-template | 0 | Absent. ADDED 2026-08-19 as a thirteenth axis, after a spike found this row wrongly struck at rank 14, above the line. Scored by one hand holding all three designs on the axis at once. No sentence in this design concerns scaffolding a product folder. The one place it says what a copy contains is the package rather than a scaffold: "The package is assembled from a list of what belongs in it." Schema validity and declared fields are not mentioned. Absent is not the same as broken: the design leaves the scaffold exactly as it stands. | none |
| cand-the-folder-answers-for-itself | req-scaffold-from-template | 0 | Absent, and for the same reason as its neighbour. Its install section is about the machine rather than the product tree: "The install runs once and leaves an editor that knows about us." The words scaffold and template do not appear. The two leading designs tie here on behaviour and not merely on silence, because both answer the bootstrap cluster with the same cell and that cell changes nothing about beginning a product. | none |
| cand-the-repository-is-the-state | req-scaffold-from-template | 2 | Partial. The template clause is answered and the field clauses are not. "THERE IS NO INSTALLER. The product is a repository template, so standing one up is a clone and one dependency install, and both are things the person already has." A clone reproduces the template tree. The design never says how a cloned file becomes schema-valid for a specific new product, and never mentions declared fields, and that hole is unrecorded. The actor also changes: the requirement says the engine shall scaffold, and here the environment does it. | none |
| cand-the-repository-is-the-state | req-setup-stops-before-partial | 1 | Gesture. "THE STOP-BEFORE-PARTIAL CHECK SURVIVES THE TRIM AND IS STILL OWED... Deleting the installer removes everything around that check and leaves the check itself standing, unbuilt." Addressed, so not absent; nothing is delivered, nothing is checkable, and the trim removes the script the requirement's wording names as the thing that must stop. | none |

## front

- [[cand-the-folder-answers-for-itself]]
- [[cand-nothing-can-be-forgotten]]
- [[cand-the-repository-is-the-state]]

## reading

THE CHART GAINED AN INSTALL ROW AFTER THIS STATE SIGNED, AND NINE CELLS MOVED. The candidates gate found the chart had no row for how the product reaches a machine with nothing on it. Three option nodes answering that question already stood in the corpus from i1 and i28, unplaced. They were given the row, each candidate took a different one, each composed what that costs it, and a fresh second hand re-scored the three install axes.

THE FRONT STILL DOES NOT MOVE. Domination recomputed over all twelve axes eliminates nothing, and all three candidates still hold a strictly best score somewhere.

WHAT DID MOVE IS THE SHAPE OF THE WHOLE CHART, and it is the biggest change any correction has made here.

- FOUR AXES HAD NO CANDIDATE AT SOLID BASELINE. Now ONE does: a surface answering in one second, where the best is still 2.
- THE REFERENCE LINE GAINED MOST. Its install answer took the setup floor from 0 to 3 and a fresh machine from 2 to 3, and it is now strictly best on three axes rather than one.
- THE LOUDNESS LINE GAINED WHERE ITS LOGIC PREDICTED AND LOST WHERE ITS COST IS. Stopping before a partial install went 2 to 3, because an image cannot half-install. The setup floor stays at 1, because an image needs a container runtime and the floor is an editor and a shell.
- THE REPOSITORY LINE LOST ON THIS ROW. Stopping before a partial install went 2 to 1: deleting the installer removes everything around that check and leaves the check itself standing, unbuilt.
- ONE FLAT AXIS STOPPED BEING FLAT. A fresh machine running was 2, 2, 2 and is now 3, 2, 2.

THE INSTALL QUESTION WAS A DIFFERENTIATOR, and the reason it looked like it was not is that nobody had asked it. The owner said so before the scores came back.

A TWELFTH AXIS HAD ARRIVED EARLIER, and the front did not move then either. The candidates gate found that `req-setup-stops-before-partial` had been wrongly cut, restored it, and had a second hand score it: 0, 2 and 2. Domination was recomputed over all twelve axes and eliminates nothing. Every claim below still holds, and the one number that changed is in the next paragraph.

A FOURTH AXIS NOW SITS BELOW BASELINE AT UTOPIA. The restored row's best score is 2. So four of twelve axes have no candidate reaching solid baseline, and THREE OF THE FOUR ARE ABOUT STANDING THE PRODUCT UP: a fresh machine running, the editor-and-shell setup floor, and stopping before a partial install. That is no longer three scattered gaps. It is one shaped hole where the chart has no cell.

NOTHING WAS ELIMINATED, so there is no elimination to accept or refuse. No candidate is at least as good as another on all eleven axes. All three stand on the front, and the arithmetic says so rather than a judgment.

EACH IS BEST SOMEWHERE AND WORST SOMEWHERE, which is what a real front looks like. The folder line owns the open clock. The loudness line owns what a produced copy carries. The repository line owns surviving a change of host.

### Three axes where every line scored alike

THE ARITHMETIC NAMES THREE FLAT AXES: resuming without a person, a fresh machine running, and a deterministic query. Two readings are on offer and they differ per axis.

- RESUMING WITHOUT A PERSON, all three at 3. THE DECISION DOES NOT TURN ON IT. Every line takes the same drawn-surface cell, and that row has only one cell on the whole chart. It was flat before anybody scored it.
- A FRESH MACHINE RUNNING, all three at 2. A CRITERION IS NOT MISSING, THE OPTION SPACE IS. No line addresses installing and verifying through the published entry, because no finder proposed anything there. The chart has no cell for it.
- A DETERMINISTIC QUERY, all three at 3. The decision does not turn on it, and this one IS suspicious: the corpus row above it is not flat, and determinism was moved above it at cut-criteria on the argument that agreement is upstream of determinism. If agreement varies and determinism does not, the second row is measuring less than it looks.

### The front sits below baseline on three axes, and that is a result about the option space

UTOPIA IS NOT REACHABLE AND THAT IS NORMAL. What is not normal is where utopia itself falls short.

- A fresh machine running: the best any line reaches is 2.
- The setup floor of an editor and a shell: the best is 1.
- A surface answering in one second: the best is 2.

ON THOSE THREE, THE BEST AVAILABLE OPTION IS BELOW SOLID BASELINE. That says nothing about the candidates and everything about what was enumerated. The method is explicit that this sends work back to enumerate-space rather than forward to a choice.

THE BOX IS NARROW ON SIX AXES AND WIDE ON THREE. Where it is wide is where the decision actually lives: what a produced copy carries (3 to 5), surviving a host change (2 to 4), and the open clock (2 to 4).

### A second hand disagreed, and it is recorded beside rather than over

TWO SCORERS RAN. That was not designed. The first was spawned into the background and the second into the foreground when the walk could not wait, so an independent second opinion exists by accident.

THEY AGREE ON 20 CELLS OF 33 AND DIFFER ON 13. Ten differ by one point. Three differ by two: the loudness line on resuming without a person, the loudness line on what a produced copy carries, and the repository line on a deterministic query.

THE FRONT IS ROBUST TO EVERY ONE OF THOSE DIFFERENCES. Recomputing domination over the second table leaves the same three candidates on the front and eliminates nothing. So the disagreement changes no decision this state makes.

THE FLAT-AXIS FINDING IS NOT ROBUST, and that is the one thing the second table breaks. Under the second table NO axis is flat. So "three axes do not turn the decision" holds under one reading and not the other, and the gate should treat it as unsettled rather than as a finding.

THE SECOND TABLE IS IN anything_else. Nothing above it was rewritten to accommodate it.

## follow_up

THE GATE INHERITS A FRONT OF THREE AND NO ELIMINATIONS. Every line stays alive into M5, which is what set-based design is for.

THREE AXES SEND WORK BACKWARD RATHER THAN FORWARD. Nothing enumerated reaches solid baseline on a fresh machine running, on the editor-and-shell setup floor, or on a surface answering in one second. The honest response is a cell on the chart, not a better score. Whether that is worth reopening enumerate-space inside this iteration is the gate's to rule on.

ONE NUMBER DECIDES WHETHER A WHOLE LINE IS VIABLE and nobody has it. What a ref or commit write costs on the walk's hot path. The repository line rests on it and says so. If that line survives the gate it is the first thing M6 should spike.

ONE ROW MAY BE MEASURING LESS THAN IT LOOKS. Determinism came out flat while agreement about the corpus did not, and determinism was ranked above nothing at cut-criteria for exactly the opposite reason. Worth one look before M5 leans on it.

TWO SCORERS RAN WHERE THE METHOD ASKS FOR ONE, because the walking agent backgrounded the first when its own next act depended on it. That is a process defect rather than a scoring one, and the second table cost nothing and bought an independent check. Recorded here so the retro can decide whether the ensemble is worth doing on purpose.

## anything_else

### The second hand's table, recorded beside the landed scores

ONLY THE CELLS THAT DIFFER ARE LISTED. The other twenty agree exactly. `landed` is the table above; `second` is the independent scorer.

| candidate | axis | landed | second | what the second said |
| --- | --- | --- | --- | --- |
| cand-the-folder-answers-for-itself | req-resume-needs-no-person | 3 | 2 | nothing in the candidate covers the 60-second measure, evidence forms, or the pending-note count and age |
| cand-the-folder-answers-for-itself | req-fresh-machine-runs | 2 | 1 | no sentence covers install or self-verification on a fresh machine; the testable list covers waking, packaging and generator staleness only |
| cand-the-folder-answers-for-itself | req-fresh-product-starts-empty | 4 | 3 | measured and tested, but a generated consumer can drift silently, so it is a solid baseline with the hole recorded rather than prior-art par |
| cand-the-folder-answers-for-itself | req-the-desk-is-usable-soon-after-the-folder-opens | 4 | 3 | the second half of the measure, that anything not ready says it is starting, is nowhere in the candidate |
| cand-the-folder-answers-for-itself | req-what-the-corpus-is-has-one-answer | 3 | 2 | the malformed-node case, which the row calls the whole row, is never mentioned and no agreement test is named |
| cand-nothing-can-be-forgotten | req-resume-needs-no-person | 3 | 1 | the body never mentions the drawn surface, the panel, or a returning person; the only basis is a carried pick in frontmatter |
| cand-nothing-can-be-forgotten | req-walk-survives-host-swap | 2 | 3 | ownership comes from whoever starts it rather than from one host's handle, and the remaining gap is recorded on the node |
| cand-nothing-can-be-forgotten | req-fresh-product-starts-empty | 5 | 3 | the include list holds, but the candidate names no external system in its own text, so the naming rule caps it at 3 |
| cand-nothing-can-be-forgotten | req-setup-floor-editor-shell | 1 | 0 | the axis is not addressed at all; the make-reuse-buy list never mentions the setup script |
| cand-the-repository-is-the-state | req-resume-needs-no-person | 3 | 2 | the mappings the panel would render are listed as work to make, and nothing states the panel still answers |
| cand-the-repository-is-the-state | req-fresh-product-starts-empty | 3 | 2 | what a scaffolded product inherits from the source repository's refs is never raised, and that hole is unrecorded |
| cand-the-repository-is-the-state | req-the-desk-is-usable-soon-after-the-folder-opens | 2 | 3 | the open is cut to one check by construction, which is the row met with holes recorded |
| cand-the-repository-is-the-state | req-query-is-deterministic | 3 | 1 | nothing states that identical requests over an unchanged corpus return identical rows, and two readers remain standing |

### Where the two scorers took different views of the same rule

THE 2-VERSUS-3 LINE IS WHERE THEY PART. The second scorer capped at 2 wherever a candidate records a hole honestly but does not close it, reasoning that anchor 3 asks for the requirement to be MET. The landed scorer read a recorded hole as satisfying anchor 3's "holes recorded" clause. Both readings are defensible from the anchor text, and the anchor text is what should be sharpened.

THE NAMING RULE ALSO SPLIT THEM. The second scorer required the candidate's OWN text to name an external system before awarding above 3. The landed scorer allowed a name drawn from the gate's prior-art comparison. That is the difference behind three of the thirteen cells, and it is a question about the rule rather than about the candidates.

### One thing both scorers said unprompted

NEITHER FOUND ANY CANDIDATE ADDRESSING THE SETUP FLOOR. Both scored it at the bottom of the scale for all three lines, independently, and both said the axis is simply not addressed. That agreement is worth more than either score alone.
