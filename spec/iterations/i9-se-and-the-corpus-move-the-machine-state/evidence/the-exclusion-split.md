---
form: the-exclusion-split
by: agent
signed_off: 2026-08-20T07:59:29.131Z
authors: agent
files: null
---

# Evidence form / the-exclusion-split

## current_situation

THE LANE HID A FOLDER, NOT A FILE. `EXCLUDED_DIRS` carried the machine-state folder's name, so nothing inside it listed. That is a proxy for the real rule, and a proxy that catches more than its rule is not the rule.

A CONTRADICTION HAD TO BE SETTLED FIRST, and it is recorded rather than quietly resolved. The vision names THREE files with doors and the reading is the third. The read verb's own description promises the opposite: reading that one path credits every document the way ahead demands.

BOTH HALVES ARE KEPT TRUE. The reading's door IS the read, so withholding it would close the door rather than guard it. Two files are withheld; the third is served, and `SERVED_DESPITE_A_VERB` in the declaration says why so nobody tidies it back in.

## built

THE SPLIT, IN THREE PLACES.

- `paths.ts` drops the folder from `EXCLUDED_DIRS`, so the folder lists like any other. A comment there points at the new rule rather than restating it.
- `paths.ts` gains `withheldDoor(rootRelative)`, which answers from the declaration and from nothing else.
- `files.ts` refuses a raw read of a door file at the top of the read verb, before the optional-read path, because a withheld file is withheld whether or not absence is forgiven.

A NEW CLAUSE, SE-C-144, and its section stands in guidance/refusals.md. The pairing rule says a clause is not done until it does, and the section carries the two withheld files, the served third with its reason, and the three edges.

THE REFUSAL NAMES THE VERB, which is the half that matters. A withheld read that only says no teaches that the folder is closed, and that belief is what this row exists to remove.

THREE EDGES ARE SERVED ON PURPOSE, and each is a way the folder rule leaked: a file in a subfolder, a name that merely extends a door's, and any file the declaration does not name. The match is the whole name and the depth is exactly one.

MEASURED. The five door cases were red before this chunk and are green after it. The withheld set is asserted equal to the declared set, so the two cannot drift quietly. Preflight is green and now prints the stray count every run.

## follow_up

### What a reader can now do that they could not

EVERYTHING IN THE FOLDER LISTS. The call log and the notes refuse a raw read and say which verb serves them. Everything else — the answers the lane spills, the session state, the roots file — reads like any other file.

### The contradiction is worth carrying forward

TWO SIGNED SOURCES DISAGREED and neither was wrong. The vision described the rule; the read verb's description described a mechanism built after it. The resolution kept both, and the declaration is where a later reader meets it.

IT WOULD HAVE BEEN EASY TO MISS. Withholding all three would have broken the reading credit, and the failure would have shown as a walk that could not prove its reading rather than as a wrong door list.

### The stray count is now visible at every boot

EIGHTEEN ENGINE LINES still spell the folder's name. Preflight prints the number and does not fail on it, because those are the move's work and its sibling has not run yet.

## anything_else

