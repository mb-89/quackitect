---
form: find_by_transforming
by: agent
signed_off: 2026-08-19T15:25:32.937Z
authors: agent
files: null
---

# Evidence form / find_by_transforming

## current_situation

Twelve operators against five subjects, sixty cells, and the misses are written down.

BOTH CATALOGUES RAN WHOLE ON EVERY SUBJECT. SCAMPER's seven letters and SIT's five patterns, each applied to each. Nothing was tried on some subjects and skipped on others.

THE SUBJECT LIST IS CHOSEN, AND THE RULE IS STATED SO A READER CAN CHECK IT. The form offers every cluster and every option in the corpus, which is about a hundred and sixty subjects. Five were taken.

- THE INCUMBENT, because this method is strong on redesign and that is what this delta is.
- cluster-the-bootstrap, because three of the four options the heuristics finder produced landed there.
- The three options this iteration minted that carry a blessed goal on their back: waking on what the folder contains, one reader with a stamped cache, and excluding at the packaging boundary.

WHAT WAS LEFT OUT, PLAINLY. The other twelve options minted in this iteration, and every option minted before it. Transforming an option nobody has adopted produces a variant of a variant, and the chart already carries more than the comparison can weigh.

THREE TRANSFORMATIONS PRODUCED SOMETHING NEW. Nine produced ideas that standing options already hold, and those cells name the option rather than minting a duplicate. The rest produced nothing.

## applies

yes

## sweep

| operator | subject | what_it_produced |
| --- | --- | --- |
| Substitute | the incumbent | The folder becomes one FILE holding all machine state, the shape a single-file database takes. RECORDED, NOT MINTED: it fights a blessed outcome of this iteration, which is that the machine state is readable in the editor as ordinary files. |
| Combine | the incumbent | Merge the machine state into version control itself, as refs and notes. Ground already held by opt-claim-as-git-note and opt-claim-as-ref-cas. |
| Adapt | the incumbent | Borrow what build tools do and name the folder something every tool already ignores. THE CHECK FAILED AND THE NEGATIVE RESULT IS KEPT: no such universal name exists. The M2 gate found the editor's shipped exclusion list names five specific patterns rather than a rule about dots. |
| Modify | the incumbent | Magnify the collapse until the engine itself sits inside every project. opt-the-vendored-engine-is-one-more-reference already holds that ground. |
| Put to other use | the incumbent | The folder, once readable in the editor, serves a person with no lane at all. That is already the story sty-walk-it-by-hand, and the reading file exists for exactly it. |
| Eliminate | the incumbent | MINTED. There is no machine-state folder, and the repository is the state. The honest form keeps one file outside version control, because a raw note may carry anything. |
| Reverse | the incumbent | Starting the product opens the folder, rather than opening the folder starting the product. That is the launcher this iteration is removing, so the operator produces the predecessor. |
| Subtraction | the incumbent | Remove waking on open entirely and let the product come up on the first call. LOSES AGAINST A STANDING MUST-ROW: req-the-editor-is-the-only-entry-point demands everything appear with no command from the person. |
| Task Unification | the incumbent | The call log takes the session state's job, so the folder holds one file fewer. Real and small, and it is a consequence of the Eliminate option rather than a rival to it. |
| Multiplication | the incumbent | One machine-state folder per worktree instead of one per machine. It names the exact hazard the collapse creates, and the Division row below carries the option. |
| Division | the incumbent | MINTED. Divide the machine state by what each part belongs to, the machine or a record, and resolve each by its own rule. |
| Attribute Dependency | the incumbent | Make the lane's exclusion vary with the caller rather than being one list. The blessed goal already splits it by file, and varying by caller adds a second axis nothing here needs. |
| Substitute | cluster-the-bootstrap | nothing. Swapping one member for another act does not change what binds the cluster. |
| Combine | cluster-the-bootstrap | Merge it with the arrival. Already opt-the-arrival-is-the-bootstrap-with-nobody-present. |
| Adapt | cluster-the-bootstrap | Borrow a package manager's lifecycle hooks, named at fixed moments and each free to fail loudly. It describes the members rather than the cluster, so nothing changes here. |
| Modify | cluster-the-bootstrap | nothing. |
| Put to other use | cluster-the-bootstrap | Its refuse-or-complete discipline put to use on every producing act in the system. That is a rule rather than a partition, so it is not an option at this milestone. |
| Eliminate | cluster-the-bootstrap | The trimming finder already asked and answered. It stays, because removing it removes the product. |
| Reverse | cluster-the-bootstrap | nothing. |
| Subtraction | cluster-the-bootstrap | Remove stand-up-a-product, its oldest member. The need does not disappear, because something still has to reach a bare computer. |
| Task Unification | cluster-the-bootstrap | nothing. |
| Multiplication | cluster-the-bootstrap | nothing. |
| Division | cluster-the-bootstrap | Split it by whether the act stands inside the tree it touches. The heuristics finder minted exactly this an hour earlier as opt-split-the-bootstrap-by-whether-the-tree-is-the-one-you-stand-in. |
| Attribute Dependency | cluster-the-bootstrap | nothing. |
| Substitute | opt-wake-on-what-the-folder-contains | Swap the host's pattern test for our own, run by a resident process. The function's own neutrality check already names a resident service as a live alternative. |
| Combine | opt-wake-on-what-the-folder-contains | Combine it with opt-the-open-pays-for-one-check-and-defers-the-rest. The host's pattern test IS the cheap check, so the two compose rather than compete. |
| Adapt | opt-wake-on-what-the-folder-contains | nothing. |
| Modify | opt-wake-on-what-the-folder-contains | Minify the pattern until it matches one file rather than a folder. That is the predecessor's answer, named in this iteration's own inputs, and opt-walk-up-until-a-marker-is-found holds it. |
| Put to other use | opt-wake-on-what-the-folder-contains | The same declared pattern serves the editor's hide rule and the packaging exclusion. That is opt-one-declaration-of-what-the-machine-state-folder-is, minted by the heuristics finder. |
| Eliminate | opt-wake-on-what-the-folder-contains | Wake everywhere and decide in our own code. The owner ruled that acceptable, so the incumbent is already the null here. |
| Reverse | opt-wake-on-what-the-folder-contains | nothing. |
| Subtraction | opt-wake-on-what-the-folder-contains | Remove the pattern's dependence on the folder's NAME and wake on content instead. Nothing implements it, because a host that tests globs tests names. |
| Task Unification | opt-wake-on-what-the-folder-contains | nothing. |
| Multiplication | opt-wake-on-what-the-folder-contains | nothing. |
| Division | opt-wake-on-what-the-folder-contains | nothing. |
| Attribute Dependency | opt-wake-on-what-the-folder-contains | nothing. |
| Substitute | opt-one-reader-answering-from-a-stamped-cache | Swap the stamp for a file-system watch. opt-cache-corpus-read-invalidated-by-file-stat and opt-reactive-push-query-on-corpus-change already hold both variants. |
| Combine | opt-one-reader-answering-from-a-stamped-cache | nothing. |
| Adapt | opt-one-reader-answering-from-a-stamped-cache | The neighbouring domain's answer is a real database, and opt-embedded-relational-store-with-sql already stands. |
| Modify | opt-one-reader-answering-from-a-stamped-cache | nothing. |
| Put to other use | opt-one-reader-answering-from-a-stamped-cache | nothing. |
| Eliminate | opt-one-reader-answering-from-a-stamped-cache | Keep one reader and drop the cache. That is the cheaper sibling and opt-derive-every-view-on-every-look already carries it. |
| Reverse | opt-one-reader-answering-from-a-stamped-cache | Reverse who asks, so the reader pushes to callers. opt-reactive-push-query-on-corpus-change. |
| Subtraction | opt-one-reader-answering-from-a-stamped-cache | Remove the reader and keep only the rule that callers must agree, checked by a test. That is opt-define-the-ambiguous-case-rather-than-the-reader, minted by another finder in this same iteration. |
| Task Unification | opt-one-reader-answering-from-a-stamped-cache | The stamp takes a second job: noticing the corpus moved under a shared machine-state folder, which is what a branch switch does. It ties this option to the branch-independence goal and is a design note rather than a rival. |
| Multiplication | opt-one-reader-answering-from-a-stamped-cache | Several readers, one per node type. opt-shard-index-by-node-type. |
| Division | opt-one-reader-answering-from-a-stamped-cache | nothing. |
| Attribute Dependency | opt-one-reader-answering-from-a-stamped-cache | nothing. |
| Substitute | opt-exclude-at-the-packaging-boundary-and-prove-it | Swap the deny list for an include list. The heuristics finder minted that an hour earlier from a different rule, as opt-package-from-an-include-list-so-an-unlisted-file-cannot-ship. Two mechanical sweeps agreeing is weak evidence and not nothing. |
| Combine | opt-exclude-at-the-packaging-boundary-and-prove-it | Merge it with the lane's own exclusion so one list serves both. They answer different questions, what may be READ and what may be COPIED OUT, and merging would hide that. |
| Adapt | opt-exclude-at-the-packaging-boundary-and-prove-it | Borrow the ignore file version control already has. The vision commits to that half already. What it does not cover is a producing act that copies the working tree rather than the committed one, which is why this option exists. |
| Modify | opt-exclude-at-the-packaging-boundary-and-prove-it | nothing. |
| Put to other use | opt-exclude-at-the-packaging-boundary-and-prove-it | nothing. |
| Eliminate | opt-exclude-at-the-packaging-boundary-and-prove-it | Let the machine state ship. What leaks is the raw call log and the raw note file, and a standing rule keeps the note out of version control precisely because it may carry anything. The need does not disappear. |
| Reverse | opt-exclude-at-the-packaging-boundary-and-prove-it | nothing. |
| Subtraction | opt-exclude-at-the-packaging-boundary-and-prove-it | Remove the test and keep the rule. The option's own text says why not: the default flipped from safe to unsafe, so a rule with no test is one somebody must remember forever. |
| Task Unification | opt-exclude-at-the-packaging-boundary-and-prove-it | nothing. |
| Multiplication | opt-exclude-at-the-packaging-boundary-and-prove-it | Two folders, one that ships and one that never does, so the exclusion is geometry again. NOT MINTED: the vision already ignores the folder's contents in version control, so most of that geometry is there, and the leak comes specifically from acts that copy the working tree. |
| Division | opt-exclude-at-the-packaging-boundary-and-prove-it | nothing. |
| Attribute Dependency | opt-exclude-at-the-packaging-boundary-and-prove-it | MINTED. What is withheld varies with which act is producing the copy. The standing option already admits two paths need it separately, and this makes that the rule's shape rather than a testing burden. |

## options

- project/spec/trace/option/opt-there-is-no-machine-state-folder-the-repository-is-the-state.md
- project/spec/trace/option/opt-divide-the-machine-state-by-what-it-belongs-to.md
- project/spec/trace/option/opt-the-exclusion-is-declared-per-producing-act.md

## follow_up

THE NULL OPTION IS NOW ON THE CHART AND IT WAS MISSING. Removing the machine-state folder entirely is the one move a delta about where that folder sits most owes an answer to, and no finder before this one proposed it.

IT DID NOT SURVIVE INTACT, WHICH IS THE USEFUL PART. Exactly one file cannot go into version control, because a raw note may carry anything and a standing rule keeps it out. So the honest form of the null option is one file rather than none, and that number is worth carrying into the comparison.

TWO CATALOGUES REACHED ONE PLACE INDEPENDENTLY. SCAMPER's Substitute on the packaging exclusion produced an include list, which the heuristics finder had already minted an hour earlier from a different rule. Agreement between two mechanical sweeps is weak evidence and it is not nothing.

ONE CELL POINTS AT A QUESTION THE ITERATION HAS NOT SETTLED. Dividing the machine state by what it belongs to leaves the walk's own position unplaced. It is bound to a record and read by a machine, and this option does not say which side it falls on. That is a real gap in the option rather than in the sweep, and it is written into the node.

ONE TRANSFORMATION FAILED ITS CHECK AND THE NEGATIVE RESULT IS KEPT. Naming the folder something every tool already ignores does not work, because no such universal name exists. The M2 gate had already found the evidence: the editor's shipped exclusion list names five specific patterns rather than a rule about dots.

## anything_else

