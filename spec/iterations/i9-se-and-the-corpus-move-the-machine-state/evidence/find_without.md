---
form: find_without
by: agent
signed_off: 2026-08-19T15:08:31.877Z
authors: agent
files: null
---

# Evidence form / find_without

## current_situation

All eight clusters were asked, not only the two this delta touches, because the question is cheap and the interesting answer is never where you expect it.

TWO TRIMS ARE REAL AND BOTH WERE CREATED BY THIS DELTA. Neither was available before the collapse, which is the kind of second-order effect a trimming pass exists to catch.

THE BIGGEST ONE IS THE ARRIVAL CLUSTER, seven functions, and it is the biggest thing in this delta's reach. The method says to trim what is expensive rather than what is cheap, and this is the expensive one.

FIVE CLUSTERS SURVIVED THE QUESTION, which is the fourth outcome and worth as much as a removal: a cluster that survived can now say why it is there.

## applies

yes

## trims

| cluster | who_takes_over | what_breaks |
| --- | --- | --- |
| cluster-the-arrival | cluster-the-bootstrap | nothing prepares a checkout on a machine with nobody at it — but the bootstrap already ends at the same live lane, and after this delta it literally feeds it |
| cluster-the-account | the environment | the person's own editor already shows the stored half, now that it is plain files in the folder they opened. What the engine COMPUTES has no file, and no editor can show that |
| cluster-the-bootstrap | nobody | nothing takes a bare computer to a running product. This is the cluster the whole iteration is about |
| cluster-the-walk | nobody | no path resolves, no step is served, no write is guarded. Every other cluster reaches the disk through this one, so trimming it moves the work rather than removing it |
| cluster-the-query | nobody | every caller reads the corpus its own way again. That is exactly the failure this delta already suffered — two readers disagreeing about one malformed node |
| cluster-the-record-life | nobody | records open, run and close with nothing holding them. It is the vehicle the method is made of |
| cluster-the-holding-pen | cluster-the-record-life | notes would land straight in as trace nodes. A raw note may carry anything private, and trace nodes are committed — so this is a standing rule, not a preference |
| cluster-the-disposition | cluster-the-query | nothing ranks candidate couplings. The absorber is plausible, but nothing in this delta touches it, so judging it from here would be a verdict with no evidence |

## options

- project/spec/trace/option/opt-the-arrival-is-the-bootstrap-with-nobody-present.md
- project/spec/trace/option/opt-the-editor-shows-what-the-folder-already-says.md

## follow_up

THE ARRIVAL TRIM IS THE ONE TO ARGUE ABOUT, and it should be argued rather than adopted. Seven functions and a cluster boundary disappear, and the two paths stop being able to drift apart. What it risks is the arrival's own two-sided guarantee: either the agent stands on a caged lane, or it is told plainly what failed. That second half exists because nobody is watching, and folding it into a path built for a present person is where it would quietly go.

BOTH TRIMS CAME OUT OF THE SAME MOVE, which is worth noticing. Putting the machine-state folder inside the opened folder is what let the editor show it, and adding one function to the bootstrap cluster is what exposed its overlap with the arrival. Neither was reachable before this iteration.

THE FIVE SURVIVORS ARE BETTER JUSTIFIED THAN THEY WERE. The query cluster in particular: trimming it produces exactly the failure this delta already suffered, so its existence is now evidenced rather than assumed.

ONE CLUSTER WAS NOT JUDGED HONESTLY AND IS MARKED AS SUCH. The disposition cluster has a plausible absorber, and nothing in this delta touches it. Judging it from here would be a verdict on somebody else's work with no evidence.

## anything_else

