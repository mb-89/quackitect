---
id: i26-m6-design-realized-in
statement: design realized in code — every requirement has a realized design ; the DESIGNING happened at M4 (elements allocated, I/O wired) and binds the build through models-adhered-to above — this check confirms the build realized that design as `design:` markers in code; it never schedules designing after building
milestone: M6
class: executed
verify: coverage:designs-realized
killer: false
depends_on: [i26-m5-gate]
---
