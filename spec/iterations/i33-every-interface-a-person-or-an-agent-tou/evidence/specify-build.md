---
form: specify-build
by: agent
signed_off: 2026-08-17T16:58:16.113Z
reopened: 2026-08-17T16:57:42.549Z — author-tests re-signed above it with a sixth spec. The design below the line grew today under goal five, so the design specs are read against what actually landed rather than against what was planned.
amended: 2026-08-17T14:40:46.575Z by agent — author-tests re-signed above it; the chunks stand
authors: agent
files: null
---

# Evidence form / specify-build

## current_situation

THREE DESIGN SPECS AND ELEVEN CHUNKS, ALL ELEVEN STAMPED, re-read five times and unmoved.

dsp-the-outside-boundaries-and-their-bounds was added when specify-build's own law refused an interface no spec realizes — thirteen times, once per boundary. The second spec and seven of the chunks were added on the redo against the kickoff's goals (2026-08-17).

THE FIRST FOUR CHUNKS IMPLEMENT NONE OF THE FOUR MILESTONES, and that is where this iteration's drift is plainest. The scope named four milestones in a forced order. The plan below it built a legibility fix, a locked-rung message, an absent-versus-zero fix and a running-work signal. Every check passed, because a chunk is checked against its design spec and the spec against its element, and neither comparison looks at the kickoff.

SO THE MILESTONES NOW HAVE CHUNKS, including the two already built, named rather than quietly folded in. model-the-boundaries gates everything after it: both pass lines take a share over the set of modelled interfaces, and until those are nodes there is no denominator to take a share of.

GOAL FIVE'S WORK LANDED OUTSIDE THE CHUNK PLAN, and that is said here rather than folded in afterwards. Three engine changes were made today on owner rulings given mid-walk: the green guard counting door reads instead of milliseconds, the amend rule reversed so a correction no longer re-greys the chain, and a field another form reads becoming unamendable. None was a planned chunk and none went through the chunk machine. WHETHER THAT IS A DEFECT IS A REAL QUESTION for the retro: a ruling given mid-walk has to land somewhere, and the alternative is refusing the owner until the next kickoff. What is not in question is that it should be VISIBLE, which is what this paragraph is for.

WHY ONE DESIGN SPEC FOR THE DELTA AND NOT MORE. Only el-mirror is touched by the delta's own concern, and dsp-mirror-render already covers the surface whole. What this delta adds sits below that: the contract between the values a bank is handed and what the bank says about an act. Three recorded sightings all had a correct renderer fed a value it could not interpret, so the concern is the contract rather than the rendering.

NO SPIKE WAS RUN AND NONE WAS PROMOTED, so nothing enters as a pre-verified starting chunk.

## design_specs

- dsp-legible-controls
- dsp-the-goal-binds-the-walk
- dsp-the-outside-boundaries-and-their-bounds

## promotions

none — no expedition promoted work into this iteration, and no spike was run. The one probe scheduled for M6, raid-asm-the-slow-phase-is-the-green-derivation-repeated, is a measurement inside this iteration's own build rather than promoted work arriving from outside it, so it is a chunk below and not a promotion here.

## follow_up

THE CHUNK PLAN, four chunks, shaped by two lenses at once.

RISK FIRST decides what goes first. The phase-split probe is the riskiest thing in the iteration, because a concentrated result invalidates milestones three and four. It runs before anything is built on its answer.

PARALLEL FLOW decides the middle. Chunks two and three touch one file each and share no edge, so they fan out.

- CHUNK ONE, realization: measurement. Split the machine phase into its four parts and read the split against drawingSets and stateDetails over 90 percent and the SVG under 50 ms. Writes the result onto raid-asm-the-slow-phase-is-the-green-derivation-repeated. DEPENDS ON NOTHING, and everything downstream of milestone three depends on it.
- CHUNK TWO, realization: code. A locked rung names its unlocking notch, from the levels already handed in rather than a second copy of the list. Turns the third case of tsp-a-control-is-legible green. Touches params.ts only.
- CHUNK THREE, realization: code. A bank position becomes absent-or-a-number rather than defaulted at the boundary, and an absent bank says it does not know where it stands. Turns the fourth case green. Touches params.ts only, and it is a different function from chunk two.
- CHUNK FOUR, realization: code. A running operation joins the panel values and renders beside the controls. Turns both cases of tsp-work-past-its-bound-signals green. Touches params.ts and mirror.ts, and it DEPENDS ON CHUNK THREE because both change what the values contract carries — running them in parallel would put two edits on one boundary.

WHAT IS NOT A CHUNK. The wording of the running signal. That is the owner's through raid-risk-an-accurate-progress-signal-can-drive-abandonment, and chunk four settles where the signal lives rather than what it says.

## anything_else

