---
form: scope-non-goals
by: agent
signed_off: 2026-08-17T16:46:43.309Z
reopened: 2026-08-17T16:46:15.726Z — frame-delta re-signed above it, carrying the kickoff's five goals down. Scope is the state where a goal list most plausibly changes an answer, so it is read against the five rather than waved through.
amended: 2026-08-17T14:36:52.691Z by agent — the kickoff re-signed above it; the four milestones stand and are all built
authors: agent
files: null
---

# Evidence form / scope-non-goals

## current_situation

The delta has one need with two pass lines, both already measured failing, and it extends vp-rigor-without-toil rather than authoring a new prop.

THE STANDING PRODUCT SCOPE IS INHERITED. Only this delta is scoped here, and at vision level. The binding system-level exclusion list comes at draw-context in M2.

## scope

FOUR MILESTONES IN A FORCED ORDER, and the order is a finding rather than a preference.

ALL FOUR HAVE BUILD CHUNKS AND ALL FOUR ARE STAMPED, and the instrument built by milestone three now reads AT LEAST 181 breaches for milestone four to own. The figure is a floor: se_log_query drops matching records without saying so.
 model-the-boundaries produced thirteen interface nodes; bind-the-bound-per-interface gave each an argued bound; breaches-reach-the-reviewer made reading them a gate's job; fix-what-the-numbers-name followed the order the instrument gave rather than the order things were noticed.

THESE FOUR ARE NOW THE KICKOFF'S FIRST FOUR GOALS (owner ruling 2026-08-17), joined by a fifth standing goal, engine improvements. Every gate below carries a goals_served field that lists all five and asks what that milestone produced for each.

WHY THAT CHANGE EXISTS, AND THIS STATE IS THE EVIDENCE. This scope was signed naming four milestones. The walk below then wrote stories, use cases, requirements and build chunks for ONE of them. Seven coverage checks passed, because every one compares a node to its NEIGHBOUR and not one looks up at the scope.

ONE — MODEL THE OUTSIDE BOUNDARIES. One interface node per element-to-neighbour pair that carries traffic. The elements exist and the neighbours exist; the edges do not. This is what supplies the denominator both pass lines need.

TWO — BIND THE DEMAND TO THEM. Each modelled interface answers inside a second, or is transparent about not doing so. The transparency half is a requirement here, not a fallback, and the prior-art scan at the kickoff says to take a percentile rather than a flat threshold and to state what happens when the budget is spent.

THREE — INSTRUMENT PER INTERFACE, and make READING the instrument somebody's job in the machine. The capability already exists and is the lesson: a phase breakdown was recorded over a second, 324 times, for days, and no state read it.

FOUR — FIX WHAT THE NUMBERS NAME.

FIVE — ENGINE IMPROVEMENTS, the standing goal, and it now has substance of its own. Three things landed under it on 2026-08-17.

- The green guard stopped reading a clock and started counting reads through the file door, measured at 245 against a ceiling of 800. A guard that answered differently under machine load was not discriminating.
- An amendment stopped re-greying the chain below it, which is the owner's rule and was built backwards. req-an-amend-leaves-the-tree-standing carries it, with a test that signs a whole chain rather than one state.
- A field that another form READS became unamendable. The kickoff's goals list feeds every gate below, so changing it is a reopen and the refusal says so.

THE FIRST MOVE SITS BEFORE ALL FOUR AND IS FOUR LINES: split the machine phase into its parts. It is raid-asm-the-slow-phase-is-the-green-derivation-repeated, and it decides whether the DAG is the right instrument for three and four. Its first reading is in: the green derivation reads its input ONCE, so the cost is real and it is not repetition.

ALSO IN SCOPE, each naming a specific interface rather than a general complaint:

- The pull replaying a record instead of resuming it.
- The bless taking over a second on a path that does no work.
- The prompt-layer placement at about five seconds.
- The aim answer's reads list misleading about what is owed.
- The log serving a large response back cut, which is an issue rather than a risk and could close inside this iteration.
- The stop-at control declining a press and reporting nothing.

## non_goals

WHAT THIS ITERATION DELIBERATELY LEAVES, each with where it went.

- THE WALK'S OWN DEFECTS. The fix-findings fallback and the battery that never fires belong to i35, which is prepared for a cloud agent. They are urgent and they are not interface questions.
- ANYTHING THAT NEEDS A POSIX BOX. The container work cannot be verified on this machine, which is why it sits with i35.
- MAKING THE WHOLE SYSTEM FAST. Only interfaces a person or an agent TOUCHES are in. Internal work that nobody waits on is out, however slow, and that exclusion is what keeps four milestones honest at minor.
- SPLITTING THE SLOWEST TEST FILE. refs.test.ts dominates the battery at 151 seconds across 23 cases. Nobody waits on a battery interactively, and i5 owns it by name.
- A GENERAL PERFORMANCE PASS OVER THE ENGINE. i12 already did the instance-by-instance version and the rule still did not hold. Repeating it is the failure mode this iteration exists to avoid.
- REWRITING i12'S CRITERION. Its pass line stands and gains a denominator. Rewriting a standing criterion to match new work would let the work define its own test.
- THE PANEL'S OWED-CHECKBOX STATE. A live debt whose trigger is the owner reviewing the rendering and deciding the visual state. It is one read from being in scope and the owner has not been asked.
- WITHDRAWING THE LOG'S RE-FETCH PROMISE. The issue is in scope; choosing between serving the whole record and dropping the promise is a decision this iteration makes at M4, not a scope line here.

## follow_up

M1 is complete once gate-motivation rules. The three states below it are signed: draft-vision with three goal conflicts ruled, log-risks with four register entries opened, and frame-delta with the delta's need and its pass lines.

WHAT THE MOTIVATION GATE INHERITS AS ITS STRONGEST FACT: two pass lines on a must-priority value prop are already measured failing, and one of them failed twice inside this iteration's own onboarding.

WHAT IT INHERITS AS ITS WEAKEST: neither pass line can be COMPUTED yet, because the list they take a share over does not exist until milestone one builds it.

## anything_else

