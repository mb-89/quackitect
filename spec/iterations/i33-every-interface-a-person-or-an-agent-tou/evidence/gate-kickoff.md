---
form: gate-kickoff
amended: 2026-08-17T17:16:49.105Z by agent — This gate claimed no engine code had been written and that the battery need not re-run, both false at its own last amendment. The identical sentence stood in three gates; two were corrected on the re-sign pass and this one was missed. A fresh-eyes tester found it.
bless: blessed by agent
by: agent
signed_off: 2026-08-17T14:35:42.641Z
authors: agent
files: null
---

# Evidence form / gate-kickoff

## current_situation

i33 is entered and its onboarding retro is signed. The inbox stands at zero.

THE GOALS ARE FIVE, and the field above holds them as a list because every gate below now measures what it produced against each one. The first four are the milestones, in a forced order. The fifth is the standing goal, and it is where the seven pulled notes and this morning's dead stop-at control live.

RESTATED AS ONE SENTENCE: every interface a person or an agent touches answers inside a second, or says plainly that it will not - and the boundaries themselves get modelled first, because today none of them exist as nodes.

THE OWNER'S FRAMING, 2026-08-15: "get the performance under one second for everything that is user facing, and everything that is over one second needs to be non-intrusive. Every interface that goes to a human or to an agent needs to be that fast. Either that fast, or very transparent about how slow it is."

THE SECOND HALF IS NOT A CONSOLATION PRIZE. An interface that cannot be fast must be honest and non-intrusive, and must never leave a person guessing whether it is working. That is the half today's system fails hardest, and the stop-at control reported this morning is a live example of it.

WHY THE GOAL BECAME A LIST (owner ruling 2026-08-17). This form carried ONE prose goal, and pulled_in carried thirteen equal bullets where the four milestones sat as peers of seven notes and a dead button. Nothing marked which were the goals, and the walk below served one of them. The goals list and the goals_served round at every gate are the fix.

THE CHANGE SIZE REASONING IN FULL, because the field itself takes one line.

WHY NOT MAJOR. The matrix's escalate tell is a new function that fits no existing cluster, which is the architecture moving. Nothing here moves it. The trace ALREADY holds 40 interface nodes, so the node type exists and this iteration adds instances of it. The vision says so for milestone one in its own words: the elements exist, the neighbours exist, and what is missing is the edges.

WHY NOT PATCH. New requirements are written rather than merely satisfied. The transparency half of the demand does not exist as a requirement yet, and patch means no new requirements and therefore no new functions.

SO THE STRUCTURE EXTENDS RATHER THAN BEING RE-DERIVED, which is the minor column's own definition at derive-functions.

STRIKES NAMED. Under minor, gate-inputs folds into the requirements gate by the owner's ruling of 2026-08-13, so the user-level picture is judged once at M3 rather than twice. The engine compiles the rest of the column and pins it here.

THE COLUMN IS A PREDICTION AND MAY BE WRONG IN PUBLIC. If milestone one finds an element-to-neighbour interface cannot be expressed without a new node type, that is the architecture moving and the walk escalates visibly rather than quietly.

## retro_drained

- note-c213238ff7c3: obsolete — superseded by i33's own record, which now carries the whole stop-at finding rather than a note pointing at it
- the inbox: drained to zero at i33/onboard-retro on 2026-08-17, as 3 done, 2 obsolete, 10 carried and 5 backlog
- the backlog: all 77 parked items walked, 10 pulled into scope and 67 kept parked with their conditions still unmet
- the debts: all 8 swept and stamped looked 2026-08-17, with two triggers found already fired and one found unfireable as worded
- the field-feedback question: asked and answered, and it produced the ruling that dropped the bound-surface demonstration
- note-8c6983ee66a9 — A PROBE WHOSE INSTRUMENT GOES IN THE ENGINE CANN: carried — captured during i33's own walk, after this gate signed, so it belongs to the next retro's drain
- note-f2bc2e1e2fb8 — THE PULL IS THE WORST BREACH OF THE ONE-SECOND R: carried — same, and it names the pull as the worst standing breach of the one-second rule
- note-009a76da02aa — A THIRD AND STRONGER CANDIDATE FOR THE DEAD STOP: carried — same, a third candidate cause for the dead stop-at notch
- note-908e2e3b0dfc — A STATE MACHINE SHOULD SHOW ITS OWN FULL TITLE A: carried — same, the owner's ask that a state machine show its own full title
- note-b621c9986b74 — THE KICKOFF'S pulled_in FLATTENS MILESTONES AND : carried — same, and it is this gate's own finding about pulled_in flattening milestones and notes
- note-5aeda2a86ceb — UNSTICKING A GREYED CHAIN IS HAND WORK, AND THE : carried — the owner asked for one act that re-freshens a whole chain, and it is engine work rather than this iteration's
- note-29960c805dc0 — CONFORMANCE NEVER REACHES A NODE NO FORM POINTS : carried — proven this walk, and its blast radius is unmeasured, so turning it on is a scope call rather than a build step
- note-360be74ad2e0 — THE BREACH WINDOW RESETS ON THE VERY SUBMIT IT I: carried — half fixed. The row no longer carries a moving count, so the field is answerable; the window itself still reads the gate's current signature rather than its previous one
- note-2b1ccd564681 — OWNER RULING 2026-08-17: THE AGENT MAY SPAWN SUB: done — written into project/guidance/contract.md as rule 11 and projected to CLAUDE.md, AGENTS.md and the Copilot instructions
- note-11fdc65a1d43 — FIX NEEDED: THE CONTRACT MUST CARRY THE SUBAGENT: carried — the contract half is done; the session prompt half is outside this repo and returns every session until it is reconfigured, so it stays open

## goals

- model the outside boundaries
- bind the one-second demand to them
- instrument every interface
- fix what the numbers name
- engine improvements

## pulled_in

- milestone one, model the outside boundaries: one interface node per element-to-neighbour pair that carries traffic
- milestone two, bind the demand: answers inside a second, or is transparent about not doing so
- milestone three, instrument per interface, and make READING the instrument somebody's job in the machine
- milestone four, fix what the numbers name
- the first move, four lines: split the machine phase into its four parts, which decides the shape of three and four
- note-afb66f5e0dee, the pull replaying a record instead of resuming it
- note-5cebd22ef8f1 and note-ff8f4378deab, the bless taking over a second on a path that does no work
- note-801f54496c1f, the prompt-layer placement at about five seconds
- note-c8e5a398b943, streaming the walk the way a game streams a level
- note-7941a76b7f0f, the aim answer's reads list misleading about what is owed
- note-e31d7d3a0e08, se_log_query serving a large response back cut, confirmed live twice today
- the stop-at control's blockers-only notch staying dead from bless, now carried in the record itself

## left_out

- the fix-findings blocker and the battery that never fires: i35's blocks 1 and 2, prepared for a cloud agent
- the container and POSIX work: i35, and it cannot be verified on this machine at all
- the short-name rename: i35 block 6 or i10's big sweep, and not an interface question either way
- deleting the reading file and removing the handover: both owner rulings from 2026-08-16, both confirmed unbuilt today, and neither has an iteration
- splitting refs.test.ts, which dominates the battery at 151 seconds: i5 owns it by name
- the options pool: i17, and the owner settled today that the work token is that pool under another name
- the checklist panel's owed state: a live debt whose trigger is the owner reviewing the rendering, and they have not been asked
- whether the run mode setting is inert: raised by today's debt sweep, and it belongs to the code-review iteration

## change_size

minor — the trace already holds 40 interface nodes so the type exists and this adds instances, new requirements are written so it is not a patch, and the structure extends rather than being re-derived, which is the minor column's own definition at derive-functions

## round_0_verify

- evidence vs claims: the retro's form is signed and every claim in it was checked against the survey rather than assumed — 0 notes, 0 expeditions, 77 backlog
- types: RUN AND GREEN, and this line said "not affected, this iteration has written no engine code" long after it had. engine/trace.ts and engine/elematrix.ts changed at the bind-the-bound chunk, and render.ts and session.ts at the fix-what-the-numbers-name chunk, both before this gate's last amendment
- lint: se_lint is not legal at this gate, and the lints that matter ran anyway. The frontmatter guard refuses a parse break before anything lands, and biome ran clean over 274 files inside the battery below
- tests: RE-RUN AND GREEN — 1401 tests, 134 suites, 0 failures. This line read "not re-run, and nothing since has touched engine code" against a figure of 1385 from 2026-08-16. THE SAME FALSE SENTENCE STOOD IN THREE GATES. Two were corrected on the re-sign pass and this one was missed, which is how a fresh-eyes tester earns its keep: a defect fixed twice and left standing once reads, from inside, exactly like a defect fixed
- the latency baseline: measured fresh today rather than inherited — 8424 calls, 1834 over a second, all of them after i12 shipped the rule on 2026-08-15
- the prediction: NOT verified, and it must not read as if it were — whether drawingSets and stateDetails hold over 90 percent is unmeasured and is now a minted assumption with its probe

## round_1_validate

- exercised against the goal: the scope answers both halves of the owner's framing, and milestone two carries the transparency half as a real requirement rather than as a fallback
- missing: nothing in the goal is unaddressed, but the kill criterion cannot be evaluated yet because its probe is the first move rather than a prior check
- wrong: my first diagnosis of the stop-at control was wrong and the owner corrected it within the minute; the withdrawn version is recorded as obsolete so it is not re-derived
- out of scope: the lane's own door at 33,461 ms is not a surface, and it is carried anyway because the owner's scope wording puts an agent-facing interface in
- prior art: scanned live at this gate, and they beat us on three counts — CI enforcement that fails a build, error budgets with a stated policy rather than a flat threshold, and percentile targets instead of a single number
- prior art, our side: ours binds the demand to a MODELLED boundary so a check can enumerate what to measure from the spec, and ours carries the honesty half that no SLO framework has — they page an operator, they never tell the person using the thing
- prior art, what to steal at milestone two: a percentile rather than a flat second, and a stated policy for what happens once the budget is spent

## round_2_red_team

- i12 shipped this exact rule two days ago and it did not hold => i12 fixed the instances it could find; this builds the thing that ENUMERATES them, and if milestone three ends with no state reading the instrument then this iteration has repeated i12 and should be judged failed
- the modelling could be ceremony that produces no speed => it is why the FIRST move is the four-line phase split rather than the modelling, so the probe decides whether the DAG is the right instrument before anything is built on it
- the worst measured breach is the lane's own door at 33,461 ms, which is not a surface at all => carried rather than dismissed, with note-afb66f5e0dee naming a cause, and the assumption's own text says a green probe proves nothing about it
- pulling seven backlog notes in at the retro is scope inflation dressed as migration => each names a specific interface that misses the rule, each was parked with a condition this iteration satisfies, and step 4 is the sanctioned place; if they prove more than a minor the walk escalates visibly
- an iteration about interfaces that leaves this morning's dead button unfixed would be absurd => it is in scope and carried in the record, and the owner ruled it is fixed during implementation rather than now
- KILL CRITERION, named and looked for => if the phase split shows the cost concentrated in ONE call rather than spread across states, the DAG is the wrong instrument and milestones three and four are aimed wrongly; looked for and NOT yet answerable, which is exactly why it is minted as an assumption rather than argued away here

## raid_additions

- raid-asm-the-slow-phase-is-the-green-derivation-repeated

## verdict

pass — the retro is drained and signed, the scope is four milestones in a forced order, the prior-art scan ran live and changed nothing in scope, and the four-line probe that could invalidate the last two milestones runs first rather than last

## follow_up

THE FIRST MOVE IS THE PROBE, and it is four lines: split the machine phase into its four parts and read the split against two numbers. It decides the shape of milestones three and four, so nothing downstream is built before it answers.

WHAT THIS GATE HANDS FORWARD:

- raid-asm-the-slow-phase-is-the-green-derivation-repeated, open, with its probe and both readings of the result written out.
- A measured latency baseline in the record, taken today rather than inherited, so milestone four can be judged against a number that predates it.
- Seven pulled notes and this morning's stop-at finding, each naming a specific interface rather than a general complaint.

WHAT IS PARKED AND NEEDS AN ITERATION IT DOES NOT HAVE: deleting the reading file, and removing the handover everywhere. Both are owner rulings from 2026-08-16 and both were confirmed unbuilt today by reading the live system.

WHAT NEEDS THE OWNER AND NOTHING ELSE: the checklist panel's owed state, whose trigger is written as the owner reviewing the rendering and deciding the visual state.

## anything_else

## bound_breaches

- if-agent-harness-to-entrypoint: AT LEAST 181 calls over its one-second bound today. THE NUMBER IS A FLOOR, NOT A MEASUREMENT, and that was found after this line was first written: se_log_query drops records matching its filter and reports `older: 0` while doing it, so the real count can only be higher (raid-iss-the-call-log-query-omits-matching-records-and-says-it-did-not). The direction of every conclusion below is unaffected; the claim to have measured is not. As counted, and MILESTONE FOUR OWNS THEM. Broken down: mirror_slow 82, se_pull 63, se_test_verdict 20, se_aim 6, mirror_profile 4, se_run 2, se_file_search 2, se_git 1, se_seed_iteration 1. THE TWENTY TEST VERDICTS ARE HONEST RATHER THAN BREACHES — if-test-runner-to-toolchain declares a bound deliberately not one second, and a battery is a minute by design. The six aims are the per-hop route recompute, the detour that stops a moved ground being followed off a cliff. TWO FIXES LANDED TODAY against the rest: the duplicate green pass removed from the render, and the signature read folded into the pass that already happens. WHAT IS NOT FIXED AND IS NAMED RATHER THAN BURIED: twelve of the thirteen modelled boundaries have no calls attributed to them, so this row can only name the lane door until a logged call says which crossing it made.
