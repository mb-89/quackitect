---
form: declare-winner
by: agent
signed_off: 2026-08-14T12:47:26.141Z
reopened: 2026-08-14T12:46:48.025Z — It declares no winner because the demand table was unsound; the table has since been re-audited blind and a fresh scoring stands, so a winner can now be named.
authors: agent
files: null
---

# Evidence form / declare-winner

## current_situation

THE WINNER IS cand-core-satellite: one core that knows the whole state, one satellite per agent that knows its own.

IT WINS ON BOTH JUDGMENTS AND THEY WERE MADE SEPARATELY. A blind demand audit found it the only line with ZERO failures across nine `must` rows, every verdict quoted from the candidate's own text. A blind scoring, given no earlier scores, put it first at 27 against 20, 18, 14, 13 and 12. Neither agent saw the other's work.

WHAT IT IS. The core owns what must be one thing - the mirror, the claim ledger, the note inbox, the call log, trunk, and the routing. A satellite owns one agent's work: the walk position, the bound record, its tree, and the engine that agent is running as a delta over trunk's. A call is routed to whatever owns the path.

WHAT THAT BUYS, and each of these is a demand it is the only line to answer.

- AN ENGINE CHANGE APPLIES WHERE IT WAS MADE. The agent's satellite comes up on the new code; the core and every other satellite keep what they were running, and the agent never steps out.
- A METHOD CHANGE NEEDS NO STEP-OUT EITHER, through the same door. The core serves trunk, so a call naming trunk is routed there rather than refused.
- VERSION CONTROL RESOLVES LIKE EVERY CALL, for free, because routing is what the line does. The case with no door today - work in a store the walk is not bound to - has one here by construction.
- A SURFACE RESOLVES TO WHAT IT SHOWS. The core knows which satellite holds what, so a panel showing one record while the walk stands in another is correct rather than a mixture.

FOUR LINES WERE ELIMINATED BY DEMANDS, each on its own record's admission. Three are one engine, so changing it stops every agent. cand-judged-path also fails the method row. cand-os-rooted fails two.

cand-live-engine SURVIVES THE DEMANDS AND LOSES ON BREADTH, seven points back. It is the recorded runner-up.

## winner


## comments

THE SEAT IS HELD BY ONE POINT AND THE DECLARATION SAYS SO.

cand-judged-path trails by exactly one, and reverse-sensitivity found two ways to close it that need no new information.

- Build raid-dec-thin-tree, which is DECIDED and UNBUILT, and req-entry-binds-worktree moves for the rival. The seat goes with it.
- Give the rival a read-side rule from the chart, and the FATAL axis moves. The seat goes with that too.

SO THIS IS A DEFENSIBLE CHOICE AND A FRAGILE ONE, and both halves are the declaration.

WHAT THE WINNER LOSES ON. req-call-answers-in-one-second, the only axis in the set with a real measurement behind it - 2.0 ms batched against 47 to 54 ms per spawned git, from exp-trunk-read-cost of 2026-08-10. The rival owns that axis and lost anyway, on the fatal one.

THE WINNER'S OWN WEAKNESS, unchanged by winning. Its confinement is a variable inside one JavaScript engine rather than a kernel handle, so it holds only while no code path bypasses the seam. The i8 field report of 2026-08-12 records that bypass being used within days of the last guard landing. cap-std and openat2 RESOLVE_BENEATH do this better and we cannot copy why: they have a kernel.

THAT IS THE KILL-CRITERION FOR THE WHOLE CHOICE. If the seam cannot be made unbypassable, this architecture is decoration and the rival's predicate - which never claimed to be structural - is the honest answer.

## follow_up

THE SEAT IS SEVEN CLEAR AND IT IS NOT SAFE, and M6 opens on that rather than on the margin.

THE PROFILE DECIDES IT. cand-core-satellite's case rests on parallelism, and raid-asm-the-target-machine-is-many-throttled-cores names its own falsifier: if the slow calls are IO-bound, more processes buy nothing. Nobody has profiled a slow call. If the time is not CPU-bound, the winner's central argument is void and cand-live-engine loses nothing by it.

THE OVERLAY CELL IS OWED A RE-SCORE. It is the winner's worst at 0 and the runner-up's best at 3. cand-core-satellite scored 0 while it carried a whole engine per record and now carries a delta - the same layering that earned the 3. It cannot close seven points and it makes the margin look wider than it is.

THREE AXES MEASURE SILENCE and the architecture gate should rule on them: vendoring, install and the note inbox are mentioned by almost no record, so they discriminate on whether an author wrote a paragraph.

WHAT THE WINNER OWES M6, from its own record and from the audits.

- WHAT A SATELLITE ACTUALLY IS - a process, a worker thread or an isolate. Four axes move with the answer and the option says it is undecided.
- WHETHER THE SHARED STATE CAN BE SERVED over a local channel. The mirror is a server today, so it is the natural core; nothing says the note inbox and the claim ledger can live behind it and no probe has run.
- WHAT A PARTIAL FAILURE LOOKS LIKE. One process either runs or does not; a core with N satellites has states in between and no other line has to answer for them.
- A START-UP MEASURED WITH THE ENGINE MODULE LOAD INCLUDED. Every figure on the board excludes it.
- A SEALING RULE FOR THE ENGINE FOLDER. It is the only record that names the hazard and it designs no rule.

THE MEMORY REFINEMENT IS DEFERRED PENDING THE PROFILE, deliberately. Holding a record's work in memory is adoptable by every line, so it is a choice the winner makes later rather than part of the seat.

## anything_else

WHAT THIS MILESTONE ACTUALLY COST, and it is worth recording because the answer changed twice after it looked settled.

SIX SCORING PASSES. The first five converged on cand-judged-path with rising confidence, and the sixth put it third. The designs barely moved between the fifth and the sixth. What moved was the standard: silence stopped scoring as competence.

A GENEROUS SCORER REWARDS A SHORT RECORD. If saying nothing about install scores the same as thinking about install and finding a problem, the line that never considered it wins the cell. Five passes ran that way, and the sensitivity analysis reported the result stable each time - correctly, about the arithmetic, and blind to what the arithmetic measured.

THREE OWNER RULINGS LANDED TODAY AND EACH CHANGED THE ANSWER.

- A must outranks a score, so a leader that fails a demand is out rather than caveated.
- An unanswered demand is incompleteness, so a gap is filled before anything is judged.
- A defect found while building is solved, not recorded and carried past.

NONE OF THE THREE CAME FROM THE METHOD, and the method had run four gate passes over the same material without producing them.

THE WORST DEFECT WAS INVISIBLE BY CONSTRUCTION. A verdict was carried from one candidate to another because they shared a design pick, and a borrowed verdict reads exactly like a derived one. It is not a stale copy, a broken link or a miscount, so no lint, no claim check and no gate round could see it. The fix is now contract rule 5, and the mechanical form - a verdict cell carrying its quote - is owed to the build.

AND IT HAPPENED TWICE MORE AFTER THE RULE WAS WRITTEN: a citation attributing a sentence to the wrong record, and a candidate completed with two of its own sections contradicting each other. Both were caught by blind agents rather than by any check.
