---
form: map-stakeholders
by: agent
signed_off: 2026-08-13T19:16:43.891Z
authors: agent
files: null
---

# Evidence form / map-stakeholders

## current_situation

draw-context is signed. The boundary and the neighbours are resident and unmoved; one relationship sharpened, the engine's to git.

THIS STATE INHERITS FOUR ROLES and adds none. What it re-checks is the disposition: an architectural move shifts who pays and who gains, and this one does.

## roles

- project/spec/trace/stakeholder/stk-engineer-driving-agents.md
- project/spec/trace/stakeholder/stk-agent.md
- project/spec/trace/stakeholder/stk-vehicle-owner.md
- project/spec/trace/stakeholder/stk-newcomer.md

## coverage

THE FOUR RESIDENT ROLES ALL STAND. No role is added and none is struck.

### Dispositions re-checked against this change

THE ENGINEER DRIVING AGENTS gains most and is unchanged in placement. They stop watching where writes land, and they stop reconciling two branches that built the same thing.

THE AGENT gains and loses, and the loss is worth marking. It gains a tree it cannot get wrong. It LOSES the escape hatch it currently uses to edit method mid-walk - until the fan-out lands, which is why the fan-out is in scope rather than deferred.

THE VEHICLE OWNER is the role this change is actually FOR, and the one that cannot test it. Every vehicle gets the binding; only this product is exempt. A rule its author never runs is a rule its author cannot vouch for from experience, which is the assumption already on the register.

THE NEWCOMER is neutral. Nothing they meet on arrival changes, and the one thing they might notice - a finished record leaving no folder behind - makes the tree simpler rather than stranger.

### The always-on classes, kept or struck

- acquirer: STRUCK. No acquirer exists; the product is not sold. Recorded at the delta's business case for the same reason.
- user: covered by the engineer driving agents, who is the user here.
- newcomer: PRESENT as a node.
- communicator: STRUCK for this change. Nothing here is explained outward; the book iteration owns that role.
- assessor: covered by the engineer, who blesses the gates. There is no separate assessor on this product.
- project-owner: covered by the engineer.
- agent: PRESENT as a node, and it is the role most directly bound by this change.
- developer-maintainer, installer, integrator, operator, regulator, service-technician, supplier, transport, end-of-life: STRUCK. No hardware and no service life.
- tester: covered by the agent, which runs the battery.

### What the coverage check proves

Every value prop's audience resolves to a role that exists. vp-the-engine names the engineer driving agents, which is the node above.

NO TENSION IS LOGGED HERE, because a tension is a register entry. One exists and is already recorded: the agent's loss of mid-walk method editing against the vehicle owner's need for isolation. It is the conflict named at draft-vision and it is answered by the fan-out being in scope.

## follow_up

write-stories is next, and it inherits four roles with their dispositions re-checked rather than re-derived.

WHAT THE STORIES SHOULD BE FOR. Two roles carry this change's whole value: the engineer who stops watching where writes land, and the agent that can no longer get the tree wrong. The vehicle owner is served by the same mechanism without touching it.

ONE THING THE STORIES MUST NOT DO. They must not narrate the mechanism. A story says what a role needs and gets; whether that arrives by moving the root or by judging the path is the architecture milestone's question and it is not settled.

## anything_else

