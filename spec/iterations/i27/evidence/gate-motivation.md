---
form: gate-motivation
bless: blessed by human
by: agent
signed_off: 2026-08-13T19:10:36.191Z
authors: agent
files: null
---

# Evidence form / gate-motivation

## current_situation

M1 is walked. Eight states signed, one of them re-earned after a reopen.

THE PACKET: vision inherited from vp-the-engine, an as-is with eight witnessed pains, a delta with its gap claim and why-now, a scope covering BOTH problems the owner named, nine non-goals each naming a receiving record, four register entries, and a pressure test that corrected the packet's own business case.

WHAT CHANGED SINCE THE FIRST SUBMIT. scope-non-goals was reopened and re-earned. The fan-out moved from non-goal into scope: it was the only non-goal naming no home, and eight drained notes put it in this record and in no other seed, so it was lost rather than deferred.

THE GATE'S JOB HERE: is the problem real and worth solving. Past this gate the vision is axiomatic. NOT which solution wins - that is the architecture milestone's question.

## vision_scope_stated

COMPLETE, with one part deliberately empty.

- Vision: inherited by pointer to vp-the-engine, with the delta argued in one paragraph.
- As-is: eight pains, each carrying a witness that is a date, a measurement or an incident.
- Delta: the gap as a claim, alternatives positioned, why-now on three matured conditions.
- Scope: both problems the owner named, each with the work that answers it.
- Non-goals: nine, and every line names a receiving record.
- Register: four entries with owners and triggers.
- Pressure test: a press release and a hostile FAQ of five questions.

THE EMPTY PART IS THE VALUE PROPS, and it is a considered none. This delta authors no new value proposition. It serves a resident one, and recording a new prop to fill a box would be inventing a promise.

## problem_agreed

TWO PROBLEMS, NAMED BY THE OWNER, and the gate's question is whether both are real.

(a) THE SYSTEM IS CONFUSED ABOUT WHERE TO READ AND WRITE. Measured: one feature built twice on two branches with three merge conflicts. Four signed claims felled when a mid-walk sync pulled 26 commits to carry two files. Twenty-four of twenty-eight trees half-synced, one holding a new session module against an older form module, which does not compile. A diagnostic taken in the wrong tree that reported a defect which did not exist.

(b) IT BLOCKS ITSELF FROM WRITING IN A WORKTREE, so it has to walk out. Measured: eight step-outs in one session, three inside a single verification. One edit cost five calls, four of which did no work.

WHY (b) IS NOT NEGOTIABLE, and this is the load-bearing sentence of the packet. THIS PRODUCT DEVELOPS ITSELF. The whole point of an iteration here is to change the machine, and the change is made while walking that machine. If method cannot be edited inside a walk, iterations cannot be used to work on ourselves at all. The blocking is not an inconvenience inside the process - it forecloses the process's main use.

BOTH ARE REAL AND BOTH ARE OURS, and neither is inferred from somebody else's reported pattern.

THE GOAL IS WORTH HAVING because the resident vision already promises it and cannot keep the promise. Its success criterion is zero double-claimed iterations. The ledger delivers half - it stops two agents taking one record. Nothing stops two records reaching one tree.

ONE HONEST CORRECTION, from the pressure test. The binding buys back UNINTENDED writes, not step-outs. That is why (b) needs the fan-out rather than the binding, and why both are in scope.

## prior_art_positioned

FOUR EXTERNAL ALTERNATIVES WERE POSITIONED AT THE DELTA. A guard that enumerates tools sheds coverage, and ours already missed the sixth door. Git worktrees alone shed enforcement entirely. Workspace-rooted build tooling resolves at build time for declared inputs and does not govern an agent's arbitrary write - CLAIMED FROM DOCUMENTATION, NOT RUN. Process isolation is the strongest and sheds self-hosting outright.

THE POSITIONING MISSED THE PRIOR ART IN THIS REPOSITORY.

raid-dec-two-layer-auth, decided at i1, already rules that a write is judged by its PATH rather than by its verb. Its trigger reads: any write landing outside the record from inside a bound walk. That is problem (a), already decided, with a candidate attached.

WHAT THAT MEANS AT THIS GATE, and only this. The problem is not merely real - it was recognised and ruled on months of work ago, and the ruling was then forgotten rather than built. A problem this system has already decided to solve twice is not a problem in doubt.

WHAT IT DOES NOT MEAN HERE. Which of the two shapes wins is an architecture question and this gate does not answer it. The comparison is logged as an open issue so the architecture milestone meets it rather than rediscovering it.

## success_measurable

EVERY LINE IS COUNTABLE, and two are inherited from the resident vision rather than invented here.

FOR (a):
- Double-claimed iterations: zero. Inherited.
- Writes landing outside the bound record: zero, by a test per path kind - method, record content, session state, repo-root files.
- Records reading as open on a peer that never started them: zero. A fresh clone showed nineteen.
- Trees standing half-synced: zero, held by the entry-time levelling that already landed.

FOR (b):
- Step-outs needed to change method: zero. With the fan-out an edit reaches every tree from wherever it is made.
- Calls per method edit: one. It is five today, four of which do no work.

INHERITED: acts from clone to first claimed iteration, two.

ONE PASS LINE IS OWED AND NOT WRITTEN. The split of past step-outs into accidents and deliberate method edits. Owed at write-requirements.

## risks_logged

FOUR ENTRIES, each a node with an owner and a trigger.

- raid-asm-engine-serves-from-the-bound-tree, assumption. Trigger: the first reload taken while bound, once the root moves. Its probe is written.
- raid-risk-a-write-lands-in-the-wrong-tree-silently, risk, fatal and likely. Trigger: the first write of each path kind after the root moves.
- raid-iss-scope-grew-past-the-kickoff-bless, issue, already fired.
- raid-iss-cheaper-alternative-never-compared, issue, opened by this gate.

THREE OF FOUR ARE ALREADY TRUE rather than speculative. A milestone whose register is mostly issues is one that discovered more than it predicted, and that is worth saying rather than dressing up.

## round_0_verify

- evidence vs claims: PASS WITH ONE INCONSISTENCY. All eight evidence files opened and read, not taken from their signatures. THE INCONSISTENCY IS REAL: frame-delta's business case claims eight step-outs as cost bought back, and pressure-test later corrected that to unintended writes only. The earlier file still carries the overstated version.
- types: PASS. tsc over the whole deliverable, exit 0.
- lint: PASS. biome over 217 files, exit 0, 7 pre-existing infos in files this record has not touched.
- tests: PASS. 61 of 61 across the reference and trace suites, answering whether the minted raid nodes resolve and conform and whether the trace still holds with them in it.

## round_1_validate

- exercised against the goal: NOT YET, by construction. Nothing is built at M1. What IS exercised is the ground under it: 27 trees levelled, the claim ledger closing, the dependency graph proving itself through the container's own offer of seventeen doors.
- missing: NOTHING NOW. The fan-out was missing and is in scope. It was found by asking where a shed item had gone, and the answer was nowhere.
- wrong: ONE CLAIM, corrected rather than removed. The business case at frame-delta overstates what the binding saves; the correction stands at pressure-test and the two files disagree.
- out of scope: NOTHING FOUND beyond the nine non-goals, and every one now names a receiving record.
- prior art: MADE FOR THE EXTERNAL FIELD, NOT RUN, and said to be. The in-house decision was missed at the delta and is now on the register.

## round_2_red_team

- STEELMAN: (b) is a discipline problem wearing an engineering costume => Its best advocate says just do not edit method mid-walk. Batch it at the retro. Free, no code. IT DOES NOT SURVIVE, AND NOT ON COST GROUNDS. THIS PRODUCT DEVELOPS ITSELF, and an iteration here exists to change the machine. The change is made while walking the machine it changes. Forbidding method edits inside a walk does not make the walk tidier - it makes iterations unusable for the one thing this product uses them for. The advocate is proposing that we stop dogfooding, which is the opposite of the argument they think they are making.
- STEELMAN: the problem is self-inflicted, so solving it is not a win => The advocate says we built the cage, then the guard, then the worktrees, and the confusion is entirely of our making. TRUE AND IRRELEVANT. The cage and the worktrees are load-bearing and the ledger depends on them. Self-inflicted is a fact about history, not about whether it costs now.
- ATTACK: it is a bundle of two problems with different causes => Two problems, two mechanisms, one record. WITHDRAWN AS AN OBJECTION, on the owner's ruling of 2026-08-13. The theme is read and write usability and the two are its two halves. AN ITERATION IS ROUGHLY ONE DAY OF AGENT WORK, and splitting coherent work to satisfy a human sprint intuition spams the container with records too small to be worth their own machinery. Bundling is a defect when the parts do not share a theme; these do.
- ATTACK: the evidence is one machine and mostly one week => WITHDRAWN, AND IT WAS BADLY REASONED. Sample size matters when a phenomenon could be machine-dependent. This one cannot be: the cause is stepping in and out of records, which is a property of the PROCESS and identical on every machine. There is no mechanism by which a different host would step out differently. The corroboration exists anyway - the same class appears in the cloud run and under Copilot on other machines - but the corroboration was never what carried the argument.
- KILL-CRITERION => The binding is wrong if it makes self-hosting impossible. LOOKED FOR IT: the exception is what prevents that, and the exception is unobserved. IT IS WEAKER THAN IT LOOKS, by the same reasoning that killed the sample-size attack. If the problem is not machine-dependent, neither is the binding's correctness, so exercising the general rule on ANY product proves it for all. The probe needs a product that does not edit the engine, not a fleet.
- ATTACK: the packet corrected itself twice and left both corrections in place => The business case is overstated in one file and corrected in another; the scope was wrong and was reopened. A reader of the earlier file alone gets the wrong number. THIS ONE STANDS, and it is the only override on the verdict.

## raid_additions

- project/spec/trace/raid/raid-iss-cheaper-alternative-never-compared.md

## verdict

pass with overrides — both problems are real, measured and ours, and one number inside the packet is still overstated.

WHY IT PASSES. The pains are witnessed rather than asserted. The resident vision cannot keep its own promise without (a). And (b) is not a convenience: this product develops itself, so a walk that cannot change the method cannot be used to change the machine, which is what iterations here are for.

TWO OBJECTIONS WERE WITHDRAWN RATHER THAN OVERRIDDEN, and the reasons are worth keeping.

The bundle objection falls to sizing: an iteration is roughly a day of agent work, the two halves share a theme, and splitting coherent work to fit a human sprint intuition spams the container.

The sample-size objection falls to mechanism: the cause is stepping in and out of records, which is a property of the process and identical on every machine. Sample size only matters where the phenomenon could be machine-dependent, and this one cannot be.

ONE OVERRIDE REMAINS. The business case at frame-delta overstates what the binding saves, and the correction lives in a different file. THE DISSENT: a reader of the first file alone gets the wrong number, inside a packet arguing for more rigor. It passes because the correction is recorded and the measured split is owed at write-requirements as a pass line.

WHAT WOULD HAVE MADE IT A FAIL: if either problem were unmeasured, or if (b) had a home elsewhere. Neither is true.

## follow_up

OWED AT write-requirements, three things.

- Rows for the six pieces of behaviour that landed before the kickoff priced them.
- The measured split of past step-outs into accidents and deliberate method edits, because the business case rests on it.
- Pass lines for both problems: writes landing outside the bound record, and calls per method edit.

OWED AT THE ARCHITECTURE MILESTONE. Which shape answers (a) - and the deciding question is whether a path judgment covers READS as well as writes, because today's newest pain is a read. The monorepo comparison is owed there too, run rather than cited.

STANDING AND UNRESOLVED: the general rule ships unexercised by its own author. Its probe needs a product that does not edit the engine.

FOUR NOTES STAND PENDING in the pool and cannot be drained inside this milestone, because carried is the retro's judgment and the retro has closed. Three go to i10 and i17 - the per-gate red team questions, the prior-art search order, and the missing return path from a record to the pool. One goes to i11.

## anything_else

TWO CORRECTIONS I OWE ON MY OWN CONDUCT, both recorded because they shaped this packet.

THE RED TEAM ASKED SOLUTION QUESTIONS AT A MOTIVATION GATE. The first version of this round compared two implementations and hung its verdict on which wins. That is architecture work dragged a milestone early, and the owner caught it. The round's guidance is the same text at every gate and never says what the gate is for, so it attacks whatever is in front of it. Noted for i10: each gate carries its own red-team question.

THE PRIOR-ART SEARCH LOOKED OUTWARD AND NEVER AT THIS REPOSITORY. The decided decision it missed was one search away. The rule says prior art is a comparison rather than a citation and says nothing about WHERE to look. Noted for i10: the register and the decisions are searched first, because a decided decision outranks any external system as prior art.

AND ONE HOLE THE OWNER NAMED, which nearly cost this packet its second problem. A record can SHED work and nothing asks where it goes. The drain demands a receiving record; the scope state takes free prose. Getting work into a record is checked and getting it out is not. Noted for i17, which owns the pool.
