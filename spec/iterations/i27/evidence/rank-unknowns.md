---
form: rank-unknowns
by: agent
signed_off: 2026-08-14T13:51:09.877Z
authors: agent
files: null
---

# Evidence form / rank-unknowns

## current_situation

THE EXPOSURE CHART COULD NOT BE READ UNTIL THIS STATE FIXED IT. Thirty-two register entries carried no placeable likelihood, so their dots were never drawn and the pick would have chosen from a chart missing a third of the register.

Nine still carried the mint's own comment. Twenty-two carried words nobody put on the scale — certain, near-certain, likely, possible, unlikely, rare. One carried no key at all.

All thirty-two are now graded on the three levels the method declares, and grades-complete.ts answers green.

ONE GRADE WAS WRONG IN DIRECTION rather than merely off-scale. raid-asm-the-target-machine-is-many-throttled-cores read near-certain, which meant near-certainly TRUE. The scale grades an assumption by how likely it is FALSE. It is now conceivable.

ONE ENTRY WAS MISSING ENTIRELY and is minted here. The core's whole premise — that the claim ledger, the note inbox and the call log can be served over a local channel — was written inside cand-core-satellite and inside if-core-satellite, and in no register entry. Nothing could rank an unknown the register did not hold.

FOUR SPIKES ARE PICKED, out of seventy-six live entries.

## seeded

- raid-risk-a-write-lands-in-the-wrong-tree-silently
- raid-asm-machine-wide-state-serves-over-a-local-channel
- raid-ar-call-answers-in-one-second
- raid-ar-crash-lands-safe

## follow_up

WHY THESE FOUR AND NOT THE OTHERS.

- raid-risk-a-write-lands-in-the-wrong-tree-silently sits at fatal and expected, the worst cell on the chart, and it came true twice this session. se_lint resolved a path against the record's worktree while the file tools served it from the machine root, with nothing on either answer saying which. The seam's whole premise is that it cannot be walked around, and a probe settles whether every verb can be routed through one.
- raid-asm-machine-wide-state-serves-over-a-local-channel is the core's premise. If an append to the call log costs a measurable share of the one-second budget, the core owns less than the architecture says it owns. Its probe section names the measurement.
- raid-ar-call-answers-in-one-second carries the two numbers every figure on the board excludes: a satellite start with the engine module load INCLUDED, and the price of a core-owned read becoming a channel hop. It also decides what a satellite is, which record-adrs says moves four scored axes.
- raid-ar-crash-lands-safe carries the architecture's own open question: what happens to an in-flight call when a satellite dies. el-satellite-supervisor names it and does not solve it, and no single-process line ever had to.

WHAT WAS CONSIDERED AND LEFT OUT.

- raid-risk-a-broken-engine-delta-has-no-way-back needs no probe. The owner ruled on 2026-08-14 that we adopt nginx's shape, so the answer is known and the work is build rather than spike.
- raid-corpus-stays-small is not an unknown. The problem is measured and the owner has already designed the fix — split the sources into sub-indexed chunks AND keep a pagination mechanism, both halves. A spike would settle nothing.
- raid-asm-the-target-machine-is-many-throttled-cores is not a blocker. The owner ruled that no decision hangs on the profile: the throughput argument was scored at zero and the winner won anyway, so a profile can only move it up. The cost measurements that DO threaten a requirement ride on raid-ar-call-answers-in-one-second instead.

THREE THINGS THE REGISTER STILL OWES, filed as notes rather than picked here.

- The two interface nodes for the lane's own seams, agent to engine and person to engine (note-9b5017d22914).
- A form check that refuses a grade outside its catalogue, and a mint that leaves how_likely absent rather than commented (note-3465043278d3).
- A form check that refuses a computed number written into prose (note-6fd08080090f).

## anything_else

WHY THE CHART WAS UNREADABLE AND NOTHING SAID SO.

Two checks guard this state and they hold different standards. grades-complete.ts asks only that the key is non-empty, so a mint comment passes it. The chart asks that the value is one of the three declared words, and anything else gets no dot.

So the exit script could report green while a third of the register was invisible to the pick. The script's own comment names that exact harm: a register row nobody weighs is a risk nobody sees.

HOW THE OFF-SCALE WORDS WERE MAPPED, so the judgment can be argued with.

- certain, near-certain and likely became EXPECTED where the standing condition already holds today, which is the scale's own test.
- possible became PLAUSIBLE where one ordinary event produces it, and CONCEIVABLE where the story needed an and-also.
- unlikely and rare became CONCEIVABLE.

THE ONE THAT MOVED ON MEANING RATHER THAN VOCABULARY. raid-asm-peer-runs-supported-platform went to expected, not because the word changed but because the condition already held: a cloud session has already run on Linux, and raid-issue-package-live-check-owed records that it could not complete there.

THE PICK IS NARROW ON PURPOSE. Four spikes on a machine with weak cores is already parallel work. Seventy-six entries stand live and most of them are neither blockers for this build nor settleable by a timebox.
