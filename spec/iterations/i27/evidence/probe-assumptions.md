---
form: probe-assumptions
by: agent
signed_off: 2026-08-13T20:15:04.125Z
authors: agent
files: null
---

# Evidence form / probe-assumptions

## current_situation

SIXTEEN ASSUMPTIONS STAND IN THE REGISTER after this state's work, and the input was the folder rather than the form above it.

THIRTEEN WERE ALREADY PROBED, most of them today by i3's walk. This state re-read their statements against this change and none was moved by it.

TWO WERE NEVER PROBED and both are mine, minted this milestone. One is probed here. One cannot be probed from this machine and says so.

ONE I MINTED WAS A DUPLICATE and is deleted. That is the second duplicate I have minted today.

## probes

- raid-asm-a-peer-understands-the-ledger: PROBED AND HOLDS, against the real parser rather than by reasoning. A ledger entry carrying an unknown word - handed_back, meaning the claim was given up - was written to a bare origin and read by a clone. The parser skipped the word, reported the claim as standing, and a different machine entering was REFUSED naming the holder. So an unknown word fails toward refusing rather than granting, and no double-claim is possible through this door. WHAT IT ALSO SHOWED, and the node records it: that direction is wrong the other way - a record genuinely handed back reads as held, so real work reads as unavailable. Safe against collision, lossy against availability.
- raid-asm-engine-serves-from-the-bound-tree: UNPROBED, with its reason. The probe needs a bound record on a product that does NOT edit the engine, and this repository has no such product. It cannot be run from here at all, which is why the trigger names the first reload after the root moves rather than a date. Naming a gap does not close it, so this stays open and the gate sees it.
- raid-lane-works-on-posix: STANDS AS PROBED, re-read rather than re-run. It was probed 2026-08-13 on a headless Linux container - the full battery at 1104 of 1117, with none of the 13 failures touching separators, worktrees or the fan-out. ITS SECOND TRIGGER IS LIVE FOR THIS ITERATION: any new path-splitting code in the lane re-owes it, and this iteration's whole subject is path resolution. So it is probed today and will be owed again the moment the resolution changes. Flagged rather than re-probed, because nothing has changed yet.
- raid-asm-remote-serializes-claims: STANDS, probed 2026-08-12, and this iteration touches it - the ledger gained a word. The serialization property is unchanged: the done stamp is another commit on the same file through the same push, so first-push-wins is unaffected.
- raid-asm-peer-runs-supported-platform: STANDS, probed 2026-08-13. Unmoved by this change.
- raid-asm-session-identity-survives-a-reload: STANDS, probed 2026-08-13. Touched by this iteration through the walk-back cost, and the property it asserts is the one that makes a reload survivable. Unmoved.
- raid-asm-line-endings-do-not-move-under-us: STANDS, probed 2026-08-13. Directly relevant to the reading credit keyed on content, which this iteration inherits rather than builds. Unmoved.
- raid-asm-dial-carries-adjudication: STANDS, probed 2026-08-12. Exercised twice today - two gates blessed by the owner, one by the agent at the standing setting. Unmoved.
- raid-asm-an-omit-is-authored-honestly: STANDS, probed 2026-08-13. Unmoved by this change.
- raid-asm-every-condition-can-say-what-it-wants: STANDS, probed 2026-08-13. Unmoved.
- raid-asm-grey-verb-distinct-from-se-help: STANDS, probed 2026-08-13. Its trigger includes i8 landing, which happened this morning - so it is owed a re-look at the next retro rather than here.
- raid-asm-help-query-vocabulary-overlaps: UNPROBED with its reason recorded, and the reason still holds - it needs real usage of the help verb to accumulate, and i8 shipped only this morning.
- raid-asm-refusals-recover-a-weak-model: STANDS, probed 2026-08-13. Unmoved.
- raid-asm-owner-pushes-keep-remote-fresh: UNPROBED, scheduled at M7, and its reason still holds. THIS ITERATION MAKES IT MORE LIVE rather than less: nothing this session has been pushed, and the dependency gating a peer reads is the remote's.

## follow_up

TWO THINGS THIS STATE HANDS THE GATE.

ONE ASSUMPTION CANNOT BE CLOSED FROM THIS REPOSITORY. The engine serving from a bound tree needs a product that does not edit the engine, and there is not one. The gate should see that as an open it cannot rule away rather than as neglect.

ONE STANDING ASSUMPTION IS RE-OWED BY THIS ITERATION'S OWN SUBJECT. raid-lane-works-on-posix carries a second trigger - any new path-splitting code in the lane - and path splitting is what this iteration changes. It is probed today and will be owed again at M7, on a POSIX host. That is the honest schedule and it belongs in the record rather than in somebody's memory.

AND ONE THING FOR THE RETRO. raid-asm-grey-verb-distinct-from-se-help has a trigger that fired this morning when i8 shipped, and nothing has looked at it since.

## anything_else

TWO DUPLICATES MINTED IN ONE SESSION, and the pattern is worth more than either.

The first: a requirement about the reading credit that said what req-reading-credit-survives-a-reload already said. The second: an assumption about path rules on other platforms that said what raid-lane-works-on-posix already said - and that one was ALREADY PROBED, on Linux, today.

BOTH WERE FOUND BY READING THE NEIGHBOURS, not by any check. The first surfaced while deriving functions; the second while sweeping the register for this state. Neither would have surfaced if the state had not happened to require reading the family.

WHY I KEEP DOING IT. Writing a node is cheap and reading two hundred is not, so the cheap act happens first and the expensive one happens only when a state forces it. That is not carelessness - it is the incentive the tooling creates, and it will do the same to anybody.

THIS IS THE THIRD ARGUMENT TODAY FOR THE SAME MECHANISM. The cross-coupling check with a retrieval half would have caught both before they were written, by putting the neighbours in front of the author at mint time rather than at sweep time. It is carried to i15, i18 and i6.
