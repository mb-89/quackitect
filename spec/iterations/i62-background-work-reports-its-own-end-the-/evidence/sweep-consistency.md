---
form: sweep-consistency
judgment: passed at 2026-08-24T17:17:43.627Z
by: agent
signed_off: 2026-08-24T17:17:41.592Z
authors: agent
files:
---

# Evidence form / sweep-consistency

## current_situation

Six behaviours changed, and each was traced to every surface that teaches it.

An entry closes itself when its process exits. A sweep closes what is already gone. Every wait declares a bound with the word that says whether it was measured or defaulted. Expiry ends the wait rather than the work, and a real ending replaces it. One instance holds a folder and its port, decided before anything touches the folder. Registering a spawned hand is legal wherever the walk stands.

Five documents were changed. Four surface classes had nothing to teach and were checked rather than assumed.

No surface is known stale and left. The residue list is empty.

## swept

- [x] command and tool docs
- [x] engine-served strings
- [x] method cards
- [x] matrix rows
- [x] templates and skeletons
- [x] guidance chapters
- [x] book chapters
- [x] README and entry documents
- [x] panels and form help

## follow_up

- NOTHING IS PARKED BY THIS SWEEP. Every surface that taught a changed behaviour was fixed in it, and none was left with a reason.

## anything_else

WHAT WAS ACTUALLY CHANGED, per class.

COMMAND AND TOOL DOCS. The `agent` argument of `se_run` now says the call is legal wherever the walk stands, even where `se_run` itself is not. That was the whole point of one of this record's goals and the verb that carries it said nothing about it. The README's lane table was read and needs nothing: it maps Bash to `se_run` and teaches no behaviour this record touched.

ENGINE-SERVED STRINGS. The same tool description is the engine-served string. The refusal texts were read for the three clauses this record could have moved and none of them changed meaning. The wait instructions are untouched.

METHOD CARDS AND GUIDANCE CHAPTERS. The cloud-runner card gained a section on a second engine refusing to start, because an unattended box is exactly where two engines happen and the message a reader sees is new. It says what to do and that there is no stale lock to clear.

THE CONTRACT. Not one of the nine classes, and the most-read taught surface there is. Its tests section teaches the work account and now teaches that a `bound reached` outcome is the account giving up rather than a verdict, that the work may still report and replace it, and what the word after the figure means.

WHAT NEEDED NOTHING, checked rather than assumed.

MATRIX ROWS. The rigor matrix decides which states are struck. This record changed no state and no phase.

TEMPLATES AND SKELETONS. No form template changed shape. The fields this record added ride existing templates.

BOOK CHAPTERS. Searched for every term this record could have made stale. Nothing in the book teaches background work.

PANELS AND FORM HELP. The panel does not render a job's outcome. The `outcome` field in the call log and the one in the machine history are separate namespaces from the entry's, and neither was touched.

ONE STALE REFERENCE FOUND AND FIXED OUTSIDE THE CLASSES. A story cited the startup reaper by line number, and the line had moved. It now names the moment instead.
