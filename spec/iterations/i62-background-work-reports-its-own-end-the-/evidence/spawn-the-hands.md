---
form: spawn-the-hands
judgment: passed at 2026-08-24T15:38:43.311Z
by: agent
signed_off: 2026-08-24T15:36:25.656Z
authors: agent
files:
---

# Evidence form / spawn-the-hands

## current_situation

Iteration i62 stands open at its first milestone, M0.

The goal is that background work reports its own end. The engine should hold the live end of everything it launched and ping it on a fixed interval. Silence past the interval ends it. A test run should close its own entry when its process exits.

Two faults of the same family ride along. Two engines can bind one folder and one port. Registering a spawned hand is refused in most states, because the verb that does it is not legal there.

This state starts the walker for M0 and nothing else. A reviewer spawns at its own gate, and a researcher at whatever state asks for research.

The walker is already running. It is registered as agent-mt7ebiwl and it shows on the work account of every lane call, standing at iterations/i62/spawn-the-hands.

## hands

- [x] Walker (deliverable/machines/methods/meth-spawn-hands.md)

## follow_up

The walker keeps pulling. It works whatever the machine serves next in M0, without aiming at a named state.

No hand is spawned ahead of its state. The M0 gate registers its own reviewer when the walk reaches it.

No notes are parked from this state. Nothing here found a stray.

## anything_else

The method card warns that this state is read-only, so a refusal on a write here is the design rather than a fault. Nothing was written outside this form.
