---
form: scope-non-goals
by: agent
signed_off: 2026-08-24T15:53:03.950Z
authors: agent
files: null
---

# Evidence form / scope-non-goals

## current_situation

The delta is framed against vp-autonomy-range, with two pass lines added to it.

The standing product scope is inherited untouched. Only this delta is scoped here.

The kickoff already named a pulled-in and a left-out list. This state restates neither: it says how much of the delta THIS record takes on, which is the narrower question.

## scope

THE WHOLE OF THE DELTA IS TAKEN ON, with one carve-out named below.

WHAT LANDS AS WORKING CODE.

- The engine holds the live end of every process it launched.
- It asks each one, on a fixed interval, whether the process still exists.
- Silence past the interval ends the process and closes its entry.
- A run closes its own entry when the process behind it exits.
- A wait carries a duration, and expiry does something rather than nothing.
- One engine holds a given folder and its port, and a second that cannot bind says so and stops.
- Registering a spawned hand is legal from anywhere inside a record.

WHAT LANDS AS A RULING RATHER THAN CODE.

- How long a completed task's file is worth keeping. The number is decided and written down here.

WHY THAT ONE IS DIFFERENT. The record's own vision asks for the decision first and the clearing second, in that order. Building the sweep before the number is agreed would bake a guess into a file that deletes things.

## non_goals

- THE SWEEP THAT CLEARS COMPLETED TASK FILES. The retention length is decided here; the code that acts on it is not written here. The pile stood at 598 one morning and 1,245 that afternoon, so this is deferred with the cost known rather than unnoticed.

- THE ROUTE, THE WALK'S OWN STATE, AND THE PROSE AN AGENT IS SERVED. The record's vision names these as out by construction, and nothing in this scope reaches them.

- THE TIMING OF THE HELPER BUDGET. The setup state asks for a hand before the gate that agrees one, so at the first milestone the request is always refused. It is a real fault and it is a standing work token, wt-the-opening-phase-of-a-record-asks-for-a-helper-before-any-b, rather than scope here. This record widens WHERE the registration may be called; it does not change WHEN the budget is set.

- A STRONGER PING THAN EXISTENCE. Asking a process whether it is making progress would catch a hang as well as a death. It needs the process to cooperate, and this engine launches shells and test runners that were never written to. The ceiling already answers the hang.

- RETROFITTING THE ENTRIES ALREADY ON DISK. Nothing here goes back and closes historical entries. They are cleared by the retention decision when its sweep is built.

## follow_up

The motivation gate comes next, and past it the vision is axiomatic.

One non-goal carries a cost that a later reader should be able to find: the completed-task pile keeps growing until the sweep is built. The retention ruling made in this record is what that work will need.

## anything_else

