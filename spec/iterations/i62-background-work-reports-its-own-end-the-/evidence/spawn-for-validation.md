---
form: spawn-for-validation
judgment: passed at 2026-08-24T17:14:51.342Z
by: agent
signed_off: 2026-08-24T17:14:50.202Z
authors: agent
files:
---

# Evidence form / spawn-for-validation

## current_situation

The implementation gate is signed and blessed, and the tree is green at rest: 1,816 cases, 157 suites, no failures.

This record's walker ceiling is zero. Its kickoff gate says so and gives the measurement behind it: three hands each spent about fifteen minutes rebuilding context the guide already held, for edits the guide had already located by file and line.

This session measured the same thing a fourth time. One hand was spawned at the setup state, ran four and a half minutes, and its only surviving output was one evidence form that took a single call to fill.

The owner ruled it directly during this record: if the ceiling is zero, go without one. So no walker is started here.

A reviewer is a different purchase and does not count against that number. One was spawned at the implementation gate and it paid for itself twice over, finding seven defects in the first build and five more in the fixes for them. The validation gate gets the same treatment.

## hands



## follow_up

- SPAWN A REVIEWER AT THE VALIDATION GATE, not here. A spawn state starts the walker and nothing else, and the reviewer belongs at the gate where the review happens. Ready when the walk reaches that gate.
- NOTHING ELSE IS PARKED BY THIS STATE.

## anything_else

WHY THE MOTIVATION'S ARGUMENT STILL HOLDS WITH NO WALKER. The form's own reasoning is that a hand which built the thing already believes it, so a fresh reader is what validation needs.

That argument is about the REVIEWER, and it is honoured. What it does not argue for is a walker, which rebuilds context rather than supplying a fresh judgment.

The two hands answer different questions, and only one of them is capped.
