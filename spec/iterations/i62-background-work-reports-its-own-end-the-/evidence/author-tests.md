---
form: author-tests
by: agent
signed_off: 2026-08-24T16:20:25.919Z
authors: agent
files: null
---

# Evidence form / author-tests

## current_situation

The structure is decomposed and the build has not started. This state authors how the six rows will be verified, before any of them is built.

One spec covers all six, because every row's verify method is test and they answer for one concern: an entry's life. A row needing a second method would get a second spec, and none does.

Eight cases stand in one file. Seven are red and one is green, and the green one is mapped honestly rather than claimed.

## checks

- tsp-background-work-reports-its-own-end

## follow_up

The build realizes this spec, and the next state observes the red before anything is written.

THE TYPECHECKER ALREADY FOUND THE FIRST GAP, and it is recorded in the test file rather than worked around. openOperation takes no way to name a live process, so the registry cannot hold the live end of what it registered. The test names that shape through a cast, so the gap is written down.

WHAT THE BUILD OWES, in the order the cases will go green: a live end on the registration, a record when two closers disagree, a bound on every entry, a workspace take, and a gate exemption for the hand registration.

## anything_else

WHY EIGHT CASES AND NOT SIX. Two rows earned a second case each, and both pairs exist for the same reason: one case alone can be satisfied by a wrong design.

THE SILENCE PAIR. An entry whose process is gone must settle; an entry whose process is alive and silent must not. An implementation that settles on silence passes the first and fails the second. Written as one case, the ruling that separates liveness from responsiveness would live in a comment.

THE SETTLE PAIR. The first outcome stands, and a disagreement is still recorded. Keeping only the first quietly would pass a test that asked only about the outcome.

WHAT NO CASE HERE COVERS. The Windows half of two probed assumptions. The spec says so in its own last section rather than leaving the reader to notice.
