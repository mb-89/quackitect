---
form: frame-delta
by: agent
signed_off: 2026-08-24T15:52:37.866Z
authors: agent
files:
---

# Evidence form / frame-delta

## current_situation

The register is open with seven entries. The frame is otherwise unchanged, so this state inherits rather than mints.

At minor the gap claim and the why-now are dropped mechanically. They stand from the resident frame.

What is left to do here is name which standing proposition the delta extends, and write the pass lines it adds.

## value_props

- vp-autonomy-range

## business_case

NO NEW PROPOSITION IS MINTED, and none is needed.

vp-autonomy-range already carries the criterion this record answers to: an unattended walk stops at gates and nowhere else, with a target of zero non-gate stops.

A FINISHED RUN THAT KEEPS REPORTING ITSELF AS RUNNING IS EXACTLY A NON-GATE STOP. It held a walk at the repair step for nineteen minutes with no process alive, and nothing the agent could call would clear it. That is the proposition's own metric going the wrong way, not a new promise.

TWO PASS LINES ARE ADDED to that proposition, because the standing ones could not have caught this.

- No walk is held by a record of work that has already finished. Metric: entries still marked running with no process behind them. Target: zero.
- Every wait an unattended walk enters carries a duration, and expiry acts. Metric: waits with no duration and no expiry behaviour. Target: zero.

WHY THE SECOND ONE IS THERE. A wait that nobody is watching is the same fault one level up: the first pass line catches a dead process, and this one catches a walk waiting on nothing at all.

WHAT THIS DOES NOT CLAIM. vp-the-engine is the parent vision and it is untouched. Nothing here changes what a drawn machine means, who blesses, or what evidence is owed.

## follow_up

The next state sets scope and non-goals against the delta.

One pointer is owed to a later reader: the two new pass lines live on spec/trace/value-prop/vp-autonomy-range.md, not in this form. The artifact is the truth and this field points at it.

## anything_else

