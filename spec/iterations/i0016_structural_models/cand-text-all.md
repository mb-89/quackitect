---
id: cand-text-all
type: candidate
axis: format-split
ratings:
  crit-editor-truth: 0.7
  crit-agent-fluency: 1.0
  crit-extractor-cost: 0.6
  crit-delta-ergonomics: 1.0
  crit-render-book: 0.9
  crit-diff-versioning: 1.0
  crit-spatial-meaning: 0.7
statement: Small text languages carry every kind: Mermaid for state, sequence, and trees, the layer list for rings. Layout is always computed. Spatial reading comes from derived renders.
class: review
killer: false
---
## Rationale (not load-bearing)
Weighted 0.835 - THE LEADER after the M3 review rounds (owner 2026-07-09: "trees are also very easy in text"; the i14 onion proved layers+flow derive from a list). Editor rose twice on the owner's own testimony: derived renders satisfy spatial reading, and the settled truth syntax mirrors the owner's own TikZ habit - ELEMENT-MAJOR declarations (one line per element: id, layer attribute, responsibility), then the order section, then flows on declared names; coordinates refused, layout derived. Elements are ALLOCATED AHEAD of code; the design-marker id is the join; sky-fall and absence lints close the loop both ways. Carried rider for M4: canvas formats stay ADMISSIBLE per kind via the views-chosen decision when arrangement is authored meaning (physical kinds, mechatronics wave) - the tray/groom requirements stay dormant Where-conditionals until then.
