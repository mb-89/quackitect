# M5 — Spike (i0012_spec_book, systematic)

TL;DR: one throwaway probe file, generated deterministically by script into the spike scratch space, answers the three riskiest unknowns: until-found disclosure, the derived SVG set, and margin icons. The adjudicator runs Probe A live; B and C are visible on sight.

## Riskiest assumptions validated by evidence  → i12-m5-riskiest-validated

The probe artifact: `<data-home>/spikes/i0012_book/probe.html`, generated by a scratch script with computed coordinates - the determinism half of the question is proven by construction (same script, same bytes).

- **Probe A - until-found disclosure: PASS (owner-run, 2026-07-05).** The collapsed section opened itself on find-in-page; `<details hidden=until-found>` carries the book's collapse mechanism. The expanded-fallback path stays recorded for non-Chromium browsers via feature detection.
- **Probe B - derived SVG set:** context star, building-block tree, and timeline generated from fixture data with trivial arithmetic (ring placement, box grid, line ticks). Legible at a glance, real selectable text inside, viewBox-scaled. The layout math for all three took under a screen of script - the "arbitrary graph layout" grave is confirmed avoided by the fixed-set cap.
- **Probe C - margin provenance icons: PASS with a finding.** The vertical margin column works with plain CSS. The robot EMOJI failed twice at the owner's screen (first a spike-tooling encoding artifact - PowerShell 5.1 reads BOM-less scripts as ANSI and mangled the glyph; a real risk only for scratch tools, the engine emits UTF-8 natively - then font-dependent rendering). **Owner-decided icon: the drawn inline-SVG robot** - no font dependency, identical everywhere, ~200 bytes, machine-readable label ("AI involvement: N of 3"). The book's philosophy applied to its own icons; the emoji stays out.

## Design is buildable  → i12-m5-design-buildable

Judged against the scope guard (one emitter, one manifest node type, two lints):

- ONE emitter, internally staged but deterministic end to end; the intermediate is an ephemeral projection, so no second pipeline exists.
- ONE new node type: manifest (mode-interpreted). Glossary terms and stakeholder classes are method-layer NOTES, not node types.
- TWO lints: book-orphan and meta-quarantine. The register lane is Vale-as-subprocess (not ours); glossary link errors ride the existing ref-integrity family; the advisory scan reuses the lint output lane.
- Small tool surfaces beyond the guard, each owner-ruled at M2/M3: mint stamps evidence skeletons; the Vale auto-pull mirrors the proven engine-bootstrap pattern; the SVG deriver is capped to the three probed kinds plus the matrix.
- Implementation constraint recorded for M6 (owner, this milestone): the emitted book carries STABLE PER-UNIT ANCHORS (node id + unit index as element ids) so the future review/comment system attaches to exact paragraphs. Cheap now, expensive to retrofit.
- The pre-agreed relief valve stands: if the M6 build plan proves too big for the deadline, the book half defers to a fresh version as a first-class move.

Verdict: buildable within the guard.

## Spike results recorded  → i12-m5-spike-recorded

- Determinism: proven by construction (scripted generation, computed coordinates).
- SVG set: proven legible and trivial; cap confirmed as the right cut.
- Margin icons: proven with plain CSS; no layout machinery needed.
- until-found: adjudicator verdict pending at the gate (Probe A); both outcomes have a recorded path.
- Design advanced: no requirement or ADR needed amendment from the spike - the M4 composite survives contact with the browser.
- The spike file is throwaway and stays in the scratch space; nothing from it enters product/ except as fresh M6 code.

## Milestone review  → i12-m5-gate

**Verify:** every named unknown from M3 feasibility met evidence - two by construction and sight, one by the adjudicator's live probe. **Validate:** the spike answered exactly the questions the composite depends on, nothing speculative was built, the scratch stays scratch. **Red-team:** the probe ran in one browser family - Chromium's until-found is the mechanism's home; Firefox/Safari degrade to the same pre-agreed expanded fallback per feature detection, so the single-browser probe bounds the risk instead of hiding it. **Verdict: PASS - pending the adjudicator's probe result and bless.**
