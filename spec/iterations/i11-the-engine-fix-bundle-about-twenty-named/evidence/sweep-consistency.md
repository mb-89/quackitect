---
form: sweep-consistency
by: agent
signed_off: 2026-08-16T13:50:10.680Z
authors: agent
files: null
---

# Evidence form / sweep-consistency

## current_situation

EVERY SURFACE THAT TAUGHT A CHANGED BEHAVIOUR NOW TEACHES THE CURRENT ONE. Four documents carried superseded text and all four are fixed.

### What the iteration changed

The test verb's shape, the battery's owner, the pull's payload, the file verbs' argument names, se_amend's shape, the narration stall, the delete's answer, the route's weighing, se_why's answer, a new control, a new refusal, and two exit scripts.

### The classes that needed work

COMMAND AND TOOL DOCS. `se_test`, `se_amend`, `se_file_delete` and `se_run` each describe behaviour that moved, and each description moved with its chunk rather than here.

GUIDANCE CHAPTERS. `AGENTS.md` and `CLAUDE.md` are one text served to two hosts, so every edit landed in both, and `walking.md` carries the same passages as their source.

- TESTS taught "scoped runs are the default and the battery is EARNED". Both halves are superseded: a scoped run is now the only one an agent makes, and the battery is nobody's to earn — it is the engine's, at verification, and no state lets an agent ask for one.
- THE TRUNCATING PIPE taught "PREFER structured results over shaping it in the shell". It is a refusal now, SE-C-137, and the passage names the shapes, the pipe-filter case, the verb each remedy points at, and the `no_tool_reason` door.
- THE PERSON'S HAND listed three controls. The stop-at dial is a fourth, and the passage says how the two dials differ and that `state end` holds transitions without the person ever walking.
- THE CHAINED BRIEF taught that a chain becomes a plan. A fork's chain stays a FORK now, with the reason on the page.
- THE STALL was never written down at all. Five warns, twelve refuses, and the gap is the grace.

`refusals.md` gained SE-C-137 whole, and SE-C-101, SE-C-120 and SE-C-133 were rewritten as their behaviour moved.

METHOD CARDS. `method/lane.md` taught the pipe as a hazard to avoid. It opens with the refusal now and keeps the explanation of what a pipe cuts underneath, because the reason is still the reason.

MATRIX ROWS. `M7_30_observe-red` and `M7_50_verification` were rewritten with their chunks. Verification's now says it grants no test verb ON PURPOSE, which is exactly the trap this record walked into.

PANELS AND FORM HELP. `panels/controls.md` gained the stop-at row and the reason its label sits on the row rather than on each button. `machines/stopat.md` is new and holds the notches, so editing it moves the buttons.

ENGINE-SERVED STRINGS. Every string i11 added was written today, so it teaches the current behaviour by construction. One was still wrong: `stopat.md` said "open a RECORD unasked", the generic the voice doc names as a forbidden word precisely because the engine coins it and then teaches it. It says "an iteration" now.

### The classes that needed nothing, checked rather than assumed

TEMPLATES AND SKELETONS. The form templates describe shapes, not behaviours, and no shape moved — the checklist still takes `- [x]`, `- [ ]` and `- [owed]`.

README AND ENTRY DOCUMENTS. They teach none of the changed behaviours, and entry documents carry no method jargon by the owner's law.

BOOK CHAPTERS. There are none. The glob over the book folder returns zero files; the book is i20's and unbuilt. The box is checked as vacuous rather than left to look swept.

THE LANE TABLE. Every verb kept its name. The argument repair understands a sibling's word without renaming anything.

## swept

- [x] command and tool docs
- [x] engine-served strings
- [x] method cards
- [x] matrix rows
- [x] templates and skeletons
- [x] guidance chapters
- [x] book chapters
- [x] README and entry documents
- [x] panels and form help

## follow_up

NOTHING BLOCKS. gate-validation is next.

ONE THING THIS SWEEP FOUND THAT IT DID NOT FIX. The forbidden generic reached a new served string on the day it was written, which is what the voice doc predicts: "correcting prose fixes the symptom; sweeping the served strings is the fix". One occurrence was caught here because the sweep looked. Nothing mechanical looks.

THE CONTRACT IS SERVED TWICE, as AGENTS.md and CLAUDE.md, and they are kept identical by hand. Four edits landed in both this round and a fifth would have been just as easy to miss. Worth a lint that refuses a drift between them.

## anything_else

