---
form: audit-the-twenty
by: agent
signed_off: 2026-08-16T13:12:52.495Z
authors: agent
files: null
---

# Evidence form / audit-the-twenty

## current_situation

TWELVE OF TWELVE CHUNKS ARE BUILT. i11's build is complete.

WHAT THE AUDIT CHANGED ABOUT THE BUNDLE. The seed listed about twenty defects. Ten are struck as fixed — five of them by this iteration. Four were fixed here, two of which were LIVE despite the seed being three days old and one of which I introduced myself. Sixteen are recorded live.

THE SEED'S OWN WARNING HELD UP. It said i34 had rewritten several of these areas and that some defects might be gone and one made worse. Both were true: the worktree item is gone entirely, and the security row had been half-fixed in a way that left its refs lying.

WHAT I DID NOT DO. I did not probe every one of the sixteen. Four are marked UNPROBED in the register entry, by name, rather than being given a verdict they did not earn.

## built

THE SEED'S LIST READ AGAINST THE SYSTEM i34 LEFT. Each item struck with its evidence, fixed, or recorded.

THE SEED SAID TO DO THIS: "Acting on a stale list without checking what still stands is precisely the failure that cost i34 the most, and repeating it here would be the joke writing itself."

### Struck — gone, with the evidence

NO IN-FLIGHT TREE SWITCH BETWEEN WORKTREES OF ONE PRODUCT. i34 deleted worktrees whole; every record is a folder on trunk. `gitlane.ts` names worktrees only in history comments. There is no second tree to switch to.

THE FILL ANSWER RETURNS THE WHOLE FORM TWICE. Fixed by `pull-payload` this iteration — the echo is a receipt now.

THE PULL MUST NEVER OVERFLOW. Same chunk: 290,280 bytes to about 4KB, measured on this walk.

se_why NAMES THE ROOT IN ONE ANSWER. Fixed by `walk-repairs`.

AN AMEND PATCHES, NEVER RESENDS. Fixed by `lane-shape`.

THE FILE TOOLS DISAGREE ON THEIR ARGUMENT NAMES. Fixed by `lane-shape`.

TESTS ARE ASKED FOR, NEVER POLLED, and THE FULL BATTERY IS THE ENGINE'S. Both fixed by `test-verb`.

A FINDING ROUTES INTO A BUCKET. Fixed by `the-bucket`.

BEFORE A NODE GOES, NAME WHAT POINTS AT IT. Fixed by `deletion-names-dependents`.

TWO BUTTONS ON THE MIRROR. Superseded by the owner's four-notch stop-at dial and built as that.

### Fixed here, because the audit reproduced them

THE MAP LIES — the seed's second do-first item, and it was LIVE. A red was written before the fix: at a dial of 0.2, `route("expeditions")` returned NO `stops_at` at all, so the line drew open the whole way and the walk then stopped. Entering a container lands on its START state, which is mechanical, so the container's own 0.4 was never weighed. `entryWeight` now charges the door's weight to the container's initial state — and only to that one, since charging every state within would shut a container from the inside. The route now stops at `expeditions/start` and says "operational work, above this session's mechanical".

THE STALE SECURITY ROW — the first do-first item, and HALF of it was already fixed. Its Detail no longer opens "THE DEMAND IS NOT MET TODAY". Its `source_refs` still cited the broken call as evidence, for an iteration after the fix. Both are corrected, and the Detail no longer narrates verification status at all — which is what made it go stale twice.

A FAILING TEST LEAF LOSES ITS MESSAGE — measured three times TODAY, each one a logged shell escape from the lane that exists to replace it. `parseTap` matched `^not ok` with no leading space, so a failure inside a `describe()` was invisible: TAP indents the child and reports the parent as "1 subtest failed" with the suite's location. It now reads every depth and DROPS the roll-up where a leaf survived it — while keeping the parent when nothing more specific was captured, because a roll-up beats silence.

A BATTERY-REFUSAL REGRESSION OF MY OWN. `nesting.test.ts` drives a full battery from the front desk to prove the battery's SHAPE, and `test-verb`'s refusal bit it. It passes `force: true` now, with the reason in place. I had not run that suite when I built the refusal.

### Recorded — live, and off this surface

raid-iss-the-seeds-live-defects-outlive-i11 carries them: sixteen items, each with what is known and what is UNPROBED said as unprobed rather than guessed.

THREE ARE WORTH NAMING HERE.

- THE ENGINE CANNOT BE ASKED HOW IT IS. No health verb exists; `se_run {job}` asks a job, never the engine. Three crashes were diagnosed through the process table.
- A NEW VERB IS UNREACHABLE IN THE SESSION THAT BUILT IT, and it matters for this iteration specifically — i11 added `ops` to se_amend and a callback to the run lane, both exercised only through in-process test servers.
- THE PROBE-ASSUMPTIONS FORM CHECKS NOTHING, and it is blocked on an owner ruling rather than on work.

### The shape worth naming

READING AN ABSENT VALUE AS AN ANSWER, three times in one chunk and once in another: a blank `minted_in` read as ours, an absent fold-back read as "folded nothing back", an unresolvable record read as a confident owner, and an empty target read as "never aimed". The third was introduced while fixing the first.

### Green

45 of 45 across `route`, `nesting`, `containerchoice`, `boot`, `branch-return` and `clear-jump` — run `test-msvtncaf-8`. 43 of 43 in `discipline.test.ts` — run `test-msvtpqpc-10`.

TWO NEW CASES, both written from a real failure. The container door drove a red before it drove a green. The TAP leaf is driven against a fixture of the exact output that hid three messages today, and asserts BOTH halves — the leaf survives, and a lone roll-up still reports.

## follow_up

NOTHING BLOCKS THE WALK. The build is done and verification is next.

ONE THING THE OWNER SHOULD SEE. The stop-at dial's buttons drew wrong when first reported, for two reasons, and both are fixed: the panel code had changed after the last reload, and `stopat.md`'s notch lines were WRAPPED, which the rungs parser reads as malformed. The panel tests use a hand-written bank, so they stayed green while the shipped file was unreadable. `scale.test.ts` now loads the real file, which is the check that would have caught it.

ONE RULING IS OWED, and it is recorded rather than asked here because it blocks nothing: whether a standing assumption's probe list should strip live items the way a retro's does.

## anything_else

