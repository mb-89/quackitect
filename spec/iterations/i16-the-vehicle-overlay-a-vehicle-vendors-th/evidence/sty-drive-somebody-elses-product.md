---
form: sty-drive-somebody-elses-product
by: agent
signed_off: 2026-08-18T20:17:34.088Z
authors: agent
files: null
---

# Evidence form / sty-drive-somebody-elses-product

## current_situation

NOT PERFORMED, and this one would FAIL if it were.

NOTHING READS THE DRIVEN RECORD AT STARTUP. `produceProject` writes `driven-by.json` and `drivenBy` can answer the three states of a tree, but no lane verb, no route and no command calls it. A function nobody calls says nothing.

SO THE HONEST STATE IS WORSE THAN UNPERFORMED. The tester found this and it is recorded as note-c1c3a1142cb1. The record is written and readable; the coming-up-in-it half does not exist.

## built

NO REPORT, AND A KNOWN NEGATIVE RATHER THAN AN UNKNOWN.

WHAT IS BUILT: the project producer, the record naming which copy drives the tree by identity, and `drivenBy` answering present, absent or unresolvable.

WHAT IS MISSING, precisely:

- nothing calls `drivenBy` at startup
- an identity cannot be resolved to a tree, because no register of seen copies exists
- so even wired, the third state is unreachable

BOTH ARE RECORDED: note-c1c3a1142cb1 for the reachability, and the resolution gap is named on raid-dec-a-driven-tree-names-which-copy-drives-it as a consequence it always had.

## follow_up

READY WHEN two things land, and neither is large.

- A VERB THAT CALLS `drivenBy`, so a tree can be asked what it is.
- A REGISTER OF SEEN COPIES, so an identity resolves to a tree. Where it lives is the same open question as where a vehicle's overlay content lives.

UNTIL THEN THIS SLIDE IS FALSE and saying so is worth more than leaving it blank.

## anything_else

THIS IS THE GATE'S KILL-CRITERION, arriving exactly where the gate said to look for it: "this is the wrong call if a produced vehicle cannot actually be used on another machine."

IT IS NOT SPECULATION NOW. The chain stops at the same place the red team predicted, for the reason it named.
