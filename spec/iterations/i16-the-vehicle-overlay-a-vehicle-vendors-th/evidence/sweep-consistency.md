---
form: sweep-consistency
by: agent
signed_off: 2026-08-18T20:48:50.580Z
authors: agent
files: null
---

# Evidence form / sweep-consistency

## current_situation

i16 built the two producing acts and shipped them. The engine makes a vehicle, which is an independent renamed copy of itself with a minted identity. A vehicle then makes a project it drives.

The walk only reached this state after a drawing had to change.

THE VALIDATION BUSBAR STARVED. This row used to fan from fill-story-evidence, beside run-demos. A fan hands out one leg and reports the rest as not walked. run-demos is a SUBMACHINE, so walking it leaves the walk at that submachine's `end`.

The engine's escape for an unwalked leg is joinStuck, in session.ts. It asks whether the state the walk stands on owes a form. An `end` owes none. So the offer was never made, and every legal move was closed: the pull refused SE-C-123, a choice refused SE-C-110, and the router drew a route the whole way round the record while moving zero hops.

THE DRAWING WAS FIXED, NOT THE WALKER. The refusal names that remedy itself. This row now depends on run-demos, and the row body says why so nobody restores the fan. The engine defect is recorded with its fix as note-ec92cfa43897.

## swept

- [x] command and tool docs
- [x] engine-served strings
- [x] method cards
- [x] matrix rows
- [x] templates and skeletons
- [x] guidance chapters
- [x] book chapters
- [x] README and entry documents
- [x] panels and form help

## follow_up

- note-ec92cfa43897 carries the engine defect and its fix. A fan's other leg cannot be offered when the walked leg was a submachine. Ready when an iteration opens on the walking core.

- The gate-validation row keeps both run-demos and sweep-consistency in depends_on. That run-demos edge is now transitive. It is harmless, because run-demos is always filled before this row completes. Left as drawn.

- `.se/HANDOVER.md` is stale. It still reports the walk standing at i16's onboard-retro. It is rewritten before this session ends.

- The two senses of "vehicle" want an owner ruling. Recorded below and left for the retro.

## anything_else

THE BOOK CLASS HAS NO FILES. There is no book directory anywhere under `project/`. Emitting the book is i20, still unopened. The box is ticked because the class is empty, not because documents were read. Globbed four times across two agents.

### What was corrected

- The lane chapter and the retro chapter taught a declared root as read-only. i16 made one writable, and that writable door is how the engine drives a project that is not itself.
- The prompt layer was re-projected into all three host files.
- Six engine surfaces taught the same superseded rule. The `se_file_read` description, the SE-C-102 remedy note, the paths.ts header, the `resolveDeclaredRoot` doc comment, the resolve.ts comment, and the SE-C-102 guidance section.
- `se_file_write` and `se_file_patch` never named the writable root at all. An agent could only learn the door existed by hitting the refusal. Both now say it.
- SE-C-102 said there are two doors outside the root and no third. A producing act writes into the tree it is making, which is neither. The rule now says it governs the paths you pass, and points at SE-C-141.
- The root README told a driven project to run `RUNME.ps1`. A driven project carries no installer. That block is gone, and the orphaned pull paragraph got its own heading.
- `meth-emit-back` taught that a vehicle overrides through an overlay the import never touches. The owner overturned that on 2026-08-18. Nothing is sealed, and a vehicle owns everything it carries.
- `version-planning.md` said i16 waits on i10. That edge was cut on 2026-08-18.
- The travelling README told a copy owner to edit `brand.json` with no warning that `instance` is not a name.

### Residue, named rather than skipped

- Five voice-lint findings stand in files i16 did not change. Two in contract.md, one each in software.md, engineering.md and voice.md. Each is a long item or a sentence run. None teaches a superseded behaviour.
- "Vehicle" now carries two senses in guidance the same agent reads. i16 gave it the product sense, meaning a produced copy. The method already used it for the size of a piece of work, at front-desk.md, idle.md and front_desk.md. Not resolved here, because renaming a method term is the owner's call.

### How it was swept

Three subagents swept in parallel. One took the book and the spec prose, one the engine-served strings, one the method cards and matrix rows. Each reported with file and line. Each re-verified after files moved under them mid-sweep, which was this walk editing the same files.

THE BATTERY RAN TWICE AND WAS GREEN BOTH TIMES. Biome over 289 files, preflight, 1471 of 1471 tests across 141 suites, and the conformance sweep over 1484 nodes.
