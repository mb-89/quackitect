---
form: cand-nothing-but-a-channel
amended: 2026-08-18T13:52:14.307Z by agent — the probe conflicted with both versions present rather than failing to merge
by: agent
signed_off: 2026-08-18T12:51:47.839Z
authors: agent
files: null
---

# Evidence form / cand-nothing-but-a-channel

## current_situation

THE SECOND OF FOUR, and the cheapest line that does anything.

EIGHT OF ITS NINE CELLS ARE THE PREDECESSOR'S — the mirror-and-overlay layout, the layered path search, whole-file overrides, the declared overlay key, the executable finding its own home, production outside the lane. All of it ran for a year.

THE NINTH IS THE CHANGE, and it is one cell: the copy is a clone that keeps its history, so an update has a commit to merge from.

## built

cand-nothing-but-a-channel is composed. Three sections into the existing candidate note; nothing minted.

HOW IT WORKS names the seam that carries the whole line: the merge base. Nothing else in the design moves, and the clone is what lets the reference refresh report anything at all.

WHAT IT COSTS is where this iteration's own probe bites hardest. A copy that merely reordered a file's sections took a merge CONFLICT on an upstream change to a line it never touched. That is the GENTLE case, not the harsh one, and it is the failure mode that decides whether this line is usable.

CORRECTED AT THE CANDIDATES GATE, 2026-08-18, where this read "could not merge". The run printed `upstream change landed: True` and `copy's own edit kept: True` beside `merge exit: 1`. Both versions are present and marked, so the cost is one human resolution per restructured file rather than lost work. That is a smaller cost than the earlier wording implied, and it is still the cost that decides this line, because it is paid on every update in proportion to how much the copy restructured.

AND ONE COST IS PAID SILENTLY AND FOREVER. Overriding one line means owning the whole file, so later upstream improvements to its untouched parts never arrive. Three vendoring systems in the survey document that consequence in their own words.

WHAT IT LEANS ON puts the load-bearing belief first: that copies do not restructure what they receive. Nothing in the design discourages it, and the owner has ruled that nothing is sealed.

## follow_up

IMMEDIATELY: the two remaining compose states.

ONE ASSUMPTION HERE IS CHEAP TO PROBE AND WAS NOT PROBED. Whether the reference refresh can compare a vendored source against its upstream and report anything useful. It is the mechanism this whole line reuses, and nothing established that it can do the job.

AND ONE QUESTION IS THE OWNER'S RATHER THAN A STATE'S. Whether a copy handed outside the team may carry the source's full commit history. The requirement only says the copy must run without NEEDING it.

## anything_else

