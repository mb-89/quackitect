---
form: derive-functions
judgment: passed at 2026-08-24T16:23:42.571Z
by: agent
signed_off: 2026-08-24T15:15:26.738Z
authors: agent
files:
---

# Evidence form / derive-functions

## current_situation

THE STRUCTURE EXTENDS AND NOTHING IS RE-DERIVED. Two standing functions take the delta's four requirements between them, and no function is minted.

NOTHING ESCALATES. This state's guidance says a new function fitting no existing cluster is the architecture moving and the tell of a major. No new function was needed, so the size holds.

WHY THE MAPPING WAS OBVIOUS RATHER THAN ARGUED. The walk's forward verb already carries both resident speed rows, and its own rationale says why: the bound sits there because it is a property of that function, and nothing else in the tree is on the driver's critical path.

## functions

- fn-run-a-governed-walk.serve-a-step
- fn-run-a-governed-walk.show-where-it-stands

## flows

- none

## neutrality

ALL FOUR ROWS PASS THE ONE QUESTION: could two honestly different designs both do this?

- THE BARE AIM RETURNS WITHOUT WALKING. It says nothing about where the direction is stored or how the walking is later triggered.
- A HOP COMPLETES INSIDE ITS BUDGET AND RECORDS ITS DURATION. It names no cache, no memo, no reuse check. Whether a passing hop keeps its verdict or is re-decided quickly is left entirely open.
- A FAILED SEARCH COSTS NO MORE THAN A SUCCESSFUL ONE. It names neither the search nor a bound on it, and the row says so in its own body.
- THE SURFACE KEEPS ANSWERING WHILE SOMETHING ELSE IS SLOW. It names neither a second process nor a queue nor a thread.

THREE OF THE FOUR REFUSE A MECHANISM IN WRITING, and each says why in its own detail section. That is deliberate: the round has not established which mechanism is right, and freezing one into an obligation is the failure this product's authoring card names.

THE ONE PLACE A SOLUTION NEARLY SLIPPED IN. An earlier draft of the aiming row said the walking shall happen on the caller's NEXT request, which names a mechanism and would have removed a capability the owner had already ruled on. It was corrected the same day on their word.

AND THE SUBTLEST TELL WAS CHECKED. Asked what each row would be called in a design we rejected, all four keep their names, because none of them describes how the work is arranged.

## follow_up

THE ASSUMPTIONS SWEEP COMES NEXT, and the register already holds one that this step sharpened.

THE STANDING ASSUMPTION is that the slow tail and the undrawn route share one cause. Deriving functions did not touch it, and the sweep should not treat it as settled because it now has a requirement pointing at it.

WHAT THIS STEP ADDS FOR THE SWEEP TO CHEW ON. The freeze row leans on something nobody here controls: that the surface and the walk are served from one loop and will stay that way. That is an assumption about the host and it is not yet written down.

## anything_else

WHY THE FLOWS ANSWER IS NONE, since a bare none reads as a gap.

THE FORWARD VERB ALREADY CONSUMES the position, the compiled machine, the harness profile and the result of a check. It already produces an instruction, a refusal and a dispatched call.

THE FOUR NEW ROWS ADD NEITHER SIDE. Each bounds how long an existing act takes, or which of two answers it already gives. A bound on an act is not a thing that crosses a boundary between functions.

ONE THING BECOMES MEASURABLE THAT WAS NOT. A hop's duration has to be recorded for its budget to be checkable. That recording goes to the call log, and the log is not a function in this tree, so it is not a flow either.

AND ONE COUPLING IS NOW NAMED WITHOUT BEING A FLOW. The surface function and the forward verb are served from one loop, so the verb running long consumes something the surface needs. That is a shared resource rather than a thing passed between them, and it is exactly why the freeze row had to be written as its own demand.

THE DISTINCTION MATTERS FOR THE NEXT STEP. An output nobody consumes is a hole this method finds by writing the flows. A shared resource is invisible to that check, which is why it took a measurement to notice.
