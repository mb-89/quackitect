---
form: re-signing-an-answer-knocks-down-what-rests-on-it
by: agent
signed_off: 2026-08-24T17:58:26.963Z
authors: agent
files: null
---

# Evidence form / re-signing-an-answer-knocks-down-what-rests-on-it

## current_situation

A work token says re-signing an answer knocks down what rests on it only sometimes: one case worked and one did not on the same day. It records the symptom and not the shape.

NOTHING WAS BUILT FOR THIS CHUNK, and this form says so rather than dressing an observation as a build.

## built

NOTHING WAS BUILT. What follows is what was OBSERVED, which is a different thing and is labelled as one.

### The guard fired every time it was exercised today

Three feeders were re-signed during this round, and each time the knock-down fired and the engine named the whole chain.

- `author-tests` re-signed — `specify-build` fell, and the answer said: its own content still passes, it is down because author-tests was RE-SIGNED after it, so it answered older ground.
- `specify-build` re-signed — `observe-red` fell, named the same way.
- `observe-red` re-signed — the walk moved on and the chain cleared.

EACH ANSWER NAMED THE ROOT, THE PATH FROM IT, AND THE CALL THAT FIXES IT. That is the mechanism working end to end, with a reader who could act on it.

### Why that is not enough to close the chunk

THE TOKEN'S FAILING CASE HAS NO REPRODUCTION. Three cases firing correctly does not answer which fourth case does not, and the token's own phrase points at direction: "whichever way the walk happens to be pointed".

CHOOSING A FIX AGAINST AN UNKNOWN CAUSE IS THE MISTAKE THIS ROUND ALREADY MADE ONCE. A plausible cost was found and code was changed before anything measured whether it was the cost, and it had to be reverted. The same reasoning applies here with LESS evidence, not more.

### What stands instead

[raid-iss-the-knock-down-has-no-reproduction-of-the-case-that-fails](spec/trace/raid/raid-iss-the-knock-down-has-no-reproduction-of-the-case-that-fails.md), open, with the two calls that would settle the direction the token suspects.

It says in its own words what must not happen: this chunk must not be marked built, because the honest standing is that its cause is unknown rather than that its work is done.

## follow_up

THE REPRODUCTION IS TWO CALLS and it is the next thing.

Re-sign a feeder with the walk pointed AWAY from the dependant, and again with it pointed AT the dependant. If the two answers differ, the token's suspicion is confirmed and the fix aims at routing rather than at claims.

WHY IT WAS NOT DONE HERE. Re-signing feeders mid-walk moves the live record, and doing it to run an experiment would mix the experiment into the round's own evidence. It belongs where a record can be stood up and thrown away.

THE GATE SHOULD RULE on whether an unbuilt chunk with an open issue and a named next step is an acceptable standing for this round, or whether it holds the gate.

## anything_else

