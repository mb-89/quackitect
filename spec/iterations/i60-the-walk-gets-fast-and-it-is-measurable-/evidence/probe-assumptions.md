---
form: probe-assumptions
by: agent
signed_off: 2026-08-24T15:20:15.754Z
authors: agent
files:
---

# Evidence form / probe-assumptions

## current_situation

THREE STANDING ASSUMPTIONS WERE PROBED, all three opened by this round, and one of them held on real data.

THE FOLDER WAS THE INPUT, not the state above. Every standing assumption in the register was considered, and the three this round opened are the ones whose triggers are live. Older entries carry their own dates and none of their triggers has fired.

WHAT THE ONE CONFIRMED PROBE CHANGED. It did not only settle its own entry. It strengthened a rival mechanism inside a second entry, and it explains why the third could not be checked from here at all.

THAT CHAIN IS THE RESULT OF THIS STATE. Probing one assumption moved two others, which is what an addressable register is for.

## probes

- raid-asm-the-engine-and-the-surface-go-on-sharing-one-loop.probe: HOLDS. Run on recorded data with no new instrument. Long engine calls cover 4.4 per cent of the window and 89 of 453 surface slow-reports overlap one, which is 19.6 per cent. Two records are one wait measured at both layers, 44,067 against 44,065 and 20,015 against 20,010 milliseconds.
- raid-asm-the-recorded-duration-is-what-the-caller-actually-waited.probe: UNPROBED at the real channel, reason recorded. The definitive check times a lane call from outside with an independent clock, and it cannot run from inside because calling the lane from a shell the lane is running blocks the loop the call needs.
- raid-asm-the-recorded-duration-is-what-the-caller-actually-waited.probed: NOT STAMPED, deliberately. A date here would say a check ran, and none did. Two timers in different paths agreed within five milliseconds on one wait, which supports it and does not settle it.
- raid-asm-the-slow-tail-and-the-undrawn-route-share-one-cause.probe: SCHEDULED to the spike milestone. The controlled probe holds a judgment live across both halves while varying only route-drawability, which is staging rather than minutes.
- raid-asm-the-slow-tail-and-the-undrawn-route-share-one-cause.probed: NOT STAMPED. The entry stays open. Its question also changed: both candidate mechanisms are now credible, so the spike separates their contributions rather than picking a winner.

## follow_up

THE SPIKE MILESTONE NOW HAS A NAMED JOB, and it is sharper than the one this round started with.

THE OLD QUESTION: which of three mechanisms makes the tail slow. THE NEW ONE: how much does contention contribute, and how much does the failed search, given that both are now credible and one has a confirmed mechanism.

AND ONE PROBE IS OWED OUTSIDE THIS SESSION. Timing a lane call with an independent clock needs a second shell while the session is idle. It is minutes of work, it cannot be done from in here, and every number this round rests on is a floor until somebody does it.

## anything_else

## fallout

NOTHING FELL, because nothing was falsified. No entry became an issue and no source_refs edge now rests on something known false.

ONE THING SHIFTED WITHOUT FALLING, and it is worth naming here rather than leaving in a body. The round is ordered around the tail assumption, and that assumption is now weaker as a single explanation than it was this morning. It is not false; it is incomplete.

WHAT RESTS ON IT. The requirement about a target that cannot be reached being refused quickly. That row demands an outcome and names no mechanism, deliberately, so it survives the shift untouched.

WHAT WOULD HAVE FALLEN IF THE SHARED LOOP HAD BEEN FALSIFIED. The freeze requirement, which leans on it entirely. The probe held, so it stands.
