---
form: specify-build
by: agent
signed_off: 2026-08-24T17:43:33.982Z
reopened: "2026-08-24T17:43:11.364Z — author-tests was re-signed after this state, so it answered older ground. What changed above it: three requirements were amended so each says what the spec under it actually checks, and the failed-route row was reworked on the owner's ruling because its measure was impossible. This state's own content also moved in the same pass — the chunk drawing gained the four items the phase brief says are owed and the first drawing omitted."
authors: agent
files:
---

# Evidence form / specify-build

## current_situation

The design specs stand as authored. Two things moved and both are named below.

### The chunk drawing gained four chunks

The first drawing had four, and they were exactly the four things that had already been built. A drawing written after the build lists what happened, so it cannot show what did not.

WHAT IT HID: four of the seven items this phase's own brief says are owed here were absent, and no form recorded their withdrawal. A cold reviewer found the gap by reading the brief rather than the drawing.

THE OWNER RULED THAT NOTHING LEAVES SCOPE, so the four are chunks now:

- `the-drawer-tells-deciding-from-failed` repays the standing debt.
- `a-record-that-holds-still-to-measure-against` is the yardstick.
- `re-signing-an-answer-knocks-down-what-rests-on-it`.
- `a-score-cell-can-say-it-has-no-evidence`.

### The requirements above moved

Three were amended so each says what the spec under it actually checks, and one was reworked because its measure was impossible to satisfy. None of that changes what is designed here or which elements the specs realize.

### One design spec gained a section

[dsp-the-walk-knows-what-its-own-hops-cost](spec/trace/design-spec/dsp-the-walk-knows-what-its-own-hops-cost.md) now carries the measurement that a hop has two halves: the drawing at about 8 milliseconds and the walking at about 5,400. Both are timed separately, and the code cites that section.

## design_specs

- dsp-the-walk-knows-what-its-own-hops-cost

## promotions

NONE. No spike ran and none could have: the prototype phase is not compiled at this size, so there is no promoted output to assign and nothing is lost by saying so.

THAT ABSENCE COST SOMETHING AND IT IS RECORDED. A probe was scheduled to that phase two states before the walk reached it, and the phase did not exist. The entry now points at implementation instead, with what the demotion costs written on it.

WHAT ENTERS THE BUILD PRE-VERIFIED INSTEAD. One repayment that a standing debt already describes in full, including why the narrow fix is the right one. That is not a promotion in the machine's sense and it is the nearest thing this round has.

## follow_up

### The four new chunks are the remaining build

Three of them are independent and fan out. `a-record-that-holds-still-to-measure-against` waits on the hop timing, because a yardstick is only useful once there is something to measure against it.

### The route-drawer repayment is the one that matters most

Its own entry carries the measurement: a route-failing pull ran past thirty seconds 36 per cent of the time, against 2 per cent for every other pull. That is the tail the owner has been feeling.

THE REPAYMENT IS NAMED IN THE ENTRY, so this does not need designing: give the route drawer its own question about a step's standing, and leave the shared boolean alone. Widening the shared one touches every caller and only the drawer wants the third word.

## anything_else

