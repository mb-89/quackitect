---
form: decompose-structure
by: agent
signed_off: 2026-08-24T15:35:24.352Z
authors: agent
files:
---

# Evidence form / decompose-structure

## current_situation

NO NEW ELEMENT AND NO NEW INTERFACE IS WANTED, so nothing escalates. This state's one legal edit at this size is allocating new functions into existing elements, and no function was minted either.

WHERE THE FOUR ROWS LAND. Three reach the structure through the walk's forward verb, which one element implements. One reaches it through the surface's verb, which a different element implements.

THE TWO ELEMENTS SIT IN DIFFERENT GROUPS. One is the walk, the other is the account.

AND THAT IS WHERE THE ARCHITECTURE DISAGREES WITH THE MEASUREMENT, which is this state's real finding rather than an aside.

## elements

- el-walk-engine
- el-mirror

## allocation

NOTHING NEW IS ALLOCATED, because nothing new was derived. Both elements already name the functions that carry this round's requirements.

- el-walk-engine implements the walk's forward verb, and through it the three rows about aiming, about a hop's budget, and about an unreachable target failing fast.
- el-mirror implements the surface's verb, and through it the row about a slow answer not freezing the surface beside it.

THE TRACE REACHES THE STRUCTURE TRANSITIVELY for all four, so none is named directly in an element's own satisfies list. A row written directly there would be one the function chain could not carry, and none of these is.

ONE STANDING FUNCTION IS WORTH NAMING because the round's leading suspect is about it. The walk element also implements a function for handing back a step that is still deciding. That is precisely the distinction the route drawer flattens away, so the structure already has a name for the thing the debt says is missing.

## follow_up

THE PROTOTYPE PHASE COMES NEXT and it inherits one scheduled probe with a sharpened question.

WHAT THE SPIKE MUST SEPARATE. Not which of two mechanisms causes the slow tail, but how much each contributes, since one is now confirmed and the other explains the tail's shape.

AND THE DISTRIBUTION GIVES IT A SECOND TARGET IT DID NOT HAVE. Aiming costs twenty seconds at the median, which is four hundred times the proposed per-hop budget, and that is not a tail at all.

ONE THING IS NOT CARRIED FORWARD AND IS NAMED HERE INSTEAD. The model has no way to express two elements sharing a loop, so the coupling this round measured lives in a requirement and a register entry rather than in the structure.

## anything_else

## interfaces

NONE IS OWED AND ONE IS MISSING ANYWAY, and the second half is the finding.

WHAT THE RULE ASKS. Where a flow's producer and consumer land in different elements, that pair owes an interface. No new flow was written this round, so no new cell is owed and nothing is added.

WHAT THE RULE CANNOT SEE. The two elements share a serving loop. That was probed on real data today and holds at three times baseline, with a 2,415 millisecond surface request answered inside a 20,010 millisecond engine call.

A SHARED LOOP IS NOT A FLOW. Nothing passes between them, so no crossing exists for the interface rule to catch, and the structure reads as two elements in two groups with no coupling at all.

SO THE ARCHITECTURE CLAIMS A SEPARATION THE RUNTIME DOES NOT HAVE. That is not a missing interface to add here; adding one would misdescribe it. It is a coupling the model has no vocabulary for, and it is why the freeze requirement had to be written as a demand rather than derived from the structure.

WHETHER THE MODEL SHOULD LEARN THAT VOCABULARY is a question above this round.

## trace_complete

ALL FOUR REACHED, ZERO UNREACHED.

- Three requirements reach through the walk's forward verb into the walk element.
- One reaches through the surface's verb into the mirror element.
- Nothing is named directly in an element's satisfies list, because the function chain carries every one of them.

COUNTED RATHER THAN ASSERTED. Four rows written this round, four with a refines edge to the use case, four served by a function, both functions implemented by an element.
