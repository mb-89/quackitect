---
form: gate-kickoff
bless: blessed by agent
by: agent
signed_off: 2026-08-24T14:41:48.215Z
authors: agent
files: null
---

# Evidence form / gate-kickoff

## current_situation

THIS ROUND OWNS THE WALK'S HOT PATH and nothing else. It began as one record holding every speed finding of the retro that preceded it, and was split three ways on the owner's instruction so three hands can work at once without fighting over files.

WHAT IT IS ANSWERING, measured over 4,048 calls in one window.

- 140 of 418 pulls ran past five seconds. Six ran between 60 and 131 seconds, and every one of those six answered that nothing routed onward.
- 36 of 48 aims ran past five seconds, against a published budget of a twentieth of a second per hop.
- The surface self-reported 642 slow requests, 16 of them past a minute.

THE CAUSE IS NOT ESTABLISHED, and that is the point of the first act. A standing debt predicts this exact symptom, and a two-call test settles whether it is the cause before anything is built.

NO WALKER WAS SPAWNED FOR M0. The remaining work there was this gate.

## retro_drained

- the inbox at this gate: empty, and the survey confirms it
- the 25 notes of this window: drained by the standalone retro an hour before this round opened
- the needs-retro trigger: dispositioned by that retro, which is what cleared the way here

## goals

- Establish where the walk's time actually goes, by measurement rather than by argument
- Make a hop's published time budget enforceable, which needs something that holds still to measure against
- Aiming returns at once, and the pull does the walking
- A target that cannot be routed to fails fast instead of spending two minutes finding out
- Three defects that live in these same modules stop standing

## pulled_in

- The route-drawer debt, whose trigger has now fired, tested before anything is built
- Per-hop timings across the route redraw, the condition evaluation and the spawned condition scripts
- The split of aiming from walking, keeping what the older ruling solved
- The fast refusal for an undrawable target
- One large committed record whose only job is to be a yardstick, in no listing and no count
- A re-signed answer that failed to knock down what declared a dependency on it
- The matrix serving 126 rows to a reader where 63 stand on disk
- A score cell with no way to say it has no evidence, which today rounds silence to nought

## left_out

- Everything served to an agent: the tool descriptions, the refusal payloads and the prompt layer. That is a sibling round, on another hand.
- The lifecycle of background work: the watchdog, the entry that never closes, the file pile, two engines on one port. That is the other sibling.
- The corpus-wide prose sweep, counted at 3,572 lines across 897 files, which a separate seeded record already owns.
- Splitting the session module along its seams. A seeded record owns that, and doing it here would collide with it.
- Every surface question. Those need the owner at a screen and belong to the record that owns surfaces.

## walkers

0 — the work is located by module and often by line, and delegated writing was measured over a full day and lost

ZERO IS THE DEFAULT AND NOTHING HERE ARGUES AGAINST IT.

THE MEASUREMENT BEHIND THAT DEFAULT. Three hands each spent about fifteen minutes rebuilding context the guide already held, for edits the guide had already located.

THE FIRST ACT IS TWO CALLS. There is nothing to fan out.

WHAT THIS DOES NOT RULE OUT. A reviewer at a gate and a researcher where research happens stand outside this number and are not discouraged. If the measurement turns into breadth nobody has scoped, that is a researcher rather than a walker.

## change_size

minor — it changes what two published verbs promise, but the design was already ruled, so there is no space of architectures to explore

WHY NOT PATCH. Aiming stops walking and the pull starts doing it. That is a contract change rather than a repair.

WHY NOT MAJOR. The owner settled the shape before the round opened: aiming returns at once, a hop gets a published budget, and a committed yardstick is what makes that budget enforceable.

ONE DESIGN QUESTION IS GENUINELY OPEN: what a reuse check would cover, so an already-passing hop is not re-decided from scratch. The owner's own instruction is not to guess it but to measure first, which is why it sits behind the measurement rather than in front of it.

THE TRIGGER THAT WOULD RE-SIZE THIS TO MAJOR, named now so it is not argued later. If the first test does NOT explain the slowness, the cause is unknown, and choosing a fix against an unknown cause is what the larger column exists for.

AND IT MAY COLLAPSE INSTEAD. The first act costs two calls and could remove most of what stands above.

## round_0_verify

- evidence vs claims: EVERY NUMBER IN THIS FORM WAS COUNTED, not remembered. The call counts come from grouping the log by the state stamped on each record. The slow-pull figures come from filtering the same log by duration.
- types: CLEAN. The lane runs the typechecker after every edit to a source file and hands errors back on the next answer. Nothing came back across this session's engine edit.
- lint: CLEAN, by the same channel.
- tests: 400 of 402 PASSING on the scoped run that covered this session's change. The two failures are the matrix row-count pair, which are pre-existing, already diagnosed, and pulled into this round's scope rather than left standing.

## round_1_validate

- exercised against the goal: NOT YET, and it cannot be. This gate prices the work before it is done. What IS exercised is the measurement behind it, and every figure in the situation box came from a real query rather than an estimate.
- missing: A CAUSE. Six pulls are known to have run past a minute and nothing establishes why. That gap is the round's first act rather than a hole in this gate.
- wrong: NOTHING FOUND WRONG. One claim was struck during the retro that fed this round — that the engine held two corpus readers — after checking the code rather than a file listing. It was withdrawn before it reached this scope.
- out of scope: THREE BODIES OF WORK, each named in the left-out box and each with a record of its own. Nothing was dropped silently.
- prior art: NOT NEEDED FOR THE SIZING. The cause is a property of this engine and the measurements come from it. Prior art becomes relevant if the round reaches a reuse check, and that decision waits on the measurement.

## bound_breaches

- if-agent-harness-to-entrypoint: NOT BREACHED. Nothing in this round reaches the boundary between the agent's harness and the entrypoint. Its items sit inside the route, the walk's own state and the condition evaluation, all of them well below that seam.

## round_2_red_team

- The two-call test comes back inconclusive => Then nothing is built on a guess. Per-hop timings are the second item precisely so the round still has a way forward, and the re-sizing trigger in the change size covers the case where the cause stays unknown.
- Three rounds edit one file at once => Only one file is genuinely shared, and it is named in all three records with the rule that whoever lands second re-reads it. This round does not touch it at all, so the exposure is between the two siblings rather than here.
- Minor is too small and the round stalls at a gate that wants more rigor => The trigger is written into the change size rather than left to a later argument. Re-sizing is a decision somebody makes on evidence, not a surprise.
- The committed yardstick rots and starts measuring its own drift => It holds still by construction, which is its entire purpose. A record still being worked on cannot serve, and that is stated in the item rather than assumed.
- Splitting aiming from walking reintroduces the problem the older ruling solved => Named in the goal itself. The pull has to walk through every already-passing hop, or an agent goes back to re-deciding them one at a time.
- The measurement work grows into its own project => It is bounded to three named suspects: the route redraw, the condition evaluation, and the spawned condition scripts. Anything wider is a researcher's job and a separate decision.

## raid_additions

- none

## verdict

pass — the sizing rests on counted evidence, the scope is disjoint from its siblings by module, and the one open question is answered first and cheapest

THE NUMBERS ARE IN THE FORM for anybody to re-run, rather than an estimate of effort.

THE DISJOINTNESS IS THE PROPERTY that lets three hands work at once.

WHAT THIS GATE CANNOT SETTLE is the cause of the slowness, and the round is ordered so the cheapest possible test answers it before anything is built.

WHAT WOULD HAVE MADE THIS FAIL. A scope with no measurement behind it, or a size chosen without naming what would change it. Neither applies.

## follow_up

THE NEXT ACT IS THE TEST, and it costs two calls.

Start a long judgment. Pull toward a target beyond it and time the answer. Then do the same with the machine quiet. A fast answer when nothing runs and a slow one when something does settles the question without profiling anything.

EVERYTHING ELSE WAITS ON THAT RESULT, including whether this round stays the size it was just given.

## anything_else

