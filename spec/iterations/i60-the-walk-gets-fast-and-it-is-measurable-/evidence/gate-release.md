---
form: gate-release
bless: blessed by agent
by: agent
signed_off: 2026-08-24T20:08:29.749Z
authors: agent
files: null
---

# Evidence form / gate-release

## current_situation

THE PACKAGE STANDS AND THE ROUND IS READY TO SHIP.

`dist/quackitect-8.1.0.zip` was assembled by script from a minor bump, and holds the README, the RUNME.ps1 installer, the editor extension and the engine.

THE VALIDATION GATE IS BLESSED with overrides, all of them named in the register.

WHAT THE ROUND DELIVERS. Every hop records what it cost, every search reports what it looked at, and a three-hop sweep fell from 15,404 milliseconds to 2,562 cold. Warm, those hops cost 34, 66 and 59.

THE SUITE IS 465 OF 465, up from 442 when the round's verification state signed.

## market_block


## round_0_verify

- evidence vs claims: five signed forms were amended against what now stands, and every amendment came from checking rather than from assertion — two of them refuted claims this round had made about itself
- types: clean, and the lane typechecks after every source edit rather than on request
- lint: biome clean on every patched file
- tests: 465 of 465 green, including one case written at this phase that the round had recorded as owed

## round_1_validate

- exercised against the goal: yes, and by the round's own instrument — `swept_ms` is what turned a theory about a slow aim into a number per hop
- missing: no live install from the package, and no closing reading of the two pull metrics the value prop counts
- wrong: nothing found standing wrong after the amendments, and a cold reviewer's six findings are each closed by work
- out of scope: moving finished records off disk, the read-context manager, restoring the third green as a readable mark, and a countdown on the idle shutdown
- prior art: none invented — the tool was `node --cpu-prof`, and the owner had to ask for it twice before it was used

## goals_served

- Establish where the walk's time actually goes, by measurement rather than by argument: SERVED, and it is the round's strongest result
- Make a hop's published time budget enforceable, which needs something that holds still to measure against: SERVED — the budget is ratified in a must-priority requirement and the flip it binds measures 20 milliseconds against 250
- Aiming returns at once, and the pull does the walking: SUPERSEDED BY RULING — going is now the default
- A target that cannot be routed to fails fast instead of spending two minutes finding out: REWORKED — the requirement asserted the converse of its own measure
- Three defects that live in these same modules stop standing: SERVED AND EXCEEDED — five stopped standing

## bound_breaches

- if-agent-harness-to-entrypoint: BREACHED COLD, INSIDE WARM. The bound is one second a call. Warm, a three-hop sweep walks in 152 milliseconds and every hop is inside it. Cold, the same sweep is 2,562 and is not, down from 15,404 before this round. What remains is the price a fresh process pays once, at boot.

## round_2_red_team

- no reviewer was spawned at this gate => DELIBERATE, and the reason is the size of the delta rather than the clock. A cold reviewer went over the whole round at the validation gate and its six findings are each closed by work. What changed since is a version bump, one archive and a contents check. The spawn card says spawning none at a state is a legitimate answer that needs no excuse, and this is that case.
- the package was never installed and run => CONFIRMED, and named in the package form rather than papered over. What stands is a contents check of the extracted archive: the README, the installer, the extension and the engine are all present. What is missing is reaching the desk's greeting from a fresh install.
- installing would put a second engine on this machine holding the same folder and port => THAT IS WHY IT WAS NOT DONE, and the record already carries it as an open work token. The honest reading is that this check cannot close until that token does.
- the archive carries `spec` => CHECKED AND CORRECT. The exclusion list lives in `deliverable/engine/produce.ts` and is shared with the producing act, and the whole archive is 3.35 MB, so records are not travelling wholesale.
- the idle shutdown cannot be armed by an agent => CONFIRMED, and it is not this round's scope. `note-93619be479d7` carries it with the missing countdown.

## raid_additions

- none

## verdict

pass with overrides — the package stands, was assembled by script, and holds every item the ship method names. The overrides are two and both are named where a reader will find them. FIRST, the package was not installed and run, so the desk greeting was never reached from it; installing puts a second engine on this machine holding the same folder and port, which is an open work token, and the gap is written into the package form rather than papered over. SECOND, the round's own open entries travel with it: three chunks are built and unreddened, the knock-down has no reproduction, and two rows wait on a testing affordance nobody has built. The suite is 465 of 465 and the round's headline is measured rather than argued — 15,404 milliseconds to 2,562 cold, and a flip of 20 milliseconds against a budget of 250. Two claims this round made about itself were refuted during its own gates, and both are closed with what they got wrong recorded.

## follow_up

### What ships with this, unresolved and named

THREE GUARDS NEED WATCHING FAIL. Break what each protects, watch the red, revert, record it. This is the one metric the round moved backwards.

ONE TESTING AFFORDANCE CLOSES TWO ROWS: a way to make an engine call run long on purpose.

THE PACKAGE CHECK CANNOT CLOSE until one engine can hold a folder without fighting another for it.

THE PULL METRICS HAVE NO CLOSING READING. The round has no after-figure for the numbers it set out to move.

### What the retro should not have to rediscover

A TRUE NUMBER COMPARED WITH THE WRONG THING IS THE HARDEST MISTAKE TO CATCH. 854 milliseconds was real, and it measured something the budget does not bound. It stood in a register entry, a gate verdict and a story before anybody asked what it was measured against.

A COLD PROBE IS NOT A LIVE ENGINE. Every speed figure in this round came from a process that rebuilt its session per run. Warm, the same hops are five to twenty times cheaper, and nothing in the round said which kind of number it was reporting until the last hour.

THE PROFILER SHOULD HAVE BEEN THE FIRST TOOL, not the fifth. The owner asked for it twice.

A REVIEWER AT A GATE PAID FOR ITSELF. It found a false breach the building hand could not see.

## anything_else

