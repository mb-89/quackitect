# M6 - Build & verify (i0019_strangers_book)

## Build planned  -> i19-m6-build-planned  (KILLER - owner adjudicates)
Seven small, resumable, dependency-ordered steps seeded as children of i19-m6-build, each one durable unit of progress with a single concern, each named after what it realizes:
- **p1 deck-anchors** - go-deck-anchors + the ARIA region/navigation marking + ToC exclusion (req-deck-links, req-deck-semantics). The substrate every link stands on, so it goes first.
- **p2 terms-lint** - go-terms-order-lint, glossary-as-term-set, advisory class (req-terms-before-use). Flat.
- **p3 white-label** - go-white-label-identity + the brand-neutral prose sweep (req-vehicle-white-label). Flat.
- **p4 onboarding-content** - chapter 2.2, the Pong deck from the spike keepers (measured timeline, lazy-init embed of spike/pong.html), the guides deck row, the README link (req-onboarding-chapter, req-pong-deck, req-deck-discoverable). Depends on p1 - its links consume the anchors.
- **p5 runme** - the install-verify-orient slim-down with the no-workspace guarantee (req-runme-orientation). Flat.
- **p6 bugfixes** - the spike's found defects fixed in their existing homes (stubs scaffold in connections mode and lint-clean; the ship copy tells one truth), class-guarding selftests extended. Flat, the bugfix lane.
- **p7 glossary** - the load-bearing vocabulary grown (killer, pager, determinizer, walk, spike and kin) so p2's lint has teeth. Flat, content.
Suite observed RED after the FULL trace wiring (the i18 lesson applied): all six tests red-observed at their final hashes (onboarding-surface @945c5cdd, deck-links @09ffec14, pong-deck @398901df, runme-orientation @66cfceee, terms-before-use @27327619, white-label-book @422c5c49).

## Models adhered-to  -> i19-m6-models
The three M4-allocated elements were filled exactly where the diagram put them: go-deck-anchors (rim--graph, beside deck-mode - it grew the fragment router and the embed budget as the walk demanded, implements extended honestly), go-terms-order-lint (beside spec-lints, consuming the renderer's own chapter order and glossary map by construction), go-white-label-identity (beside title-card, the workspaceProduct seam). No unsanctioned element: model-conformance green. Content-realized statements bind in method design blocks (method-runme-orientation, method-onboarding-surface in dependencies.md - the whole stranger's route-in contract in one doc).

## Build  -> i19-m6-build
All seven planned steps realized: p1 deck citizenship (anchors, region landmarks, ToC exclusion, Minutes timeline, lazy template embed with the single new-Function execution lane); p2 the terms lint (advisory, mechanically pinned - its first live run flagged the red-team's exact seven terms); p3 white-label (the vehicle-titled book, engine as colophon credit, planted-leak check); p4 the six-slide Pong deck per the owner's outline (self-contained starter prompt, the rigor beat, the real-walk timeline - measured 4:46 walk-only, fixed friction excluded, half-minute steps, 5 minutes total (owner ruling) - and the playable embed) + chapter 2.2 + the guides deck row + the README Pages link; p5 RUNME check-install-verify-orient (no workspace, ever); p6 the spike bugfixes (connections-mode stubs from birth, lint-clean examples, one-truth ship summary); p7 nine glossary entries. Two mid-build engine catches fixed under their regions: the timeline float artifact (10.399... -> 10.4) and the missing over-budget embed fallback.

## Internal quality ok  -> i19-m6-quality
gofmt clean (the build's analysis gate); stdlib only, zero new dependencies; deterministic renders (parity green throughout); the external-link lint was NARROWED never weakened (own-clone-URL in a deck only, derived live from the origin remote - foreign URLs and out-of-deck uses still flag, both directions selftested); one honest stray captured to the notes lane (in-book deck links reach bookGoto without deck delegation) rather than chased.

## Implementation risks acceptable  -> i19-m6-impl-risks
- The five-minute timeline is the measured 4:46 walk-only time rounded to half-minute steps (owner ruling: friction fixed this iteration is excluded - connections-mode and clean stubs are gone for every future newcomer; the attest grant stays on slide 2 as the adjudicator's honest act; the M5 evidence keeps the raw 10:24).
- The facet-preset router is new JS in the one-owner rail; its three delegation points (hashchange, load, bookGoto-miss) are asserted in the selftest, and replaceState semantics keep history clean.
- The white-label sweep found no further shell identity leaks; deep method-prose voice remains for the M7 consistency sweep to audit (carried, not hidden).

## Verification green  -> i19-m6-verification  (derived: coverage:tests-pass)
FULL battery ZERO FAIL across all iterations at golden 77ff1d23784b (post the owner's deck design round 2: timeline closes at the last slide, two-column auto-starting discussion with the owner's formulation, bold options, the chat figure, the book's OWN figures reused on s3/s4 - never duplicated art - and in-book README self-links riding the rails; rounds 3-4: pong DATA through the book renderers - its own register table and its own onion-view model via the new mermaid-fence lane - the always-visible court with script-owned start/stop and stop-on-leave, the prominent numbered timeline, present-mode term TOASTS with the new grant glossary entry, the general Prerequisites headline, five artifacts in list+diagram with the ship packaging README and engine-fallback RUNME for every project, and the old spec-compiled presentation removed). All six i19 selftests green: deck-links, terms-before-use, white-label-book, runme-orientation, onboarding-surface, pong-deck. Coverage: clean (no holes). Blocking lint: zero. Books regenerated from the final build; drift clean. The deck verified by a second pair of eyes on the rendered book: the deep link opens present mode, slide 1/6 carries both get-it options and the verbatim prompt, the timeline renders 5 min in half-minute ticks; in-book deck links enter present mode through bookGoto's deck delegation (the owner's found dead path, fixed and re-verified).

## Milestone review  -> i19-m6-gate  (KILLER - owner adjudicates)
**Verify:** every requirement's test went red at its final wired hash and is green now; every design region sits where M4 allocated it; coverage, conformance, and parity are computed clean, not asserted. **Validate:** the built surface IS the owner's design - the six slides in the given order, self-contained, measured minutes, the playable deliverable, RUNME that only installs, a vehicle book that presents the vehicle. **Red-team:** the sharpest attack - "five minutes is marketing": the timeline is the measured 4:46 walk rounded to half-minutes, with the friction THIS iteration fixed excluded by owner ruling (a newcomer will not live a bug we removed); the derivation is stated in the manifest and the M5 evidence keeps the raw 10:24. Also fixed at this gate's review: in-book deck links now enter present mode (bookGoto delegates to the deck rail) - the owner's own click found the dead path. Second attack - "the terms lint could be vacuous": its first live run produced seven real findings, the red-team's own list. **Verdict: PASS from the agent side - hand-off for the owner's M6 bless.**
