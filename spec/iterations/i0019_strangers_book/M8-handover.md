# M8 - Package & hand over (i0019_strangers_book)

## Docs complete & match the actual surface  -> i19-m8-docs  (KILLER - owner adjudicates)
The M7 consistency sweep IS this check's evidence trail: everything i19 changed is re-documented where it is described - the deck template teaches every new mechanism (timeline, columns, embed lanes, the model fence, fig reuse, toasts), the integrate prompt documents the white-label identity seam, the voice doc carries the owner's entry-documents-plain-language law, compose-reference is connections-mode-current, the role charter and targeted-verify law are baked. The README survived an independent cold read (its one jargon leak rewritten plain); the book is regenerated from the final build on both published paths.

## Packaged & versioned  -> i19-m8-packaged
`quack ship` packages the zip with book.html + report.html + README.md + RUNME.ps1 + RUNME.sh at the zip root (the i19 engine fix: the README travels with the work, the installer rides from the workspace's tools/ or the engine's - every project's receiver can set up the toolchain). Run at this walk; contents verified below.

## Configuration baselined  -> i19-m8-config
Golden root re-baselined at every content change through the walk (final at the ship); the glossary (now 20+ terms incl. grant and ADR) and the terms-lint advisory class are pinned by selftests; the field-schema config home is untouched (i0020_field_ux territory).

## Handover accepted  -> i19-m8-handover
The owner drove the deliverable through four live design rounds and walked every gate personally; the M8 gate bless below closes the hand-over on the ledger.

## Milestone review  -> i19-m8-gate  (KILLER - owner adjudicates)
**Verify:** the ship zip's contents enumerated by the one-truth summary; the battery ZERO FAIL at the final build; blocking lint zero; books byte-identical on both paths. **Validate:** the deliverable is the iteration's reason - the book survives first contact, demonstrated by the cold read. **Red-team:** anything shipped but undocumented? The sweep's two catches were exactly that class, found and fixed; nothing else surfaced across two independent reads. **Verdict: PASS from the agent side - hand-off for the owner's M8 bless, then engage ship completes the iteration.**
