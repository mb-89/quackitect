---
form: spawn-for-prototype
judgment: passed at 2026-08-26T14:02:13.112Z with deliverable/engine/bin/hands-spawned.ts@8be27987d4e0
by: agent
signed_off: 2026-08-26T14:02:12.283Z
authors: agent
files: null
---

# Evidence form / spawn-for-prototype

## current_situation

The architecture gate is blessed. The winner is "Archive in git": work is an editable file while its record is open, and a closed record folds into one file, leaves the working tree and is read back at a recorded commit.

ONE SPIKE IS OWED AND IT IS OVERDUE. The cross-panel drag was named at the candidates gate as one of five things owed BEFORE the winner was declared. Four were discharged; this one travelled past the declaration, past the structure and past the evaluation, and the gate passed over it on the owner's disposition: bless, run the spike, and if it does not work go back and find another solution.

THAT LATENESS IS A METHOD QUESTION FOR THE RETRO, NOT A FINDING AGAINST THIS DESIGN. The owner ruled that explicitly, and it is captured as a note rather than a register entry. The machine has a prototype phase and this is it; what the retro must ask is whether a spike that gates BUILDABILITY belongs here or in the space state that probes.

A SECOND UNKNOWN ARRIVED WITH THE GATE. The folded format is now the designer's choice, with human readability and human editability both lifted, and one demand raised in their place: a whole iteration must be reconstructable for browsing. Every already-archived record folds too, and none of the 68 holds a work token.

## hands

- [ ] no walker is spawned for the drag spike — it is one question about one affordance, and a hand per spike buys parallelism this phase does not need
- [owed] the folded format must read 68 archived records that carry no work tokens — raid-iss-the-old-archived-folders-are-folded-too-and-carry-no-work-tokens

## follow_up

Two things this phase must answer, and they are unequal.

THE DRAG SPIKE IS THE ONE THAT CAN REOPEN A SIGNED COMPARISON. Whether a row moves from one pane to another is not a preference, it is whether the bucket editor can exist. The owner's disposition is explicit: run it, and if it does not work go back and find another solution.

THE FORMAT QUESTION IS SMALLER BUT IT HAS A NEW CONSTRAINT. Every already-archived record folds too, and none of the 68 holds a work token. A format chosen against this iteration's records alone will not read the other 68.

What this phase does NOT owe: a hand per spike. The state's own motivation says spikes run side by side and parallel work needs more than one hand. Two spikes, one of them a single yes-or-no about an affordance, does not reach that bar. Spawning against the motivation rather than the letter.

ONE THING IS OWED AT WHICHEVER STATE ALLOWS A DELETE. raid-iss-a-prototype-arrives-after-the-winner-is-declared-and-cannot-change-it was minted here and the owner ruled it is a note rather than a register entry. The note stands as note-2f3e0bb8a6c1; the node has to go, and delete is illegal at this state and at the gate before it.

## anything_else

