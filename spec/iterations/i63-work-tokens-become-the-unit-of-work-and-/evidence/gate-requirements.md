---
form: gate-requirements
bless: blessed by agent
reopened: "2026-08-26T12:24:58.678Z — three should rows joined the register after this gate blessed, and probe-assumptions re-signed with the volume outcome"
amended: "2026-08-26T11:10:46.200Z by agent — the invented word obligation becomes work token, the name the owner uses"
by: agent
signed_off: 2026-08-26T12:25:03.145Z
authors: agent
files:
---

# Evidence form / gate-requirements

## current_situation

The requirements milestone is walked. Four states stand signed: the requirements, the functions, the assumptions and their probes.

THIRTY-TWO REQUIREMENTS ARE NEW and twelve standing ones carry additions. Five functions are new, eleven standing ones took additions, and four flows were minted. Every figure here was counted by a script over the corpus rather than remembered.

EIGHT ASSUMPTIONS STAND AGAINST THIS ROUND and every one carries a probe outcome. One was falsified at the code and changed kind to an issue. The eighth was opened at this gate and is listed below.

TWO OWNER RULINGS LANDED MID-MILESTONE and both changed how the work was done. Extend a standing row rather than mint a duplicate. And a state that may author may also delete, which was armed as emergency mode so eleven folded rows could be removed where they were written.

TWO COLD REVIEWS RAN AT THIS GATE, each spawned with no shared context. The first failed the gate with thirteen findings, four of them crippling. The second read the corrections and ruled pass with overrides. Both are answered below.

## round_0_verify

- evidence vs claims: checked, and one claim was struck by this milestone's own probe. The kickoff goal says read evidence is global and version-keyed. It is version-keyed and it is NOT global, verified at deliverable/engine/sessionreads.ts line 101 rather than at the comment above it. Every count quoted in this gate came from a search, a lint run or a test run rather than from memory.
- types: not applicable, and the formatter says so too. This milestone wrote no source file. Biome ran as part of the battery and exited 0.
- lint: RED, and the number is this milestone's own. The voice lint swept the record's 22 evidence forms and found 1 clean. The four forms this milestone authored carry 15 findings between them: write-requirements 2, derive-functions 4, identify-assumptions 7, probe-assumptions 2. Every one is a long sentence, a comma chain or an item that grew, none is a wall, and no write was refused under SE-C-125. The record's own signed gates carry 34 to 48 each, so this is systemic rather than this milestone's.
- tests: RED ON THE BATTERY AND GREEN ON THE CHECK THAT READS THIS WORK. The engine chose the whole suite, because 96 files would have run piecemeal since the last one. Result: 1,861 tests, 1,859 passing, 2 failing. Both failures are the comment-attribution ratchet over deliverable/tests, standing at 207 against a ceiling of 204. THIS MILESTONE WROTE NO FILE THERE. What it did write is corpus nodes, and the conformance sweep passed over 3,102 nodes in 1,394 ms with markers and the widget guard green. Preflight green. The red is filed as raid-iss-the-battery-is-red-on-a-comment-ratchet-this-milestone-did-not-move rather than carried.

## round_1_validate

- exercised against the goal: yes, and the exercise changed the work twice. Every capability the inputs gate enumerated was walked against the register, and every register entry was walked against the requirements. The owner's fold ruling then removed eleven rows that duplicated standing demands, and the granularity ruling flagged the function grain for a later retro without changing this round.
- missing: the mapping from two old difficulty ladders onto one complexity value. The kickoff gate listed it as missing, this milestone opened it as an assumption, and its probe is authoring rather than checking. It belongs to the design milestone and it is named rather than hidden.
- wrong: one thing was wrong and it is now recorded as wrong. Read evidence being global was a kickoff goal and it is false at the code. The requirement that demands it is now the fix rather than the assumption, and the register entry changed kind from assumption to issue.
- out of scope: three things, each naming where it went. Whether private work exists at all is the owner's and is unruled. The storage shape is standing dissent. The cross-panel drag is a risk with a spike the owner ordered run first.
- prior art: SCANNED LIVE AT THIS GATE, and it found the exit rule is not ours. WHAT THEY DO BETTER: enforcement of a blocking checklist is shipped and in production. Jira's workflow validators refuse a transition to Done while checklist items stand open, delivered by Smart Checklist and by Multiple Checklists for Jira. ServiceNow's Checklist Pro marks items Required and refuses to close the record until they are done. Both are ordinary configuration for their users and ours is a design. WHAT OURS SHEDS: their checklists are AUTHORED per issue, and a validator is a per-project setting a team can decline. Ours derives the work tokens from the method itself, so nobody authors the list and nobody can decline it. WHERE THE FIELD IS MOVING TOWARD US: runbook platforms now convert a documented procedure into executable steps with completion tracking and timestamped audit records, which is derivation rather than authoring. That is the half of our claim that is no longer novel. WHAT IS STILL OURS: deriving work tokens from the READING a step demands, and routing them by which hand can carry them. Neither appeared in either scan.

## goals_served

- Every piece of work is a work token: one markdown file per item, carrying frontmatter and prose together.: SERVED. req-every-piece-of-work-is-one-addressable-item demands the four properties a form field has none of, and fn-run-a-governed-walk.mint-what-a-state-owes is the act that gives an work token its identity. The file half is an addition to req-every-artifact-is-readable-text rather than a new row, because that row already demanded it.
- A position has TWO SLOTS. Incoming holds what must be taken in before it can be worked. Outgoing holds what must be produced before it can be left.: SERVED. The addition to req-form-is-built-and-checked carries the bucket table naming which block and which do not, and req-a-state-shows-what-it-owes-without-being-opened puts a count on each.
- A method's steps become outgoing tokens, one per marked heading, each carrying its own guidance in the body beneath it and its evidence in subheadings under that.: SERVED, AND IT IS THE ROUND'S MOTIVATING INCIDENT. req-a-method-step-becomes-exactly-one-work-token demands exactly one in both directions, and raid-asm-a-heading-per-token-survives-the-retro-card carries the probe that decides whether the format holds.
- Reading requirements become incoming tokens, and only where the evidence is not already proven. Read evidence is global and version-keyed.: SERVED IN HALF, AND THE OTHER HALF OF THE GOAL IS FALSE. The minting half is req-a-state-mints-its-work-tokens-on-entry, whose Detail subtracts proven reading. The globality half was checked at deliverable/engine/sessionreads.ts line 101 and does not hold; it is now an issue rather than an assumption, and the addition to req-reading-credit-survives-a-reload is what makes it true rather than a claim that it already is.
- A token has a PLACE and a STATUS, and they are separate. Place is a position or the backlog. Status is open, in work, or one of several terminal kinds.: SERVED. The addition to req-the-raw-note-stays-local-and-is-marked-drained carries the separation, and the case that names it is routing work out of scope, which changes place and never status.
- A position may be left when every token in it has reached a terminal status or moved elsewhere. Moved is a real exit, not a failure.: SERVED AND IT IS THE CENTRAL RULE. The exit half is the addition to req-form-is-built-and-checked; the moved half is req-moving-work-releases-the-state-it-left, allocated to fn-run-a-governed-walk.judge-a-claim because the judgment is what has to accept it.
- A token may depend on another token, or on a position finishing.: SERVED. req-readiness-is-derived-from-a-declared-dependency carries both edge targets and the outcome kinds, and fn-run-a-governed-walk.offer-the-work-that-is-ready withholds on them. This was the one capability the inputs gate found uncovered, and it was fixed there.
- Outside a record everything is ephemeral. Inside a record a done token IS the evidence, and there is no second act of writing it.: SERVED, both halves. req-settled-work-is-the-evidence-inside-a-record removes the second write. req-work-outside-a-record-goes-when-its-state-completes carries the ephemeral half, as an addition to fn-run-a-governed-walk.hold-the-work read from the other end.
- The pull returns open tokens rather than instructions.: SERVED, and it is the largest single change. It lives as an addition to req-pull-answers-from-record rather than as a new row, on the owner's fold ruling, and the gate should read it as this round's work rather than as an old row.
- The four ladders become two. Complexity is a ROUTING key that decides which hand a token is given to. Autonomy is unchanged.: SERVED IN PART, AND THE REMOVAL IS NOT PROVEN. req-work-carries-how-hard-it-is demands the offer be filtered by the mark, narrowed to that because the mark itself already stood. What is NOT settled is whether one axis carries what two carried, and that is now an open assumption whose probe is the design's own work.
- Every position shows a count per slot, and clicking one opens the token editor.: SERVED. req-a-state-shows-what-it-owes-without-being-opened, fn-run-a-governed-walk.count-what-is-owed producing flow-owed-count, req-a-states-outstanding-count-is-read-at-a-glance for legibility, and req-the-work-editor-needs-no-new-instruction for what opens.
- THE ARCHITECTURE ANALYSIS LATER IN THIS ITERATION SWEEPS EVERY PLACE WORK IS DONE, so nothing the work token system touches is missed. The state machines and the token editor are the two largest surfaces, and they are not the whole list.: PARTLY, AND THE SWEEP ITSELF IS A LATER MILESTONE'S. What this milestone contributed is the demand: req-every-place-work-is-modelled-is-named-in-one-list makes the list a written checkable artifact rather than a promise, and the addition to fn-run-a-governed-walk.catch-the-system-up gives it a home. The sweep needs a design to sweep against, so it could not have run here.

## bound_breaches

- if-agent-harness-to-entrypoint: none recorded, and that is unobserved rather than clean. This milestone exercised no modelled interface under measurement, because it produced corpus nodes and ran no path that crosses that boundary. An empty result here is not evidence of speed. What WAS measured this milestone sits in the register instead: minting on every entry has no measurement at all, and it is an open assumption against the standing per-hop budget.

## round_2_red_team

- STEELMAN, the opposing case at its strongest: work tokens re-describe the evidence form. A form is already a checklist a state cannot be left without filling, already derived from the state note, already refusing a submit on an empty required field. Thirty-two rows and twelve additions buy one thing it genuinely lacks, which is that an work token can be MOVED, and everything else is the form re-described in nouns at the cost of a file per work token. => IT LANDS ON ONE POINT AND FAILS ON FOUR. It lands on volume: the per-position count is measured at 4 to 15 with a median of 5 over 21 signed positions, and nobody has opened a folder holding a whole record's work tokens, which stands as an open assumption. It fails because a form field cannot be moved, cannot be counted from outside its state, cannot be withheld until a dependency settles, and cannot be routed by which hand can carry it. Four kickoff goals turn on exactly those four, and none is reachable by extending the form.
- KILL-CRITERION ONE, the sentence that would make this the wrong call: one marked method heading does not reliably become exactly one work token. => LOOKED FOR, AND NOT SETTLED. The probe is the retro card split and it has not run. Fail it and nobody derives the list, the work tokens go back to being authored, and this round has bought a heavier form. The design milestone may not rest on the derivation before it runs. Carried as raid-asm-a-heading-per-token-survives-the-retro-card.
- KILL-CRITERION TWO, cheaper to test: minting on every entry does not fit the standing per-hop budget. => LOOKED FOR, AND UNMEASURED. Minting sits on the hot path of every entry into every position. A miss forces storing and diffing rather than computing, which is a different design rather than a tuning pass. Carried as raid-asm-minting-on-every-entry-stays-inside-the-per-hop-budget.
- FIRST COLD REVIEW: thirteen findings, four of them crippling, and it FAILED the gate. => All four crippling findings are closed, each at its source, and the second review confirmed each one by reading the file rather than the claim.
- Crippling one: two owner rulings had no row at all, including the in-work status the whole narration removal rests on. => Both are rows now. req-work-says-when-a-hand-is-on-it carries a state model whose first line forces the question of what creates the mark, and req-re-entering-a-state-decides-what-its-work-does carries all four re-entry cases in a table.
- Crippling two: the coverage argument rested on a list of 27 capabilities rather than on the use-case steps, which is the same substitution an earlier round already failed on. => Replaced by the step walk. All 52 steps and extensions of the three new use cases were walked one at a time, then 30 more across the two standing ones. Two holes turned up, both the same shape, and one row closes both.
- Crippling three: a snapshot exception stood on one side only, so the rebuild rule and the snapshot rule disagreed with each other. => req-bound-field-rebuilds-from-nodes now carries the exception on its own side, with the reason it has to be written in both places.
- Crippling four: the editor row's measure was a population figure nobody could gather. => Restated as the single watched attempt its own register entry prescribes.
- SECOND COLD REVIEW: ten of thirteen closed, verdict pass with overrides. => It found five things the corrections had not, and all five were fixed before this gate was filled.
- Three amendment strings carried wrong counts: eight rows where seven arrived, 42 standing where 44 stand, thirty-three and thirteen where thirty-two and twelve stand. => Corrected on write-requirements, derive-functions and identify-assumptions. Every count in this gate is now script output over the corpus rather than memory.
- The step-walk claim said the five use cases where 52 counts only the three new ones. => Corrected to three, and the two standing use cases were then genuinely walked: 16 items for taking a step and 14 for walking a record on a smaller model, with no further hole.
- Half of one row was unallocated. The parts row demands both that a hand may create parts and that the parts are shown under the work they came from, and only the creating half sat on a function. => Drawing them now sits on fn-run-a-governed-walk.show-where-it-stands, which is where it fails differently: parts created correctly and drawn flat leave a reader with more rows and no more understanding.
- One row demanded something of the RECORD rather than of the system, which is a process demand wearing a requirement's clothes. => Restated. An open register entry recording an unanswered question about a drawing is now carried as outstanding work on every state whose evidence rests on that surface, and the row moved from keeping the record to minting.
- An upstream form said twenty-six steps across the three new use cases where the count is 52. => Corrected on generalize-use-cases.
- One extension label jumped from 8a to 9b with no 9a. => Renumbered to 9a in all three places that carried it, including the blessed inputs gate that cited it.
- PRIORITY INFLATION, measured rather than impressionistic: 29 must, 3 should and 0 could across 32 rows. The authoring card says inflation defeats the design milestone, because if most rows are must no candidate can differ. => RAISED AS AN OVERRIDE AND THEN RULED ON BY THE OWNER AT THIS GATE. All six named candidates were demoted to should, leaving 23 must and 9 should. The owner also supplied the test the authoring card lacked: a must is fatal to the iteration, a should costs technical debt and is recoverable by a later round.
- ONE SEAM BETWEEN TWO ROWS WAS REAL AND UNSTATED: a gate judges on work minted and finished before it, while temporary work goes when its state completes. => They reconcile through req-settled-work-is-the-evidence-inside-a-record, because gates exist only inside records, where work is lasting. Stated here for the design milestone rather than turned into a fourth row.
- A DEFECT FROM OUTSIDE THIS ROUND BLOCKED THIS GATE. Four flows an earlier iteration landed had one end each, and the closure check fires only when a walk leaves one state, so a merge landed a red check nothing looked at until this walk arrived. => Fixed at the source. Two were world-facing and now declare that they cross in, one had its producer and consumer written the wrong way round, and one had no consumer named although the producing function's own rationale said which function reads it. Captured as a note asking for the closure to run in the conformance sweep instead.
- WHAT THE TWO REVIEWS DID NOT DENT. => No two rows demand opposite things. Every row refines a use case whose steps demand it, checked mechanically in both directions and by hand at step grain. No row names a mechanism. Both reviewers read the two largest folds — the exit rule and the pull's new payload — as this round's work rather than as old rows.

## raid_additions

- raid-iss-the-battery-is-red-on-a-comment-ratchet-this-milestone-did-not-move
- raid-asm-a-hand-marks-work-in-progress-before-it-acts

## verdict

pass with overrides — the register is sound, and the one override was an inflated priority spread that the owner has now ruled on and which is corrected.

THIRTY-TWO NEW ROWS AND TWELVE ADDITIONS STAND. Every one refines a use case whose steps demand it, every one is served by a named function, and none names a mechanism.

THE OVERRIDE WAS PRIORITY, and the dissent is recorded rather than smoothed over. Measured across the 32 new rows before the ruling: 29 must, 3 should, 0 could. The authoring card's own rule says priority inflation defeats the design milestone, because if most rows are must then no candidate can differ from another.

THE OWNER RULED IT AT THIS GATE and gave the test that had been missing. A MUST IS FATAL: if it is not done, the iteration is purged, because there is no point finishing it. A SHOULD IS RECOVERABLE: broken, the work still ships carrying a lot of technical debt, and a later round fixes it.

THAT IS NOT PERMISSION TO SKIP THE SHOULDS. An iteration failing all of its shoulds is still a failure. What changes is only that the failure is recoverable.

SIX ROWS WERE DEMOTED TO SHOULD, each for the same reason: broken, it costs debt rather than the iteration. The spread now stands at 23 must and 9 should across 32 rows.

- req-work-records-when-it-opened-and-when-it-closed. Timestamps make reading easier and nothing gates on them.
- req-a-destination-holding-nothing-is-revealed-while-work-is-placed. It is an affordance during a move, and its own spike may yet say the gesture is not buildable.
- req-outstanding-work-is-listed-narrowed-and-grouped-in-one-place. The list must exist; narrowing and grouping are convenience over it.
- req-a-tree-that-models-work-two-ways-refuses. A guard against a future mistake rather than a capability anybody uses.
- req-every-place-work-is-modelled-is-named-in-one-list. A completeness aid a later milestone consumes.
- req-one-word-names-one-thing-and-the-walks-marker-is-not-a-token. A naming rule that is general rather than about work tokens at all, and the owner placed its real home in a voice sweep elsewhere.

THE TEST ITSELF IS CAPTURED AS A NOTE, because the authoring card carries the MoSCoW scale and its consumer but never said what makes one row a must.

WHAT WOULD HAVE FAILED THIS GATE, said plainly because it nearly did. The first cold review failed it on four crippling findings. The largest was that the coverage argument rested on a summary of the use cases rather than on their steps. That argument is gone, and the step walk stands in its place.

TWO KILL-CRITERIA ARE OPEN AND NEITHER BLOCKS THIS GATE. Whether one marked heading becomes exactly one work token, and whether minting fits the per-hop budget. Both are register entries with probes, and both belong to the design milestone. A pass here says the requirements are right, not that the design is safe.

## follow_up

THE DESIGN MILESTONE TAKES THESE REQUIREMENTS AS ITS INPUT, and three probes should run before it commits to anything.

- The cross-panel drag spike. The owner ordered it first, two journeys cannot be told without it, and req-a-destination-holding-nothing-is-revealed-while-work-is-placed rests on it.
- The volume half of the file-per-work-token count. The per-position half is measured at 4 to 15; nobody has opened a folder of a whole record's work tokens in the vault.
- The retro card split, which decides whether one marked heading really becomes one work token.

TWO PROBES CANNOT RUN UNTIL SOMETHING IS BUILT and they are the before-and-after kind: what minting costs per hop, and whether a weaker hand raises a mark rather than answering.

ONE MAPPING IS OWED TO THE DESIGN AND NOBODY HAS WRITTEN IT. Two old difficulty ladders onto one complexity value, across every matrix row, naming every row where the two pulled in different directions.

THREE NOTES CAME OUT OF THIS MILESTONE and none is this round's work.

- The requirement-authoring card should demand a pass over the standing register before any row is written.
- A trace table a person can read is owed, because the coverage answer is mechanical and its presentation is not.
- The function grain wants a retro of its own, over the whole corpus rather than over this round's five.

TWO MECHANICAL GAPS WERE MEASURED WHILE WALKING THIS MILESTONE and both are captured as notes: a state that may author a node may not delete one, and a drawn field is displayed from the nodes and then demanded back as a copy.

## anything_else

THE TRACE, AS A TABLE, because the coverage answer is mechanical and its presentation was not. The owner read this milestone's hand-off and could not tell whether anything was missed. This is the shape that was owed.

EVERY ROW BELOW IS READ FROM THE NODES rather than typed from memory. A script printed each row's own refines edges, and the table is that output arranged.

| use case | stories under it | rows this round put under it |
| --- | --- | --- |
| work a state's work tokens to completion (22) | walk a state that will not let me skip a step | addressable item, mints on entry, one step one work token, survives a rewording, snapshot or live reading, readiness from a dependency, a close that is not done carries its reason, moving releases the state, settled work is the evidence, person-only says so on its face, work outside a record goes, work outlives its state or goes with it, the progress account, work carries how hard it is, a hand is on it, a hand may break work into parts, re-entering decides what its work does, a submachine step takes work instead, a gate judges on work before it, a tree modelling work twice refuses, every place work is modelled is listed, a surface silence is answered |
| read what the system owes and what it is doing (14) | see every open thing at one glance, watch the machine think | shows what it owes without being opened, records when it opened and closed, the count read at a glance, the work editor needs no new instruction, a hand is on it, a hand may break work into parts, work outlives its state, work outside a record goes, the progress account, addressable item, a gate judges on work before it, a tree modelling work twice refuses, every place work is modelled is listed, a surface silence is answered |
| route outstanding work to where it is done (10) | browse the backlog and decide what happens next, steer a running iteration by moving work | listed narrowed and grouped, placing makes the destination owe it, a destination holding nothing is revealed, a carrier grouping ends when it empties, an open point is ruled on at the next checkpoint, addressable item, moving releases the state, re-entering decides what its work does, a submachine step takes work instead, the work editor needs no new instruction |
| walk a record on a smaller model (3) | a smaller model walks a record | work carries how hard it is, person-only says so on its face, one word names one thing |
| take a step (5) | carry a finding without stopping | placing makes the destination owe it, settled work is the evidence, the progress account, snapshot or live reading, one word names one thing |

TWELVE STANDING ROWS TOOK ADDITIONS and they are not in the table above, because their use-case edges were already drawn. They are: every artifact is readable text, a bound field rebuilds from nodes, a harmless finding is carried, a harmless finding names an open entry, the raw note stays local, a form is built and checked, a weaker driver owes a reason, reading credit survives a reload, a reload restarts clean, one call reports every piece of work out of sight, a minted option says when it comes back, and the pull answers from the record.

NOTHING IS CROSS-CUTTING WITHOUT AN EDGE ANY MORE. Four rows protect every journey rather than deriving from one step, and each one names the use cases whose passes it protects rather than sitting outside the graph.

WHY THE COVERAGE HOLDS, and it is the step walk rather than a capability list. The first pass argued from 27 capabilities the inputs gate enumerated, which is a summary of the use cases rather than the use cases themselves. A cold reviewer named that substitution and the gate failed on it.

WHAT WAS WALKED INSTEAD. All 52 steps and extensions of the three new use cases, one at a time — 23 for working a state's work tokens, 14 for reading what is owed, 15 for routing work. Two holes turned up, both the same shape, and one row closes both.

THE TWO STANDING USE CASES WERE WALKED TOO, and they add 30 items — 16 for taking a step, 14 for walking a record on a smaller model. Every one is either answered by a row or an addition this round writes, or left untouched by the change. That walk found no further hole.

EIGHTY-TWO ITEMS IN ALL. The engine checks the same coverage mechanically in both directions, and this is the by-hand walk at step grain that the mechanical check cannot do.

WHAT THE TABLE STILL DOES NOT SHOW is which use-case STEP each row answers. That sits on each row's own source_refs, and rendering it is what the trace-table note asks for.
