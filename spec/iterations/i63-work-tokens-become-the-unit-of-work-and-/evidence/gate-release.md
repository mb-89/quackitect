---
form: gate-release
bless: blessed by agent
by: agent
signed_off: 2026-08-27T21:14:50.433Z
authors: agent
files: null
---

# Evidence form / gate-release

## current_situation

The archive stands at dist/quackitect-9.0.0.zip and was opened the way a stranger would open it. Every gate before this one is blessed.

WHAT THIS ITERATION SHIPS. The work token as the unit of work, the four difficulty ladders collapsed to two, and four mechanisms built tonight from findings the owner raised: the feed carries a token's name and the hand's comment, the served editor's first paint arrives folded, a gate refuses its thumb over a record's open work with emergency armed, and a state the walk stands on can be left because its verdict is finally consumed.

THE BANNER IS RIGHT AND IT MATTERS HERE. The running lane serves 8.1.0 while the tree is at 9.0.0, so this gate is judged from a lane that predates the version it is shipping. A reload at idle is owed.

## market_block

not a to-market iteration, so real-world validation does not block this ship. The work moved the engine's own model and nothing goes outward to a customer.

## round_0_verify

- evidence vs claims: every claim on this form cites a file, a measurement or a run in the call log; the three separations this record did not buy are named at three gates rather than once
- types: clean, run by the lane after every source edit
- lint: biome clean over 428 files
- tests: 2275 of 2275 across 235 files, sweep green over 3,264 nodes, and the package check found nothing wrong

## round_1_validate

- exercised against the goal: yes, by the walk that built it. Every mechanism this iteration added was used by this record's own walk before it was judged.
- missing: the install. RUNME.ps1 was not run, no extension was loaded from the archive, and no walk was started inside it. The archive is well formed and unproven on a fresh machine.
- wrong: nothing found wrong in the package. The one thing wrong in this record is the demos drawing, named at the validation gate.
- out of scope: the sweep budget, the three bless doors, the flaky wall-clock assertions - all three carried to the retro with notes.
- prior art: NOT COMPARED, for the third time on this record. No packaging or work-tracking system people actually use was named and measured against this one.

## goals_served

- Every piece of work is a work token: one markdown file per item, carrying frontmatter and prose together.: served and shipped - the store, the mint and the editor all travel in the archive
- A position has TWO SLOTS. Incoming holds what must be taken in before it can be worked. Outgoing holds what must be produced before it can be left.: served - slotOf and the leaving guard ship together
- A method's steps become outgoing tokens, one per marked heading, each carrying its own guidance in the body beneath it and its evidence in subheadings under that.: served - 329 marks across 76 of 87 cards travel with the method
- Reading requirements become incoming tokens, and only where the evidence is not already proven. Read evidence is global and version-keyed.: served - the read credit ships and was exercised across a reload tonight
- A token has a PLACE and a STATUS, and they are separate. Place is a position or the backlog. Status is open, in work, or one of several terminal kinds.: served - both fields ship on every token
- A position may be left when every token in it has reached a terminal status or moved elsewhere. Moved is a real exit, not a failure.: served and widened tonight to hold a whole record at a gate
- A token may depend on another token, or on a position finishing.: served by place, which ships
- Outside a record everything is ephemeral. Inside a record a done token IS the evidence, and there is no second act of writing it.: served in part - lifetime ships and the second act still happens, which the walk-a-state deck says plainly
- The pull returns open tokens rather than instructions.: served - the pull's do carries them and did so at every state tonight
- The four ladders become two. Complexity is a ROUTING key that decides which hand a token is given to. Autonomy is unchanged.: served - sizing ships and was observed at seven spawn states
- Every position shows a count per slot, and clicking one opens the token editor.: served in the data and in the served card; the click is unexercised
- THE ARCHITECTURE ANALYSIS LATER IN THIS ITERATION SWEEPS EVERY PLACE WORK IS DONE, so nothing the work token system touches is missed. The state machines and the token editor are the two largest surfaces, and they are not the whole list.: served - the sweep ships and runs green over 3,264 nodes

## bound_breaches

- if-agent-harness-to-entrypoint: none over its bound this window. The per-hop sweep cost improved rather than breached: 3,800 ms per green hop before tonight's reload, 600 to 2,100 ms after.

## round_2_red_team

- STEELMAN: the version was bumped to a major by the agent, and a version is an outward promise nobody asked it to make => The strongest form is that 9.0.0 tells every reader something about compatibility, and the agent chose it. It survives because the change size was PINNED major by the owner at the kickoff, 8.1.0 had already shipped, and the archive is named from the manifest rather than from a judgment made at packing time. It is one edit to reverse and the reasoning is on the package form.
- KILL-CRITERION: this would be the wrong call if the archive did not actually come up => Looked for it and could NOT settle it. The package was inspected, not installed. That is the honest limit of this gate and it is the one thing a fresh machine would answer in minutes.
- THE PACKAGE WAS CHECKED BY THE HAND THAT BUILT IT => This state's own motivation says why that is the weak version. Third time this record records an unbought separation, at the implementation gate, the validation phase and here.
- THE LANE SHIPPING IS OLDER THAN THE TREE => The banner says the running lane serves 8.1.0 while the tree is 9.0.0. So the engine changes made tonight are in the ARCHIVE but were not serving the walk that judged it. The battery ran against the tree, so the code is proven; what is unproven is the lane's own behaviour on 9.0.0 sources, and a reload answers it.

## raid_additions

- none

## verdict

pass with overrides — The archive assembles by script, carries what a stranger needs, leaks none of the six things that must stay home, and renders its front door with no placeholders. The tree behind it is green at 235 files. THREE OVERRIDES, ALL CARRIED FORWARD RATHER THAN NEW. The package was inspected and not installed. The hand that built it checked it. Prior art was not compared. Each is named here as at the earlier gates, and the install is the one that could be closed cheapest - a fresh machine and ten minutes. A fail would say the package does not stand, and it does.

## follow_up

- Run RUNME.ps1 from the archive on a machine that has never held this tree. It is the cheapest unbought check in the record.
- se_reload at idle, which the banner asks for: the running lane serves 8.1.0 against a 9.0.0 tree.
- note-c71d78487f20, note-13326ca46434 and note-bedbeb45e2e7 wait for the retro, along with the demos-drawing check named at the validation gate.

## anything_else

