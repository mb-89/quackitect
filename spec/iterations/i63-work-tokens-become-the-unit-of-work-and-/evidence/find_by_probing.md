---
form: find_by_probing
by: agent
signed_off: 2026-08-26T10:37:04.121Z
authors: agent
files: null
---

# Evidence form / find_by_probing

## current_situation

The register carries three probes that should run before the design rests on anything, and the owner ordered the drag spike first. That one needs a screen and their word, so it is not this state's to run.

WHAT THIS STATE COULD RUN IS THE COUNTING QUESTION, and it turned out to be the one that mattered. The prior-art sweep had just found two shipped trackers refusing files on volume grounds, and nobody had ever counted what our own volume would be.

## applies

yes

## probes

| question | timebox | what_was_faked | verdict |
| --- | --- | --- | --- |
| how many work token files would ONE record's folder hold | one script, minutes | the state count came from this machine and is a major's, so it is an upper figure rather than an average; and evidence sections were counted as a proxy for work tokens, which holds only if one marked step becomes exactly one work token | 32 filled positions carry 201 evidence sections, at 4 to 15 each with a median of 5. Across 64 states that projects to 320 at the median and 402 at the mean |
| does that number put the whole vault past what a person can browse | one script, minutes | nothing was faked; the record count and the node count were both read off the tree as it stands | 69 records stand on trunk. At 320 to 402 each that is 22,080 to 27,738 work token files, against 1,821 trace nodes standing today. The corpus would grow twelve to fifteen times over |

## options

- opt-work-tokens-are-folded-into-the-evidence-when-a-record-closes

## dead_ends

- the cross-panel drag spike was NOT run here. It needs a screen, and looking at one is the owner's word to give per request. It stays where the register put it
- the retro card split was NOT run here. It is a probe about a method card's format, and it belongs to whoever next edits that card rather than to the enumeration
- no probe was run against the object store or the replay log. Both would need something built, and the counting question answered the design decision more cheaply

## follow_up

THE ASSUMPTION IS FALSIFIED AS WRITTEN, and it should be flipped rather than probed again. The register asks whether one file per work token stays workable in the vault and the repository. Twenty-two thousand files against eighteen hundred standing nodes is the answer, and it is no.

WHAT IT DOES NOT FALSIFY is the file for a record that is OPEN. Four hundred files in one folder is browsable, and that is the distinction the new option rests on.

THE PROBE COST TWO SCRIPTS AND ANSWERED A QUESTION THREE FINDERS HAD BEEN CIRCLING. That is the case for this finder existing, and it is worth saying plainly: every other finder reasons about options, and this one measured one.

## anything_else

