---
form: position-token-count
by: agent
signed_off: 2026-08-26T14:22:31.083Z
authors: agent
files:
---

# Evidence form / position-token-count

## current_situation

Counted over the 137 real method cards rather than an invented example: 424 marked headings in total, median 2 per card, largest 23.

THE ASSUMPTION MOSTLY HOLDS. The median position owes two things, so a count read at a glance is fair for almost every card in the tree.

ONE OUTLIER DECIDES THE DESIGN. meth-derive-criteria carries 23, the gate review 15, requirement authoring 14. The surface must draw 23 without becoming unreadable while drawing 2 almost every time.

SO THE DESIGN QUESTION CHANGES SHAPE: not whether the number is small, but whether the rare large one degrades gracefully. A surface built only for the median breaks on the card people spend longest in.

## built

- exp-how-many-items-a-position-actually-owes

## follow_up

The surface work is the mirror's and this only tells it what to expect. Nothing must be built here.

ONE CAVEAT THE SIBLING SPIKE CREATES. These counts assume a heading is a step, and that rule was found too narrow. Once cards carry an explicit mark the counts move: the gate review falls from 15 to about 2, and the retro rises from 1 to 12.

THE SHAPE OF THE ANSWER SURVIVES THAT. A median in single figures with a rare outlier in the twenties is what the surface has to handle either way, so the finding stands whichever marking rule wins.

WHAT THIS DOES NOT MEASURE, said plainly: whether a person reads the number faster than some bar. The row it serves asks for a two-second read, and that is a watched attempt rather than a count. This experiment measures the number.

## anything_else

