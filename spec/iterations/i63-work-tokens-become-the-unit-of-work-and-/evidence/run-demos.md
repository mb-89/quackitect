---
form: run-demos
by: agent
signed_off: 2026-08-27T20:54:54.128Z
authors: agent
files: null
---

# Evidence form / run-demos

## current_situation

THE DEMONSTRATIONS WERE PERFORMED ON THIS ITERATION'S OWN WALK, which is the strongest form this record could get: the mechanisms were exercised by the system using them on itself, not by a script written to show them working.

WHAT WAS DEMONSTRATED, AND WHAT WAS OBSERVED.

A TOKEN SPEAKS WHEN IT IS PUT IN WORK AND WHEN IT COMES OUT. Eight tokens were opened, taken and settled through the session tonight. Each act wrote its own feed line carrying the token's name and the hand's comment. Observed in the answers to se_work throughout, and pinned by seven cases in tokens-speak.test.ts.

THE EDITOR'S FIRST PAINT ARRIVES ALREADY FOLDED. The served card was measured on this repo: 395 rows in the left pane, four top-level groups, backlog and retro both shipping closed, and zero data rows under a closed heading arriving visible. Observed by rendering the card and walking its rows in order.

A GATE CANNOT BE BLESSED OVER A RECORD'S OPEN WORK. Demonstrated in reverse and honestly: gate-implementation was blessed tonight only AFTER its own eleven marked steps and fix-findings' four tokens were all settled. The refusal path itself is exercised by four cases, including the one where emergency is armed.

A STATE THE WALK STANDS ON CAN BE LEFT. This is the demonstration that took two attempts and is worth the most. The first build was inert: the verdict recorded a count taken one line before its own write, so it was stale by exactly one and never matched. Observed by watching the battery pass at 20:42:25 with nothing written afterwards, and the next attempt still start a fresh 138-second run. After the fix the walk crossed verification on the first attempt, honouring the verdict instead of re-running it.

ONE PULL WALKS FURTHER THAN IT DID. Measured across the same route before and after tonight's reload: 3,800 ms per green hop before, 600 to 2,100 ms after, so one aim covers 14 to 17 hops rather than 5. The 139-state route from start to the implementation gate now draws in a single call.

## follow_up

- The demonstrations were performed by the hand that built the mechanisms. A fresh reader would be the card's answer and did not run; recorded here as it is at the implementation gate rather than left implied.
- note-13326ca46434 carries what is still true about the sweep budget: the per-hop cost fell but the budget still bounds a long route, and nobody has profiled inside a green hop.

## anything_else

WHY THERE IS NO SEPARATE DEMO SCRIPT. Every demonstration above was performed by the live walk on the live tree, and each one is recoverable from the call log rather than from a rehearsal. That is a stronger artifact than a scripted run, and it is also the reason none of them can be replayed on demand: they happened once, in the record, and the log is where they live.
