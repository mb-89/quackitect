---
form: partition-functions
by: agent
signed_off: 2026-08-26T12:26:19.146Z
reopened: "2026-08-26T12:26:13.626Z — derive-criteria was re-signed after it, and the three new rows land on functions that already have their cluster"
authors: agent
files:
---

# Evidence form / partition-functions

## current_situation

Sixty-eight functions stand across three roots. Five of them are this round's, and the engine's clustering search left all five unassigned.

THE FIVE COUPLE TIGHTLY TO EACH OTHER. Four of the edges between them carry the same flow, which is the piece of work itself.

NOTHING ELSE MOVED. Every standing function keeps the cluster it already had, because this round changed what a position hands out rather than how the system is partitioned.

RECHECKED 2026-08-26 AFTER THE MILESTONE REOPENED. The owner added two storage options. Storage is a choice about how the work cluster is realised, not about which functions belong to it, so the partition below is untouched.

## clusters

| element | cluster |
| --- | --- |
| [[fn-run-a-governed-walk.mint-what-a-state-owes]] | [[cluster-the-work]] |
| [[fn-run-a-governed-walk.place-work-where-it-will-be-done]] | [[cluster-the-work]] |
| [[fn-run-a-governed-walk.offer-the-work-that-is-ready]] | [[cluster-the-work]] |
| [[fn-run-a-governed-walk.settle-a-piece-of-work]] | [[cluster-the-work]] |
| [[fn-run-a-governed-walk.count-what-is-owed]] | [[cluster-the-work]] |

## follow_up

THE TWO TEAR CANDIDATES ARE NAMED ON THE CLUSTER NODE, and the enumeration should treat them as real seams. Counting hands its figure to the surface; settling hands its result to the judgment and to the work account. A candidate that allocates either elsewhere pays one thin link.

ONE PRE-EXISTING GAP WAS SEEN AND NOT FIXED HERE. Eight benchmark functions carry a cluster name that has no cluster node behind it, so the partition names a group nothing defines. It belongs to whichever round owns that subsystem.

THE SECOND WALK ADDS ONE THING FOR THE ENUMERATION. Counting what is owed is the function the storage choice hits hardest. A folder listing, an index scan, a line scan over one file and a query are four realisations of one act, and the two new options pick the third.

## anything_else

