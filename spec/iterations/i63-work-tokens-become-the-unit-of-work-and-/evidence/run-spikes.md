---
form: run-spikes
by: agent
signed_off: 2026-08-26T14:14:05.396Z
authors: agent
files: null
---

# Evidence form / run-spikes

## current_situation

Seven spikes were picked and all seven are answered. Five hold, one fails, and one turned out to be two questions of which only the unneeded half was at risk.

THE SHARPEST ONE HOLDS BY THREE ORDERS OF MAGNITUDE. Minting what a position owes runs on every entry, and it costs a median of 0.039 ms against a 1000 ms per-hop budget. The worst single card is 0.570 ms, which is 0.06 percent of the budget.

ONE FAILS, on the card the register named as the case that would decide it. The retro's steps are a numbered list inside one heading spanning 342 lines, so minting on marked headings would produce one item covering twelve acts.

ONE WAS MIS-SIZED RATHER THAN MIS-ANSWERED. The cross-panel drag was held as able to reverse the design. The drag the design needs is inside one editor and crosses nothing; the drag that does cross two webviews is required by no goal in this record.

TWO CLOSED ON THE OWNER'S RULINGS RATHER THAN ON A PROBE, and both were my misreading rather than a design fault.

## follow_up

THE ONE THING THE BUILD MUST DECIDE, because a spike proved the rule too narrow.

A method's marked headings do not carry every card's steps. The retro writes twelve steps as a numbered list under one heading. Three ways out stand on the entry: rewrite the card, widen the minter to read a top-level numbered item, or declare the card an exception. Widening the minter looks right and is not the spike's to choose — a card's steps are whatever its author wrote as a sequence, and headings are one way of writing that.

THREE FIGURES THE BUILD INHERITS.

Minting costs 0.039 ms median and 0.570 ms worst, against 1000 ms. It measures deriving the set, not writing it, and a mint that also writes one file per item pays a write nobody has timed.

A work token's longest path is 203 characters of 260, which is about thirteen characters of folder depth from failing. Nothing caps the record folder name today.

A position owes a median of 2 items and at most 23. The surface's problem is not whether the number is small but whether the rare 23 degrades gracefully.

ONE HALF-ANSWER, said plainly. One file per item is measured against the repository and NOT against the vault. Nothing here opened the vault or timed its index, and 424 more markdown files being small is an argument rather than a measurement.

ONE QUESTION LEFT DELIBERATELY OPEN. Whether two webviews can exchange a drag at all. The vendor documentation was not read, so nothing here asserts they cannot — only that the code shows them as separate documents. It is cheap to leave open because nothing this record ships depends on it.

## anything_else

