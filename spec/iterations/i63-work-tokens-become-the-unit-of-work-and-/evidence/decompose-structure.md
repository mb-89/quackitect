---
form: decompose-structure
amended: "2026-08-26T13:29:01.708Z by agent — the standing element count was one short, and the cut was stated as write-against-read when the store serves two reads and the fold belongs to the record store"
by: agent
signed_off: 2026-08-26T13:04:10.885Z
authors: agent
files:
---

# Evidence form / decompose-structure

## current_situation

The winner becomes structure. Twenty-nine elements already stand and the matrix named five functions no element implemented: minting, offering, settling, placing and counting.

THOSE FIVE ARE THE WORK CLUSTER, and they are what this round adds. Two new elements carry them.

THE CUT IS ON WRITES. The store writes every piece of work and the offer writes none. Stating it as write-against-read overstates it, because the store serves two reads itself: what a position has settled, and the standing position it must know before it writes.

THE FOLD IS NOT IN EITHER OF THEM. Closing a record is a record-level act, and the record store already implements closing and the archive. Putting the fold in the work store would give one close two owners.

EVERYTHING ELSE IN THE MATRIX IS INHERITED. The arrival, the bootstrap, the query, the benchmark run and the record's life keep the elements they had.

## elements

- el-work-store
- el-work-offer

## allocation

THE MATRIX IS COMPUTED and this section carries the argued spread rather than the cells.

THE FIVE UNIMPLEMENTED FUNCTIONS ARE NOW ALLOCATED, three to the store and two to the offer.

- el-work-store takes minting, placing and settling. Every act that WRITES a piece of work.
- el-work-offer takes offering and counting. Every act that READS one.

WHY THE CUT IS THERE AND NOT ELSEWHERE. Two arguments, and the second is the one that decides it.

- THEY FAIL DIFFERENTLY. A wrong write loses work. A wrong read shows a wrong number and is recoverable by asking again.
- THE READ IS ON THE HOT PATH AND THE WRITE IS NOT. Every look at a position counts what it owes; only entering one mints. Folding them puts a derivation on every entry, which is exactly the cost the round's sharpest open assumption is about.

THE PARTITION'S TWO TEAR CANDIDATES ARE BOTH HONOURED. Counting hands its figure to the surface as a flow rather than letting the surface invent one. Settling hands its result to the judgment and to the work account, and stays with the writes because it is one.

ONE THING THE MATRIX REPORTS THAT THIS ROUND DOES NOT FIX. `el-arrival` implements nothing and ten interfaces are undemanded. Both are pre-existing and belong to whichever round owns those subsystems.

THE FOLD'S READERS ARE NOT YET ALLOCATED. Fourteen engine files read the iteration folder from disc across 33 sites, and the decision says each learns the folded file or the commit read. Which element owns that translation is the implementation milestone's, and naming it here would freeze a mechanism.

## follow_up

THREE THINGS THIS MILESTONE FOUND ARE OWED TO THE IMPLEMENTATION.

- THE FOURTH DOOR. `se_git`'s allowlist carries `show`, so an agent reads any folded blob at every autonomy setting. Three of four archive doors are closed and the person-only demand is not met.
- WHAT FOURTEEN ENGINE FILES DO once the archive read is person-only. The candidate says the read is a version-control operation for the system and for a person alike; the person-only graft deletes the system half and nothing replaces it.
- A FRAME BUDGET FOR THE PLACEMENT SURFACE. The revealed destination has to re-render mid-drag, and the standing one-second bound would pass a design that hitches every drag.

THE MISSING DISPOSITION FIELD IS NOW ALLOCATED. It lives on a piece of work, in the store, and the close is refused until it carries a reason.

THAT CREATES A KNOWN DIVERGENCE. A state still handing out an evidence form has nowhere to record why something was closed undone, so the two models differ on one more dimension until every state mints work.

THE ARCHITECTURE EVALUATION IS NEXT, and it reads this structure against the coupling the partition drew.

## anything_else

