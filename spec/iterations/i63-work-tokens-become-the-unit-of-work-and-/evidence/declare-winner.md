---
form: declare-winner
by: agent
signed_off: 2026-08-26T12:54:28.227Z
authors: agent
files: null
---

# Evidence form / declare-winner

## current_situation

Archive in git holds the seat. It led both convergence runs, the result is stable, and no rival overtakes it by taking anything it does better.

WHAT IT IS. Editable files while an iteration is open, exactly as the round proposed. At close the whole iteration folds into one file and leaves the working tree, and it is read back out of version control.

THE ROUND'S OWN GOAL SURVIVES, changed at one end. One markdown file per piece of work is what a person still edits. What changes is what happens to those files once nobody edits them again.

THE MARGIN IS THIN AND THE RECORD SAYS SO. Fold at close reaches a tie by taking two documentation acts, so the seat is held by a nose rather than by a length.

## winner


## comments

THE SELECTION RESTS ON THREE MEASUREMENTS rather than on preference, and each is repeatable from a script in the scratchpad.

- Reading an archived iteration out of version control is FASTER than reading the working tree at scale: 114 ms against 161 ms at 20,000 files.
- Writing is where the file-per-token shape hurts: git add 26,073 ms against 97 ms folded.
- The tree stops growing. 68 folders, 1,312 files and 9.7 MB stand on trunk today and nothing opens them.

WHAT THE WINNER IS WEAKEST AT, said before anybody discovers it downstream.

- IT BREAKS 37 PROSE CITATIONS by its own count, and a regex can rewrite the path but not keep it a link that opens something.
- IT INHERITS THE MERGE SURFACE WHOLE while an iteration is open, and records no cost for it. Inverted scores twice as well there and it is the only prior-art par any rival holds.
- IT HAS NO PERSON-ONLY DOOR on the three verbs that read at a commit, so the archive's person-only rule is weaker under it than under any rival.
- IT KEEPS THE MOST BYTES AND RECORDS NO DISPOSITION. Retention is not disposition, and no candidate names a field carrying why work was closed without being done.

ONE CLAIM IN THE CANDIDATE'S OWN PROSE IS WRONG AND IS NOT CORRECTED HERE. It says the fold is irreversible in practice. Its own premise defeats that: history is never rewritten, so the pre-fold folder stands at its commit and a bad format is re-foldable from that ref. The scorer scored the design rather than the sentence, and correcting the sentence after seeing the score is the edit the compose-and-judge split forbids.

THE OWNER'S CONDITION BINDS THE BUILD. Version control counts as kept only where the commit can be named, so a design that deletes owes an address recorded at the moment of deletion. "It is in the history somewhere" is not an address.

## follow_up

THE STRUCTURE COMES NEXT, and the graft is the first thing it owes.

ONE GRAFT IS STRUCTURAL AND THE LEADER CANNOT WRITE ITS WAY TO IT. Inverted's non-colliding writes, where no position keeps a list so two hands adding work touch disjoint bytes. That is a relation shape rather than a document, and it is the only cell where a rival reaches prior-art par.

TWO GRAFTS ARE CHEAP AND ARE OWED ANYWAY. Naming every place work is modelled, and saying where the open-note count comes from. Both are acts the leader already performs, and a rival taking them ties the score.

FIVE THINGS FROM THE CANDIDATES GATE ARE STILL OWED. The missing disposition field, the cross-panel drag spike, the predecessor read, two candidates' prose that argues them down, and a second reader on the broken-trace cell.

ONE REQUIREMENT MUST BE DELETED BEFORE THE BUILD. req-a-closed-records-folder-stays-on-trunk was rewritten to demand readability and is now redundant with two standing archive rows. Its deletion is owed at a state that allows one, carried as note-e030fcec34b2.

AND ONE STANDING TEST GOES WITH IT. deliverable/tests/onetree.test.ts asserts a closed record's folder stays in the working tree. It passes today because the engine is unchanged, and the winner reverses exactly what it asserts.

## anything_else

