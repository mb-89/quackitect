---
id: q-cli-steering
type: question
state: decided
decided_via: A - block it; an agent-channel ledger command over the bare CLI is refused with a pointer to the MCP tools
statement: Should the bare CLI stay open on the agent channel once the MCP tools serve?
class: review
killer: false
provenance:
  state: user-ruling via chat
  decided_via: user-ruling via chat
---
## Options
A) Block it. An agent-channel ledger command over the bare CLI is refused with a pointer to the MCP tools.

B) Steer it. The command runs. The reply carries a hint toward the MCP tools.

C) Leave it. Both lanes stay equal.

## Rationale (not load-bearing)
Blocks the final shape of req-cli-steer. The owner rules.
