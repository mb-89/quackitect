---
form: gate-implementation
bless: blessed by agent
by: agent
signed_off: 2026-08-21T13:35:38.366Z
authors: agent
files:
---

# Evidence form / gate-implementation

## current_situation

THE BATTERY IS GREEN and the build is complete. Nine build chunks, a fresh-eyes verification, and two fix passes closing every finding it raised.

WHAT THIS RECORD BUILT, one line each.

- ONE CALL LISTS EVERY PIECE OF WORK OUT OF SIGHT. Shell jobs, scoped test runs and a step's leaving judgment are one table, and the account rides beside every lane answer without replacing it.
- EVERY ENTRY SAYS HOW MUCH LONGER AND WHAT THAT RESTS ON. The figure and its basis are one value, so a duration a reader cannot discount never appears alone.
- A LONG LEAVING CHECK NO LONGER FREEZES THE PULL. The judgment starts, the call answers inside its bound, and the verdict lands on a later call.
- A DIFF NO TEST ANSWERS FOR RUNS NOTHING FROM THE SUITE, and names every uncovered file.

IT PROVED ITSELF ON ITS OWN BOOT THIS SESSION. `prepare_idle` takes 3.6 seconds; the first pull was answered at 760 ms with the check named as running, and the outcome arrived on the next call. The same held here: a 98-second battery ran with the walk answering throughout.

## quality_ok

- [x] Dependencies stay layered
- [x] Every new element carries one stated responsibility
- [x] The linter and the complexity ceiling are clean, with no new suppression
- [x] Every new behavior carries its check, and the battery is green at rest
- [x] Nothing speculative shipped
- [x] What changed is findable
- [x] Every quick-and-dirty taken stands as a visible raid debt entry

## debt_taken

- [[raid-debt-the-route-drawer-reads-a-standing-as-a-boolean]]

## risks_acceptable

acceptable — the two entries added this iteration both err toward refusing work rather than passing it, and each carries a trigger and what would close it

## round_0_verify

- evidence vs claims: every claim in this record's forms rests on a run or a file, and the ones that could not be checked are named open rather than left blank
- types: tsc exit 0 across the tree, measured this session
- lint: biome checked 352 files, no fixes applied, no warnings
- tests: the battery is green at rest — biome, preflight, selftest, and the conformance sweep over 2504 nodes

## round_1_validate

- exercised against the goal: yes, and on this session's own boot rather than only in a fixture
- missing: the whole answering call is never timed, only the bounded wait inside it — recorded with what would close it
- wrong: nothing found standing. Nineteen findings across two verification rounds are closed or recorded
- out of scope: the route drawer still flattens the standing to a boolean, which the requirement itself names as the safe direction
- prior art: the predecessor carried this iteration's load-bearing goal in `responsiveness.md` at ref main and it was never carried into v3. This record restored it

## goals_served

- One lane call reports every piece of work running out of sight, each entry saying how much longer it needs.: SERVED. Measured on this boot with six judgments in one answer, each carrying its standing and its basis.
- A step whose leaving condition runs a long program answers at once and hands its verdict back on a later call.: SERVED and measured. The bound is 750ms, and the timing case measures the call rather than reading the source.
- The engine picks which tests answer for a change, so a documents-only edit stops firing the whole battery.: SERVED. A diff nothing answers for runs nothing and names every uncovered file, with the whole list on the decision.
- Engine improvements, the standing goal, holding the two defects found entering this record.: SERVED, and the record found more than it entered with.

## bound_breaches

- if-agent-harness-to-entrypoint: no breach. The lane answered every call this session. One denial came from the host's own permission layer above the lane, carrying no clause and no remedy, which is how the card says to tell the two apart.

## round_2_red_team

- the account could report a stale run as current => FOUND AND FIXED. openOperation returned early on any existing id, so a re-run never entered the table. Measured 98 seconds stale on this record's own verification.
- a failing check could hide why it failed => FOUND AND FIXED. The refusal now carries the previous run's output, and the rule is written into guidance as NO BLANKET ERROR MESSAGE.
- the stall detector could call healthy work stalled => FOUND AND FIXED. It counted reads of the table; it measures the run's own clock now.
- a refusal could spend state it was not entitled to => FOUND AND FIXED. Only opted-in decorators run on a refusal.
- a gate could read a green it had not earned => FOUND AND FIXED. The gate asks whether a feeder is still deciding, not only whether it signed.
- the timing case could pass while the bound rose => FOUND AND FIXED. It reads the constant out of the source instead of copying it.

## raid_additions

- [[raid-debt-the-route-drawer-reads-a-standing-as-a-boolean]]
- [[raid-risk-the-whole-answering-call-is-never-timed]]

## verdict

pass — every requirement is served and measured, nineteen verification findings across two rounds are closed or recorded with triggers, and the battery is green at rest

## follow_up

- VALIDATION AND RELEASE are next and nothing blocks them.
- THE FIELD REPORT is owed to the owner as a downloadable file, out of version control, per the ruling recorded this session.
- FOUR NOTES STAND for design work the owner raised: aiming at the front desk should trigger an escape; walking back should always be allowed, with rewind and stepping back distinguished; a judgment that runs twice needs an accounting shape; and a judgment running the battery could carry a figure since the battery writes progress.
- WINDOWS AND macOS stay unmeasured for [[raid-asm-a-check-left-running-survives-on-every-platform]].

## anything_else

THE FRESH-EYES PASS IS WHAT MADE THIS RECORD HONEST. The build looked finished after nine signed chunks and a green battery. A tester with no share of the builder's context found fourteen items, and its second round found five more that the first round's fixes had introduced. Green was never the same as right, and the gap was measured rather than argued.
