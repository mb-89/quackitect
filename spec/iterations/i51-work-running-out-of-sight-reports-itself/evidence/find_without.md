---
form: find_without
by: agent
signed_off: 2026-08-21T09:30:53.321Z
authors: agent
files: null
---

# Evidence form / find_without

## current_situation

Finder five of the fan. Three signed, three still owed after this one.

The question was asked of every cluster this change touches, and of every piece inside them, including the load-bearing one.

One trim produced an option. Three produced a reason to keep what is there, which the method counts as a result.

## applies

yes

## trims

| cluster | who_takes_over | what_breaks |
| --- | --- | --- |
| the-record-life, the estimate | the caller, by asking again on an interval it guesses | the caller still does not know how long to wait, so it polls with fewer calls rather than not polling |
| the-record-life, the unified listing | the caller, by learning two doors and remembering which holds what | the refusal for an unknown test job keeps pointing at a list that cannot hold it, which is already failing in the code |
| the-walk, the deferred verdict | nobody | the caller waits for the whole check, and past the harness's limit is told the work failed while it had moved |
| the-record-life, the narrower test scope | the agent, by waiting out a battery it did not need | a minute per occurrence and nothing else; this is the trimmable piece |
| the-walk, the third standing value | nobody | the deferred verdict becomes unbuildable, because a step between passed and failed has nowhere to be |

## options

- [[opt-ship-the-non-freezing-exit-and-no-estimate-at-all]]

## follow_up

Three finders are still owed: heuristics, transforming and probing.

One trim is worth carrying to the candidate evaluation rather than leaving in a row. Dropping the estimate produces a real, smaller design that still gets the load-bearing goal, and it should be scored rather than dismissed.

One row is a decision the design owes. The narrower test scope is the trimmable piece, and if the iteration runs long it is the piece to drop.

## anything_else

THE TRIM THAT MATTERED WAS THE ONE I EXPECTED TO WIN AND NO LONGER DO.

Written before the probe ran, dropping the estimate would have been the recommendation. It carries two register entries, one graded corrosive and expected, and it rests on an instrument recorded as wrong by a factor of twenty. It is also the only part of this iteration that can be WRONG rather than merely absent.

THE PROBE MADE IT CHEAP. A running job already reports `cases_done`, `files_touched` and `files_total` beside `elapsed_ms`. The numerator, the denominator and the clock are all on the answer already, and the missing work is a division.

SO THE TRIM IS STILL THE SAFE OPTION AND NO LONGER THE CHEAP ONE. That is what a trim is for: it did not change the answer, it changed what the answer rests on.

AND THE METHOD'S FAILURE MODE WAS AVOIDED ON PURPOSE. Trimming what is cheap feels productive and saves nothing. The expensive piece here is the estimate, and it was the first thing asked about rather than the last.

THE LOAD-BEARING PIECE SURVIVED THE QUESTION and is better justified for it. Nothing takes over from the deferred verdict. Not another cluster, not the environment, not the caller. Its absence is the measured failure this iteration exists to end.
