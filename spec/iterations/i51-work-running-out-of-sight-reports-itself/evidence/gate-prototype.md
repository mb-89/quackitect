---
form: gate-prototype
bless: blessed by agent
by: agent
signed_off: 2026-08-21T10:49:08.150Z
authors: agent
files:
---

# Evidence form / gate-prototype

## current_situation

Three spikes ran, one held and two fell, and every one settled inside its timebox.

THE DESIGN IS BUILDABLE AND SMALLER THAN M6 STARTED WITH. Each falling spike named a fix rather than opening a question.

NO REQUIREMENT IS REOPENED and the winner is untouched. The structure gained detail, not shape.

THE ONE RED THE SPIKES SURFACED IS FIXED. Five nodes this record minted had no design spec realizing them; two specs now do.

## buildable

yes — every part of the design now rests on something measured on this machine rather than on an argument. The load-bearing mechanism is proven: a leaving check left running after its call answered ran to completion and its verdict was readable, and the call answered in 4 ms against a 3-second judgment, using the product's own spawn shape. The hardest open question has a two-field answer — the operation record owes the state it belongs to and the total its progress divides into, both additions to a record that already exists. WHAT IS NOT PROVEN, named rather than glossed: Windows and macOS were not reached, and process lifetime after a parent exits is the half the operating system decides rather than Node. THE ESTIMATOR IS THE WEAKEST PART — a linear projection over-predicted in one measurement and under-predicted in another, so its direction is not dependable. The design survives that because the basis travels beside the figure, not because the figure is good.

## round_0_verify

- evidence vs claims: every sentence in the three experiment nodes carries its own measurement with a clock time or a millisecond count. The two register entries were rewritten to match what was measured, and one of them was rewritten because the measurement contradicted it.
- types: not run — se_run and se_test are not legal at fold-back or at this gate. No TypeScript changed in M6; every edit was a markdown node or a scratchpad script.
- lint: green at 10:40 in the third spike's own run — biome checked 350 files in 966 ms with no fixes applied.
- tests: RED and then fixed, unverified. 1715 of 1716 passed; the one failure was the design coverage sweep naming five nodes this record minted. Two design specs answer it and the sweep re-runs at verification, which is where the fix is confirmed.

## round_1_validate

- exercised against the goal: yes, and by running rather than arguing. One spike measured the handback's spawn, one measured what a fresh session can see, one measured a real deferred verdict end to end.
- missing: a platform. Two of three supported platforms were not reached, and the assumption's trigger is left armed for that reason.
- wrong: one register entry was. [[raid-ar-walk-resumes-from-repo]] said the work is lost when a session ends; the measurement says the work finishes and its answer has nowhere to go. The entry is rewritten.
- out of scope: promoting spike code. The throwaway law binds, nothing is promoted, and the findings reached the build as a design spec instead.
- prior art: the product's own machinery, twice. The spawn shape came from sessionscript.ts and the pace sentence came from a live test job's record, so neither the handback nor the estimate is being invented from nothing.

## goals_served

- One lane call reports every piece of work running out of sight, each entry saying how much longer it needs.: served, and the spikes measured why it is hard — two job tables stand on disk, `.se/jobs` with 35 entries and `.se/test-jobs` with 1, and neither can see the other.
- A step whose leaving condition runs a long program answers at once and hands its verdict back on a later call.: served and proven. The call answered in 4 ms while a 3-second judgment ran, using the product's existing spawn.
- The engine picks which tests answer for a change, so a documents-only edit stops firing the whole battery.: not advanced this milestone, and the run showed the cost plainly — 131 changed files had no test that answers for them, so the engine chose the whole battery for a diff of markdown nodes.
- Engine improvements, the standing goal, holding the two defects found entering this record.: served. Both defects now have an element, a contract and a measurement behind them.

## bound_breaches

- if-agent-harness-to-entrypoint: still breached and untouched by M6. No spike measured lane latency, and the narrowing claimed at gate-architecture rests on removing the inline await at deliverable/engine/session.ts:3686, which is not built yet. The entry stays open and unmoved by this milestone.

## round_2_red_team

- The two falling spikes might just be re-stating what the risks already said => no, and the difference is the fix. [[raid-ar-walk-resumes-from-repo]] said the work is lost; the measurement says the opposite and points at two missing fields. That changed the entry's text and its route.
- A spike that fakes its own subject proves nothing => the faked part is named on each node. The judgment's body was stood in for; the spawn, the job table, the durable record and the deferred verdict were all real. What was measured is what the assumption is about.
- One platform out of three is a third of an answer => true, and it is stated as one third rather than as a pass. The assumption keeps status open and its trigger armed.
- The estimator was measured twice and disagreed with itself => that is the finding rather than a flaw in the finding. It is why [[raid-dec-the-duration-and-its-basis-are-one-return-value]] is load-bearing and not decoration.
- Fixing the design coverage red inside a spike milestone is scope creep => the check's own message says to fix it where the id was minted, and the ids were minted at decompose-structure in this record. Carrying it would push a wall of names onto whoever reaches specify-build next.
- A gate blessed by the same hand that ran the spikes is not a review => true, and it is what the dial permits. The disconfirming questions are written above rather than left for somebody else to ask, and the weakest parts are named in the buildable answer first.

## raid_additions

- none

## verdict

pass — the design is buildable, every seeded unknown has an experiment node with a verdict and a measurement, and the two spikes that fell each named a fix rather than opening a question. THE STRONGEST CASE FOR FAILING IS THE PLATFORM COVERAGE: the load-bearing mechanism was proven on one supported platform out of three, and the untested half is the one the operating system decides rather than Node. It does not fail this gate because the assumption stays open with its trigger armed, and because the fallback is pre-agreed and written down — keep the await and cap what an exit script may do. THE UNVERIFIED FIX IS THE SECOND-STRONGEST CASE: the design coverage sweep is red until verification re-runs it, and that is stated rather than assumed away.

## follow_up

M7 IS NEXT: author-tests, specify-build, observe-red, build-steps, trace-design, verification, fix-findings, gate-implementation.

THREE THINGS GO STRAIGHT INTO AUTHOR-TESTS. The two newly flagged fitness candidates, [[req-work-past-its-bound-says-it-is-working]] and [[req-one-operation-reads-its-input-once]], and the design coverage sweep that is currently red.

SPECIFY-BUILD INHERITS A SPEC ALREADY WRITTEN. [[dsp-the-work-account]] carries the operation's fields, the settle path, the failure behaviour and the estimator's basis. What it does not carry is the rule a fresh session applies to a step it finds still deciding, and that is the fatal risk M7 must close.

BUILD-STEPS IS ALSO WHERE GIT REOPENS. The work through this gate is committed and pushed already, at bb49341c.

## anything_else

TWO THINGS THIS MILESTONE FOUND THAT NOBODY WAS LOOKING FOR.

THIRTEEN CANDIDATE FILES FROM EARLIER RECORDS ARE MISSING from the working tree and no lane verb removed them. Standing elements still point at them. Captured as `note-67f43216996d` and deliberately left out of the commit so history keeps the files.

THE WALK ROUTED ITSELF BACKWARDS FIVE MILESTONES after a spike arrived, because the target reset to the front desk and the route to the desk runs in reverse through the machine. One `se_aim` recovered it and nothing signed was lost. Captured as `note-ac76632ba6bb`, and it is the same sore spot the owner raised by voice today.
