---
form: specify-build
by: agent
signed_off: 2026-08-21T10:57:40.039Z
authors: agent
files:
---

# Evidence form / specify-build

## current_situation

The design specs were written before this state, at fold-back, because the design coverage sweep demanded them where the ids were minted rather than here.

[[dsp-the-work-account]] REALIZES the new element and three of the four new contracts. [[dsp-the-sizing-block]] GAINED the fourth, which is the return leg it always detailed and never named.

THE CHUNK MACHINE IS SEEDED WITH NINE CHUNKS IN THREE STRANDS. Two strands fan out from one shared first chunk; the third is connected to nothing.

NOTHING IS PROMOTED. All three experiments carry `promote: none`, and the findings reached the build as a design spec rather than as code.

## design_specs

- [[dsp-the-work-account]]
- [[dsp-the-sizing-block]]

## promotions

- none

## follow_up

OBSERVE-RED IS NEXT and it records the six failing cases author-tests wrote.

BUILD-STEPS FOLLOWS and it walks the nine chunks. Two strands can fan out to separate builders once `the-table-holds-every-kind` lands; the scope strand can start immediately beside them.

THE DEEPEST CHUNK IS THE FATAL ONE. `a-fresh-session-knows-a-deciding-step` closes [[raid-ar-walk-resumes-from-repo]], and it is last because it needs everything above it to exist before there is anything for a fresh session to find.

## anything_else

TWO LENSES DISAGREED ABOUT THE FIRST CHUNK, and the drawing records which won.

RISK FIRST wanted the handback earliest — it is the load-bearing mechanism and it carries the fatal risk.

PARALLEL FLOW wanted the table earliest, because both other strands lean on it and a lot waiting on three others is a bottleneck drawn in advance.

THE TABLE WON AND RISK FIRST STILL SHAPED THE REST. The handback is the first chunk after the table rather than the last.

SPINE FIRST WAS CONSIDERED AND DROPPED. A thin end-to-end slice would touch the table, the handback and the rider in one pass, and the seams between them are not where the surprise is.
