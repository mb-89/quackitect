---
form: what-a-fresh-session-sees
by: agent
signed_off: 2026-08-21T10:40:17.330Z
authors: agent
files:
---

# Evidence form / what-a-fresh-session-sees

## current_situation

The third standing is not built, so it cannot be left running and looked at. What can be measured is the machinery it would have to live in, and all of that is real.

A 25-SECOND JOB RAN OUT OF SIGHT and the repository recorded nothing about it. Its only trace is `.se/jobs/job-mt2tgjw4-16.jsonl`, and `.gitignore` line 3 excludes `.se/*`.

A LEAVING SCRIPT'S VERDICT IS WEAKER STILL. `deliverable/engine/sessionscript.ts` line 105 holds it in an in-memory Map and line 156 deletes the entry the moment the run settles. It never reaches disk at all.

A STEP'S PASS IS DURABLE. `signed_off` and `by` sit in the evidence form's own frontmatter, in the repository.

SO TWO OF THE THREE STANDINGS SURVIVE and the third has no home anywhere.

THE TWO TABLES THE ITERATION EXISTS TO JOIN ARE TWO FOLDERS. `.se/jobs` holds 35 entries and `.se/test-jobs` holds 1, and neither can see the other.

## built

- [[exp-what-a-fresh-session-sees]]

## follow_up

[[raid-ar-walk-resumes-from-repo]] IS CONFIRMED AND SHARPENED. The fix is smaller than the risk was written to imply.

WHAT M7 MUST BUILD IS TWO FIELDS AND A SETTLE PATH. The operation record needs the state it belongs to and the total its progress divides into. The verdict then lands in the evidence form's frontmatter, where every other durable standing already lives.

THE JOB RECORD'S SHAPE IS THE EVIDENCE. It carries id, command, started, exit, running and pid — no state, no total. Without the first a verdict has nowhere to land; without the second no duration can be computed.

FOLD-BACK SHOULD RESTATE THE RISK. It says the standing is lost. Read with [[exp-does-a-left-check-survive-its-call]], the truth is that the judgment finishes and its answer has nowhere to go.

## anything_else

THE VERDICT IS `falls`, and that is the useful answer rather than a bad one.

The question was whether a session holding only the repository can learn a judgment was running and read its verdict. It cannot, on both halves, and the measurement says exactly which field is missing in which file.

A SPIKE THAT CONFIRMS A RISK EARNS ITS TIMEBOX as much as one that clears it. This one turned a fatal risk with an unknown fix into a fatal risk with a two-field fix.
