---
form: fold-back
by: agent
signed_off: 2026-08-21T10:47:57.946Z
authors: agent
files: null
---

# Evidence form / fold-back

## current_situation

Three spikes ran and all three settled inside their timeboxes.

ONE HOLDS AND TWO FALL, and the two that fall are the useful ones. Each turned a risk with an unknown fix into a risk with a named one.

NOTHING INVALIDATES THE DESIGN. No requirement is reopened and the winner is untouched. The structure gains detail rather than changing shape.

TWO REGISTER ENTRIES ARE REWRITTEN against the measurements. [[raid-ar-walk-resumes-from-repo]] said the work was lost; it is not, and the entry now says the answer has nowhere to go. [[raid-risk-a-hop-that-finishes-later-makes-green-ambiguous]] was a design worry; it is now an observed state with a number on it.

ONE RED CAME OUT OF THE THIRD SPIKE'S RUN and it is fixed here rather than carried. Five nodes this record minted had no design spec that realizes them, and the sweep's own message says to fix it where it was minted.

## folded

| [[exp-does-a-left-check-survive-its-call]] | the spawn shape needs no change: the build stops awaiting and alters nothing else about how a condition script is started | none |
| [[exp-what-a-fresh-session-sees]] | the operation record owes two fields, the state it belongs to and the total its progress divides into, plus a settle path that writes where a fresh session looks | none |
| [[exp-does-a-standing-hold-still]] | a reader needs the operation's start time on the standing itself, because a stale answer and a live window look identical without it | none |

## follow_up

GATE-PROTOTYPE IS NEXT and it reads these three verdicts.

NOTHING IS PROMOTED, and that is the throwaway law rather than a shortfall. The spike scripts are not a head start on the build; the findings are the product and they are already in [[dsp-the-work-account]].

M7 INHERITS A SMALLER JOB THAN M6 STARTED WITH. The design spec names the operation's fields, the settle path, the failure behaviour and the estimator's basis, and each of those sentences rests on a measurement taken this milestone.

THE ASSUMPTION'S TRIGGER STAYS ARMED. Windows and macOS are unmeasured, and process lifetime after a parent exits is the half most likely to differ.

## anything_else

THE DESIGN COVERAGE RED IS WORTH RECORDING because of where it was caught.

The check fired on a spike's own test run, not at verification. Its message says the sweep exists so that a crossing minted today does not surface later on somebody else's iteration as a wall of names they did not write.

TWO SPECS ANSWER IT. [[dsp-the-work-account]] realizes [[el-work-registry]] and the three contracts around it. [[dsp-the-sizing-block]] gains [[if-sizing-to-walk-engine]], which is the return leg it always detailed and never named.

THE FIX IS UNVERIFIED HERE. No test verb is legal at fold-back, so the sweep re-runs at verification and that is where it is confirmed.
