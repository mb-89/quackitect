---
form: find_by_heuristic
by: agent
signed_off: 2026-08-21T09:32:16.019Z
authors: agent
files:
---

# Evidence form / find_by_heuristic

## current_situation

Finder five of seven. Four are signed and two remain after this one.

The whole catalogue was held against both clusters this change touches. Eight rules times two clusters is sixteen rows, and every one is answered.

Six rules bit. Ten produced nothing, and each of those says `nothing` rather than being left out.

## applies

yes

## sweep

| heuristic | cluster | what it suggested |
| --- | --- | --- |
| group what changes together, separate what changes apart | the-walk | the asking and the judging change apart, which is the separation in time already minted from the contradiction finder |
| group what changes together, separate what changes apart | the-record-life | both kinds of work out of sight change together, so they belong on one surface; same as the uniform-object option from finder one |
| make the common case cheap, make the rare case possible | the-walk | the common case is a state with NO leaving check, and it must cost nothing extra; the deferred path may not tax states that never defer |
| make the common case cheap, make the rare case possible | the-record-life | the common case is nothing running, and its answer is an empty list computed from an empty table |
| one source of truth, everything else derives | the-walk | the verdict lives on the step and every reader derives from it, rather than each reader keeping its own idea of where the walk stands |
| one source of truth, everything else derives | the-record-life | the work's own progress record is the source and the report derives; no second counter is kept beside it |
| push decisions to the last responsible moment | the-walk | nothing; the walk already decides whether it may leave at the moment it leaves |
| push decisions to the last responsible moment | the-record-life | the duration is computed when asked, not when the work starts. MINTED, and it names a live defect: `batteryPace` decides at handoff and the sentence never changes |
| make the illegal unrepresentable, not merely checked | the-walk | a standing is one word from a closed set of three, never a boolean with a flag beside it. MINTED |
| make the illegal unrepresentable, not merely checked | the-record-life | an entry carrying a duration with no basis should be unrepresentable; sharpens the two-fields option from the contradiction finder |
| small interfaces between big parts beat the reverse | the-walk | nothing |
| small interfaces between big parts beat the reverse | the-record-life | nothing |
| if it must be remembered, it must be recorded | the-walk | the pending verdict is recorded rather than held in memory, the way the test job table already persists under `.se/test-jobs` |
| if it must be remembered, it must be recorded | the-record-life | nothing new; the progress record already does exactly this |
| the default should be the safe thing | the-walk | a reader that does not recognise the third standing must not silently see a passed; folded into the closed-word option |
| the default should be the safe thing | the-record-life | where no basis exists the default is "cannot estimate", never a number; already the honesty rule |

## options

- [[opt-the-duration-is-computed-when-asked-not-when-started]]
- [[opt-a-standing-is-one-closed-word]]

## follow_up

Two finders remain: transforming and probing.

One row is a live defect rather than a suggestion, and it should reach the design as one. `batteryPace` decides the wait figure at handoff and the sentence never changes afterwards, at `deliverable/engine/tools-run.ts` line 483.

One row is a constraint on the build. The common case is a state with no leaving check at all, and the deferred path must not make those slower.

## anything_else

TEN OF SIXTEEN ROWS SAY NOTHING, and writing them is the point.

A partial sweep and a thorough one look identical once the table exists. Four blank rows would have read as four rules that did not apply, and there is no way afterwards to tell that from four rules nobody held up.

THE RULE THAT PAID BEST WAS THE DULLEST. "Push decisions to the last responsible moment" found a live defect rather than a design idea: the wait figure is decided at the earliest possible moment today, and never revised.

THAT DEFECT WAS ALREADY VISIBLE AND NOBODY HAD NAMED IT. The as-is recorded that `batteryPace` reports the previous run's wall clock at start. What the heuristic added was the word for why that is wrong.

TWO ROWS DEDUPE WITH FINDER ONE AND TWO ROWS WITH FINDER THREE. That is four of the six bites arriving twice by different routes, which is what the fan is for. The chart dedupes and `found_by` records which lens turned each up.
