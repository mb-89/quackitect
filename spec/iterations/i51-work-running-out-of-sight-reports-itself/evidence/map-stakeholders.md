---
form: map-stakeholders
by: agent
signed_off: 2026-08-21T08:56:37.220Z
authors: agent
files:
---

# Evidence form / map-stakeholders

## current_situation

The boundary is drawn and the excluded list is binding.

The map is inherited and re-checked rather than re-derived. Four stakeholder nodes stand, and this change creates none.

THE VEHICLE-OWNER ROLE FOLDED INTO THE ENGINEER (owner ruling 2026-08-23). `stk-vehicle-owner` is outdated and its file is gone. The engineer IS the vehicle owner, so `stk-engineer-driving-agents` carries that role now. Nothing was dropped; one role absorbed another.

One role's concerns were extended. `stk-agent` is the role that pays for both defects, and neither was written down.

## roles

- [[stk-agent]]
- [[stk-engineer-driving-agents]]
- [[stk-reviewing-agent]]
- [[stk-newcomer]]

## coverage

EVERY VALUE PROP'S AUDIENCE RESOLVES. Ten propositions stand. They resolve to `stk-engineer-driving-agents`, which now carries the vehicle-owner role as well as its own. No proposition points at a role that is not there.

A ROLE NO PROPOSITION SERVES IS WORTH NOTICING, and there are three: `stk-agent`, `stk-reviewing-agent` and `stk-newcomer`. That is not a hole. The propositions are written for the people who choose to use this product, and these three are roles the product must SERVE without any of them buying it.

THE ALWAYS-ON CLASSES, kept or struck deliberately.

- acquirer: kept, as `stk-engineer-driving-agents`. Nobody funds or buys this, and adoption of a produced vehicle is decided by the same engineer who drives the machine.
- user: kept, as `stk-engineer-driving-agents`. The person who operates the thing.
- newcomer: kept, as `stk-newcomer`.
- communicator: struck. Nothing here is explained to a third party by a distinct role today, and the book that would need one is not built.
- assessor: kept, as `stk-reviewing-agent`.
- project-owner: kept, as `stk-engineer-driving-agents`, which holds both roles. One person holds several roles and the role outlives the person.
- agent: kept, as `stk-agent`.

THE HARDWARE AND SERVICE-LIFE CLASSES ARE ALL STRUCK. This is software with no installation, no field service and no supplier chain, so installer, integrator, operator, production engineer, regulator, service technician, supplier, transport and end-of-life have nothing to hold. Tester and developer-maintainer collapse into `stk-engineer-driving-agents` and `stk-agent`, which do that work here.

THE PROJECT AND ORGANISATION ROLES. Nobody funds it, one role approves it, and that role is `stk-engineer-driving-agents`. Who inherits it when this effort ends is genuinely open and is not modelled, said here rather than answered.

WHAT THIS CHANGE SHIFTS. `stk-agent` newly gains two concerns, because it is the role that pays for both defects and neither was written down.

- It cannot see the work it started, so polling is its only strategy.
- A step can hold its only verb, and it was once told its work had failed when the work had moved.

NO DISPOSITION IS RE-MARKED. `stk-agent` stands at neutral by design, on the reasoning in its own notes: the product may never rely on its goodwill. Fixing two of its concerns does not change that, and marking it a winner would be exactly the mistake that node warns against.

NO TENSION FOUND, so no risk minted. The three conflicts this iteration rules are between GOALS, not between roles. Every role named here gains or is unaffected.

## follow_up

The stories come next, and they are what makes the two new concerns concrete.

One thing is named rather than answered. Who inherits this product when the current effort ends is not modelled, and no role in the map holds it. That is not this iteration's to settle, and it is written here so a later record finds it rather than rediscovering it.

## anything_else

TWO CONCERNS WENT ONTO A ROLE THAT NOBODY IS BUILDING A PROPOSITION FOR, and that is the interesting shape here.

`stk-agent` buys nothing, chooses nothing and has neutral disposition by design. It is also the role that suffered both measured failures.

THE COVERAGE CHECK CANNOT SEE THAT. It asks whether every proposition's audience resolves, which is a check in one direction. A role carrying real, measured pain and no proposition passes it silently.

Stated as an observation about the method rather than a finding against this map. The map is correct as it stands.
