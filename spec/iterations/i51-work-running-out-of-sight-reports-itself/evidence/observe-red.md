---
form: observe-red
judgment: passed at 2026-08-21T12:56:04.235Z
by: agent
signed_off: 2026-08-21T10:58:00.079Z
authors: agent
files: null
---

# Evidence form / observe-red

## current_situation

Three test-specs stand for this record and all three carry `method: test`.

SO THE CHECKLIST IS EMPTY. It holds only the non-test specs — demonstration, inspection, analysis — where no run can show the red, and this record authored none of those.

THE SIX REDS ARE THE ENGINE'S TO OBSERVE. The submit runs `tests/handback.test.ts`, `tests/work-account.test.ts` and `tests/discipline.test.ts` and refuses unless a case fails.

EVERY CASE WAS WRITTEN TO FAIL FOR ITS OWN REASON, not to fail by accident. Two drive a real function over a temporary fixture, two ask a real class what it offers, and two read source for a call shape no runtime value exposes.

## red_observed

- none

## follow_up

BUILD-STEPS IS NEXT and it walks the nine chunks.

THE REDS ARE THE BUILD'S TARGET LIST. Each of the six names the sentence it is waiting for, and the chunk that turns it green is named in its spec's Steps.

THREE MORE CASES ARE OWED once the mechanism exists: a timing case for the handback, boundary cases for the account, and the middle scope partition where some parts map and some do not.

## anything_else

ONE CASE MAY GO GREEN WITHOUT A BUILD, and it is worth watching rather than assuming.

`the answer names every changed part that no test covers` asserts that an unanswered file is named back to the caller. Measured 2026-08-21, the engine already reports "131 changed file(s) have no test that answers for them" and lists them.

IF IT PASSES AT BIRTH, that is a genuine finding rather than a broken test: the reporting half of the requirement already stands and only the sweeping half is owed. The engine's own refusal will say so.
