---
form: partition-functions
by: agent
signed_off: 2026-08-21T09:21:04.465Z
authors: agent
files:
---

# Evidence form / partition-functions

## current_situation

The criteria pool is settled and the new row is placed in the standing order.

This state places every function in a cluster and names each cluster's coupling. The matrix is derived from the flow edges on the function notes, so nothing is drawn here.

Two functions are new. One was placed by its subject and moved by its flows, which is what this state is for.

## clusters

| element | cluster |
| --- | --- |
| fn-run-a-governed-walk.hand-back-a-step-still-deciding | the-walk |
| fn-run-a-governed-walk.account-for-work-out-of-sight | the-record-life |
| fn-run-a-governed-walk.answer-with-tests | the-record-life |

## follow_up

The candidate space opens next, enumerated from this partition.

One placement is worth re-reading when the design lands. `hand-back-a-step-still-deciding` publishes `flow-step-standing` to readers that are not functions in this tree — a gate, the route drawer, and whatever paints a step. If a later record ever models those readers as functions, this cluster's coupling class gets sharper than "tight".

## anything_else

TWO CLUSTERS ARE TOUCHED and both already stood. No cluster is created and none is renamed.

THE-WALK gains `hand-back-a-step-still-deciding`. COUPLING CLASS: tight, and tighter than the cluster's average. It consumes and produces `flow-instruction`, which is `serve-a-step`'s own flow, and it publishes `flow-step-standing`, which the route drawer and every gate read. Everything in this cluster answers whether the walk may move and what it is told, and this function answers exactly that.

THE-RECORD-LIFE keeps `account-for-work-out-of-sight`. COUPLING CLASS: loose to the walk, tight to the testing function beside it. It consumes `flow-work-under-way` and `flow-test-timings`, and `answer-with-tests` in this same cluster produces both. Nothing in it decides whether the walk may move.

THE MOVE IS THE ONLY REAL WORK THIS STATE DID. `hand-back-a-step-still-deciding` was first placed in the-record-life, because the subject sounded like a record concern — a leaving check on a state — and both new functions were written in one sitting with one cluster on both.

THE FLOWS DISAGREED, and the flows are the matrix. Nothing about the subject matter says `flow-instruction` belongs to the-walk; only the edge does.

THE TWO NEW FUNCTIONS SHARE NO FLOW AT ALL, which is why one iteration's work lands in two clusters. That was decided by the edges before anybody argued about the subject.

WHY NO NEW CLUSTER. A cluster is a group of functions, never an element, and two functions do not make a group with a name worth having. Both fit a standing group by their edges.

EVERY OTHER CLUSTER INHERITS. The change's cone reaches two functions and touches no other function's flows.

WHAT THIS PARTITION IS NOT. It is not a cut into elements. That cut is per candidate and comes later.
