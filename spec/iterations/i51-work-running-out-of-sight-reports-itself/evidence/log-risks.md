---
form: log-risks
by: agent
signed_off: 2026-08-21T08:46:20.116Z
authors: agent
files: null
---

# Evidence form / log-risks

## current_situation

The register opens for iteration fifty-one. One entry already stood from the kickoff gate, and the vision packet's three named conflicts are the entries this state owes.

The existing entries were re-read rather than re-derived. `raid-asm-battery-timings-measure-work` stands open and is cited by the first new entry rather than restated.

One correction landed on the way. `raid-asm-a-first-run-has-timings-to-estimate-from` carried `how_likely: likely`, which is not one of the scale's three words. It now reads `expected`, because the standing condition already holds: a fresh container has no recorded timings.

## raid_opened

- [[raid-risk-a-time-remaining-is-believed-more-than-it-deserves]]
- [[raid-risk-a-hop-that-finishes-later-makes-green-ambiguous]]
- [[raid-risk-a-narrower-test-scope-misses-a-break]]
- [[raid-asm-a-first-run-has-timings-to-estimate-from]]
- [[raid-asm-battery-timings-measure-work]]

## follow_up

The next states define the actual and frame the delta.

Two entries here are the design's to answer rather than the register's to watch.

- The pending-verdict answer that `raid-risk-a-hop-that-finishes-later-makes-green-ambiguous` demands has to be settled before anything is built, because every reader of green depends on it.
- The no-estimate answer that `raid-asm-a-first-run-has-timings-to-estimate-from` demands is the same shape, and its probe is written.

One stray was captured rather than chased. Two register entries elsewhere carry `certain` as a likelihood, which the scale does not have. The note is `note-7790a4d719ae`.

## anything_else

THREE NEW ENTRIES, ONE PER CONFLICT the goal system named and ruled.

- The estimate believed more than it deserves is conflict one, ruled for honesty over usefulness.
- The hop that finishes later is conflict two, ruled for the non-freezing exit. It is graded `crippling` because green is what every gate reads.
- The narrower test scope is conflict three, ruled for narrowing, with the full battery still firing at verification.

TWO ENTRIES ALREADY STOOD and are carried rather than restated. The first-run assumption was minted at the kickoff gate. The battery-timings assumption predates this iteration and is why the first new risk is graded `expected` rather than speculative.

A LINT WOULD HAVE CAUGHT ALL THREE BAD GRADES AT THE WRITE. Both grade fields have closed vocabularies, written down in `meth-damage-scale.md` and `meth-likelihood-scale.md`. A membership test is the whole check.

The corpus write guard already refuses frontmatter the engine's own reader cannot load. This is the same shape one level up: frontmatter that parses and still says something the method does not allow.
