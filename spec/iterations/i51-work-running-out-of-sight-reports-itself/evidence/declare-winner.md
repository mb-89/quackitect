---
form: declare-winner
by: agent
signed_off: 2026-08-21T09:59:35.554Z
authors: agent
files:
---

# Evidence form / declare-winner

## current_situation

The convergence computed a seat and the sensitivity hunt tested it. This state records the selection so everything downstream can point at one name.

Nothing here recomputes or overrides the arithmetic. The comments carry what the arithmetic cannot.

One finding from the state before this one changes what the winner will look like after the graft, and it is named in the comments rather than acted on here.

## winner

cand-the-account-that-follows-you

## comments

THE SEAT IS COMPUTED AND IT IS THIN. Two Pugh runs, converging at plus one and confirming at minus one, on three axes of equal weight.

EVERY RIVAL SITS WITHIN THREE SIGN SWINGS of this seat. That is inside the threshold the sensitivity state tables, so the margin is worth stating in the declaration rather than leaving in a form behind it.

WHAT THE WINNER IS. The predecessor's acknowledge-then-report shape on the handback row, the standard's state word on the standing row, a duration computed at the ask, and the account riding every lane answer so the caller never has to ask.

WHAT IT LOSES ON. `req-a-wrong-act-never-passes-silently`, the only fatal-graded axis in the set, where it scores 2 against the runner-up's 4. Its own seams admit why: the open state word lets a reader silently ignore a new value, and the rider's size bound is a discipline with nothing enforcing it.

THE DECLARATION CARRIES A KNOWN OVERTAKE, and this is the one thing a reader must not miss. `raid-tripwire-i51-the-leader-s-strengths-are-both-graftable` records it: both strengths that won this seat come from ONE cell, `opt-the-account-rides-every-answer`, and the runner-up can take that cell without giving anything up. With it the runner-up would score 4, 5, 3 against this winner's 2, 5, 3, which is domination on every axis.

SO THE SEAT IS DECLARED AND THE GRAFT IS OWED. The graft state is the sanctioned door for a loser's best cell to reach the winner, and here it runs in the other direction: the winner's decisive cell is the one that travels.

WHY THE CONVERGENCE IS NOT RE-RUN. Scoring with the answer visible is the poisoning the whole weights-before-candidates order exists to prevent. The finding arrived after the seat was computed, which is exactly when the sensitivity state is meant to find it, and the fallback it names is the graft rather than a re-score.

I SIGNED A RESULT I WOULD NOT HAVE PICKED, and said so at the convergence. The veto here is withholding the submit, and withholding it because I prefer the other candidate is a preference wearing a method's clothes. What I believe instead is that three axes in one band makes a fatal row weigh the same as two corrosive ones, and that is an argument about the method rather than about this decision. It goes to the retro.

FROM HERE ON "THE WINNER" NAMES `cand-the-account-that-follows-you`, and every later state means that one thing.

## follow_up

The graft runs next, then the ADRs record why.

THE GRAFT HAS ONE CELL IT MUST CONSIDER AND TWO IT SHOULD. `opt-the-figure-and-its-basis-are-two-fields` is the runner-up's strongest thing on the fatal axis, and the winner does not have it. `opt-a-standing-is-one-closed-word` is the other, and the winner's own seams say why it matters.

AND ONE FINDING GOES THE OTHER WAY. The guided caller's admitted hole — work finished and unnoticed because the next-ask time was too long — is closed by the winner's rider. A losing candidate's defect cured by the winner's cell is worth knowing when the two are combined.

The deciding ADRs are where the three reservations from the convergence live on. They are not repeated here.

## anything_else

