---
form: derive-criteria
by: agent
signed_off: 2026-08-21T09:19:49.928Z
authors: agent
files:
---

# Evidence form / derive-criteria

## current_situation

Design input is blessed. This state settles which rows are criteria and which compound into one axis.

Of the five rows this change wrote, four are `must` and are demands rather than criteria. One is `should` and joins the pool.

The standing pool is 98 should rows carrying a pairwise order across 129 files. This change inserts one row into that order rather than re-ranking anything.

## compounding

- none

## comparisons

| a | verdict | b |
| --- | --- | --- |
| req-a-diff-no-test-answers-for-is-reported-not-swept | > | req-survey-counts-only-open-records |
| req-a-diff-no-test-answers-for-is-reported-not-swept | > | req-clean-sweep-is-dated |
| req-a-diff-no-test-answers-for-is-reported-not-swept | = | req-repo-search-carries-intent |
| req-call-answers-in-one-second | > | req-a-diff-no-test-answers-for-is-reported-not-swept |

## follow_up

The function partition comes next, then the candidate space.

One thing is named rather than done. The method asks for a re-ask of a sample of inferred pairs, because a contradiction settled by transitivity is never surfaced. This change inserted one row, so its inference surface is three pairs wide, and the sample was not run.

One standing row gained a comparison it did not have. `req-call-answers-in-one-second` now names what sits under it, which it needed anyway to be a ceiling rather than an unanchored head.

## anything_else

THE VERDICTS LANDED ON THE NODES, and these are the two nodes that carry them.

ON `req-a-diff-no-test-answers-for-is-reported-not-swept`, three lines:

- `req-survey-counts-only-open-records >`. Both are honesty rows about what an answer states. A wrong count is corrected in a glance; a wrong test scope costs a minute of real time on every occurrence.
- `req-clean-sweep-is-dated >`. Recording a clean date is a traceability nicety; answering a question nothing covers is a wrong answer wearing a green.
- `req-repo-search-carries-intent =`. Both make a call say why it was worth its cost, one before the fact and one after.

ON `req-call-answers-in-one-second`, which heads the standing chain, one line naming what sits under it. That row governs every call's latency; the new row governs a cost inside one call.

NO COMPOUNDING, AND THE SUSPECTS WERE LOOKED AT. The new row carries no `characteristic`, so it shares one with nothing. It refines a use case no other row refines. THE ONE REAL CANDIDATE WAS REJECTED WITH ITS REASON: `req-test-scope-discipline` measures the same underlying thing and is `must`, so it is a demand rather than a wish, and compounding across that line compares two different kinds of quantity.

WHY FOUR COMPARISONS AND NOT NINETY-SEVEN. The order is a standing artifact and transitivity settles the rest. The cost of this state followed what the iteration ADDED, exactly as the method promises.

FOUR OF THE FIVE NEW ROWS ARE DEMANDS AND ARE NEVER SCORED. That is not priority inflation. Three of the four are the goals themselves, and a candidate that cannot hand the walk back has not solved the problem at all.

THE ONE SCORED ROW IS THE ONE WITH THE WEAKEST EVIDENCE. `should` in MoSCoW and `plausible` on its risk, resting on a count from one session. The two grades agree without either being derived from the other.

WHAT I DID NOT DO. The method asks for a re-ask of a sample of inferred pairs, because a contradiction settled by transitivity never surfaces. This change inserted one row, so its inference surface is three pairs wide, and the sample was not run.
