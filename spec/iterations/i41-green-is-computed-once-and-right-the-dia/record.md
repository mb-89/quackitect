---
id: i41-green-is-computed-once-and-right-the-dia
status: seeded
opened: 2026-08-20T16:48:56.504Z
goal: "Green is computed once and right: the diamond cache poisoning that drops trace nodes, the minted_in short-id law, per-machine green times, route-memo invalidation in the one write funnel, the join machinery grant, one repair-edge quantifier, and the bound record's real folder."
vision: |-
  Item list in spec/overhauls/2026-08-20/plan.md, seed 3 — eight subtle engine defects, each with a repro recipe.

  DONE LOOKS LIKE: a diamond refinement corpus keeps every node's root in the drawing; a short-stamped node counts as its record's own (63 nodes affected today); two sibling generated machines cannot swap signature times; every public write door clears the route memo, proven by a spy test; standing on a join, the packet and the gate agree about the machinery tools; a bound iteration's decision-graph check reads the iteration's own decisions.jsonl.
inputs:
  - spec/overhauls/2026-08-20/plan.md
depends_on:
  - i40-every-write-path-is-guarded-the-pool-s-b
---

# i41-green-is-computed-once-and-right-the-dia

## Goal

Green is computed once and right: the diamond cache poisoning that drops trace nodes, the minted_in short-id law, per-machine green times, route-memo invalidation in the one write funnel, the join machinery grant, one repair-edge quantifier, and the bound record's real folder.

## Rough vision

Item list in spec/overhauls/2026-08-20/plan.md, seed 3 — eight subtle engine defects, each with a repro recipe.

DONE LOOKS LIKE: a diamond refinement corpus keeps every node's root in the drawing; a short-stamped node counts as its record's own (63 nodes affected today); two sibling generated machines cannot swap signature times; every public write door clears the route memo, proven by a spy test; standing on a join, the packet and the gate agree about the machinery tools; a bound iteration's decision-graph check reads the iteration's own decisions.jsonl.

## Inputs

- spec/overhauls/2026-08-20/plan.md
