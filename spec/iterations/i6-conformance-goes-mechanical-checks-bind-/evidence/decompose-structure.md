---
form: decompose-structure
by: agent
signed_off: 2026-08-16T16:40:15.719Z
authors: agent
files: null
---

# Evidence form / decompose-structure

## current_situation

ONE NEW FUNCTION ALLOCATED, ONE NEW INTERFACE MINTED, and the element set is otherwise unchanged. No new element.

fn-run-a-governed-walk.guard-a-write lands on el-walk-engine, which already implements judge-a-claim — build the form a step owes and CHECK what comes back.

TWO CROSSINGS BECAME OWED and were answered differently.

- if-satellite-to-walk-engine is NEW. Nothing in the walk engine consumed a dispatched call before.
- if-walk-engine-to-account already existed and gained one flow. That is not a new interface.

THE MATRIX IS NOW CLEAN. Zero missing cells.

THE OWNER RULED ON THE SIZE, 2026-08-16: one interface does not make this a major. Write it and stay at minor.

## elements

- el-walk-engine
- el-core
- el-account
- el-bootstrap
- el-engine-delta
- el-entrypoint
- el-front-desk
- el-holding-pen
- el-method-compiler
- el-mirror
- el-record-store
- el-resolution-seam
- el-satellite-supervisor
- el-satellite
- el-test-runner

## allocation

THE MATRIX IS COMPUTED. This section carries only the argued spread — what the computation cannot decide.

### The one allocation this delta makes

fn-run-a-governed-walk.guard-a-write goes to el-walk-engine.

THREE ELEMENTS WERE CONSIDERED AND TWO WERE REJECTED, each for a stated reason rather than a preference.

- el-core routes every call to whatever owns its path. It ROUTES. Deciding whether content may land is not routing, and allocating here owed FOUR interfaces because el-core touched none of the guard's flows.
- el-account records every act and derives views. Its own rationale says in as many words that it decides nothing, and a refusal decides. It would have owed zero interfaces, which is exactly why it was tempting and exactly why taking it would have been scoring around the problem.
- el-walk-engine serves steps and judges claims. Checking what comes back is already its subject. It produces flow-dispatched-call and reaches el-account across a standing interface.

### Why the spread on flow-dispatched-call is information rather than noise

FOUR ELEMENTS NOW TOUCH IT. el-satellite and el-walk-engine produce it; el-account, el-resolution-seam and el-walk-engine consume it.

THAT IS THE SHAPE OF A CALL PATH, and each consumer asks a different question of the same thing. The account asks who did it. The resolution seam asks which tree it names. The guard asks whether it may land.

SPLITTING THE FLOW PER QUESTION WOULD HIDE THAT THEY ARE ONE CALL. Three views of one object beat three objects.

### The one interface minted, and why it is synchronous

if-satellite-to-walk-engine waits for its answer, where if-satellite-to-account appends and does not.

THE ANSWER DECIDES WHETHER THE WRITE HAPPENS AT ALL, so it cannot be fire-and-forget. That is the cost of conformance at the write, and it is the cost raid-asm-a-bound-check-runs-inside-the-write-budget measures — 4 to 12 ms against 1000 today, so the round trip has room.

### What the matrix caught that a listing would have carried through

THE FIRST ALLOCATION OWED FOUR INTERFACES. Two causes, both mine, both found by the computation rather than by reading.

- THE FUNCTION'S PORTS WERE OVER-SPECIFIED. It named flow-trace-graph as an input. The trace graph is a DERIVED view; a guard reading a bound rule reads the FILES, which flow-repository already covers. Dropped.
- THE ELEMENT WAS WRONG. el-core, corrected to el-walk-engine.

FOUR OWED BECAME ONE. The one that remained was real, and it got written.

## follow_up

M7 OPENS at author-tests. At minor, M4, M5 and M6 are struck.

WHAT IS OWED THERE, in order.

- ONE REQUIREMENT ROW, still unwritten. A node that PARSES and carries a value outside its declared vocabulary must refuse at the write. That is the defect that trapped this walk for eleven calls between M3 and gate-requirements, and req-a-write-that-breaks-the-corpus-refuses explicitly does not cover it.
- A CASE PER ROW. Eight rows today, nine once the row above is written, every one verify_method test.
- THE BUILD ORDER IS BINDING, from raid-risk-the-small-fixes-crowd-out-the-conformance-system. The write-budget probe first, the conformance chunks before the fixes, gate-implementation checking the order rather than the count.

WHAT SWEEP-CONSISTENCY CARRIES, and it grew here.

- Three files citing the dead i27 long id.
- vp-the-engine's Unlike section, still saying a claim refuses collision after i34 retired claims.
- el-record-store's statement, still saying records are read from git branches never from folders on disk. i34 reversed that exactly.
- req-product-is-a-folder carrying the heading Detail twice, both empty.

NOTHING IS BLOCKED.

## anything_else

### The escalation question, and why it did not escalate

THE STATE GUIDANCE SAYS wanting a new element or a new interface IS the architecture moving, and is the tell for a major.

I BROUGHT IT RATHER THAN DECIDING IT, because the column is the owner's. The ruling came back: one interface does not make a major.

THE RULING IS WORTH RECORDING AS MORE THAN A ONE-OFF. The guidance's line reads as a bright rule and behaves like a threshold with the threshold unstated. Four owed interfaces would have been a different conversation from one.

WHAT MADE THE DIFFERENCE HERE was that the interface was owed by a correction, not by a design change. The structure did not move; my first allocation was wrong and the matrix said so.

THAT DISTINCTION IS NOT IN THE GUIDANCE and it should be. Noted for the retro rather than fixed here — editing a rigor-matrix row from inside a walk that compiles from it is the kind of change contract rule 2 keeps out of a state that did not name it.

### The matrix is the good kind of check, third instance

IT READS BOTH SIDES FROM DISK — every element's implements list, every function's ports, every standing interface — and computes the crossings.

SO IT FOUND AN ERROR I HAD ALREADY SIGNED. fn-run-a-governed-walk.guard-a-write's ports were signed at derive-functions with flow-trace-graph among them. Nothing in that state's own checks could catch it, because a port list is just a list until something computes what it implies.

THAT IS THE ARGUMENT FOR req-a-coverage-check-computes-both-sides made a third time, by a third mechanism, and this time it caught something rather than merely being satisfiable without examination.
