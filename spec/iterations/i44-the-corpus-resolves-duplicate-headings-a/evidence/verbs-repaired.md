---
form: verbs-repaired
by: agent
signed_off: 2026-08-28T11:12:15.935Z
authors: agent
files: null
---

# Evidence form / verbs-repaired

## current_situation

Two nodes taught a git verb the engine no longer serves. Both are rewritten to what i34 actually left behind.

The class was measured rather than guessed. Seven nodes name a dead verb, and these two were the ones teaching it.

## built

`spec/trace/use-case/uc-land-work-on-trunk.md`, the two lane-door bullets. They named `se_git_sync` for keeping a record and trunk in step, and `se_git_land` for the landing itself. Neither exists. The bullets now say there is nothing to keep in step, because a record is a folder on trunk, and that the landing is a commit the machine makes behind the release gate.

`spec/trace/story/sty-land-the-work.md`, the evidence half of one slide. It named `se_git_land` behind M9's gate-release and cited mid-iteration syncs by commit. It now says the landing is a commit on trunk, and that since i34 there is no branch to merge.

## follow_up

Arm the dead-verb check over use cases and stories.

The five remaining mentions stay. Two raid entries record that the verb was retired, and one requirement names a missing verb as the example it is about.

## anything_else

THE REPAIR CHANGED WHAT THE NODES CLAIM, not just their vocabulary. Both described a two-tree world with syncs and a landing verb. i34 replaced it with one tree, and neither node had caught up.

SO THIS IS THE STALE-NARRATION CLASS as much as the dead-verb class. The verb was the tell; the wrong model of the system was the defect.
