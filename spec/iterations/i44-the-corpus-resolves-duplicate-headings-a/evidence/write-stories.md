---
form: write-stories
by: agent
signed_off: 2026-08-28T10:42:35.382Z
authors: agent
files: null
---

# Evidence form / write-stories

## current_situation

i44 repairs corpus documents and arms lints. It creates no new audience, no new outcome and no new value proposition.

The pass its enforcement half delivers is already told. `sty-the-write-refuses-the-break` follows an agent who writes a node breaking a corpus rule, hears it at the write with the line named, and fixes it in one move.

i44 widens what that refusal reaches. It does not change the pass, so writing a second story would rewrite a standing one.

## stories

- none

## follow_up

Derive the requirements that bind the repairs and the five lints.

The standing story is the validation container at M8 for the enforcement half, so the demonstration hangs off it rather than off anything minted here.

## anything_else

THE RULE THIS ANSWER FOLLOWS is the state's own: a new story that rewrites an old one is a sign the delta is bigger than declared. The delta is not bigger. It is the same pass over more of the corpus.

WHAT WAS CHECKED before answering none. The story folder holds 53 nodes. Two of them already cover this iteration's two halves: `sty-the-write-refuses-the-break` for the refusal at the write, and `sty-a-finding-outlives-the-box-that-found-it` for the finding that travels.

NO MUST STORY IS MINTED HERE, so this iteration demonstrates against a standing one rather than its own.
