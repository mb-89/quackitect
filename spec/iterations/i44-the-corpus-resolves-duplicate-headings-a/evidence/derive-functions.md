---
form: derive-functions
by: agent
signed_off: 2026-08-28T10:46:10.327Z
authors: agent
files:
---

# Evidence form / derive-functions

## current_situation

The five new requirements map onto two functions that already stand. No new function is minted, which is the answer a minor should give.

fn-run-a-governed-walk.catch-the-system-up gains four rows. It already carries the sweep's demands, and the four extend what the sweep looks at.

fn-run-a-governed-walk.offer-what-may-be-taken-up gains one. The unreferenced-token report is the pool being complete, which is that function's own statement.

## functions

- fn-run-a-governed-walk.catch-the-system-up
- fn-run-a-governed-walk.offer-what-may-be-taken-up

## flows

- flow-repository
- flow-sweep-result
- flow-standing-option

## neutrality

THE TEST IS WHETHER TWO HONESTLY DIFFERENT DESIGNS COULD BOTH DO THIS, and neither function changed its statement, so neither moved toward or away from a solution.

WHAT THE NEW ROWS SAY. Each names what is checked and what counts as passing. None names a checker, a file format, a regular expression or a pass ordering.

WHERE A SOLUTION COULD HAVE SLIPPED IN, and did not. The rows could have said "ripgrep shall scan" or "preflight shall exit non-zero". They say the engine shall report, which any of a lint, a sweep pass or a write-time guard could satisfy.

ONE ROW IS DELIBERATELY LESS NEUTRAL THAN ITS SIBLINGS. The reference row names the unreachable MARKER as an acceptable outcome, and a marker is close to a mechanism. It is kept because the marker is the ruling from draft-vision rather than an implementation choice: it says what counts as answered, not how the answer is stored.

NO ESCALATION. The state names the tell of a major as a new function fitting no existing cluster. Nothing new was minted, so the architecture did not move.

## follow_up

Sweep the five rows for what they lean on, and record each assumption in the register.

The row most likely to be leaning on something is the reference one, because the class list it checks is a control somebody has to keep current.

## anything_else

A FINDING WHILE READING THE POOL FUNCTION. fn-run-a-governed-walk.offer-what-may-be-taken-up still says "option" where the vocabulary is now "work token", in its statement, its input flow and its rationale.

That is the rename family the seed already routes into this iteration, and this node is a member the plan's own list does not name. It joins the work rather than becoming a separate finding.
