---
form: headings-repaired
by: agent
signed_off: 2026-08-28T11:11:23.682Z
authors: agent
files: null
---

# Evidence form / headings-repaired

## current_situation

The corpus reports zero repeated headings over 1,529 trace nodes, down from 26.

The class split three ways, and only two of the three were this iteration's to repair.

## built

TWENTY-FOUR ADJACENT DOUBLES, all of them `## Detail` in requirement nodes, dropped in one atomic patch of 24 operations. That is exactly the plan's count of eleven plus thirteen.

TWO SEPARATED REPEATS in corpus nodes, repaired by naming each section rather than deleting either.

- `raid-asm-the-target-machine-is-many-throttled-cores` carried three `## Probe` sections. The second is now `## Probe — the comparison that would settle it` and the third `## Probe — why it cannot run from here`.
- `tsp-the-arrival` carried two `## Steps`. The second is now `## Steps — the load-bearing ones`.

SEVEN LEFT, and none is a corpus node. Five are in one closed record's signed evidence, one more in another of its forms, and one in a prep document that carries no node frontmatter at all.

## follow_up

Arm the heading check, scoped to nodes that declare a type.

The seven remaining repeats stay as they are. They belong to the records that own them.

## anything_else

THE SPLIT IS A RULING, not a convenience, and the crippling risk forced it.

Armed over every file under spec, the check turned the sweep red on the seven. The requirement says every NODE carrying a repeated heading, and a form instance is not a node: it declares `form:` rather than `type:`.

SO THE CHECK NOW READS FRONTMATTER THAT DECLARES A TYPE. That is the requirement's own word, made mechanical.

EDITING ANOTHER RECORD'S SIGNED EVIDENCE WAS ALREADY A NAMED NON-GOAL at scope-non-goals. An amend belongs to the record that owns the form.
