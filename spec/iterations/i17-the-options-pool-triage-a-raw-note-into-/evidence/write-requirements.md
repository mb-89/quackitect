---
form: write-requirements
by: agent
signed_off: 2026-08-18T09:32:58.436Z
authors: agent
files: null
---

# Evidence form / write-requirements

## current_situation

Both new use cases are signed and eight rows are written for them: five over the mint, three over the reading.

THE FAN-OUT WAS COUNTED AND THE LOOK WAS TAKEN. uc-put-a-finding-where-it-outlives-the-machine draws five rows, which is exactly the smell threshold. Three facets were FOLDED into one row — a mint demands a statement, demands a re-entry condition, and accepts an explicit "cannot be stated yet" — because all three say the author must say something deliberate, and what is refused is silence rather than honesty.

ONE FACET WAS DELIBERATELY NOT FOLDED WITH THEM. The privacy line stands alone as req-a-minted-option-is-authored-never-the-note-s-own-text, because it is the only FATAL row in the set and folding it into a content check would bury the one demand the whole boundary rests on.

TWO EXTENSIONS ARE COVERED BY NO ROW, ON PURPOSE, and they are named under `complete` rather than left for a reviewer to find.

## register

- req-draining-to-the-pool-mints-an-option-on-trunk
- req-a-minted-option-is-authored-never-the-note-s-own-text
- req-a-minted-option-says-what-it-is-and-when-it-comes-back
- req-the-raw-note-stays-local-and-is-marked-drained
- req-the-crossing-is-the-same-act-for-a-person-and-an-agent
- req-open-work-is-answered-from-the-repository-not-a-local-store
- req-a-windowed-pool-answer-says-that-it-was-windowed
- req-the-pool-answers-a-person-and-an-agent-from-one-source

## set_criteria

- complete: every step of both use cases has a covering row, and TWO EXTENSIONS DO NOT — named here rather than hidden. uc-put-a-finding 4a, an option that restates one already standing, has no row because there is no fifth disposition to merge into; it is raid-risk-the-pool-accumulates-duplicates-with-no-way-to-merge and a non-goal. uc-see-the-whole-pool 4a, a re-entry condition that has already come true, has no row because nothing evaluates a condition; it is named in the mint's own Detail as what is NOT demanded, and Linear's snooze is the prior art for the version that would.
- consistent: no two rows conflict, and the one place they could was checked. The mint row says an option carries a re-entry condition; the windowed-answer row says an answer may omit options. Those are different objects — a condition is a field on a node, a window is a property of one answer — and no row demands that an answer be complete. Every term means one thing across the set: MINT is always the write that produces an option, DRAIN is always the disposition act, and POOL is always the standing options on trunk, never the inbox.
- affordable: buildable and verifiable together, and the argument is that the doors already exist. The drain is one function in engine/inbox.ts and already refuses a backlog disposition with no `where`. The corpus reader, the writer and the sweep are built. Seven of the eight rows verify by test against a temporary root, which the suite already does 142 files over. The eighth verifies by inspection, deliberately, and that is the cheapest method that would catch it.
- bounded: every row answers to the two new use cases and through them to one value prop. Nothing here specifies a surface, a merge, a wake mechanism or a routing — all four are non-goals with a register entry or a reference behind them. The one row that reaches furthest, the windowed answer, is graded `should` for exactly that reason rather than smuggled in as a must.
- comprehensible: a reader from any of the three disciplines involved can say what the system must do from the set alone. THE TEST WAS APPLIED RATHER THAN ASSERTED: an engineer reads five rows about a write and three about a read; a reviewer reads one FATAL row and knows where to look hardest; a person who knows only the privacy rule finds it stated as its own row with its own pass line. What the set does NOT say in one place is the shape of the option node's frontmatter, and that is M7's, not M3's.
- no_tbd: RUN, and zero. The sweep for TBD, TBC, TBR and ??? across every requirement, use case, story and value prop in the corpus returns 0 matches. Run rather than assumed, and run over the whole register rather than only over the eight new rows.
- behaviour_modelled: THE LOOK WAS TAKEN ON ALL EIGHT and ONE earned a model. req-draining-to-the-pool-mints-an-option-on-trunk carries a `## Behaviour` LIFECYCLE, because the participant test is the whole question that row exists for — its first line is `(nothing) -> pending`, and its last is `(nothing) -> minted: never`, which is what says the pool has exactly one door. That is precisely the gap the claim ledger's seven correct rows never showed. THE OTHER SEVEN WOULD HAVE BEEN NOISE and the reason is the same for all of them: each is one condition and one response, and a diagram of that restates the statement in a second notation that can then drift. The nearest miss was req-the-raw-note-stays-local-and-is-marked-drained, which looks like a state model — it is not, because after `drained` the note has no further transitions at all.

## follow_up

- M7 decides the option node's frontmatter shape; the set deliberately does not, and that is the one thing a reader cannot get from it
- the FATAL row is the one to build first and to test hardest: a note, a paste, and a refusal naming the overlapping text
- req-a-windowed-pool-answer-says-that-it-was-windowed becomes a must the day the migration runs; it is graded `should` now and the reason is written into the row rather than into this form
- the two uncovered extensions are each a standing register entry, so nothing is lost by their being uncovered — but a later reader who finds them uncovered should find this paragraph first

## anything_else

