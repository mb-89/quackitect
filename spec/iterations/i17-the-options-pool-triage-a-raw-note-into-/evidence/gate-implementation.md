---
form: gate-implementation
bless: blessed by agent
by: agent
signed_off: 2026-08-18T11:10:57.639Z
authors: agent
files: null
---

# Evidence form / gate-implementation

## current_situation

Verification is signed after one full repair round. The battery stands at 1475 tests in 143 suites with 0 failures, biome is clean over 289 files, and the sweep walks 1366 nodes in 359 ms.

WHAT THIS GATE IS JUDGING is a delivery that was re-cut twice by things the walk found rather than by anything planned: a tester's eleven findings, and the owner's ruling that the pool's name was already taken.

THE FIVE MECHANICAL CHECKS ARE UPSTREAM and this form does not re-ask them. What is left is judgment, and the honest summary is that the mechanism is sound and its ONE fatal demand rests on a check with a named, tested limit.

## quality_ok

- [x] Dependencies stay layered
- [x] Every new element carries one stated responsibility
- [x] The linter and the complexity ceiling are clean, with no new suppression
- [x] Every new behavior carries its check, and the battery is green at rest
- [x] Nothing speculative shipped
- [x] What changed is findable
- [x] Every quick-and-dirty taken stands as a visible raid debt entry

## debt_taken

- none

NO DEBT WAS TAKEN, and the word is used strictly: a debt is a conscious trade of quality for speed, and this iteration made none. Two things could be mistaken for one and neither is.

THE PASTE CHECK'S LIMIT IS NOT DEBT. It cannot catch a bare name, and lowering the opaque-token threshold far enough to catch one would flag every long word in the language. That is a boundary of what a text check can do, not a shortcut taken to save time — and it is pinned by a case that demonstrates the limit rather than hiding it.

THE MIGRATION BEING OUT OF SCOPE IS NOT DEBT EITHER. It cannot be run from this machine at all: the 205 parked options are machine-local on the owner's laptop and this clone holds three notes. Work that is impossible here is not a shortcut.

## risks_acceptable

acceptable — the build made the privacy risk's case STRONGER rather than weaker and two of its four holes are closed; the two that remain — word insertion and a bare name — are closed by nothing a text check can do, and the threat model is a lazy author rather than a hostile one. The duplicates risk fell in cost, because a second drain of one note now refuses and points at carried. The two assumptions behind the fatal row stay unprobed and are owed to the migration, honestly and by construction.

## round_0_verify

- evidence vs claims: CHECKED, and the check moved a claim. The design spec said prose-inspect sweeps a minted token for leaked names "and it holds, probed 2026-08-18". The tester read the four needles it actually uses — all four are THIS machine's own identity — and minted three tokens carrying somebody else's email, path and password with no finding. The spec is corrected rather than argued with
- types: RUN AND GREEN. tsc clean over the tree
- lint: RUN AND GREEN. biome over 289 files, no fixes, no warnings. It was red once in this pass on a constant left dead by a refactor, and the battery caught it rather than a reader
- tests: RUN AND GREEN — 1475 tests, 143 suites, 0 failures, through the lane's own verb at the state that grants it. 25 of those cases are this iteration's
- the sweep: 1366 nodes in 359 ms, green
- flow closure: RED then GREEN, and the two breaks were not this record's. Both predate it at origin/v3

## round_1_validate

- exercised against the goal: the mint writes a work token onto trunk, the raw note stays local and marked drained, and the survey reads the pool from the repository. All three of the goal's own clauses are built and tested
- missing: THE MIGRATION, which is the mechanism's first USE and cannot run from this machine. And the pool has no surface, which is i23's
- wrong: TWO THINGS WERE WRONG AND BOTH WERE CAUGHT BY SOMETHING OTHER THAN ME. The pool's folder and node type collided with a declared kind holding 95 nodes — found by listing a directory, not by the suite, because the fixture never copies the corpus. And se_file_write was a second door into the pool — found by the tester, because only reading proves a path does not exist
- out of scope: the merge, the machine-evaluable wake, the owned queue, the routing, the criteria machinery. All named at the kickoff and none arrived through the back door
- prior art: MADE AT M1 and cited rather than repeated. Its influence is traceable: the duplicates risk and the fourth goal conflict came from it, and req-a-minted-option-says-what-it-is-and-when-it-comes-back names Linear's self-waking snooze as what it deliberately does NOT demand

## goals_served

- the pool travels: draining a note to backlog mints a committed, rewritten item on trunk: BUILT AND GREEN. engine/pool.ts mints a work token under project/spec/trace/work-token/, and standingTokens reads them from the repository. Nine cases
- the rewrite is the privacy boundary: a raw note never enters version control, and what cannot be stated cleanly is refused rather than guessed: BUILT, ATTACKED, AND STRENGTHENED. SE-C-140 refuses a run of six shared words AND any shared identifier, and the tokeniser now flattens punctuation the way the design always claimed. The `cannot be stated cleanly yet` escape is a legal statement and has its own case. THE LIMIT IS PINNED RATHER THAN HIDDEN: a bare name is not caught, and a case says so out loud
- an unattended walk can file into the pool, so a finding survives the box being released: BUILT. The crossing is one act with one set of demands whoever makes it, and guardNoSecondDoor makes that mechanical rather than asserted
- engine improvements: SERVED WELL BEYOND THIS RECORD. Three boot defects at M0, the repair loop the owner named, two flows i35 minted without their crossing, three engine files no design spec claimed, and the corpus's formatter drift caught at the moment it crossed its own threshold

## bound_breaches

- if-agent-harness-to-entrypoint: the pulls carrying documents and the form submits, as at every gate in this record. Nothing new, and no new cause
- if-test-runner-to-toolchain: the battery at 68s wall. That boundary declares a bound deliberately not one second, so it is not a breach — recorded because a reader counting long calls would find it and wonder
- the count is a floor rather than a rate, for the same reason as at every gate here: this clone's call log starts inside this session

## round_2_red_team

- the fatal row rests on a check the tester walked through four times => TWO OF THE FOUR ARE CLOSED and the other two are closed by nothing, ever: inserting or deleting a word defeats any contiguous-run check, and a bare name is a word. The honest answer is that the threat model is a lazy author, and a system whose privacy rests on defeating a determined one needs a different mechanism entirely
- the collision was found by luck, not method => TRUE, AND IT IS THE MOST UNCOMFORTABLE THING IN THIS GATE. Seventeen green cases sat over it because freshRoot never copies the corpus. The fixture that makes the suite fast is the fixture that cannot see a corpus-wide defect, and nothing in the suite closes that today
- the walk escaped the machine to fix its own findings => it did, and the hole that forced it is fixed and pinned on the shipped matrix. But the escape happened, and a reader of this record should see that the loop the owner named was real
- the pool is minted and still not read by anything a person touches => the survey reads it, which is the lane's answer. No surface does, and that is i23's — so the fourth success criterion is served for an agent and owed for a person
- this gate could pass on a fixture nobody will ever use => the kill criterion from gate-motivation is still the right one, and it is now checkable: M8 looks at what the desk consults for open work. Finding .se there is the criterion firing

## raid_additions

- raid-iss-a-verification-finding-that-is-not-a-test-failure-has-no-route

## verdict

pass with overrides — the mechanism is built, attacked by fresh eyes, and green, and the two things this gate cannot close are named rather than carried quietly.

TWO OVERRIDES.

1. THE PRIVACY CHECK HAS LIMITS NO TEXT CHECK CAN CLOSE. A bare name and a word-insertion obfuscation both walk through, and both are pinned by cases that demonstrate the limit. Carried by raid-asm-a-verbatim-overlap-check-catches-the-paste-that-matters, which is unprobed and stays that way until the pool has content.

2. THE SUITE CANNOT SEE A CORPUS-WIDE COLLISION. freshRoot gives every case an empty temp root, which is what makes the battery parallel, and it is why seventeen green cases sat over a head-on collision with a declared node kind. Nothing closes this today, and the cheap guard — refusing a new kind whose folder or prefix another kind already declares — is named in the field report rather than built here.

WHY PASS. Everything mechanical is green and checked by the engine rather than by me. Both overrides are properties of what a check can do, not work left undone, and each is pinned by a case that fails the day somebody quietly changes its terms.

## follow_up

- M8 checks the kill criterion by looking at what the desk consults for open work; finding .se there is the criterion firing
- the migration of the 205 runs where the notes are, and its first act is a report of what did not fit rather than a silent conversion
- the paste probe is owed EARLY rather than at fifty mints, because the row it defends is the only fatal one in the delta
- the guard that would have caught the name collision — refusing a node kind whose folder or id_prefix another kind already declares — is named and not built; it belongs to whichever record takes the machine next
- the criteria machinery, the whole other half of this record's seed, is still owed its own iteration

## anything_else

