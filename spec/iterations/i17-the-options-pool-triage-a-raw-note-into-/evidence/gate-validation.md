---
form: gate-validation
bless: blessed by agent
by: agent
signed_off: 2026-08-18T11:31:38.540Z
authors: agent
files: null
---

# Evidence form / gate-validation

## current_situation

The delta is built, verified by fresh eyes, blessed at implementation, and
swept. The battery stands at 1475 tests in 143 suites with 0 failures, biome
is clean over 289 files, and the sweep walks 1368 nodes.

WHAT THIS GATE JUDGES is whether the pool serves the need, not whether it was
built well — that was the last gate. The delta mints one new value prop and
two new must stories, and those three are the ones this gate owes a real
answer.

THE SWEEP ITSELF RAISED ONE FAILURE, and it belongs in this gate's record.
sweep-consistency edits project/guidance/ by design, project/guidance/ is the
SOURCE of the prompt layer, and preflight fails on a stale projection. The
battery went red here on exactly that, and this state was granted no verb to
clear it. Fixed in the matrix for the next iteration; re-projected through
se_file_write for this one.

## meets_need

- vp-the-ledger: MET, and this delta is the whole of it. The prop was minted by this iteration and nothing served it before. A drain to backlog now writes a work token onto trunk, where a clone that has never touched this box reads it. Argued from sty-a-finding-outlives-the-box-that-found-it and sty-see-what-the-other-machine-may-pull-from, both filled, and from the nine cases in pool-mint and the seven in pool-offer
- vp-qualities: SERVED SIDEWAYS, and honestly. The prop is about a box arriving, not about a pool. What this delta did for it is that a finding made on such a box now survives the box: three boot defects found at M0 on this machine are fixed in the tree rather than lost with the container. That is a real service and it is not the prop's own criterion, so it is reported as service and not as a criterion met
- vp-the-engine: MET, and exercised rather than asserted. The cage refused se_prompt_place at this gate with the state's exact tool list, se_file_replace at sweep-consistency, an unknown argument on se_file_search, a wrong field name on se_file_patch, and a truncating shape on se_test. Every one of the five named the legal alternative
- vp-the-ledger: MET. Twelve raid entries opened across the walk, one issue opened for the verification hole the escape was taken for, and two gate overrides logged WITH their dissent rather than as a clean pass
- vp-systematic-engineering: MET. The delta traced value prop to stories to use cases to eight requirements to two functions to an element to a design spec to three test specs, and the coverage laws refused until it actually did — twice on the element matrix, once on an unseeded chunk drawing
- vp-rigor-without-toil: MIXED, and the toil is measurable. The method carried this walk with no person present through M0 to M8. Against that: 65 of 330 lane calls were refusals, and 25 of those 65 were the narration stall — four one-line briefs that the comma-chaining rule split into twelve phantom checklist items, each of which then had to be resolved before any call would pass. That is toil with no rigor behind it
- vp-autonomy-range: NOT TOUCHED by this delta, and it moved anyway — for a reason worth recording. The dial had to go from tactical to strategic because blessing refuses on autonomy <= priority, strictly above. Entering a gate at tactical and being unable to sign it is the range behaving as documented, and the owner ruled the raise rather than the rule
- vp-qualities: not touched by this delta
- vp-vendoring: not touched by this delta

## musts_demonstrated

- sty-a-finding-outlives-the-box-that-found-it: THIS RUN IS THE DEMONSTRATION, and it is one of the two must stories this delta authored. Three boot defects were found on this cloud box before the record was touched, and all three are fixed in the tree. The mechanism the story asks for is built and green in pool-mint. What is NOT demonstrated is the story's own last slide — a finding read back from a different machine — because no note has been drained to backlog in this clone
- sty-see-what-the-other-machine-may-pull-from: BUILT AND TESTED, NOT DEMONSTRATED, and the gap is structural rather than an omission. pool-offer's fixture is exactly the story's condition: a clone holding the repository with an EMPTY local note store. Seven cases pass over it. What no test can stand in for is a second real machine, and this box is one machine
- sty-hand-over-and-walk-away: EXERCISED, for the whole walk. The owner set the dial and left, and the machine carried M0 to M8 without a person. It stopped twice: once at the kickoff bless above the dial, which is the story working, and once by escape at verification, which is the story failing and is filed as raid-iss-a-verification-finding-that-is-not-a-test-failure-has-no-route
- sty-review-a-gate: EXERCISED FOUR TIMES — kickoff, motivation, requirements and implementation. Each refused until its evidence actually stood, and gate-implementation passed with two written overrides rather than a clean pass
- sty-the-control-that-says-why-it-declined: EXERCISED SIXTY-FIVE TIMES and it held every time. Every refusal was typed, named what it expected against what it got, and carried a remedy that was executable as written. The one that mattered most was SE-C-112 naming all nine unchecked boxes at once rather than the first
- sty-the-write-refuses-the-break: EXERCISED AND IT CAUGHT ME TWICE, both on text this sweep wrote. A wall of eight unbroken lines in the retro card, and an item grown to three sentences in the fix for it. Neither would have been caught by a reader
- sty-the-agent-proves-it-read: EXERCISED AT EVERY BOOT, and it caught a defect rather than the agent. The probe refused a verbatim answer because the reading normaliser split on em dashes; the fix is in readproof.ts with the live probe as its fixture
- sty-carry-a-finding-without-stopping: EXERCISED. Strays became notes and the walk continued — the boot-deadlock finding, the verification-route hole, and the checklist-line-match finding all landed as records while the state in hand kept moving
- sty-the-slow-call-that-says-it-is-working: EXERCISED. se_test hands off a job and answers each poll with cases_done against cases_last_run and files_touched against files_total, so a 69-second battery is legible while it runs
- sty-the-call-that-comes-back-inside-a-second: EXERCISED AND MEASURED. Median lane call 2 ms across 330 calls; total lane time 0.86 minutes against 78 minutes of wall clock. The lane is not what makes a walk slow
- sty-ask-the-lane-what-it-can-do: EXERCISED NEGATIVELY, which is the honest report. se_help was not reached for; what happened instead is that five refusals each printed this state's exact legal-tool list. The lane answered the question without being asked it, which serves the story and is not the story's own route
- sty-send-an-agent-to-a-cloud-box: EXERCISED — this walk ran on one, unattended, from M0 to M8
- sty-work-on-two-machines: NOT DEMONSTRATED, and this delta is the machinery FOR it rather than a demonstration OF it. The pool is what one machine leaves for another to read
- sty-a-check-binds-without-engine-code: not exercised by this delta. SE-C-140 is engine code, deliberately, because the paste refusal has to hold whoever writes it
- sty-answer-what-does-this-touch: not exercised by this delta
- sty-dispose-a-candidate-coupling: not exercised by this delta
- sty-ramp-up: not exercised by this delta
- sty-start-a-new-product: not exercised by this delta
- sty-walk-it-by-hand: not exercised by this delta

## market_tier


## round_0_verify

- evidence vs claims: CHECKED AND IT MOVED ONE. The claim that this gate would judge a green battery was false when first run — preflight was red on a stale prompt layer that the sweep itself made stale. Reported rather than re-run quietly
- types: RUN AND GREEN across the tree
- lint: RUN AND GREEN. biome over 289 files, no fixes applied, no warnings
- tests: RUN AND GREEN — 1475 tests, 143 suites, 0 failures, through the lane's own verb at a state that grants it
- the sweep: 1368 nodes green, 357 ms. Two nodes more than at gate-implementation, which are this state's own evidence and the sweep's
- preflight: RED THEN GREEN. Three projections stale, caused by this milestone's own guidance edits, re-placed and re-run

## round_1_validate

- exercised against the goal: YES, and by the sweep rather than by the build. The goal's three clauses were green at the last gate. What M8 added is that every surface teaching the OLD behaviour now teaches the new one, which is the difference between a mechanism that works and a mechanism a reader can find
- missing: THE MIGRATION, still, and it cannot run here — the 205 parked notes are machine-local on the owner's laptop. And the pool has no surface a person touches, which is i23's
- wrong: ONE THING, AND THE SWEEP FOUND IT. The engine taught two false sentences about its own drain — that backlog "PARKS" a note, and that "re-draining IS the migration mechanism". Both were true before this iteration and neither had been corrected by the build. A reader following either would have been wrong about what the code does
- out of scope: the merge, the machine-evaluable wake, the owned queue, the routing, the criteria machinery. All named at kickoff, none arrived late
- prior art: MADE AT M1, cited rather than repeated, and it earned its place at this gate too. ref-triage-and-option-pools-2026 is where the name "options pool" comes from, and it is why the collision with our own `option` kind was a naming problem rather than a modelling one

## goals_served

- the pool travels: draining a note to backlog mints a committed, rewritten item on trunk: SERVED AND NOW TAUGHT. engine/pool.ts mints under project/spec/trace/work-token/; nine cases. M8 added the part a reader needs: walking.md, the retro card and the tool description all now say what the drain does
- the rewrite is the privacy boundary: a raw note never enters version control, and what cannot be stated cleanly is refused rather than guessed: SERVED, WITH ITS LIMIT ON THE RECORD. SE-C-140 refuses a six-word shared run and any shared identifier. A bare name is not caught, a case says so out loud, and refusals.md now carries the clause so the limit is findable rather than folklore
- an unattended walk can file into the pool, so a finding survives the box being released: SERVED. guardNoSecondDoor makes the one-door rule mechanical, and this walk is the case in point — every defect it found is in the tree rather than in a container
- engine improvements: SERVED WELL BEYOND THIS RECORD, and M8 added two more. The matrix now grants se_prompt_place to the two states that can make the prompt layer stale, which closes a hole of the same shape as the repair loop the owner named. Before that: three boot defects, the repair loop itself, two flows i35 minted without their crossing, three engine files no design spec claimed

## bound_breaches

- if-agent-harness-to-entrypoint: the pulls carrying documents and the form submits, as at every gate in this record. Nothing new and no new cause. The count remains a floor rather than a rate, because this clone's call log starts inside this session
- if-test-runner-to-toolchain: the battery at 69s wall, twice in this state. That boundary declares a bound deliberately not one second, so it is not a breach. Recorded because a reader counting long calls would find two of them here and wonder

## round_2_red_team

- STEELMAN: a gate whose own milestone broke the build should not sign it => The strongest version says preflight went red at this state, caused by this milestone's edits, and a gate that then passes is a gate grading its own homework. WHAT DEFEATS IT: the red was the guard working exactly as designed. The prompt layer is a projection and preflight exists to catch a source edit that has not been re-projected. The failure mode it protects against is an agent reading a stale contract, and it fired on the first battery after the edit. What is legitimately damning is not the red — it is that this state had no verb to clear it, and that is fixed on the matrix rather than argued away
- KILL-CRITERION from gate-motivation: if the pool is never READ, the whole extension is wrong => STILL THE RIGHT CRITERION and it is now checkable from the desk. survey reads standingTokens from the repository, and pool-offer's fixture is a clone with an empty local note store, which is the only state where reading the wrong source is visible. The criterion has not FIRED, and the honest note is that it cannot fire until something is minted here
- THE SWEEP CHECKED NINE BOXES AND ONE CLASS WAS EMPTY => Conceded. "Book chapters" has no documents because the book is i20's work, so the tick is vacuous. It is a tick on an empty set rather than a claim about documents, and saying so beats a silent green
- THE VOCABULARY FIX COULD BE COSMETIC => Tested against the strongest version, which says renaming "option" to "work token" in prose changes nothing a machine reads. It fails on the facts: the collision was real, project/spec/trace/option/ holds 95 morphological-chart nodes, and survey reported counts.backlog: 95 before the rename. A reader who trusted the word would have been reading the wrong folder
- THE DISPOSITION IS STILL SPELLED backlog EVERYWHERE => True, and deliberately. Renaming an API argument is a refactor no state in this milestone named, and contract rule 2 forbids the unasked one. The cost is a permanent seam between what the argument is called and what it does, and a reader meets it at the first drain
- THIS GATE PASSES ON A MECHANISM WITH ZERO PRODUCTION USES => Conceded plainly and it is the largest thing this gate cannot close. Every case runs on a temp root. The first real mint happens at a retro on the owner's machine, and the first thing it will produce is a report of what did not fit

## raid_additions

- raid-iss-a-verification-finding-that-is-not-a-test-failure-has-no-route

## verdict

pass with overrides — the delta serves the need it was cut for, and the one thing this gate cannot close is named rather than carried quietly.

THE OVERRIDE: NEITHER NEW MUST STORY IS DEMONSTRATED ACROSS TWO MACHINES, which is the only condition either was written for. Both are built, both are green, and pool-offer's fixture reproduces the condition as closely as one box can — a clone holding the repository with an empty local note store. What no fixture reproduces is a second machine. Carried by the migration, which runs where the 205 notes are.

WHY PASS RATHER THAN HOLD. Holding here would demand a demonstration this box cannot produce, on a mechanism whose every mechanical check is green and whose limits are each pinned by a case that fails the day somebody quietly changes its terms. The dissent is recorded: a reader who wants proof that a finding travelled between machines will not find it in this record.

## follow_up

- The matrix now grants se_prompt_place to sweep-consistency and gate-validation. The RUNNING instance keeps the column it was compiled with at kickoff, so this iteration did not get the fix it wrote. The next one does
- The first mint in a real clone is owed a report of what did not fit, not a silent conversion
- The guard that would have caught the name collision — refusing a node kind whose folder or id_prefix another kind already declares — is still named and not built
- The narration stall is the largest measured friction in this walk: 25 of 65 refusals, all from four one-line briefs split into twelve phantom items. The comma-chaining rule turns a brief into a checklist, and a checklist that was never planned cannot be resolved honestly
- The criteria machinery, the other half of this record's seed, is still owed its own iteration

## anything_else

WHY THE PROMPT-LAYER RED IS WORTH MORE THAN ITS FIX. Two states in this
milestone can edit project/guidance/. sweep-consistency does it as its whole
job. Neither could re-project, and the state that runs the battery is the one
that sees the failure. That is the same shape as the verification and
fix-findings hole the owner named: a state that can DETECT a condition and is
granted no verb to CLEAR it. Both are now closed, and the shape is worth
looking for elsewhere.
