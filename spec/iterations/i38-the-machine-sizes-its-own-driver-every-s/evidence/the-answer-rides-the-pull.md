---
form: the-answer-rides-the-pull
by: agent
signed_off: 2026-08-20T20:42:58.303Z
authors: agent
files: null
---

# Evidence form / the-answer-rides-the-pull

## current_situation

The block answers and nothing asked it. This is the outbound half: the statement rides the pull beside the state and the tier, and the lane starts nothing on account of it.

THE PULL IS THE ONE CALL THE AGENT MAKES, so the statement reaches a reader without a second channel and without a protocol.

## built

`needs` RIDES THE PULL'S HEAD, carrying the rung and the pair it came from. `Session.strengthNeeded()` sizes the step the walk stands on and adds the field.

A STEP WITH NO RATING PUBLISHES NOTHING. The block refuses rather than guessing, the pull swallows that refusal, and the field is absent. The walk carries on exactly as it did before this existed — no refusal reaches the agent, and no fallback is invented.

A DESCENDING STEP PUBLISHES NOTHING EITHER, for the reason the promotion gives: it routes into a sub-machine and the work is in the states below.

THE LANE STARTS NOTHING. `engine/sizing.ts` carries no import of `node:child_process` and no spawn or exec call, and a case asserts it — on CALLS and IMPORTS rather than on words, because the first version searched for the substring "spawn" and flagged the file's own sentence promising it never happens.

### What this file's checks can hold, and what they cannot

ONLY AN ITERATION'S STATES CARRY A RATING, and that is the design rather than a gap. A difficulty is declared on a matrix CELL, and the boot machine's states are not matrix rows. So a `needs` on a LIVE pull needs a bound iteration walked to a rated step.

THAT IS A DEMONSTRATION AND NOT A UNIT CHECK, and `run-demos` is where it belongs. The file says so in its own comment rather than leaving a reader to assume the end-to-end path was asserted.

WHAT IS ASSERTED IS EVERY HALF THE PULL DEPENDS ON: a rated step produces the envelope, an unrated one produces nothing, and a live pull standing where nothing is rated is unchanged from before this existed — no field, and no refusal either.

## follow_up

`run-demos` OWES THE END-TO-END WALK. Seed an iteration, rate one step, walk to it, and read `needs` off the pull. The unit checks cover both halves of the join and not the join itself.

NOTHING IN THE PRODUCT'S OWN MATRIX IS RATED, so the field is absent on every real pull today. That is the seam `the-cell-declares-a-difficulty` recorded: the mechanism ships able to publish, and the ratings are the matrix owner's to write.

THE SIZING CHAIN IS COMPLETE. Ten chunks, eighty-two cases green across the eight files this build touched.

## anything_else

