---
form: trace-design
by: agent
signed_off: 2026-08-20T23:53:36.694Z
reopened: "2026-08-20T23:49:47.754Z — the walk standing inside a sub is standing in the state that RUNS it, and the submit guard did not know that: the form was served at run-demos/end and its own submit refused."
authors: agent
files: null
---

# Evidence form / trace-design

## current_situation

The build is done: ten chunks, and 82 cases green across the eight files it touched. This state checks the seam between what the design SAID and what the code IS — every element realized, every spec's files existing, every deliverable file claimed.

BOTH SPECS CLAIMED A FILE NOTHING LANDED IN, and this is the state that catches it. A planned name is legal while a spec is being written; what it may not do is survive the record unrealised.

## design_trace

| design-spec | realizes | files |
| --- | --- | --- |
| [[dsp-the-sizing-block]] | el-sizing · if-engine-delta-to-sizing · if-method-compiler-to-sizing | project/deliverable/engine/sizing.ts · project/deliverable/engine/rigor-matrix.ts · project/deliverable/engine/machine.ts · project/deliverable/engine/session.ts |
| [[dsp-the-three-coordinates-on-a-call]] | el-account | project/deliverable/engine/calllog.ts · project/deliverable/engine/tools.ts · project/deliverable/engine/tools-run.ts · project/deliverable/engine/mirror.ts · project/deliverable/engine/session.ts |

### THE SUBMIT GUARD TOOK THE SAME ASCENT

Serving the parent's form at the sub's end was half the repair: the stamp then
refused, because "the walk stands in this state" also read the leaf alone. A
walk inside a sub IS standing in the state that runs it.

WHERE IT LIVES: `dsp-walk-machine.md` § Machinery ascends to the state that
runs the sub, closing paragraph.

## follow_up

A REGISTER ENTRY IS OWED FOR THE UNRATED MATRIX and this is the first state since the build that can write one. `req-every-matrix-row-declares-its-complexity` asks for a refusal WHEN THE MATRIX IS LOADED, and the load-time refusal is off until the matrix folder's README carries the line saying every active cell is rated. Until then the demand is met at the point of use instead, which is narrower than what it asks for.

`specify-build` IS OWED A RE-SIGN. Two design specs changed under it during the build: the sizing spec's cell shape was corrected from a nested map to a scalar, and both file lists were corrected here. The claim that state signed — that the design below the line is specified — still stands; what changed is what it specifies.

THE END-TO-END PULL IS OWED A DEMONSTRATION AT `run-demos`. Only an iteration's states carry a rating, so seeing `needs` on a live pull needs a bound iteration walked to a rated step. The unit checks hold both halves of that join and not the join itself, and the test file says so on its own face.

BOTH INSPECTION SPECS ARE OWED A REAL RUN AT `verification`. They were accepted as unfalsifiable at `observe-red` because the path did not exist. It exists now, and a spec accepted as unfalsifiable before the build and never re-run after it is coverage that never happened.

## anything_else

