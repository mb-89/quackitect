---
form: gate-validation
bless: blessed by agent
reopened: "2026-08-20T23:57:13.034Z — the machinery-ascends repair landed below this: a reopened claim on a state that runs a sub-machine is reachable from inside that sub, and its submit is legal there. dsp-walk-machine.md carries the design and tests/reopen-past-a-sub.test.ts guards it. No shipped behaviour this state answers for changed."
by: agent
signed_off: 2026-08-20T23:57:15.806Z
authors: agent
files: null
---

# Evidence form / gate-validation

## current_situation

M8 is walked: the story evidence is filled from the shipped system, the one must story is demonstrated with its report, and the consistency sweep found four documents teaching the superseded way.

THIS BLESS IS THE ACCEPTANCE ACT. There is no second sign-off artifact.

THE STATE OF THE THING BEING ACCEPTED, in one paragraph. A matrix cell can declare how hard a row is, the compile carries it onto the step, and a step's pull names the hand its work needs — a rung and the pair it was derived from, never a model. Every call record carries which model answered, which state the walk stood in, and which hand made the call. Nothing in the shipped matrix is rated, so the sizing half publishes nothing until somebody rates it.

## meets_need

- vp-a-finished-walk-can-say-who-did-which-part-of-it: SERVED, AND THIS IS THE HALF THAT SHIPS WORKING. Its outcome asks that a completed record be askable which states were under-driven, which over-driven, and how much of it the strong hand did. All three coordinates are on every record now, the log groups by each, and a grouping key nothing carries says so rather than returning one bucket that looks like an answer. THE PROPOSITION'S OWN CAVEAT STANDS: it says it is not a claim that the record is trustworthy yet, because the only party who knows what answered is the party being measured. That is still true, the record marks both claimed values, and the schema tells a caller before they send.
- vp-the-machine-says-how-strong-a-hand-each-step-needs: SERVED FOR A STEP, AND THE PROPOSITION SAYS STEP. The mechanism is demonstrated end to end — one cell rated, a record opened with no model named by anybody, and the step opening with a rung and its pair. WHAT IS NOT SERVED IS THE MILESTONE MAXIMUM: the declared winner publishes per state, so `sizeUnit` is built, tested and called by nothing. And nothing in the product's own matrix is rated, so no real pull carries the field today. THE PROPOSITION IS SERVED BY A MECHANISM RATHER THAN BY AN ANSWER, which is the honest line and the one a reader needs.

## musts_demonstrated

- sty-the-machine-picks-the-hands: DEMONSTRATED, `reports/rpt-sty-the-machine-picks-the-hands.md`, six steps run once against modules built from this code. One cell rated by hand at `C4/R1`; a record opened and pinned with no model named on any command line, in the seed or in the pin; the walk reaching that step; and the pull carrying `hand = {"pair":{"judgement":"C4","reading":"R1"},"rung":"frame"}`. THE SAME STEP UNRATED PUBLISHED NOTHING and the walk carried on, which is the second half of the same rule. A delegation left a record reading `part=guide relayed_by=walker`. WHAT THE RUN DOES NOT SHOW is a walker deciding to delegate — nothing compels one — and the report says so on its own face.

## market_tier

not a market iteration — this changes how the machine sizes its own work and reaches no customer surface, so the expensive real-world tier does not apply.

## round_0_verify

- evidence vs claims: THE CLAIMS WERE CHECKED BY DELETING THE MECHANISMS BEHIND THEM, not by reading them. A fresh-eyes tester built an isolated copy and mutated: five mechanisms could be removed with no red, and two of the five were broken rather than merely untested. All nine findings are fixed and the four weak checks now go red on deletion — verified for the pull case by deleting the line and running it.
- types: `npx tsc -p .` exit 0, no diagnostics, on the whole tree.
- lint: `npx biome check .` clean over 342 files, no new suppression. One finding was the linter's own — an optional chain that would have thrown on undefined — and it was fixed rather than silenced.
- tests: 1642 pass, 0 fail, 147 suites. Ten cases more than the tester ran against, all of them added by the fix pass.

## round_1_validate

- exercised against the goal: PARTLY, AND THE PART THAT IS NOT IS NAMED. The mechanism is exercised end to end by a check that seeds an iteration, pins its column, rates the cells, walks to a real step and reads the published statement off a live pull. What is NOT exercised is the product's own matrix, because nothing in it is rated — that is the debt entry, not an omission.
- missing: THE DEMONSTRATION. A must story is served by this build and its report belongs at run-demos, not here. The end-to-end pull is the join the unit checks hold both halves of.
- wrong: TWO THINGS WERE, AND A TESTER FOUND BOTH. A lane call with a bad part vanished from the log; the head's lookup had never once found a rated step. Both fixed, both with a case that goes red on deletion.
- out of scope: RATING THE 154 ACTIVE CELLS, declared at specify-build before the build began. The build makes a difficulty declarable, readable and refusable; it declares none.
- prior art: THE NO-MATCH VALUE IS OASIS XACML 3.0's, verified at the primary — OASIS Standard, 22 January 2013 — where NotApplicable and Indeterminate are distinct returned results. Nothing else here rests on prior art, and the rung vocabulary is ours.

## goals_served

- Every state in the rigor matrix carries a complexity rating on a five-rung ladder (C0 derive, C1 transcribe-or-rule, C2 apply, C3 author, C4 frame), each rated with evidence rather than asserted.: THE LADDER AND THE DECLARATION SHIP, THE RATINGS DO NOT. A cell carries `<column>_complexity: C3/R1` and the loader refuses a value outside either vocabulary. No cell in the shipped matrix is rated, and rating them is the matrix owner's judgement — raid-debt-the-load-time-complexity-refusal-is-off-until-the-matrix-is-rated.
- ONE fixed model list lives in the repo, identical on every host, mapping each rung to a model name.: NOT SERVED, AND DELIBERATELY. The declared winner publishes a rung and holds NO roster, which is the choice M4 made and gate-candidates blessed. The owner's ruling for a fixed list was recorded as an input to that choice rather than as an obligation on it, and declare-winner says so.
- Each milestone names the driver it needs before it is walked, computed live from the matrix and never pinned into a record's demands.: SERVED, AND THE SECOND HALF IS THE ONE WITH TEETH. The pull carries `hand` at every rated step, and a complexity change moves neither the demand digest nor the step shape — proved by folding the difficulty into the ledger and watching two cases go red. What ships names a STEP rather than a milestone, because the design publishes per state.
- Every call in the lane records which model actually answered it, so a walk can be attributed after the fact.: SERVED AND MARKED AS A CLAIM. The field is on every record and the transport still hands the engine a client name and no model, so the value comes from the party being measured. `claimed` says so on the record itself, and the schema tells the caller before they send it.
- A submachine takes the MAXIMUM complexity over its items, so one walker strong enough for the hardest item walks all of them and a fan-out never becomes a fleet.: BUILT, TESTED, AND CALLED BY NOTHING. `sizeUnit` takes the maximum per figure and keeps the spread. The declared design publishes per state, so there is no unit to reduce over — el-sizing records that under "One function it implements and never calls", because leaving it unallocated would hide a conflict between a standing must and the declared architecture.

## bound_breaches

- if-agent-harness-to-entrypoint: BREACHED AT 3.7 PER CENT, MEASURED THROUGH THE LANE'S OWN LOG. 321 of 8,685 lane calls exceeded the one-second bound. Excluding the two that spawn external work by design changes nothing at one decimal place. THE BREACH IS CONCENTRATED AND THAT IS THE USEFUL PART: `se_pull` alone is 305 of the 321, which is 24.5 per cent of all pulls. Every other verb is under one per cent. THE BOUND STILL CARRIES NO PERCENTILE to be judged against, for the fifth milestone running — a bound stated as an absolute over a distribution decides nothing, and this iteration adds a fifth measurement without fixing it.

## round_2_red_team

- THE DEMONSTRATION COULD HAVE BEEN RUN AGAINST A SERVER THAT DOES NOT HAVE THE FEATURE => AND IT WOULD HAVE PASSED. The lane server this whole iteration was walked through started before the build landed; it does not know the `as` argument exists. A demonstration against it shows a pull with no `hand` field and a log with no `part`, which is exactly what an unrated matrix also shows. The demo drawing names the trap before the run rather than after it, and the run used modules built from this code.
- TWO VALUE PROPS ARE SERVED AND ONLY ONE OF THEM DOES ANYTHING TODAY => TRUE, AND THE ASYMMETRY IS WORTH SAYING AT THE ACCEPTANCE. The attribution half works on every call from the moment a server built from this code runs. The sizing half publishes nothing until 154 cells are rated by hand. A reader who takes 'served' at face value across both would be wrong about one of them.
- THE MUST STORY'S OWN DECK CARRIED TWO SLIDES DESCRIBING A DESIGN THAT WAS NOT BUILT => FIXED AT fill-story-evidence, AND THE SHAPE IS WORTH KEEPING. One promised a model list the declared winner does not hold; the other said no field is declared for the weaker-driver reason. Both were true when written and became false at M4 and M7 respectively, and neither state that falsified them owns the deck. A story's slides are filled at M8 and can be stale for two milestones without anything noticing.
- THE ACCEPTANCE RESTS ON A DEMONSTRATION WHOSE HARNESS WAS DELETED => DELIBERATE, AND THE REPORT SAYS SO. The report carries the procedure and each line the run printed, and the same six observations are held by cases that go red when their mechanisms are deleted. A demonstration proves it happened once; a check proves it keeps happening. Keeping the harness would have added a third test file nobody would maintain.

## raid_additions

- project/spec/trace/raid/raid-debt-the-load-time-complexity-refusal-is-off-until-the-matrix-is-rated.md
- project/spec/trace/raid/raid-iss-the-matrix-cache-is-keyed-on-size-and-modification-time.md

## verdict

pass with overrides — the need is met by a mechanism, and one of the two propositions has nothing to show until somebody rates a matrix.

WHAT IS ACCEPTED. A call record that can say which hand did which part of a walk, on which model, in which state — working from the moment a server built from this code runs. And a sizing mechanism that declares, carries, refuses and publishes how strong a hand a step needs, demonstrated end to end.

THE OVERRIDES, and every one of them is on the record already.

- NOTHING IN THE MATRIX IS RATED, so the sizing half publishes nothing in the shipped product. Debt, with a three-act repayment.
- THE MILESTONE MAXIMUM IS BUILT AND CALLED BY NOTHING, because the declared design publishes per state.
- THE SAFETY ASYMMETRY IS TWICE VOLUNTARY and its register entry stands at crippling.
- THE MATRIX CACHE CAN SERVE A STALE ANSWER after a mtime-preserving restore. Older than this build, registered by it.
- THE ONE-SECOND BOUND IS BREACHED AT 3.7 PER CENT with no percentile to be judged against, for the fifth milestone running.
- NO LIVE EVIDENCE FOR THE THREE COORDINATES EXISTS YET, because the running lane server predates the build.

## follow_up

THE RATING PASS IS THE NEXT REAL WORK AND IT IS NOT THIS ITERATION'S. 154 active cells, judgement, and `exp-two-hands-rating-the-same-six-cells` measured what it costs: two readers agreed on five of six and disagreed on exactly the row that stands in for work happening elsewhere. The debt entry carries the three acts, in order, and the middle one is watching every open record re-pin.

WHAT A RETRO SHOULD LOOK AT, and it is the same thing twice. Two adversarial passes found sixteen things in about twenty minutes between them, and the battery was green through every one. Both worked the same way: delete the mechanism, re-run, see whether anything notices. Nothing in the method asks for that. `observe-red` asks a check to fail BEFORE the build and never asks it to fail again once the build is taken away.

AND THE SECOND THING A RETRO SHOULD LOOK AT is that a fix written against one probe held for that probe and nothing beside it. The guard that stopped a bad `part` losing a record compared raw arguments, and a relay with no declared part walked straight past it into the same swallow.

## anything_else

