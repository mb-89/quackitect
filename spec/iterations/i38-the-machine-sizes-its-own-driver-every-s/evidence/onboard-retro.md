---
form: onboard-retro
by: agent
signed_off: 2026-08-20T09:17:12.478Z
authors: agent
files: null
---

# Evidence form / onboard-retro

## current_situation

Fresh cloud box, arrived 2026-08-20T09:09Z. The lane came up through se-arrive; boot's four documents were read; preflight went red once on a stale prompt-layer projection and was repaired with place-prompt-layer.ts.

The survey stands at 0 open expeditions, 21 open iterations, 0 pending notes and 29 standing work tokens in the pool.

The owner routed one goal into this session: run i38 — the machine sizes its own driver. The dials were raised on the owner's word to strategic autonomy and the blockers-only stop notch, so gates are this walk's to bless.

i38's record already carries seven owner rulings as design input, written into record.md rather than left in machine-local notes. The iteration has not been worked yet: this is its M0.

## field_feedback

Asked and answered in the message that opened this session. The owner's report from outside the machine, verbatim in substance:

- "Same as always in the cloud. You have all the autonomy you need. You can bless everything."
- "You can use sub agents and deep research as you see fit."
- "Only stop on blockers."
- "Everything you find, write it into a field report because notes will not survive the container."

The last one is a field observation about this machine and not a preference: .se/ is machine-local and the box is reclaimed, so se_note is a write-only hole on a cloud run. It is answered below, in waste_leads and follow_up.

No other field report was offered and none was withheld — the question was put and this is what came back.

## notes_drained

- none — the inbox stood at zero on se_survey before this state was entered, so there was nothing to drain and nothing was judged.

## call_log_mined

- THE WINDOW IS THIS SESSION ONLY: se_log_query {since: last_retro} opened at 2026-08-20T09:09:32Z, the first record in the file, because a cloud box clones fresh and .se/calls.jsonl starts empty. No previous walk is visible from here.
- Tools over 37 records: se_pull 14, se_update 10, mirror_slow 4, se_survey 2, mirror_autonomy 2, se_log_query 2, se_run 1, se_file_read 1, mirror_stop_at 1.
- Failure rate: 5 rejected of 42. Four are narration and one is a wrong argument name.
- Refusal clauses by frequency: SE-C-120 three times, SE-C-101 once, SE-C-121 once.
- se_run count is 1 and it was avoidable: place-prompt-layer.ts run from the shell where the lane already carries se_prompt_place.
- LEAD: preflight names a script in its remedy where a lane verb exists; naming se_prompt_place instead removes the only shell call of the window.
- LEAD: group_by "clause" buckets all 38 records under "(none)", so the retro's first ranking cannot be produced with the tool the method prescribes.
- LEAD: se_log_query silently ignores an unknown key inside filter — {outcome: "rejected"} returned all 42 records instead of refusing under SE-C-101, so a wrong filter reads as a real answer.
- Demand log is empty: nothing asked the lane for a capability it lacked.
- Test timings have nothing to mine: the battery has not run on this box, and record-inspect said so on the way in.

## waste_leads

- The prompt layer arrived stale on a fresh clone and cost a red preflight plus one repair call. The projection is deterministic from project/guidance/, so a clone can only ever be stale or identical; placing it during se-arrive would remove the red entirely.
- Three SE-C-120 refusals in one boot, all the same mistake: a comma inside a one-line brief reads as a chained list. The engine already computes the split it wants — for update it rewrites the brief into a plan — but for a resolution it refuses. Nothing warns before the third.
- se_note is a write-only hole on a cloud box, and the owner named it before the machine did. Notes land in .se/notes.jsonl, which is machine-local and never committed, so a note captured on a reclaimed box is lost. The contract sends strays to se_note in four separate rules and cloud-runner.md repeats it twice; on this host every one of those instructions writes into a file nobody will read.
- The retro's own log-mining step assumes a log that outlives the session. On a cloud clone it never does, so the mining is structurally thin and the step cannot say so from inside.

## promotions

- The empty-inbox skip is already in both retro rows; it fired correctly here and needs no promotion.
- No local machine edits were made this window, so the state-machine promotion question has nothing to answer yet — i38 has not authored a state.
- ONE PROMOTION CANDIDATE, and it is guidance rather than a template: cloud-runner.md tells an unattended walk to capture every stray with se_note, and on this host that is advice to write into a file the box will eat. The card already knows the shape of this problem — it says in as many words that an unpushed commit did not happen — and the same reasoning applies to a note. Recorded as follow-up rather than edited mid-walk.

## process_stale

Compared against what this box actually is, rather than against an older retro.

The process assumes a machine with continuity: a call log that spans sessions, a notes inbox that survives, a person beside the box to answer a sanctioned stop. A cloud run has none of the three, and the method has been patched for two of them — cloud-runner.md answers the person (capture a note, keep walking) and the push ruling answers the disk (push or the work did not happen).

The third is not answered. The note is the patch for the missing person, and the note does not survive either. So the guidance currently routes a cloud walk's findings into machine-local storage and then relies on that storage to reach the retro that would act on them. The owner closed the loop by hand this session — write it into a field report — which is exactly the shape of a rule that has not been written down yet.

Not stale in its judgment, then. Stale in one assumption, on one host, and the fix is a durable home for a cloud walk's findings that is not .se/.

## follow_up

- Carry the owner's ruling into this walk: findings go into a field report on the branch, not into se_note, because .se/ dies with the container. Applied from here on.
- The three call-log leads (preflight's remedy naming a script, group_by clause returning (none), an unknown filter key answering instead of refusing) are engine work, not i38's scope. They belong to whoever next opens an engine iteration and are written into the field report so they survive this box.
- The retro's log-mining step owes a sentence about a fresh-clone window. Same home, same reason.
- Next: gate-kickoff, where i38 proposes its change size.

## anything_else

