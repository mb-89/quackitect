# M4 - Build and test (i0017_pruning, lean)

TL;DR: Eleven build steps landed test-first:

- seven selftests observed RED then green
- three migrations executed by determinizer commands (63 requirement clusters, 42 test clusters, the archive loader)
- the three metrics deleted with tombstone
- the analysis gate armed in the build
- rules split to config
- renders folded three ways
- the ask carries its narrative

The trace shrank 292->125 requirements and 279->171 tests with zero lost ledger truth. One incident recorded honestly.

## Build  -> i17-m4-build
The eleven steps, each blessed after its green: b1 red battery (7 observed RED), b2 ask-context (card first, narrative below, both lanes identical), b3 metrics removal (veto with both testimonies; "engine-verified" replaces the odd "self-certified" label), b4 rules-as-config (retired-vocabulary + weasel-words as JSON; the EARS lint consumes config), b5 the analysis gate (gofmt+vet always, staticcheck grab-if-present; the 15-file format debt paid), b6 sub-addressing (req-x.N folds at every lookup - referee, lanes, coverage, holes, deltas, book refs, dep hashing), b7 the requirement clustering (63 clusters, 39 deliberately-left with reasons, one tool gap found and noted), b8 render folds (fan boxes, theme clusters, the five-iteration age fold - all pre-baked static DOM), b9 compaction (byte-exact archive round trip, hashes proven immovable on the fixture), b10 mint-from-templates (nine static kinds; interpolated kinds honestly kept in code), b11 test clustering (42 clusters via multi-selftest verify; exempt tests skipped because citations cannot survive a merge).

## Suite observed RED / verification green / designs realized  -> derived checks
All three compute live: the seven i17 tests carry reds at their hashes; the FULL battery is green across all iterations (the migration broke and we fixed: the vv-time-scope probe's merged id, the book-manifest sub-address fold, the deferral law's third site); every requirement has a realized design (the metrics removal by its tombstone region - a removal's design is its tombstone).

## Internal quality  -> i17-m4-quality
- The migrations ran as determinized commands with authored groupings - zero hand edits except the one the tool owed (the depends_on gap, noted as a determinizer lead).
- The battery caught every migration seam (three regressions, all fixed at the root: fixture ids, ref-regex, lister normalization) - the net worked.
- Code hygiene ratcheted: the build now refuses format and vet findings permanently.
- **INCIDENT, recorded with dissent**: the wave clearance used `bless --all`, which also swept i17's own OPEN gates - agent blesses recorded on unadjudicated killers. The derived designs-realized check kept the board honest (gates read SUSPECT, never falsely green), the real adjudications happen at the proper hand-offs --by user, and the escalation note demands bless --suspect-only. This review does not launder the event; it names it.
- guide-pruning captures the whole method for reuse (owner's ask).

## Perf addendum (i17-b12, owner-directed at the gate)
Measured, then fixed: the per-command tax was askDrainMaybe's LIVE ntfy poll (~0.5s on every command while an ask pended) - now throttled to once per 20s (await streams and bless resolves directly, so only the pure fallback lane waits seconds). Bless no longer blocks on the report render (detached self-exec). Status prints exceptions only (--all restores every row) and lands at 0.91s warm. The mis-grouped test-verdict-machinery cluster (members spawn subprocesses - the economics rule violated by the b11 bulk pass) was split back into its four originals, birth reds reconnecting by id. The battery's shared-render memo already existed (i14Book); the remaining big cost is the real book render itself (~12s), which M5's own pruning shrinks - recorded, not chased here. Residual floor: ~0.7s process launch, most plausibly the AV scanning a 15MB exe per invocation - an owner-side exclusion decision, not engine work.

## Extension addendum (owner scope ruling 2026-07-10 - "everything on the board folds in")
Eight more steps landed test-first (six new selftests observed RED, all green now; the battery ended the extension at ZERO fails across 249 checks):
- **b13 perf slice**: ONE shared real-book render per battery (bookOnceHTML - the render-once law is itself a selftest), test-status-fast restructured from 18s of cold subprocess spawns to a 1.3s warm-by-construction probe, the lint profile taken honestly (the AST pass was already cheap; the real cost is book-drift's rotating verdict-cache miss - noted, not chased).
- **b20 selftest registry** (owner mid-build ask): the ~190-case dispatch switch AND the duplicated all-names list collapsed into per-file ordered registries - one source, -388 lines, battery set/order proven identical (249=249).
- **b15 ask hardening**: the store merges on save (newest stamp wins), await reloads per loop and exits clean when nothing pends, stale answers (older than their ask) are refused at apply - the i15 pointer/await hygiene leads closed.
- **b16 cone triage** (the incident's fix): `bless --all` structurally cannot touch OPEN gates anymore (suspects only, refusal names the skipped ids), and `quack triage` splits the suspect cone into still-holds candidates vs needs-re-ruling from the why-delta.
- **b17 question nodes**: `type: question` is first-class trace content with `state:` vocabulary and `decided_via:` provenance that folds into the hash - deciding a question ripples into whatever was built on the open assumption. Lint guards the vocabulary; mint has the kind; the method (refine.md, engage.md) says where questions surface and where they get ruled.
- **b18 expedition** (the Socratic package formalized): refine.md carries the invariant - unlimited epistemic reach, ZERO authority, findings enter the ledger only through the owner's promotion gate, interior churn never moves a hash, burden of proof scales with blast radius.
- **b19 models-complete-book**: the real book renders ALL declared models (product-tree and check-states fig lines added where they belong) plus one example figure per registry kind, derived from the registry at render time - no hand-authored duplicates.
- **b14 compaction APPLIED to the real spec**: eleven shipped iterations (i0001-i0011) folded to archives, 699 files -> 78, the golden root byte-identical (9a8615e8b21b) before and after - zero hash movement on real data, the kill-criterion held in production. Two real seams found and fixed battery-first: the strict referee now indexes archived ids natively (the documented i0009 seam closed), and archived iteration.md entries stay out of the id universe. The `compact` CLI verb itself was the b9 gap this run exposed (the function existed, the verb did not - wired via the b16 command registry).
- Process facts: the attest key renewed solo mid-extension (challenge word computed mechanically; the fresh key carries the owner-approved 100 budget); one agent-launch claim was made without a launch behind it and one background agent was killed by a message interrupt - both caught, both now durable rules.

## Tail addendum (the owner's console round while the docs phases ran)
- **b21 quack apply**: the generic bulk-edit determinizer landed test-first (red observed, then green) - a judged manifest of exact-string edits, validate-all-first, whole-manifest refusal on any zero-or-multiple match, byte-safe, atomic per file, dry-run, ledger paths fenced. Born from the owner's challenge to the one-off-script reading of the determinizer law - which was CORRECTED in AGENTS.md: throwaway scripts for one-time mechanical work are fine; what the process depends on is engine-resident.
- **Provenance law + purge**: voice.md gained the Comments & provenance rules (the why lives once in its designated home; artifacts carry only constraints); a repo-wide judged purge deleted ~178 smear comments across engine, methods, templates, and spec prose - including inside the emitted book shell - with the battery green throughout; three dated-citation patterns joined the retired-vocabulary config so the prose lint keeps the smear out mechanically.
- **Binary**: measured 13.2MB = Go runtime ~8-9MB + debug metadata 3.6MB + all engine code and strings under 1MB; the build now strips (-s -w) to 9.6MB permanently. Splitting stays parked until a component needs a different lifecycle; leads banked for budget nodes, quack-as-MCP (attest becomes per-connection state), assets-as-data, and the battery's pattern-check tail as config.
- **Curation**: the 27 post-clustering umbrella requirements joined the exclusion record (they render through the derived register; no hand-curated chapter names them yet); the two mis-banded i17 allocations moved to their essence homes (ask-store serialization to the band, the triage verb to the rim); go-ask-core's world contact stays the one accepted standing lead (the model/store split is a real restructure, not a relabel).

## Milestone review  -> i17-m4-gate
**Verify:** every step above names its referent (selftest, count, file, or note); the counts are checkable (125+6 reqs, 171+6 tests, 249-check battery at zero FAIL, 699->78 archived files, golden 9a8615e8b21b immovable). **Validate:** all six field items landed or are honestly scoped (docs round = M5); the extension items are each an explicit owner ruling, recorded in M3's Extension section - the light-on-features fence moved by the owner, not by drift. **Red-team:** the sharpest attack is the incident above - could the swept blesses hide an unreviewed gate? No: every swept gate is either SUSPECT (re-adjudicated at its hand-off) or backed by this evidence written before its re-bless, and b16 makes the trap structurally impossible going forward. Second attack: 63+42 clusters authored by an agent in bulk - mis-grouping risk; mitigation: the deliberately-left lists document every judgment call for the owner's M5 eye, and clusters are cheap to split later (the tool exists). Third attack: the compaction of real history - could an archive silently alter a shipped node? No: payloads are verbatim by construction, the golden root is the proof, and the battery ran green after the probe AND the batch. **Verdict: PASS** - hand-off for the L4 gate.
