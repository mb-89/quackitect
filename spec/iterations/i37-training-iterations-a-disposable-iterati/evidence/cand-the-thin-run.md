---
form: cand-the-thin-run
by: agent
signed_off: 2026-08-19T18:44:16.572Z
authors: agent
files:
---

# Evidence form / cand-the-thin-run

## current_situation

The first of three compose states in the candidates drawing this state seeds.

The thin run is the cheapest curve through the box. Every cell it picks either already exists in the codebase or was probed in this iteration.

## built

COMPOSED, NOT CODED. M4 composes candidates; nothing is implemented until M7.

The artifact is `project/spec/trace/candidate/cand-the-thin-run.md`, carrying the ten picks and the four sections the template demands: why this one, how it works, what it costs, what it leans on.

THE CURVE, one cell per row.

- the past state: export the content subtree at the rewind commit
- the future out of reach: the ceiling rests on git's ancestry primitive
- proving the guard: calibrate against a known specimen
- earlier results: hidden by the binding
- which iteration: the person chooses
- an incomplete run: reports where it stopped
- the conditions: record what cannot be recovered
- comparability: one fixed course, only the machine varies
- the observer effect: the agent is told
- the apparatus: measuring rides an ordinary walk

TWO OF ITS CELLS CARRY MEASUREMENTS rather than claims. The export produced 1149 files in seconds with zero mentioning the walked iteration. The ancestry primitive answered 0 forward and 1 reversed.

## follow_up

- The other two compose states run beside this one, then evaluate-set judges all three.
- ITS ONE BLOCKER IS A LIST ENTRY. `merge-base` is not on `se_git`'s allowlist, so this candidate needs the list to grow by one verb or the ancestry answer derived from `log` or `rev-parse`.
- ITS ONE WEAKNESS IS STRUCTURAL and evaluate-set has to weigh it: the guard is checked rather than unrepresentable, and a checked guard fails open when the check errors.

## anything_else

A CORRECTION TO WHAT THIS ITERATION WROTE ONE STATE AGO.

At run-candidates the agent wrote that no sub-machine was seeded and argued the shape did not earn its keep here, because each candidate is one curve through rows whose cells were already authored.

THE MACHINE REFUSED TO LEAVE THE STATE. SE-C-112: the candidates drawing still carried the pin's placeholder, so entering would have walked an empty machine and reported it done.

THE ARGUMENT WAS WRONG ON ITS OWN TERMS. Composing a candidate is not assembling ten existing things — it is stating what the assembly costs and what it leans on, which is different for each curve and is exactly what these three states record.

IT IS WRITTEN DOWN RATHER THAN QUIETLY FIXED because this iteration measures where the machine stops an agent, and this is one of those places: the reasoning was plausible, the refusal was correct, and the refusal is what caught it.
