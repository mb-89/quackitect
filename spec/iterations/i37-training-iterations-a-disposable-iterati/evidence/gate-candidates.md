---
form: gate-candidates
reopened: 2026-08-20T08:25:45.145Z — a feeder re-signed above it
bless: blessed by agent
by: agent
signed_off: 2026-08-20T08:25:49.980Z
authors: agent
files: null
---

# Evidence form / gate-candidates

## current_situation

i37 stands at gate-candidates, the end of M4. Four states are signed: derive-criteria, partition-functions, the whole enumerate-space sub-machine, run-candidates with its three compose states, cut-criteria and evaluate-set.

Twenty-five options across ten morph-box rows. Four candidates, three drawn at build_chart and a fourth minted here because the scores pointed at it.

The recommendation is the fourth curve.

## reasons_hold

THE CUTS HOLD AND THE DEPTH IS THE THING TO JUDGE. 106 of 109 rows were cut.

- Three survived because the four curves genuinely score differently on them: silent failure, a fallen condition being named, and surviving a host swap.
- Everything else was cut because it scores identically for every candidate. A criterion that cannot separate the options is not a criterion for this decision.

THE MOVES: none. The rows arrived in a settled order and none was reordered.

ONE REASON DOES NOT HOLD AND IT IS RECORDED RATHER THAN DEFENDED. This iteration's own three fitness criteria were not on the offered list at all, though all three were weighed at derive-criteria. They were read against the candidates in evaluate-set's reading instead of scored in its table. That is a workaround, not a judgment, and the owner has already said a bug in this area is being fixed.

## round_0_verify

- evidence vs claims: every number carried by this milestone was measured on this build. 1149 files exported with 0 mentioning i33; the i33 record folder holding exactly record.md; merge-base answering 0 forward and 1 reversed; 48 full and 4 tailored cells at the major column; 106 of 109 rows cut. The two external citations were fetched; everything else in the scan is marked RECALLED.
- types: nothing built. M4 composes; no engine code exists in this iteration.
- lint: nothing built. What was written is corpus nodes and evidence, and every write passed the corpus reader.
- tests: nothing built, and no test call was made. The battery belongs to verification.

## round_1_validate

- exercised against the goal: the goal is a benchmark that costs nothing to author. The export probe answered it directly and produced the fixture rather than describing it.
- missing: no requirement covers how a run is REACHED — lane verb, desk door, or neither. Named at gate-inputs and again at gate-requirements, deferred twice, and still open at the end of M4.
- wrong: one claim written at run-candidates was wrong and is corrected in place. The agent argued no candidates sub-machine was needed; the machine refused to leave the state, and the argument was wrong on its own terms. The correction is on cand-the-thin-run's compose state.
- out of scope: replay without re-invoking the agent stays i31. Drag ranking stays i32. A tree at an arbitrary past commit for reverse engineering was found by find_by_transforming and belongs to i30; it was recorded rather than routed.
- prior art: scanned live at M1 and applied here. Six options came from it directly, and the calibration transfer from find_analogy became a rule this iteration now follows about its own measurements.

## goals_served

- A benchmark run re-walks a named archived iteration from the commit before that iteration started.: SERVED IN DESIGN AND PROVED BY PROBE. cand-the-refusing-run-with-recorded-conditions carries the export cell. Measured: git archive of project/spec at 5f85977f^ produced 1149 files, 0 mentioning i33, and the i33 record folder holding exactly record.md.
- The lane refuses to resolve any commit that is not an ancestor of the run's rewind point, so the original answers are unreachable while the run is bound.: SERVED, WITH ONE GAP NAMED. Every candidate carries a ceiling. The exact primitive was probed — merge-base --is-ancestor, 0 forward and 1 reversed — AND merge-base is not on se_git's allowlist, so the verb is unreachable through the lane today.
- A run is chosen by iteration id, or drawn by size, and a draw records its seed so it repeats.: SERVED AND NARROWED. opt-the-choosing-is-done-by-the-person-and-no-chooser-is-built won the row after the owner ruled a person triggers every run. The seed survives only for the draw when no iteration is named.
- Runs cycle through the archive rather than repeating the last one, and the reports folder is the only scheduler state.: SERVED IN DESIGN, UNTOUCHED BY M4. Cycling is a read over the reports folder and no candidate varies on it, so no option and no axis in this milestone bears on it.
- A run fills a benchmark-run item template, and the filled report is the only thing committed.: SERVED. The report-writing row divides the candidates: written at the end, or as the walk goes. The winner writes at the end and records where it stopped.
- The benchmarks folder is concealed while a run is bound and visible everywhere else.: SERVED, AND IT INHERITS A KNOWN WEAKNESS. Every candidate but the guarded run rides the binding over three exclusion lists that disagree, and the reading says so rather than hiding it.
- Where a run stops is configurable, and the whole walk is the default.: SERVED BY RULING RATHER THAN BY DESIGN. The owner ruled it configurable with the whole walk as default, so no candidate varies on it and M4 had nothing to decide.
- vp-rigor-without-toil gains one success criterion measuring whether the machine carries more of the weight over time.: NOT SERVED YET, and it is the one goal M4 did not advance. The criterion is authored at the state that derives value, which this column has not reached.

## bound_breaches

- if-agent-harness-to-entrypoint: one, and it is a real breach rather than a slow answer. The lane process restarted between build_chart and run-candidates, came back at tactical, and silently dropped the owner's full-autonomy grant. The record's position survived; the grant did not. Captured as note-ef85e0c86b5e. The remedy applied was to restart the lane with SE_AUTONOMY and re-earn the boot reading.

## round_2_red_team

- STEELMAN: the guarded run wins the arithmetic 12 to 9 and the gate is endorsing something that scored lower => the arithmetic is endorsed and the reading overrides it for one stated reason. The guarded run's winning cell has never been run. A 5 on an unprobed mechanism and a 5 on a measured one score identically, and the form has no column for that difference.
- STEELMAN: minting a fourth candidate after the scores is fitting the answer to the data => it is, and it is declared rather than hidden. cand-the-refusing-run's own compose state named the swap BEFORE evaluate-set scored anything, which is the only reason this is a reading rather than a retrofit.
- Three of four candidates share nine of ten cells, so the evaluation is theatre => the four differ on exactly the axis this iteration ranked first, which is what a shortlist is for. The theatre would be scoring 109 criteria that cannot separate them.
- The whole milestone rests on one probe run once => yes, and it is the strongest evidence in the iteration precisely because it was run rather than argued. It is one command and any later reader can re-run it. The claim it does NOT support is named on the option: the engine has not been run against the exported tree.
- The ceiling's verb is not on the allowlist, so the winner may be unbuildable => it is a list entry, not a design flaw, and the fallback is named: derive ancestry from log or rev-parse, at more code for a worse answer.
- This iteration keeps finding faults in its own measurements => three times now, and each was caught by a control rather than by care. That is the calibration rule working, and it is the strongest argument that the instrument being built is worth building.

## raid_additions

- none as new nodes. The register's additions belong to log-risks and identify-assumptions, both signed. M4 moved one standing item rather than adding: raid-asm-a-throwaway-tree-at-an-old-commit-can-run-the-engine was settled by owner ruling on 2026-08-19 and carries its new seam — the current engine reads today's rigor matrix, so a rewound run walks today's column over yesterday's work.

## verdict

pass — the space was enumerated by seven finders, the shortlist separates on the axis this iteration ranked first, and the recommendation rests on probed mechanisms rather than on the highest score.

WHAT THE BLESS ENDORSES: cand-the-refusing-run-with-recorded-conditions as the candidate carried into M5, with cand-the-guarded-run's unrepresentable ceiling carried into M6 as the spike that can still overturn it.

BLESSED BY THE AGENT UNDER THE OWNER'S STANDING GRANT of 2026-08-19. The grant was silently dropped by a lane restart during this milestone and was restored deliberately; that breach is recorded above rather than papered over.

THE DISSENT WORTH KEEPING. The arithmetic picked a different candidate. The override rests on evidence grade rather than on score, and if the M6 spike shows the lane can read from a truncated history, the guarded run's ceiling should be taken and this ruling revisited at its own escalation.

## follow_up

- M5 opens at converge-pugh with four candidates and a stated recommendation.
- M6 CARRIES ONE RANKED SPIKE: can the lane read from a history that ends at the rewind commit? It decides whether the ceiling is checked or structural, and it is the only thing that could reopen this gate's ruling.
- ONE REQUIREMENT IS STILL OWED and has now been deferred three times: how a run is reached. It cannot be deferred past M5's architecture without the architecture inventing it silently.
- TWO WORK TOKENS STAND IN THE POOL. The visibility drift is this iteration's own dependency for the concealment cell. The search provider needs owner configuration.
- Four notes are in the inbox for the next retro, including the dropped autonomy grant and the compare-card confusion the owner asked to have written down.

## anything_else

THE MILESTONE'S BEST WORK WAS DONE BY TWO INDEPENDENT FINDERS ARRIVING AT THE SAME MECHANISM.

find_analogy transferred the laboratory positive control — never trust an absence without a control, because a blank that reads zero and an instrument that is switched off look identical. find_by_transforming reached the same idea from the other side, by reversing the ceiling's question: instead of asking whether the guard held, make the run prove it.

TWO LENSES, NO CONTACT, ONE MECHANISM. It is in every candidate this gate carries forward.

AND IT WAS NOT ACADEMIC. This session produced that exact failure three times — an empty search read as no-rows-match, a probe read as falsifying the iteration's central premise, and a silently reset autonomy dial. All three were unpaid tolls or dropped state wearing the clothes of a finding. Each was caught by a control rather than by care.

THAT IS THE ARGUMENT FOR THE WHOLE ITERATION, made accidentally. An instrument that measures where the machine stops an agent is worth building because the agent cannot reliably tell a stop from a result.
