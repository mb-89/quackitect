---
form: cand-the-guarded-run
by: agent
signed_off: 2026-08-19T18:44:17.633Z
authors: agent
files: null
---

# Evidence form / cand-the-guarded-run

## current_situation

The second compose state. The guarded run is the curve that pays most so that nothing in the measurement can fail quietly.

## built

COMPOSED, NOT CODED. The artifact is `project/spec/trace/candidate/cand-the-guarded-run.md` with its ten picks and four sections.

WHERE IT DIVERGES FROM THE THIN RUN, in four cells.

- the future is made UNREPRESENTABLE rather than checked — the run reads from a source whose newest commit IS the rewind point, so a request for anything newer cannot be formed
- one visibility rule is the single source, and read, search, glob and list all derive from it
- every run carries a deliberately broken guard: one request that must be refused, its refusal recorded, and a run whose forbidden request succeeded is discarded
- the report is written as the walk goes, so a run that dies still leaves what it measured

THE FOURTH DIVERGENCE MATTERS MORE THAN IT LOOKS. The runs that fail are the most interesting ones for an iteration that measures where the machine stops an agent, and they are exactly the runs that leave nothing under an end-of-walk report.

## follow_up

- evaluate-set weighs it against the other two.
- TWO OF ITS CELLS ARE UNPROBED. Nothing has tested whether the lane's verbs can read from a truncated history at all, and that is the M6 spike this candidate depends on most.
- ONE CELL IS NOT THIS ITERATION'S TO FINISH. The single visibility rule is a standing work token, so taking this curve turns a neighbour into a dependency.

## anything_else

THIS CANDIDATE IS THE ONLY ONE THAT ATTACKS THE FATAL RISK STRUCTURALLY RATHER THAN BY TESTING.

The register's fatal item is a ceiling that fails open and produces a confident wrong answer while the report still looks valid. The thin run and the refusing run both answer it with a check plus a control. This one answers it by making the wrong request unexpressible.

THE HONEST COUNTERWEIGHT: it is the only candidate whose central mechanism has never been run. The export was probed. The ancestry primitive was probed. A history that genuinely ends at a commit, read through this lane, has not been.
