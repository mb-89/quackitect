---
form: run-candidates
by: agent
signed_off: 2026-08-19T18:41:34.761Z
authors: agent
files:
---

# Evidence form / run-candidates

## current_situation

i37 stands at run-candidates, the placeholder M4 fills with one compose state per shortlisted combination.

Three candidates were drawn at build_chart and each is already composed on its own node: why it was drawn, how it works, what it costs and what it leans on.

- cand-the-thin-run leans only on primitives that were probed in this iteration.
- cand-the-guarded-run makes a future commit unrepresentable rather than refused, and leans hardest on things not yet run.
- cand-the-refusing-run keeps the cheap ceiling and moves the safe default to bind time.

The lane restarted between build_chart and here, and the boot reading plus nine method cards were re-earned. The record's position survived; the autonomy grant did not, and that is captured as a note.

## follow_up

- cut-criteria is next, then evaluate-set and gate-candidates.
- THE AXIS THE THREE SAMPLE is how much to pay so the guard cannot fail quietly. evaluate-set has to rule on it, and derive-criteria already ranks the ceiling first among this iteration's three fitness candidates.
- ONE FACT DECIDES BETWEEN TWO CELLS in the ceiling row and it is not a judgment: se_git's allowlist has no merge-base. Either it grows by one verb or the ancestry answer is derived from log or rev-parse.
- The engine-against-an-exported-tree spike is still unrun and is ranked first for M6. It bears on cand-the-guarded-run more than on the other two, because that candidate's history-with-no-future has never been read through the lane at all.

## anything_else

NO SUB-MACHINE WAS SEEDED HERE, and the reason is worth stating rather than leaving as an absence.

This state's own description says it is the placeholder a seeded candidate machine fills, with one parallel compose state per shortlisted combination. That shape earns its keep when candidates are large enough that composing each one is real work, and when they can be composed independently.

NEITHER HOLDS HERE. Each candidate is one curve through ten rows whose cells were already authored as option nodes with their own mechanism sections. Composing one is assembling ten things that already exist and saying what the assembly costs — which is what the candidate node's four sections are, and they are written.

SO SEEDING A MACHINE WOULD ADD STATES WITHOUT ADDING WORK. The composition is on the nodes, where the chart reads it, and the chart already renders all three curves.

THE HONEST COST OF THAT CHOICE: there is no per-candidate record of who composed what and when, which a seeded machine would have given. For three candidates authored in one sitting by one agent, that record would say the same thing three times.
