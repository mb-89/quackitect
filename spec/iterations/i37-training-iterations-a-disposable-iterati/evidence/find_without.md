---
form: find_without
by: agent
signed_off: 2026-08-19T17:49:22.928Z
authors: agent
files:
---

# Evidence form / find_without

## current_situation

i37 stands at find_without, the fourth finder of enumerate-space. Three finders are signed with fourteen options between them.

The cluster holds eight functions. Every one is asked here whether it can go, plus the cluster itself.

Two trims survive as options. Two fail outright and say so.

## applies

yes — the owner has struck four designs from this iteration already, so asking what else can go is the finder most likely to be right.

## trims

| cluster-the-benchmark-run | nobody | The whole iteration goes. Every improvement to the machine stays a matter of taste, which is the as-is this record was opened to end. |
| cluster-the-benchmark-run | the user | Choosing goes to the person, who names an iteration every run. Cycling is what breaks: a person naming one from memory drifts to the same few, and coverage quietly stops growing. |
| cluster-the-benchmark-run | another cluster | Locating the rewind point goes to git itself. Nothing breaks functionally, and what is lost is the NAME — the ability to say which commit a result was taken at without re-deriving it. |
| cluster-the-benchmark-run | nobody | The ceiling goes. Nothing stops working and every number becomes worthless, because a run can read what the original concluded and the report still looks valid. This is the one trim that fails outright. |
| cluster-the-benchmark-run | nobody | The concealment goes. Runs still work and results still pair. What breaks is slower: a run reads the last reading and drifts toward it, and the drift is invisible in any single report. |
| cluster-the-benchmark-run | another cluster | Deriving the cost goes to the call log's own query verb. Nothing breaks if that verb can partition by state, and today it cannot — the record carries no state field. |
| cluster-the-benchmark-run | another cluster | Stating the conditions is absorbed into deriving the cost. The version and the matrix hash survive the merge; the model, the effort and the harness do not, because nothing writes them down today. |
| cluster-the-benchmark-run | the environment | The throwaway tree goes to git worktrees. Nothing breaks, and the trim is real: the tree is a mechanism rather than a function, and the owner's engine ruling already shrank it to content only. |
| cluster-the-benchmark-run | nobody | Filling the report goes. The run still happens and nothing survives it, which makes the whole exercise unrecorded. This is the second trim that fails outright. |

## options

- [[opt-the-choosing-is-done-by-the-person-and-no-chooser-is-built]]
- [[opt-the-conditions-are-taken-from-the-log-rather-than-stated-separately]]

## follow_up

- The remaining finders run next: heuristic, probing and transforming.
- TWO TRIMS ARE LIVE CANDIDATES and go into the morphological box rather than being decided here.
- ONE TRIM IS ALREADY HALF-TAKEN BY A RULING. The throwaway tree shrank to content only when the owner ruled the current engine runs, and find_without says the rest of it may be a mechanism rather than a function.
- The cost-derivation trim depends on whether the call log can partition by state, which is an open assumption from M3 and is answerable against this session's own log.

## anything_else

TWO TRIMS FAIL OUTRIGHT AND THE PAIR IS INSTRUCTIVE.

REMOVE THE CEILING and nothing stops working. Every number becomes worthless and every report still looks valid. That is the worst failure shape in the iteration and it is the reason the ceiling ranks first among the criteria.

REMOVE THE REPORT and nothing stops working either. The run happens, the tree is discarded, and no trace of it survives.

BOTH TRIMS PASS THE OBVIOUS TEST AND FAIL THE REAL ONE. Nothing breaks in the sense of throwing an error. What breaks is the reason the thing exists, which is exactly the failure a trimming exercise is prone to waving through.

SO THE COLUMN ASKING WHAT BREAKS HAS TO MEAN WHAT STOPS BEING TRUE, not what stops running. Six of the ten rows above are only interesting under that reading.
