---
form: gate-release
bless: blessed by agent
by: agent
signed_off: 2026-08-20T13:40:35.377Z
authors: agent
files:
---

# Evidence form / gate-release

## current_situation

gate-release, the last gate. M9 is packaged and driven.

THE ARCHIVE IS BUILT AND WAS USED: `dist/quackitect-6.0.0.zip`, unpacked into an empty directory, installed, and driven. Every guard held in a tree that is not this one.

THE BATTERY IS 1634 OF 1634, tsc exit 0, biome clean over 342 files — all three read fresh for this gate rather than carried from an earlier one.

SEVEN OF EIGHT GOALS ARE SERVED. The eighth is served too, and was served before I first reported it missing.

## market_block

not applicable — this iteration is not declared to market. It builds machine overhead measurement, which the kickoff ruled is not a customer-facing promise and which is why it carries no dedicated value proposition.

## round_0_verify

- evidence vs claims: the claims that could be run were run. Two benchmark runs against this repository's own archive, one named and one by default. The package unpacked, installed and driven in a fresh directory. The started-commit probe over all sixteen shipped iterations. What could NOT be evidenced is named rather than smoothed: neither run went through the lane verb, because the server in this session predates it and se_reload is illegal where the walk stood.
- types: `npx tsc --noEmit` exit 0, read directly. The lane's cached typecheck lagged a round twice during this walk and reported a fixed error as live, so the direct read is the one this gate rests on.
- lint: `npx biome check .` clean over 342 files. Two ratchets bit during the build and both were re-earned with written reasons rather than raised quietly.
- tests: 1634 of 1634 across 151 suites. The benchmark file is 40 of 40, and its green means something now: at verification it was green over a function that returned nothing on real data.

## round_1_validate

- exercised against the goal: a run binds against a real archived iteration, stands a tree whose future is absent, records what it was taken under, refuses if it cannot, and closes saying where it actually ended. Demonstrated twice. What is NOT exercised is the comparison the goal exists for, which needs two runs on two machine versions.
- missing: a lane verb that consults the ceiling. Everything else the kickoff blessed is built.
- wrong: three claims I carried forward without re-checking, all found this milestone. The eighth goal was served and I said otherwise four times. The concealment block did not exist. The started-commit assumption was written against eleven records when there are sixteen, and is false for ten of them.
- out of scope: nothing left scope. Two things came IN after they had been passed over — the door, on the tester's argument, and the concealment, on the owner's question.
- prior art: applied and one piece of it decided a field. Tau-bench's finding that agents vary between identical runs by more than the effect being measured is why the item template REQUIRES a median over at least three runs, and why two runs are recorded here as 'a pair is reachable' rather than as a result. TPC-H is why change_size is a required condition and an unpinned iteration refuses at bind. SPEC's ratio is deliberately shed by owner ruling. WHERE THE COMPARISON WAS NOT MADE: no system was found that benchmarks a process machine by re-walking its own history, and that negative stays weak — se_web_search refuses for want of a key, so sources were named in advance rather than found.

## goals_served

- A benchmark run re-walks a named archived iteration from the commit before that iteration started.: SERVED AND DEMONSTRATED TWICE. Bound to i33 at 20abd831 and, after the pool filter, to i11 by default. Both stood a tree and passed their control.
- The lane refuses to resolve any commit that is not an ancestor of the run's rewind point, so the original answers are unreachable while the run is bound.: SERVED IN THE TREE, NOT AT A VERB, and this is the one override that survives to release. HEAD does not resolve inside a bound tree and the rewind point does — both observed. No lane verb consults resolvesInBoundTree.
- A run is chosen by iteration id, or drawn by size, and a draw records its seed so it repeats.: SERVED FOR THE ID PATH, and the size draw is not built. The owner's ruling that a person triggers every run removed its caller, so building it would have been building for nobody.
- Runs cycle through the archive rather than repeating the last one, and the reports folder is the only scheduler state.: SERVED, AND USABLE ONLY AFTER M9. The reports folder is the only scheduler state. The default pick refused every time until the pool was filtered to iterations that can bind — six of sixteen, with the ten it drops NAMED.
- A run fills a benchmark-run item template, and the filled report is the only thing committed.: SERVED. The template is written, conditionsFor fills it, and a report missing any of the ten fields is refused. Verified in the shipped package on an empty archive: eleven problems from an empty report.
- The benchmarks folder is concealed while a run is bound and visible everywhere else.: SERVED, AND IT WAS AN OVERRIDE TWO GATES AGO. Nine cases green, every door watched closing and reopening, and the block that deferred it turned out not to exist.
- Where a run stops is configurable, and the whole walk is the default.: SERVED. stop_at defaults to the whole walk, and both runs recorded stop_at and ended_at disagreeing rather than omitting one.
- vp-rigor-without-toil gains one success criterion measuring whether the machine carries more of the weight over time.: SERVED SINCE M1, in commit 11515b64, with three metrics. I reported it unserved at four gates and corrected that at gate-validation.

## bound_breaches

- if-agent-harness-to-entrypoint: TWO BREACHES, both the same fault and both before M6 — a lane restart silently dropped the owner's autonomy grant, notes note-ef85e0c86b5e and note-a02771bee06a. No third occurrence across the rest of the walk. DISPOSITION: an entrypoint defect and not this iteration's subject. It needs the owner, because the dial has no lane verb by design.

## round_2_red_team

- STEELMAN: nothing here has been used by an agent, so shipping it ships an untested affordance => true, and it is the sharpest thing that can be said. Both demonstrations drove the functions because the running server predates the verb. What the verb's existence rests on is the trace-coverage enumerator counting it and the typecheck compiling it, which is not the same as a call.
- STEELMAN: an author who got three carried claims wrong should not adjudicate their own release => the three are why this form cites a commit hash for the goal, names the two runs by their rewind commits, and quotes the package output rather than summarising it. Every claim here is a thing a reader can re-run.
- STEELMAN: the pool is six, so the mechanism has a third of the material its design assumed => true and measured this milestone. The mechanism is unaffected and the CLAIM is narrower: an archived iteration is the benchmark, and six of them are reachable.
- ATTACK: the feature is inert on delivery => true, found at package and nowhere else. A fresh receiver has no shipped iterations, so their pool is zero and se_benchmark refuses. The refusal is the right one and the README says nothing about benchmarks at all.
- ATTACK: the ceiling reaches no verb, so a bound run reads the live root => true, carried as the one surviving override, and narrower than it was: the git objects are genuinely absent inside the rewound tree and the concealment now covers the lane half.
- KILL-CRITERION: this is the wrong call if the mechanism cannot be trusted to report what it measured => the place that would show it is a real run's conditions, and two were taken. All ten fields recorded both times, stamp_covers carrying six directories rather than the matrix hash alone, and reached_the_end false where a lazier design would have dropped a field. What survives is that the mechanism reports honestly and refuses rather than guessing. What does not survive is any claim that an agent has driven it.

## raid_additions

- [[raid-asm-every-shipped-iteration-carries-a-started-commit-naming-it]]
- [[raid-iss-the-package-proof-is-run-by-hand-and-nothing-repeats-it]]
- [[raid-iss-a-corpus-wide-law-with-a-state-local-trigger-bills-a-stranger]]

## verdict

pass with overrides — the build is done, driven and packaged, and ONE requirement is not met and is not pretended to be.

THE OVERRIDE, with its dissent. `req-a-ceiling-that-cannot-prove-ancestry-refuses` is not met as worded: no lane verb consults `resolvesInBoundTree`, and the function returns a bare boolean so a missing `.git` and an out-of-range commit are indistinguishable. DISSENT: the requirement's own `breaks_if_removed` says a ceiling that goes quiet looks exactly like a ceiling that passed, and this ships with the ceiling quiet at the verb layer. What makes it tolerable rather than fatal is that the objects are genuinely ABSENT inside the rewound tree — the git half is structural — and the lane half is now covered by the concealment.

WHAT ELSE SHIPS KNOWN AND NAMED, none of them a requirement failure.

- THE FEATURE IS INERT ON DELIVERY. A fresh package has no shipped iterations, so `se_benchmark` refuses until the receiver has walked and shipped one.
- THE POOL IS SIX OF SIXTEEN here, and the ten it drops are named rather than swallowed.
- NO AGENT HAS DRIVEN THE VERB. Both demonstrations drove the functions.
- THE SIZE DRAW IS NOT BUILT, because the owner's ruling removed its caller.

BLESSED BY THE AGENT under the owner's grant of 2026-08-19, widened the same day to cover every gate in this iteration.

## follow_up

- shipped is next and it closes the iteration.
- THE FIRST THING A NEXT SESSION SHOULD DO is drive `se_benchmark` through the lane, on a reloaded server. That is the one claim this iteration could not make.
- WIRE THE CEILING TO THE REF-TAKING VERBS. `se_file_read`, `se_file_search` and `se_file_glob` all take a `ref`; none asks `isBound`. Cheaper now that `isBound` has four consumers.
- TWO RUNS ON TWO MACHINE VERSIONS is what turns this from a mechanism into a measurement. Everything needed for it exists.
- FIVE EMITS DID NOT LAND and each is a note or a register entry. The next record's `promotions` field reads that list.
- THE RETRO IS OWED and it opens with the owner's own field feedback.

## anything_else

WHAT THIS ITERATION ACTUALLY PRODUCED, beyond the mechanism.

THREE ENGINE DEFECTS FIXED AND SHIPPED. A reopened placeholder that could not be re-signed and had stopped a previous unattended run dead. A corpus-wide law with no sweep, which billed i37 for nine crossings i9 minted before the law existed. A checklist that asked an agent to tick reds it never observed, where one state disagreed with itself about whose specs it meant.

ONE MEASURED CASE FOR FRESH EYES. I signed four build chunks on evidence that was entirely green — tsc, biome, preflight, sweep, 1616 of 1618 — and the central function returned nothing on real data. The fixture was written from the design document rather than from the record it would meet, so the test and the code agreed with each other and neither agreed with the product. Then two of my FIXES were worse than what they replaced, and the same standing gatekeeper caught both.

ONE PATTERN THAT COST MORE THAN ANY DEFECT. A statement about the corpus, once written into a form, gets copied forward rather than re-asked. The eighth goal, the concealment block, the started-commit assumption. Each was one grep from the truth and none was checked until something outside forced it — the owner asking, a deferred until coming due, and a gate field making me open a file for an unrelated reason.

THE LIMIT THAT MATTERS MOST, AND IT IS NOT A DEFECT. The read ratchet caught three wrong file reads by refusing to let a count rise without a written reason. That is a cost the machine imposes on purpose, and its signature — a slow state, several refusals, a fix — is identical to drag. A report that ranks states by cost will rank that state badly and has no way to tell the difference. This iteration met its own blind spot from the inside, which is the most useful thing a first run of an instrument can do.
