---
form: cand-the-refusing-run
by: agent
signed_off: 2026-08-19T18:44:18.682Z
authors: agent
files: null
---

# Evidence form / cand-the-refusing-run

## current_situation

The third compose state. The refusing run sits between the other two: the cheap ceiling, with the safe default moved earlier.

## built

COMPOSED, NOT CODED. The artifact is `project/spec/trace/candidate/cand-the-refusing-run.md` with its ten picks and four sections.

ITS ONE STRUCTURAL IDEA: a run exercises its own ancestry test BEFORE anything binds, and refuses to bind if the test cannot be run.

WHY THE EARLIER POINT IS THE BETTER ONE. A run that starts and then refuses everything produces a report full of refusals that reads as a machine failure. A run that never starts produces one refusal naming one cause.

IT KEEPS THE THIN RUN'S CHEAP CELLS everywhere else, except the conditions, which it derives from the call log rather than writing at bind time.

## follow_up

- evaluate-set judges all three next.
- ITS WEAK CELL IS THE CONDITIONS. The model, the reasoning effort and the harness are properties of a session and appear in no call record, so deriving them from the log cannot work for those three.
- THAT WEAKNESS IS FIXABLE BY SWAPPING ONE CELL, which is worth saying at evaluate-set: this candidate with the thin run's conditions cell is a fourth curve nobody drew.

## anything_else

THE THREE CANDIDATES ARE NOT INDEPENDENT DESIGNS AND SHOULD NOT BE JUDGED AS IF THEY WERE.

Nine of the ten rows have at least two candidates sharing a cell. What separates them is a handful of cells, all on one axis: how much is paid so the guard cannot fail quietly.

SO EVALUATE-SET IS RULING ON THAT AXIS, not choosing between three architectures. The winning curve is likely to be one of these three with one or two cells swapped, and the swap this state already names — the refusing run with the thin run's conditions cell — is the obvious first one.

SAYING SO HERE RATHER THAN AT THE GATE, because a shortlist that pretends to be three sealed alternatives makes the evaluation look more decisive than it is.
