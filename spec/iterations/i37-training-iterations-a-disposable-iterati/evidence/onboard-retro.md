---
form: onboard-retro
by: agent
signed_off: 2026-08-19T16:42:19.255Z
authors: agent
files: null
---

# Evidence form / onboard-retro

## current_situation

22 iterations stand, 19 of them seeded and 3 open. No expedition is open.

The inbox is empty. Three notes were pending at entry and all three were drained in this state.

i37 is the record being walked. It was seeded today from a research conversation with the owner, and its design was settled across four rounds of rulings recorded on the record itself.

The dial stands at ideation. The owner granted full autonomy for this walk in as many words, and the lane was restarted with SE_AUTONOMY to make it so.

## field_feedback

The field is the owner, and the feedback was continuous rather than reported.

FOUR RULINGS CAME BACK AGAINST THE AGENT'S OWN DESIGN, and each one made the design smaller.

- An authored scenario pool was struck. The archive already holds real design inputs.
- A sandbox package was struck. A re-walked iteration writes real tests against a real tree.
- A path mask was struck. Rewinding the tree hides the answers by construction.
- A dedicated value proposition was struck. This is machine overhead, not a customer-facing promise.

THE PATTERN IS WORTH NAMING. Every strike removed authoring work the agent had proposed and replaced it with something the repository already had. The agent reached for new mechanism four times where reuse was available.

## notes_drained

- note-6e4e08262b4e: obsolete. It argued that the narrow size distribution in the archive forces authored fixtures. The owner ruled that patch, product and specification are not gaps, so the premise fell.
- note-b92076461465: backlog. Minted wt-outward-scanning-states-cannot-reach-a-search-engine-on-this. The lane has no search provider configured, so a prior-art state must name its sources in advance.
- note-b821100070a7: backlog. Minted wt-three-separate-lists-decide-which-paths-a-lane-verb-may-see-. Three exclusion lists disagree and the reading verb consults none of them.

## call_log_mined

- 218 calls in the span. 16 were rejected, which is 7 percent.
- SE-C-121 fired 4 times, all on se_update, and it is the clearest lead. A chained brief is SPLIT into items by the engine's own correction, which mints node ids the caller never chose. The caller then names a node that does not exist. The correction is right and the ids it creates are invisible until the next refusal.
- SE-C-040 fired 3 times. The toll lapsed inside long reading runs, which is the shape the reading-pays-nothing rule was meant to cover.
- SE-C-101 fired twice, and both are sibling-verb drift. se_file_search REQUIRES intent; se_file_glob REFUSES it. se_log_query refuses since, which every log tool in the world accepts.
- SE-C-046 fired twice. se_file_patch wants ops rather than a bare path, and nothing in the neighbouring write verb suggests that.
- SE-C-106, SE-C-125 and SE-C-110 fired once each and were self-explanatory.
- se_update is the most-called verb at 47, ahead of se_file_read at 35 and se_pull at 32. Narration costs more calls than reading does.

## waste_leads

- Four rounds of design were spent proposing mechanism the repository already had. The benchmark walk in tests/fallback-outcome.test.ts was found only after the first full design was written.
- Two web fetches returned nothing usable. One page was JavaScript-rendered and one arXiv API call timed out twice.
- The lane was restarted once because the autonomy dial cannot be raised in place, and the restart cost a full boot reading loop.

## promotions

- none-found. This walk wrote no local change to a template. The two findings that would promote are already minted as work tokens rather than as template edits.

## process_stale

THE PROCESS HELD, and the one place it strained is narration.

se_update is the most-called verb in the span, at 47 calls against 35 reads. Four of its rejections came from ids the engine minted by splitting a chained brief. So the narration mechanism cost calls twice: once to narrate, once to recover from its own correction.

THE READING LOOP IS RIGHT. Boot's four documents are heavy but they land, and the probes caught nothing missing.

THE STATE OF THE ART OUTSIDE was scanned for this iteration and is recorded at ref-agent-benchmark-harnesses-2026. Nothing found there says this process is behind. What it says is that the field measures agents with fixed task sets and reports reliability over repeated trials, and this iteration is how that arrives here.

## follow_up

TWO WORK TOKENS WERE MINTED IN THIS STATE and are in the pool.

- wt-outward-scanning-states-cannot-reach-a-search-engine-on-this. Ready when the owner puts a search provider key in the server environment.
- wt-three-separate-lists-decide-which-paths-a-lane-verb-may-see-. Ready when a hiding rule must bind more than one verb, which i37 itself needs for the benchmark folder.

ONE LEAD IS NOT YET A TOKEN and belongs to a later retro rather than to this iteration: the chained-brief correction mints node ids the caller cannot see, and SE-C-121 fired four times because of it.

THE WALK CONTINUES to gate-kickoff, where the change size is proposed.

## anything_else

THE SECOND WORK TOKEN IS THIS ITERATION'S OWN DEPENDENCY, and that is worth saying plainly rather than leaving in the pool.

i37 needs the benchmarks folder concealed while a run is bound and visible otherwise. That is a conditional hiding rule, and the drift the token names is exactly what stands in its way.

So the token is not parked work. It is a piece of i37 that was found before i37 started.
