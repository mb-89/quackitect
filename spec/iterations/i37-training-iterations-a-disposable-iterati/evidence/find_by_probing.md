---
form: find_by_probing
by: agent
signed_off: 2026-08-19T17:55:29.179Z
authors: agent
files:
---

# Evidence form / find_by_probing

## current_situation

i37 stands at find_by_probing, the last finder of enumerate-space. Six finders are signed with twenty-three options between them.

Three probes ran here, all timeboxed at minutes. Two answered outright and one answered half, and the half it did not answer is the more useful finding.

Two options are minted.

## applies

yes — two of the design's load-bearing claims were cheap to settle by running something, and one of them turned up a blocker nothing else had seen.

## probes

| Can a content-only rewind be materialised at all, and is the result complete and free of the answers the walk must derive? | minutes — one command, abandon if it needs more | the walk itself; nothing was run against the exported tree, and no engine was started | HOLDS AND EXCEEDS. git archive of project/spec at 5f85977f^ into an empty directory produced 1149 markdown files. Zero mention i33. The i33 record folder holds exactly record.md — the seeded input, with no evidence and no machines folder. The fixture is correct by construction. |
| Can the ancestry test the ceiling rests on be answered exactly, and is the verb reachable through the lane? | minutes — two calls | the lane; the test ran through se_run under a declared no_tool_reason rather than through se_git, which is not legal in this state | HALF HOLDS. git merge-base --is-ancestor answers exactly: 0 forward, 1 reversed. AND THE VERB IS NOT ON se_git'S ALLOWLIST — status, log, diff, show, add, commit, fetch, branch, rev-parse, restore, merge, checkout. The ceiling's natural verb is unreachable through the lane today. |
| Does a lane read at a ref honour the ref, or fall back to the working tree? | minutes | nothing — this one ran clean through the lane's own search verb | HOLDS, and it is the probe the whole iteration rests on. A search of project/spec/trace at 5f85977f^ for i33 returns 0 files against 60 today, with a control search for i15 returning 123 at the same ref. |

## options

- [[opt-the-content-subtree-is-exported-at-a-commit-rather-than-the-repository-checked-out]]
- [[opt-the-ceiling-rests-on-the-ancestry-primitive-git-already-answers]]

## dead_ends

- Asking the ancestry question through se_git. Killed by SE-C-110: se_git is not legal at find_by_probing. The question was answered through se_run under a declared no_tool_reason instead, and the reason is filed.
- Asking for merge-base through the git lane at all. Killed by the allowlist, which has no merge-base. This is a finding rather than a dead end and is recorded on the option.
- Running the engine against the exported tree. Not attempted, and named rather than skipped: it needs node_modules and a boot, which is past a minutes-long timebox. It is the M6 spike.

## follow_up

- build_chart is next, then converge, and then the enumerate-space sub-machine returns to M4.
- ONE BLOCKER CAME OUT OF THIS STATE and it is small. se_git's allowlist has no merge-base, so the ceiling's exact primitive is unreachable through the lane. Either the allowlist grows by one verb, or the ancestry answer is derived from log or rev-parse, which is more code for a worse answer.
- THE ENGINE-AGAINST-EXPORTED-TREE SPIKE IS THE ONE THING STILL UNRUN. It is ranked first for M6 and the probe here has already built half its fixture.
- The export probe cost minutes and settled the shape of the rewind. It should be the first thing any later reader re-runs if the design is doubted.

## anything_else

THE EXPORT PROBE ANSWERED MORE THAN IT ASKED, and the extra answer is the best evidence in the iteration.

The question was whether a content-only rewind can be materialised. It can, in seconds, with one command.

WHAT CAME FREE WAS THE FIXTURE'S CORRECTNESS. The i33 record folder at that commit holds exactly one file: `record.md`. Not a partial evidence folder, not a stale machines directory. The seeded input, alone.

THAT IS THE WHOLE DESIGN DEMONSTRATED RATHER THAN ARGUED. Every earlier state reasoned that the archive holds real design inputs at the right commits and that the answers are absent. This state exported the tree and looked.

AND THE SECOND PROBE FOUND THE ONLY CONCRETE BLOCKER IN THE ITERATION. Everything else outstanding is a spike or a ruling. This one is a missing verb on a list, and it is the kind of thing that is invisible until somebody tries to run the exact call.
