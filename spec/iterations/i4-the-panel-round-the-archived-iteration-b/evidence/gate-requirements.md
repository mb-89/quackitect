---
form: gate-requirements
bless: blessed by agent
by: agent
signed_off: 2026-08-23T16:59:06.052Z
authors: agent
files: null
---

# Evidence form / gate-requirements

## current_situation

DESIGN INPUT ENDS HERE, and the register gained no row.

THE ROUND'S FOUR OPENING PIECES ALL TRACE TO STANDING ROWS, and the two that were named but never quoted are now read to the sentence.

A COLD REVIEWER READ THE SAME FIVE FORMS and found five behaviours the session shipped that no record owns. That finding is confirmed against the code and it is what this gate turns on.

## round_0_verify

- evidence vs claims: TWO CLAIMS FALSIFIED. write-requirements says the round changes "no interface, no data". The session added a `role` argument to the se_run tool schema, which is an interface change, and created the estimate store at .se/estimates.jsonl, which is a data change. The sentence is true of the round's four pieces and false of the working tree, and the form does not say which reading was meant. Everything else checked out: the grey-state row and the archive rows were read to the sentence and say what the forms claim.
- types: nothing outstanding. The lane typechecks after every edit to a source file and hands errors back on the next answer; none came back during this state.
- lint: nothing outstanding, by the same channel. Earlier in the session three complexity ceilings were hit and each was fixed by extracting a helper.
- tests: NOT RUN IN THIS STATE, and that is correct rather than a gap. The engine decides scope and the full battery is verification's own. No source changed here — the state wrote spec nodes and evidence.

## round_1_validate

- exercised against the goal: YES for three of the four opening pieces. The archived read, the bless repaint and the grey reason each trace to a row whose statement was quoted, not merely named.
- missing: FIVE BEHAVIOURS. Only walkers count against the ceiling, a reviewer is never weaker than its guide, a reference renders as a link, an estimate grades itself per model, and narration marks the acting hand alive. Each is a thing the system must now do and none has a row.
- wrong: NOTHING WRONG IN THE REGISTER ITSELF. No row contradicts another and no row was authored this round that could.
- out of scope: THE SURFACE COLLAPSE NEEDS NO ROW, ruled by the owner on 2026-08-23. Removing an obsolete design element creates no obligation, and two surfaces was residue from an earlier HTML version rather than a demand.
- prior art: THE MULTI-AGENT REPORT WAS READ AND IT CHANGED THE ROUND. The walker ceiling defaults to zero because of it, recorded as its own decision node.

## goals_served

- the archived iteration browses like the live one, read from its branch, looking as it stood at the close: SERVED by req-a-closed-records-folder-stays-on-trunk and req-a-records-own-status-decides-whether-it-is-open, both pointing at uc-browse-the-archive.
- a bless repaints the surface without an engine reload, the whole wire: SERVED by req-a-surface-shows-the-state-an-act-produced, which demands the changed state be rendered before the actor can act again.
- a grey state says why it is grey, with why-not-green riding the state's detail: SERVED by req-one-verb-says-why-a-state-is-grey, which demands every condition standing in the way be named.
- render.ts and mirror.ts collapse into one surface, which is the HTML mirror cut seen from this side: NO ROW AND NONE OWED, by owner ruling. Six per-widget decisions are owed before the deletion and they live on the assumption node, not in the register.
- the shell, pane and trace-graph defects i23 had already ruled, built rather than re-argued: NOT INDIVIDUALLY TRACED. scope-non-goals names them as a group of eight and no form maps them one by one. That is a gap in the scope statement rather than in the register.

## bound_breaches

- if-agent-harness-to-entrypoint: NO BREACH OF THIS BOUND. Nothing this round changes crosses from the harness into the entrypoint; the surfaces, the register and the assumption nodes all sit inside the engine and the spec. One breach does stand that this bound does not name: five behaviours were built this session that appear in no record's scope and in no exclusion list, which is the boundary failing rather than being crossed.

## round_2_red_team

- the completeness argument is circular => CONFIRMED. It reads "this round authors no row, so it can neither open nor close a coverage hole". The coverage check computes requirement-to-use-case linkage, so a behaviour with no row is invisible to it. An empty register is trivially complete against itself.
- the function argument inherits that defect => CONFIRMED. derive-functions derives nothing because the register gained nothing. It is the same claim one hop later, not an independent check.
- the assumption sweep missed two of its three => CONFIRMED AND FIXED. The first pass looked only at the four opening pieces. The reviewer read the shipped code and found both with file and line. Both are now recorded and one has already probed false.
- three forms described the round while the tree held more => THE PATTERN BEHIND ALL THREE. Each form was internally consistent and nothing in the machine compares a form against the working tree.
- deriving the changed list from git would catch it => ACCEPTED AS THE FIX, owner proposal 2026-08-23. Commit at the start and end of a section, then read what changed from the diff. It would not replace the judgment fields, which no diff can answer.

## raid_additions

- raid-the-panel-can-carry-everything-the-mirror-renders
- raid-three-samples-make-a-per-model-correction-trustworthy
- raid-a-hand-with-no-role-recorded-is-a-walker

## verdict

pass with overrides — the register binds the round's four opening pieces and every mechanical check is green, but five behaviours shipped this session belong to no record, and deciding which record owns them is the owner's scope call rather than mine

## follow_up

ONE DECISION IS OWED AND IT IS THE OWNER'S. Five behaviours shipped this session belong to no record. Adopting them into i4 means five rows, five functions and a reopened design-input chain. Routing them to another record costs nothing here. Scope is not mine to set.

ONE DEFECT NEEDS NO DECISION TO EXIST. A hand registered with no role is counted as a walker, and ten of twelve registered hands carry no role, one of them a reviewer. The ceiling can therefore refuse a legal walk and name a walker that is not there. Recorded as an issue with its fallout named.

SIX DECISIONS ARE OWED BEFORE THE MIRROR IS DELETED, one per widget, and that is M7's work.

THE GIT-DERIVED CHANGE LIST IS A METHOD CHANGE and belongs in the retro rather than here.

## anything_else

