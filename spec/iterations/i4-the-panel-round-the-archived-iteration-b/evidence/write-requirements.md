---
form: write-requirements
by: agent
signed_off: 2026-08-23T16:48:10.328Z
reopened: 2026-08-23T16:47:14.240Z — the surface collapse has no standing row; the register said none while a fourth scope piece is unbound
authors: agent
files: null
---

# Evidence form / write-requirements

## current_situation

A large requirement corpus already stands under spec/trace/requirement/, and this round introduces no new need.

THE FOUR PIECES ARE ALREADY REQUIRED. Two standing rows carry the archived read, and one carries the bless repaint in its own words.

SO NOTHING IS AUTHORED HERE. The rows this round touches are re-read for continued truth instead.

## register

- none

## set_criteria

- complete: computed, not attested. The field declares covers: use-case, so the engine refuses this state while any requirement refines no use case or any use case is covered by none. This round authors no row, so it can neither open nor close a coverage hole.
- consistent: no new row means no new term and no new conflict. The three rows this round leans on were read directly: req-a-closed-records-folder-stays-on-trunk and req-a-records-own-status-decides-whether-it-is-open both point at uc-browse-the-archive, and req-a-surface-shows-the-state-an-act-produced states "When an act on a control changes the state that control reads from, the product shall render the changed state on that surface before the actor is able to act on it again" — which is the bless-repaint piece exactly.
- affordable: the set is unchanged in size, so its buildability is unchanged. What this round adds is build work against rows that already existed, not new obligations.
- bounded: nothing gold-plated, because nothing was written. The scope cut lives at scope-non-goals and draw-context, and this state adds no row that could exceed it.
- comprehensible: unchanged. No new vocabulary entered the set.
- no_tbd: RUN, not asserted. A search for TBD, TBC, TBR and ??? across spec/trace/requirement returned zero matches over the whole folder.
- behaviour_modelled: none here wanted one. This round changes what a surface shows, not what the system does over time, so no sequence, state model or lifecycle earned its place. A model on every row is slop and worse than the gap it closes.
- quality_groups_swept: two of the nine are touched and seven are not. INTERACTION CAPABILITY is touched — a grey state that says why, and a surface that repaints after an act, are both about a person understanding what happened. PERFORMANCE EFFICIENCY is touched — the archived read carries the one-second rule, already logged as a risk with its own trigger. Functional suitability, compatibility, reliability, security, maintainability, flexibility and safety are not touched by this round: it renders what the machine already knows and changes no behaviour, no interface, no data and no failure mode.

## follow_up

THE TWO UNQUOTED PIECES WERE RE-READ, and the register still gains nothing.

THE GREY-STATE REASON IS COVERED. req-one-verb-says-why-a-state-is-grey states: "When asked why a state is grey, the engine shall name every condition standing in the way of that state." That is the piece exactly, and the state's own detail already demands the condition be named rather than the colour.

THE SURFACE COLLAPSE NEEDS NO ROW, by the owner's ruling of 2026-08-23: "We don't need a requirement to remove an obsolete design element. The HTML, it shouldn't exist. It's just because we used to be HTML in earlier versions." Two surfaces was never a demand anybody made. It is residue from a version that rendered to HTML, and removing residue creates no obligation.

I HAD ARGUED THE OPPOSITE AND REOPENED THIS STATE FOR IT. The argument came from the probe: six widgets live only in deliverable/engine/render.ts and nothing outside that file consumes one. That finding is real and it is a BUILD finding. A row saying one surface shall carry everything would have written today's implementation into the register as a demand.

WHAT SURVIVES INSTEAD: six decisions, one per widget, owed before anything is deleted. They live on raid-the-panel-can-carry-everything-the-mirror-renders and belong to M7.

## anything_else

