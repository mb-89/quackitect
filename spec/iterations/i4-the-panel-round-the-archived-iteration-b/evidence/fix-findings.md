---
form: fix-findings
judgment: not passed at 2026-08-24T11:21:27.991Z
by: agent
signed_off: 2026-08-24T11:19:57.079Z
authors: agent
files:
---

# Evidence form / fix-findings

## current_situation

THE BATTERY STOOD AT 1763 tests, 1736 passing, 27 failing when this round of repair began. Every finding was collected before anything was fixed.

ONE CAUSE DOMINATED. The rigor matrix grew a roster row at position 05 of every milestone, M0 through M9 — spawn-the-hands at M0 and spawn-for-<phase> at M1 through M9. Ten rows landed and the checks that count rows were never moved with them.

WHAT THAT BROKE, all measured rather than reasoned:

- The matrix row count, 53 against a real 63.
- The major column, 54 against 64.
- The minor column, 30 against 40, and the patch column, 20 against 30.
- Both table views, 53 against 63.
- The floor law, which named four rows where fourteen now declare it. The roster row is FLOOR and never struck, so all ten copies survive every size.
- The contracted incoming sets at patch. The roster row absorbs the contraction: a struck stretch collapses onto it rather than reaching past it.

A SECOND CAUSE WAS THIS SESSION'S OWN. A preflight guard added earlier today compared the installed extension against the source rendered with this product's name. In a root with no product configuration that comparison renders against a fallback and reports a stale install, which held boot short of the front desk and failed every walk fixture. It now runs only where the root has its own configuration, and the missing-configuration check already speaks for the other case.

THE CLAIM-OPERATIONS FIXTURE WAS CLEARED FIRST, and it was the blocker the previous round could not move. Three rules decided it, each measured: a claim is green only if its feeders are; a bare signed note is a whole claim; a section is checked against its field's template. The roster row became the first claimful state and its only field is a checklist, so the sentence the fixture writes could never pass.

## follow_up

WHAT THIS ROUND PRODUCED, beyond the repairs.

A FUNCTION WAS MISSING FROM THE STRUCTURE and the flow-closure check found it. flow-view-model was minted with an interface carrying it from the view resolver to the mirror, and both elements implemented one function. A flow whose two ends sit inside a single function can never close, because closure is a relation between two functions. fn-run-a-governed-walk.resolve-the-view now owns the resolving half, and the drawing half consumes it.

WHAT IS PARKED, with its reason.

- The comment-rule ceiling stands above its bound and is older than this round. It names its own offenders, so it is mechanical to work down, and it is not this state's finding.
- Two surface tests, the morph and the details pane, assert behaviour the resolver move changed. They need a reading of what the move intended, not a number.
- A design pointer cites a section that no longer exists under that name, and the guidance names an editors index that is nowhere. Both are citations to nowhere left by earlier work.

THE ROSTER ROW NEEDS ITS OWN NOTE IN THE METHOD. The floor law now names fourteen rows, and a reader meeting that list should find the argument for it where the method lives rather than in a test.

## anything_else

