---
form: evaluate-set
by: agent
signed_off: 2026-08-23T17:50:08.553Z
reopened: 2026-08-23T17:48:05.725Z — the score table scores four axes and the cut now stands at six, one of them struck
authors: agent
files: null
---

# Evidence form / evaluate-set

## current_situation

THE AXIS SET CHANGED AFTER A COLD REVIEW, so this state is answered against five scored axes rather than four.

THE FIRST TABLE WAS DISCARDED RATHER THAN EXTENDED, because a scorer that has seen its own earlier numbers is no longer a clean hand.

THE SECOND TABLE REVERSES THE FIRST. On four axes the repeater dominated everything. On five it does not dominate at all.

## scores

| candidate | axis | score | anchor | prior_art |
| --- | --- | --- | --- | --- |
| cand-the-guarded-collapse | req-a-wrong-act-never-passes-silently | 4 | "a check refuses any other module that emits surface markup. The refusal is typed and carries a remedy", docked one because its own cost section concedes the guard may degrade "to a convention with extra words" | the lane's own typed refusals, which name a clause and an executable remedy |
| cand-the-repeater-surface | req-a-wrong-act-never-passes-silently | 3 | "The moment the surface has to derive one field from another, the seam has leaked" — the violation is named and no detector for it is | none named |
| cand-the-host-first-surface | req-a-wrong-act-never-passes-silently | unscorable | its three scored sections name no check, refusal, correction or failure report at all | none named |
| cand-the-repeater-surface | req-panel-shows-the-machine | 5 | the engine resolves "which control is grey, why, what the label reads, which row is highlighted" and the surface draws it | an air data computer feeds a complete instrument picture that no display assembles for itself |
| cand-the-guarded-collapse | req-panel-shows-the-machine | 4 | "each of the six widgets is ported into the surviving view or dropped with a signed reason" — the licence to drop permits part of the machine to stop being shown | VS Code UX Guidelines, Webviews: a webview view carries what the host has no widget for |
| cand-the-host-first-surface | req-panel-shows-the-machine | 2 | its own words: "A TREE IS NOT A GRAPH... this candidate pays its price exactly where the drawing is most useful" | none named |
| cand-the-guarded-collapse | req-surface-answers-in-one-second | 3 | "It is a rule in the lint pass, not a runtime guard" — no render cost added, and none addressed | none named |
| cand-the-repeater-surface | req-surface-answers-in-one-second | 2 | "all computed once, server side" with the surface holding "no position of its own", so every look is a round trip | none named |
| cand-the-host-first-surface | req-surface-answers-in-one-second | unscorable | nothing in its scored sections speaks to render or response time | none named |
| cand-the-repeater-surface | req-controls-draw-from-their-spec | 4 | the engine owns control rendering and the view model is closed, so a widget "cannot quietly reach around it"; short of 5 because the source of truth is the engine's computation rather than a spec file | the lane's own control bar, drawn from a spec file that refuses an undeclared widget type |
| cand-the-guarded-collapse | req-controls-draw-from-their-spec | 3 | it supplies the refusal half only, and for a different subject; nothing says the bar is drawn from a spec | none named |
| cand-the-host-first-surface | req-controls-draw-from-their-spec | 1 | "THE STATES BECOME TREE ITEMS the host draws" and "What a tree item can carry is fixed by the editor", so the spec cannot govern the vocabulary | none named |
| cand-the-repeater-surface | req-filter-draws-only-what-serves | 3 | no filter is named, but the mechanism covers it: the engine computes the whole view and input crosses back as an intent | none named |
| cand-the-host-first-surface | req-filter-draws-only-what-serves | 2 | "the host's own item model" is a seam, so which nodes and levels get drawn falls to the host | none named |
| cand-the-guarded-collapse | req-filter-draws-only-what-serves | unscorable | its scored sections are entirely about who may emit markup; filtering and node selection appear nowhere | none named |

## front



## reading

NOTHING DOMINATES ANYTHING, and that is the honest headline.

THE GUARDED COLLAPSE AND THE REPEATER TRADE. The guarded collapse is better on catching a wrong act, 4 against 3, and on latency, 3 against 2. The repeater is better on showing the machine, 5 against 4, and on controls, 4 against 3. Neither is at least as good on every axis, so both stand.

THE HOST-FIRST SURFACE LOSES EVERY AXIS IT CAN BE COMPARED ON: 2 against 5, 1 against 4, 2 against 3. It escapes elimination only because two of its cells are unscorable, and incomparability is not a virtue. It should be read as last rather than as a survivor.

THE FIRST TABLE SAID THE OPPOSITE, and the difference is the two axes the review restored. On four axes the repeater dominated everything. Adding controls-draw-from-their-spec and filter-draws-only-what-serves, and striking the engine-internal corpus axis, turned a clear winner into a genuine trade. That is the cut mattering, which is what the cut is for.

FIVE OF FIFTEEN CELLS ARE NOT NUMBERS. Three are unscorable and the scorer refused to guess them, which is correct and which also means the table is thinner than it looks.

THE LATENCY COLUMN STILL RESTS ON NO MEASUREMENT. Both scored cells are inferred from where each candidate says the work happens, and the repeater's 2 is the one score most likely to move under a real number.

NO WINNER IS PICKED HERE.

## follow_up

THE TRADE IS THE REAL RESULT and it should reach convergence intact. A guard that catches a wrong act against an engine that makes the wrong act impossible to express is the choice, and neither table cell decides it.

THE REPEATER'S LATENCY SCORE NEEDS A NUMBER. It is the one cell that could flip the trade, and it is the cheapest thing on this list to measure.

THE HOST-FIRST CANDIDATE SHOULD NOT BE CARRIED FORWARD AS AN EQUAL. It survives on silence, and its own note already records the owner's field experience against it.

THE SCORER FOUND MY BRIEF WRONG ON ONE POINT: no candidate note carries a `## Mechanism` heading, they carry `## How it works`. It said so rather than scoring nothing.

## anything_else

