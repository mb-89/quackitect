---
form: pressure-test
reopened: 2026-08-23T15:22:55.326Z — frame-delta was re-signed after it, so pressure-test answered older ground
by: agent
signed_off: 2026-08-23T15:23:01.827Z
authors: agent
files: null
---

# Evidence form / pressure-test

## current_situation

Vision, register, as-is, delta and scope are all signed. The packet is drafted, which is the precondition for pressing on it.

This state presses, and anything the FAQ cannot answer cleanly folds back before the gate.

## prfaq

THE PRESS RELEASE.

The panel now tells the truth about every record, not only the ones still moving. A record that shipped last month opens from its own branch and reads exactly as it stood at the close: green where it passed, thumbs on the gates that were blessed, every deck and form there to read. A gate blessed today repaints under the hand that blessed it, with no reload. A state that is not green says why, in its own detail. And the controls are drawn once, by one renderer, so a change in the engine reaches every surface at the same moment.

THE HOSTILE FAQ.

Q: Is this not just polish on a board that already works?
A: No. A closed record is currently unreadable, which was witnessed on i51 the day this round opened. History that cannot be opened is history that was not kept.

Q: You are collapsing two renderers. What breaks for the surface that loses?
A: Neither loses. The collapse is a shared function both call, and the pattern was already proven this session when one shared renderer fixed a panel that drew on one surface and not the other.

Q: Reading from a branch is slow. Are you not breaking the one-second rule to win a feature?
A: Yes, deliberately, for one case, and it is on the record as a risk with an owner and a trigger. A fast surface that cannot show a closed record fails the thing the surface is for.

Q: Twelve pieces in one record. Which one ships if the round runs out?
A: The renderer collapse, because everything else draws through it. That argument is on the record; the order itself is not yet fixed, and that is logged.

Q: The UI record covers the same renderers. Which record owns the job?
A: This one builds, that one argues. The split is written into both records rather than held in conversation, and the dependency was already declared before the split.

Q: What if the archived record's machine definition has since changed?
A: It renders from its own branch, so it shows the machine it was walked under. That is what looking as it stood at the close means.

Q: What would make you stop?
A: A measurement showing people stop opening archived records because the read is too slow. At that point the trade bought nothing and the mechanism needs rethinking.

## findings_folded

TWO QUESTIONS DID NOT ANSWER CLEANLY, and both fold into things already standing rather than into new work.

THE ORDER QUESTION folds into the risk register. raid-r-the-panel-round-carries-more-than-it-opened-with already carries it, with the build plan naming its first chunk as the trigger. The FAQ's answer is an argument, not a decision, and the entry says so.

THE SLOW-READ QUESTION folds into raid-r-the-archived-read-may-breach-the-one-second-rule, which names what would change the ruling. The FAQ could state the trade but not prove it acceptable, because nothing has been measured yet.

EVERY OTHER QUESTION ANSWERED FROM SOMETHING ALREADY ON RECORD, which is the result this state exists to produce. Nothing folded back into the vision.

## follow_up

Nothing new is parked. Both unclean answers landed on entries that already exist, each with its own owner and trigger.

The packet stands ready for the gate.

## anything_else

