---
form: run-candidates
by: agent
signed_off: 2026-08-23T17:24:30.132Z
authors: agent
files:
---

# Evidence form / run-candidates

## current_situation

THREE CANDIDATES STAND, drawn across a box built from five options that seven finders produced.

- The guarded collapse: one surface, plus a check that makes a second one unwritable.
- The repeater surface: one surface that derives nothing, because the engine computes the view.
- The host-first surface: one surface, most of it the editor's own widgets.

THIS STATE IS A PLACEHOLDER. It holds the parallel compose states a seeded candidate machine would fill, one per shortlisted combination, and no such machine was seeded for this round.

## follow_up

COMPOSING EACH CANDIDATE IN FULL IS NOT WHAT THIS ROUND NEEDS. The three differ in one decision each, and every one of them starts with the same collapse. What separates them is a guard, a view model or a widget source, and each is a paragraph rather than an architecture.

THE CUT AND THE SCORING DO THE REAL WORK NEXT. cut-criteria drops the axes that do not discriminate, and evaluate-set scores what is left.

ONE CANDIDATE CANNOT BE SCORED ON EVIDENCE. Whether the walk is tree-shaped enough for the host's own widgets is unprobed and unprobeable by script, so the host-first surface carries an honest unknown into the comparison rather than a number.

## anything_else

