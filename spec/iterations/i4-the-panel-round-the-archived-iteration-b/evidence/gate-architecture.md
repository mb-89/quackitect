---
form: gate-architecture
bless: blessed by agent
by: agent
signed_off: 2026-08-23T18:37:40.089Z
reopened: 2026-08-23T18:37:20.286Z — evaluate-architecture was re-signed after it, moving three cards from at risk to addressed
authors: agent
files: null
---

# Evidence form / gate-architecture

## current_situation

THE DECOMPOSITION IS TWO NEW ELEMENTS AND ONE SUPERSEDED, with eleven interfaces answering every crossing the element matrix computes.

IT WENT THROUGH A COLD REVIEW AND CAME BACK WRONG. Nine findings landed, one of them structural: no interface carried the view model out of the resolver. All nine are fixed and the fixes are on the record rather than in this prose.

FIFTY-FIVE QUALITY SCENARIOS ARE RULED, eleven addressed by the new structure and five at risk.

## round_0_verify

- evidence vs claims: THE STRUCTURE NUMBERS ARE ZERO WHERE THEY MUST BE. Unimplemented functions: zero. Owed crossings without an interface: zero. TWO CLAIMS IN MY OWN FORMS WERE WRONG and both are corrected: decompose-structure said one interface was owed where the matrix computed seven, and evaluate-architecture's summary said ten cards moved where the walk lists fifty-five.
- types: nothing outstanding. No source file changed in this milestone.
- lint: nothing outstanding, by the same channel.
- tests: NOT RUN, and correctly. Nothing the engine watches changed here.

## round_1_validate

- exercised against the goal: YES, and it took two passes. The first decomposition realised the decision on paper and could not have delivered it, because the element that computes the view had no way to hand it over.
- missing: A LATENCY MEASUREMENT, still. Two of five at-risk verdicts turn on it and neither rests on a number.
- wrong: NINE THINGS WERE, AND ARE NOT NOW. The missing outbound interface, the guard's cluster, the guard's self-contradiction, two feeds bypassing the resolver, a dropped flow, three scenarios misplaced outside the cone, and one ruled addressed that is at risk.
- out of scope: THE OTHER THREE OPENING PIECES remain build work that this architecture does not change.
- prior art: cited on the nodes rather than paraphrased, in both directions — the publisher's guidance and an instrumentation analogy.

## goals_served

- the archived iteration browses like the live one, read from its branch, looking as it stood at the close: NOT TOUCHED by this architecture.
- a bless repaints the surface without an engine reload, the whole wire: SERVED, and only after the repair. The act crosses on if-mirror-to-view-resolver and the new view returns on if-view-resolver-to-mirror. Before the repair the first pass cited an interface carrying no act at all.
- a grey state says why it is grey, with why-not-green riding the state's detail: SERVED. The reason is computed with the rest of the view.
- render.ts and mirror.ts collapse into one surface, which is the HTML mirror cut seen from this side: SERVED, with the element recorded as superseded and its surviving realization named.
- the shell, pane and trace-graph defects i23 had already ruled, built rather than re-argued: NOT REACHED; build work, no element bears on them.

## bound_breaches

- if-agent-harness-to-entrypoint: NO BREACH. Every element and interface this round adds sits inside the engine and the surface it draws.

## round_2_red_team

- no interface carried the view model out of the resolver => FIXED, and the mechanical check could not have caught it, because it computes owed crossings from flows and the missing flow was mine to mint.
- the guard sat in the wrong cluster and contradicted itself => FIXED.
- two feeds delivered the compiled machine straight to the surface => FIXED; both now reach the resolver.
- flow-filter was dropped while the resolver claimed the filter axis => FIXED.
- eight scenarios were ruled outside the cone while inside it => FIXED.
- req-panel-shows-the-machine was addressed while an unminted assumption stood against it => FIXED, and that assumption is now probed.
- THE LATENCY RISK WAS IMAGINARY => MEASURED AND WITHDRAWN. A profiled render spends 1163.6 of 1190.2 ms in one engine phase; the surface's own share is 26 ms. The round trip three cards were at risk on does not exist.
- THE GUARD'S RULE WAS THE WRONG RULE => CORRECTED BY RUNNING IT. One entry point does not fit a tree where 38 modules emit and twenty of them must. Only a registered module may emit.
- THE SURFACE CANNOT DERIVE NOTHING => THE DECISION'S SENTENCE CHANGED. Six places already keep a caret, a scroll or an unsubmitted edit, and it now says the surface derives nothing about the walk.
- three of this round's own judgments were overturned by its own later states => THAT IS THE PROCESS WORKING, and each names what changed it.

## raid_additions

- raid-the-surface-repeats-a-computed-view-behind-a-guard
- raid-the-computed-view-answers-inside-the-second
- raid-emitting-a-widget-is-detectable-by-a-check
- raid-an-editor-view-can-be-passive-enough

## verdict

pass — the decomposition survived a cold review that found nine defects and three spikes that overturned two of its own verdicts, and every one was fixed rather than recorded and walked past; two scenarios remain at risk and both now carry a probed hinge rather than a suspicion

## follow_up

M7 BUILDS INSIDE THIS BASELINE. A new element found mid-build returns here.

THE GUARD'S RULE IS NOW SPECIFIC ENOUGH TO BUILD: only a module registered in the editor index may emit widget markup. The registry exists, so the check compares two standing lists.

THE REDRAW EXCEPTION IS THREE SHAPES WIDE and must be decided once rather than six times: focus, scroll offset, and an unsubmitted edit.

A STANDING DEFECT IS NAMED AND OWNED BY NOBODY YET. machine.sets costs 1163.6 ms alone, and that is where the one-second budget breaks.

THE VIEW MODEL WILL DECAY AT ITS FIRST GAP. The guard catches a module emitting markup; it does not catch a derivation inside a registered one.

## anything_else

