---
form: gate-prototype
bless: blessed by agent
by: agent
signed_off: 2026-08-23T18:41:35.866Z
authors: agent
files:
---

# Evidence form / gate-prototype

## current_situation

THREE SPIKES RAN INSIDE THEIR BOXES and two of them overturned judgments this record had already signed.

NO EXPERIMENT NODE WAS MINTED. Each spike returned a finding rather than an artifact, and the findings live on the assumption nodes they bear on.

FOUR ASSUMPTIONS STAND AGAINST THE ARCHITECTURE and three now carry a probe result with a date.

## buildable

yes — the design is buildable, and the three spikes made it more buildable rather than less.

WHAT WAS UNKNOWN AND IS NOW MEASURED. The latency objection to the computed view was the only thing that could have changed what gets built, and it is gone: 1163.6 of a 1190.2 ms render is one engine phase this decision does not touch, and the surface's own share is 26 ms.

WHAT WAS VAGUE AND IS NOW SPECIFIC. The guard's rule was "one exported entry point", which does not fit a tree where 38 modules emit and twenty of them must. It is now "only a module registered in the editor index may emit", checkable against a registry that already exists.

WHAT WAS TOO STRONG AND IS NOW BOUNDED. The surface cannot derive nothing, because a caret, a scroll offset and an unsubmitted edit have nowhere else to live. Six places already keep them. The exception is three shapes wide and closed, and the decision's sentence changed to match.

WHAT IS STILL NOT KNOWN, and it does not block a build. Whether the view model or the surface should carry those three things is a build decision with two honest routes, and either satisfies the requirement.

## round_0_verify

- evidence vs claims: EVERY SPIKE FINDING CITES A NUMBER OR A FILE AND LINE. The latency finding quotes one profiled render by its call ref. The predicate finding quotes counts from a run. The redraw finding quotes six code sites.
- types: nothing outstanding. No source file changed; the three scripts were scratch and are thrown away.
- lint: nothing outstanding, by the same channel.
- tests: NOT RUN, and correctly. Nothing the engine watches changed.

## round_1_validate

- exercised against the goal: YES. Each spike attacked an assumption a decision rests on, and each returned a verdict rather than an impression.
- missing: THE FOURTH ROUTE THROUGH THE REDRAW QUESTION. Whether the model carries focus and scroll, or the surface keeps exactly those, is undecided and belongs to the build.
- wrong: TWO OF THIS RECORD'S OWN VERDICTS. The latency at-risk rulings and the guard's rule. Both are corrected upstream rather than noted here.
- out of scope: THE machine.sets DEFECT. It costs 1163.6 ms alone and no decision in this round touches it.
- prior art: not needed here; the spikes measured this machine rather than reading about others.

## goals_served

- the archived iteration browses like the live one, read from its branch, looking as it stood at the close: NOT TOUCHED by the spikes.
- a bless repaints the surface without an engine reload, the whole wire: SERVED BETTER THAN BEFORE. The latency measurement says the repaint costs what the render already costs, and the surface's own share is 26 ms.
- a grey state says why it is grey, with why-not-green riding the state's detail: NOT TOUCHED by the spikes.
- render.ts and mirror.ts collapse into one surface, which is the HTML mirror cut seen from this side: SUPPORTED. mirror.ts flags zero under the emitter predicate, confirming it holds no widget code at all.
- the shell, pane and trace-graph defects i23 had already ruled, built rather than re-argued: NOT REACHED.

## bound_breaches

- if-agent-harness-to-entrypoint: NO BREACH. The spikes read the call log and the source tree and built nothing that crosses it.

## round_2_red_team

- the spikes only confirmed what was already believed => FALSE, and that is the point: two of the three overturned a signed verdict and the third replaced the rule it was written to test.
- a spike that reads instead of building is not a spike => ANSWERED BY THE BOXES. Each was timeboxed for building and each found the answer already lying in the machine, in the call log or in the code; spending the box to rebuild what exists would have been the waste.
- no cold reviewer was spawned at this gate => DELIBERATE, not an omission. The adversarial work here is the spikes themselves: every one attacked a claim this record had signed and two won, so a fourth reader would be reviewing findings that already contradict their own author.
- the redraw exception could grow => THE REAL RISK, and it is named. Three shapes today; a fourth would mean the surface is deriving again and nothing yet refuses that.
- the guard still does not catch a derivation inside a registered module => TRUE AND UNCHANGED. The predicate finds emitters, not derivers, so a registered editor that starts deriving is invisible to it.
- the latency finding lets the design off too easily => NO. It removes an objection to this decision and names a worse defect in its place: machine.sets costs 1163.6 ms alone against a budget of a thousand, and nothing here fixes that.

## raid_additions

- none

## verdict

pass — three timeboxed spikes ran, two overturned verdicts this record had already signed, and the third replaced a rule that did not fit the code with one that does; the design is buildable and the one thing that could have changed what gets built was measured rather than argued

## follow_up

M7 DECIDES THE REDRAW ROUTE ONCE. Either the view model carries focus, scroll and unsubmitted edits, or the surface keeps exactly those three and nothing else. Six ad-hoc rules become one either way.

THE GUARD'S RULE IS BUILDABLE AS STATED: only a module registered in the editor index may emit widget markup.

A DERIVATION INSIDE A REGISTERED MODULE IS STILL INVISIBLE. The guard catches emitters. Nothing yet catches a registered editor that begins to derive, and that is the seam the view model decays through.

machine.sets IS THE REAL LATENCY DEFECT and it belongs to nobody. 1163.6 ms in one phase, on a budget of a thousand.

## anything_else

