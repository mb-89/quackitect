---
form: identify-assumptions
by: agent
signed_off: 2026-08-23T16:54:46.768Z
reopened: "2026-08-23T16:54:31.683Z — two assumptions ride the code this session shipped and the sweep missed both"
authors: agent
files:
---

# Evidence form / identify-assumptions

## current_situation

THE REGISTER GAINED NO ROW, so the sweep runs over the standing rows this round leans on rather than over new ones.

THREE ASSUMPTIONS WERE FOUND. One is the round's own surface question. Two ride the code this session shipped beside the round's four opening pieces.

TWO OF THE THREE CAME FROM A COLD REVIEWER, not from this sweep as first written. It read the shipped code and named both with file and line. The first pass looked only at the four opening pieces, which is exactly the blind spot this state exists to catch.

## assumptions

- raid-the-panel-can-carry-everything-the-mirror-renders
- raid-three-samples-make-a-per-model-correction-trustworthy
- raid-a-hand-with-no-role-recorded-is-a-walker

## sweep

- environment: ONE FOUND. The self-calibrating estimate leans on a history that does not exist yet. Three samples were chosen as the smallest number with a middle value, and nobody measured whether three is enough. Recorded as raid-three-samples-make-a-per-model-correction-trustworthy.
- toolchain: none. No new dependency, no new build step, no new language feature. The collapse removes code rather than adding tooling.
- host: ONE FOUND. The editor panel is a surface inside an editor host, and what it may render is bounded by that host rather than by this project. Recorded as raid-the-panel-can-carry-everything-the-mirror-renders.
- platform: none NEW. The archived read from a branch leans on git behaving the same on both platform families, and that already stands as a risk with its own trigger. Re-logging it here would put one claim in two places.
- neighbours: ONE FOUND, and it is an internal neighbour rather than a third party. The ceiling check and the registration call must agree about what a roleless hand is, and neither checked the other. Recorded as raid-a-hand-with-no-role-recorded-is-a-walker.
- people: none. The one candidate — that the panel is the surface people read and the HTML mirror is not — stopped being an assumption on 2026-08-23, when the owner ruled it. A ruling is a decision, and a decision is owned rather than assumed.

## follow_up

PROBING IS THE NEXT STATE'S WORK, and it probes every standing assumption rather than only these three.

THE PANEL PROBE IS CHEAP AND ALREADY RUN. Both surfaces are assembled in one file, so it was a reading exercise. It did not pass, and six decisions are now owed before the mirror is deleted.

THE CALIBRATION PROBE WAITS ON DATA. The store was created today and holds too few rows to answer. Nothing has to be built for it; the walk fills it by running.

THE ROLE-DEFAULT PROBE IS SECONDS OF READING and has not been run.

## anything_else

