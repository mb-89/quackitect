---
id: adr-views-engine
decided_in: i0016_structural_models
type: adr
adjudicated_by: user
statement: Views chosen for the engine (i16 dogfood, owner rulings 2026-07-09/10) - model-engine-layers as the onion layer map, plus two structural models: model-check-states (state, the check lifecycle), model-product-tree (element-tree, "flow doesn't catch all" - composition of the shipped product); sequence rejected for now (the i15 ask loop carries its diagram in the pairing guide); context derives as always.
class: review
killer: false
---
## Rationale (not load-bearing)
The b9 discussion settled the onion physics: rank = abstraction gradient (rim mechanical, kernel = core algorithms); transforms live in code-owning seam bands; identity transit legal; signals originate at the rim; a-to-b notation, never input/output; ambient band for meaning-free utilities. Granularity ruling: ELEMENTS ARE DESIGN REGIONS, FILES ARE THEMES (the Schlauch, derived from co-location) - both industry camps at once: layer purity where the checks bind, feature cohesion where the reader lives. Unmarked local helpers are architecturally invisible (Bass's line). The earlier two-model proposal is superseded by the owner's third-model ruling; element-tree UNREJECTED accordingly. Deferred (owner): rework of the region system to point implementations at model elements; per-language applicability caveat (Go works today; other languages need their own guidance or an honest unchecked standing).
