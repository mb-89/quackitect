---
id: adr-handoff-html
type: adr
decided_in: i0021_field_ux
adjudicated_by: user
statement: The adjudication hand-off is ONE browsable HTML page per bless moment: any gate - milestone, killer, combined - renders its cone as collapsed color-coded rows (the prefilled nodes under adjudication), expandable to full field and provenance detail, with the y/n bless action ON the page riding the existing ask path (actor and channel recorded); the SAME page travels to the phone as the ask's link. There is NO standing register: provenance data lives in the nodes and colors render only at hand-off moments. Supersedes adr-register-in-report (the report-section placement the owner rejected in the first visual round); the prose-plus-ASCII-card hand-off retires to a one-line pointer. Datum: the report-embedded register. It loses on the owner's ruling: adjudication is a MOMENT, not a dashboard - a standing register is noise the report never needed. Reverse-sensitivity: if hand-off pages prove too ephemeral for audits, the ledger already holds every record - a rendered history view can derive later without a standing surface.
class: review
killer: false
kind: architecture
provenance:
  class: schema-default (review)
  killer: schema-default (false)
  kind: agent-proposal: first of architecture|project|waiver|quality|risk - veto or confirm
---
## Rationale (not load-bearing)
TODO
