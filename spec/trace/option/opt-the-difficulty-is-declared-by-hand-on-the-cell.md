---
minted_in: i38-the-machine-sizes-its-own-driver-every-s
id: opt-the-difficulty-is-declared-by-hand-on-the-cell
type: "[[option]]"
cluster: the-sizing
question: how a step's difficulty is arrived at
statement: a person writes the difficulty into the matrix cell beside the rigor word, and nothing derives, computes or observes it
found_by: without
source: the seed's own proposal, given a node so the chart can hold it as a value rather than as an absence
---

## The found_by on this node is not earned, and a claim was built on it

FLAGGED AT gate-candidates BY A HOSTILE PASS, 2026-08-20. This node carries
`found_by: without`. find_without's own form lists two options and neither is on
this axis, so the trimming finder never reached it. This node was minted at
build_chart, by the state drawing the chart, and the field was stamped with the
nearest of seven legal values rather than left honest.

WHAT WAS BUILT ON IT. build_chart's dropped_finders says the axis `how a step's
difficulty is arrived at` was "reached from three different finders each — from
trimming, prior art and transformation". Two finders reached it. The third is
this node, and the chart counted its own mint as an independent arrival.

WHY THE FIELD CANNOT SIMPLY BE CORRECTED. `found_by` is checked against a closed
list of seven finders and there is no value for "minted at build_chart because the
incumbent had no node". The honest repairs are both upstream: add this option to
find_without's own list, which makes the attribution true, or strike the
three-finder claim in build_chart. Neither is reachable from where this was found
— see the field report's F29.

THE NODE ITSELF IS SOUND. What it says about the incumbent stands; what is wrong
is the provenance stamped on it and the convergence counted from that stamp.

## Mechanism

THIS IS WHAT THE SEED ASKS FOR AND IT HAD NO NODE. Every other candidate on the
chart is a way of not doing this, and a chart whose incumbent is missing scores
the alternatives against nothing. Minted here at build_chart so the baseline is a
cell somebody can pick.

WHAT IT IS. A third key on the matrix cell, filled by hand, changed by hand,
argued about by hand. Fifty-three rows times four change sizes is two hundred and
twelve values, and the omitted cells cut that to the hundred and fifty-four the
compile actually reaches.

WHY IT IS NOT OBVIOUSLY WRONG. A declaration is the only source that can express
a judgement nobody has found a formula for — a state that asks one field and
needs deep original thought reads as easy under every derivation on the chart,
and a person writing the number can simply say it is hard. Every derived
alternative trades that away for something else.

AND IT IS THE ONLY ONE THAT WORKS ON DAY ONE. No formula to design, no history to
accumulate, no experiment to run. The number is there because somebody typed it.

## What it costs

IT DRIFTS AND NOTHING SAYS SO. raid-risk-a-hand-declared-rung-drifts-upward-and-nothing-ever-says-so
is written about this option specifically: raising a declaration is cheap and safe
and nothing contradicts it, lowering one needs an argument, so the ratchet only
turns one way. Probe 3 measured that ratchet running in this very record —
seventeen forms repaired, seventeen grew, none shrank.

AND THE SCHEDULER LITERATURE IS ABOUT THIS EXACT FAILURE. The kickoff scan found
sixty-nine percent of requested CPU unused across more than twenty-three thousand
production clusters, with the cause given as declarations nobody updates.
