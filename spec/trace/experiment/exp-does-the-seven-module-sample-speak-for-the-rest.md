---
minted_in: i54-everything-exported-has-a-door-a-sweep-o
id: exp-does-the-seven-module-sample-speak-for-the-rest
type: "[[experiment]]"
statement: Does the improve-against-lengthen split found in the seven heaviest modules carry to the rest of engine core, measured by applying one rule to both sides?
probes:
  - raid-asm-the-seven-heaviest-modules-speak-for-the-other-fifty-three
timebox: one script over the remaining modules
form: script
chunk: none — both sides were counted whole, 64 sites inside the seven and 59 outside
faked: the SORTING RULE. The original improve-against-lengthen judgment was made per site by a person and could not be reconstructed, so a mechanical proxy stood in. The proxy reproduces two of the seven modules exactly and diverges hard on a third.
fallback: pre-agreed at seeding. If the rest of engine core looks more like run.ts, the door's reach is smaller than the sample suggests and the scope set from it is cut.
verdict: unsettled
measured: 2026-08-26. On the proxy the sample carries — 80 percent improve inside the seven against 76 percent outside. On the shape measure it does not — read-modify-writes are 33 percent inside and 19 percent outside, about 1.7 times denser. The two measures disagree, so the answer is unsettled rather than yes.
folds_to: The assumption moves from open to probed and splits. The door's reach may be set from the sample, which agrees within four points. The claim about which object pays may not, because the read-modify-write shape is 1.7 times denser inside the seven. The other side is 59 sites in 22 files, not 53.
promote: none - the finding is the product
source_refs:
  - rank-unknowns, the seeded pick
  - raid-asm-a-door-in-front-of-the-engine-s-own-disk-access-pays-for-itself — the sample this one questions
---

## Setup

### First, check the seven really are the heaviest

Verified rather than assumed. Write-site counts: `session.ts` 11, `iterations.ts` 10, `run.ts` 10, `sessionclaims.ts` 10, `benchmark.ts` 9, `produce.ts` 8, `sessionforms.ts` 6.

That is 64 sites. The next file down is `bound.ts` at 4, so the seven are genuinely the top seven.

### The other side is 59 sites in 22 files

The original claim said 53. The corrected engine-core total of 123 sites in 29 files puts the remainder at 59.

The node's own name still carries 53, because the id is what other nodes cite.

### The sorting rule could not be reconstructed

The original 42-to-22 split was a judgment made per site by a person. Nothing recorded the rule it applied.

A mechanical proxy stood in, and IT IS MARKED AS A PROXY. A site is lengthen-only if it appends to a log, or if its target path comes from a helper defined in the same module or imported from `paths.ts`, and it is not a read-modify-write.

The proxy reproduces `run.ts` at 0 of 10 and `sessionclaims.ts` at 10 of 0 exactly. It diverges hard on `produce.ts`, giving 8 to 0 against the original 3 to 5.

So it is NOT the same rule. Only its like-for-like comparison across both sides means anything, and that is the only way it is used here.

## Result

UNSETTLED. THE TWO MEASURES DISAGREE.

### On the proxy, the sample carries

- the seven: 51 improve, 13 lengthen, 80 percent improve
- the other 22 files: 45 improve, 14 lengthen, 76 percent improve

Four points apart is not a sample that misleads.

### On the shape measure, it does not

Read-modify-writes are 33 percent of sites inside the seven, at 21 of 64. Outside they are 19 percent, at 11 of 59.

Adding the preceding `mkdirSync` gives 25 of 64 inside against 6 of 59 outside.

The shape the original probe named as the payoff is about 1.7 times denser in the sample than in the rest of the engine.

### Why that is a real disagreement rather than noise

The bias the assumption predicted is exactly this one. A module carries many writes partly because it REPEATS a shape, so the heaviest modules are the ones most likely to over-represent whatever shape they repeat.

The proxy does not see that, because it sorts on where a path comes from rather than on what the write does.

### What this means for scope

THE DOOR'S REACH MAY BE SET FROM THE SAMPLE. On the measure the assumption actually names, the two sides agree.

THE CLAIM ABOUT WHICH OBJECT PAYS MAY NOT. That rests on the read-modify-write shape, and the shape does not carry.

### The raw counts, as the sanctioned fallback

The other 22 files, by write-site count: `bound.ts` 4, `decisions.ts` 4, `discipline.ts` 4, `promptlayer.ts` 4, `tools-run.ts` 4, `vault.ts` 4, `vscoderegistry.ts` 4, `bases.ts` 3, `calllog.ts` 3, `compaction.ts` 3, `inbox.ts` 3, `records.ts` 3, `brand.ts` 2, `files.ts` 2, `help.ts` 2, `lifecycle.ts` 2, `move.ts` 2, `shoot.ts` 2, `notes.ts` 1, `pool.ts` 1, `sessionreads.ts` 1, `tables.ts` 1.
