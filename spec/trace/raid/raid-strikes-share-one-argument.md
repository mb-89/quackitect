---
minted_in: i1
id: raid-strikes-share-one-argument
type: "[[raid]]"
kind: risk
statement: All 37 struck axes rest on one argument — no candidate choice touches the mechanism serving them — so if that argument fails, all 37 return together.
owner: the driving agent
trigger: any candidate added or changed that touches a struck mechanism
status: open
breaks_how_badly: crippling
how_likely: plausible
impact: The returned axes rejoin the sort, the line is re-cut, and the ranking can move. The failure is collective by construction, never one axis at a time.
source_refs:
  - iterations/i1 evidence gate-candidates, reasons_hold
  - iterations/i1 evidence cut-criteria
---

One judgment, applied 37 times. Each strike says the five candidates meet
the requirement identically because none of the five choices reaches the
part of the system serving it.

Three strikes were spot-checked at the source and held. The thinnest case
is the bootstrap cluster: req-engine-port-fallback was struck as plumbing,
and a one-command install could plausibly own the port question.

The trigger above is the live part: a new or changed candidate that reaches
a struck mechanism ends the identical-by-construction equality for every
axis that mechanism serves.
