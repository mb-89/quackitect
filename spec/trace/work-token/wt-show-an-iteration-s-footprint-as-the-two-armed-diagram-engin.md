---
id: wt-show-an-iteration-s-footprint-as-the-two-armed-diagram-engin
type: "[[work]]"
statement: "Show an iteration's footprint as the two-armed diagram engineers outside this project already read without a glossary. One arm goes down into detail, the other climbs back up through checking. Only what the iteration touched is drawn, so the picture stays legible. The larger promises show as smaller shapes nested inside the iteration's own outline. First thing to settle: whether the drawing shows only links the corpus already holds, or needs new ones written. The owner parked the rest."
ready_when: ready when an iteration opens on the trace graph or the board views
source: note-514f5f26b7d9
---

## Why it stands

Show an iteration's footprint as the two-armed diagram engineers outside this project already read without a glossary. One arm goes down into detail, the other climbs back up through checking. Only what the iteration touched is drawn, so the picture stays legible. The larger promises show as smaller shapes nested inside the iteration's own outline. First thing to settle: whether the drawing shows only links the corpus already holds, or needs new ones written. The owner parked the rest.

## When it comes back

ready when an iteration opens on the trace graph or the board views
