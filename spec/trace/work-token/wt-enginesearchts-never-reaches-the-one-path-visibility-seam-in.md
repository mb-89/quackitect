---
id: wt-enginesearchts-never-reaches-the-one-path-visibility-seam-in
type: "[[work]]"
statement: engine/search.ts never reaches the one path-visibility seam in engine/paths.ts, so the verb most likely to surface a hidden file is the verb no per-path rule binds. i37 hung the benchmark concealment off search.ts's own filter as a result. This SUPERSEDES wt-three-separate-lists-decide-which-paths-a-lane-verb-may-see-, which counts the lists instead of naming the file; the pool has no edit door, so the sharper version arrives as a second token.
ready_when: ready when a hiding rule has to bind the search verb, or when the exclusion lists are next touched
source: note-32a76746e2fb
---

## Why it stands

engine/search.ts never reaches the one path-visibility seam in engine/paths.ts, so the verb most likely to surface a hidden file is the verb no per-path rule binds. i37 hung the benchmark concealment off search.ts's own filter as a result. This SUPERSEDES wt-three-separate-lists-decide-which-paths-a-lane-verb-may-see-, which counts the lists instead of naming the file; the pool has no edit door, so the sharper version arrives as a second token.

## When it comes back

ready when a hiding rule has to bind the search verb, or when the exclusion lists are next touched
