---
id: wt-half-of-this-is-already-answered-and-half-is-unstarted-the-o
type: "[[work]]"
statement: "Half of this is already answered and half is unstarted. The owner asked whether the shell should be stopped from running checks behind the lane's back: it already is, at a threshold of zero, with the hatch explicitly closed for that one rule. THE JUDGING HALF IS THE REAL WORK. Nobody has counted the session's check calls against what actually changed, or read the log to see whether the scope rule flipped everything to the whole battery. That measurement can run before anything is rebuilt."
ready_when: "ready when a reviewer is spawned to judge the session's check calls from the call log — the blocking half needs no work at all and should be struck when the token is opened"
source: "note-4aaf0e4f284b"
---

## Why it stands

Half of this is already answered and half is unstarted. The owner asked whether the shell should be stopped from running checks behind the lane's back: it already is, at a threshold of zero, with the hatch explicitly closed for that one rule. THE JUDGING HALF IS THE REAL WORK. Nobody has counted the session's check calls against what actually changed, or read the log to see whether the scope rule flipped everything to the whole battery. That measurement can run before anything is rebuilt.

## When it comes back

ready when a reviewer is spawned to judge the session's check calls from the call log — the blocking half needs no work at all and should be struck when the token is opened
