---
id: wt-make-iteration-activation-publish-an-engine-managed-ownershi
type: "[[work]]"
statement: Make iteration activation publish an engine-managed ownership marker, so a later session can detect active work before binding.
ready_when: ready when iteration admission or concurrent-worker coordination is changed
source: note-90c25bbfdea7
---

## Why it stands

Make iteration activation publish an engine-managed ownership marker, so a later session can detect active work before binding.

## When it comes back

ready when iteration admission or concurrent-worker coordination is changed
