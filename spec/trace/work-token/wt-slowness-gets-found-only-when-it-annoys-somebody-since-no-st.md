---
id: wt-slowness-gets-found-only-when-it-annoys-somebody-since-no-st
type: "[[work]]"
statement: "Slowness gets found only when it annoys somebody, since no step is charged with hunting for it. A standing measurement would replace that. Numbers taken on 2026-08-25 give the next look a floor to compare against. Starting one checking session costs 536 milliseconds by itself: roughly 120 to lay the product tree down, roughly 330 to take in the guidance corpus, and no model anywhere in it. That same start costs 1453 milliseconds with twelve running at once, and 4013 with forty-two, which is seven and a half times worse at the width really in use. Fewer starts is the lever, not a quicker start. One question is left over: total effort dropped by a minute and wall clock hardly shifted, because a single file finishes last and everything else waits behind it."
ready_when: "ready when a round opens on how the system watches its own speed, which the owner placed in a sweep rather than a look-back"
source: "note-a794dc827cac"
---

## Why it stands

Slowness gets found only when it annoys somebody, since no step is charged with hunting for it. A standing measurement would replace that. Numbers taken on 2026-08-25 give the next look a floor to compare against. Starting one checking session costs 536 milliseconds by itself: roughly 120 to lay the product tree down, roughly 330 to take in the guidance corpus, and no model anywhere in it. That same start costs 1453 milliseconds with twelve running at once, and 4013 with forty-two, which is seven and a half times worse at the width really in use. Fewer starts is the lever, not a quicker start. One question is left over: total effort dropped by a minute and wall clock hardly shifted, because a single file finishes last and everything else waits behind it.

## When it comes back

ready when a round opens on how the system watches its own speed, which the owner placed in a sweep rather than a look-back
