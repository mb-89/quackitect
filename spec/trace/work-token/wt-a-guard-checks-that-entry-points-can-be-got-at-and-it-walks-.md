---
id: wt-a-guard-checks-that-entry-points-can-be-got-at-and-it-walks-
type: "[[work]]"
statement: "A guard checks that entry points can be got at, and it walks a hand-written list holding two. Everything else exported goes unchecked, including two working pieces of code that turned out to have no door at all. Wanted is a sweep over every exported entry point rather than a literal pair."
ready_when: "ready when the iteration that widens the dead-code sweep is opened"
source: "note-d0884030dc6c"
---

## Why it stands

A guard checks that entry points can be got at, and it walks a hand-written list holding two. Everything else exported goes unchecked, including two working pieces of code that turned out to have no door at all. Wanted is a sweep over every exported entry point rather than a literal pair.

## When it comes back

ready when the iteration that widens the dead-code sweep is opened
