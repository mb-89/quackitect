---
id: wt-the-fix-everybody-names-is-already-built-and-naming-it-again
type: "[[work]]"
statement: "The fix everybody names is already built, and naming it again buys nothing. Passing the corpus down instead of re-fetching it was done: the whole route runs inside one pass, with the measurement recorded beside it, and the drawing is held across hops. So the cost that remains is somewhere else and nobody has looked. THE REAL QUESTION IS WHAT ONE ALREADY-FINISHED HOP SPENDS ITS TIME ON, and it needs a profile rather than another proposal."
ready_when: "ready when somebody profiles the inside of a hop that is already finished, since the proposed fix turned out to be built and the real question was never asked"
source: "note-13326ca46434"
---

## Why it stands

The fix everybody names is already built, and naming it again buys nothing. Passing the corpus down instead of re-fetching it was done: the whole route runs inside one pass, with the measurement recorded beside it, and the drawing is held across hops. So the cost that remains is somewhere else and nobody has looked. THE REAL QUESTION IS WHAT ONE ALREADY-FINISHED HOP SPENDS ITS TIME ON, and it needs a profile rather than another proposal.

## When it comes back

ready when somebody profiles the inside of a hop that is already finished, since the proposed fix turned out to be built and the real question was never asked
