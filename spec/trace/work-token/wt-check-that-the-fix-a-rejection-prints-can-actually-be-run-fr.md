---
id: wt-check-that-the-fix-a-rejection-prints-can-actually-be-run-fr
type: "[[work]]"
statement: Check that the fix a rejection prints can actually be run from where it fired. Every rejection carries an exact call to make instead, and nothing verifies that call is permitted in the state that raised it. A reader who follows one and hits a second wall has been sent in a circle by the machine itself.
place: i39-the-lane-tells-the-truth-about-itself-de
ready_when: ready when somebody builds a check that reads each printed next-call against the state map
source: note-379f2dcc9816
---

## Why it stands

Check that the fix a rejection prints can actually be run from where it fired. Every rejection carries an exact call to make instead, and nothing verifies that call is permitted in the state that raised it. A reader who follows one and hits a second wall has been sent in a circle by the machine itself.

## When it comes back

ready when somebody builds a check that reads each printed next-call against the state map
