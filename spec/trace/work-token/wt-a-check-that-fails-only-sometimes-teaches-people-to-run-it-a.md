---
id: wt-a-check-that-fails-only-sometimes-teaches-people-to-run-it-a
type: "[[work]]"
statement: "A check that fails only sometimes teaches people to run it again, which is the opposite of what a check is for. One case in the turn-ending guard's suite exits non-zero having written nothing at all to either output stream, and only while dozens of other files run beside it. Run by itself it succeeded twenty times out of twenty. That named case no longer launches a separate process, so its symptom cannot recur there, but three sibling cases still launch one and the cause was never established. Find out why starting a TypeScript entry point repeatedly under heavy parallelism produces a silent non-zero exit."
ready_when: "ready when somebody hunts the silent non-zero exit, or when the checking suite next goes red without a reason"
source: "note-d5b89a09aa56"
---

## Why it stands

A check that fails only sometimes teaches people to run it again, which is the opposite of what a check is for. One case in the turn-ending guard's suite exits non-zero having written nothing at all to either output stream, and only while dozens of other files run beside it. Run by itself it succeeded twenty times out of twenty. That named case no longer launches a separate process, so its symptom cannot recur there, but three sibling cases still launch one and the cause was never established. Find out why starting a TypeScript entry point repeatedly under heavy parallelism produces a silent non-zero exit.

## When it comes back

ready when somebody hunts the silent non-zero exit, or when the checking suite next goes red without a reason
