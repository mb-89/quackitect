# Success criteria - each need carries its own pass lines (folded in from the retired criterion
# nodes, owner ruling 2026-07-08). The view renders every need with its Success criteria section.
filters:
  and:
    - 'type == "need"'
views:
  - type: table
    name: Success criteria
    render: full
