filters:
  and:
    - 'type == "stakeholder"'
views:
  - type: table
    name: Stakeholders
    order: [file.name, role, statement, preset, guide, weight]
    sort:
      - property: role
        direction: ASC
