filters:
  and:
    - 'type == "requirement"'
views:
  - type: table
    name: Requirements register
    order: [name, statement, kind]
    sort:
      - property: weight
        direction: DESC
    groupBy: kind
