filters:
  and:
    - 'type == "raid"'
views:
  - type: table
    name: RAID register
    order: [name, kind, statement, probability, impact, status]
    sort:
      - property: kind
        direction: ASC
