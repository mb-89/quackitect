---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: claim rewrite drops notes
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-relay-trial
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 8d9a5e420da1f5c93e2cb9ef24c89291bdd643f5
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - ba912061f9f4220f1badb84b0d15d0af389a41e3
# how it ended. Only an ended token carries one.
disposition: dropped
# why it was dropped
reason: The same fault was minted twice. wk-98739fa19c asked it first, in stricter terms, and carries the fix and its test.
---

## detail

FOUND IN THE VERDICT ON wk-c698d46866, landed as 8b224a63.

Publish's recovery in src/engine/claim.go moves refs/se/claims to the remote's head with update-ref and then calls writeTheClaims, which read-trees that head and adds only the paths in `files`. Its comment says "Moving the local ref loses nothing: what it held is these same files". That holds only when the local ref is ahead by this claim alone.

A box whose push fails keeps its claim on the local ref and nowhere else. That is the state this verdict was given from: the cloud proxy answers HTTP 403 on every push. Claim A lands on the ref unpushed. The next claim, B, pushes, fails, throws the ref back to the remote's head and rewrites with B only. A is gone from the ref, and when a push finally succeeds only B is published.

The damage is what claims exist to stop: this box holds A, no other box can see it, and the queue goes on offering A. The old code kept A, because the fetch failed and it returned early.

## proposed action

Carry the local ref's own notes over the remote's head rather than dropping them: read-tree the remote head, then re-add the note paths the local ref held that the remote lacks, before writing the claim again. Or keep the local head as parent and hand writeTheClaims every unpushed note path. Either way the comment that says nothing is lost stops being wrong.

src/engine/claim.go was under another session's hand when this was minted (wk-4759d90994, claims ride a branch), so read what that landed first.

## done when

- a Publish whose local ref holds an earlier unpushed claim note keeps that note after the rewrite, decided by: a test in src/engine that publishes note A with the push failing, then publishes note B, and asserts the tree written the second time carries both, answers ok
- sh util/checks/battery.sh reports no new failure against the run before the change

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | it is, and it was already asked once as wk-98739fa19c | wk-98739fa19c |
| [x] | every done-when line is decidable, and names the command where one decides it | both were, and the older token's three are stricter | wk-98739fa19c |
| [x] | the basics it stands on exist, or are minted first | wk-4759d90994 landed first, as this asked | wk-4759d90994 |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | read, and it says a repetition is a defect, so this is dropped rather than worked twice | work-token |
| [x] | one test was written first and seen red for the reason expected | on wk-98739fa19c: claim A absent from the local ref and the remote | 15:20 run |
| [x] | the same test was seen green after the change, and named | TestPublishKeepsEarlierClaimsWhenThePushKeepsFailing | 95c84863 |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | no change is this token's. The fix landed on the older one | 95c84863 |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | this token is the duplicate, and dropping it is the cleanup | wk-98739fa19c |

