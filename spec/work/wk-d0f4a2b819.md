---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: guidance keeps human rules
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: voice
# tokens that have to close before this can start
depends_on:
  - "[[wk-c1d8306e57]]"
  - "[[wk-f2b58d1c04]]"
# what has to be true before this is worth reading again
ready_when: nothing. The owner ruled in September 2026 that every mechanical rule leaves the guidance, with no exceptions, and that something mechanical is built for each.
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 757e6a32baa6eaf06372e72b12a5af5c7f917575
---

## detail

The guidance holds 161 actionables across 13 files.
`engine_design_principles` rule 2 says a rule a program can check is a check.

Classified here, roughly: 25 are already a refusal the engine makes, 62 more are findable by a lint or the language server, and 4 are a fix a formatter makes.
So about 90 of 161 are mechanical, and about 65 of those are written as guidance while a program could hold them.

An agent reading 161 rules gives each less attention than it would give 70.
A rule the engine already refuses teaches nothing by also being written down.

## approach

One pass over the 13 files, rule by rule.
A rule a program holds becomes a link to the check, the schema or the config that holds it.
A rule needing a person stays, in its own words.

`spec/guidance/driving-the-engine.md` goes first: 12 of its 15 name a refusal the engine already makes.

The classification is written down as it is made, so a later reader can disagree with a call rather than redo it.
The count is what the retro reads to see whether the guidance is shrinking.

## done when

- each of the 161 actionables carries a verdict of gate, fix, lint or person, decided by reading the classification written on this token
- every rule marked gate, fix or lint names the thing that holds it, decided by the same reading
- no rule marked gate is still written as an instruction, decided by reading the guidance files
- the projection carries fewer rules and the same meaning, decided by counting the actionables before and after

