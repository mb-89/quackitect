---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: criteria miss the change
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: checks
---

## detail

Found reviewing wk-75c9fd102b, whose one production hunk is decided by none of its three done-when lines.

The token asked whether take writes the claim it says it writes. It does, and the loss was elsewhere: the walk over what an actor already holds, at src/engine/pull.go:1101, asked Blocked and WaitsForAPerson and not the claim. The hunk adds that question, and it is right.

Measured on 2026-09-06 by putting the old whyNotNow back and running all three tests. TestTheHandOutWritesTheClaimItSaysItWrites and TestAStandingClaimIsNotTakenByTheQueue stayed green, because they drive take, which claimed already. Only TestAHeldTokenUnderAnotherBoxsClaimGoesBack failed: "the queue handed wk-9680b33b22 back while another box's claim stands on it". No done-when line names that test.

The branch head says the same thing without any mutation. Commit 43cdb96 carries two of the three tests and not the hunk, and it is green. The hunk and its test came later, at 4bc104d.

So a reviewer who ran the three criteria and stopped would pass this token whether or not the change is in the tree, and the criteria cannot tell a reader what the token changed.

Nothing catches the class. No check reads a token's span for tests its criteria do not name.

## proposed action

A test a token adds is named by one of its done-when lines, and a check says so.

## done when

- doc/work/wk-75c9fd102b.md names the test that decides its change. Decided by: se find --regex TestAHeldTokenUnderAnotherBoxsClaimGoesBack --path 'doc/work/*.md', which answers 0 today and must answer 1
- a check names every Go test added between a token's began and ended that no done-when line mentions. Decided by running that check over the tree, where it names this token today
- the check is quiet where the criteria do name their tests. Decided by running it over a token whose done-when lines name each test it added, where it exits zero

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

