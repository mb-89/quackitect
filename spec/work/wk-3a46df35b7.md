---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: three files keep arguments
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-fir
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 2e88d38bbb221b332215655bd7f4aaf311238142
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - ac438c53cd1705a9fb50ee795552af4c0c018057
# how it ended. Only an ended token carries one.
disposition: done
# what it became. They have to exist.
successors:
  - "[[wk-2d6e10c446]]"
  - "[[wk-a53e262ad6]]"
---

## detail

From the verdict on wk-5b51fc14e4, rationales get a kind.

That token's detail names the harm: schema.go, gate.go and store.go carry paragraph-long ALL-CAPS arguments about why a thing is the way it is, and so do the MCP tool descriptions. It built the cure. There is a rationale kind with a schema, a folder, a template, and TestARationaleIsNamedNotRepeated driving the link both ways.

Only the descriptions were treated. doc/rationale holds two notes, a-description-is-an-instruction explaining src/mcp/lane.go and a-field-is-read-or-gone explaining src/schemas/work-token.schema.yaml. None of the three Go files the detail names was touched. src/engine/schema.go carries 6 such openers, src/engine/gate.go 34 and src/engine/store.go 18, and none of the three names a rationale by link.

The token's done-when lines never asked for them, so it closed green with its own motivation standing. Nothing goes red for it either: the test walks the notes already under doc/rationale, so a file that never got a rationale is a file it never looks at.

Wanted is the extraction the detail asked for, one file at a time, and each argument moved rather than copied.

## proposed action

Take src/engine/gate.go first, since it carries the most. Move each ALL-CAPS argument into a rationale under doc/rationale, listing gate.go under explains, and leave the instruction in the comment naming the note by link. TestARationaleIsNamedNotRepeated then covers it without a line of new test. Then schema.go and store.go the same way, each its own token if this grows.

## done when

- src/engine/gate.go names a rationale by link and carries none of its sentences, decided by: go test -C src/engine -run TestARationaleIsNamedNotRepeated ./... answers ok with gate.go among the files it drives
- the instruction left in gate.go still says what to do, decided by: a reader of each touched comment can act on it without opening the note
- the engine still builds and its gate tests pass, decided by: go build -C src/engine ./... answers nothing and go test -C src/engine -run 'Guard|Refus' ./... answers ok

## evidence: note

gate.go opened with 34 paragraph-long capitalised arguments and named no rationale, so the test that keeps the two halves joined never looked at the file. The write gate decision now lives in doc/rationale/no-token-no-writing.md: the owner's rule, the arming call that failed, why a shell is refused with the rest, and why reads stay open. The three comments it came out of keep what to do and name the note by link. Thirty openers are left in gate.go, and a rationale allows 250 words of argument, so one note cannot hold them. They are wk-2d6e10c446. schema.go and store.go, which this token's detail names and its criteria never covered, are wk-a53e262ad6.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one rationale and one file. The rest of gate.go is wk-2d6e10c446, and the other two files are wk-a53e262ad6 |  |
| [x] | every done-when line is decidable, and names the command where one decides it | one Go test, one reading of the comments left behind, and go build |  |
| [x] | the basics it stands on exist, or are minted first | the rationale kind, its schema, its folder and its test all ship already |  |

## evidence: step 2. do

doc/rationale/no-token-no-writing.md was written first, explaining src/engine/gate.go. TestARationaleIsNamedNotRepeated then went red three ways: why ran to 260 words where 250 are allowed, gate.go does not name the note, and gate.go still carries a sentence of it. The note was trimmed and gate.go's three opening comments were rewritten to keep the instruction and name the note. Green after: TestARationaleIsNamedNotRepeated, beside TestEveryWritingToolIsRefusedAndNamesItsVerb, TestARefusalSaysWhatTookTheCommandOutOfTheException, TestRunmeIsTheEngineForAGuardsException, TestEveryDoorARefusalNamesGetsPastTheGuard, TestAStopClaimPassesEveryGuard and four more guard tests. go build -C src/engine ./... answered nothing, and gofmt names gate.go nowhere. The whole battery stays owed.

