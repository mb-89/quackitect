---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: gate keeps 30 arguments
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-fir
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 0401e7d601a9a49dd3e70608b1a41ccad55ede28
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - b112929e814bc1e882f0777deede18b80da6fca2
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

From wk-3a46df35b7, which moved the first of gate.go's arguments out.

src/engine/gate.go opened 34 paragraph-long capitalised arguments. The write gate decision is now doc/rationale/no-token-no-writing.md, and gate.go names it by link and carries none of its sentences.

Thirty openers are left. They are separate decisions: the todo list as a sub-token, the names an actor answers to, a slot handed on between sessions, what makes a session live, and the shape of a refusal.

A rationale allows 250 words in its why chapter, so one note cannot hold thirty arguments. Each cluster is a note of its own, and the comment left behind keeps the instruction and names the note.

## proposed action

Take the clusters one at a time. Write a rationale under doc/rationale listing src/engine/gate.go under explains, move the argument into it, and leave the instruction naming the note by link. TestARationaleIsNamedNotRepeated covers each without new test code.

## done when

- no comment in src/engine/gate.go carries an argument that is not in a rationale, decided by: reading each capitalised opener left in the file
- every rationale it names is reachable and unrepeated, decided by: go test -C src/engine -run TestARationaleIsNamedNotRepeated ./... answers ok
- the engine still builds and its gate tests pass, decided by: go build -C src/engine ./... answers nothing and the guard tests answer ok

## evidence: note

gate.go's remaining arguments are now six notes under doc/rationale, each one decision: thinking-is-not-a-change for the scratchpad carve-out, a-refusal-is-a-menu for what a refusal owes the agent, a-todo-is-a-sub-token for the harness list, a-take-up-is-a-swap for the hold and the claim, an-actor-answers-to-two-names for the harness name beside the pulled-with name, an-actor-is-a-session for the name another session holds, and the-engine-is-the-exception for the command that runs the engine. Every comment they came out of keeps what to do and names its note by link. The refusal strings the agent reads were left alone: they are the product, not a comment. Nothing about schema.go or store.go was touched, and they are wk-a53e262ad6.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one file of comments and seven notes, each a decision on its own |  |
| [x] | every done-when line is decidable, and names the command where one decides it | one reading of the openers left, one Go test, and go build |  |
| [x] | the basics it stands on exist, or are minted first | the rationale kind and its test ship, and no-token-no-writing is the shape they follow |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | each argument moved rather than being copied, and the comment left behind keeps what to do |  |
| [x] | one test was written first and seen red for the reason expected | with the notes written and gate.go untouched, TestARationaleIsNamedNotRepeated named four sentences still carried in the file |  |
| [x] | the same test was seen green after the change, and named | TestARationaleIsNamedNotRepeated, with seven guard tests and both refusal checks beside it |  |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | src/engine/gate.go and six new notes under doc/rationale |  |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | schema.go and store.go are wk-a53e262ad6, which was minted before this one |  |

