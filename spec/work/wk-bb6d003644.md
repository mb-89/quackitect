---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: format reaches other languages
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: cage
---

## detail

The format verb landed on wk-8ec7483730 reaching Go alone. The approach on that token says format reaches everything it can, which is Go, TypeScript, shell and prose, and lint reaches the same. The four done-when lines named Go and golangci-lint only, so the rest was left.

What is gained by doing it is that one door settles the layout of every file the tree carries, so an agent editing the extension or a check script asks the same verb as one editing Go.

What breaks if it is never done is that the guidance says the engine names the programs while the engine names them for one language, and a TypeScript file is formatted by whoever remembers which tool.

The shape is already there. TheGoModules finds the folders, theProgramSaid runs one program over one, and a program the box has not got comes back as a refusal naming it.

## proposed action

Give format a table of languages: the folders each is found in, the program, and its arguments. Go is the row that exists. Add the rows for TypeScript and shell, and let the missing-program refusal carry the rest.

## done when

- se format reaches TypeScript, decided by: run it over a file the formatter would change under src/extension and read the file after
- se format reaches shell and says so where no formatter is on the box, decided by: run it with the formatter hidden and read the refused list
- a language with no formatter on this box is named in refused rather than passed over in silence, decided by: read the answer of one run

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | one door settles the layout of every file the tree carries, not only the Go | |
| [x] | what breaks if it is never done, and not only that it stays undone | the guidance says the engine names the programs while it names them for one language | |
| [x] | the ask is small enough to review whole, or it is split first | one table of languages, and Go is the row that already exists | |
| [x] | every done-when line is decidable, and names the command where one decides it | each line runs the verb and reads either the file after or the refused list | |
| [x] | the basics it stands on exist, or are minted first | TheGoModules, theProgramSaid and the missing-program refusal all landed on wk-8ec7483730 | |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

