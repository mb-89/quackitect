---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: nothing holds the door
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: checks
---

## detail

A finding from the verdict on [[wk-963dbf6898]].

The lane names itself at src/mcp/lane.go:530, as the literal "lane" in a verbCall. The engine reads that at src/engine/pullverb.go:88 and settles without handing work when it is anything else. Everything rests on that one literal.

No test goes red if it is removed. The four tests in src/engine/shellsubmits_test.go build a verbAsk in the engine's own package and set Door themselves. So they hold the engine's half and never the lane's.

The damage is silent. A lane that stops naming itself is read as a shell, so every agent submission is answered settled and handed nothing. The agent then waits for work the queue is willing to give it, and nothing in the record says why.

That is not hypothetical here. The lane process running in this session was started before the change landed, so it sends no door, and every submission this session made came back settled with no next token. The engine was right and the lane was old, which is the same shape a deleted literal would have.

The check to widen is lane-carries-every-flag, which already reads what the door offers against what the call behind it sends.

## proposed action

Hold the lane to naming itself: a check that reads the verbCall the lane builds and fails when the door is absent.

## done when

- removing the door from the lane's verbCall turns a check red, decided by: take it out and run the check
- the check is green as the lane stands, decided by: run it against the tree

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

