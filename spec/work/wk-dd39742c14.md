---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: save moves nothing now
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: archive
---

## detail

A finding on wk-7a9da9eec2, the folder decides.

SaveToken in src/engine/store.go still carries the move it no longer does.

Line 643 reads from := noteAt(r, t.ID). Line 644 reads dir := dirFor(r, t), and dirFor now opens with the same call: if at := noteAt(r, t.ID); at != "" then return filepath.Dir(at). noteAt answers filepath.Join(dir, id+".md"), so whenever from is not empty, dir is filepath.Dir(from) and final at line 649 is from.

So the branch at lines 668 to 671, if from != "" and from != final, then os.Remove(from) and IndexFile(r, from), cannot be entered. The engine no longer moves a token file at all: a move is a hand move now, done outside it.

Worse than the dead lines is the comment above them, at 636 to 642: WHERE IT IS NOW, so a token whose process moved it leaves nothing behind, and A save wrote into the folder the process names. That is the mechanism this token deleted, and the comment on dirFor thirty lines up now says the opposite. Two accounts of what decides the folder, in one file, is the second answer the token was minted to remove.

TestAHandMovedTokenStaysMoved asserts it is in both stores, so one id names two files. That passes because nothing writes the second file, not because a cleanup ran.

## proposed action

Delete the from variable and the branch at 668 to 671. Rewrite the comment at 636 to 642 to say what is true: a token is written back into the folder it is already in, so the engine never moves a token file.

Keep the MEASURED sentence only if it is rewritten as history of a rule that is gone.

## done when

- SaveToken names no from variable: se find --regex "from := noteAt" --path src/engine/store.go answers count 0
- no comment in SaveToken says the process names the folder: se find --regex "the folder the process names" --path src/engine/store.go answers count 0
- a token is still written back where it is: TestAHandMovedTokenStaysMoved is green

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

