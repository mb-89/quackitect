---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: voice rules reach code
status: open
bucket: voice
depends_on:
  - "[[wk-b3d1f07a92]]"
  - "[[wk-f2b58d1c04]]"
---

## detail

`isProse` in `src/engine/hook.go` line 1386 answers true for `.md`, `.markdown` and `.txt`.
Every Go, TypeScript and mjs file is outside the voice check.

A comment is prose and a person reads it.
The tree holds 19528 comment lines in Go and 3107 in mjs.
No check reads one of them.
The engine's own messages to a person are string literals in those files, so they go unchecked too.

Vale lifts comments out of source with tree-sitter, so the reach comes with wk-f2b58d1c04.
What is left here is the engine's own doors, which decide per path what they check.

## done when

- `isProse` answers true for `.go`, `.ts` and `.mjs`, decided by a Go test over it naming each extension
- the write path checks a comment and skips a string literal, decided by a Go test over one Go fixture holding the same sentence in both
- the battery reports no new failure against the run before the change, decided by `util/checks/battery.sh`
