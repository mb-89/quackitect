---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: rules file refuses antithesis
status: open
bucket: voice
depends_on:
  - "[[wk-b3d1f07a92]]"
  - "[[wk-f2b58d1c04]]"
---

## detail

Voice rule 8 asks the writer to say what is.
Nothing checks it, and the tree holds 3679 constructions that say what is not.

Measured over 813666 words, excluding the `doc` mirror:

| phrase | count |
|---|---|
| `rather than` | 1215 |
| `never` | 1114 |
| `and not` | 643 |
| `is not a` | 336 |
| `X, not Y` | 308 |
| `instead of` | 63 |

Each is a line pattern, so each is one `existence` rule in the style folder wk-f2b58d1c04 lands.
The owner names this the worst tic on the surface.
The shape is a first half that says the thing and a second half that says the other thing.

## done when

- the style folder carries a rule for each of the six patterns, decided by reading it
- each rule says what to write instead, decided by reading its `message` field
- a write carrying `rather than` is refused and the refusal names the phrase, decided by `se lint` over a fixture holding it
- the count over the tree falls, decided by running the counter in the evidence before and after
