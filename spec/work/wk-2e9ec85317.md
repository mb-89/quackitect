---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: messages name spec
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: voice
---

## detail

Files across the tree name `src/config`, `src/processes` or `src/schemas`. Those folders moved to `spec` in commit 7a85c71. `se find --regex 'src/(config|processes|schemas)'` answers how many are left.

The engine reads the right place. `SpecAt` in `src/engine/roots.go` prefers `spec` and falls back to `src`, so nothing is broken at run time.

What is stale is the prose. `src/engine/config.go` line 217 answers a person `src/config/parameters.json is not readable`, naming a file that does not exist. `src/engine/icons.go` says the same for icons. The generated control skills each end `Source: src/config/parameters.json`.

So a person told where to look is sent to a folder the tree left behind. Every one of these is a message the agent wrote and nothing checks.

## done when

- no message a person reads names `src/config`, `src/processes` or `src/schemas`, decided by `se find --regex 'src/(config|processes|schemas)'` over the tree
- the generated control skills name the file that holds them, decided by reading one
- a check refuses a message naming a folder the tree does not carry, decided by running it over a fixture holding one
- the battery reports no new failure, decided by `util/checks/battery.sh`

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

