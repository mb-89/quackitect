---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: no view draws parked
# where the token stands. The process owns these values.
status: closed
# who did the work step, so the verdict is never theirs
author: worker-fir
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 458d66d4e2c7da2f0daad33e08e9c5307f926735
  - ca12290031b793edcde040010809852856178213
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - d76b4f5def9195d5f7866b9f037ddd4df27a268f
  - 2d9ff1d4b8abc89aa789939d130b6e71953c1b46
# how it ended. Only an ended token carries one.
disposition: done
---

## detail

From the verdict on wk-5bba8e497a, ready_when leaves the queue.

That token's third done-when line asks that se query and the state of play both name every token carrying a ready_when, with the condition it holds. The state of play does now: TheStateOfPlay builds ParkedOn, one line per token with its id, title and what it waits on, and Screen prints them under the count.

se query does not. src/engine/query.go declares ready_when as a column of the row a view filters and shows, so a view could name it, and no view in the tree does. There is no doc/views folder at all, and nothing else in the tree filters on the field. The author said so plainly on the token and left it for a reviewer, which is why this exists rather than a correction.

A parked token is the one kind that needs a person to come and look. The screen is where an agent looks and the query is where a person looks, so the half that is missing is the person's half.

Wanted is one shipped view that lists parked work with its condition, or a ruling that se query needs none and the done-when line narrowed to the screen.

## proposed action

Ship one view that filters on ready_when being set and shows the id, the title and the condition. Put it where the engine reads views from, and drive it with a test that mints a parked token and finds it in what the view answers. Where the owner would rather se query said nothing about parked work, reword the third done-when line on wk-5bba8e497a to name the screen alone and say why here.

## done when

- a view names every token carrying a ready_when with its condition, decided by: se query over that view answers a parked token's id, title and condition
- the view is driven by a test, decided by: a Go test in src/engine mints a parked token and finds all three in what the view answers
- the engine still builds, decided by: go build -C src/engine ./... answers nothing

## evidence: note

The token asked for a view that lists parked work, or a ruling that se query needs none. The view is already there. util/views/work.base carries the parked group and the ready_when column, and se query over it names each parked token by id, title and condition. So no view was shipped and none was ruled out. What was missing is the test the token asked for: src/engine/parkedview_test.go copies the shipped work.base into a tree, mints a parked token beside an unparked one, renders the view, and reads the id, the title and the condition out of the parked group. A group taken out of the shipped file turns it red. One correction to the detail: views live in util/views, not doc/views, and the third done-when line of wk-5bba8e497a needs no narrowing.

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | one test file, and the view it drives already ships |  |
| [x] | every done-when line is decidable, and names the command where one decides it | se query --view work, one Go test, and go build -C src/engine ./... |  |
| [x] | the basics it stands on exist, or are minted first | the parked group went into util/views/work.base at 6cbecc0a, so only the test was owed |  |

## evidence: step 2. do

The view already ships. util/views/work.base declares a group named parked, filter ready_when != "", and the left table draws a ready_when column. That group went in at commit 6cbecc0a, after this token was written. Live: se query --view work answers the parked group with 6 lines, each carrying id, title and the condition, among them wk-345a943f66 the queue is quiet. What was owed was the test, and it is src/engine/parkedview_test.go. It was seen red against the view as it stood before 6cbecc0a, saying no group of the work view draws parked work. Green after: TestTheWorkViewDrawsWhatIsParked and TestATokenWaitingOnAPersonIsNotHandedOut both ok. go build -C src/engine ./... answered nothing. The whole battery stays owed.

