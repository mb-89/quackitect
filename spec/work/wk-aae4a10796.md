---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: why se run refused
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: queue
---

## detail

wk-3fe6395f91 asks the queue to pass over a token claimed here by another actor. Two things stand against that shape.

NoClaimHere and ClaimedHere both say a claim is the box's and not the agent's. ClaimedHere adds that reading a claim as one agent's would hide work from the box that took it. Main claims a block and workers pull it, so the shape would stop that.

Measured at HEAD. mintStandard claims as main, and 54 Pull sites under src/engine then pull as worker-N or reviewer-N. The command that counts them is `se find --regex 'Pull\(r, "(?:worker|reviewer)' --path 'src/engine/*_test.go'`.

The refusal wk-3fe6395f91 reports names no other actor. se run answered that the token travels and this box holds no claim, which is the branch NoClaimHere takes when ClaimedNow is empty. So the claim was gone at that moment, rather than held under another name.

What is gained is a fix aimed at the defect that happened. What breaks otherwise is a change that hides work from every worker on the box.

## proposed action

Find why ClaimedNow answered empty for a token the note said main had claimed, and fix that. Refuse the pass-over shape on wk-3fe6395f91, or accept it with the fixtures moved first.

## done when

- the token says why ClaimedNow answered empty for that token, and names the door. The reviewer reads the note
- the token accepts or refuses the shape wk-3fe6395f91 proposes, and says why. The reviewer reads the note
- the queue still hands out what main claimed. Decided by `./RUNME.sh test --on <this id> --propose src/engine/TestAnOversizedChapterDoesNotStopTheQueue | jq -e '.ok and ((.unreached // []) | length == 0)'`

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | what is gained by doing it, and not only what it does |  |  |
| [ ] | what breaks if it is never done, and not only that it stays undone |  |  |
| [ ] | the ask is small enough to review whole, or it is split first | — |  |
| [ ] | every done-when line is decidable, and names the command where one decides it |  |  |
| [ ] | the basics it stands on exist, or are minted first | — |  |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [ ] | the guidance this token names was read and applied | — |  |
| [ ] | one test was written first and seen red for the reason expected |  |  |
| [ ] | the same test was seen green after the change, and named |  |  |
| [ ] | the change is git diff began..ended, the two hashes the engine wrote on this token | — |  |
| [ ] | the cleanup the change revealed is in the change, or is a token of its own | — |  |

