---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[standard]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: tooltips name their keywords
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: surface
# tokens that have to close before this can start
depends_on:
  - "[[wk-71ca9e0b46]]"
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 77fd9d3bf03afc92a8b381e1ede4252a76531c20
---

## detail

A parameter reachable by keyword has to say so where a person reads about it, which is the tooltip.

Writing the keyword into each title by hand would put one fact in two places. The tooltip would then drift from the command it names, which is the defect the lane already carries and has a token of its own.

Some controls make no sense away from a desk. Opening the work editor or the homes does nothing for a person with only a chat. Those carry no keyword and should say nothing about one.

## approach

A node says whether it is reachable from a console. That flag is the only thing anybody writes.

The engine owns the keywords and answers what they are. The panel draws what it was answered, so the line a person reads is the exact string the matcher takes. It is copied rather than composed, so the two cannot differ by a character.

A control that makes no sense away from a desk carries no flag and draws no line.

The flag also answers which parameters have keywords at all, mechanically, which is what the check in the last criterion reads.

## done when

- a flagged node draws its keyword line in the tooltip, decided by: read the panel
- no title in the declaration names a keyword, decided by: search the declaration for one
- the work editor and the homes carry no flag and draw no line, decided by: read the panel
- the line the tooltip draws is the text the engine matches, decided by: a check that sends the drawn line and reads the state

## evidence: step 1. ask

<!-- write what is asked, the approach, and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | what is gained by doing it, and not only what it does | A word nobody can find is a word nobody uses. | the tooltip line |
| [x] | what breaks if it is never done, and not only that it stays undone | A person away from a desk cannot learn the door exists. | the detail |
| [x] | the approach is on the token before any work, as an interface or a shape a reader can disagree with | it names the flag, and says the panel copies the word | the approach |
| [x] | every done-when line is decidable, and names the command where one decides it | all four by one check that sends back what the panel drew | the new check |
| [x] | the change is small enough to review whole, or it is split first | — |  |
| [x] | the basics it stands on exist, or are minted first | the matcher landed first | wk-71ca9e0b46 |

## evidence: step 2. do

<!-- make the change with its tests, hand the engine the delta to test, and write the note -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | Read. The check went in first and was watched red on its refuse line. | the check |
| [x] | the change follows the approach on the token, or the token says why it departed | It follows it. The engine fills the word and the panel draws it verbatim. | config.go, panel.ts |
| [x] | se test --on this token answered ok, and what it ran is named | The new check green, with drive-panel, render-check, panel-icons and panel-draws-the-register. | tooltips-name-their-keywords |
| [x] | the note says what changed and why, for a reader who was not here | The check's header carries the reason, and the panel says why it copies. | the check |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | In the change. The flat package may only shrink, so Reachable folded into its one caller. | config.go |

