---
kind: [[work-token]]
process: [[trivial]]
guidance: [[work-token]]
title: maxSentences counts paragraphs
status: open
bucket: voice
depends_on:
  - "[[wk-b3d1f07a92]]"
  - "[[wk-e5a19c74b3]]"
---

## detail

`maxSentences` counts a whole section, at `src/engine/schema.go` line 602.
Voice rule 2 is about a paragraph.

So the keyword and the rule disagree about what they measure.
One section declares the keyword today: guidance `Motivation`, at ten.
A ten-sentence section of two paragraphs passes, and each paragraph may run to nine.

The owner rules that the count is per paragraph.

## done when

- `maxSentences` refuses a paragraph over the limit and passes a section of many small ones, decided by a Go test over `ValidateNote` with a two-paragraph fixture
- the refusal names the paragraph and its count, decided by reading the message in that test
- guidance `Motivation` carries a limit that suits a paragraph, decided by reading `spec/schemas/guidance.schema.yaml`
- every note under `spec` passes, decided by `se lint` over the tip
