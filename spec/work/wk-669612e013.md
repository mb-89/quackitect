---
# which schema reads this note
kind: [[work-token]]
# which process shapes this token and says how it moves
process: [[trivial]]
# the rules for filling this token
guidance: [[work-token]]
# the name this token is known by, in references and in links
title: stale clone hands closed
# where the token stands. The process owns these values.
status: open
# the person's own name for a group. It does not move the work
bucket: archive
# the tree each time the work was taken up, snapshots the engine wrote
began:
  - 15cc895ef1410239075ba15703ecfbeddc27c1f4
  - 4fbee166124ec157ca38aa7853f2cafd5f34b3c6
# the tree each time the work was put down or closed, snapshots the engine wrote
ended:
  - aafae7bbbc721e4738b90778feba9d1fc2c1ef5c
---

## detail

This box handed out wk-a463727d87 as open work. The branch had it closed and archived hours before: the change is commit 2218cabe, the note on the branch carries both checklists answered, and doc/work/archive.jsonl names the token.

The queue reads doc/work in the tree it runs over. A clone that is behind carries the note as it stood before the close, so the token reads open here whatever the branch says, and the pull hands it to somebody who then does the work again. The claim published on refs/heads/se/claims says nothing about it, because a claim says who holds a token and not whether it has ended.

Two hands on one box were three hours behind the branch today, which is how long the same work stayed available to be redone.

## proposed action

Decide where the close is read from. Either the pull refuses a token the archive on the branch already names, read from the fetched branch rather than from the working tree, or the engine brings doc/work into step before it answers a pull.

## done when

- a pull over a clone whose branch has archived a token does not hand that token out, decided by a test that archives on a branch and pulls in a tree behind it
- the refusal or the sync says which it did, so an agent is not left guessing why the queue is shorter

## evidence: step 1. ask

<!-- write what is asked and what done means, one criterion per line -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the ask is small enough to review whole, or it is split first | — | one reader of the fetched branch and one wrap of the queue: 4 files, commit d415ef5d |
| [x] | every done-when line is decidable, and names the command where one decides it | go test -count=1 -run TestAPullDoesNotHandOutWhatTheBranchArchived ./src/engine decides the first line, and -run TestAPassOverNamesTheBranchThatArchivedIt the second | both ran from src/engine of a worktree at d415ef5d: ok quackitect/engine 1.270s |
| [x] | the basics it stands on exist, or are minted first | — | the archive list on the branch and gitHere already stand, and nothing was minted |

## evidence: step 2. do

<!-- write one test, watch it go red, make the change, watch it go green -->

| done | criterion | evidence | receipt |
|---|---|---|---|
| [x] | the guidance this token names was read and applied | — | read and applied: one test per criterion, red first |
| [x] | one test was written first and seen red for the reason expected | TestAPullDoesNotHandOutWhatTheBranchArchived and TestAPassOverNamesTheBranchThatArchivedIt in pullbehindthebranch_test.go | red at 602d451a: the pull handed out wk-90420e7324, and the answer named no pass-over |
| [x] | the same test was seen green after the change, and named | the same two tests, from src/engine at d415ef5d | ok quackitect/engine 1.270s, and the package at 2635f405 showed only the six retro and liveengine failures the tip shows |
| [x] | the change is git diff began..ended, the two hashes the engine wrote on this token | — | commit d415ef5d on origin/v4, made in a worktree of the tip. This clone is behind, so began..ended holds only the helpers under .se |
| [x] | the cleanup the change revealed is in the change, or is a token of its own | — | aCloneBehindTheClose moved into fixture_test.go, where the suite's guard wants it |

