# v2:f8f2ad4288c0cc7fc743742d5842c47de3247b9668592c31c6079058d4c62edb — 8 findings

This sweep compared the walk machine, the human gates, the trace corpus and the tool lane against the strongest prior art reachable: LangGraph, Temporal, XState, Step Functions and Argo for orchestration; DO-178C trace practice, Stage-Gate and GitHub Spec Kit for spec-driven development; MCP gateways, RFC 9457 and OPA for tool mediation. Four mechanisms stand up as KEEPs with sources on both sides: the recompute-from-repository position model (replay and snapshot resume are both inapplicable or footgun-laden for an LLM driver), the graded autonomy/bless design (every surveyed HITL mechanism is binary pause/resume), the typed refusal (RFC 9457 plus an executable remedy and a repeat counter it lacks), and the reading proof (no equivalent found anywhere surveyed). Three adoptable gaps carry CODE findings. First, unattended waits have no engine-side deadline where Step Functions and Argo both bound approval pauses. Second, the cage enforces through client config and prose, and the contract itself records a measured silent bypass; sandbox-level enforcement is the practice-backed fix for unattended runs. Third, the trace corpus sits at the heavy end of practice — 1355 nodes, with 208 settled option/candidate/cluster nodes still under the live conformance sweep — where DO-178C scales trace depth by criticality and no surveyed practice keeps decided alternatives as live checked artifacts; the proposal is an archive slice, not deletion. One further KEEP: the discipline lane's in-process one-table policy beats OPA-style externalization while there is a single enforcement point. All removals and adoptions are the owner's decision; evidence is file:line on our side and primary-or-marked sources outside.

- **[KEEP]** prior-art / orchestration position-and-resume — spec/trace/design-spec/dsp-walk-machine.md:44
  claim: The recompute-from-repository position model is a defensible and in one respect stronger design than the checkpoint/replay models of LangGraph, Temporal and XState, at a documented cost in per-call latency and memo-invalidation complexity.
  evidence: Ours: dsp-walk-machine.md:44-46 'The session recomputes position from the repository on every call... never trusts a client-held position'; :56 'A crash lands safe: the walk resumes from the repository'; :165 'THE FRAME IS `make`: name a target, compute what is needed, run it'; :180-181 'IT RE-ASKS ON EVERY PULL... no plan is stored and none can go stale'. Theirs: LangGraph interrupts require a serialized checkpointer and 'the node restarts from the beginning of the node where the interrupt() was called when resumed, so any code before the interrupt() runs again' (https://docs.langchain.com/oss/python/langgraph/interrupts, fetched); Temporal replay 'checks Commands against an existing Event History' and breaks on non-determinism (https://docs.temporal.io/encyclopedia/event-history); XState restores a persisted snapshot and its own guidance warns persisted state 'is prone to incompatible state' (https://stately.ai/docs/persistence, via search summary). What they do better first: Temporal's event history is a complete durable per-step audit with exactly-once activity results; ours keeps only a capped call log outside se_run output. What ours does better: an LLM driver cannot be deterministic, so replay-based resume is inapplicable, and serialized-snapshot resume carries the schema-versioning and re-execution footguns our derive-from-evidence model has no place for ('Green is green, whoever walked it', dsp-walk-machine.md:313-319). The cost is real and measured in the same file: 200 ms per route (:805) and repeated memo-staleness wedges (:843-847).
  proposal: Keep the model. Record the LangGraph/Temporal/XState contrast in dsp-walk-machine.md's rationale so the next redesign debate does not re-derive it.
- **[KEEP]** prior-art / human-in-the-loop gates — spec/trace/design-spec/dsp-walk-machine.md:461
  claim: The graded autonomy design — per-state entry weight, ordered rung scale, separate stop-at dial, and bless-from-one-rung-above — is materially richer than every surveyed HITL mechanism, which are all binary pause/resume.
  evidence: Theirs first: LangGraph interrupt() pauses and Command(resume) continues, binary (https://docs.langchain.com/oss/python/langgraph/interrupts); AWS Step Functions waitForTaskToken pauses until SendTaskSuccess/Failure, binary (https://docs.aws.amazon.com/step-functions/latest/dg/connect-to-resource.html); Argo 'suspend: {}' waits for manual resume, binary (https://argo-workflows.readthedocs.io/en/latest/walk-through/suspending/). All put the approval POLICY outside the engine. Ours: every state carries an entry weight (dsp-walk-machine.md:298-301 'the weight of the decision to ENTER this state, 0.01 (mechanical) .. 1 (milestone). The agent may enter only when priority <= the session threshold'), entering and blessing use deliberately different comparisons so 'a gate is reviewed from one rung above the work it reviews' (:461-464), and the person holds two orthogonal dials (guidance/walking.md:171-173; deliverable/machines/stopat.md:44-45). The gate shape itself matches Stage-Gate practice — deliverables, criteria, go/kill output (Cooper, https://onlinelibrary.wiley.com/doi/full/10.1002/9781444316568.wiem05014) — so the design has independent grounding, not just novelty.
  proposal: Keep. Nothing in surveyed prior art is worth adopting into the gate model itself.
- **[CODE]** prior-art / unattended waits — guidance/method/cloud-runner.md:11
  claim: A wait or an owed bless on an unattended run has no engine-side deadline, where both Step Functions and Argo bound their human-approval pauses with timeouts — an adoptable mechanism.
  evidence: Ours: cloud-runner.md:11-12 'On a laptop a person notices when something stalls. Here nothing does, so the rules below replace them' — and the replacements are prose rules for the agent, not an engine clock. Engine timeouts exist only for child processes and sweeps (dsp-lane-door.md:417-420 'whole-tree killed on timeout'; session.ts:889 spawnSync timeout): grep of pull.ts/session.ts/route.ts finds no walk-level deadline. Theirs: Step Functions callback tasks support TimeoutSeconds and HeartbeatSeconds so a forgotten approval cannot run forever (https://docs.aws.amazon.com/step-functions/latest/dg/connect-to-resource.html); Argo suspend takes a duration that auto-continues or can time out the approval (https://argo-workflows.readthedocs.io/en/latest/walk-through/suspending/).
  proposal: Add an optional duration to wait-shaped stops (bless owed, threshold above dial) on unattended runs: on expiry the engine emits a notification record or takes a drawn fallback edge rather than hanging silently. Owner decides whether expiry escalates or aborts.
  lint: A conformance check: every machine reachable by the unattended entrypoint either carries wait durations or is explicitly marked as attended-only.
- **[CODE]** prior-art / trace corpus size — spec/trace/design-spec/dsp-trace-corpus.md:19
  claim: The corpus sits at the heavy end of practice — 18 node types, 1355 nodes, with 787 in raid/requirement/option alone — and mature practice both scales trace depth by criticality and does not keep decided alternatives as live checked artifacts.
  evidence: Ours: directory counts (spec/trace/: 311 raid, 285 requirement, 191 option, 120 test-spec, 9 candidate, 8 cluster, 38 work-token...), and 'Conformance runs the item templates' declared checks over every node' (dsp-trace-corpus.md:19-21), so every kept node is a standing check load. Theirs: DO-178C traces requirements→design→code→tests and explicitly REDUCES trace depth at lower criticality levels — 'For level D, there's no need to have traceability down to low-level requirements, source code, or software architecture' (https://www.parasoft.com/learning-center/do-178c/requirements-traceability/; primary standard paywalled, not seen); its life-cycle data items include no live pool of decided alternatives. Spec Kit, the agentic-SDD end of practice, maintains four core artifacts — constitution, spec, plan, tasks (https://github.com/github/spec-kit, fetched). Ours does already have one criticality mechanism (change-size rigor cells, dsp-walk-machine.md:242-255), which weakens any blanket over-built claim — the specific gap is the 208 option/candidate/cluster nodes of settled decisions that remain first-class conformance-checked inputs after their decision closed. Whether keeping them live earns its cost is not answered by these sources; what practice does is archive them.
  proposal: Owner decision: an archive slice for options/candidates/clusters whose decision has shipped — kept readable for provenance, dropped from the live conformance sweep and the loader's derived graph. Not deletion.
  lint: A sweep metric: nodes per type never referenced by any live claim, edge, or route in N iterations — surfaces archive candidates mechanically.
- **[KEEP]** prior-art / refusal design — guidance/refusals.md:8
  claim: The typed-refusal design independently reinvents RFC 9457 problem details and extends it in two ways suited to LLM agents: a mandatory executable remedy, and a refusal that counts its own repeats.
  evidence: Theirs first: RFC 9457 standardizes machine-readable error content — a type URI, title, detail — 'to avoid the need to define new error response formats' (https://www.rfc-editor.org/rfc/rfc9457.pdf); it carries no remedy field and no repeat detection. Ours: 'A refusal is typed. It carries the clause and the remedy' (refusals.md:8-9), with a test RFC 9457 lacks — 'could somebody act on it without asking a second question? If not, it is a diagnosis rather than a remedy' (refusals.md:37-38) — and the repeat counter: 'From the third identical one on, the served refusal carries a repeated block naming the count' (dsp-lane-door.md:547-549), motivated by a measured 27-refusal loop in nine seconds (:558-560). No surveyed error-format practice carries either extension.
  proposal: Keep. Optionally note the RFC 9457 correspondence in refusals.md so the shape is recognizable to an outside engineer.
- **[CODE]** prior-art / tool-mediation lane — guidance/method/lane.md:16
  claim: The cage's enforcement is client-side configuration plus a prose contract, which the system's own contract records being silently bypassed once; gateway and sandbox practice puts enforcement at a boundary the agent cannot bypass.
  evidence: Ours: 'Your native tools are blocked here, tool by tool, by an explicit list... Which file holds that list depends on the host' (guidance/method/lane.md:6,16) — per-host config the host must honor; and the measured failure: 'the conflict is silent from both sides... it surfaces only as an agent quietly working outside the cage while believing it is following instructions' (guidance/contract.md:25-28, MEASURED 2026-08-20). Theirs: MCP gateway practice interposes a proxy so 'every tool call [routes] through a central point where authentication is validated, permissions are enforced, and every action is logged', and filters the tool LISTING before discovery, not just before invocation (https://tyk.io/learning-center/mcp-gateway-architecture-technical-guide/, https://nhimg.org/articles/mcp-tool-filtering-is-not-access-control-for-ai-agents/ — vendor/industry sources: they prove the pattern exists, not that it is better). The structural point stands on our own evidence: a host's native Read/Bash are not MCP calls, so no gateway reaches them — only an OS-level boundary (checkout readable solely by the lane's process user, or a container mounting only the lane) makes the cage a wall rather than a request.
  proposal: Owner decision: for unattended runs, move enforcement one layer down — run the harness in a workspace where the project tree is reachable only through the lane process (separate OS user or container), so a host that ignores its settings file finds nothing to read natively.
  lint: A boot check: attempt a native-path read as a probe; if it succeeds, the banner reports the cage is advisory on this host.
- **[KEEP]** prior-art / reading proof — guidance/walking.md:185
  claim: The reading proof — three spread fill-in-the-blank probes proving a document is actually in the agent's context — has no equivalent in any surveyed orchestration or SDD system, and its design rationale explicitly beats the obvious alternative.
  evidence: Ours: 'THREE PROBES, SPREAD THROUGH THE DOCUMENT... near the 30%, 60% and 92% marks, so all of it has to be in hand' (guidance/walking.md:185-187), with the rationale 'WHY PROBES AND NOT A HASH: you cannot compute one, and one the engine handed you would prove only that a message arrived' (walking.md, reading section). Theirs: nothing comparable appears in LangGraph HITL docs (https://docs.langchain.com/oss/python/langgraph/interrupts), Temporal (https://docs.temporal.io/workflow-execution), Step Functions, Argo, or Spec Kit (https://github.com/github/spec-kit) — all assume the agent consumed what it was handed. Stated per the evidence rules: absence in surveyed sources, not proof nothing exists anywhere.
  proposal: Keep. This is one of the system's genuinely novel mechanisms and a candidate for outward publication.
- **[KEEP]** prior-art / policy engine placement — spec/trace/design-spec/dsp-lane-door.md:233
  claim: Keeping the discipline lane's policy in-process as one table feeding classifier, refusal text and tool description is the right call at this scale; externalizing to an OPA-style engine would trade the one-source-three-outputs guarantee for a dependency and buy nothing.
  evidence: Theirs first: OPA's core value is decoupling policy decisions from service code so one policy serves many enforcement points (https://www.openpolicyagent.org/docs) — a win when N services share policy. Ours has one enforcement point and derives feed-forward and feedback from the same source by construction: 'ONE TABLE, THREE OUTPUTS... feed-forward and feedback generated from the same source, so they cannot drift apart' (dsp-lane-door.md:250-253), with the ladder harvested from 2,589 logged calls (:239). An external policy file would reopen exactly the drift the one-table design closed. Tradeoff named: OPA-style externalization wins if a second enforcement surface (e.g., a gateway from the finding above) ever needs the same rules.
  proposal: Keep as is; revisit only if a second enforcement point appears.

---

# v2:c3734c7598a900d3a664f8f1d039ca4343211218c1fc31b2310ee3abc62830a6 — 5 findings

Both research jobs are delivered with sources. JOB 1: the debt overhaul.md:98-101 names is paid as a concrete eight-criterion proposal for choosing repair, overhaul, or replace on a software artifact, each criterion mapped from named maintenance-engineering literature — SAE JA1011's consequence-classification and five strategies, MSG-3's applicable-and-effective level-2 logic and task order, Nowlan & Heap's failure patterns (age-related wear-out is only 11-23% of failure modes, which maps drift-from-a-moved-standard to the one software failure mode scheduled overhaul actually fits), Moubray's P-F interval (a cheap indicator makes the action repair-on-red, never a schedule), and engineering economy's defender-vs-challenger EUAC (replace when the rewrite's forward cost beats the rising repair toll; sunk authorship counts for nothing). A KEEP records that overhaul.md's mechanise-everything section independently restates RCM's central empirical result and can now cite it. JOB 2: the in-house lint (deliverable/engine/lint.ts + voice-lint.md card + prose-inspect.ts) already covers voice.md's structural rules; the two uncovered mechanically-checkable classes are substitution (forbidden words: WEDGE blocking, RECORD report-only — measured 972 "record" word-hits in engine TS including the TypeScript Record<> type, so blocking would cry wolf) and existence (teaser openers, whose phrase list voice.md already carries as its own quoted examples, plus minute-count and weasel-quantifier patterns). Verdict against wholesale Vale/textlint adoption, both sides evidenced: Vale's substitution and existence checks are the exact prior art and its editor integration is genuinely better, but Vale's code support lints comments only — it cannot reach the served strings voice.md:96 names as the real fix — and it lints files, so lane-passing form/brief prose where the no-teasers rule is actually broken is invisible to it. Minimal adoption: two ~30-line rules in lint.ts with parameters on the existing card, and a served-strings sweep script beside prose-inspect.ts.

- **[CODE]** guidance/method — maintenance-engineering prior art (JOB 1) — guidance/method/overhaul.md:98
  claim: The debt overhaul.md names at lines 98-101 is payable now: RCM (Nowlan & Heap / SAE JA1011), MSG-3's level-2 decision logic, and engineering economy's replacement analysis together yield eight named criteria for choosing repair (patch), overhaul (rework in place), or replace (rewrite/adopt) for a software artifact.
  evidence: guidance/method/overhaul.md:98-101 — "Those sources cover the REMOVAL half only. The method half wants maintenance engineering's own literature, which turns on exactly this choice: overhaul, repair, or replace. That prior art is not adapted here yet, and it is owed." Sources: SAE JA1011 seven-step criteria incl. "Classify the consequences of failure" and five strategies (condition-based, scheduled overhaul, scheduled replacement, failure-finding, re-design) — https://accendoreliability.com/sae-ja1011-standard/. MSG-3 five task types in fixed order, each gated by "is it applicable and effective" — https://www.hbkworld.com/en/knowledge/resource-center/articles/creating-initial-scheduled-maintenance-plans-for-aircraft-msg-3 (ATA MSG-3 primary is proprietary; primary not seen). Failure patterns: age-related wear-out is 11-23% of failure modes, the rest random — https://reliabilityweb.com/en/articles/rcm-failure-modes-patterns (reports Nowlan & Heap 1978; primary not fetched). P-F interval — https://www.aladon.com/the-p-f-curve-explained/. Economic service life / defender-vs-challenger EUAC — https://www.sfu.ca/~jones/ENSC201/Unit09/lecture9.html.
  proposal: Extend overhaul.md with a second named list beside SORTED — "the action: REPAIR, OVERHAUL, or REPLACE" — eight criteria, each with its source: 1. CONSEQUENCE CLASSIFIES FIRST (JA1011 step 5; MSG-3 top-down). What does this artifact being wrong COST? An artifact that routes real work gets a scheduled sweep; one that merely reads badly is fixed on encounter; run-to-failure is a legal verdict for the lowest class. This grounds "Take the Erroneous first". 2. THE INDICATOR DECIDES REPAIR (Moubray P-F interval; MSG-3 asks the inspection question before restoration or discard). Where a cheap check can see degradation before a reader is misled, the check is the plan and the action is repair-on-red: patch the named defect when the indicator fires, never on a schedule. 3. OVERHAUL ONLY WHERE WEAR IS AGE-LIKE (Nowlan & Heap: scheduled restoration applies only to age-related wear-out, 11-23% of failure modes). Software's one true wear-out is DRIFT — the standard moves and everything written before it wears out together. So scheduled overhaul is applicable to drift and to nothing else; a point defect fails "randomly" and gains nothing from a sweep. The interval is not calendar time: the potential-failure event is "a rule changed", which is the literature's own ground for scope-by-the-standard's-delta and for "age is never a defect". 4. RESTORATION NEEDS A WRITTEN STANDARD (MSG-3 restoration returns an item to a specific standard). Overhaul-in-place is only on the menu where the standard is written; guidance is that standard here, and for a recovered product the reverse-engineering function must reconstruct it first — the existing "waits on that" sentence, now with its reason. 5. APPLICABLE AND EFFECTIVE, BOTH, PER ACTION (MSG-3 level-2 logic). Applicable: a patch cannot fix an artifact whose structure is wrong; an overhaul cannot fix one whose method is superseded. Effective: the action must stop recurrence at bearable cost. Recurrence is the escalation signal: repair→overhaul when defects recur within one artifact; overhaul→replace when the same artifact tops the findings two overhauls running. 6. DEFENDER VS CHALLENGER, FORWARD COSTS ONLY (economic service life / EUAC). Replace when the rewrite's full cost — writing, migration, relearning — beats the defender's rising repair toll for its remaining life. Sunk authorship counts for nothing. The call log and the findings register are the cost series; each sweep buying less time than the last is the economic-life bell. 7. HIDDEN FAILURES GET FAILURE-FINDING TASKS (JA1011 failure-finding strategy). An Orphaned artifact fails invisibly — no reader, so no report. The reference-scan (does anything read this?) is the failure-finding task and belongs in step 2's machine run, not in judgment time. 8. NO TASK WORKS → REDESIGN (RCM re-design default; JA1011 step 7). When repair recurs, overhaul recurs, and a same-shape rewrite would meet the same fate, the defect is in the rule or mechanism — a retro item, because the retro is where the standard moves. Owner decides adoption; this is a proposal with sources, not an edit.
- **[KEEP]** guidance/method — mechanise-everything section (JOB 1) — guidance/method/overhaul.md:75
  claim: Overhaul.md's "Mechanise everything you can" section states RCM's central empirical result without citing it; the section is right and can now carry its source.
  evidence: guidance/method/overhaul.md:75-76 — "The prior art is blunt here. Debt work fails as a periodic cleanup sprint. It succeeds as a continuous check." The literature's version: scheduled overhaul was found inapplicable for the majority of failure modes (only 11-23% age-related — https://reliabilityweb.com/en/articles/rcm-failure-modes-patterns), and both RCM and MSG-3 rank on-condition/inspection tasks ahead of restoration and discard (https://www.hbkworld.com/en/knowledge/resource-center/articles/creating-initial-scheduled-maintenance-plans-for-aircraft-msg-3). "If an overhaul finds the same thing twice, it was always a lint" (overhaul.md:85) is the on-condition principle restated.
  proposal: When criterion set from the previous finding is adopted, cite the failure-pattern result at this section as its ground. Per the repo's own rule (lint.ts:89-92, REFERENCE_HOME), the citations land as reference notes under spec/references/, cited from the prose.
- **[LINT]** voice lint — forbidden words (JOB 2) — guidance/voice.md:90
  claim: Voice.md's forbidden-words list (RECORD, WEDGE) is a substitution-table lint — the exact class Vale calls a substitution rule — but only an in-house rule can reach the served strings voice.md names as the real fix, because Vale's code support lints comments only, never string literals.
  evidence: guidance/voice.md:94-98 — the two entries; voice.md:96 — "Correcting prose fixes the symptom; sweeping the served strings is the fix." voice.md:102 — "the list is the rule's memory... A rule with no examples cannot be checked." Vale substitution rule shape (extends: substitution, swap map, ignorecase) — https://docs.vale.sh/checks/substitution.md. Vale code support: "Vale supports linting source code comments" in 28 languages incl. TypeScript; no string-literal linting — https://docs.vale.sh/formats/code.md. Our side, measured: "wedge" has 24 word-hits in deliverable/engine/*.ts but nearly all are comments or trace-layout.ts's legitimate geometry term (radial wedge, trace-layout.ts:225-233); the one served surface found (session.ts:1329-1336) already carries no "wedge" in its message text, only in its source locator. "record" has ~972 word-hits including the TypeScript Record<> type and legal generic uses — voice.md:95 itself says "The generic is legal only where the sentence genuinely covers both."
  proposal: Write the rule in lint.ts, not in Vale. lintProse already runs at every form submit (stateform-problems.ts:658-660) and under se_lint (tools-desk.ts:153,183,187), so the new rule bites everywhere the broken prose actually passes, at zero new dependency. What Vale does better, stated: maintained rule packages, LSP/editor quick-fixes, 12 extension points, battle-tested tokenization — worth it for a docs corpus edited by humans in editors, which this is not.
  lint: A `forbidden-word` rule in deliverable/engine/lint.ts, its word table as data in deliverable/machines/lint/voice-lint.md (the split that card already declares: "The rules' LOGIC lives in deliverable/engine/lint.ts; only parameters belong here"). Scope: prose surfaces only — markdown bodies, frontmatter prose, and served-string extraction from engine TS (a small script in the prose-inspect.ts mold; Vale cannot do this half). WEDGE goes on the `blocking:` list (no legitimate prose use found); RECORD stays report-only (collision with legal uses — the same colliding-needle honesty prose-inspect.ts:118-125 already practices).
- **[LINT]** voice lint — teaser openers (JOB 2) — guidance/voice.md:82
  claim: The no-teasers rule's fixed-phrase half is mechanically checkable as an existence-class rule anchored to paragraph starts — voice.md:82 explicitly orders this lint — and the phrase list is already written into voice.md itself as quoted examples.
  evidence: guidance/voice.md:82 — "This is the most-broken rule on this page. Broken again, it wants a LINT rather than another sentence." The phrase inventory sits in the rule's own text: voice.md:66 ("Something you will want to hear", "this will surprise you", "the interesting part is"), voice.md:77 ("Two things here", "the second one matters more", "before I answer that"), voice.md:79 ("That settles it", "this changes everything"), voice.md:80 ("Fair point", "good catch", "you're right to ask"). Prior art: Vale existence rule — tokens become "a word-bounded, non-capturing group", regex allowed, raw for anchoring — https://docs.vale.sh/checks/existence.md; write-good ships sentence-start checks as standard practice: "so — Checks for `so` at the beginning of the sentence", "thereIs — Checks for `there is` or `there are` at the beginning" — https://github.com/btford/write-good. Our side: lint.ts has no opener rule today (rules are wall, long-sentence, comma-chain, dash-chain, sentence-run, item-grew, pyramid, external-link — voice-lint.md card, "The names are the rules' own"). Grep found zero teaser-opener lines in the committed md corpus — the breakage lives in chat-adjacent prose (form fields, briefs, notes), which only the engine sees.
  proposal: Add the opener rule where the forbidden-word rule goes, in the same change. The general no-teasers judgment ("DELETE YOUR FIRST SENTENCE") stays human — only the fixed-phrase memory is the machine's, which is exactly voice.md:102's doctrine that the list is the rule's checkable memory.
  lint: An `opener` rule in lint.ts: flag a paragraph or field whose first sentence starts with a listed phrase, phrases as data in voice-lint.md. Seed the list from voice.md's own quoted examples plus the agreement-preamble family ("Great question", "Good point"). Same class, same rule file, two more instances: the minute-count pattern ("in N minutes" where voice.md:~178 demands a clock time — regex `\bin ~?\d+ minutes\b`) and the weasel quantifiers serving NUMBERS OVER ADJECTIVES (voice.md:69) — "some/several/various/a few" before a countable noun, write-good's `weasel` check being the prior art. Report-only at first; the `blocking:` card promotes any of them later, per the card's own doctrine ("Start with the rule that is already binding, watch what the reports say, and add the next one deliberately").
- **[CODE]** voice lint — adoption verdict (JOB 2) — deliverable/engine/lint.ts:181
  claim: Minimal adoption is two new rule classes in the existing in-house lint (substitution, existence), not a Vale/textlint rollout: the in-house lint already covers voice.md's structural rules and runs where the violations actually occur, while every off-the-shelf tool misses the two surfaces that matter (served strings in engine TS, and lane-passing prose that never lands in a file).
  evidence: Coverage, our side: lint.ts implements long-sentence (voice.md:28), wall/SE-C-125 (voice.md:33-40, errors.ts:80), comma-chain and sentence-run for unrendered lists (voice.md:32, 44-48), item-grew (voice.md:50), pyramid (voice.md:180-186), external-link; prose-inspect.ts covers bare method terms in entry docs (item 1) and roles-not-names leak scanning (item 3). Thresholds are deliberately looser than the prose aim (voice.md:28 aims fifteen words; lint.ts:76 flags at 28) — the card's cry-wolf doctrine, not drift. Coverage, their side: Vale — 12 extension points, substitution/existence fit the two gaps exactly (https://docs.vale.sh/checks/substitution.md, https://docs.vale.sh/checks/existence.md) but its code support reads comments only (https://docs.vale.sh/formats/code.md) and it lints files, so form/brief/note prose passing through the lane is invisible to it; write-good — nine fixed checks (passive, weasel, so, thereIs, adverb, tooWordy, cliches, illusion, eprime — https://github.com/btford/write-good), only `weasel` serves a voice.md rule; proselint — opinionated fixed English-usage set, orthogonal to voice.md's house rules (https://best-of-web.builder.io/library/amperser/proselint); alex — inclusive-language, answers no voice.md rule (the people-and-privacy rule is a leak check prose-inspect item 3 already runs); textlint — a plugin framework that could host custom rules in markdown (https://earthly.dev/blog/markdown-lint/) but adds an npm dependency tree to re-implement what lint.ts already is.
  proposal: Extend lint.ts with `forbidden-word` and `opener`, parameters on the voice-lint.md card, plus a served-strings sweep script beside prose-inspect.ts for the engine TS surface. Vale earns its place only if this repo's markdown ever gets human editors working in editors — its LSP quick-fixes and maintained packages are real advantages the in-house lint does not have, and none of them touch the two surfaces this repo's violations live on.
  lint: The two rules from the previous findings. Nothing further: identifiers-expanded, evidence-with-reference, BLUF and verdict-cites-sentence are judgments no token pattern reaches.

---

# v2:cc93273a8e02a46a50535117cfefc1f9f73afeb25d8374335085a3e89f7bfd7e — 24 findings

Both modules were read in full (session.ts 4,147 lines, sessionclaims.ts 1,839). The worst finds are two E-grade contradictions: se_amend's `chain` argument is documented "NOT IMPLEMENTED — accepted and ignored" while the handler forwards it and refreshChain rewrites every downstream signed claim; and legal() omits join states from the MACHINERY grant that packet/pullHere/stateInfo all advertise, so the gate refuses a tool the packet calls legal. A systematic D: 21 of 22 refusal `source:` strings in sessionclaims.ts still say "engine/session.ts" after the lift. Correctness-adjacent CODE finds: route-memo invalidation is missing on five write paths because forgetRoute lives on the wrappers instead of the stateFormSave funnel; openRecordPoints/formHome hardcode "expeditions" so a bound iteration's decision-graph check vacuously passes against a nonexistent file; GreenPass.times is keyed by bare state id across machines that share state names; and outcomeFor/takesRepairEdge answer the repair-edge question with opposite quantifiers. Duplication clusters: visitState defined three times (one copy orphaned), formDone's unsigned-feeder refusal copies stateBlockers', stateFormGet/stateFormExport share a forty-line preparation block, claimStall duplicates ownClaimProblems, and the fill-instruction string is pasted three times. The split answer: session.ts still holds two clean extractable seams — the route machinery (~500 lines) and the pull answer-shaping surface (~750 lines) — both fitting the existing ClaimsHost pattern, plus a settings-persistence seam; sessionclaims.ts was lifted whole and divides into green computation, grey diagnosis, and state-form CRUD. The one-tree migration (i34) left laneRoot computing pathKind only to return the same root in every branch. Several finds repeat a shape (missed invalidation, per-module source strings, duplicated served strings, hand-rolled qualified-id splits), so lints are proposed for each class.

- **[E]** session pair — deliverable/engine/sessionclaims.ts:918
  claim: The se_amend tool description says `chain` is "NOT IMPLEMENTED — accepted and ignored", but amendClaim implements it: chain=true runs refreshChain, which rewrites every signed downstream claim file with an amended stamp.
  evidence: tools.ts:291 description: "chain: NOT IMPLEMENTED — accepted and ignored..."; tools.ts:303 handler passes `args.chain === true`; sessionclaims.ts:918 `const chained = chain ? this.refreshChain(name, m, reason, by) : {}` and refreshChain (924-963) does `writeFileSync(h.instanceAbs, withAmended(raw, when, by, why))` per downstream claim.
  proposal: Pick a side: either delete the `chain` parameter from amendClaim plus refreshChain (the owner reversed re-greying 2026-08-17, so its premise is gone), or fix the tool description. The current state is a doc promising a no-op while the wire mutates a whole cone of signed files.
  lint: a test asserting a tool description containing 'NOT IMPLEMENTED' corresponds to an argument the handler does not forward
- **[E]** session pair — deliverable/engine/session.ts:2876
  claim: legal() grants the MACHINERY tools only to start/end states while packet(), pullHere() and stateInfo() advertise MACHINERY for join states too — so standing on a join, the packet says se_file_read is legal and gate() refuses it.
  evidence: session.ts:2876 `if (s.kind === "start" || s.kind === "end") for (const t of MACHINERY) tools.add(t);` versus the display ternary `s.kind === "start" || s.kind === "end" || s.kind === "join" ? [...MACHINERY] : ...` at session.ts:2286, 3644, 3965. "join" is a real kind (machine.ts:206).
  proposal: Extract one `machineryFor(s: StateDecl)` helper used by all four sites, including join in the enforcement path (the three display sites already agree; legal() is the outlier).
  lint: lint: the start/end/join kind-test may appear exactly once; today the same ternary is triplicated and the fourth copy diverges
- **[D]** session pair — deliverable/engine/sessionclaims.ts:225
  claim: 21 of the 22 refusal `source:` strings in sessionclaims.ts still say "engine/session.ts ..." although the code was lifted into sessionclaims.ts — a reader following the source lands in the wrong file.
  evidence: grep of sessionclaims.ts: sources at lines 225, 282, 739, 767, 776, 793, 840, 859, 876, 899, 914, 979, 987, 998, 1504, 1524, 1551, 1563, 1593, 1612, 1628, 1800 — only line 282 says "engine/sessionclaims.ts"; e.g. line 225 `source: "engine/session.ts route"` sits inside Claims.stalledClaim.
  proposal: Sweep the source strings to name their own file, or derive them from a per-module constant.
  lint: lint: a `source: "engine/<file>"` literal must name the file it appears in — one grep per module
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:1164
  claim: Route-memo invalidation is missed on several write paths: Claims.stateFormSave never calls forgetRoute, so submitEvidence (direct call), flipRuling, scenarioVerdict, stateFormIngest and Session.formConfirm all change form content that routes and formsMet depend on while routeMemo keeps serving the pre-write answer.
  evidence: forgetRoute call sites are only session.ts:1707/3231/3262 and sessionclaims.ts:756/829/969. Direct stateFormSave callers bypassing them: session.ts:3532 (submitEvidence), sessionclaims.ts:1116 (flipRuling), 1161 (scenarioVerdict), 1805 (stateFormIngest). formConfirm (session.ts:3243-3259) writes the instance with no invalidation. routeMemo's key (session.ts:1820) covers position/autonomy/generation, not form content.
  proposal: Move `host.forgetRoute()` into Claims.stateFormSave itself (the single write funnel) and add it to formConfirm and mintInterfaceCell; delete the now-redundant calls in the wrappers.
  lint: test: every method that calls writeFileSync/writeNode on form or trace paths must invalidate the route memo — assert via a spy that a save through each public door clears it
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:129
  claim: openRecordPoints and formHome hardcode the "expeditions" path segment, so with a bound ITERATION they read/write spec/expeditions/<iteration-id>/... — openRecordPoints then reads a nonexistent decisions.jsonl and the graph-is-evidence check vacuously passes.
  evidence: sessionclaims.ts:129 `join(this.host.bound!.path, "spec", "expeditions", this.host.bound!.id, "decisions.jsonl")`; session.ts:3004 `const recRel = ["spec", "expeditions", this.bound.id]`; contrast sessionclaims.ts:256 which derives kind: `this.host.bound.branch.startsWith("it/") ? "iterations" : "expeditions"`; iterationOpen writes the sink to spec/iterations/... (session.ts:975); replayFile returns empty for a missing file (decisions.ts:95).
  proposal: Extract one `boundRecordDir()` on the host that derives the kind the way stateFormHome already does, and use it in openRecordPoints, formHome and the two setExtraSink calls.
  lint: test: bind an iteration, write an open decision op, assert formLint reports it — today it cannot
- **[R]** session pair — deliverable/engine/sessionforms.ts:14
  claim: visitState is defined three times verbatim — session.ts:31, sessionclaims.ts:21, sessionforms.ts:14 — and the sessionforms copy is used by nothing at all (O), while sessionclaims uses its own local copy and only a test imports session.ts's.
  evidence: grep: definitions at session.ts:31, sessionclaims.ts:21, sessionforms.ts:14; the only external import is tests/gitlane.test.ts:10 `import { visitState } from "../engine/session.ts"`; sessionforms.ts never references it after defining it.
  proposal: Keep one copy (a small ids/qualified-name module fits — see the qualified-id finding), import it in the other two files, and point the test at it.
  lint: biome/lint: exported symbol with zero imports; duplicate-function detection catches the identical bodies
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:820
  claim: reopenClaim normalizes a qualified state id through bareState (with a comment saying "every form lookup takes the bare name"), but its siblings amendClaim and formBless do not — the same qualified id is accepted by se_reopen and refused by se_amend.
  evidence: sessionclaims.ts:754 `const name = this.bareState(rawName);` with the comment at 743-748 naming the i15 cost; amendClaim's signature (820-828) uses `name` raw at 831 `this.stateFormState(name, m)`; formBless (968-971) likewise. tools.ts passes `String(args.state)` unmodified to both.
  proposal: Move the normalization into stateFormState (the shared lookup) so every verb gets it, and delete the local bareState call in reopenClaim.
  lint: test: each state-naming verb accepts `iterations/iN/<state>` — parametrize over reopen/amend/bless
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:476
  claim: GreenPass.times is keyed by bare state id while one pass spans many machines, and sibling generated iteration machines share state names — recordDone's staleness fixed point (line 599) reads the map after drawingDone has recursed into sub-machines, so machine X's signature time can be read as machine Y's.
  evidence: sessionclaims.ts:476 `pass.times.set(s.id, claimTime(fm))` (bare id); recordDone order: standingClaims (592) then drawingDone recursion (593-597) then `const signedAt = pass.times ?? new Map()` (599); pass is shared through recursion (drawingDone 538 `this.recordDone(sub, seen, pass, paint)`); iteration machines are generated from one matrix, so i15 and i16 both carry e.g. draft-vision.
  proposal: Namespace the key: `pass.times.set(`${decl.id}\0${s.id}`, ...)` and read it the same way, or hold a per-decl map. Cheap, and it removes a class of wrong stale-verdicts. Unconfirmed in a live repro — flagged from the code's own ordering.
  lint: test: two sibling generated machines with one shared state name and different signature times; assert neither pollutes the other's staleness verdict
- **[CODE]** session pair — deliverable/engine/session.ts:3280
  claim: formDone rebuilds the unsigned-feeder refusal that stateBlockers already builds — same feederFault walk-back, same `— the break is upstream at ...` string, same remedy notes — duplicated across the two modules.
  evidence: session.ts:3280-3304 (feedersUnsigned + feederFault + upstream string + remedy note "go to ${root.state} and fix what it names — the feeder above it is already signed...") is near-verbatim sessionclaims.ts:1566-1595 (kind "unsigned_feeder").
  proposal: Have formDone throw the unsigned_feeder blocker from claims.stateBlockers (assertStateFormMet already shows the pattern at sessionclaims.ts:1649), deleting the copy.
  lint: duplicate-string detection: a served sentence appearing in two modules
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:330
  claim: stateFormGet and stateFormExport duplicate the whole model+fills+bindView preparation, including a verbatim ten-line comment block.
  evidence: sessionclaims.ts:330-370 and 1748-1776 both call stateFormModel with the same eight arguments, both loop `fills[f.name] = stripComments(section(body, f.name)).trim()`, both `Object.assign(fills, bindView(...))`; the comment "A BOUND FIELD IS REBUILT FROM THE NODES..." appears verbatim at 361-367 and 1768-1775.
  proposal: Extract a private `modelAndFills(name, m): { s, h, raw, model, fills }` used by both; the comment lives once on the helper.
  lint: duplicate-block detection (jscpd-style) over the engine
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:166
  claim: claimStall duplicates ownClaimProblems — both resolve the decl, fetch the iteration, read the evidence note body and call claimProblems with a freshly loaded trace.
  evidence: claimStall (166-181) and ownClaimProblems (1410-1425) differ only in claimStall's extra guards (`evidence_form.length === 0`, recordDone membership); both end in `claimProblems(this.host.traceRoot(it), decl, body, loadTrace(this.host.traceRoot(it)))`.
  proposal: claimStall becomes: resolve bare id, return [] unless claimful-and-not-done, else `this.ownClaimProblems(bare, m)`.
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:629
  claim: lawProvenStates and suspectStates each run their own full corpus pass although render.ts calls them in the same paint as recordPaint — the exact repeated-scan shape the class's own comments condemn, and which blessedGates was already fixed for.
  evidence: sessionclaims.ts:633 `const corpus = loadTrace(traceRoot)` (fresh); suspectStates:682 `new Set(this.recordDone(decl))` with a fresh default pass; render.ts:813-817 calls recordPaint, blessedGates(decl, paint), lawProvenStates(decl), suspectStates(decl) back to back; blessedGates' comment (641-645) names recomputation as "the exact shape i33 exists to remove".
  proposal: Give lawProvenStates and suspectStates an optional GreenPass/painted-set parameter like blessedGates, and pass one shared pass through drawingSets in render.ts.
  lint: perf test: one drawingSets call may load the trace at most once
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:510
  claim: The law-proven state set is a magic literal duplicated across modules: sessionclaims hardcodes `endsWith("fill-story-evidence")` while rigor-matrix.ts:183 tests `row.name !== "fill-story-evidence"` — a second law-proven row added in the matrix would be silently missed here.
  evidence: sessionclaims.ts:510 `if (!s.id.endsWith("fill-story-evidence")) continue;` with the comment at 504 admitting "rigor-matrix's refuseBadRow names the set"; rigor-matrix.ts:183 carries the other copy.
  proposal: Export a `LAW_PROVEN_ROWS` set (or an isLawProven(row) predicate) from rigor-matrix.ts and import it in both places.
  lint: lint: a state-name literal may appear in exactly one module
- **[CODE]** session pair — deliverable/engine/session.ts:1696
  claim: stepBackTo (session) and walkBackTo (claims) are two near-duplicate mechanisms for putting the walk back on a fan leg, reached from two doors — and only stepBackTo records the move in history, so the same act is logged through one door and silent through the other.
  evidence: session.ts:1696-1709 sets active/current, pushes history {outcome:"reopened"}, forgetRoute, notifyChange; sessionclaims.ts:1304-1311 sets active/current/status and notifies only. stepBackTo is the sweep's back-hop (2105), walkBackTo is the stuck-join choice (2690).
  proposal: One method on Session, taking a `recordHistory` reason; the stuck-join path passes its own reason so both doors leave the same trail.
- **[CODE]** session pair — deliverable/engine/session.ts:1341
  claim: outcomeFor and takesRepairEdge answer the same question — is this hop a repair edge — with opposite quantifiers, so a hop whose target has both a normal and a fallback edge is scored "failed" by outcomeFor while takesRepairEdge insists it is a forward move and still demands the form.
  evidence: session.ts:1341-1345 `taken.some((e) => e.role === "fallback" || e.role === "error") ? "failed"`; sessionclaims.ts:1643-1647 `edges.length > 0 && edges.every((e) => ...)` with the doc "Only a target reachable ONLY by a repair edge is a repair".
  proposal: Make outcomeFor use the same every-quantifier (or call takesRepairEdge), so a shared-target hop is consistently a filled forward move.
  lint: test: state with normal+fallback edges to one target; assert outcome and form-gate agree
- **[S]** session pair — deliverable/engine/session.ts:940
  claim: laneRoot computes pathKind(rel) and then returns the identical root in every branch — the workRoot/machineRoot/laneRoot distinction was superseded by the one-tree migration (i34) and only the ceremony remains.
  evidence: session.ts:940-953: kind "session" → machineRoot(), "method" → machineRoot(), otherwise `void rel; return this.workRoot()` — and workRoot() (920-922) returns machineRoot(). The comment at 947-951 says "both answers are now the same root, so the question is not asked" while the code still asks pathKind per call.
  proposal: Collapse laneRoot to `return this.root`, keeping the method as the documented seam; delete the dead pathKind call and the `void rel`. Whether workRoot/machineRoot merge is the owner's call — they are the seam a future two-tree design would reopen.
- **[CODE]** session pair — deliverable/engine/session.ts:264
  claim: Session carries ~32 one-line delegate methods to its five sub-objects, each with the same repeated "Not private because a private member cannot satisfy a structural interface" comment — the facade layer is pure ceremony.
  evidence: session.ts:264-337 (13 Claims delegates), 346-360 (Scripts), 363-377 (Views), 385-422 (Liveness), 432-442 (ReadGate); the structural-interface comment recurs at 216, 221, 225, 339, 419, 748, 1022, 1059, 1263, 1272, 1354, 1367, 3502.
  proposal: Make the sub-objects public readonly (`session.claims`, `session.views`, ...) and let tools.ts/mirror.ts/render.ts call through them; the Host Pick<> types keep working since the members they name stay public. Removes ~150 lines and the repeated comment.
- **[CODE]** session pair — deliverable/engine/session.ts:1415
  claim: session.ts still holds two coherent, extractable subsystems: the route machinery (~500 lines) and the pull answer-shaping surface (~750 lines) — the same host-interface pattern that already carved out Claims/Views/Scripts/Liveness/ReadGate applies cleanly to both.
  evidence: Route seam: declForPrefix (1415), expandNode (1448), entryWeight (1497), routeAim (1595), nextObjective (1612), subObjective (1645), deepOwed (1671), branchReturn (1719), routeJudgments (1747), routeReadList (1771), route/routeNow (1788-1857), stateAt (1860), routeFan (1874), popOutNexts (3740) — pure "expand the drawing and search it". Pull seam: pull (2299), pullStepGate (2483), takeReadProof (2545), pullSaveOrChoose (2575), pullPickChoice (2669), pullAfterSweep (2715), doAdvice (2804), waitUnroutable (3079), fillAdvice (3133), drawnNote (3118), refusedBlock (3173), doorOption/optionsAt/pullOptions/pullHere (2175-2296), sweep (2067) — pure answer-shaping. Settings persistence (464-807, ~250 lines) is a third, smaller seam.
  proposal: sessionroute.ts (RouteHost = Pick<Session, machine|claims greens|views|reads|active|declIteration...>) and sessionpull.ts, mirroring ClaimsHost; optionally sessionsettings.ts. session.ts drops to ~1,500 lines of walk core (tick, gate, escape, seedSubs, landing).
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:202
  claim: Claims.stalledClaim builds a fully pull-shaped response (pull:"do", refusal, do-advice) from head/extra closures passed in by Session.pull — pull answer-shaping living in the claims module, the wrong side of the seam its own file header declares.
  evidence: sessionclaims.ts:202-230: returns `{ pull: "do", ...head(), stopped_at, refusal: {...}, do: ..., ...extra() }` with `source: "engine/session.ts route"`; the file header (1-9) says the module is about making/reading/explaining claims.
  proposal: stalledClaim returns the stall facts (a Blocker, as claimBlockers does) and Session.pull shapes the answer — also dissolves the head/extra closure plumbing through the facade.
- **[CODE]** session pair — deliverable/engine/sessionclaims.ts:584
  claim: sessionclaims.ts was "lifted out of Session whole" and still contains three separable concerns: the green computation (~300 lines), the grey diagnosis (~450 lines), and state-form CRUD (~800 lines).
  evidence: File header line 3: "Lifted out of Session whole." Green: standingClaims/lawProvenClaims/drawingDone/recordDone/recordPaint/blessedGates/suspectStates/driftReopen/signedTimes/staleFeeders/wouldFall (436-721, 808-818). Diagnosis: claimStall/stalledClaim/ownClaimProblems/fallenRemedy/claimBlockers/stateBlockers/feederFault/feedersUnsigned/greyRoots/whyGrey/walkBackToFault (155-230, 1330-1744, 1809-1839). CRUD: stateForm* + reopen/amend/bless/mint/flip/scenario (232-429, 727-1311, 1746-1806).
  proposal: Split along those three seams (sessiongreen.ts, sessionwhy.ts), sharing the existing ClaimsHost; the diagnosis half depends on the green half one-directionally, so the cut is clean.
- **[LINT]** session pair — deliverable/engine/session.ts:2440
  claim: Long served-string literals are pasted repeatedly: the submit-flag fill instruction appears 3 times verbatim and "the dial alone cannot wake you" 6 times in slightly different wordings — edits to one copy drift the others.
  evidence: grep -c "there is no submit verb, and a pull without the submit FLAG" session.ts → 3 (lines 2440, 2532, 2758); grep -c "dial alone cannot wake you" session.ts → 6 (860, 2394, 2497, 2729, 3094, and packet advice); the stall advice sentence "the stopped step says what it wants — do that, then pull again" also lives in both session.ts:2805 and sessionclaims.ts:227.
  proposal: A small served-strings module of named constants for instructions used more than once; the lint is duplicate-literal detection above ~60 characters.
  lint: lint: no string literal over 60 chars may appear twice in the engine
- **[T]** session pair — deliverable/engine/session.ts:1182
  claim: escape() takes a `_channel` parameter it never uses, and computes a full packet() (condition status for every active state) that its main caller — the pull — throws away, reading only `.escaped`.
  evidence: session.ts:1182 `escape(reason: string, _channel: Channel = "agent")`; returns `{...this.packet(), escaped, note}` at 1230-1234 and 1256-1260; pull (2336-2343) uses only `out.escaped` and builds its own wait answer. The mirror (mirror.ts:325) does use the full result, so the packet stays for that door.
  proposal: Drop `_channel`; have escape return `{ escaped }` plus a `packet()` the mirror route composes itself, so the agent path stops paying for a discarded packet.
  lint: noUnusedParameters already catches the underscore-suppressed argument if the suppression is removed
- **[KEEP]** session pair — deliverable/engine/session.ts:106
  claim: The empty RESTRICTED set ("Nothing is restricted today") looks dead — `all && !RESTRICTED.has(tool)` always passes — but it is a documented extension point with a dsp anchor, cheap to keep and expensive to rediscover.
  evidence: session.ts:105-106 `const RESTRICTED: ReadonlySet<string> = new Set<string>();` with `see dsp-lane-door.md#nothing-is-restricted-today`; sole use at 2918.
  proposal: Keep. Removal would save two lines and delete the seam the doc names.
- **[CODE]** session pair — deliverable/engine/session.ts:1448
  claim: Qualified-id splitting (`lastIndexOf("/")` → prefix/id) is hand-rolled at least ten times across the pair, alongside three other spellings of the same idea (Session.qual, qualHere, bareState, resolveShort) — a tiny qualified-name module would delete all of them and give visitState its one home.
  evidence: the cut/prefix/id triple at session.ts:1448-1451, 1596-1598, 1613-1615, 1721-1725, 1747 (via steps), 1860-1865, 2216-2218, 2312, 2664, 3747-3749; sessionclaims.ts:749-751, 1676, 1697; plus Session.qual (1438) and qualHere (1396).
  proposal: A `qualid.ts` exporting split(q) → {prefix, id}, qual(prefix, id), bare(q), and visitState; replace the hand-rolled splits.
  lint: lint: `lastIndexOf("/")` outside qualid.ts flags a hand-rolled split

---

# v2:06b179cfe8214cfee8be6e083f5812d10bc815d8d3d31dcfa61420356971fd47 — 22 findings

Swept files 1-143 (alphabetical) of spec/trace/requirement, reading every frontmatter and body and spot-checking each behavioral claim against deliverable/engine and deliverable/tests. 118 of 143 files are KEEP; the corpus is in strong shape — statements are falsifiable, refines edges all resolve, and every raid/uc/vp/sty node cited by these rows exists. The dominant defect class is Erroneous-by-success: requirement bodies that describe their motivating defect in present tense ('the six sites that decide it today', 'mergeAndRetire runs git rm... that line goes') after the fix landed, so a reader is told the engine still misbehaves in ways it verifiably does not — four instances, one proposed lint. Second class: broken evidence references — one wrong node id (req-a-refusal-carries-its-remedy for req-refusal-carries-remedy), one wrong path (guidance/CLAUDE.md), and 35 files citing .se/req-mine files that exist neither on disk nor in git history. Third: drift against newer rulings — the three-tier autonomy statement versus the shipped five-rung ladder, 'the slider' vocabulary versus the categorical row that retires it, 'the record's tree' versus i34's one tree, and req-container-offers-its-records' Behaviour both contradicting the newer i34 no-choice-no-entry row and naming a 'choose' instruction the engine never emitted. Cosmetic: eleven files with a doubled '## Detail' heading and four with duplicate source_refs entries — both trivially lintable. Three rows admit in their own bodies that their ids are misnomers with the rename still owed; that sweep is proposed once, for all three.

- **[E]** requirements corpus 1-143 — spec/trace/requirement/req-a-served-instruction-names-the-next-act.md:17
  claim: source_refs cites req-a-refusal-carries-its-remedy, a node that does not exist; the real node is req-refusal-carries-remedy.
  evidence: Line 17: '- req-a-refusal-carries-its-remedy'. spec/trace/requirement/ holds req-refusal-carries-remedy.md and no file matching the cited id (checked by script over all trace dirs).
  proposal: Correct the id to req-refusal-carries-remedy.
  lint: Resolve every source_refs entry that parses as a node id against the trace corpus; flag misses (see the broken-refs LINT below).
- **[E]** requirements corpus 1-143 — spec/trace/requirement/req-a-records-own-status-decides-whether-it-is-open.md:21
  claim: The Detail asserts in present tense that six sites decide open-ness by asking the filesystem, and the code no longer does.
  evidence: Lines 21-23: 'THE SIX SITES that decide it today, each by asking the filesystem: itList, engine/iterations.ts:77 — open: existsSync(path)'. deliverable/engine/iterations.ts:84 now reads 'open: !RECORD_FINISHED.has(status)' — the status field decides, which is this row's own demand, met.
  proposal: Rewrite the Detail to past tense as the motivating measurement; the statement stands and is implemented.
  lint: Stale-code-citation lint: extract engine/<file>.ts:<line> and backticked symbol citations from trace bodies and verify the symbol still exists in the named file.
- **[E]** requirements corpus 1-143 — spec/trace/requirement/req-a-closed-records-folder-stays-on-trunk.md:21
  claim: The Detail describes the i34 change as pending ('That line goes') while naming a function, mergeAndRetire, that no longer exists anywhere in the engine.
  evidence: Line 21: 'WHAT CHANGES. mergeAndRetire at engine/records.ts runs git rm -r -q --ignore-unmatch'. grep for mergeAndRetire, 'git show' and 'cat-file' across deliverable/engine returns nothing — the removal it promises already happened.
  proposal: Rewrite the Detail to past tense; the requirement is met and the body should say so instead of describing the change as future.
  lint: Same stale-code-citation lint as above.
- **[E]** requirements corpus 1-143 — spec/trace/requirement/req-help-query-logged-with-result.md:13
  claim: source_refs cites guidance/CLAUDE.md, a path that does not exist; the quoted content lives in the repo-root CLAUDE.md. The row also largely restates req-every-call-logged for one tool.
  evidence: Line 13: '- guidance/CLAUDE.md "the lane — the tools, and the cage around them"...'. ls guidance/CLAUDE.md and project/guidance/CLAUDE.md both fail; the quoted sentence 'Every call is logged raw to .se/calls.jsonl' is in the repo-root CLAUDE.md. req-every-call-logged (same dir) already binds 'exactly one log line per dispatched call' for every lane call, se_help included.
  proposal: Fix the path. Owner decides whether the row survives as a constraint (its only unique content is forbidding a second log for hits, distinct from the miss demand-log req-help-miss-is-logged requires) or folds into req-every-call-logged.
- **[S]** requirements corpus 1-143 — spec/trace/requirement/req-container-offers-its-records.md:49
  claim: The Behaviour model's one-open branch ('enter it, as today') is contradicted by the newer req-a-pull-carrying-no-choice-enters-no-iteration, and its 'answer choose' names a pull instruction the engine has never emitted.
  evidence: Lines 49-50: '-> one record open: enter it, as today / -> several open: answer `choose`, listing them'. req-a-pull-carrying-no-choice-enters-no-iteration (i34, owner ruling 2026-08-16) demands 'the engine shall enter no iteration and shall answer with the offer' unconditionally. deliverable/engine/pullnotice.ts:8: 'choose — options ride a `do`'; the walking contract states the engine has never emitted a choose instruction.
  proposal: Retire this row in favor of the i34 row, or rewrite the Behaviour to the do-options shape and strike the one-open auto-enter branch so the two rows agree.
- **[D]** requirements corpus 1-143 — spec/trace/requirement/req-autonomy-is-categorical.md:5
  claim: The statement pins three tiers (operational, tactical, strategic, emergency above); the shipped scale is five rungs plus blocked with emergency riding the top rung, so the hardcoded list drifted while the categorical demand held.
  evidence: Line 5: 'a categorical tier - operational, tactical, strategic, with emergency above'. deliverable/machines/scale.md: 'FIVE RUNGS, plus blocked... none, then mechanical, operational, tactical, strategic, ideation — emergency rides with ideation' (owner cut-over ruling 2026-08-12).
  proposal: Restate to demand a categorical ladder read from the scale file, without hardcoding the rung names the scale file owns.
- **[D]** requirements corpus 1-143 — spec/trace/requirement/req-gate-needs-a-persons-verdict.md:31
  claim: The Detail names the autonomy control 'the slider' four times — vocabulary req-autonomy-is-categorical retires — and the id contradicts the statement, which the row itself calls out as having been wrong.
  evidence: Lines 8, 31, 32, 51 all say 'the slider' ('Where the gate outweighs the slider...'). req-autonomy-is-categorical:5 demands 'no slider shall remain on any surface, state note or guidance page'. The row's own Detail: 'This requirement used to read "a person's verdict" everywhere, and it was wrong' — yet the id still reads gate-needs-a-persons-verdict.
  proposal: Sweep slider to dial/rung in the body; queue the id rename with an inbound-link sweep alongside the other admitted misnomers.
- **[D]** requirements corpus 1-143 — spec/trace/requirement/req-a-method-change-reaches-every-tree.md:57
  claim: The id is an admitted misnomer whose rename has been 'owed as a sweep' since 2026-08-14 and never done, and the row parks an unresolved adjudication in the corpus with no owner named.
  evidence: Lines 57-58: 'THE ID IS A MISNOMER. It reads reaches-every-tree and the row does not ask for that... the rename is owed as a sweep'. Line 108: 'THE ADJUDICATOR DECIDES. Both readings are recorded here' — no register entry or owner is cited for that pending decision. The mechanism claim holds: Session.laneRoot exists at deliverable/engine/session.ts:940.
  proposal: Run the owed rename sweep (three rows now admit id misnomers in-file: this one, req-an-engine-change-applies-in-its-own-record, req-gate-needs-a-persons-verdict) and put the contested-restoration question in front of the owner so the ruling lands somewhere trackable.
- **[D]** requirements corpus 1-143 — spec/trace/requirement/req-guidance-edit-lands-where-it-compiles.md:26
  claim: The Detail table routes method cards to 'the record's tree', a tree i34 deleted — one tree now holds everything.
  evidence: Line 26: '| method cards read at state entry | the record's tree |'. deliverable/engine/paths.ts:308: '`fansOut` IS GONE (i34)'; '.worktrees' survives in the engine only inside skip-directory lists (tables.ts:138, vault.ts:39). req-every-record-path-resolves-in-one-tree (same dir) records the one-tree law.
  proposal: Rewrite the table for the one-tree world; keep the vendored-overlay row, which still holds. Also carries a doubled '## Detail' heading (see LINT).
- **[D]** requirements corpus 1-143 — spec/trace/requirement/req-a-preflight-check-asks-the-reader-where-it-looked.md:48
  claim: The Detail says preflight checks its own copy of the configuration path; preflight now imports the path from the same module the reader uses, so the motivating defect is gone.
  evidence: Line 48: 'copy of the path, so the two go stale together and the guard the comment promises is not there'. deliverable/engine/bin/preflight.ts:12: 'import { brandPath, palettePath } from "../brand.ts"' — the same module deliverable/engine/render.ts:19 imports. One occurrence per path, which is this row's own measure, met.
  proposal: Past-tense the Detail; the statement stands as the regression guard.
- **[T]** requirements corpus 1-143 — spec/trace/requirement/req-desk-offers-a-tour.md:15
  claim: The frontmatter carries an empty weighs_against: key dangling with no value.
  evidence: Lines 15-16: 'weighs_against:' immediately followed by the closing '---'. The only such file in the first 143 (checked by script).
  proposal: Delete the empty key, or fill it if a judgment was meant.
- **[T]** requirements corpus 1-143 — spec/trace/requirement/req-call-answers-in-one-second.md:14
  claim: source_refs lists the same two entries twice, differing only in letter case.
  evidence: '.se/req-mine-v1.md: the mirror — book, report, hand-off' appears twice and '.se/req-mine-v2.md: the loop and serving' appears twice within one source_refs block (case-insensitive duplicate check by script).
  proposal: Drop the duplicates.
  lint: Duplicate-source_refs lint: flag case-insensitive duplicate entries within one block. Four files in the first 143 hit it.
- **[T]** requirements corpus 1-143 — spec/trace/requirement/req-broken-trace-is-a-defect.md:14
  claim: source_refs lists 'uc-trace-a-decision-to-its-origin ext 3a' twice.
  evidence: The entry appears at two positions in the same source_refs block (script check).
  proposal: Drop the duplicate.
- **[T]** requirements corpus 1-143 — spec/trace/requirement/req-close-serves-its-findings.md:13
  claim: source_refs lists 'uc-close-a-record step 2' twice.
  evidence: The entry appears at two positions in the same source_refs block (script check).
  proposal: Drop the duplicate.
- **[T]** requirements corpus 1-143 — spec/trace/requirement/req-land-demands-fresh-green.md:34
  claim: The Detail carries two near-identical section leads, a copy-paste doubling.
  evidence: 'What the gate demands of the battery, and how it reads the answer:' and, a few lines later, 'What the gate demands of the battery:' each introducing a bullet list in one Detail section.
  proposal: Merge the two lists under one lead.
- **[LINT]** requirements corpus 1-143 — spec/trace/requirement/req-answer-recorded-with-question.md:20
  claim: Eleven of the first 143 requirement files carry the heading '## Detail' twice in immediate succession — a copy-paste class no check catches.
  evidence: Script count over files 1-143: req-answer-recorded-with-question, req-bless-outputs-ride-the-bless, req-comparison-carries-both-sides, req-controls-never-advance-walk, req-coverage-checked-both-ways, req-drain-one-home-with-payload, req-filter-draws-only-what-serves, req-first-green-needs-a-red, req-gate-rounds-stay-readable, req-guidance-edit-lands-where-it-compiles, req-ideation-opens-no-record — each with 2 '## Detail' headings.
  proposal: One sweep fixes all eleven; then arm the lint.
  lint: Corpus lint (prose-inspect or the write guard): refuse or report a node body repeating the same heading consecutively.
- **[LINT]** requirements corpus 1-143 — spec/trace/requirement/req-acts-carry-role-and-channel.md:14
  claim: 35 of the first 143 files cite .se/req-mine-v1.md, v2 or sebots as evidence, and those files exist neither in the tree nor anywhere in git history — unfollowable references presented as primary sources.
  evidence: Script count: 35 of 143 files carry a '.se/req-mine' source_ref. .se/ holds no req-mine file; 'git log --all -- .se/req-mine-v1.md' (both roots) returns nothing, so no committed ref reaches them. Voice rule: 'THE EVIDENCE IS A REFERENCE SOMEBODY CAN FOLLOW'. Two more broken refs found the same way: the wrong node id in req-a-served-instruction-names-the-next-act and the wrong path in req-help-query-logged-with-result.
  proposal: Either commit the mining files somewhere reachable, or mark these citations 'primary not reachable' per the voice rule; do not delete the rows over it — age is not a defect.
  lint: Broken-source_refs lint: for every source_refs entry that parses as a repo path or a node id, check it resolves on disk or at a committed ref; report misses. The corpus already demands this of guidance (req-guidance-names-only-what-the-engine-has) and of ref: branches (req-every-ref-the-corpus-cites-resolves-on-arrival) but nothing sweeps requirement source_refs.
- **[LINT]** requirements corpus 1-143 — spec/trace/requirement/req-a-pull-carrying-no-choice-enters-no-iteration.md:29
  claim: Requirement bodies cite engine symbols and line numbers in present tense that drift silently once the code moves — four instances found in this half alone.
  evidence: markStarted cited 'at engine/iterations.ts:233-241', now at :186; 'itList, engine/iterations.ts:77 — open: existsSync(path)', now status-based at :84; mergeAndRetire cited and gone entirely; preflight's own-path-copy cited and gone. Each body still reads as a claim about today.
  proposal: Arm the lint, then sweep the four named bodies to past tense in one pass.
  lint: Extract engine/<file>.ts:<line> and backticked symbol citations from spec/trace bodies; verify the symbol exists in the named file (line drift tolerated, symbol absence flagged).
- **[KEEP]** requirements corpus 1-143 — spec/trace/requirement/req-every-record-path-resolves-in-one-tree.md:24
  claim: Notable KEEP: every symbol the row says must not exist is verified gone.
  evidence: Roots.bound, machineRootOf, fansOut, methodFilesIn, setMethodMirror, fanOutMethod — grep across deliverable/engine finds none; deliverable/engine/paths.ts:308 records '`fansOut` IS GONE (i34)', and recordOwnerOf/pathKind survive at paths.ts:292/298 exactly as the row says they should.
  proposal: None — the row is met and stays falsifiable.
- **[KEEP]** requirements corpus 1-143 — spec/trace/requirement/req-a-fallback-fires-when-its-condition-fails.md:752
  claim: Notable KEEP: the fallback edge role and completeState both stand where the row points.
  evidence: deliverable/engine/machine.ts:16 declares EdgeRole including 'fallback'; completeState at machine.ts:649; machine.ts:590 activates OR paths (alternative, recovery, fallback, error) directly.
  proposal: None.
- **[KEEP]** requirements corpus 1-143 — spec/trace/requirement/req-guidance-names-only-what-the-engine-has.md:24
  claim: Notable KEEP: the mechanical check the row promises exists.
  evidence: deliverable/tests/guidance-verbs.test.ts is present, exactly as cited in source_refs.
  proposal: None.
- **[KEEP]** requirements corpus 1-143 — spec/trace/requirement/req-bm25-returns-ranked-candidates.md:6
  claim: Notable KEEP: the BM25 sibling the three bm25 rows govern is built.
  evidence: deliverable/engine/disposition.ts and deliverable/engine/tools-query.ts both carry BM25 code (grep).
  proposal: None — covers req-bm25-returns-ranked-candidates, req-bm25-candidates-need-disposition, req-bm25-below-threshold-returns-empty.

---

# v2:4a00fae89649e11191779c38b265ea28febeb1b0b521cad40caf8b7a4b93dd90 — 19 findings

Swept files 144-285 (142 requirements) of spec/trace/requirement/, reading every frontmatter and body, then spot-checking cited engine code, tests, machines, and RUNME.ps1. The corpus is largely healthy: 101 of 142 files are clean KEEPs, every refines target resolves, and all 17 test files cited as reverse-engineering sources exist. The substantive defects are stale narration: req-walk-resumes-from-repo still demands serving a written handover the owner retired 2026-08-07 (E), and four rows (scoped-run timings, RUNME export, srcOf actor inference, se-mcp --version) narrate pre-fix code as current after their fixes landed (D) — a class the corpus itself names in req-mirror-stays-on-the-machine's body, so it wants the lint, not more prose. One dangling relation survived a deletion: req-unshipped-dependency-refused weighs against req-force-release-recorded, retired with the machine-locking spec at i34. Three mechanical classes recur: 13 files carry a duplicated "## Detail" heading from one i1 template bug; 7 files write weighs_with as a scalar (including the literal "none", which mergeEqualities in stateform.ts turns into an equality judgment against a phantom node — the guard its sibling function has at line 1188 is missing at line 448); and ~11 rows carry path-shaped source_refs ("engine/...", "tests/...") that resolve nowhere from the repo root, one naming the deleted worktree.ts. Two CODE findings: the mergeEqualities guard, and access-control-allow-origin:* now on three mirror endpoints including /api/cards (which serves method cards a vehicle keeps private) where the justifying comment covers only /api/alive. One cross-document drift: CLAUDE.md's "the project root is the parent of the folder you have open" contradicts req-the-machine-state-sits-in-the-folder-that-is-open, and the in-flight v3 restructure sides with the requirement.

- **[E]** requirements second half — spec/trace/requirement/req-walk-resumes-from-repo.md
  claim: The row still demands that boot serve a written handover, a mechanism the owner retired on 2026-08-07.
  evidence: Detail: "Where a handover was written for the next session, the boot shall serve its content before the first step of new work" and scenario response "serves any handover written for the next session" — against spec/trace/design-spec/dsp-walk-machine.md ~line 1925: "THE WRITTEN HANDOVER IS GONE (owner ruling 2026-08-07)... boot DERIVES the briefing instead of asking anyone to write it", and CLAUDE.md rule 8: "THERE IS NO HANDOVER FILE, and nobody writes one".
  proposal: Rewrite the handover clause to demand the derived briefing (boot describes the last session from the call log); keep the resume-from-repo core unchanged.
  lint: A lint flagging requirement bodies that name retired mechanisms listed in retirement rulings (raid-dec-* retire entries).
- **[E]** requirements second half — spec/trace/requirement/req-refusal-carries-remedy.md:19
  claim: One source_refs entry is YAML-mangled garbage.
  evidence: Frontmatter source_refs last item reads literally: - '- \".se/req-mine-v2.md: the edit model and the file lane' — an escaped quote and a nested dash inside a single-quoted string, unclosed as a citation.
  proposal: Repair to ".se/req-mine-v2.md: the edit model and the file lane".
  lint: Lint: a source_refs entry may not begin with a dash or an unbalanced quote.
- **[D]** requirements second half — spec/trace/requirement/req-scoped-run-records-its-timings.md
  claim: The body narrates the defect as current — "The scoped path does not [record timings]... engine/tools.ts builds its own argv carrying --test-reporter=tap and nothing else" — but the fix landed.
  evidence: deliverable/engine/tools-run.ts:24 imports testReporterArgs from testreporters.ts; deliverable/engine/testreporters.ts:32 attaches bin/test-timings.mjs; deliverable/engine/bin/selftest.ts:66: "shared with the scoped path so the two cannot drift". Also tools.ts no longer holds the run path (tools-run.ts does).
  proposal: Cut the today-narration; the corpus's own rule (req-mirror-stays-on-the-machine Detail: "VERIFICATION STATUS BELONGS IN EVIDENCE, NEVER IN A REQUIREMENT BODY") says exactly why.
  lint: See the stale-narration lint proposed on req-the-actor row.
- **[D]** requirements second half — spec/trace/requirement/req-one-command-produces-a-complete-copy.md
  claim: The body says "RUNME.ps1 --export <folder> <name> <abbr> at lines 57-155 produces a real independent repository today" — RUNME.ps1 no longer carries an export path.
  evidence: RUNME.ps1 lines ~55-74: the export was superseded by lane-run producer buttons — "This will supersede some of the stuff in RUNME, so you can delete it from there... The act now runs inside the lane"; grep for "--export" in RUNME.ps1 finds only comments. The lane now offers se_produce_project / se_produce_vehicle.
  proposal: Repoint the "What already exists" section at the lane producer (el-vehicle-producer), keeping the facet table unchanged.
- **[D]** requirements second half — spec/trace/requirement/req-record-opens-on-word.md
  claim: source_refs cite contract rules by numbers that the contract has since renumbered, and one ref is duplicated.
  evidence: source_refs: "contract rule 8 (never open a record unasked)" and "contract rule 5 (confirm before you compose)" — CLAUDE.md now numbers these "## 9. Never open a record unasked" and "## 6. Confirm before you compose". "uc-get-work-routed ext 5a" appears twice in the same list.
  proposal: Cite contract rules by name only; dedupe the list.
  lint: Lint: a source_ref matching "contract rule N (name)" must find a contract heading whose number and name agree; duplicate entries in refines/source_refs refuse.
- **[D]** requirements second half — spec/trace/requirement/req-the-actor-is-recorded-where-the-call-is-served.md
  claim: The source_refs assert present-tense code behavior that the fix has since removed — the requirement is met and its evidence anchors describe deleted code.
  evidence: source_ref: "engine/render.ts: srcOf returns tool.startsWith(\"mirror_\") ? \"human\" : \"agent\"" — deliverable/engine/render.ts:574-579 now reads the recorded actor first ("if (actor === \"human\" || ...) return actor") and falls back only for unstamped records, which the row itself permits.
  proposal: Date-stamp the refs as the minting-time measurement or move them to the iteration's evidence.
  lint: Lint: a source_ref quoting code (contains ".ts:" or a code snippet) must either match the live file or carry a date marking it a measurement.
- **[D]** requirements second half — spec/trace/requirement/req-the-entrypoint-answers-its-version-without-starting.md
  claim: source_ref "engine/bin/se-mcp.ts: --help exists and --version does not" is now false — the flag was built.
  evidence: deliverable/engine/bin/se-mcp.ts:94: if (argv.some((a) => a === "--version" || a === "-v" || a === "-V")) — and the help text documents it at line 167.
  proposal: Same treatment as req-the-actor: date the ref as the pre-fix observation. Requirement itself holds (KEEP-grade demand, satisfied).
- **[D]** requirements second half — spec/trace/requirement/req-reachable-capability-is-traced.md
  claim: The body claims "All 35 verbs sit in a use case" in present tense; the live verb count is 40.
  evidence: deliverable/tests/trace-coverage.test.ts comment: "40 AFTER TWO VERBS ARRIVED ON DIFFERENT BRANCHES AND MET AT A MERGE. se_benchmark... se_coverage..." — the row's own ratchet history (35 at i3, 36, 34 at i34, 36 at i16) shows the number moves every few iterations.
  proposal: Replace hard counts in the body with "every registered verb" and leave counting to the test that already ratchets it.
- **[D]** requirements second half — spec/trace/requirement/req-unshipped-dependency-refused.md
  claim: weighs_against names req-force-release-recorded, a requirement deleted when the machine-locking specification was retired whole at i34; refines also lists uc-open-an-iteration twice.
  evidence: spec/trace/raid/raid-dec-the-machine-locking-specification-is-retired-whole.md:50 lists "req-force-release-recorded" among the retired rows; no file spec/trace/requirement/req-force-release-recorded.md exists; git cf258197 ("the claim system out of the trace"). The frontmatter refines block repeats uc-open-an-iteration.
  proposal: Drop the dangling weighs entry, dedupe refines. Note guard.ts:26-27 already covers weighs_* keys but only REPORTS — the report evidently reaches nobody.
  lint: Surface guard.ts dangling-reference reports as a failing check in the trace-coverage battery for the requirement corpus.
- **[T]** requirements second half — spec/trace/requirement/req-test-scope-discipline.md
  claim: refines lists uc-answer-a-question-with-tests twice — the duplicate adds nothing.
  evidence: Frontmatter refines block: uc-land-work-on-trunk, uc-let-the-system-catch-up, uc-answer-a-question-with-tests, uc-answer-a-question-with-tests.
  proposal: Dedupe.
  lint: Same duplicate-entry lint as req-record-opens-on-word.
- **[LINT]** requirements second half — spec/trace/requirement/req-open-notes-stay-visible.md
  claim: Thirteen files in this half carry a duplicated "## Detail" heading immediately followed by a second "## Detail" — one template bug stamped across the i1 mint.
  evidence: Verified by script over files 144-285: req-open-notes-stay-visible, req-option-carries-cost-and-shed, req-red-is-never-carried, req-rejection-carries-its-reason, req-roles-never-usernames, req-size-choice-is-the-bless, req-size-proposal-names-strikes, req-stray-captured-in-one-call, req-sweep-covers-every-drift-class, req-test-result-is-structured, req-tour-highlights-the-named-part, req-tour-reads-what-stands, req-two-options-beyond-the-obvious — each contains the run "## Detail\n\n## Detail". All are minted_in: i1.
  proposal: One mechanical sweep deletes the duplicate line in all 13 (first half likely holds more).
  lint: Lint: a trace node body may not repeat an identical section heading.
- **[LINT]** requirements second half — spec/trace/requirement/req-outbound-query-logged.md
  claim: weighs_with appears in two shapes — a YAML list in most rows, a bare scalar in seven — and "no entries" is written three ways (key absent, scalar none, list item none).
  evidence: Scalars found by script: req-outbound-query-logged, req-reject-names-the-redo, req-the-benchmark-history-is-unreadable-while-a-run-is-bound (weighs_with: none), req-the-desk-is-usable-soon-after-the-folder-opens, req-tour-shows-live-instances, req-tour-speaks-plainly, req-what-the-corpus-is-has-one-answer. nodeList tolerates the scalar (deliverable/engine/stateform.ts:1271), so nothing breaks today — the next parser that reads the block form only will silently drop seven relations.
  proposal: Normalize to list form; standardize the empty case to omitting the key.
  lint: Lint: weighs_with/weighs_against must be block lists; the literal value none refuses.
- **[CODE]** engine — deliverable/engine/stateform.ts:448
  claim: mergeEqualities pushes weighs_with targets into the order walk's judgments without checking the target exists, unlike its sibling — so the none placeholder six i36/i37 rows use rides in as an equality judgment against a phantom node.
  evidence: stateform.ts:448-458 has no existence check before out.push({a: id, b: other, verdict: "="}), while criterionAxisItems at stateform.ts:1188 guards with "if (!ids.has(other)) continue;". req-the-benchmark-history-is-unreadable... carries weighs_with: none and req-repeated-failure-shape-becomes-durable-work carries "- none" — both produce {b: "none", verdict: "="}.
  proposal: Add the ids.has guard to mergeEqualities (or special-case none) so a placeholder cannot become a judgment.
  lint: The guard itself is the mechanism; plus the weighs-format lint above removes the input.
- **[CODE]** engine — deliverable/engine/mirror.ts:758
  claim: access-control-allow-origin: * now stands on three mirror endpoints including /api/cards, which serves the method cards — for a vehicle these can carry private overlay guidance, and the star lets any web page in the same user's browser read them cross-origin despite the loopback bind.
  evidence: mirror.ts:744 (/api/levels), :758 (/api/cards — res.end(JSON.stringify({cards: loadCards(o.root)}))), :800 (/api/alive) all set "access-control-allow-origin": "*". The comment at :793-799 justifies the star only for /api/alive ("an EMBEDDER's page (the VS Code webview) polls") and only on the alive rationale; req-mirror-stays-on-the-machine's source_ref likewise says the star is "on the alive endpoint" (singular). Loopback binding (mirror.ts:1008) stops the network, not the user's own browser.
  proposal: Restrict the star to /api/alive, or serve cards/levels with the webview's actual origin; update the requirement's source_ref either way.
  lint: Test: assert the set of endpoints carrying the star equals an allowlisted set with a recorded reason each.
- **[LINT]** requirements second half — spec/trace/requirement/req-mirror-stays-on-the-machine.md
  claim: Path-shaped source_refs across at least eleven rows use prefixes that resolve nowhere from the repo root — "engine/..." and "tests/..." where the tree holds deliverable/engine/ and deliverable/tests/ — and one names a deleted file.
  evidence: Instances: req-mirror-stays-on-the-machine ("engine/mirror.ts"), req-narration-toll-is-collected ("tests/narration.test.ts and tests/feed.test.ts"), req-shutdown-fires-only-idle-or-end ("tests/power.test.ts"), req-structure-verdicts-are-mechanical, req-query-is-the-file, req-reader-keeps-their-place, req-table-refuses-what-it-cannot-draw, req-table-rows-derive-from-notes, req-the-panel-s-paint-says-which-kind-of-green-it-is ("engine/renderstyle.ts"), req-the-size-is-read-by-one-extractor, req-one-operation-reads-its-input-once. req-what-the-corpus-is-has-one-answer cites "worktree.ts:125" — deliverable/engine/worktree.ts no longer exists (ls confirms), while other rows (req-no-state-demands-what-it-cannot-supply) already use the full "deliverable/engine/..." form that resolves.
  proposal: Sweep the prefixes to the resolving form; mark refs to deleted files as dated measurements.
  lint: Lint: a source_ref containing a path-shaped token ending .ts/.md/.mjs must resolve from the repo root or carry a ref/date marker.
- **[D]** contract vs requirement — CLAUDE.md
  claim: The contract's lane section says the project root is the parent of the folder you have open, while req-the-machine-state-sits-in-the-folder-that-is-open (must, crippling) demands the open folder IS the product root — and the v3 restructure moving everything out of project/ to the repo root sides with the requirement, leaving the contract's sentence stale.
  evidence: CLAUDE.md lane section: "PATHS ARE ROOT-RELATIVE TO THE PROJECT ROOT, which is the parent of the folder you have open. You open project/" — against spec/trace/requirement/req-the-machine-state-sits-in-the-folder-that-is-open.md statement: "the product's root folder shall be the folder that is open"; git status on v3 shows project/* deleted and the same tree now standing at the repo root.
  proposal: Owner decision: once the restructure lands, rewrite the CLAUDE.md path paragraph; until then the contradiction misroutes every path a caged agent writes.
- **[KEEP]** requirements second half — spec/trace/requirement/req-mirror-stays-on-the-machine.md
  claim: The core demand is met and verifiable: the mirror binds loopback explicitly.
  evidence: deliverable/engine/mirror.ts:1008: server.listen(o.port, "127.0.0.1", ...) — and the body's rule that verification status stays out of requirement bodies is the right cure for four other rows in this sweep.
  proposal: Keep; fix only the source_ref path prefix and the singular "alive endpoint" claim per the CORS finding.
- **[KEEP]** requirements second half — spec/trace/requirement/req-the-full-battery-runs-where-the-method-says.md
  claim: Spot-checked and holding: the verification row exists with the engine as filler exactly as cited.
  evidence: deliverable/machines/rigor_matrix/rows/M7_50_verification.md:6: filled_by: engine.
  proposal: Keep.
- **[KEEP]** requirements second half — spec/trace/requirement/req-trees-never-mix.md
  claim: The deleted-in-error-and-restored history in the body checks out and is the strongest argument in the corpus for why near-duplicate-looking rows need their scope sentences.
  evidence: Body documents i34's wrong retirement and same-day restore, naming the verifier's one-sentence catch against i27's acceptance line; the three overlay rows it cites (req-overlay-resolution, req-overlay-survives-update, req-overlay-drift-reported) all stand.
  proposal: Keep.

---

# v2:443bba9f51210589f53381eaaf132383736576c98089b3f98bec4e625e64256d — 16 findings

Swept deliverable/tests/ (169 test files, 1,564 test cases): helpers.ts and the 15 largest files read in full, the rest skimmed by test names, every ratchet re-counted independently. The suite is in unusually good shape — every ratchet is honest to the digit (read ceiling 116 = measured 116, write ceiling 42 = 42, verb count 40 = 40, rigor-matrix pin 53 = 53), no test is assertion-free (11 scan candidates all assert via a throwing refusal() helper), and incident-pinned cases carry dated rationale. The worst defect sits at the suite's trace seam: 11 shipped test-spec nodes name test files that no longer exist (worktree, suspect, ideation, tour, ...) and 156 of 179 path-shaped refs still use the pre-collapse `tests/...` base — nothing in the battery resolves them, so the rot is invisible. The dominant code smell is helper duplication: gitInit copied byte-identically into 6 files (plus 3 variants), refusal() into 10, and an 8-line seed-an-iteration boot sequence into ~12 sites across 7 files, all while helpers.ts is the declared one home — this class wants two new cases in the existing testlint.test.ts rather than more prose. Efficiency: writeguard (8 full boot walks, including one scenario booted twice for split assertions) and mcp (9 boots) pay for isolation the owner has already ruled against paying. String coupling is real but mostly deliberate: ~635 string-coupled assertions, with mirror-contract.test.ts (65) pinning exact client-source substrings that break on any renderclient rename; a marker/constant seam would keep the regression memory without the refactor tax. Orphans: helpers.readDocs (dead export) and fixtures/voice-matrix.base (loaded by nothing, invisible to the vault sweep, and carrying a documented defect).

- **[D]** test-spec trace layer vs the suite — spec/trace/test-spec/tsp-close-and-land.md:16
  claim: 11 shipped test-spec nodes name test files that exist nowhere in the suite, and the layer's path base disagrees with itself: of 179 path-shaped `files:` refs, 156 resolve only under deliverable/ (written as `tests/...` from the pre-collapse layout) and 12 resolve from the repo root.
  evidence: spec/trace/test-spec/tsp-close-and-land.md names `tests/worktree.test.ts`; also missing everywhere: bound-engine.test.ts, suspect.test.ts (tsp-claims-and-drift.md), ideation.test.ts, latency.test.ts, overhaul.test.ts, overlay.test.ts, scaffold.test.ts, setup.test.ts, tour.test.ts. Verified by resolving every `files:` entry against the repo root and against deliverable/ (script run 2026-08-20: 'path-shaped refs: 179, exist at root: 12, exist under deliverable/: 156, missing both: 11'). The engine's own check deliberately skips existence: deliverable/engine/stateform-problems.ts:459 'NAMED, not existing — ... existence gets its teeth at verification, never here' — and nothing in the suite runs that existence check against the shipped corpus.
  proposal: Repair the 11 dead refs (worktree/suspect/ideation etc. were removed or renamed — e.g. one-tree i34 removed worktrees), settle one base (repo-root paths, matching what requirement-checks fixtures already do), and add the lint below so this class cannot recur.
  lint: A testlint-style case: for every spec/trace/test-spec/*.md, each `files:` entry that is path-shaped (not a 'none — ...' prose value) must exist from the repo root.
- **[E]** fallback-outcome.test.ts header — deliverable/tests/fallback-outcome.test.ts:42
  claim: The REPO_ROOT comment describes a directory chain that no longer exists and miscounts the levels the code actually climbs.
  evidence: deliverable/tests/fallback-outcome.test.ts:42 — '/** The repository root — three levels above this file (tests/ → deliverable/ → project/ → root). */' above `new URL("../..", import.meta.url)` (line 43), which climbs TWO levels; the `project/` folder is empty since the folder-levels collapse (verified: `ls project/` at the repo root returns nothing).
  proposal: Correct the comment to 'two levels above this file (tests/ → deliverable/ → root)'.
- **[D]** engine/bin/selftest.ts header (adjacent to the suite) — deliverable/engine/bin/selftest.ts:5
  claim: The selftest header still sizes the battery at '~74s over 359 tests'; the suite today holds 169 test files carrying 1,564 test() cases (plus loop-generated ones).
  evidence: deliverable/engine/bin/selftest.ts:5 'This file is the full battery, ~74s over 359 tests'; counted 2026-08-20: 169 *.test.ts files, 1,564 `test(` occurrences in deliverable/tests/.
  proposal: Update the figure, or drop the count and keep only the duration claim which testreporters measures per run.
- **[R]** helpers duplicated across files — deliverable/tests/drift.test.ts:35
  claim: gitInit is defined locally in 7 test files instead of living in helpers.ts: 6 byte-identical copies (init+config+add+commit 'seed') and one variant, while helpers.ts already exports a template-cached gitInit that does init+config only.
  evidence: Identical bodies at claimops.test.ts:22, drawnsub.test.ts:22, drift.test.ts:35, fallback-outcome.test.ts:29 (renamed gitInitLoop), iterations.test.ts:18, reopen.test.ts:17; gitlane.test.ts:56 re-implements exactly what helpers.gitInit (helpers.ts:431) does, without the per-process .git template cache the helper exists for. nesting.test.ts:100 (gitSeed) and discipline.test.ts:386 (gitRoot) are two more local variants.
  proposal: Give helpers.gitInit a `commit: boolean` option (the template covers init+config; add+commit still runs per root), delete the seven locals, and add the lint below.
  lint: Extend testlint.test.ts (the existing suite-lint home): refuse `function gitInit` / `function gitInitLoop` / `function gitSeed` in any *.test.ts — helpers.ts is the one home.
- **[R]** helpers duplicated across files — deliverable/tests/tables.test.ts:58
  claim: The refusal() assertion helper is redefined in 10 test files — 8 byte-identical sync copies, one async variant, one assert-flavored variant — instead of one export in helpers.ts.
  evidence: bases.test.ts, baseui.test.ts, choice-refused.test.ts (async), emergency.test.ts, expr.test.ts, floor.test.ts, frontmatter.test.ts (assert-flavored), power.test.ts, pull-seam.test.ts, tables.test.ts:58 all define `function refusal(fn ...)` with the same try/catch/instanceof-Rejection body (grep 'function refusal(' *.ts = 10 files).
  proposal: Move refusal() (sync and async overload) into helpers.ts, delete the ten locals. Same lint class as the gitInit finding — one testlint case covers both.
  lint: testlint case: refuse `function refusal(` in *.test.ts.
- **[O]** helpers.ts dead exports — deliverable/tests/helpers.ts:291
  claim: helpers.readDocs is a dead export: it boots a Session to ask the route's reading list, and nothing calls it — its only mention outside the definition is a comment.
  evidence: helpers.ts:291 `export function readDocs(root: string)`; the only other hit in the whole tree is testlint.test.ts:8, a comment ('it derives what the walk owes from the engine's own route (readDocs, checkDocs)'); no engine or vscode file references it either.
  proposal: Delete it (checkDocs covers the live use), or if the testlint comment's story needs it, note why it stays.
- **[T]** helpers.ts export hygiene — deliverable/tests/helpers.ts:396
  claim: guidanceRoots and laneSources are exported with zero external consumers — both are used only inside helpers.ts itself.
  evidence: helpers.ts:396 (guidanceRoots, consumed only at helpers.ts:335 inside checkDocs) and helpers.ts:604 (laneSources, consumed only at helpers.ts:612 inside laneSource); grep across all test files finds no other user of either.
  proposal: Drop the export keyword on both, or leave as-is — cosmetic.
- **[O]** fixtures — deliverable/tests/fixtures/voice-matrix.base:1
  claim: fixtures/voice-matrix.base is read by nothing: no test loads it, and the vault-wide 'every declared view draws' sweep cannot see it because the engine skips the tests directory.
  evidence: tables.test.ts references only fixtures/rigor-matrix.base (line 37) and fixtures/depends.base (line 221); engine/tables.ts:138 `SKIP_DIRS = new Set([... "tests"])` keeps listBases out of deliverable/tests (verified: listBases('.') returns 26 bases, none under tests/). Its only repo mention is spec/bases-port.md:316, which documents a latent defect IN the file ('line 39 lists `- COMMENT` ... The column will render empty').
  proposal: Propose removal to the owner, or wire it into a test the way rigor-matrix.base is — an unused fixture carrying a documented defect is pure liability.
- **[CODE]** string-coupled assertions — deliverable/tests/mirror-contract.test.ts:165
  claim: The suite carries roughly 635 string-coupled assertions (362 assert.match with multi-word patterns + 273 .includes("...")); the heaviest seam is mirror-contract.test.ts with 65, many pinning exact client-source substrings so that renaming a client variable or reformatting renderclient*.ts breaks dozens of tests with no behavior change.
  evidence: Counted 2026-08-20 across *.test.ts. Examples of identifier-level pins: mirror-contract.test.ts:165 `html.includes("settleUntil = Date.now() + 250")`, :124 `html.includes("if (navigatingAway) return;")`, :211 `html.includes("if (id === CARD_NOW) { if (CARD_PREV !== null) promoteCard(CARD_PREV); return; }")`, :560 a multi-line regex over `new EventSource` placement. Top files: mirror-contract 65, scripts 26, files 22, requirement-checks 22, baseui 18, discipline 18.
  proposal: Keep the incident pins (they are this repo's regression memory, and refusal-message matches that quote DATA back are stable), but for the fragile identifier-level subset give the client a stable seam: exported named constants or data-marker attributes the tests assert instead of raw source substrings — or run the client script under a minimal DOM for the two or three genuinely behavioral cases (loading bar, morph). The tradeoff is real: today these are cheap and readable; the cost is refactor friction, paid on every renderclient cleanup.
- **[CODE]** suite wall clock — deliverable/tests/writeguard.test.ts:38
  claim: Several files pay a full boot walk per test where one shared server would do: writeguard.test.ts boots 8 servers, mcp.test.ts 9, boundrules 6, seed-dependency 6, sehelp 5, surveywindow 5 — and writeguard's cases 1 and 2 are the identical scenario booted twice with the assertions split.
  evidence: helpers.ts:497 prices the boot: 'IT COSTS A FULL BOOT WALK (about eight seconds)'. writeguard.test.ts:21-36 and :38-54 run the same refused se_file_write on fresh roots (a refused write leaves the tree untouched, per the file's own asserts), differing only in what they assert about the one refusal. The owner has already ruled for the shared-walk shape: fallback-outcome.test.ts:240-248 quotes them — 'if I have to start a session anyways for every single test, then I might as well run all the tests in one session'.
  proposal: Merge writeguard cases 1 and 2 into one, and share one booted server across the refusal-only cases (they mutate nothing); apply the same to mcp.test.ts's protocol checks. Cuts on the order of 10+ boot walks (~1.5 min of battery wall clock) with no coverage loss.
- **[CODE]** missing fixture helper — deliverable/tests/iterations.test.ts:294
  claim: The 'boot by human hand, seed an iteration, enter it' sequence (~8 lines: advance x2, checkDocs, advance x3, setAutonomy(1), iterationSeed, sid regex, advance('iterations'), advance(sid)) is duplicated about 12 times across 7 files with drifting variants, while helpers.ts has boot helpers for every path except this one.
  evidence: iterations.test.ts has it 5 times (e.g. :294-307, :451-462, :516-526); fallback-outcome.test.ts:310-321, claimops.test.ts:50-60, reopen.test.ts:34-42 and :167-174, drawnsub.test.ts:52-58, feedback-loop.test.ts; nesting.test.ts:112-119 defines yet another local variant (bootHuman). iterations.test.ts also duplicates the read/do drain loop twice with the identical comment (:531-535 and :564-567).
  proposal: Add `seededIterationSession(root, goal, vision)` to helpers.ts returning {session, id, sid}, plus a `drainToFill(session)` for the read/do loop; replace the twelve copies. drift.test.ts's thrice-repeated decl/gate/feeders/signAt scaffolding (lines 217-232, 288-304, 342-356) wants the same treatment as a local helper.
- **[R]** duplicate coverage — deliverable/tests/tables.test.ts:124
  claim: tables.test.ts re-tests the bare-property predicate that expr.test.ts already covers, under the identical test name — the only duplicated test name in the suite.
  evidence: expr.test.ts:257 test("a bare property means it carries something") asserting passes("status")/passes("missing"); tables.test.ts:124 same name asserting matches("depends_on", ...) — tables.matches delegates to the same evaluator one wrapper higher.
  proposal: Keep one (expr.test.ts owns the evaluator; if the tables wrapper adds no logic, drop its copy) or rename to state what the wrapper-level case adds. A testlint case refusing duplicate test names across files would hold the line.
- **[KEEP]** ratchet honesty — deliverable/tests/files.test.ts:145
  claim: Every ratchet in the suite is honest, independently re-counted: the read ratchet (CEILING 116) and write ratchet (CEILING 42) sit exactly at their measured counts with the 20-unit slack guard holding; the verb-count ratchet (40) matches the 40 declared lane verbs; the 53-row rigor-matrix pin matches 53 row files; the trace-corpus guard (>100 requirements) is far under the 285 on disk.
  evidence: files.test.ts:145 CEILING=116 vs counted 116 readFileSync( in engine/ excluding bin/ and notes.ts; files.test.ts:203 CEILING=42 vs counted 42 writeFileSync(; trace-coverage.test.ts:142 expects 40 vs 40 unique `name: "se_*"` declarations across tools*.ts; tables.test.ts:196 pins 53 vs `ls deliverable/machines/rigor_matrix/rows/*.md | wc -l` = 53; trace-coverage.test.ts:68 vs 285 files in spec/trace/requirement/.
  proposal: No action. Also examined closely and worth keeping: every ratchet raise carries a dated reason inline, and the two-sided slack guard stops the ceiling rotting upward or downward.
- **[KEEP]** assertion integrity — deliverable/tests/bases.test.ts:375
  claim: The suite has no assertion-free tests: a scan flagged 11 candidates and all of them assert through the throwing refusal() helper or a throwing boot helper.
  evidence: Scan over all *.test.ts bodies for assert/rejects/throws (script, 2026-08-20) flagged bases.test.ts:375/388/393/404, emergency.test.ts:44/65, expr.test.ts:286/289/307, pull-seam.test.ts:86, editsafety.test.ts:124 — each verified to call `refusal(() => ...)` (throws when no Rejection) or `pullBoot` ('throws if the walk strands', editsafety.test.ts:128).
  proposal: No action; folding refusal() into helpers.ts (see the R finding) keeps this property in one place.
- **[KEEP]** probe maths seam — deliverable/tests/helpers.ts:447
  claim: proofFor is imported from the engine and re-exported by helpers.ts rather than hand-mirrored — the previous mirrored copy went stale the day the engine stopped counting list markers as words, and two tests in iterations.test.ts record that lesson at their call sites.
  evidence: helpers.ts:447-452 'proofFor IS RE-EXPORTED, NEVER MIRRORED (owner, 2026-08-18). It used to be a hand-kept copy ... and the comment did not stop it going stale'; iterations.test.ts:528-530 'this test used to inline its own copy, and the copy went stale'.
  proposal: No action — this is the seam the string-coupled mirror-contract assertions should imitate where they can.
- **[LINT]** suite-lint coverage — deliverable/tests/testlint.test.ts:22
  claim: testlint.test.ts already proves the suite can lint itself (it refuses hardcoded guidance paths); the duplication findings above are all the same class and want two more cases there rather than more prose.
  evidence: testlint.test.ts:22 test('no test names a real guidance path — helpers.ts is the one home') sweeps every *.test.ts line by line, comments excluded — the exact mechanism the new cases need.
  proposal: Add: (1) 'no test file redefines a helpers.ts resident' — refuse `function gitInit(`, `function refusal(`, `function bootHuman(` in *.test.ts; (2) 'every shipped test-spec files: entry resolves' — path-shaped entries in spec/trace/test-spec/*.md must exist from the repo root, prose 'none — ...' entries pass; (3) optionally 'no duplicate test name across files'.
  lint: All three are single readdirSync+regex sweeps in the existing file's shape.

---

# v2:602d12339b9ac257a64d8ad0704055491be1e1f47a3deebc463ef1895841b0d3 — 18 findings

The mechanical orphan list is unreliable for this dimension: it excluded spec/iterations/, so all 4 candidates, all 3 design-specs, and 60 of 82 options it called unreferenced are in fact cited from iteration evidence — the true zero-reference option count is 22. Of those 22, eight are cells of the claim/machine-locking design the owner retired whole in i34 with an explicit written-fresh-not-revived trigger (S), and fourteen split between phrase-considered chart cells, adopted-behavior records whose i1/i2 considering evidence was deleted, and genuine O material for owner triage. The candidates and design-specs all earn their place, with one real defect: cand-b-the-trimmed-spread's content now contradicts its own id, i36's chart row, and an evidence sentence claiming it never changed. Two of six experiments probe deleted subsystems with dangling refs (exp-claim-verb-race, exp-watchdog); the other four are sound dated records folded into live raids. The strongest systemic finding is a rename drift family: pool.ts mints work-tokens at spec/trace/work-token, while dsp-the-options-pool, flow-standing-option and uc-see-the-whole-pool-from-any-clone still specify options with ready_when at spec/trace/option (D×3). The flow/use-case sample found uc-land-work-on-trunk teaching two verbs i34 deleted from the lane (E, duplicated in sty-land-the-work), plus drifted scope-naming in uc-answer-a-question-with-tests and person-only gate blessing in uc-adjudicate-a-gate; 6 of 10 use-cases and 9 of 10 flows sampled match today's engine. Both defect classes are mechanisable: the corpus dangling-ref sweep only checks refines edges (el-satellite-supervisor is cited by 10 nodes and exists nowhere), and the verbs-name-what-exists sweep stops before spec/trace. Not judged: 57 of the 82 listed options beyond the reference re-count, 64 of 74 flows, 43 of 53 use-cases, and the raid/test-spec/work-token portions of the orphan list, which belong to other finders.

- **[E]** orphan-scan input — scratchpad/overhaul-orphans.txt:1
  claim: The mechanical orphan pass excluded spec/iterations/, so most of its 'referenced nowhere' claims are false: all 4 candidates, all 3 design-specs, and 60 of the 82 listed options are referenced by name from iteration evidence.
  evidence: cand-a-the-adopted-baseline-refined is cited at spec/iterations/i36-the-harness-is-not-claude-measure-what-e/evidence/build_chart.md:19 ('| [[cand-a-the-adopted-baseline-refined]] | The adopted baseline, refined |'); dsp-front-desk is cited from 10+ files e.g. spec/iterations/i16-the-vehicle-overlay-a-vehicle-vendors-th/evidence/trace-design.md; a repo-wide re-count found only 22 of the 82 options with zero inbound references.
  proposal: Re-run the orphan scan over every tracked file including spec/iterations before acting on any O in this corpus; treat the current list as advisory only.
  lint: The orphan scan script should glob the whole repo (spec/**, guidance/**, deliverable/**) and assert its own file list against git ls-files, so an excluded directory is loud.
- **[KEEP]** candidates — spec/trace/candidate/cand-the-repository-is-the-state.md:1
  claim: All four listed candidates (cand-a-the-adopted-baseline-refined, cand-b-the-trimmed-spread, cand-the-repository-is-the-state, cand-the-thin-run) are live records of their iterations' design comparisons, each referenced by that iteration's chart, evaluation and winner evidence.
  evidence: spec/iterations/i9-se-and-the-corpus-move-the-machine-state/evidence/build_chart.md, converge-pugh.md, evaluate-set.md all cite cand-the-repository-is-the-state; spec/iterations/i37-training-iterations-a-disposable-iterati/decisions.jsonl cites cand-the-thin-run; i36 evidence cites both cand-a and cand-b (build_chart.md:19-20).
  proposal: Keep all four; they are the recorded consideration the O test was looking for.
- **[D]** candidates — spec/trace/candidate/cand-b-the-trimmed-spread.md:5
  claim: The standing node contradicts both its own id and the chart row that links to it: the file is now 'The defended adapter' (keep harness identification, add a heartbeat) while i36's build_chart row for [[cand-b-the-trimmed-spread]] describes the opposite design (drop per-harness identification for a lowest-common-denominator bound).
  evidence: cand-b-the-trimmed-spread.md:5-6 'name: The defended adapter / statement: Identify the harness from the MCP handshake into a closed type with Unknown … heartbeat'; spec/iterations/i36-…/evidence/build_chart.md:20 'The trimmed spread | Drop per-harness identification in favour of one fixed lowest-common-denominator bound'; i36 evidence the-trimmed-spread.md:17 claims 'Composed at build_chart and unchanged since', which the divergence disproves.
  proposal: Owner decision: either rename/re-mint the node so id, name and chart row agree, or annotate the chart row with when and why the candidate was recomposed; the 'unchanged since' evidence sentence is false either way and should be amended.
  lint: A check that a candidate's name/statement match the chart row that cites it, or at least that an id's slug words appear in its own name field.
- **[KEEP]** design-specs — spec/trace/design-spec/dsp-front-desk.md:1
  claim: All three listed design-specs (dsp-front-desk, dsp-the-install-preflight, dsp-the-one-corpus-reader) realize elements that exist, point at files that exist, and are cited from iteration trace-design/specify-build evidence.
  evidence: realizes targets el-front-desk.md, el-preflight.md, el-corpus-reader.md all present in spec/trace/element/; files deliverable/machines/main.canvas, deliverable/engine/bin/preflight.ts, deliverable/engine/trace.ts, deliverable/engine/stateform.ts all exist; dsp-the-install-preflight still matches RUNME.ps1:374 ('node engine\bin\preflight.ts --root $root') and prepare_idle.md:7 naming it as exit script.
  proposal: Keep all three.
- **[S]** experiments — spec/trace/experiment/exp-claim-verb-race.md:7
  claim: The experiment races a claim verb the owner retired whole with i34, and its probes ref names a raid that no longer exists in the corpus.
  evidence: probes: raid-asm-remote-serializes-claims (line 7) — spec/trace/raid/ holds no such file; the experiment's own promote field says 'i34 deleted the claim system whole, so there is no verb left to promote it into'; raid-dec-the-machine-locking-specification-is-retired-whole (owner-decided) rules 'the specification is written fresh rather than revived'.
  proposal: Owner call whether the dated measurement stays as history; if kept, the dangling probes ref needs a RETIRED marker or rewrite, because it currently points at nothing.
  lint: Corpus-wide dangling-ref sweep over probes (see the LINT finding).
- **[S]** experiments — spec/trace/experiment/exp-watchdog.md:18
  claim: The experiment measures a heartbeat for the core–satellite channel, a subsystem the corpus itself records as off the live path, and its source_ref el-satellite-supervisor resolves to no file despite being cited by 10 nodes.
  evidence: exp-watchdog.md:18 'el-satellite-supervisor' — no such file in spec/trace/element/ (25 elements, none satellite); raid-debt-core-and-satellite-is-off-the-live-path: 'nothing the running server imports reaches it … its green tests read as proof it works'; grep for 'satellite' over deliverable/engine/*.ts returns nothing.
  proposal: Owner call: retire with the subsystem or keep as history with the dangling element ref repaired; the same repair is owed by the other 9 citers of el-satellite-supervisor.
  lint: Same dangling-ref sweep.
- **[KEEP]** experiments — spec/trace/experiment/exp-can-the-host-state-which-folder-it-handed-over.md:1
  claim: The other four experiments (host-folder-handover, exclusion-lists-reach, machine-state-path-count, criteria-cut-audit) are completed measured records whose probe raids exist and carry the folded verdicts.
  evidence: raid-asm-the-hosts-pattern-test-and-its-handover-name-the-same-folder.md:13 carries the fold ('probe: FALSE, AND FOR A REASON THE ENTRY DID NOT ANTICIPATE…'); raid-iss-the-reading-verb-consults-no-exclusion-list-at-all, raid-risk-a-path-that-resolves-the-machine-state-folder-is-missed, raid-iss-most-of-the-criteria-cut-was-judged-from-a-rows-name all exist in spec/trace/raid/.
  proposal: Keep; these earn their place as the dated evidence behind standing raids.
- **[S]** options — spec/trace/option/opt-claim-local-then-reconcile.md:1
  claim: Eight zero-reference options are cells of the machine-locking/claim design the owner retired whole: opt-claim-local-then-reconcile, opt-claim-file-per-iteration, opt-checkout-card-claim, opt-preassign-at-seeding, opt-machines-advertise-availability, opt-seeds-ride-their-stub-branch, opt-abandon-by-deleting-the-branch, opt-the-claim-rides-the-iterations-own-branch.
  evidence: raid-dec-the-machine-locking-specification-is-retired-whole (minted i34, owner): 'no requirement, use case or story describing the claim ledger survives i34' with trigger 'the specification is written fresh rather than revived'; the owner overturned i34's own keep-as-dormant ruling the same day; zero inbound references repo-wide for each of the eight (i1/i2/i28 evidence folders hold only record.md).
  proposal: Propose removal under the standing owner decision — keeping dormant claim-design cells is the exact rejected option that decision records.
- **[O]** options — spec/trace/option/opt-unrepresentable-not-checked.md:1
  claim: Fourteen more options have zero inbound references repo-wide, but they split: the i9/i16 ones were considered by phrase in surviving charts, several i1 ones describe behavior the system adopted, and only the rest are truly unconsidered.
  evidence: Zero-ref set: opt-unrepresentable-not-checked, opt-the-override-replaces-the-whole-artifact, opt-the-machine-pushes-the-step, opt-the-form-is-the-editor, opt-split-the-record-by-lifetime, opt-serialised-merge-queue, opt-sealed-unit-landing, opt-reading-credit-survives-the-engine, opt-producing-happens-outside-the-lane, opt-phase-gate-state-machine, opt-graph-with-cycles, opt-decision-trace-schema, opt-answer-carries-a-reference, opt-activate-last-and-decide-in-our-own-code. Phrase-level consideration exists for some: i9 build_chart.md:30 'activate last, decide in our code'; i16 build_chart.md:97 'producing happens outside the lane'. Adopted-behavior examples: opt-answer-carries-a-reference matches today's ref/bounded answers; opt-the-machine-pushes-the-step matches the pull's pre-walked 'do'.
  proposal: Owner triage in three piles: link the phrase-considered and adopted ones from the design-specs that realize them (cheap, preserves rationale); judge the i1 ones whose considering evidence was deleted with the i1/i2 evidence folders individually; only the truly unconsidered remainder is removal material.
  lint: An option-coverage report: every opt- node either cited by id somewhere or listed as unconsidered — the anti-join se_coverage already computes for other type pairs.
- **[D]** options pool drift — spec/trace/design-spec/dsp-the-options-pool.md:32
  claim: The spec says the drain mints an OPTION at spec/trace/option/opt-<slug>.md carrying ready_when, but the shipped pool mints WORK-TOKENS: the code writes wt- ids under spec/trace/work-token, and 0 of 191 option files carry ready_when while work-tokens do.
  evidence: dsp-the-options-pool.md:32-48 '## Where an option lives … spec/trace/option/opt-<slug>.md … ready_when | the re-entry condition'; deliverable/engine/pool.ts:22 'POOL_PREFIX = "spec/trace/work-token"' and :109 'let id = `wt-${slug}`'; grep -l ready_when over spec/trace/option/*.md returns 0 files; wt-a-chart-field….md carries 'ready_when: ready when an engine round takes the form editors'.
  proposal: Rewrite the spec's location/id/frontmatter sections to the work-token reality (or record the rename decision inside it); it is the design-spec for pool.ts and currently teaches the wrong folder, prefix and node type.
  lint: The guidance-names-only-what-exists path check extended to design-spec prose would flag the opt-<slug> path claim.
- **[D]** options pool drift — spec/trace/flow/flow-standing-option.md:5
  claim: The flow's subject — 'an option somebody may commit to, stated for a reader who never saw the note it came from' — is the thing the engine now calls a work token, and no flow node exists for the work token.
  evidence: flow-standing-option.md:5; the same defining phrase now describes work tokens ('statement is what the token IS, for a reader who never saw the note' — walking guidance served every turn); pool.ts:22,109 mints wt- not opt-.
  proposal: Rename or re-statement the flow to the work token (its source_refs and material/signal reasoning stay valid unchanged).
- **[D]** options pool drift — spec/trace/use-case/uc-see-the-whole-pool-from-any-clone.md:9
  claim: The guarantee promises 'every option standing in the pool, each with its statement and its re-entry condition' — the pool it describes now holds work tokens, and the use case's option vocabulary no longer names anything the survey serves.
  evidence: uc-see-the-whole-pool-from-any-clone.md:9; pool.ts:22 POOL_PREFIX = spec/trace/work-token; the survey reads the pool (dsp-the-options-pool '## The shape': survey.ts reads the pool).
  proposal: Sweep option→work token through this node's guarantee, scenario and extensions; the behavior itself still exists, only the noun moved.
- **[E]** use-cases — spec/trace/use-case/uc-land-work-on-trunk.md:26
  claim: The Lane doors section teaches two lane verbs deleted by i34 — se_git_sync and se_git_land — and the precondition and scenario rest on a worktree that went with the second tree.
  evidence: uc-land-work-on-trunk.md:26-27 ('se_git_sync keeps the record and trunk in step … se_git_land is the landing itself') and :8 'its worktree holds the work'; i34 evidence delete-the-seam.md:39 'se_git_land and se_git_land are gone from the lane' and :53 'DEAD IN PUBLIC … while the lane still advertised them'; neither name is registered anywhere in deliverable/engine/*.ts.
  proposal: Take first (E outranks): rewrite the landing use case to the one-tree reality — se_git allowlisted verbs plus the release gate — or mark the dead doors RETIRED; sty-land-the-work.md:52 carries the same dead verb and needs the same edit.
  lint: Extend the guidance-verbs sweep to spec/trace (see LINT finding).
- **[D]** use-cases — spec/trace/use-case/uc-answer-a-question-with-tests.md:18
  claim: Step 2 has the asker name the test scope ('They name the scope that could answer it, and nothing outside that scope runs'), but the engine moved to the opposite contract: the caller states only the question and the engine reads what changed.
  evidence: uc-answer-a-question-with-tests.md:18 and extension 2a 'The named scope holds no test file'; deliverable/engine/tools-run.ts:191 remedy note: 'You do not name the scope; the engine reads what changed.'
  proposal: Rewrite step 2 and extension 2a to the question-only contract; the rest of the node (durable job handle, battery-at-gate) still matches.
- **[D]** use-cases — spec/trace/use-case/uc-adjudicate-a-gate.md:9
  claim: The guarantee says a gate is 'blessed or rejected by a person', but the owner ruled 2026-08-09 that at high autonomy the agent blesses its own gate, and the engine enforces the dial rather than personhood.
  evidence: uc-adjudicate-a-gate.md:9; the served contract records the ruling: 'at high autonomy the agent uses both (owner ruling 2026-08-09). Blessing your own gate is normal here … Below the dial it is theirs' — with the engine refusing an agent bless at or below the gate's weight.
  proposal: Amend the guarantee to 'blessed by whichever hand the dial sanctions, and the record keeps whose hand it was' — the actor-stamp half is already true.
- **[KEEP]** use-cases and flows — spec/trace/use-case/uc-take-a-step.md:1
  claim: The rest of the sample matches today's engine: 6 of 10 use-cases (capture-a-stray, drain-the-inbox, take-a-step, get-work-routed, query-the-corpus-by-structure, install-quackitect) and 9 of 10 flows are accurate against the registered verbs and current behavior.
  evidence: uc-take-a-step's lane-door list matches the registered tool set (se_file_move, se_file_delete, se_why all registered); uc-query-the-corpus-by-structure's se_query and se_coverage are registered (deliverable/engine/tools-query.ts, tools-desk.ts); uc-drain-the-inbox's done/obsolete/carried/backlog match se_note_drain; sampled flows (flow-instruction, flow-refusal, flow-position, flow-evidence-form, flow-test-question, flow-note-inbox, flow-option-sketch, flow-filled-claim, flow-stamped-claim) are one-line boundary declarations whose source_refs all resolve — note 'claim' there means the evidence-form claim, which survived i34's claim-LEDGER retirement.
  proposal: No action; recorded so the sample's pass rate is visible beside its failures.
- **[LINT]** corpus integrity — deliverable/tests/trace-coverage.test.ts:81
  claim: The only corpus-wide dangling-reference sweep covers refines edges alone, so standing nodes cite deleted ids silently — el-satellite-supervisor is cited by 10 nodes and exists nowhere, exp-claim-verb-race probes a deleted raid — while tsp-trace-integrity claims 'zero dangling edges' verified by test.
  evidence: trace-coverage.test.ts:79-83 builds `dangling` from n.refines only; guard.ts:18-28 REFERENCE_KEYS omits probes, picks and folds_to and only REPORTS at write time; grep -rln el-satellite-supervisor spec/trace/ returns 10 files against no element file; refs.test.ts tests the form checker on synthetic fixtures, not the standing corpus.
  proposal: Extend the trace-coverage sweep to every reference key guard.ts names plus probes/picks, over the real corpus, with a RETIRED/NOT BUILT YET marker as the exemption door; add probes and picks to guard.ts REFERENCE_KEYS so new writes report too. This single lint catches the S/E dangling findings above as a class, forever.
  lint: Exactly as proposed: one test iterating spec/trace/**, resolving every id-shaped token in reference-carrying frontmatter keys.
- **[LINT]** corpus prose vs engine — deliverable/tests/guidance-verbs.test.ts:1
  claim: The verbs-paths-clauses sweep stops at guidance and machine states, so trace nodes teach dead verbs for months — the same defect found twice (uc-land-work-on-trunk and sty-land-the-work both teach se_git_sync/se_git_land), which is the threshold where the rule wants a lint rather than another edit.
  evidence: tsp-guidance-names-only-what-exists Scope: 'Covers every guidance page and machine state note the walk is served' — spec/trace is outside it; the two E findings above are the twice-found defect; i34's own sweep-consistency evidence missed both nodes.
  proposal: Widen the same test's file set to spec/trace/**/*.md (the NOT BUILT YET / RETIRED exemption door already exists in its design), so an se_ name in any corpus node must be registered, exempted, or marked.
  lint: One glob change plus the existing checker; the exemption grammar is already specified.

---

# v2:b0974853c3862ca063636a1781bb39183fb0a655986c6aee927b649ded5b8bbe — 26 findings

Sweep of the lane surface (tools.ts, tools-desk/file/run/query, files.ts, files-patch seam, web.ts, testreporters.ts, run.ts, all 28 bin scripts) plus the five unshipped-candidate modules. The worst finds are descriptions asserting the opposite of their handlers: se_amend's `chain` is documented 'NOT IMPLEMENTED — accepted and ignored' while it actually rewrites every downstream signed claim; se_run declares a timeout_ms it never applies (with SE-C-107 defined, documented in refusals.md, and raised nowhere); and se_run's 'logged IN FULL' promise is false past a 30,000-char middle-cut whose marker points at a log copy holding the same cut. Four code defects survived verification: the pool's one-door guard normalizes backslashes with a wrong regex so a backslash-spelled path bypasses it; the corpus-parse and pool guards run only on se_file_write, leaving se_file_patch (the recommended edit verb) and se_file_replace unguarded; the arg_repaired note leaks from a refused call onto the next successful one because decorators only run on success; and the battery's conformance sweep hardcodes ok:true so a red sweep can never fail verification. Fifteen refusal source labels still say engine/tools.ts after the file split — proposed as a lint, along with a path-resolution lint for served strings. On the five modules: cage-inventory and payload-limit are KEEP (their tests judge shipped artifacts, so the battery enforces them — with a noted Claude-cage gap); failure-shapes, stopping-layer, and update.ts's change reporter are O — each fully built and fixture-tested with zero live callers, and for stopping-layer the event-producing side is already live so only the reader is unwired. Smaller E/D items: the stop hook's 'FOUR STOPS' banner lists five, place-prompt-layer's help documents a default the code doesn't have, se-start still says seven steps after the adopt step's deletion, and se-arrive duplicates se-start's node-pin check verbatim.

- **[E]** lane surface — deliverable/engine/tools.ts:289
  claim: se_amend's `chain` argument is described as 'NOT IMPLEMENTED — accepted and ignored' while it is fully implemented and, when true, rewrites every downstream signed claim.
  evidence: tools.ts:289 description: 'NOT IMPLEMENTED — accepted and ignored… this argument has no work to do'. But tools.ts:302 forwards `args.chain === true`, session.ts:295-297 passes it through, and sessionclaims.ts:918 runs `const chained = chain ? this.refreshChain(name, m, reason, by) : {}` — refreshChain (sessionclaims.ts:924-962) writeFileSync's an amended stamp onto every clean downstream signed form.
  proposal: Either make the description true (strip the forward so the flag really is inert) or rewrite the description to say what chain:true actually does. A description that calls a mutating flag inert is the most dangerous drift in the surface.
- **[E]** lane surface — deliverable/engine/tools-run.ts:296
  claim: se_run declares a `timeout_ms` argument the handler never reads, and the matching clause SE-C-107 (RUN_TIMEOUT) is defined, documented in guidance, and raised nowhere.
  evidence: tools-run.ts:296 declares `timeout_ms: { type: "number" }`; the handler (300-335) never touches it and run.ts contains no timeout/kill-on-duration logic at all (runToCompletion awaits jobDone unboundedly). errors.ts:64 defines RUN_TIMEOUT 'command exceeded its time budget'; grep shows 0 raise sites. guidance/refusals.md:238 documents '### SE-C-107 — the command timed out' as a live refusal.
  proposal: Implement the timeout (killTree on expiry, refuse/annotate with SE-C-107) or delete the argument, the clause, and the refusals.md section. As it stands a caller passing timeout_ms gets silence — the declared arg passes the unknown-args guard and does nothing.
  lint: a test that every CLAUSES entry not commented 'reserved' has at least one raise site outside errors.ts
- **[E]** lane surface — deliverable/engine/tools-run.ts:282
  claim: se_run promises output 'logged IN FULL under the returned call ref' but stdout/stderr are middle-cut at 30,000 chars before the log ever sees them; the full text sits in .se/jobs/<id>.stdout files no verb serves.
  evidence: tools-run.ts:282 'Output is engine-captured and logged IN FULL under the returned call ref'; calllog.ts:3-4 'se_run responses are logged IN FULL under their ref'. But run.ts:21 `OUT_CAP = 30_000` and run.ts:156 `capMiddle(s, OUT_CAP)` cap the JobView the observer logs (tools.ts:933-935 logs rec.response for se_run uncapped-further — but rec.response is already the capped view). jsonio.ts:12's default cut marker even says 'the whole lives in the call log', pointing at a copy holding the identical cut. se_log_query's find(ref) (tools-desk.ts:344) reads only the log record; nothing reads .se/jobs/*.stdout back.
  proposal: Make se_log_query {ref} (or se_run {job}) page the persisted per-job stdout/stderr files, or change the description and the capMiddle marker to say the cap. Today the honest `truncated: true` flag contradicts three louder promises.
- **[E]** lane surface — deliverable/engine/bin/se-hook-stop.ts:171
  claim: The stop hook's sanctioned-stops banner says 'FOUR STOPS ARE SANCTIONED AND NOTHING ELSE IS', then lists five numbered stops and closes with 'Stopping for one of the four?'.
  evidence: se-hook-stop.ts:171 'FOUR STOPS ARE SANCTIONED…' followed by '(1)…(2)…(3)…(4) THE RETRO'S FIELD-FEEDBACK QUESTION… (5) A PLAN, BEFORE IT IS ACTED ON' (lines 171-177) and line 181 'Stopping for one of the four? Say which'.
  proposal: Change both counts to five. This text is served to an agent at the exact moment it is deciding whether a stop is legal; a self-contradicting rule invites the wrong reading.
- **[CODE]** lane surface — deliverable/engine/pool.ts:122
  claim: guardNoSecondDoor's path normalization matches two literal backslashes instead of one, so a pool path spelled with Windows single-backslash separators walks straight past the one-door privacy guard.
  evidence: pool.ts:122 `if (!rootRelativePath.replace(/\\\\/g, "/").startsWith(`${POOL_PREFIX}/`)) return;` — the regex /\\\\/g matches TWO consecutive backslashes (verified at byte level with cat -A). Every other normalization in the lane uses the one-backslash idiom, e.g. files.ts:301 `path.replace(/\\/g, "/")`. A write to `spec\trace\work-token\wt-x.md` is not normalized, startsWith fails, and the guard returns without refusing — the exact i17 fabricated-token hole (files.ts:410-414) reopened for backslash spellings.
  proposal: Change the regex to /\\/g and add a test writing a pool path with backslash separators expecting SE-C-140.
  lint: a lint flagging /\\\\/g in path-normalization position (replace(...,"/")) — the four-backslash literal is almost always this mistake
- **[CODE]** lane surface — deliverable/engine/files-patch.ts:277
  claim: The corpus write guard (guardParses / SE-C-138) and the pool one-door guard run only on se_file_write; se_file_patch and se_file_replace bypass both, so a patch can land an unparseable corpus node or write into the pool.
  evidence: fileWrite runs all four guards (files.ts:409 guardMachineNote, 414 guardNoSecondDoor, 420 guardParses, 421 guardRawNul). filePatch's writeStaged runs only guardMachineNote + guardRawNul (files-patch.ts:277-278); fileReplace likewise (files.ts:600-601). grep confirms guardParses and guardNoSecondDoor have exactly one caller each: files.ts. model-fs.ts:54-66 routes the verbs directly to these functions. errors.ts:91 says SE-C-138 is 'refused before it lands' — true only for the write verb; CLAUDE.md tells agents to prefer se_file_patch for every source edit, i.e. the unguarded path is the recommended one.
  proposal: Call guardParses and guardNoSecondDoor from writeStaged and from fileReplace's staging loop (both already stage full next-content before writing, so the guards drop in). The conformance sweep catches corpus breaks later, but the requirement (req-a-write-that-breaks-the-corpus-refuses) is refusal at the write.
  lint: a test patching a corpus node's frontmatter into unparseable YAML via se_file_patch and expecting SE-C-138
- **[CODE]** lane surface — deliverable/engine/tools.ts:449
  claim: The arg-name repair note leaks across calls: `argRepairs` is module state set in a guard and cleared only by a success decorator, so a repaired-then-refused call stamps its `arg_repaired` onto the next unrelated successful call.
  evidence: tools.ts:449 `let argRepairs … | undefined;` set by repairArgNames (461-466) inside the shape guard (869). mcp.ts:241-258: decorators run only on the success path — `refused()` returns before `for (const d of this.decorators)`. The update-guard resets its own three vars every call (tools.ts:686-688); nothing resets argRepairs. Sequence: call A repairs `pattern`→`query`, state gate (tools.ts:911) refuses A, decorators skipped; call B succeeds and the decorator at 841-846 attaches A's repair note to B.
  proposal: Reset argRepairs at the top of the first guard (beside updateComplaint), or key the repair to the call by threading it through args instead of module state.
- **[CODE]** lane surface — deliverable/engine/tools-run.ts:430
  claim: runSweep hardcodes ok: true regardless of exit code, so a red conformance sweep can never fail the battery verdict.
  evidence: tools-run.ts:430 `return { script: …, ok: true, exit: r.status, … }` while every sibling result tracks exit (435-441 `ok: format.status === 0`, 452 `ok: r.status === 0`). runBattery line 455 `const ok = results.every((x) => x.ok)` — with sweep ok pinned true, sweep.ts's exit 1 (bin/sweep.ts:28) never turns the battery red. The se_test description (341) sells the sweep as the thing that 'says everything' about a documents diff.
  proposal: `ok: r.status === 0` — or, if the sweep is deliberately advisory, drop the ok field from its row and say 'advisory' so the shape stops lying.
- **[D]** lane surface — deliverable/engine/tools-run.ts:341
  claim: se_test's description promises job-handle behavior and per-run timing that only one of the two paths delivers.
  evidence: Description (341): 'Structured as a durable job: starting returns a handle' — but scoped runs block and return the verdict inline (536-539 `await entry.done; return entry.verdict`, per dsp-lane-door.md#a-scoped-run-answers-the-caller-that-asked); only the battery returns handed_off (540-544). 'EVERY RUN RECORDS ITS TIMINGS… the verdict says how many it timed' — runScoped includes timingReport (420) but runBattery's verdict (459) carries no timed field. Also the comment at 449-450 'Configurable, generous default' describes a timeout that exists nowhere in spawnNode; the only cap is selftest.ts's hardcoded CAP_MS = 300_000 (bin/selftest.ts:70), which is not configurable.
  proposal: Reword the description to the real split (scoped answers inline, battery hands off; battery timing lives in .se/test-last-run.json), and fix or delete the 'Configurable' comment.
- **[LINT]** lane surface — deliverable/engine/tools-desk.ts:34
  claim: Fifteen refusal `source` labels across the three split tool files still say 'engine/tools.ts', so a refusal names a file the code left in the split.
  evidence: grep -c 'source: "engine/tools.ts' → tools-desk.ts:5, tools-file.ts:5, tools-run.ts:5 (e.g. tools-desk.ts:34 'engine/tools.ts prose-wall', tools-run.ts:153 'engine/tools.ts se_test'); only se_coverage names its real home (tools-desk.ts:285 'engine/tools-desk.ts se_coverage'). Same class: sessionclaims.ts:841 'engine/session.ts amend'. The headers say 'Split out of tools.ts' — the labels never moved.
  proposal: Sweep the source strings to the real filenames, then add the lint: same defect found 15+ times is a class.
  lint: a test walking every `new Rejection({...source})` literal and asserting the source string starts with the raising file's own engine-relative path
- **[D]** lane surface — deliverable/engine/bin/se-mcp.ts:426
  claim: se-mcp's proxy comment cites SE-C-114 as the live arbitration mechanism while the clause registry marks SE-C-114 reserved and never issued.
  evidence: se-mcp.ts:426 'the machine arbitrates the hands (SE-C-114)' vs errors.ts:70 'STALE_POSITION: "SE-C-114", // reserved, never issued — old logs carry it' and refusals.md:355 'Reserved. Never issued by this engine'.
  proposal: Drop the clause citation from the comment (the proxy behavior itself is real); or if hand-arbitration refusals are intended, mint a live clause.
- **[E]** lane surface — deliverable/engine/bin/place-prompt-layer.ts:18
  claim: place-prompt-layer's help text documents '--opened … Default: <root>/product' while the code defaults --opened to the root itself.
  evidence: Line 18 help: '--opened  the folder the person opens. Default: <root>/product.' vs line 25 `const opened = resolve(argValue("--opened") ?? root);`.
  proposal: Fix the help line to 'Default: the root'.
  lint: the help-conformance idea preflight already hints at: assert each documented default appears in the code path
- **[D]** lane surface — deliverable/engine/bin/se-start.ts:5
  claim: se-start still announces 'SEVEN STEPS' (and briefs the agent 'THE SEVEN STEPS ALREADY RAN') after the adopt step was deleted, leaving six.
  evidence: Line 5 'SEVEN STEPS, AND EACH EXITS NON-ZERO NAMING ITSELF'; line 209 '── adopt: DELETED (i34)'; main() (274-287) runs verify, install, start, wait, fetch, launch = six; briefing line 270 'THE SEVEN STEPS ALREADY RAN.'
  proposal: Say six in both places.
- **[R]** lane surface — deliverable/engine/bin/se-arrive.ts:108
  claim: se-arrive duplicates se-start's engines.node major.minor pin check nearly verbatim — the one rule both files carry a war story about getting wrong.
  evidence: se-start.ts:67-75 (`const ver = (s) => …` + major/minor compare + the 22.18 comment) vs se-arrive.ts:108-131 (`function version(s)` + identical compare + the same 22.18 comment). Both parse deliverable/package.json engines.node independently. The bin folder also carries ~13 identical copies of `argValue()`.
  proposal: Extract one `assertNodePin(root)` (and optionally argValue/say/die) into engine/, import from both bins. The comments in both files record that this exact check drifted once already; two copies re-arm that failure.
  lint: a lint flagging function bodies duplicated verbatim across bin/ scripts
- **[D]** lane surface — deliverable/engine/tools.ts:1
  claim: tools.ts's header still says the state machine 'wires in here next' and the surface is 'deliberately COMPLETE for daily work and nothing else' — both sentences predate what the file now holds.
  evidence: Lines 1-4: 'The state machine wires in here next (one generic guard reading the active state's legal list); the surface below is deliberately COMPLETE for daily work and nothing else.' The gate exists in the same file (line 911 `server.addGuard((tool, args) => session.gate(tool, args))`), and the surface now carries se_benchmark, se_aim, se_why, se_reopen, se_amend, the producing acts and the expedition verbs.
  proposal: Rewrite the header to describe the file as it stands (dispatch guards, decorators, the assembled surface).
- **[T]** lane surface — deliverable/engine/tools-file.ts:259
  claim: se_file_patch's unknown-field refusal lists only the exact-match verb's fields as 'expected', hiding the other four verbs the KNOWN set accepts.
  evidence: Line 259 `expected: "op fields: path, old_string, new_string, base_hash?, replace_all?"` vs the KNOWN set (233-246) which also holds pattern, replacement, flags, expect_count, append, prepend, at — the same fields the description (201) advertises.
  proposal: Generate `expected` from the KNOWN set so the two cannot drift.
- **[CODE]** lane surface — deliverable/engine/tools.ts:274
  claim: Sibling verbs spell the same op-level flag two ways: se_amend ops take `all` where se_file_patch ops take `replace_all`, and the ARG_SYNONYMS repair covers only top-level args so the habit refuses with no repair.
  evidence: tools.ts:274 se_amend op field `all: { … replace every occurrence …}` vs tools-file.ts:215 se_file_patch op field `replace_all`. repairArgNames operates on top-level keys only (tools.ts:451-467); op-level unknown fields go through the patch handler's own KNOWN/ALIAS check (tools-file.ts:233-253), whose ALIAS map does not include replace_all/all either.
  proposal: Pick one spelling (replace_all matches the older verb) and accept the other via the patch ALIAS map — the same one-candidate-repair philosophy the file already implements for find/replace/old/new.
- **[T]** lane surface — deliverable/engine/errors.ts:100
  claim: The clause registry's comment for QUERY_UNKNOWN_FIELD is glued onto the end of SEARCH_PATTERN's line, leaving SE-C-144 uncommented and SE-C-145 double-commented.
  evidence: errors.ts:100 `QUERY_UNKNOWN_FIELD: "SE-C-144",` has no trailing comment; errors.ts:105 SEARCH_PATTERN's line ends '…rides the refusal // a structured query names a field the matched kind does not carry…' — the second half describes SE-C-144.
  proposal: Move the misplaced half up one entry.
- **[D]** lane surface — deliverable/engine/tools-run.ts:244
  claim: se_shoot's schema and description offer four widgets while the renderer serves six and the handler's own cast admits a fifth.
  evidence: Description (244) and schema (248) list 'machine | details | log | terminal'; the handler cast (255) adds "table"; renderMirror's signature (render.ts:910) accepts 'machine | details | log | terminal | table | trace'. An agent cannot discover the table or trace shots.
  proposal: Either list table/trace in the schema or narrow the renderer types — one source for the widget vocabulary.
- **[KEEP]** built-but-unshipped — deliverable/engine/cage-inventory.ts:14
  claim: cage-inventory.ts is live despite having no bin importer: its test reads the SHIPPED cage file, so the battery enforces the inventory — but only for the Copilot cage.
  evidence: tests/cage-inventory.test.ts:12-17 loads deliverable/cage/copilot-cage.json and asserts inventoryProblems() empty; the battery runs the tests dir (tools-run.ts:446, bin/selftest.ts:62). Gap: NATIVE_PROJECT_TOOLS (cage-inventory.ts:14-39) is Copilot vocabulary (view/create/edit/powershell); no test runs the Claude cage (deliverable/cage/claude-settings.json, placed by se-arrive.ts:154 and se-start.ts:226) through any inventory, so the Claude-side hole class the header warns about (a host adding a tool later) is unwatched.
  proposal: KEEP; extend the inventory (or a sibling) to the Claude cage's deny list, or record the one-host coverage as a known gap on the requirement.
  lint: the extension itself: a test diffing claude-settings.json's deny list against a canonical Claude-native tool set
- **[O]** built-but-unshipped — deliverable/engine/failure-shapes.ts:53
  claim: failure-shapes.ts (recurring refusal shapes become durable work) is built, tested on synthetic fixtures only, and wired to nothing — no live path ever feeds it call records or mints its work statements.
  evidence: Sole importer: tests/failure-shapes.test.ts:7 (grep over the tree). The test fabricates records (`const refusal = (clause, tool) => …`, failure-shapes.test.ts:9-12); no caller of recurringShapes or asWorkStatement exists in session, retro, desk or boot code. Its own header (lines 1-10) says the shape 'should leave the session as something a later one can pick up' — req-repeated-failure-shape-becomes-durable-work — and nothing performs that step.
  proposal: Owner's call: wire it (the retro or the boot last-session summary already reads the call log and could surface recurringShapes over the window, minting via the existing pool mint) or park it with a NOT WIRED marker. As-is the requirement reads satisfied and is not.
- **[KEEP]** built-but-unshipped — deliverable/engine/payload-limit.ts:46
  claim: payload-limit.ts is a live guard through its test, which measures the real guidance corpus against the tightest measured host limit and pins the over-the-line page count.
  evidence: tests/payload-limit.test.ts:60-75 runs oversizedPayloads over pages(GUIDANCE) (the real repo guidance/) and asserts `over.length <= 4` with the four pages named as of 2026-08-20; battery runs it. Thin edge: measuredLimit() (payload-limit.ts:57-59) has no non-test caller — the engine's bound takes smallestInlineOutputBytes from harness.ts directly (bound.ts:4,19).
  proposal: KEEP. Optionally drop the measuredLimit() wrapper or point a preflight line at oversizedPayloads so the 4-page debt is visible at boot, not only when the battery runs.
- **[O]** built-but-unshipped — deliverable/engine/stopping-layer.ts:47
  claim: stopping-layer.ts's diagnosis (which layer ended an interrupted call) has zero non-test callers, while the lifecycle data it reads is fully produced by live code — the reader is the only missing piece.
  evidence: grep: stoppingLayer appears only in stopping-layer.ts:47 and tests/stopping-layer.test.ts. Data side is live: se-mcp.ts:219 records 'exit', se-hook-stop.ts:201 records 'stop-block', mirror.ts:1007 wires recordClientFailures → 'client-reset'/'client-error' (lifecycle.ts:41). req-interrupted-call-names-the-stopping-layer wants the layer NAMED to an engineer; no surface (boot banner, LastSession, se_why, mirror) calls it.
  proposal: Wire the diagnosis into the boot banner's last-session summary (calllog.ts LastSession already derives 'what the last sitting did' — the natural home), or park the module explicitly. Cheap to wire; the expensive halves (event capture, conclusive-order logic) are done.
- **[O]** built-but-unshipped — deliverable/engine/update.ts:81
  claim: update.ts's inventory() (el-change-reporter, 'what did this vehicle make its own') is implemented and tested against real produced trees but reachable from no lane verb, bin script, or surface — a vehicle owner cannot invoke it.
  evidence: Importers: only tests/produce.test.ts:29 (calls at 348, 363, 377 on temp-dir vehicles). No tool in tools*.ts, no bin entrypoint, and no mirror route names it. The file is honest about its sibling: lines 5-11 mark el-update-runner NOT BUILT with the reason ('Writing a runner for a format nobody has specified would be fabrication').
  proposal: Expose inventory() — a natural fit is a read-only lane verb or a produce-report line in the vehicle flow — or mark el-change-reporter's surface half NOT WIRED in the element so the corpus stops reading it as shipped. The NOT-BUILT discipline on the runner half is exemplary; the reporter half needs the same honesty or a door.
- **[KEEP]** lane surface — deliverable/engine/tools-desk.ts:255
  claim: se_coverage genuinely shares its computation with the submit-time laws, as its description promises — the one description in the surface I checked hardest and found exactly true.
  evidence: Description (255): 'It is the SAME computation those laws run at submit'. Both call uncoveredOf: tools-desk.ts:288 and stateform-problems.ts:628, one implementation at trace.ts:500.
  proposal: None — recorded as a notable KEEP because it is the pattern the drifted descriptions above should copy: description and law share one function, so they cannot disagree.
- **[LINT]** lane surface — deliverable/engine/tools-desk.ts:118
  claim: Paths cited inside served strings drift silently: se_lint's description cites 'machines/lint/voice-lint.md' while the real root-relative path is 'deliverable/machines/lint/voice-lint.md' — the exact dropped-prefix hazard the contract documents for writes, appearing in read-me text.
  evidence: tools-desk.ts:118 '(machines/lint/voice-lint.md)' vs lint.ts:87 `export const LINT_CONFIG = "deliverable/machines/lint/voice-lint.md"`. Same class as the source-label drift: a path in a served string has no check.
  proposal: Lint: extract path-shaped tokens from tool descriptions and refusal remedy notes, assert each resolves from the project root. Catches this, the source labels, and every future rename under a description.
  lint: battery test resolving every path-shaped substring in ToolDef descriptions against the tree

---

# v2:1eb4e0c48ab20f0ebb1b72e664e97361ef9642d855351b46caf700f2607ceec9 — 18 findings

Sweep of ten core engine modules (stateform, stateform-problems, machine, trace, expr, decisions, discipline, rigor-matrix, toll, bound), all read fully and every claim verified by grep or a runnable repro. The sharpest live bug is in trace.ts: rootsAllOf poisons its cache on diamond refinement shapes (roots enter `seen` and are never cached), and with the corpus's real load order a node can lose all its wedges and vanish from the trace drawing — reproduced with a four-node repro. Two E findings: stateform's delta-default scoping compares minted_in against the full record folder name while the recorded ruling (dsp-evidence-forms.md:488) demands short-id comparison and the corpus mixes both stampings (63 nodes say `minted_in: i36` against folder i36-the-harness-...), so records silently lose their own nodes; and fallback-outcome.test.ts still asserts the fix-findings row "guards the fallback on a counter" when the row's guard is gone — the regex passes on a prose quotation of the removed guard. machine.ts carries a dead edge-walker (advance/tryMove, imported by nothing) and a header describing that dead world (priority total, guard-exhausted escapes, reentry that nothing reads); the `derived` evidence type's promised refusal of hand-written values is filtering only. Duplication clusters wanting one home: four YAML-list coercers (one silently not comma-splitting), four regex-escape copies (forms.ts's omits `$`), four markdown-cell splitters (only tableRow honors escaped pipes, so tableProblems refuses rows tableRow accepts), three wiki-ref extractors, two element-matrix assemblies, and a second hand-rolled frontmatter parser (nodeField/nodeList) that diverges from the real YAML parse on CRLF files. Two lint proposals: a dead-export check (registerMethod has zero callers; a dozen-plus exports serve nobody outside their file) and duplicate-helper detection. toll.ts, the corpus door's caching discipline, and decisions' idempotent ops are recorded as notable KEEPs.

- **[CODE]** trace graph — deliverable/engine/trace.ts:630
  claim: rootsAllOf poisons its cache on diamond shapes and silently drops nodes from the trace drawing.
  evidence: deliverable/engine/trace.ts:624-646 — walk() adds a root to `seen` before the isRoot early-return and never caches roots, so a sibling path that reaches the same root second gets [] and caches it. Runnable repro with corpus load order (value-prop folder sorts last, traceFiles sorts entries at trace.ts:144): nodes A(refines B,C), B(refines vp), C(refines vp) yield "A -> ['vp']  B -> ['vp']  C -> []". deliverable/engine/trace-layout.ts:258 then filters `rootsShown(n.id).length > 0`, so C vanishes from the radial view. No test covers rootsAllOf.
  proposal: cache roots too (cache.set(id,[id]) before the early return) or use a path-local seen set; add the diamond test
  lint: a unit test over a diamond corpus with the value-prop declared last, asserting every node keeps its root
- **[E]** delta scoping — deliverable/engine/stateform.ts:1083
  claim: scopedToOwner/boundOwner/promotionItems compare minted_in against the full record folder name, while the standing ruling and half the corpus use the short id — the record's own short-stamped nodes are silently excluded from delta-defaulted sources.
  evidence: deliverable/engine/stateform.ts:1083-1086 `mintedInOf(n).trim() === owner` with owner = basename(dirname(evidenceDir)) (line 1077-1081); same exact compare at promotionItems line 1061. The ruling: spec/trace/design-spec/dsp-evidence-forms.md:488 "THE OWNER IS COMPARED AS A SHORT ID, because minted_in carries the short form after the 2026-08-15 rename and the long one before it." Corpus mixes both: 63 nodes carry `minted_in: i36` while the folder is spec/iterations/i36-the-harness-is-not-claude-measure-what-e; i9 appears both as `i9` (46 nodes) and `i9-se-and-the-corpus-move-the-machine-state` (38). The sibling module already normalizes: deliverable/engine/stateform-problems.ts:305-307 shortRecordId, used at line 352. tests/node-scoping.test.ts:61 stamps only full ids, so the exact compare passes in tests while failing on the live corpus.
  proposal: move shortRecordId to a shared home and route boundOwner/scopedToOwner/promotionItems comparisons through it
  lint: a test whose fixture stamps the short id against a long folder name
- **[E]** tests vs matrix row — deliverable/tests/fallback-outcome.test.ts:205
  claim: The fix-findings guard pin asserts "the row still guards the fallback on a counter", but the row's guard was removed on 2026-08-18 — the regex now passes only on a prose quotation of the old guard.
  evidence: deliverable/tests/fallback-outcome.test.ts:205-206 `assert.match(row, /guard: verification_attempts < 3/, "the row still guards the fallback on a counter")`. The row's frontmatter (deliverable/machines/rigor_matrix/rows/M7_60_fix-findings.md:1-30) carries no `guard:` key, and its body says at line 71: "THE ATTEMPT GUARD IS GONE, and it was never real." The regex matches the backticked historical quote at line 71-72. The test's comment block (fallback-outcome.test.ts:191-203) still describes the wedge as live ("SO THE ESCAPE CAN NEVER FIRE, and the loop above is unbounded"), which the row's exit-script hold superseded.
  proposal: retarget the pin: assert the frontmatter carries NO guard and the engine still writes no counters; rewrite the stale comment
  lint: pin frontmatter keys with a YAML read instead of a whole-file regex
- **[O]** machine kernel — deliverable/engine/machine.ts:702
  claim: advance() and its helper tryMove() are dead code — no file imports them; the token op completeState and Session.advance replaced them.
  evidence: deliverable/engine/machine.ts:702 (advance), 678 (tryMove). Grep across engine+tests: no import of advance from machine.ts anywhere; session.ts imports only {activeStates, branchToReturnTo, claimFeeders, completeState, INPUT_ROLES, reopenStates} (session.ts:16-26) and defines its own Session.advance (session.ts:3913). Tests calling `s.advance()` call the session method. completeState's unused `now` parameter (`void now;` at machine.ts:673) is leftover from the same old API.
  proposal: delete advance and tryMove (and drop completeState's dead `now` parameter), or record why they are kept
  lint: ts-prune/knip dead-export check in preflight
- **[D]** machine kernel — deliverable/engine/machine.ts:5
  claim: machine.ts's header schema rules describe the dead edge-walker world: the priority total, the escape-records-guard rule and the re-entry default live only in never-imported advance(), and `reentry` is parsed but read by nothing.
  evidence: deliverable/engine/machine.ts:5-12 — "Priority total: authored > fallback > escape > ask-human", "Escape records which guard was exhausted", "Re-entry declared per machine; default restart". The priority ordering and guard-exhausted escape exist only inside advance() (lines 702-719), which nothing imports. `.reentry` has one writer (machines/compile.ts:259 `fm.reentry ?? "restart"`) and zero readers (grep). The counters half ("Extended state: counters as variables") is already pinned as unwritten by tests/fallback-outcome.test.ts:204-211.
  proposal: cut the header to the live token model (fireOutbound/activatePowered/completeState) and either delete `reentry` or mark it NOT BUILT YET
- **[D]** evidence types — deliverable/engine/machine.ts:26
  claim: The `derived` evidence type promises "the engine computes it and refuses a hand-written value", but no refusal exists — the engine only filters derived fields out of the served form.
  evidence: deliverable/engine/machine.ts:26-28 and deliverable/engine/machines/supply.ts:79-81 both state the refusal. Actual handling is filtering only: session.ts:2290, 3656, 3969 `.filter((f) => f.type !== "derived")`. No submit-path check refuses a hand-written value for a derived field (grep for unknown-field/derived refusals: only tools-file.ts:260, about patch ops).
  proposal: implement the refusal at submit, or reword both comments to "filtered from the served form" per the NOT-BUILT-YET rule
- **[R]** frontmatter reading — deliverable/engine/stateform.ts:1226
  claim: stateform's nodeField/nodeList are a second, hand-rolled frontmatter parser beside notes.ts's real YAML parse, and the two diverge: on a CRLF file nodeField's fence detection fails and its FRONTMATTER-ONLY guard silently turns off; block scalars read as their indicator character.
  evidence: deliverable/engine/stateform.ts:1226-1283 vs deliverable/engine/notes.ts:15-37 (parseYaml). nodeLines splits on "\n" only (notes.ts:211), so a CRLF file's fence line is "---\r" and stateform.ts:1232 `lines.indexOf("---", 1)` returns -1 — the whole body is then scanned for `key:`, the exact "silent fill" the comment at 1230-1231 warns against. parseStateNote handles CRLF (`split(/\r?\n/)`, trim). noteOf caches the parsed note (notes.ts:216-221), so the perf case for the hand parser is thin. Corpus is all-LF today (0/100 files CRLF), so the divergence is latent, on a Windows repo.
  proposal: reimplement nodeField/nodeList over noteOf's frontmatter, keeping unquote in one place
  lint: a test feeding both readers one CRLF node and one block-scalar value, asserting they agree
- **[R]** list coercion — deliverable/engine/stateform.ts:751
  claim: Four copies of the YAML-list-or-comma-string coercer exist, and one silently differs: trace.ts's asList does not comma-split, so the same frontmatter value parses differently depending on which module reads it.
  evidence: deliverable/engine/stateform.ts:751 fmList (self-described "the house rule for every list"), deliverable/engine/rigor-matrix.ts:90 asList, deliverable/engine/machines/compile.ts:409 asList — all comma-split; deliverable/engine/trace.ts:126 asList returns a single-element list for "a, b".
  proposal: one fmList in notes.ts (the frontmatter home); trace.ts either adopts it or documents why refines must not comma-split
  lint: a duplicate-helper lint over `function asList` / identical bodies
- **[R]** regex escaping — deliverable/engine/stateform-problems.ts:33
  claim: Four copies of the regex-escape helper, and the forms.ts copy omits `$` from its character class — a key containing `$` builds a wrong regex there.
  evidence: deliverable/engine/stateform-problems.ts:33, deliverable/engine/files.ts:664, deliverable/engine/search.ts:164 all escape `[.*+?^${}()|[\]\\]`; deliverable/engine/forms.ts:311 escapes `[.*+?^{}()|[\]\\]` — no `$`.
  proposal: one escapeRe in a shared home; forms.ts gains the `$`
  lint: the same duplicate-helper lint; or adopt RegExp.escape when the runtime carries it
- **[R]** table parsing — deliverable/engine/stateform-problems.ts:773
  claim: Markdown row cells are split four different ways, and only tableRow honors escaped pipes — tableProblems' bare cell count refuses a legal row whose cell carries `\|`, while tableRow accepts it.
  evidence: deliverable/engine/stateform.ts:583-592 tableRow splits on `(?<!\\)\|`; deliverable/engine/stateform-problems.ts:773 tableProblems counts `l.split("|").length - 2`; stateform-problems.ts:719-726 chartProblems and deliverable/engine/trace.ts:736 refsInRows both bare-split. A cell containing an escaped pipe is one cell to tableRow and two to tableProblems.
  proposal: route every row split through tableRow
  lint: a test with an escaped pipe in a table cell run through both checkers
- **[R]** wiki-ref extraction — deliverable/engine/trace.ts:688
  claim: Three strip-the-wiki-brackets extractors coexist: morphbox.bare and trace.refId are near-identical, and stateform-problems.rowId is a weaker third that keeps a display half or path where the others strip it.
  evidence: deliverable/engine/morphbox.ts:38-42 bare (quotes + brackets + display + path), deliverable/engine/trace.ts:688-694 refId (brackets + display + path + .md), deliverable/engine/stateform-problems.ts:638 rowId (brackets only).
  proposal: one extractor (refId is the most complete); bare and rowId delegate to it
  lint: same duplicate-helper lint
- **[R]** structure checks — deliverable/engine/stateform-problems.ts:497
  claim: The element-matrix input assembly is built twice — once from traceFolder for the form view, once from the corpus for the law check — so a schema change to element/function/interface keys must be edited in both.
  evidence: deliverable/engine/stateform.ts:778-789 elementMatrixArgs vs deliverable/engine/stateform-problems.ts:497-511 structureLawProblems; both hand-assemble {id, group/implements}, {id, inputs, outputs}, {id, source, destination, carries} for elementMatrixView.
  proposal: one builder taking (id, frontmatter) pairs, called by both
- **[CODE]** form re-check — deliverable/engine/stateform-problems.ts:131
  claim: claimProblems restates all 27 FieldArgs keys that NO_ARGS exists to avoid restating.
  evidence: deliverable/engine/stateform-problems.ts:131-169 builds the full literal; deliverable/engine/stateform.ts:272-274 documents NO_ARGS as "Exported so a test can spread it rather than restate fifteen keys it does not care about".
  proposal: spread NO_ARGS and override only the keys the re-check keeps; the null-view comments move onto the overrides
- **[CODE]** stateform-problems scaffolding — deliverable/engine/stateform-problems.ts:270
  claim: Two near-identical find-the-record's-file loops (seededStepIds, foldBackExperiments) and two copies of the .se-filtered git-porcelain parse in discipline.ts want one helper each.
  evidence: deliverable/engine/stateform-problems.ts:270-285 and 288-302 differ only in the file they open and the extraction; deliverable/engine/discipline.ts:197-207 (testFingerprint) and 350-359 (changedSinceBattery) both parse porcelain and skip `.se`.
  proposal: extract recordFile(recordRoot, only, relPath) and dirtyFiles(root) helpers
- **[T]** narration refusals — deliverable/engine/decisions.ts:221
  claim: decisions.malformed()'s `expected` line omits defer from the legal ops, though defer is accepted and SHAPE_NOTE documents it.
  evidence: deliverable/engine/decisions.ts:221 "an update op: {op: plan|fork|done|obsolete|revert|update, ...}" vs line 270 accepting `op === "defer"` and SHAPE_NOTE (lines 39-44) documenting it.
  proposal: add defer to the expected line, or build both from one OPS constant
  lint: derive the expected string from the same op list the parser checks
- **[CODE]** answer bound — deliverable/engine/bound.ts:19
  claim: ANSWER_BOUND_BYTES mixes units: the requirement and the check measure UTF-16 characters, the harness limit is bytes — masked today by a 3.4x margin, but a host measured under ~18KB could overflow on multi-byte prose.
  evidence: deliverable/engine/bound.ts:8 OWN_CEILING commented "in characters of serialised JSON"; line 19 Math.min with smallestInlineOutputBytes() (harness.ts:47: 20,480 bytes); line 87 compares whole.length (code units). spec/trace/requirement/req-oversized-results-remain-recoverable-through-the-lane.md:11 measures "at most 6,000 characters". This repo's prose is em-dash heavy (3 bytes each).
  proposal: convert the measured host limit to characters conservatively (divide by a worst-case factor) or measure with Buffer.byteLength throughout, and rename to match the unit
  lint: a test asserting Buffer.byteLength(text) <= the measured host byte limit for a multi-byte payload
- **[LINT]** dead exports — deliverable/engine/expr.ts:96
  claim: A machine-checkable class: exports with no consumer anywhere, or none outside their own file — registerMethod has zero callers (its 'seam' twin registerGlobal is test-only), and a dozen-plus stateform/trace/discipline exports serve nobody outside.
  evidence: deliverable/engine/expr.ts:96-103 registerMethod (no engine or test caller; its body also duplicates method() at lines 80-89); expr.formatDate, trace.typeName (trace.ts:186), trace.fieldValue (trace.ts:392), discipline.changedSinceBattery, discipline.LANE_RULES, bound.BoundedAnswer, and stateform's readA3, fieldBox, breachItems, goalItems, scenarioDeckArgs, exposureArgs, structureMetricsArgs, functionItems, clusterItems, flowItems, optionItems, candidateItems — all verified by grep to have no importer outside their module (tests included, except where noted).
  proposal: add the dead-export lint; delete registerMethod or give the seam a caller; demote the internal-only exports to module-private
  lint: wire knip or ts-prune into preflight.ts with an allowlist for deliberately test-facing exports (NO_ARGS documents its own)
- **[KEEP]** toll and corpus door — deliverable/engine/toll.ts:93
  claim: toll.ts and the trace corpus door are notably well-built: the toll commits its call count only when the call is served (so retries of one refused call stop compounding), exempts machine-forced hops with measured justification, and loadTrace returns a defensive copy under an epoch+stamp two-level cache.
  evidence: deliverable/engine/toll.ts:93-97 ("THE COUNT IS NOT COMMITTED UNTIL THE CALL IS SERVED... the log showed 22, 23, 24, 25 for a single call"), toll.ts:55-80 (measured 25/36 refused polls); deliverable/engine/trace.ts:506-524 (copy-on-return, epoch fast path, stamp fallback). Decisions' idempotent plan/fork/resolve (decisions.ts:613-665) makes every refusal remedy replay-safe.
  proposal: no change; these are the patterns the rest of the engine should copy

---

# v2:b92708809a4816967c8eedd429239b65f4f24b16c348a401eb05f8adc7c11922 — 25 findings

Swept the eight assigned guidance files against deliverable/engine/ source, verifying every named verb, clause, number and behavior; the corpus was being edited concurrently (walking.md changed mid-sweep at 17:54), so every cited quote was re-pinned against the current file. The clause registry, reading-proof maths, stall/toll/brief thresholds, repeated-refusal counter, drain gating, mint guards, bless weighting and git-push refusal all check out exactly — the load-bearing numbers in walking.md and refusals.md are accurate. The defects cluster in three shapes. First, residue of the tree flattening: lane.md still teaches the project/ path prefix for a project/ folder that is now empty (the repair's own HANDOVER note says the root IS the project folder), and a mechanical prefix strip left lane.md:41 saying "a path beginning `spec/` where it should begin `spec/`" — a self-negating sentence that ships in every session's prompt layer. Second, guidance outrun by rulings and code: walking.md still says asking for the battery is refused (SE-C-130/131 were retired), says the narration control has five notches (it is two 0-1440 integers), and says no separate aim verb exists twenty lines above naming se_aim; contract.md rule 11 still warns that an unbriefed subagent finds its native tools blocked, which subagents.md measured to be the wrong failure on 2026-08-20; refusals.md's SE-C-144 section describes kind-frontmatter semantics the view-order code never had, its SE-C-106 section covers one of four call sites, and its preamble still calls the pairing enforcement parked though refusals.test.ts enforces it; retro.md's per-step-cost and drain-legality claims are behind the stamped `where` field and the two-tier drain rule. Third, the same rule in multiple homes: reading-proof mechanics in three places, SE-C-120 chain correction in two, SE-C-140 mint rules in four — for which a cross-file longestSharedRun lint (the engine already owns the primitive in pool.ts) would catch the class forever. Three small CODE findings ride along: SE-C-144's registry comment glued to SE-C-145's line, a wrong `source` string in the prose-wall refusal, and se_file_read's served description still teaching the retired handover file.

- **[E]** lane — guidance/method/lane.md:41
  claim: The path-prefix warning sentence negates itself: both prefixes read `spec/`, so the sentence that exists to teach the one-word mistake now says nothing.
  evidence: lane.md:41-42: "A path beginning `spec/` where it should begin `spec/` resolves, the write succeeds". It plainly once read `project/spec/` on one side; a mechanical prefix strip during the tree flattening produced the tautology. lane.md is assembled into every session's prompt layer (deliverable/engine/promptlayer.ts:14 lists "guidance/method/lane.md").
  proposal: Rewrite or delete the whole DROPPING-THAT-ONE-WORD block; with the flattening (see next finding) the project/ prefix hazard it describes no longer exists.
  lint: A lint flagging a sentence of the form 'X where it should be X' (identical code spans on both sides of 'where it should') would catch this class.
- **[D]** lane — guidance/method/lane.md:38
  claim: The paths block teaches a two-level layout (open project/, prefix every path with project/) that the flattened tree no longer has.
  evidence: lane.md:38-39: "You open `project/`; a path you pass starts `project/`". But project/ is empty (ls -a project → nothing), .se/HANDOVER.md (2026-08-20 15:38) says "ONE LEVEL. The `project/` folder is gone... the root IS the project folder", and the engine's own refusal example is prefix-free: paths.ts:217 "a path inside the project root (root-relative, e.g. spec/x.md)".
  proposal: Rewrite lane.md:38-52 for the one-level layout: paths are root-relative to the repository root; drop the i15 project/-prefix story or mark it history.
- **[E]** contract — guidance/contract.md:243
  claim: Rule 11's closing warning — a subagent without the cage briefing "reaches for its native tools, finds them blocked" — is contradicted by the newer measured finding that subagents are not in the cage at all.
  evidence: contract.md:243-245: "One that does not know about the cage reaches for its native tools, finds them blocked, and reports that it could not read anything." vs subagents.md:31 "The `se` MCP server is NOT in a subagent's tool set" and :37 "Its native Bash and Read were not blocked, and it used them" and :39 "So the warning above prepares for the wrong failure" (measured 2026-08-20). lane.md:185 ("Pass this file's rule to every subagent you spawn") carries the same stale instruction, and subagents.md:15-27 still opens with the outdated 'give it the cage' section above its own correction.
  proposal: Replace the tails of contract.md rule 11 and lane.md:185 with the subagents.md:42-48 instruction (native tools, root-relative paths, lane exists but is unreachable), and fold subagents.md's outdated first section into the corrected one.
- **[D]** walking — guidance/walking.md:335
  claim: The Tests section still says the full battery runs only at verification and that asking for one anywhere else is refused — the regime whose refusal clauses were retired 2026-08-16.
  evidence: walking.md:335-338: "It runs once, at verification... Asking for one anywhere else is refused". vs refusals.md:285-311 (SE-C-130/131 RETIRED; "The engine reads what changed, picks the battery, a named set of files, or nothing at all") and tools-run.ts:341 se_test description: "picks the scope — the whole battery, a named set of test files, or nothing at all... There is no argument that widens or narrows it". No refusal exists for a battery-shaped question; there is nothing to ask for and nothing that refuses.
  proposal: Rewrite the paragraph: the engine picks the scope on every se_test question, the battery included; `force: true` stays the person's flake hunt.
- **[E]** walking — guidance/walking.md:99
  claim: "There is no separate verb and none is missing" stands twenty lines above "THE JUMP IS ONE CALL: `se_aim {to}`" — the separate verb now exists and the same page names it.
  evidence: walking.md:99-100: "There is no separate verb and none is missing." vs walking.md:120: "THE JUMP IS ONE CALL: `se_aim {to}`"; tools.ts:187-212 registers se_aim (setTarget + sweep, go default true). The sentence predates commit 5c7b6136 "se_aim goes by default".
  proposal: Cut or reword the sentence to what it still means: you never invent a target, but se_aim is how you move on one.
- **[D]** refusals — guidance/refusals.md:491
  claim: The SE-C-144 section describes kind-frontmatter field filtering with id/type/statement always legal; the code filters by the .base view's own column order with no always-legal set, and a second thrower uses the clause for unknown node types.
  evidence: refusals.md:496-499: "A KIND FILTERS WHICH FIELDS EXIST. `id`, `type` and `statement` are always legal; everything else comes from that kind's own frontmatter". vs query.ts:22-23 "the view IS the pinned column list; there is no wider frontmatter escape hatch here" and :65 "const legal = new Set(view.order)"; also tools-desk.ts:277 throws QUERY_UNKNOWN_FIELD from se_coverage for an unknown node TYPE.
  proposal: Rewrite the section to the view-order semantics and mention both throwers (se_query unknown field, se_coverage unknown type).
- **[D]** refusals — guidance/refusals.md:231
  claim: The SE-C-106 section covers only web search, but the clause (NOT_CONFIGURED) also fires for git failures in record seeding and for a missing mirror — readers refused there are routed to a section about search backends.
  evidence: refusals.md:231 "### SE-C-106 — no search backend answered" vs errors.ts:64 NOT_CONFIGURED "lane exists but needs owner configuration", thrown at iterations.ts:30 and records.ts:58 ("expected: git <what> to succeed") and tools.ts:91 ("a listening mirror"). errors.ts:20-22 clauseGuidance points every SE-C-106 refusal at this one section.
  proposal: Either split NOT_CONFIGURED into distinct clauses per failure family, or widen the section to name all four call sites.
- **[E]** boot — guidance/method/boot.md:14
  claim: Boot's startup step points to AGENTS.md for how to make the lane callable per host, and AGENTS.md carries no such instruction.
  evidence: boot.md:14: "How depends on the host — AGENTS.md says which way per host." AGENTS.md is assembled solely from contract/walking/lane/voice (promptlayer.ts:7-18); grep of AGENTS.md for toolsearch/schema/deferred/callable returns zero hits.
  proposal: Say the actual way per host in boot.md itself (or in lane.md, which is in the prompt layer), and drop the dead pointer.
  lint: A lint that follows every 'X says/holds Y' cross-reference in guidance and greps the named file for the promised keyword would catch dead pointers as a class.
- **[E]** refusals — guidance/refusals.md:13
  claim: The preamble says mechanical enforcement of the clause-section pairing is parked; the enforcing test exists and the registry's own comment names it.
  evidence: refusals.md:13-15: "The mechanical enforcement of that rule is parked for the engine iterations; until it lands, authorship carries it." vs errors.ts:14 "refusals.test.ts enforces that the section exists" and deliverable/tests/refusals.test.ts:3 "feed-forward section in guidance/refusals.md, and every section there must...".
  proposal: Delete the parked sentence; the pairing is enforced.
- **[D]** retro — guidance/method/retro.md:377
  claim: The mechanical half says draining is legal only in the retro's drain state; the engine and the rest of the corpus say done/obsolete drain anywhere the tool is legal, with only carried/backlog retro-gated.
  evidence: retro.md:377-378: "Draining is legal only in the retro's drain state" vs inbox.ts:130-141 (refusal note: "done and obsolete are checks anyone can run, so they drain wherever the tool is legal"; only JUDGMENT = carried/backlog gated), walking.md:290-293, and retro.md's own step 1 ("Since e22 the front desk drains too").
  proposal: Correct the bullet to the two-tier rule the code enforces.
- **[D]** retro — guidance/method/retro.md:306
  claim: "PER-STEP COST IS NOT COMPUTABLE TODAY" is stale: every call record now carries a stamped `where` position that group_by reaches.
  evidence: retro.md:306-309: "The call log cannot be grouped by state: the state a call was made in rides inside a narration record's arguments, and se_log_query's group_by does not reach it." vs calllog.ts:24-35 (CallRecord.where, "stamped by the code that knows... see dsp-call-log.md#the-walk-position-is-stamped-not-inferred"), tools.ts:923 observer stamps `where: session.active()`, and calllog.ts:341 group_by digs any record field.
  proposal: Update step 9: per-step cost is computable via group_by on the stamped position; keep the caveat only for the pre-stamp log window.
- **[E]** retro — guidance/method/retro.md:146
  claim: The memory-drain step asserts `.se/roots.json` already declares a `sessions` root; no roots.json exists in the tree and nothing in the engine seeds one, so on a fresh clone the step's mandated first move fails.
  evidence: retro.md:146-148: "`.se/roots.json` already declares `sessions` as the harness's projects folder... So `@sessions/<project-slug>/memory/` reads." But `.se/roots.json` is absent (ls: no such file), and paths.ts:89 only reads a caller-declared file — no code writes a sessions default.
  proposal: State it conditionally ("where the owner has declared a sessions root...") and lean on the step's existing declare-a-root branch as the first move when none exists.
- **[D]** walking — guidance/walking.md:274
  claim: The narration cadence is described as "five notches" on the mirror's bar; the control is two free integer inputs, 0-1440 each, with 0 stopping that clock.
  evidence: walking.md:274-275: "Five notches, both clocks running". vs session.ts:629-640 cadence(): "a whole number from 0 to 1440 — 0 stops that clock"; mirror.ts:752 exposes narration.minutes/calls; params.ts:181 renders them as numeric inputs. The only five-notch slider in the engine's history is the retired power control (sessionlive.ts:48).
  proposal: Say what the control is: two numbers, minutes and calls, whichever falls due first; 0 disables a clock.
- **[D]** lane — guidance/method/lane.md:6
  claim: The cage list (seven items ending "web") no longer matches the deny list the same paragraph points at, which holds eleven entries and blocks WebFetch while leaving WebSearch allowed.
  evidence: lane.md:6-14 lists Read/Write/Edit/Bash/Glob/Grep/web. .claude/settings.json deny: Bash, BashOutput, KillShell, Read, Write, Edit, NotebookEdit, Glob, Grep, WebFetch, Skill — with allow mcp__se__*. lane.md's own table (line 35) says WebSearch is allowed, contradicting the bullet's blanket "web".
  proposal: Either drop the inline list (the file is the source) or mirror it exactly; the current half-list invites wrong beliefs about what is blocked.
  lint: cage-inventory.test.ts territory: assert the guidance list equals the settings deny list, or that guidance carries no list.
- **[E]** lane — guidance/method/lane.md:33
  claim: The se_git row's "push stays with the user, EXCEPT on a cloud run" reads as an se_git exception; the lane refuses every push and the cloud push bypasses the lane entirely.
  evidence: lane.md:33 vs gitlane.ts:30-41 (unconditional SE-C-003 refusal of push) and cloud-runner.md:272-273: "SE-C-003 refuses every push without exception, so the push runs through the host's own git rather than through `se_git`". refusals.md:53 agrees: "Every push refuses here, without exception".
  proposal: Reword the row: push is always refused on the lane; a cloud run pushes through host git per cloud-runner.md.
- **[D]** walking — guidance/walking.md:286
  claim: The wall-guard example ("one paragraph of six hundred characters is refused") implies roughly double the real headroom; the guard refuses any break-less text over 300 characters.
  evidence: walking.md:286-287 vs tools-desk.ts:25: "if (text.length <= 300 || text.includes(\"\\n\")) return;" then throws SE-C-125.
  proposal: State the number: over 300 characters without a line break refuses.
- **[R]** guidance-duplication — guidance/method/boot.md:21
  claim: The reading-proof rules live in three homes: walking.md's reading section, boot.md's stability bullets, and the served se_pull description.
  evidence: boot.md:21-24 ("three questions... between `«` and `»`... QUOTE GENEROUSLY... PUNCTUATION IS NOT A WORD") repeats walking.md:186-204 nearly point for point; tools.ts:47 carries a third copy inside se_pull's description. All three were verified correct against readproof.ts today, but three homes drift independently.
  proposal: Make walking.md the single prose home; boot.md keeps one line and a pointer; the served description is the mechanical copy the engine owns.
- **[R]** guidance-duplication — guidance/walking.md:262
  claim: The SE-C-120 chain-correction rules stand near-verbatim in walking.md and in refusals.md, the designated feed-forward home.
  evidence: walking.md:264-269 "An `update` chain becomes the PLAN it wanted to be. A `fork` chain STAYS a fork..." duplicates refusals.md:392-398 "An `update` chain becomes the PLAN it wanted to be... A `fork` chain stays a FORK..." — a shared run well past six words.
  proposal: Keep the behavior in refusals.md § SE-C-120; walking.md keeps only the one-line brief rule and the clause pointer.
- **[R]** guidance-duplication — guidance/refusals.md:506
  claim: The SE-C-140 mint rules (authored statement, six-word run, single distinctive word, tell-the-subagent) are spelled out in four guidance homes.
  evidence: refusals.md:506-549, walking.md:296-303, retro.md:104-121, subagents.md:87-98 all restate the same rule set; pool.ts (COPY_RUN=6, OPAQUE identifier check) is the single enforcing code home.
  proposal: refusals.md keeps the full section; the other three keep one sentence plus the clause pointer.
- **[LINT]** guidance-duplication — guidance/refusals.md:1
  claim: Rule text duplicated across guidance pages is a machine-checkable class — the engine already owns the exact primitive.
  evidence: pool.ts longestSharedRun() detects shared word-runs of six or more between two texts; the three R findings above are runs of 15-40 identical words across guidance files.
  proposal: A prose-inspect check: for every pair of guidance pages, flag shared runs above ~12 words outside quotations; each hit is a rule with two homes.
  lint: Extend deliverable/engine/bin/prose-inspect.ts with a cross-file longestSharedRun sweep over guidance/.
- **[CODE]** engine — deliverable/engine/errors.ts:104
  claim: SE-C-144's descriptive comment is glued onto the end of SE-C-145's line, leaving QUERY_UNKNOWN_FIELD commentless and SEARCH_PATTERN carrying two unrelated comments.
  evidence: errors.ts:99 `QUERY_UNKNOWN_FIELD: "SE-C-144",` (no comment); errors.ts:104 ends "...the escaped literal rides the refusal // a structured query names a field the matched kind does not carry — refused by name...".
  proposal: Move the second half of line 104's comment up to line 99.
- **[CODE]** engine — deliverable/engine/tools-desk.ts:34
  claim: refuseProseWall's rejection names the wrong source file — it says engine/tools.ts but lives in tools-desk.ts.
  evidence: tools-desk.ts:34: `source: "engine/tools.ts prose-wall"` inside a function defined in tools-desk.ts (file header: "Split out of tools.ts").
  proposal: Update the source string; a reader following the refusal to its code lands in the wrong file.
  lint: A test asserting every Rejection's `source` names the file it is thrown from.
- **[CODE]** engine — deliverable/engine/tools-file.ts:127
  claim: se_file_read's served description still teaches "the handover" as the canonical optional:true example after the owner retired the handover file.
  evidence: tools-file.ts:127: "Pass optional: true for a file that is ALLOWED to be missing (the handover)" vs contract.md:174-177: "THERE IS NO HANDOVER FILE, and nobody writes one (owner ruling 2026-08-07)... That file is gone." Served strings teach vocabulary (voice.md's own point about 'record').
  proposal: Swap the example for a live one (e.g. a machine's optional condition note).
- **[KEEP]** walking — guidance/walking.md:186
  claim: The reading section is exactly right against the code — probes at 30/60/92%, four following words, guillemet delimiters with plain-quote fallback, contains-check, case/space ignored, punctuation-not-a-word, missed probes named and banked.
  evidence: readproof.ts:8 AT=[0.3,0.6,0.92], :11 RUN=4, :59-61 delimit(), :84-87 probesMissed contains-check over WORDY tokens; session.ts:2556 bankProbes.
  proposal: None — record that this section was verified closely.
- **[KEEP]** walking — guidance/walking.md:270
  claim: Narration numbers all verified: stall warns at five and refuses at twelve, brief cap 90, toll defaults 5 minutes/20 calls, and the three toll exemptions (reading-only pull, se_test poll, .se/answers cursor) are in the code, the answers-dir state-gate exemption included.
  evidence: decisions.ts:495 NUDGE_AFTER=5, :510 REFUSE_AFTER=12, :348 cutToFit cap 90; toll.ts:6-7 defaults, :55-80 isReadingHop; session.ts:2901 .se/answers state-gate exemption; sessionclaims.ts:982-990 bless refused at autonomy <= gate weight, both rungs named.
  proposal: None — these are the load-bearing numbers and they hold.

---

# v2:dc38c6905fbbf03a50be139249c4c74c390793c7b6055a75940accc3ba2a0031 — 46 findings

The raid register holds 311 entries: 216 open, 42 decided, 19 closed, 10 probed, 7 each superseded/mitigated/deferred, 3 accepted; by kind 87 issues, 84 assumptions, 74 risks, 48 decisions, 16 debts, 2 dependencies. The reported "16 open debts unswept since i5" is wrong on both halves: there are 12 open debts of 16 total, and every debt carries sweep sections through 2026-08-20 — but the 2026-08-20 sweep stamped 11 entries with one identical boilerplate paragraph, contradicting two entries' own prior verdicts (core-and-satellite was already "repaid by cutting"; ten-checks was already "DUE, not waiting"). The real hole is the other 204 open entries: 202 of 220 target files carry no looked date, and reading them against HEAD found roughly twenty that are provably fixed, superseded, or discharged yet still open — including boot repair-tools granted (prepare_idle.md), the write-time YAML guard (files.ts:291), scoped-run timings (testreporters.ts), the actor stamp (calllog.ts:22), writable declared roots with SE-C-143 (paths.ts), the log-query paging test (logquery.test.ts:45), the node-22.18 pin, and the committed cage settings. A second cluster is superseded by later architecture rulings: four entries hinge on the cut core-and-satellite elements, two on the reversed branch-is-the-record design, and four on the i1 convergence whose losing candidate was deleted at i34. Duplication concentrates in two families the entries themselves ask to merge: six entries carrying the person/second-machine check backlog, and four carrying the serve-the-delta-not-the-corpus repair. Three lint-able classes recur: 43 entries with a probed date over unprobed probe text, five falsified assumptions that never changed kind despite the method's own rule, and 13 entries missing minted_in plus one with malformed frontmatter. The strongest verified-live entries worth routing into work are the stale-lane defect (the serving engine ran v5 while the tree said v6, blinding both walks and retros), the push contradiction (SE-C-003 vs cloud-runner), the mirror bar starving six of nine panel values, and the two i36 must-requirements whose modules still have zero production importers.

- **[E]** debt sweep integrity — spec/trace/raid/raid-debt-core-and-satellite-is-off-the-live-path.md
  claim: The 2026-08-20 standalone-retro debt sweep stamped 11 entries with one identical boilerplate paragraph without measuring anything, contradicting two entries' own prior sweeps.
  evidence: grep 'i37 did not touch what this entry' hits 11 files. raid-debt-core-and-satellite-is-off-the-live-path: i9 sweep says 'REPAID BY CUTTING' yet 08-20 adds 'RE-AFFIRMED AS STANDING, trigger unchanged'. raid-debt-ten-checks-wait-on-a-person: i9 sweep says 'DUE, NOT WAITING — the first half of the trigger has fired' yet 08-20 says 'trigger unchanged'. raid-debt-the-bound-surface (status: decided, discharged 2026-08-17) also got 'RE-AFFIRMED AS STANDING'.
  proposal: Strike the boilerplate paragraphs from the three contradicted entries; rule that a sweep entry must state what was checked, not only a date.
  lint: Refuse a sweep section whose text is byte-identical across more than N entries in one commit.
- **[E]** debt register — spec/trace/raid/raid-debt-core-and-satellite-is-off-the-live-path.md
  claim: raid-debt-core-and-satellite-is-off-the-live-path is open although its forbidden state cannot exist: the whole subsystem was cut before i16 shipped.
  evidence: ls deliverable/engine | grep -i 'core|satellite' returns nothing; el-core, el-satellite, el-satellite-supervisor missing from spec/trace/element/; entry's own i9 sweep: 'THE SUBJECT IS GONE... REPAID BY CUTTING, WITH TWO GAPS'.
  proposal: Close it; carry the two named gaps (lost i27 trace at be703899/6396c282, no recorded authorisation for the cut) as one new entry if the owner wants them kept.
- **[KEEP]** debt count check — spec/trace/raid
  claim: The reported '~16 open debts unswept since i5' is half wrong: there are 16 debt entries of which 12 are open, and every one carries sweep sections through 2026-08-20 — the debts are the ONLY swept part of the register.
  evidence: grep kind/status cross-tab: debt = 12 open, 2 closed, 1 decided, 1 mitigated. Sweep sections dated 2026-08-19 (i5 retro, i9 onboard-retro) and 2026-08-20 exist in all. Verdicts: parallel-fan OPEN-real (sessionclaims.ts guard unchanged); checklist-panel OPEN but statement drifted; cloud-validation MITIGATED-correct (repaid on i37 run); core-satellite OPEN-wrong (subject cut); delta-default OPEN-correct (8/15 resolvers fixed, stateform.ts:539); demonstration-reds OPEN-real; file-grain OPEN-parked on i7; harness-fallback OPEN-part-repaid (web.ts fallback landed, finalUrl fix not: web.ts returns request url, not res.url); human-observed OPEN-owner-accepted; i15-gate CLOSED; i16-demos OPEN-real; ten-checks DUE per i9 sweep; bound-surface DECIDED; merge-push CLOSED; two-drawing-mechanisms OPEN-parked on i13; two-must-requirements OPEN-real (stopping-layer.ts and failure-shapes.ts still have zero production importers).
  proposal: Report the corrected numbers to the owner; the real gap is elsewhere — see the no-looked finding.
- **[LINT]** register structure — spec/trace/raid
  claim: Only debts are ever re-read: 202 of the 220 open-or-debt files carry no looked field, and roughly twenty open issues/risks are provably already fixed — the sweep discipline covers one kind of four.
  evidence: grep -L '^looked:' over the 220 targets returns 202. The E findings below are the harvest of reading the unswept 204.
  proposal: Extend the retro sweep (or a cheaper mechanical pass) to open issues, risks and assumptions, not only debts.
  lint: A check that every open register entry carries a looked date newer than N days, or is flagged in the survey.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-boot-grants-no-tools-while-promising-repair.md:9
  claim: raid-iss-boot-grants-no-tools-while-promising-repair is fixed: prepare_idle now grants exactly the repair tools the entry demanded.
  evidence: deliverable/machines/states/prepare_desk.md line 12: 'repair_tools: se_file_read, se_file_search, se_file_glob, se_file_list, se_file_patch, se_file_write, se_run' beside the guidance sentence the entry quoted as contradicted.
  proposal: Close it, citing prepare_idle.md.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-a-write-can-leave-the-corpus-unparseable.md
  claim: raid-iss-a-write-can-leave-the-corpus-unparseable is fixed: the write path now refuses frontmatter that does not parse, exactly the colon-in-scalar case that spawned the entry.
  evidence: deliverable/engine/files.ts:291-308 — 'A ": " inside an unquoted frontmatter scalar starts a nested mapping' → refusal 'frontmatter that parses as YAML — nothing was written'. The entry's trigger said 'revisit when the first bound check ships'; it shipped.
  proposal: Close it.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-a-bound-record-records-no-test-timings.md
  claim: raid-iss-a-bound-record-records-no-test-timings is fixed: both test paths now attach the timing reporter, the reporter is told where to write, and a run that recorded nothing says so in its verdict — the entry's exact closure condition.
  evidence: deliverable/engine/testreporters.ts:1-11 ('The battery attached it and the scoped path did not... Both paths now build their argv here', 'THE REPORTER IS TOLD WHERE TO WRITE', SE_TIMINGS_DIR) and timedSince 'Counted rather than trusted: the verdict reports it'; tools-run.ts:421 spreads timingReport into the scoped verdict.
  proposal: Close it; its sibling assumption battery-timings-measure-work becomes probeable (see D finding).
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-the-call-log-query-omits-matching-records-and-says-it-did-not.md
  claim: raid-iss-the-call-log-query-omits-matching-records is fixed by its own bar: a test now pins that total plus older account for every record.
  evidence: deliverable/tests/logquery.test.ts:45 'assert.equal(p.older + p.records.length + p.offset, p.total, ...)' and :50-55 pin the newest/oldest edges. Entry trigger: 'open until a test pins the paging'.
  proposal: Close it; the 'every number is a floor' caveats in downstream evidence can be lifted where re-measured.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-the-path-jail-has-one-write-target.md
  claim: raid-iss-the-path-jail-has-one-write-target is resolved: writable declared roots with the SE-C-143 fail-closed source guard are built, which is the second write target the entry said could not exist.
  evidence: deliverable/engine/paths.ts:21-25 DeclaredRoot {path, writable} with 'no declaration anybody already wrote becomes writable by upgrading'; errors.ts:96-98 WRITE_TARGET_IS_SOURCE SE-C-143 fails closed. Entry said 'IT STAYS OPEN UNTIL THAT CODE EXISTS'. The code exists.
  proposal: Close it; the machinery-note (facet 3) residue is covered by raid-asm-a-machinery-note-still-has-a-home, which stays open.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-se-lint-has-no-whole-repo-sweep.md
  claim: raid-iss-se-lint-has-no-whole-repo-sweep is superseded: a whole-corpus sweep exists (bin/sweep.ts), runs at every boot, and the owner ruled deliberately that it is not a verb.
  evidence: deliverable/machines/states/prepare_desk.md exit_script includes deliverable/engine/bin/sweep.ts; its body: 'THE SWEEP RUNS HERE BECAUSE THERE IS NO VERB FOR IT (owner ruling 2026-08-16)... 1015 nodes in 388 ms, and six standing breaks'.
  proposal: Close it citing the ruling; the corpus-wide checks it worried about now have a runner.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-a-recorded-act-carries-no-acting-role.md
  claim: raid-iss-a-recorded-act-carries-no-acting-role is fixed: the call log stamps the actor where the call is served.
  evidence: deliverable/engine/calllog.ts:22 'actor?: "human" | "agent" | "ui"' with comment 'The stamp is taken AFTER the handler runs'. The entry was minted in i5 and i5 built exactly this; the channel half was explicitly out of scope.
  proposal: Close it; if the channel half is still wanted, that is a new narrower entry.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-the-lanes-argument-names-disagree-between-its-own-tools.md
  claim: raid-iss-the-lanes-argument-names-disagree is repaired: a sibling verb's word is now understood and the repair announced, which is the alias fix the entry asked for.
  evidence: deliverable/engine/tools.ts:405 'a caller who uses a sibling's word is understood'; guidance/refusals.md SE-C-101 block: 'A SIBLING VERB'S WORD IS UNDERSTOOD, and the repair is announced.'
  proposal: Close it after re-taking its own measurement (SE-C-101/046 share of refusals in a recent window).
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-the-log-serves-a-cut-response-while-the-guidance-promises-otherwise.md
  claim: raid-iss-the-log-serves-a-cut-response is closed by its second closing option: the guidance now says plainly the log is a capped trail and a truncated answer is re-asked smaller.
  evidence: CLAUDE.md lane section: 'WHAT THE LOG ACTUALLY KEEPS, because this used to promise more than it holds... EVERY OTHER RESPONSE IS CAPPED... SO A HOST-TRUNCATED ANSWER IS ASKED FOR AGAIN, SMALLER.' The entry ruled out only 'the promise and the behaviour both standing and disagreeing'.
  proposal: Close it.
- **[E]** open issues, fixed — spec/trace/raid/raid-host-payload-offload-breaks-read-proof.md
  claim: raid-host-payload-offload-breaks-read-proof's stated need — a reliable refetch path for large structured results — was built as the answer bound with spill files and an always-legal cursor.
  evidence: deliverable/engine/bound.ts:19,68,145 (ANSWER_BOUND_BYTES derived from measured host limits, spill to .se/answers, cursor path); CLAUDE.md: reads under .se/answers are exempt from the state gate and toll 'in every state'.
  proposal: Close it; the residual cost is already carried by raid-iss-reading-one-spilled-answer-back-costs-eight-calls, which stays open.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-a-bound-tables-header-reads-as-a-reference.md
  claim: Two i5 self-fixed extractor issues (bound-table header read as reference; refs check reading written cells) record their own fix, name no residue, and still stand open.
  evidence: Both entries carry 'What was done' sections: 'THE EXTRACTOR NOW SKIPS A HEADER' and 'A node-table's references are read from the FIRST cell only'. stateform.ts:581 comment shows the header rule skip.
  proposal: Close both (raid-iss-a-bound-tables-header..., raid-iss-the-refs-check-reads-a-node-tables-written-cells).
- **[E]** open issues, fixed — spec/trace/raid/raid-issue-the-lane-is-not-in-git-so-a-cloud-agent-starts-uncaged.md
  claim: raid-issue-the-lane-is-not-in-git-so-a-cloud-agent-starts-uncaged is superseded: the cage-placing settings are committed and the arrival hook has since placed the cage on real cloud runs with nothing typed.
  evidence: git ls-tree HEAD shows .claude/settings.json committed; raid-debt-cloud-validation's 08-20 section: 'THE ARRIVAL RAN TO COMPLETION... The arrival hook wrote it. Nobody placed the cage by hand' (i37, 2026-08-19 15:28).
  proposal: Close it; the stale _cage/ gitignore comment it flags is a one-line follow-up.
- **[E]** open issues, fixed — spec/trace/raid/raid-iss-the-collapse-hides-the-one-thing-a-newcomer-must-run.md
  claim: raid-iss-the-collapse-hides-the-one-thing-a-newcomer-must-run is resolved by its own first candidate: the launcher and an entry document now sit at the repository top level.
  evidence: git ls-tree --name-only HEAD lists README.md and RUNME.ps1 at root, above the project folder.
  proposal: Close it.
- **[E]** open risks, fixed — spec/trace/raid/raid-risk-an-owed-item-without-a-guard-ships-a-known-defect.md
  claim: raid-risk-an-owed-item-without-a-guard-ships-a-known-defect is retired: both guard halves exist and are driven by tests.
  evidence: raid-asm-an-entry-status-says-whether-it-is-open probe (SETTLED 2026-08-17): form-side guard in engine/stateform.ts, close-side DISPOSED set at engine/records.ts:40, tests/owed-ref.test.ts drives all eight statuses.
  proposal: Close it.
- **[E]** open risks, fixed — spec/trace/raid/raid-risk-geometry-stops-excluding-session-state-and-nothing-replaces-it.md
  claim: raid-risk-geometry-stops-excluding-session-state is retired for the produce path: .se is on the producer's exclusion list with tests asserting nothing arrives.
  evidence: deliverable/engine/produce.ts:38-41 EXCLUDE_DIRS includes '.se'; raid-iss-a-case-guarded-local-artifacts describes the produce case asserting exclusions against a real produced tree.
  proposal: Close it after one look at engine/bin/package.ts for the same exclusion, which the entry names as separate code.
- **[E]** open risks, fixed — spec/trace/raid/raid-sweeping-the-folders-early-hides-every-seeded-iteration.md
  claim: raid-sweeping-the-folders-early-hides-every-seeded-iteration is retired: the open test now reads the record's status, not folder existence.
  evidence: deliverable/engine/iterations.ts:84 'open: !RECORD_FINISHED.has(status)' — the entry cited 'open: existsSync(path)' at line 71 as the mechanism.
  proposal: Close it.
- **[E]** open assumptions, fixed — spec/trace/raid/raid-asm-the-declared-node-floor-matches-what-the-engine-needs.md
  claim: raid-asm-the-declared-node-floor's three-line repayment landed: the engine pin is >=22.18.0 and the floor assertion matches.
  evidence: deliverable/package.json:8 '"node": ">=22.18.0"'; tests/unattended-start.test.ts:88 asserts 'the floor must be a version where that is the default — 22.18 or newer'. The entry's impact still claims 'the pin demands 24'.
  proposal: Close it.
- **[E]** open dependencies, fixed — spec/trace/raid/raid-dep-reading-credit-outlives-se-move.md
  claim: raid-dep-reading-credit-outlives-se-move is discharged exactly as specified: the reading credit is keyed to document content hashes.
  evidence: deliverable/engine/sessionreads.ts:11 imports contentHash; :44-45 credit(path, hash); the entry's discharge test was 'one read of the storage shape'.
  proposal: Close it.
- **[S]** open risks, superseded — spec/trace/raid/raid-no-iterations-are-visible-without-a-reachable-remote.md
  claim: Two i28-era entries guard a design i34 reversed — records are folders on trunk again, so 'reading open from git refs' and 'no folder on disk' are not the system's shape.
  evidence: guidance/refusals.md SE-C-003: 'A record is a folder on trunk now and the claim system is retired'; ls spec/iterations shows all records as folders on trunk; no it/* branches locally.
  proposal: Close raid-no-iterations-are-visible-without-a-reachable-remote and raid-removing-the-folder-makes-the-engine-the-only-door as superseded by i34.
- **[S]** open risks, superseded — spec/trace/raid/raid-risk-git-fake-drifts-from-git.md
  claim: raid-risk-git-fake-drifts-from-git protects a test file and a claim lane that no longer exist.
  evidence: deliverable/tests/ holds no claims.test.ts (only claimops.test.ts); SE-C-003 records the claim system retired at i34; the entry's whole subject is claims.test.ts's 176-invocation git fake.
  proposal: Close as superseded; if the git-seam fake pattern survives elsewhere, mint a narrower entry naming the live file.
- **[S]** cut-subsystem debris — spec/trace/raid/raid-ar-crash-lands-safe.md
  claim: Four architecture entries hinge on elements the core-and-satellite cut removed, and one assumption's trigger (the first satellite) can never fire.
  evidence: el-satellite-supervisor and el-core missing from spec/trace/element/ (checked by ls); raid-ar-crash-lands-safe, raid-ar-every-call-logged hinge on el-satellite-supervisor; raid-ar-mirror-stays-on-the-machine on el-core; raid-asm-machine-wide-state-serves-over-a-local-channel triggers on 'the first satellite'. raid-iss-a-cut-subsystem-left-its-interface-node-behind already lists exactly these as owner cleanup.
  proposal: Close the four; also update the satellite-flavoured impact lines on raid-ar-fresh-machine-runs and raid-ar-newcomer-one-command, which are otherwise live.
- **[S]** i1 convergence relics — spec/trace/raid/raid-flip-living-form-on-resume-needs-no-person.md
  claim: Four i1 entries guard the original architecture convergence whose loser (thin worktrees) was later deleted wholesale, and whose triggers name pipeline stages that ended with i1.
  evidence: raid-flip-living-form-on-resume-needs-no-person, raid-flip-living-form-on-walk-survives-host-swap ('re-run the convergence'), raid-strikes-share-one-argument, raid-unverified-score-comparisons (trigger 'before M5 convergence reads the scores'). i34 removed worktrees entirely (raid-dec-one-tree-beats-a-record-travelling-between-machines).
  proposal: Close all four as superseded history; the convergence they protect cannot be re-run against a deleted candidate.
- **[D]** drifted assumptions — spec/trace/raid/raid-asm-i15-corpus-suits-lexical-matching.md
  claim: The four i15 assumptions' probe fields still say the query/BM25 sibling 'has not been built' although se_query, se_couplings and the BM25 disposition shipped on 2026-08-19.
  evidence: raid-asm-i15-corpus-suits-lexical-matching probe: 'se_file_search of deliverable/engine for bm25 returns no hits' — today grep bm25 hits deliverable/engine/disposition.ts and tools-query.ts exists; raid-debt-i15-gate...'s Closed section names both registered verbs. Same staleness in i15-one-threshold, i15-query-plus-rows, i15-unindexed-scan.
  proposal: Refresh the four probe fields; three of the four probes are now cheap to actually run.
- **[D]** drifted assumptions — spec/trace/raid/raid-asm-every-condition-can-say-what-it-wants.md
  claim: Triggers on three assumptions fired without any re-look: se_why exists (grey-state verb), se_help exists (its sibling), and the pool's first mint happened.
  evidence: deliverable/engine/tools.ts:215 registers se_why; se_help registered (tools.ts, dsp-help-search); raid-asm-every-condition-can-say-what-it-wants triggers on 'the grey-state verb is designed'; raid-asm-grey-verb-distinct-from-se-help on 'i8 lands, or either verb's surface is designed' — i8 shipped; raid-asm-the-pool-is-a-node-kind triggers on 'the first mint' and the pool held 24 tokens on 2026-08-19 (measured in raid-asm-a-node-file-per-option).
  proposal: Run the three cheap probes (enumerate computed guards behind se_why; put the two verb signatures side by side; run the three-pass pool check) and close or convert.
- **[D]** drifted entries — spec/trace/raid/raid-debt-checklist-panel-lacks-owed-state.md
  claim: raid-debt-checklist-panel-lacks-owed-state's statement describes code that no longer exists — the panel has rendered owed as a disabled box with its ref since the i17 merge, and the entry's own sweep says the statement wants rewriting.
  evidence: deliverable/engine/editors/checklist.ts:25-28,34 render owed disabled with muted ref tail and hand the line back unchanged on collect; entry i9 sweep: 'TWO OF THE THREE CLAUSES ARE NOW FALSE... THIS ENTRY'S statement NOW MISDESCRIBES THE CODE'.
  proposal: Rewrite the statement down to the surviving clause: a person cannot SET an owed state from the panel.
- **[D]** drifted risks — spec/trace/raid/raid-thin-evidence-base.md
  claim: raid-thin-evidence-base's trigger — 'when a full iteration lands end to end, measure it and compare' — has fired roughly thirty-seven times and the measurement never happened.
  evidence: Entry minted i1 with that trigger; spec/iterations holds 37+ shipped records; no comparison artifact exists; i37's benchmark harness is the first attempt at the instrument.
  proposal: Re-point the trigger at the i37 benchmark landing its first report, which is now the honest earliest moment.
- **[LINT]** falsified assumptions keep their kind — spec/trace/raid/raid-asm-one-scoring-pass-is-enough-to-eliminate.md
  claim: Five assumptions carry FALSE/FALSIFIED probe results yet still read kind: assumption, against the register's own rule (meth-assumption-probing) that a falsified assumption becomes an issue keeping its id.
  evidence: raid-asm-one-scoring-pass ('FALSIFIED in i16'), raid-asm-the-hosts-pattern-test ('FALSE, AND FOR A REASON...'), raid-asm-the-rigor-matrix-hash ('FALSE, read 2026-08-20', rigor-matrix.ts:448 still hashes rows only), raid-asm-first-time-readers (falsified, self-flagged 'the next register pass can make the change'), raid-asm-the-declared-node-floor ('false at the edge'). Counter-examples show the rule works when applied: raid-asm-the-isolation-rule and raid-corpus-stays-small changed kind properly.
  proposal: Flip the five kinds (ids keep); this overhaul is the register pass one of them explicitly waits for.
  lint: Sweep rule: kind==assumption AND probe matches /\bFALS(E|IFIED)\b/ at line start → flag.
- **[LINT]** probed field semantics — spec/trace/raid/raid-asm-an-omit-is-authored-honestly.md
  claim: 43 entries carry a probed: date while their probe text says unprobed, scheduled, owed or not-yet — the date field is being used as 'last considered', so a reader cannot tell a probed entry from a parked one.
  evidence: Mechanical scan over spec/trace/raid: 43 hits, e.g. raid-asm-an-omit-is-authored-honestly (probed: 2026-08-13, probe: 'unprobed'), raid-asm-documented-harness-limits (probed: 'unprobed 2026-08-19'), raid-asm-battery-timings (probed: 2026-08-15, probe: 'scheduled').
  proposal: Rule the semantics (probed = a probe ran) and add a separate considered/looked field for parked probes.
  lint: Refuse or flag probed: <date> where probe: begins with unprobed/scheduled/owed/not yet.
- **[CODE]** frontmatter integrity — spec/trace/raid/raid-asm-the-stop-hook-fires-the-same-on-posix.md:13
  claim: raid-asm-the-stop-hook-fires-the-same-on-posix.md has malformed frontmatter: the source_refs: key is missing, leaving an orphan list item dangling under how_likely.
  evidence: Lines 13-14 read 'how_likely: plausible' directly followed by '  - raid-lane-works-on-posix' with no source_refs: key — invalid YAML mapping shape that the write-time frontmatter guard (files.ts:291) would refuse today, so it predates or bypassed the guard.
  proposal: Insert the missing 'source_refs:' line; sweep the corpus for the same shape.
  lint: The existing sweep should parse every stored node's frontmatter strictly and flag orphan sequence items.
- **[LINT]** missing provenance — spec/trace/raid/raid-ar-crash-lands-safe.md
  claim: 13 register entries carry no minted_in, all but one from the i1-era evaluate-architecture batch, so no sweep can tell which record owes them.
  evidence: grep -L '^minted_in:' spec/trace/raid/*.md → 13 files (12 raid-ar-*, plus raid-un-reachable-capability-is-traced). Two open debts also lack looked: raid-debt-harness-fallback and raid-debt-two-must-requirements.
  proposal: Backfill minted_in (the ar- batch is i1/i16 evaluate-architecture); add looked to the two i36 debts.
  lint: Conformance check: every raid node carries minted_in; every open debt carries looked.
- **[R]** duplicate backlog — spec/trace/raid/raid-debt-ten-checks-wait-on-a-person-or-a-second-machine.md
  claim: Six open entries carry one underlying backlog — checks needing a person or second machine — and two of them already instruct whoever consolidates to merge them.
  evidence: raid-issue-must-demos-owed (i1), raid-debt-human-observed-demonstrations (i3, sweep: 'WHOEVER RUNS THE CONSOLIDATION SHOULD MERGE THE TWO'), raid-debt-ten-checks-wait-on-a-person (i33, itemised list), raid-issue-package-live-check-owed (i8, 'same underlying limit'), raid-iss-the-vehicle-demonstration (i16), raid-debt-i16-ships-with-its-demonstrations-unperformed (i16).
  proposal: Consolidate onto raid-debt-ten-checks (the itemised, routable one) and close the rest pointing at it; ten-checks is DUE by its own i9 sweep since the work-token pool can now carry tasks.
- **[R]** duplicate repair — spec/trace/raid/raid-debt-delta-default-views.md
  claim: Four open entries describe one repair — per-record forms served the whole corpus instead of the delta — and each names at least one of the others.
  evidence: raid-debt-delta-default-views (partial fix landed, stateform.ts:539 ':all' opt-in), raid-iss-a-gate-form-asks-the-standing-set (i5 sweep in the debt: 'the two describe one repair and it should be built once'), raid-debt-demonstration-reds-are-re-asked, raid-iss-whole-product-claims-reverified (i27).
  proposal: Keep the debt as the carrier (it holds the closure bar and the fix history); close or fold the other three into it.
- **[R]** duplicate finding — spec/trace/raid/raid-un-surface-answers-in-one-second.md
  claim: raid-un-surface-answers-in-one-second (i16) and raid-iss-nothing-answers-the-surface-bound-and-nobody-enumerated-an-option (i9) record the same gap — no element owns the surface one-second bound — from two evaluate-architecture runs.
  evidence: Both cite req-surface-answers-in-one-second and 'the only card ruled unaddressed'; tests/latency.test.ts named by tsp-engine-lifecycle is absent from the tree (verified).
  proposal: Merge into one entry carrying both measurement sets; the i16 one is richer.
- **[T]** record-scoped entries outlive their record — spec/trace/raid/raid-risk-the-bundle-s-item-list-is-stale-and-the-day-shrinks.md
  claim: Five risks are scoped to a single record's own walk, their deciding state ran when that record shipped, and nothing wrote the outcome back — they can never be acted on now.
  evidence: raid-risk-the-bundle-s-item-list-is-stale (closes at i5's frame-delta; i5 shipped), raid-risk-the-small-fixes-crowd-out (i6 gate-implementation), raid-risk-splitting-the-heaviest-test-file (i5 decompose-structure), raid-risk-every-gate-in-this-record-is-blessed-by-its-own-author (i5), raid-note-i15-narrow-grammar-timing (trigger 'before M5 treats the elimination as settled' — i15 shipped).
  proposal: Close all five; the general lesson (self-blessed gates) is already carried by the method.
  lint: Lint: an open register entry whose trigger names a state of a shipped record.
- **[LINT]** decision status vocabulary — spec/trace/raid/raid-dec-autonomy-tier-ladder.md
  claim: Three decisions carry status: open against 40 carrying status: decided, and the register's own status ruling makes decided the settled value for a ruling — an open decision reads as carrying an unresolved claim.
  evidence: Cross-tab: decision decided=40, open=3, superseded=5. The three: raid-dec-autonomy-tier-ladder (trigger 'superseded only'), raid-dec-an-import-is-read-only (its one owed act, removing req-engine-folder-is-sealed, is done — the row is absent from spec/trace/requirement/), raid-dec-serve-the-overlay-and-report-the-drift.
  proposal: Move the three to decided.
  lint: kind==decision AND status==open → flag.
- **[KEEP]** verified live, high value — spec/trace/raid/raid-iss-the-running-lane-is-not-the-code-the-walk-is-editing.md
  claim: raid-iss-the-running-lane-is-not-the-code-the-walk-is-editing is the sharpest live entry: the serving lane ran version 5.0.0 while the tree said 6.0.0, and four shipped improvements never executed.
  evidence: Entry's measurements (se_version 5.0.0 on all 16,157 window records; zero where-stamps; 25 stall refusals carrying the replaced wording after the fix merged); same mechanism class as the two-must-requirements debt, which I verified: stopping-layer.ts and failure-shapes.ts still have no production importer (grep over deliverable, only tests and one comment).
  proposal: Prioritise its cheapest fix (the lane reports its own age on the banner); it also unblocks i37's cost-per-state deliverable.
- **[KEEP]** verified live — spec/trace/raid/raid-iss-a-cloud-run-must-push-and-the-lane-refuses-it.md
  claim: Five open entries I re-verified against HEAD are still true and correctly open.
  evidence: (1) cloud-run-must-push: refusals.md:52-53 'Every push refuses here, without exception' vs cloud-runner.md:249 'You must push before you finish (owner ruling 2026-08-19)'. (2) every-host-hands-in-every-value: render.ts:1006 panelValues still builds 3 of 9 values. (3) kickoff-gate-demands-a-live-scan: M0_90_gate-kickoff.md:12-22 grants no web verb. (4) two-entrypoints-place-the-cage: se-arrive.ts and se-start.ts import only node builtins, no shared cage module. (5) typecheck-unreachable: no tsc in selftest/preflight/battery; corroborated by i37's 16 shell typechecks (raid-iss-half-the-shell-calls).
  proposal: These five plus raid-iss-a-verification-finding-that-is-not-a-test-failure and raid-iss-the-call-log-names-every-vehicle are the open items most worth routing into iterations — each is a live contradiction or a silent-failure class, several waiting only on an owner ruling.
- **[D]** stale sweep advice inside open entry — spec/trace/raid/raid-harness-half-life.md
  claim: raid-harness-half-life's 2026-08-13 sweep names .se/HANDOVER.md as the durable home, and the handover file was later abolished by owner ruling — an open entry now teaches a dead mechanism.
  evidence: Entry: 'Fix: durable home is .se/HANDOVER.md'; CLAUDE.md rule 8: 'THERE IS NO HANDOVER FILE, and nobody writes one (owner ruling)... That file is gone.'
  proposal: Add a dated correction to the sweep section; the entry itself (a recurring retro question) stays open.
- **[D]** read-proof accessibility — spec/trace/raid/raid-the-read-proof-locks-weaker-models-out-of-the-system.md
  claim: raid-the-read-proof-locks-weaker-models-out locates the defect in the engine's feedback, and that feedback was substantially rebuilt since (guillemet anchors, contains-semantics, per-probe misses named, banked rights) with no re-look recorded.
  evidence: Entry: 'IN THE ENGINE'S FEEDBACK, not in the model... a refusal that says the proof was wrong without saying exactly what is counted'. CLAUDE.md reading section documents the rebuilt mechanics: 'A WRONG ANSWER NAMES EXACTLY WHICH PROBES MISSED, and the ones you got right are BANKED', anchors between «», contains-check, punctuation rule.
  proposal: Re-probe on the next weaker-model session; the note-4671c830fca7 question (does a tool-delivered document need proving at all) is still the deeper open thread.
- **[D]** windows longpaths — spec/trace/raid/raid-issue-windows-longpaths.md
  claim: raid-issue-windows-longpaths' remaining trigger (a peer's deep-root seed through the worktree path that carried the id twice) references a seeding mechanism i34 replaced with folders on trunk.
  evidence: Entry: 'the record path carries the long iteration id twice' — worktree-era; i34 removed worktrees; the landed half (preflight sets core.longpaths) is still in place per the entry's own sweep.
  proposal: Re-verify the path arithmetic under the one-tree layout; likely closeable.
- **[D]** sweep repair options reference cut architecture — spec/trace/raid/raid-iss-the-shell-writes-method-with-no-path-to-judge.md
  claim: raid-iss-the-shell-writes-method-with-no-path-to-judge recommends as its first-choice fix a satellite rooted in a thin tree — both satellite and thin trees were later removed, so the entry's live repair menu is one option shorter than it reads.
  evidence: Entry: 'THE SECOND IS THE ARCHITECTURE'S OWN ANSWER... It is also untested: no satellite exists' — and no satellite can exist now (subsystem cut, worktrees removed at i34).
  proposal: Strike the satellite option in a dated note; the entry itself (SE-C-134 cannot be retired on routing alone) stays valid.
- **[KEEP]** partially repaid, worth noting — spec/trace/raid/raid-risk-the-rewrite-carries-the-private-sentence-across.md
  claim: raid-risk-the-rewrite-carries-the-private-sentence's first mitigation shipped: the mint refuses a statement sharing a run of the note's own words.
  evidence: deliverable/engine/pool.ts:46-58 verbatim-run guard ('a separator substituted for a space cannot disguise a run of the note's own words'); SE-C-140 in CLAUDE.md notes section. The residual (a paraphrased leak) keeps the entry honestly open.
  proposal: Add a dated partial-repayment note so the next reader does not re-derive it.